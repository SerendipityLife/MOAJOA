---
phase: 23-web-first-foundation
plan: 04
type: execute
wave: 2
depends_on: ["23-01", "23-02", "23-03"]
files_modified:
  - packages/api/src/types/database.ts
autonomous: true
requirements: [MOA-01]
must_haves:
  truths:
    - "supabase db reset이 0024·0025 포함 클린 통과한다 (42P17 recursion 0) — 성공 기준 1"
    - "동시 40건 삽입에 순번이 무중복·무결번이고 hard delete 후에도 재부여되지 않는다 — MOA-01"
    - "익명 세션(is_anonymous)이 발급되고 join_moa가 share_mode 분기로 editor/voter를 부여한다 — 성공 기준 3"
    - "kakao authorize가 kauth.kakao.com으로 redirect된다 — 성공 기준 4 로컬 절반"
    - "재생성된 타입 위에서 core/api 기존 테스트·typecheck가 그린이다"
  artifacts:
    - path: packages/api/src/types/database.ts
      provides: "trip_messages·seq_no·share_mode·companion·join_moa 반영된 생성 타입"
      contains: "trip_messages"
  key_links:
    - from: "supabase db reset"
      to: packages/api/src/types/database.ts
      via: "pnpm supabase:types (적용 → 재생성 순서 강제)"
      pattern: "trip_messages"
    - from: supabase/tests/place_seq_concurrency.sh
      to: "assign_place_seq 트리거"
      via: "동시 INSERT 실검증"
      pattern: "PASS"
---

<objective>
**[BLOCKING] 스키마 적용 게이트.** Wave 1 산출물(0024·0025·config.toml)을 로컬 스택에 실제 반영하고 — 스택 재시작(config 로딩) → `supabase db reset`(42P17=0) → `pnpm supabase:types` — 그 위에서 MOA-01 동시성 하네스와 익명·join_moa·kakao smoke를 실행해 성공 기준 1·2·3·4(로컬)를 실증한다.

Purpose: 타입은 생성 파일에서 오므로 적용 없이도 빌드는 통과한다 — 적용 없는 검증은 false-positive다. 이 플랜이 통과해야 Wave 3+(core/api 계약)가 진짜 스키마 위에서 작업한다.
Output: 재생성된 `packages/api/src/types/database.ts` + 하네스·smoke PASS 로그.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-web-first-foundation/23-RESEARCH.md
@.planning/phases/23-web-first-foundation/23-VALIDATION.md

환경 전제: supabase local은 colima 필요 — `docker info` 실패 시 `colima start` 후 `pnpm supabase:start` (RESEARCH Pitfall 9, 프로젝트 메모리). config.toml 변경(익명·kakao)은 **DB reset이 아니라 스택 재시작**으로 로딩된다 — GoTrue 컨테이너 env는 `supabase start` 시점에 굳는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] 스택 재시작 + db reset + 타입 재생성 + 기존 스위트 그린</name>
  <files>packages/api/src/types/database.ts</files>
  <read_first>
    - supabase/migrations/0024_place_seq.sql (23-01 산출물 — 적용 대상 확인)
    - supabase/migrations/0025_web_share.sql (23-02 산출물 — 적용 대상 확인)
    - supabase/config.toml (23-03 산출물 — enable_anonymous_sign_ins=true·kakao 블록 확인)
  </read_first>
  <action>
순서 그대로 실행한다:

1. **가동 확인:** `docker info` (실패 시 `colima start` 후 재시도) → `supabase status` (미가동이면 `pnpm supabase:start`).
2. **config 반영 재시작:** `pnpm supabase:stop && pnpm supabase:start`. `env()` 치환 대상 KAKAO 변수가 없어 start가 실패/경고하면 더미값으로 재시도: `KAKAO_REST_API_KEY=dummy-local KAKAO_CLIENT_SECRET=dummy-local pnpm supabase:start` (로컬은 authorize redirect 생성까지만 검증 — 실값 불필요).
3. **[BLOCKING] 스키마 적용:** `pnpm supabase:reset` — 0016~0025 전체 클린 재적용. 출력에 `0024_place_seq`·`0025_web_share` 적용 라인 존재 + 에러 0 + `42P17` 문자열 0.
4. **타입 재생성:** `pnpm supabase:types` — `packages/api/src/types/database.ts` 재생성. diff에 `trip_messages` 테이블, places `seq_no`, trips `share_mode`·`companion`·`last_place_seq`, Functions에 `join_moa`가 나타나야 한다. **손편집 금지** (생성물).
5. **무회귀 게이트:** `pnpm --filter @moajoa/core test && pnpm --filter @moajoa/api test && pnpm --filter @moajoa/core typecheck && pnpm --filter @moajoa/api typecheck` 전부 exit 0 (신규 컬럼은 additive — 기존 코드 무영향이어야 정상. 깨지면 스키마가 기존 계약을 침범한 것 — 마이그레이션을 고치되 0016~0023은 무수정).
  </action>
  <acceptance_criteria>
    - `pnpm supabase:reset` exit 0, 출력에 `42P17` 0건
    - `grep -c "trip_messages" packages/api/src/types/database.ts` ≥ 1, `grep -c "seq_no" ...` ≥ 1, `grep -c "share_mode" ...` ≥ 1, `grep -c "join_moa" ...` ≥ 1
    - core test + api test + 양쪽 typecheck 전부 exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm supabase:reset && pnpm supabase:types && grep -q "trip_messages" packages/api/src/types/database.ts && grep -q "join_moa" packages/api/src/types/database.ts && pnpm --filter @moajoa/core test && pnpm --filter @moajoa/api test</automated>
  </verify>
  <done>0024·0025가 로컬에 실적용되고(성공 기준 1 전반부) 재생성 타입 위에서 기존 스위트 무회귀.</done>
</task>

<task type="auto">
  <name>Task 2: MOA-01 동시성 하네스 실행</name>
  <files></files>
  <read_first>
    - supabase/tests/place_seq_concurrency.sh (23-01 산출물 — 실행 대상)
  </read_first>
  <action>
`bash supabase/tests/place_seq_concurrency.sh` 실행. 기대 출력: `PASS: place_seq concurrency (40|40|40, hard-delete→41, soft-restore 3|t, forge→42)` + exit 0.

실패 시 디버깅 순서: (a) `40|40|40` 실패 → 트리거 미부착 또는 advisory lock 키 불일치 — `select tgname from pg_trigger where tgrelid='places'::regclass` 확인; (b) `41` 실패 → 카운터가 아닌 max+1로 구현됐다는 뜻 — 0024 재검토 (MOA-01 위반, 절대 우회 금지); (c) forge `42` 실패 → 트리거가 NEW.seq_no를 덮어쓰지 않음. 0024를 고치면 반드시 `pnpm supabase:reset` 후 재실행.
  </action>
  <acceptance_criteria>
    - `bash supabase/tests/place_seq_concurrency.sh` exit 0
    - 출력에 `PASS: place_seq concurrency` 포함 (40|40|40 · 41 · 3|t · 42 네 단언 전부 통과했다는 뜻)
  </acceptance_criteria>
  <verify>
    <automated>bash supabase/tests/place_seq_concurrency.sh</automated>
  </verify>
  <done>MOA-01 실증 완료 — 동시 무중복·무결번 + 삭제·복원 순번 유지 + hard-delete 무재사용 + forge 차단 (성공 기준 2).</done>
</task>

<task type="auto">
  <name>Task 3: 익명 세션 + join_moa + kakao authorize smoke 실행</name>
  <files></files>
  <read_first>
    - supabase/tests/web_share_smoke.sh (23-02 산출물 — 실행 대상)
  </read_first>
  <action>
`bash supabase/tests/web_share_smoke.sh` 실행. 기대 출력: `PASS: anon(is_anonymous=true, role=authenticated) + join_moa(both→editor, dates→voter) + kakao authorize` + exit 0.

실패 시 디버깅 순서: (a) 익명 signup 4xx → Task 1의 재시작이 config를 못 실었다는 뜻 — `supabase stop && supabase start` 재수행 (reset만으론 GoTrue env 미반영); (b) join_moa 404 → 0025 미적용 또는 grant 누락; (c) role 불일치 → join_moa의 case 분기 (D-A1) 재검토; (d) kakao redirect 실패 → config.toml kakao 블록 `enabled = true` 확인 + env 더미값으로 start 했는지 확인. 반복 실행 시 익명 rate limit 30/hr/IP 유의 — 같은 세션 재사용 또는 잠시 대기.
  </action>
  <acceptance_criteria>
    - `bash supabase/tests/web_share_smoke.sh` exit 0
    - 출력에 `PASS: anon` 포함 (is_anonymous=true·role=authenticated·editor/voter 분기·kauth.kakao.com redirect 전부 통과)
  </acceptance_criteria>
  <verify>
    <automated>bash supabase/tests/web_share_smoke.sh</automated>
  </verify>
  <done>성공 기준 3 완결 + 성공 기준 4 로컬 절반 완결 (프로덕션 대시보드는 23-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 검증 없는 적용 주장 → 후속 wave | 적용 없이 타입만 재생성하면 false-positive — 이 플랜 전체가 그 방어 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-13 | Repudiation | "적용됨" false-positive | mitigate | reset 실행이 [BLOCKING] — 타입 재생성·하네스·smoke 전부 라이브 DB 대상. 성공 기준 1을 명령 exit code로 실증 |
| T-23-09 | DoS | RLS 42P17 재귀 | mitigate | `supabase db reset` 클린(42P17=0)이 이 플랜의 acceptance — 0025 정책이 헬퍼-only임을 런타임 확인 |
| T-23-01 | Tampering | seq_no forge | mitigate | 하네스 (4) forge 케이스 실행으로 실증 |
</threat_model>

<verification>
- reset 클린(42P17=0) + 타입 재생성 diff에 신규 스키마 반영
- 하네스 PASS + smoke PASS (exit 0)
- core/api 기존 스위트 무회귀
</verification>

<success_criteria>
- ROADMAP 성공 기준 1(전반부: reset 클린+타입+테스트 그린)·2(MOA-01)·3(익명+join_moa)·4(로컬 절반) 실증
- 원격(`supabase db push`)은 이 phase 범위 외 — 23-07에서 `supabase migration list`로 상태만 확인 (Open Q1 결정)
</success_criteria>

<output>
완료 후 `.planning/phases/23-web-first-foundation/23-04-SUMMARY.md` 생성. 하네스·smoke의 PASS 출력 라인을 SUMMARY에 그대로 기록할 것 (verify-work 근거).
</output>
