---
phase: 29-chat-unification
plan: 05
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CHAT-08]
files_modified:
  - apps/web/app/moa/[id]/page.tsx
  - apps/web/app/t/[slug]/_components/guest-surface.tsx

must_haves:
  truths:
    - "호스트가 /moa 본화면에서 poll이 존재하면 후보 날짜·집계·투표자 현황을 볼 수 있다"
    - "poll이 없는 트립은 현황 섹션이 렌더되지 않는다 (빈 트립 무회귀)"
    - "poll 조회 실패가 무음으로 사라지지 않고 콘솔에 기록된다 (both-mode 재발 진단)"
  artifacts:
    - path: apps/web/app/moa/[id]/page.tsx
      provides: "호스트 owner-RLS poll 조회 + pollSlot(PollVoteIsland 현황 뷰) MoaIsland 전달"
      contains: "getPollByTrip"
    - path: apps/web/app/t/[slug]/_components/guest-surface.tsx
      provides: "getPublicTripPoll 실패 진단 로깅 (무음 catch 해소)"
      contains: "console.error"
  key_links:
    - from: "apps/web/app/moa/[id]/page.tsx"
      to: "getPollByTrip / getPollOptions"
      via: "owner RLS direct-read (am_trip_owner)"
      pattern: "getPollByTrip\\(supabase"
    - from: "apps/web/app/moa/[id]/page.tsx"
      to: "MoaIsland pollSlot (moa-island.tsx:587 {pollSlot})"
      via: "PollVoteIsland 현황 뷰 ReactNode 전달"
      pattern: "pollSlot="
---

<objective>
UAT Gap 3 종결 — 호스트가 /moa 본화면(/moa/[id])에서 날짜 투표 현황(후보 날짜·집계·투표자 목록)을 볼 수 있게 한다.

현재 상태(진단, .planning/debug/host-t-no-poll-status.md): 호스트 본 표면 /moa/[id]는 poll 현황 UI가 어느 phase에도 존재한 적 없다. page.tsx가 pollSlot을 전달하지 않고(:77-91 "호스트 /moa는 미전달=diff 0"), MoaIsland 자체엔 poll 조회 코드가 0, ShareSheet는 후보 세팅 step만 있고 집계·투표자 표시가 없다. 호스트가 투표 현황을 찾으러 가는 본 화면이 비어 있다.

Locked design decision(사용자, 2026-07-14): 호스트 /moa 본화면에 날짜 투표 현황 섹션을 추가한다. `getPollByTrip`(owner RLS 통과)로 poll을 조회 → poll이 존재할 때만 pollSlot으로 후보날짜·집계·투표자 현황 뷰를 host MoaIsland에 전달. **게스트 pollSlot idiom을 그대로 재사용**(신규 컴포넌트 0 — 현황 뷰는 기존 PollVoteIsland). Scope는 /moa 본화면만.

핵심 관찰: MoaIsland는 이미 `pollSlot?: ReactNode` seam을 갖고(moa-island.tsx:68) `{pollSlot}`을 PlaceSheet 안 PlanSection 직전에 렌더한다(:587). 게스트 both 분기(guest-surface.tsx:370-377)가 이 seam을 쓰는 검증된 idiom이다. 따라서 **MoaIsland는 한 줄도 바꾸지 않는다** — 변경은 page.tsx가 seam을 채우는 것뿐(최대 surgical, §3.3).

Purpose: 호스트 투표 현황 가시성(CHAT-08) — dates/both 공유에서 호스트가 누가 언제 가능한지 본 화면에서 바로 확인.
Output: page.tsx가 poll 조회→pollSlot 배선, guest-surface 무음 catch 진단 로깅.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-chat-unification/29-HUMAN-UAT.md
@.planning/debug/host-t-no-poll-status.md
@.planning/phases/29-chat-unification/29-UI-SPEC.md
@.planning/phases/29-chat-unification/29-PATTERNS.md

<interfaces>
<!-- 실행자가 코드베이스 탐색 없이 바로 쓸 계약. 전부 코드에서 추출. -->

@moajoa/api — packages/api/src/queries/date-polls.ts (배럴 export * 확인됨):
```typescript
// owner-readable poll 메타데이터만 (no voter PII, T-19-07). date_polls 직접 read.
export function getPollByTrip(
  client: MoajoaSupabaseClient, tripId: string,
): Promise<{ id: string; poll_code: string | null; mode: 'range' | 'grid'; status: 'open' | 'closed' } | null>;

// 후보 날짜 windows. date_poll_options 직접 read (owner RLS date_poll_options_write/read).
// share-sheet(25-07)가 이미 호스트 owner 컨텍스트에서 호출 — owner 읽기 검증됨.
export interface PollOption { id: string; start_date: string; end_date: string }
export function getPollOptions(client: MoajoaSupabaseClient, pollId: string): Promise<PollOption[]>;
```

PollVoteIsland — apps/web/app/poll/[code]/_components/poll-vote-island.tsx (현황 뷰 컴포넌트, 'use client'):
```typescript
// 후보 날짜(투표 UI) + 라이브 집계(집계 bar + 투표자 nickname 칩) + 최다 배지 렌더.
// tally는 getPollTally(code)로 클라이언트 하이드레이트(RSC seed 불필요).
// nickname prop 주입 시 inline 닉네임 게이트 스킵 → 즉시 현황 노출.
interface Props {
  code: string;            // poll.poll_code
  tripId: string;
  mode: 'range' | 'grid';
  status: 'open' | 'closed';
  options: { id: string; start_date: string; end_date: string }[];
  nickname?: string;       // 주입 시 게이트 스킵
  onRequireMember?: ...;   // 호스트는 미전달 (이미 멤버)
}
export function PollVoteIsland(props: Props): JSX.Element;
```

MoaIsland — apps/web/app/moa/[id]/_components/moa-island.tsx (변경 금지):
```typescript
export interface MoaIslandProps {
  // ... 기존 필드
  currentUserNickname: string;
  hideHostControls?: boolean;   // 호스트 /moa는 미전달 → PlanSection도 렌더됨
  hidePlaceAdd?: boolean;
  pollSlot?: ReactNode;         // :587에서 {pollSlot} — PlaceSheet 안 PlanSection 직전
}
```

게스트 both 분기 pollSlot idiom (재사용 대상, guest-surface.tsx:370-377):
```tsx
pollSlot={
  pollMeta != null ? (
    <section>
      <h3 className="text-sm font-semibold text-neutral-700">날짜 정하기</h3>
      {pollSection /* = <PollVoteIsland .../> */}
    </section>
  ) : undefined
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 호스트 /moa page.tsx가 owner-RLS poll을 조회해 pollSlot(현황 뷰)을 MoaIsland에 전달</name>
  <read_first>
    - apps/web/app/moa/[id]/page.tsx (수정 대상 — RSC seed 패턴 :57-91, pollSlot 미전달이 결함 지점)
    - apps/web/app/t/[slug]/_components/guest-surface.tsx:279-300, 360-377 (analog — pollSection 구성 + both 분기 pollSlot idiom)
    - apps/web/app/poll/[code]/_components/poll-vote-island.tsx:40-101 (Props 계약, nickname 게이트 스킵 :343)
    - packages/api/src/queries/date-polls.ts:123-146, 209-220 (getPollByTrip / getPollOptions 시그니처)
    - apps/web/app/moa/[id]/_components/moa-island.tsx:576-620 (pollSlot 렌더 위치 :587 — 변경 금지 확인)
  </read_first>
  <action>
    **apps/web/app/moa/[id]/page.tsx만 수정한다. MoaIsland·PollVoteIsland·date-polls.ts는 한 줄도 바꾸지 않는다.**

    1. import 라인에 추가:
       - `@moajoa/api` from-import에 `getPollByTrip, getPollOptions` 추가 (기존 멀티라인 import 블록 :3-13에 알파벳/기존 순서 맞춰 삽입).
       - 파일 상단 import에 `import { PollVoteIsland } from '@/app/poll/[code]/_components/poll-vote-island';` 추가 (guest-surface.tsx:23과 동일 경로). MoaIsland import(:15) 아래에 둔다.

    2. `profileNames` 조회(:74) 직후, `return` 전에 poll 조회 삽입 (owner RLS direct-read — 호스트는 owner라 getPollByTrip이 통과):
       ```tsx
       // Gap 3 (CHAT-08) — 호스트 본 화면 날짜 투표 현황. getPollByTrip은 owner RLS
       // (am_trip_owner)로 poll 메타를 읽는다(no voter PII, T-19-07). poll이 있을 때만
       // pollSlot을 채운다 — 없으면 seam은 undefined로 남아 기존 렌더와 동일(무회귀).
       const poll = await getPollByTrip(supabase, id);
       const pollOptions = poll ? await getPollOptions(supabase, poll.id) : [];
       const pollSlot =
         poll?.poll_code ? (
           <section>
             <h3 className="text-sm font-semibold text-neutral-700">날짜 투표 현황</h3>
             <PollVoteIsland
               code={poll.poll_code}
               tripId={id}
               mode={poll.mode}
               status={poll.status}
               options={pollOptions}
               nickname={profileNames[user.id] ?? '나'}
             />
           </section>
         ) : undefined;
       ```
       - `poll?.poll_code` 가드 필수 — poll_code는 `string | null`이고 PollVoteIsland.code는 `string` required. (ensure_poll_code 트리거가 항상 발급하므로 실무상 null 아님이나 타입 안전.)
       - `nickname={profileNames[user.id] ?? '나'}` 주입 → PollVoteIsland inline 닉네임 게이트 스킵(:343 `!nickname && !onRequireMember`) → 호스트가 즉시 현황을 본다. `onRequireMember`는 미전달(호스트는 이미 owner 멤버).
       - 헤딩은 게스트("날짜 정하기")와 구분해 **"날짜 투표 현황"** (호스트는 세팅이 아니라 현황을 본다 — 결정 문구).

    3. `<MoaIsland ... />` JSX(:78-91)에 `pollSlot={pollSlot}` prop 추가 (기존 prop 나열 끝, `currentUserNickname` 다음 줄). 다른 prop·라인은 diff 0.

    **Pitfall 8 (PATTERNS:88) 준수:** 호스트 /moa는 MoaIsland를 단 1회 마운트하고 pollSlot도 1회만 전달 → PollVoteIsland의 `poll:{tripId}` 채널 구독이 1개뿐이다. sibling 중복 렌더 없음(게스트 both의 2채널 배달 탈취 위험 부재). 절대 pollSection을 MoaIsland 밖에 또 렌더하지 말 것.

    **Karpathy §3.3 surgical:** `hideHostControls`는 미전달 유지 → 호스트는 pollSlot(현황) + PlanSection([일정])을 둘 다 본다(:593 `!hideHostControls`). PlaceSheet·MoaIsland·ShareSheet 무접촉. 워크스페이스 import에 `.js` 확장자 금지(§4.5). iOS 무접촉(§5).
  </action>
  <verify>
    <automated>grep -q "getPollByTrip(supabase" "apps/web/app/moa/[id]/page.tsx" && grep -q "getPollOptions" "apps/web/app/moa/[id]/page.tsx" && grep -q "PollVoteIsland" "apps/web/app/moa/[id]/page.tsx" && grep -q "pollSlot={pollSlot}" "apps/web/app/moa/[id]/page.tsx" && grep -q "날짜 투표 현황" "apps/web/app/moa/[id]/page.tsx"</automated>
  </verify>
  <done>
    - `apps/web/app/moa/[id]/page.tsx`가 getPollByTrip·getPollOptions·PollVoteIsland import + pollSlot 구성 + `pollSlot={pollSlot}` 전달을 포함.
    - `CI=true pnpm --filter web typecheck` exit 0 (poll_code null 가드·mode/status/options 타입 정합).
    - `CI=true pnpm --filter web build` PASS (`ƒ /moa/[id]` 라우트 빌드 성공).
    - `CI=true pnpm --filter web test` exit 0 (기존 스위트 무회귀 — moa-island 15·guest-surface 등).
    - `grep -rn "from '@moajoa/api/.*\.js'\|from '@/.*\.js'" apps/web/app/moa/[id]/page.tsx` 0건.
    - `git diff --stat apps/ios` 0 (iOS 무접촉).
    - `git diff apps/web/app/moa/[id]/_components/moa-island.tsx` 빈 diff (MoaIsland 무변경).
  </done>
</task>

<task type="auto">
  <name>Task 2: guest-surface 무음 catch에 진단 로깅 1줄 (both-mode poll 미표시 재발 진단)</name>
  <read_first>
    - apps/web/app/t/[slug]/_components/guest-surface.tsx:126-143 (getPublicTripPoll effect + 무음 catch :135-137)
    - apps/web/app/t/[slug]/_components/guest-surface.tsx:255-256 (기존 console.error(err) idiom — 동일 패턴 재사용)
  </read_first>
  <action>
    **결정(사용자, 2026-07-14): both-mode 무음 catch는 이번 gap 주 범위 밖이나, 재발 진단을 위해 console.error 1줄 추가는 surgical하게 포함 허용.** Gap 3 진단(host-t-no-poll-status.md Evidence:50-51/71)이 "런타임 1회 확인 필요"로 남긴 유일한 무음 실패 지점을 관측 가능하게 만든다.

    guest-surface.tsx:132-137의 poll 조회 try/catch를 다음으로 변경:
    ```tsx
    try {
      const raw = (await getPublicTripPoll(client, slug)) as PollMeta | null;
      if (active && raw) setPollMeta(raw);
    } catch (err) {
      // poll 미노출 시 조용히 스킵 — dates 표면만 비게 됨. 단, both-mode에서 poll이
      // 흔적 없이 사라지는 재발(Gap 3)을 진단할 수 있게 err는 남긴다.
      console.error('[guest-surface] getPublicTripPoll failed', err);
    }
    ```
    - `catch {` → `catch (err) {`로만 시그니처 확장 + console.error 1줄 추가. setPollMeta 미호출(무음 스킵 동작) 유지 — 표면 동작은 무변경, 로깅만 추가.
    - 다른 라인·다른 effect·분기 로직 무접촉(§3.3). 이 파일의 다른 무음 catch(:157-159 카운트)는 건드리지 않는다 — Gap 3 진단 대상이 아니다.
  </action>
  <verify>
    <automated>grep -q "console.error('\[guest-surface\] getPublicTripPoll failed'" "apps/web/app/t/[slug]/_components/guest-surface.tsx" && grep -q "catch (err) {" "apps/web/app/t/[slug]/_components/guest-surface.tsx"</automated>
  </verify>
  <done>
    - guest-surface.tsx poll catch가 `catch (err)`로 err를 캡처하고 `console.error('[guest-surface] getPublicTripPoll failed', err)`를 호출.
    - setPollMeta 스킵 동작 무변경(무음 스킵 유지 — 로깅만 추가).
    - `CI=true pnpm --filter web test` exit 0 (guest-surface 스위트 무회귀 — mock resolve 경로라 catch 미발화).
    - :157-159 카운트 catch는 빈 diff (범위 밖 무접촉).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (authenticated owner) → Supabase RLS | 호스트가 자신의 트립 poll/options/tally를 읽는다. anon 키 + 쿠키 세션, 서비스 롤 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Information Disclosure | page.tsx getPollByTrip / getPollOptions (owner direct-read) | accept | `am_trip_owner` RLS가 이미 owner에게 poll 메타·후보 날짜 전체 read를 부여(자기 트립). voter PII 미노출(T-19-07 — getPollByTrip은 poll_code/mode/status만). 신규 노출 표면 0 |
| T-29-05-02 | Information Disclosure | PollVoteIsland getPollTally 클라이언트 하이드레이트(투표자 nickname 칩) | accept | 게스트가 이미 쓰는 동일 poll_vote_tally RPC. 호스트(owner)는 누가 가능한지 볼 정당한 권한 — 현황 가시성이 CHAT-08의 목적. device_token 미노출(nickname만) |
| T-29-05-03 | Tampering | 호스트의 PollVoteIsland 재사용 → castDateVote(device_token 경로)로 호스트 투표 가능 | accept | 호스트는 인증된 멤버(owner). device_token 투표는 레거시 /poll 동작 그대로 — 권한 상승·집계 위조 없음. 현황 뷰의 부수 효과이며 scope(가시성) 밖 |
| T-29-05-04 | Information Disclosure | guest-surface console.error(err) 추가 | accept | err는 클라이언트 콘솔에만(브라우저). Supabase 에러 객체에 시크릿 미포함(anon 키 컨텍스트). 서버 로그·타 사용자 미도달 |
</threat_model>

<verification>
- `CI=true pnpm --filter web typecheck` exit 0
- `CI=true pnpm --filter web build` PASS (`ƒ /moa/[id]`)
- `CI=true pnpm --filter web test` exit 0 (전 스위트 무회귀)
- `git diff apps/web/app/moa/[id]/_components/moa-island.tsx` 빈 diff (MoaIsland 무변경 — pollSlot seam 재사용)
- `git diff --stat apps/ios packages/core supabase/migrations` 0 (iOS·core·migrations 무접촉)
- `.js` 워크스페이스 import 0건
</verification>

<success_criteria>
- 호스트가 /moa/[id] 접속 시 poll이 존재하면 "날짜 투표 현황" 섹션(후보 날짜 + 집계 bar + 투표자 nickname 칩)을 PlaceSheet 상단에서 본다 (Gap 3 truth: "호스트가 /moa 본화면에서 날짜 투표 현황을 볼 수 있다").
- poll이 없는 트립은 현황 섹션이 렌더되지 않고 기존 화면과 동일하다 (무회귀).
- MoaIsland·PollVoteIsland·date-polls.ts는 무변경 — 변경은 page.tsx pollSlot 배선 + guest-surface 진단 로깅뿐 (최대 surgical).
- both-mode에서 getPublicTripPoll 실패가 콘솔에 기록된다 (재발 진단 관측성).
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-unification/29-05-SUMMARY.md`
</output>
