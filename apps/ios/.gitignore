# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli
expo-env.d.ts
# @end expo-cli

# Native build output — regenerable via `pnpm --filter @moajoa/ios prebuild`.
# We use the Expo managed workflow as the source of truth (app.config.ts).
# Anchor with leading slash so the pattern only matches the prebuild directory
# at apps/ios/ios/, not any nested "ios" directory inside e.g. modules/.../ios/.
/ios/
/android/

# Expo
.expo/
.expo-shared/

# Build artifacts
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/

# Metro
.metro-health-check*
