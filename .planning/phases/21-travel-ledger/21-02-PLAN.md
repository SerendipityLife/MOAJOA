---
phase: 21-travel-ledger
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - packages/core/src/schemas/ledger.ts
  - packages/core/src/schemas/ledger.test.ts
  - packages/core/src/schemas/index.ts
  - packages/core/src/constants.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [LEDGER-02, LEDGER-03, LEDGER-06]
must_haves:
  truths:
    - "LedgerEntrySchema가 5요소(amount_foreign/currency/fx_rate/fx_source/fx_as_of) + trip_id nullable + status enum을 검증한다 (0022 컬럼 정합)"
    - "LedgerParseOutputSchema가 LLM 결제정보 추출 출력을 검증한다 (플랫폼/카드4/통화/금액/결제일/krw_amount?/fx_rate?/matched_trip_id?/confidence)"
    - "deriveAmountKrw(amount_foreign, fx_rate)가 fx_rate null이면 null, 아니면 원화 파생값을 반환한다 (KRW=파생, 원천 아님)"
  artifacts:
    - "packages/core/src/schemas/ledger.ts"
    - "packages/core/src/schemas/ledger.test.ts"
  key_links:
    - "LedgerStatus/FxSource enum ↔ 0022 CHECK 제약 값 완전 일치 (문자 단위)"
    - "LedgerParseOutputSchema ↔ parse-email pipeline/claude.ts(21-04)가 로컬 재선언할 정본"
---

<objective>
`@moajoa/core`에 ledger 도메인 계약 락 — 모든 다운스트림(EF·api·iOS)이 import. 가계부 행 스키마(5요소 원자 저장 + nullable trip_id) + LLM 파싱 출력 스키마 + KRW 파생 순수함수를 checklist.ts/date-poll.ts idiom으로 신설한다 (LEDGER-02/03/06 계약 측면).

Purpose: EF·api·iOS가 병렬로 빌드할 단일 정의 seam. 환율 5요소·상태 enum·파싱 출력이 여기서 락되지 않으면 세 소비자가 drift한다. KRW 파생 로직을 core 순수함수로 두어 원천(원통화)과 표시값(KRW)을 구조적으로 분리(Pitfall 4).
Output: ledger.ts 스키마 + 파싱출력 스키마 + 순수함수, 전 테스트 green, core typecheck 0.
</objective>

<execution_context>
GSD execute-plan 부재 — 수동 실행. TDD 권장(테스트 동시 신설). 워크스페이스 import `.js` 금지(§4.5). 외부 입력 Zod(§4.5).
</execution_context>

<context>
@.planning/phases/21-travel-ledger/21-RESEARCH.md
@.planning/phases/21-travel-ledger/21-PATTERNS.md
@packages/core/src/checklist.ts
@packages/core/src/schemas/date-poll.ts
</context>

## Artifacts this plan produces
- `packages/core/src/schemas/ledger.ts` — const-enum(LedgerStatus, FxSource) + LedgerEntrySchema + LedgerParseOutputSchema + deriveAmountKrw/needsReview 순수함수
- `packages/core/src/schemas/ledger.test.ts` — 스키마 파싱 + 5요소 + KRW 파생 + 파싱출력 케이스
- barrel(schemas/index.ts + src/index.ts) + constants.ts(필요 시 append)

<tasks>

<task type="auto">
  <name>Task 1: ledger.ts 스키마 + 순수함수 (TDD)</name>
  <files>packages/core/src/schemas/ledger.ts, packages/core/src/schemas/ledger.test.ts</files>
  <read_first>
    - packages/core/src/checklist.ts (const-enum 3쌍 + 스키마 .default() + 순수 파생 idiom)
    - packages/core/src/schemas/date-poll.ts (Request 스키마 + optional/.default() + Limits 재사용)
    - packages/core/src/schemas/date-poll.test.ts (테스트 규약 — .parse().toEqual(), .toThrow())
    - .planning/phases/21-travel-ledger/21-PATTERNS.md §ledger.ts 섹션
    - supabase/migrations/0022_ledger.sql (CHECK 제약 값 — enum 문자 일치 대조)
  </read_first>
  <action>
    `packages/core/src/schemas/ledger.ts` 신설:

    1. **const-enum 2쌍** (checklist.ts idiom, 0022 CHECK와 문자 일치 주석):
       - `export const LedgerStatus = ['pending','processing','ready','needs_review','failed'] as const; export type LedgerStatusType = (typeof LedgerStatus)[number];`
       - `export const FxSource = ['email','frankfurter','unavailable'] as const; export type FxSourceType = ...;`
    2. **LedgerEntrySchema** (0022 컬럼 정합):
       ```
       z.object({
         id: z.string().uuid(),
         owner_user_id: z.string().uuid(),
         trip_id: z.string().uuid().nullable(),
         status: z.enum(LedgerStatus),
         platform: z.string().nullable(),
         merchant: z.string().nullable(),
         card_last4: z.string().regex(/^\d{4}$/).nullable(),
         amount_foreign: z.number().nullable(),
         currency: z.string().length(3).nullable(),
         fx_rate: z.number().nullable(),
         fx_source: z.enum(FxSource).nullable(),
         fx_as_of: z.string().nullable(),   // ISO date
         amount_krw: z.number().nullable(),
         paid_at: z.string().nullable(),
         created_at: z.string(),
         updated_at: z.string(),
       })
       export type LedgerEntry = z.infer<typeof LedgerEntrySchema>;
       ```
    3. **LedgerParseOutputSchema** (LLM 계약 — parse-email이 로컬 재선언할 정본):
       ```
       z.object({
         platform: z.string().nullable(),
         card_last4: z.string().regex(/^\d{4}$/).nullable(),
         merchant: z.string().nullable(),
         amount_foreign: z.number().nullable(),
         currency: z.string().length(3).nullable(),
         paid_at: z.string().nullable(),        // ISO date/datetime as read from mail
         krw_amount: z.number().nullable(),     // 메일 명시 원화 청구액 (있으면 fx_source='email')
         fx_rate: z.number().nullable(),        // 메일 명시 청구환율
         matched_trip_id: z.string().uuid().nullable(),  // 사용자 trip 교집합만 (EF 방어)
         confidence: z.enum(['high','low']),
       })
       export type LedgerParseOutput = z.infer<...>;
       ```
    4. **순수함수:**
       - `export function deriveAmountKrw(amount_foreign: number | null, fx_rate: number | null): number | null` — 둘 중 null이면 null, 아니면 `Math.round(amount_foreign * fx_rate)`. (KRW=파생, 원천 아님 — Pitfall 4.)
       - `export function needsReview(status: LedgerStatusType): boolean` — `status === 'needs_review'`.
    5. barrel: schemas/index.ts + src/index.ts에 `export * from './ledger'` 추가.

    ledger.test.ts: date-poll.test.ts 구조 — (a) LedgerEntrySchema.parse fixture .toEqual, trip_id null 허용·bad status/fx_source reject; (b) LedgerParseOutputSchema.parse 정상 + confidence enum + card_last4 regex reject; (c) deriveAmountKrw(1000, 9.12)===9120, (null, 9.12)===null, (1000, null)===null; (d) needsReview.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/core test -- ledger</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LedgerEntrySchema\|LedgerParseOutputSchema" packages/core/src/schemas/ledger.ts` >= 2
    - ledger.test.ts 전 케이스 green (파싱·5요소·KRW 파생·파싱출력)
    - `pnpm --filter @moajoa/core typecheck` exit 0
    - `grep -c "export \* from './ledger'" packages/core/src/schemas/index.ts` == 1
    - core 전체 suite 무회귀 (기존 green 유지)
  </acceptance_criteria>
  <done>ledger 스키마·파싱출력·순수함수가 checklist/date-poll idiom과 일치, 전 테스트 green</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| T-21-07 | Tampering | LLM 파싱출력이 스키마 밖 필드/타입 → LedgerParseOutputSchema.parse 강제(EF서), matched_trip_id는 uuid 형식만(교집합 방어는 EF) |
| T-21-08 | (correctness) | KRW를 원천 저장 시 fx 변경에 부정합 → deriveAmountKrw 순수함수로 파생 강제, 원천은 5요소 |
</threat_model>

<verification>
- ledger.ts enum 값이 0022 CHECK와 문자 단위 일치(status 5·fx_source 3)
- core 전체 무회귀
</verification>

<success_criteria>
- ledger 계약 락 — EF(21-04)·api(21-03)·iOS(21-05)가 동일 정의 import
- KRW 파생 분리로 환율 부정합 구조적 방어
</success_criteria>

<output>
Create `.planning/phases/21-travel-ledger/21-02-SUMMARY.md` when done
</output>
