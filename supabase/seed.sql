-- Seed data for local dev. Run automatically after `supabase db reset`.
-- Useful for quick smoke testing without going through the full auth flow.

-- NOTE: in local Supabase, you can create test users via the Studio at
-- http://localhost:54323. This seed only inserts data that doesn't require
-- a real auth.users row — leave empty for now and seed via UI / Edge Function tests.
