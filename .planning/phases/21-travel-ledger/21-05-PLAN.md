---
phase: 21-travel-ledger
plan: 05
type: execute
wave: 4
depends_on: ["21-02", "21-03", "21-04"]
files_modified:
  - apps/ios/app/trip/[id]/(tabs)/ledger.tsx
  - apps/ios/components/ledger/ledger-row.tsx
  - apps/ios/components/ledger/ledger-entry-sheet.tsx
  - apps/ios/app/me.tsx
  - apps/ios/lib/forwarding-address.ts
  - apps/ios/app.config.ts
  - apps/ios/__tests__/ledger.test.tsx
  - apps/ios/.env.local.example
autonomous: false
requirements: [LEDGER-01, LEDGER-03, LEDGER-06]
user_setup:
  - service: expo/eas env
    why: "전달 주소 도메인은 실값 env 배선(하드코딩 금지 §4.7)"
    env_vars:
      - name: EXPO_PUBLIC_FORWARDING_DOMAIN
        source: "21-04 Task 5에서 확정된 도메인(예: ledger.moajoa.app). apps/ios/.env.local"
must_haves:
  truths:
    - "ledger.tsx가 book.tsx 상태머신을 미러해 로딩/에러/빈온보딩/리스트를 렌더하고, 미분류·needs_review 항목을 상단 섹션으로 강조한다 (LEDGER-06)"
    - "미분류/needs_review 항목 1탭 → LedgerEntrySheet로 trip 배정 또는 결제정보 수정 → status='ready' (LEDGER-06)"
    - "LedgerRow가 원통화 금액 + KRW 파생 + 환율 출처 배지(email=실청구/frankfurter=추정/unavailable=확인안됨)를 표시한다 (LEDGER-03 가시화)"
    - "me.tsx에 전달 주소 카드가 있고 getOrCreateForwardingAddress로 발급된 {token}@{도메인}을 복사할 수 있다 (LEDGER-01)"
  artifacts:
    - "apps/ios/app/trip/[id]/(tabs)/ledger.tsx (스텁 교체)"
    - "apps/ios/components/ledger/{ledger-row,ledger-entry-sheet}.tsx"
    - "apps/ios/lib/forwarding-address.ts"
    - "apps/ios/__tests__/ledger.test.tsx"
  key_links:
    - "ledger.tsx → @moajoa/api ledger/forwarding 쿼리(21-03) + @moajoa/core LedgerEntry/deriveAmountKrw(21-02)"
    - "전달 주소 도메인 ← app.config.ts extra.forwardingDomain(EXPO_PUBLIC_FORWARDING_DOMAIN)"
    - "LedgerRow 환율 출처 배지 ↔ 21-UI-SPEC 3색 규칙"
---

<objective>
가계부 iOS 표면 완성 — ledger.tsx(스텁→홈, book.tsx 상태머신 미러) + LedgerRow + LedgerEntrySheet(미분류/needs_review 1탭 처리) + me.tsx 전달 주소 카드 + 복사 lib. 파이프라인이 채운 가계부를 열람하고 애매 항목을 1탭 확인·수정한다 (LEDGER-01/03/06 UI 측면).

Purpose: 파이프라인(21-04)이 자동 입력한 데이터의 유일한 사용자 표면. 미분류 인박스+needs_review를 단일 확인 흐름으로(LEDGER-06 체감), 환율 출처 3색으로 신뢰 가시화(LEDGER-03 differentiator), 전달 주소 1탭 복사로 시작 마찰 최소(LEDGER-01).
Output: ledger 3파일 + me 수정 + forwarding lib + app.config + 테스트. jest green, typecheck 0. 디바이스 UAT는 human-verify.
</objective>

<execution_context>
GSD execute-plan 부재 — 수동. NativeWind only, 인라인 style 최소(§UI-SPEC). 실값 env 배선. jest `--watchman=false`. 21-UI-SPEC 카피·색 계약 준수.
</execution_context>

<context>
@.planning/phases/21-travel-ledger/21-UI-SPEC.md
@.planning/phases/21-travel-ledger/21-PATTERNS.md
@apps/ios/app/trip/[id]/(tabs)/book.tsx
@apps/ios/components/booking/checklist-row.tsx
@apps/ios/app/me.tsx
@apps/ios/lib/share-board.ts
</context>

## Artifacts this plan produces
- `apps/ios/app/trip/[id]/(tabs)/ledger.tsx` — 스텁 재작성(상태머신 + 미분류/needs_review 섹션 + 확정 목록)
- `apps/ios/components/ledger/ledger-row.tsx` — 결제 행(금액+KRW+환율 출처 배지)
- `apps/ios/components/ledger/ledger-entry-sheet.tsx` — 1탭 trip 배정 / 결제정보 수정 시트
- `apps/ios/app/me.tsx` — 전달 주소 카드 삽입(제휴 안내 인접)
- `apps/ios/lib/forwarding-address.ts` — 주소 조립 + 복사 래퍼
- `apps/ios/app.config.ts` — extra.forwardingDomain + `.env.local.example`
- `apps/ios/__tests__/ledger.test.tsx` — RNTL 상태별 단언

<tasks>

<task type="auto">
  <name>Task 1: LedgerRow + LedgerEntrySheet 컴포넌트</name>
  <files>apps/ios/components/ledger/ledger-row.tsx, apps/ios/components/ledger/ledger-entry-sheet.tsx</files>
  <read_first>
    - apps/ios/components/booking/checklist-row.tsx (ROW_SHADOW 카드 + KIND_ICON 맵 + 3-state 컨트롤)
    - apps/ios/app/trip/[id]/(tabs)/book.tsx (BottomSheet 게이팅 idiom)
    - apps/ios/components/me/profile-sheets (필드 입력 시트 idiom)
    - .planning/phases/21-travel-ledger/21-UI-SPEC.md §Component 0/1
  </read_first>
  <action>
    **ledger-row.tsx** (checklist-row 미러): ROW_SHADOW `bg-white rounded-2xl mb-2.5 px-3 py-3 flex-row items-center`. 좌 플랫폼 칩(`w-11 h-11 rounded-xl bg-neutral-100` + PLATFORM_ICON 맵[airplane/bed/ticket/card, 기본 receipt-outline] `#4B5563`). 중앙 flex-1: merchant `text-sm font-semibold text-neutral-900` + 캡션 `text-xs text-neutral-400`(`{platform} · ••••{card_last4} · {MM.DD}`). 우: 금액 `{amount_foreign} {currency}` `text-base font-semibold`; 아래 `≈ ₩{amount_krw}` + **환율 출처 배지**(21-UI-SPEC 3색: email→`bg-[success/10]` "실청구" / frankfurter→`text-neutral-400` "추정 환율" / unavailable→warning "환율 확인 안 됨"). status가 needs_review/미분류(trip_id null)면 우상단 배지(`bg-brand-50 text-brand-600 text-[10px]`) + `onPress` 콜백(행 자체는 URL 지식 없음 — onPress만).

    **ledger-entry-sheet.tsx** (book BottomSheet 미러): `@gorhom/bottom-sheet` `snapPoints ['55%']` `enablePanDownToClose`, `{open && ...}` 게이팅. props `{ open, mode: 'assign'|'review', entry, trips, onAssign, onReview, onClose }`.
    - assign 모드: 제목 "어느 여행의 예약인가요?" + trips.map(Pressable → onAssign(tripId)) + "여행 없이 두기".
    - review 모드: 제목 "결제 정보를 확인해주세요" + 금액(numeric)/통화(3자)/결제일 입력 + **확인**(`bg-brand-500`) → onReview(patch).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- ledger-row --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - ledger-row.tsx에 환율 출처 3색 분기 존재(`grep -cE "실청구|추정 환율|확인 안" ledger-row.tsx` >= 3)
    - ledger-row는 onPress 콜백만(URL/배정 로직 없음 — 상위 소유)
    - ledger-entry-sheet가 assign/review 2모드 렌더
    - typecheck 0
  </acceptance_criteria>
  <done>LedgerRow(환율 가시화)·LedgerEntrySheet(1탭 2모드)가 UI-SPEC 계약대로</done>
</task>

<task type="auto">
  <name>Task 2: ledger.tsx 홈 (상태머신 + 미분류/needs_review 흐름)</name>
  <files>apps/ios/app/trip/[id]/(tabs)/ledger.tsx</files>
  <read_first>
    - apps/ios/app/trip/[id]/(tabs)/book.tsx (load Promise.all + AppState quiet refetch + 상태머신 early-return + 낙관적 롤백 + showToast)
    - Task 1 컴포넌트
    - packages/api/src/queries/ledger.ts + forwarding.ts (21-03)
    - packages/core/src/schemas/ledger.ts (LedgerEntry, deriveAmountKrw, needsReview)
    - .planning/phases/21-travel-ledger/21-UI-SPEC.md §Screen 1/3
  </read_first>
  <action>
    17-04 스텁 재작성. book.tsx 상태머신 미러:
    - **⚠️ trip id 훅 = `useGlobalSearchParams<{ id: string }>()`** (NOT useLocalSearchParams). 탭 화면은 탭바 탭으로 진입하면 `useLocalSearchParams`가 부모 `[id]` 세그먼트를 못 넘겨받아 `id=undefined` → `if(!id) return`이 `setLoaded(true)` 전에 반환 → 무한 스피너(F-20-1, book.tsx/map.tsx가 이 버그로 수정됨 commit cd056cb). ledger.test.tsx mock에도 `useGlobalSearchParams` 포함.
    - import: @moajoa/api(listLedger/listUnassignedLedger/listNeedsReview/assignTripToEntry/updateLedgerEntry/deleteLedgerEntry) + @moajoa/core(LedgerEntry 타입·needsReview) + supabase + Task 1 컴포넌트.
    - `load` = `Promise.all([getTrip, listLedger(trip), listUnassignedLedger, listNeedsReview(trip)])`. AppState quiet refetch(파이프라인 백그라운드 반영). inFlight 가드. `if(!id) return` 시에도 안전하게 `setLoaded(true)` 보장(F-20-1 방어).
    - 상태머신 early-return: `!loaded`→ActivityIndicator(#2979FF); `error`→"가계부를 불러오지 못했어요…" + 다시 시도; ledger+미분류+needs_review 전부 0→**빈 온보딩**("아직 정리된 예약이 없어요" + body + "내 주소 보기" text link→me); else 리스트.
    - 리스트: (a) **상단 확인 섹션**(미분류 ∪ needs_review 있으면) — "어느 여행인지 확인해주세요" 라벨 + LedgerRow(배지 표시, onPress→시트) (b) **확정 항목**(status ready·trip 배정, paid_at desc) + 총액 캡션.
    - LedgerEntrySheet: 미분류→assign 모드(assignTripToEntry, 성공 토스트 "'{trip}' 가계부에 추가했어요"), needs_review→review 모드(updateLedgerEntry status='ready'). 낙관적 반영 + 롤백 + showToast(book idiom).
    - 삭제: Alert.alert("이 항목을 삭제할까요?", …destructive) → deleteLedgerEntry.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- ledger --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - ledger.tsx "곧 제공돼요" 스텁 문자열 0 (`grep -c "곧 제공" ledger.tsx` == 0)
    - 상태머신 4분기 존재(loaded/error/빈온보딩/리스트)
    - `grep -cE "어느 여행인지 확인|아직 정리된 예약" ledger.tsx` >= 2 (UI-SPEC 카피)
    - typecheck 0
  </acceptance_criteria>
  <done>ledger.tsx가 book 상태머신 미러 + 미분류/needs_review 확인 흐름 완성</done>
</task>

<task type="auto">
  <name>Task 3: me.tsx 전달 주소 카드 + forwarding-address lib + app.config</name>
  <files>apps/ios/app/me.tsx, apps/ios/lib/forwarding-address.ts, apps/ios/app.config.ts, apps/ios/.env.local.example</files>
  <read_first>
    - apps/ios/app/me.tsx (제휴 안내 카드 삽입 위치 — 20-07 선례)
    - apps/ios/lib/share-board.ts (얇은 래퍼 idiom + Constants.expoConfig.extra)
    - apps/ios/app.config.ts (extra 필드 EXPO_PUBLIC_* 배선)
    - packages/api/src/queries/forwarding.ts (getOrCreateForwardingAddress)
  </read_first>
  <action>
    - **app.config.ts:** extra에 `forwardingDomain: process.env.EXPO_PUBLIC_FORWARDING_DOMAIN`(기본값 없음 — 미배선=undefined). `.env.local.example`에 `EXPO_PUBLIC_FORWARDING_DOMAIN=ledger.moajoa.app` placeholder 추가.
    - **lib/forwarding-address.ts:** `buildForwardingAddress(token): string` = `${token}@${Constants.expoConfig?.extra?.forwardingDomain}`. `copyForwardingAddress(addr): Promise<void>` — **expo-clipboard 신규 설치**(`pnpm --filter @moajoa/ios add expo-clipboard`) `Clipboard.setStringAsync(addr)`; 실패 시 throw(호출부서 토스트). (Share.share 대안 가능하나 복사 UX가 자연스러움 — RESEARCH Open Q, expo-clipboard 채택.)
    - **me.tsx:** 제휴 안내 카드 인접(위)에 **내 예약 메일 주소** 카드(`bg-white rounded-3xl p-5 mb-4` `cardShadow`): 최초 마운트 시 `getOrCreateForwardingAddress(supabase)` → `buildForwardingAddress(token)` 표시(selectable Text) + "주소 복사" 버튼(`bg-brand-50` `text-brand-600`) → copyForwardingAddress + showToast("주소를 복사했어요"). 로딩 중 placeholder.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "forwardingDomain" apps/ios/app.config.ts` >= 1 (하드코딩 도메인 0 — env)
    - `grep -c "EXPO_PUBLIC_FORWARDING_DOMAIN" apps/ios/.env.local.example` >= 1
    - `grep -c "내 예약 메일 주소\|주소 복사" apps/ios/app/me.tsx` >= 1
    - `grep -c "getOrCreateForwardingAddress" apps/ios/app/me.tsx` >= 1
    - typecheck 0
  </acceptance_criteria>
  <done>전달 주소 발급·표시·복사 완성, 도메인 env 배선</done>
</task>

<task type="auto">
  <name>Task 4: ledger.test.tsx (RNTL 상태별)</name>
  <files>apps/ios/__tests__/ledger.test.tsx</files>
  <read_first>
    - apps/ios/__tests__/book.test.tsx (RNTL 하네스: @moajoa/api·@/lib/supabase·expo-router·@expo/vector-icons·@gorhom/bottom-sheet 스코프 mock, waitFor)
  </read_first>
  <action>
    book.test.tsx 하네스 복사. 스코프 mock(@moajoa/api ledger 쿼리·@/lib/supabase·expo-router·icons·bottom-sheet 스텁). 케이스:
    - 빈 온보딩: 전 리스트 empty → "아직 정리된 예약이 없어요" 렌더
    - 미분류 인박스: listUnassignedLedger 1건 → "어느 여행인지 확인해주세요" + 행 렌더
    - needs_review 1탭: 행 press → 시트 오픈(review 모드)
    - 낙관적 assign 롤백: assignTripToEntry reject → 원상복구 + 에러 토스트
    reanimated mock은 드래그 없으면 불필요(book 하네스만).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- ledger --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - ledger.test.tsx 전 케이스 green
    - iOS 풀스위트 무회귀 (기존 green 유지)
    - `pnpm --filter @moajoa/ios typecheck` exit 0
  </acceptance_criteria>
  <done>상태별 RNTL 테스트 green, 무회귀</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [UAT] 디바이스 가계부 흐름 + 실메일 파싱</name>
  <files>(디바이스 UAT — 코드 변경 없음)</files>
  <what-built>ledger 탭·전달 주소 카드·미분류/needs_review 시트 완성. 실제 메일 파싱 정확도·환율 표시는 실기기 + 실메일 필요.</what-built>
  <action>사용자 수행(21-04 Task 5 CF 배포 완료 전제):
    1. `pnpm sim`(또는 실기기) → me 탭 전달 주소 복사 → 본인 예약/카드 메일 1건 전달.
    2. ledger 탭 복귀 → 항목 자동 생성 확인(파이프라인). 환율 출처 배지(실청구/추정) 정확성.
    3. 미분류 항목 → 1탭 trip 배정 → 멤버 공유 확인. needs_review → 1탭 수정 → ready.
    4. 외화 결제 메일로 5요소(원통화·환율·결제일) 보존 + KRW 파생 확인(LEDGER-03).
    5. 한국 카드사 메일 실포맷으로 claude 프롬프트 정확도 평가(RESEARCH A3 — 오파싱 시 프롬프트 튜닝 후속).
  </action>
  <verify>사용자 sign-off("가계부 UAT 통과") 또는 파싱 이슈 리포트</verify>
  <acceptance_criteria>LEDGER-01/02/03/06 런타임 sign-off</acceptance_criteria>
  <done>디바이스 + 실메일 가계부 흐름 검증</done>
  <resume-signal>"가계부 UAT 통과" 또는 파싱 이슈 상세</resume-signal>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| T-21-18 | Info Disclosure | 전달 주소 노출(화면 캡처 등) → 토큰은 추측불가 base62, 재발급 옵션(후속). 표시만, 유출 시 재발급 |
| T-21-19 | (correctness) | 환율 출처 오표시로 신뢰 오도 → fx_source 3색 규칙 엄격(email만 '실청구') |
| T-21-20 | Elevation | 미분류 행 UI서 타 trip 배정 시도 → RLS(owner write)가 서버 게이트, UI는 본인 행만 노출 |
</threat_model>

<verification>
- ledger.tsx 스텁 문자열 0, 상태머신 4분기
- 환율 출처 3색(email/frankfurter/unavailable)
- 전달 주소 도메인 env(하드코딩 0)
- iOS 무회귀
</verification>

<success_criteria>
- 파이프라인 데이터를 열람·1탭 확인·수정(LEDGER-06)
- 환율 출처 가시화(LEDGER-03 differentiator)
- 전달 주소 1탭 복사(LEDGER-01)
</success_criteria>

<output>
Create `.planning/phases/21-travel-ledger/21-05-SUMMARY.md` when done
</output>
