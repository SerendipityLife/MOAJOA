---
phase: 02-extraction-pipeline-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0004_extraction_hardening.sql
  - packages/core/src/constants.ts
autonomous: true
requirements:
  - EXTRACT-03
  - EXTRACT-04
  - EXTRACT-05

must_haves:
  truths:
    - "places table has source_kind column with NOT NULL DEFAULT 'ai' and CHECK constraint for ('ai','manual')"
    - "places table has inferred_city column (nullable text)"
    - "extraction_costs table exists with all D-08 columns and FK to links(id)"
    - "extraction_costs has RLS enabled with no permissive policies"
    - "Google Places API calls use explicit FieldMask with no wildcard"
    - "Shared constants for channel prefix and cost pricing exist in @moajoa/core"
  artifacts:
    - path: "supabase/migrations/0004_extraction_hardening.sql"
      provides: "Schema changes for extraction hardening"
      contains: "extraction_costs"
    - path: "packages/core/src/constants.ts"
      provides: "Shared domain constants including extraction channel prefix"
      contains: "EXTRACT_CHANNEL_PREFIX"
  key_links:
    - from: "supabase/migrations/0004_extraction_hardening.sql"
      to: "supabase/migrations/0001_init.sql"
      via: "ALTER TABLE places ADD COLUMN (existing table)"
      pattern: "ALTER TABLE places"
    - from: "supabase/migrations/0004_extraction_hardening.sql"
      to: "links table"
      via: "FK reference from extraction_costs.link_id"
      pattern: "REFERENCES links"
---

<objective>
Create the database migration for extraction pipeline hardening and add shared constants used by both Edge Function and client code.

Purpose: The migration adds `source_kind` and `inferred_city` columns to `places` (EXTRACT-03 per D-06/D-07), creates the `extraction_costs` table (EXTRACT-04 per D-08/D-09/D-11), and enables RLS on the new table. Shared constants prevent channel name mismatch (Pitfall 1 from RESEARCH.md) and centralize cost pricing. FieldMask verification (EXTRACT-05 per D-12) is confirmed via grep.

Output: `0004_extraction_hardening.sql` migration file, updated `constants.ts` with extraction-related constants.
</objective>

<execution_context>
@/Users/a15139/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a15139/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extraction-pipeline-hardening/02-CONTEXT.md
@.planning/phases/02-extraction-pipeline-hardening/02-RESEARCH.md

@supabase/migrations/0001_init.sql
@supabase/migrations/0003_boards_owner_default.sql
@packages/core/src/constants.ts
@supabase/functions/extract-youtube/pipeline/places.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0004_extraction_hardening.sql</name>
  <files>supabase/migrations/0004_extraction_hardening.sql</files>
  <read_first>
    supabase/migrations/0001_init.sql
    supabase/migrations/0003_boards_owner_default.sql
    .planning/phases/02-extraction-pipeline-hardening/02-CONTEXT.md
  </read_first>
  <action>
Create new migration file `supabase/migrations/0004_extraction_hardening.sql`. This is append-only (CLAUDE.md section 4.3 -- never modify 0001-0003).

Part 1 -- ALTER `places` table (per D-06, D-07):
- Add column `source_kind` as `text NOT NULL DEFAULT 'ai'` with CHECK constraint `(source_kind IN ('ai', 'manual'))`. Use `ADD COLUMN IF NOT EXISTS` for idempotency.
- Add column `inferred_city` as `text` (nullable, no default). Use `ADD COLUMN IF NOT EXISTS`.
- Do NOT rename existing `source_timestamp_sec` or `source_quote` columns (per D-07 -- already cover EXTRACT-03's `video_offset_sec` and `quote` requirements).

Part 2 -- CREATE `extraction_costs` table (per D-08):
- Columns: `id uuid PRIMARY KEY DEFAULT gen_random_uuid()`, `link_id uuid NOT NULL REFERENCES links(id) ON DELETE CASCADE`, `provider text NOT NULL CHECK (provider IN ('anthropic', 'google_places'))`, `model text`, `input_tokens int`, `output_tokens int`, `cost_usd numeric(10,6)`, `duration_ms int`, `created_at timestamptz NOT NULL DEFAULT now()`.
- Enable RLS: `ALTER TABLE extraction_costs ENABLE ROW LEVEL SECURITY;` with NO permissive policies (per D-11 -- service role only).

Part 3 -- Indexes (Claude's discretion per CONTEXT.md):
- `CREATE INDEX extraction_costs_link_idx ON extraction_costs (link_id);` for per-link cost aggregation.
- `CREATE INDEX extraction_costs_created_idx ON extraction_costs (created_at DESC);` for time-range queries.

Include a header comment block explaining the migration purpose. Follow the existing migration style from 0001_init.sql (lowercase SQL keywords, block comments).
  </action>
  <verify>
    <automated>grep -c 'source_kind' supabase/migrations/0004_extraction_hardening.sql | grep -q '[1-9]' && grep -c 'inferred_city' supabase/migrations/0004_extraction_hardening.sql | grep -q '[1-9]' && grep -c 'extraction_costs' supabase/migrations/0004_extraction_hardening.sql | grep -q '[1-9]' && grep -c 'ENABLE ROW LEVEL SECURITY' supabase/migrations/0004_extraction_hardening.sql | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0004_extraction_hardening.sql` exists and is valid SQL
    - Contains `ALTER TABLE places ADD COLUMN IF NOT EXISTS source_kind text NOT NULL DEFAULT 'ai'` with CHECK for ('ai', 'manual')
    - Contains `ADD COLUMN IF NOT EXISTS inferred_city text`
    - Contains `CREATE TABLE extraction_costs` with all 9 columns from D-08 (id, link_id, provider, model, input_tokens, output_tokens, cost_usd, duration_ms, created_at)
    - Contains `REFERENCES links(id) ON DELETE CASCADE` for link_id FK
    - Contains `CHECK (provider IN ('anthropic', 'google_places'))` for provider column
    - Contains `ALTER TABLE extraction_costs ENABLE ROW LEVEL SECURITY` with NO create policy statements for extraction_costs
    - Contains two index creation statements for extraction_costs
    - Does NOT modify files 0001, 0002, or 0003
  </acceptance_criteria>
  <done>Migration file 0004 exists with places column additions (source_kind, inferred_city) and extraction_costs table with RLS enabled, FK to links, and indexes.</done>
</task>

<task type="auto">
  <name>Task 2: Add extraction constants to @moajoa/core + verify FieldMask</name>
  <files>packages/core/src/constants.ts</files>
  <read_first>
    packages/core/src/constants.ts
    supabase/functions/extract-youtube/pipeline/places.ts
    .planning/phases/02-extraction-pipeline-hardening/02-RESEARCH.md
  </read_first>
  <action>
Edit `packages/core/src/constants.ts` to add three new exported constants at the end of the file (after the existing `ExtractionStatus` block). Follow the existing JSDoc comment style.

1. `EXTRACT_CHANNEL_PREFIX` -- string constant `'extract:'`. JSDoc: "Realtime Broadcast channel prefix. Server broadcasts to `extract:{link_id}`, clients subscribe to same. Shared to prevent channel name mismatch." This prevents Pitfall 1 from RESEARCH.md.

2. `PlaceSourceKind` -- readonly array `['ai', 'manual'] as const` with exported type `PlaceSourceKindType`. JSDoc: "Origin of a place pin. 'ai' = auto-extracted by Edge Function, 'manual' = user-added." Mirrors the CHECK constraint in 0004 migration.

3. `ExtractionStep` -- readonly array `['metadata', 'transcript', 'llm', 'places', 'done', 'error'] as const` with exported type `ExtractionStepType`. JSDoc: "Broadcast step names for extraction progress (D-02). Clients display progress based on these steps."

Then verify EXTRACT-05: run `grep -n '*' supabase/functions/extract-youtube/pipeline/places.ts` to confirm no wildcard FieldMask. The existing `FIELD_MASK` constant on lines 25-31 already uses explicit fields (`places.id,places.displayName,places.formattedAddress,places.location,places.primaryType`). This is a verification-only step per D-12.
  </action>
  <verify>
    <automated>grep -c 'EXTRACT_CHANNEL_PREFIX' packages/core/src/constants.ts | grep -q '[1-9]' && grep -c 'PlaceSourceKind' packages/core/src/constants.ts | grep -q '[1-9]' && grep -c 'ExtractionStep' packages/core/src/constants.ts | grep -q '[1-9]' && ! grep -q "'\*'" supabase/functions/extract-youtube/pipeline/places.ts && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/src/constants.ts` exports `EXTRACT_CHANNEL_PREFIX` with value `'extract:'`
    - `packages/core/src/constants.ts` exports `PlaceSourceKind` as `['ai', 'manual'] as const`
    - `packages/core/src/constants.ts` exports `PlaceSourceKindType` type
    - `packages/core/src/constants.ts` exports `ExtractionStep` as `['metadata', 'transcript', 'llm', 'places', 'done', 'error'] as const`
    - `packages/core/src/constants.ts` exports `ExtractionStepType` type
    - `supabase/functions/extract-youtube/pipeline/places.ts` FIELD_MASK does NOT contain `'*'` wildcard (EXTRACT-05 verified)
    - No existing constants or exports were modified or removed
  </acceptance_criteria>
  <done>Shared extraction constants exist in @moajoa/core for channel prefix, source kind, and extraction steps. FieldMask verified as explicit-only (EXTRACT-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Function -> DB | Service role writes to new extraction_costs table (no RLS policies = service-only access) |
| Migration DDL | Schema changes must not break existing data (NOT NULL columns need DEFAULT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | extraction_costs table | mitigate | RLS enabled with no permissive policies -- only service role can read/write. No client-facing access. |
| T-02-02 | Tampering | places.source_kind | mitigate | CHECK constraint `('ai','manual')` prevents invalid values at DB level. DEFAULT 'ai' ensures existing rows are safe. |
| T-02-03 | Denial of Service | extraction_costs unbounded growth | accept | Low risk at MVP scale. link_id FK with ON DELETE CASCADE ensures cleanup when links are deleted. |
</threat_model>

<verification>
- Migration file `0004_extraction_hardening.sql` contains valid SQL with both ALTER TABLE and CREATE TABLE statements
- No wildcard FieldMask in `places.ts` (grep verification for EXTRACT-05)
- Constants file compiles without TypeScript errors
- No existing migration files (0001-0003) were modified
</verification>

<success_criteria>
- `0004_extraction_hardening.sql` exists with `source_kind`, `inferred_city` columns on places and `extraction_costs` table
- `packages/core/src/constants.ts` exports `EXTRACT_CHANNEL_PREFIX`, `PlaceSourceKind`, `ExtractionStep`
- EXTRACT-05 verified: no wildcard in `places.ts` FIELD_MASK
</success_criteria>

<output>
Create `.planning/phases/02-extraction-pipeline-hardening/02-01-SUMMARY.md` when done
</output>
