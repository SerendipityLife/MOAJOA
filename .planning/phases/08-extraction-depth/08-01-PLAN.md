---
phase: 08-extraction-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0008_extraction_summaries.sql
  - packages/core/src/schemas/place.ts
  - packages/core/src/schemas/link.ts
  - packages/core/src/schemas/extraction.ts
  - packages/core/src/types/index.ts
autonomous: true
requirements: [EXTRACT-12, EXTRACT-13, VIEW-08]
must_haves:
  truths:
    - "places.summary_ko column exists (nullable text) and links.summary_ko column exists (nullable text)"
    - "public_board_view RPC emits summary_ko in BOTH the places and links jsonb objects"
    - "PlaceSchema and LinkSchema accept a nullable summary_ko; PublicBoardView Pick unions include summary_ko for both places and links"
    - "Legacy rows with NULL summary_ko remain valid against the schemas (nullable, not optional-undefined mismatch)"
  artifacts:
    - path: "supabase/migrations/0008_extraction_summaries.sql"
      provides: "Two nullable text columns + full re-issue of public_board_view with summary_ko added to both jsonb objects"
      contains: "create or replace function public_board_view"
    - path: "packages/core/src/schemas/place.ts"
      provides: "PlaceSchema.summary_ko nullable + ExtractedPlaceCandidateSchema.summary_ko optional"
      contains: "summary_ko"
    - path: "packages/core/src/schemas/link.ts"
      provides: "LinkSchema.summary_ko nullable"
      contains: "summary_ko"
    - path: "packages/core/src/types/index.ts"
      provides: "PublicBoardView Pick unions extended with summary_ko (places + links)"
      contains: "summary_ko"
  key_links:
    - from: "supabase/migrations/0008_extraction_summaries.sql (RPC jsonb_build_object)"
      to: "packages/core/src/types/index.ts (PublicBoardView Pick)"
      via: "explicit field list parity — RPC emits the field, type opts it into the public surface"
      pattern: "summary_ko"
---

<objective>
Lay the type + storage foundation for place/video Korean commentary. This plan owns the
"contract layer" of the 4-link data-flow chain identified in 08-PATTERNS.md §CRITICAL: it adds
the two NULLABLE columns, re-issues the `public_board_view` RPC so the new column actually reaches
the web (the RPC uses an explicit jsonb field list, not `select *` — a column not named there is
invisible), and extends the shared Zod schemas + the `PublicBoardView` TS type.

Purpose: Every downstream plan (backend pipeline 08-02, web render 08-03) compiles and stores
against these contracts. Getting the optional-vs-nullable trichotomy right here prevents the
silent "column never reaches browser" failure.
Output: Migration 0008 + extended schemas/types. No runtime behavior yet (the migration is applied
in the autonomous:false gate 08-04; build/typecheck pass WITHOUT it because web reads through the
hand-maintained Pick type, not the generated db types).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extraction-depth/08-CONTEXT.md
@.planning/phases/08-extraction-depth/08-PATTERNS.md

<project_rules>
CLAUDE.md §4.3: migrations append-only — NEVER modify 0001-0007, only add 0008. Columns NULLABLE.
CLAUDE.md §4.5: workspace imports NO `.js` extension.
CLAUDE.md §4.2: packages/core schema changes pair with the SQL migration (both in this plan).
</project_rules>

<optional_vs_nullable_idiom>
The one rule to get right everywhere (08-PATTERNS.md §Shared Patterns):
- Persisted / row schemas (PlaceSchema, LinkSchema): `.max(N).nullable()` → DB null.
- LLM candidate schemas (ExtractedPlaceCandidateSchema, extraction.ts mirror): `.optional()` → undefined.
Match each side's existing idiom. `source_quote` (place.ts:38 `.nullable()` vs place.ts:85 `.optional()`)
is the exact twin to copy.
</optional_vs_nullable_idiom>

<interfaces>
Current PublicBoardView (packages/core/src/types/index.ts:25-43) — extend BOTH Pick unions:
```ts
links: Pick<Link, 'id' | 'source_kind' | 'url' | 'title' | 'thumbnail_url' | 'author_name'>[];
places: Pick<Place, 'id' | 'link_id' | 'name_local' | 'name_ko' | 'name_en' | 'lat' | 'lng'
  | 'category' | 'source_timestamp_sec' | 'source_kind' | 'confidence'>[];
```

Current 0006 RPC places jsonb (0006:68-79) ends with `'source_kind', p.source_kind, 'confidence', p.confidence`.
Current 0006 RPC links jsonb (0006:56-62) ends with `'author_name', l.author_name`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0008 — columns + public_board_view RPC re-issue</name>
  <read_first>
    - supabase/migrations/0007_board_dates.sql (entire — column-add header/style analog)
    - supabase/migrations/0006_trust_ui_onboarding.sql:20-90 (Part 2 — RPC re-issue analog; copy lines 25-90 VERBATIM)
    - 08-PATTERNS.md §7 (migration assignment)
  </read_first>
  <action>
    Create NEW file supabase/migrations/0008_extraction_summaries.sql (do NOT touch 0001-0007).
    Structure, in order:

    1. A fat header comment block matching 0007's tone, explaining: two nullable text columns for
       Korean commentary (places = 1~2 sentence place summary, links = 2~3 sentence video TL;DR),
       both nullable/no-default = non-locking backward-compatible additive change; legacy rows stay
       valid. Note the RPC re-issue is required because public_board_view uses an explicit jsonb
       field list (not select *).

    2. The two column adds (bare `add column`, matching 0007 style):
       ```sql
       alter table places add column summary_ko text;
       alter table links  add column summary_ko text;
       ```

    3. A `create or replace function public_board_view(p_slug text)` block. Copy the ENTIRE body
       from 0006_trust_ui_onboarding.sql:25-90 VERBATIM (declare block, board select, owner select,
       jsonb_build_object for board/owner/links/places, the order-by clauses, the `where ... and
       l.extraction_status = 'ready'` / `where ... and p.hidden_at is null` filters, `return v_result`,
       `end; $$;`, AND the trailing `grant execute on function public_board_view(text) to authenticated, anon;`).
       Keep `language plpgsql stable security definer set search_path = public` exactly.
       Then add EXACTLY these two lines into the copied body:
       - in the links jsonb_build_object (after `'author_name', l.author_name`): `, 'summary_ko', l.summary_ko`
       - in the places jsonb_build_object (after `'confidence', p.confidence`): `, 'summary_ko', p.summary_ko`

    Do NOT copy 0006 Parts 3-4 (the profiles_create_first_board trigger / backfill) — Part 2 only.
    Do NOT add CHECK constraints or defaults (free text).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0008_extraction_summaries.sql && grep -q "alter table places add column summary_ko text" supabase/migrations/0008_extraction_summaries.sql && grep -q "alter table links  *add column summary_ko text" supabase/migrations/0008_extraction_summaries.sql && grep -q "create or replace function public_board_view" supabase/migrations/0008_extraction_summaries.sql && grep -c "summary_ko" supabase/migrations/0008_extraction_summaries.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'summary_ko'" supabase/migrations/0008_extraction_summaries.sql` ≥ 2 (one in links jsonb, one in places jsonb)
    - `grep -q "grant execute on function public_board_view(text) to authenticated, anon" supabase/migrations/0008_extraction_summaries.sql` succeeds (RPC grant preserved)
    - `grep -q "security definer" supabase/migrations/0008_extraction_summaries.sql` succeeds
    - `grep -q "profiles_create_first_board" supabase/migrations/0008_extraction_summaries.sql` FAILS (Parts 3-4 not copied)
    - `git status --porcelain supabase/migrations/0006_trust_ui_onboarding.sql supabase/migrations/0007_board_dates.sql` is empty (append-only — older files untouched)
  </acceptance_criteria>
  <done>0008 exists with both column adds, full RPC re-issue carrying summary_ko in both jsonb objects, grant preserved, trigger/backfill not copied, 0001-0007 untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Zod schemas — place.ts, link.ts, extraction.ts</name>
  <read_first>
    - packages/core/src/schemas/place.ts (entire — PlaceSchema:13-67 nullable side, ExtractedPlaceCandidateSchema:81-90 optional side)
    - packages/core/src/schemas/link.ts:4-24 (LinkSchema)
    - packages/core/src/schemas/extraction.ts (entire — LLMExtractionOutputSchema:29-32)
    - 08-PATTERNS.md §3, §4, §5
  </read_first>
  <behavior>
    - PlaceSchema accepts `{ ...validPlace, summary_ko: '라멘 맛집' }` and `{ ...validPlace, summary_ko: null }`; rejects when summary_ko is a number.
    - ExtractedPlaceCandidateSchema accepts a candidate WITHOUT summary_ko (optional) and WITH summary_ko string.
    - LinkSchema accepts `summary_ko: null` and a string; LinkSchema.parse on a legacy row (no summary_ko key) must NOT throw is NOT expected — nullable requires the key. (Legacy DB rows return null via the column, so the key is always present post-migration.)
    - LLMExtractionOutputSchema accepts `{ places: [...], video_summary_ko: 'TL;DR 한국어' }` and accepts the same object OMITTING video_summary_ko.
    Note: packages/core has NO test runner (test script is a no-op). These behaviors are asserted in
    08-02's deno test (claude.ts mirror) and 08-03's web vitest. Here the gate is `tsc --noEmit`.
  </behavior>
  <action>
    1. place.ts — in PlaceSchema (persisted), add right after `source_quote` (line 38), matching the
       `.nullable()` idiom:
       ```ts
       /** 1~2문장 한국어 해설 (Phase 8 EXTRACT-12). null = legacy/근거 없음. */
       summary_ko: z.string().max(500).nullable(),
       ```
       And in ExtractedPlaceCandidateSchema (LLM-side, lines 81-90), add after `source_quote` (line 85),
       matching the `.optional()` idiom:
       ```ts
       summary_ko: z.string().max(500).optional(),
       ```
    2. link.ts — in LinkSchema, add after `author_name` (line 15), `.nullable()` idiom:
       ```ts
       /** 2~3문장 한국어 영상 TL;DR (Phase 8 EXTRACT-13). null = legacy/없음. */
       summary_ko: z.string().max(800).nullable(),
       ```
    3. extraction.ts — in LLMExtractionOutputSchema (29-32), add the VIDEO summary key (place-level
       summary already flows in via ExtractedPlacesPayloadSchema.shape — do NOT duplicate it here):
       ```ts
       export const LLMExtractionOutputSchema = z.object({
         reasoning: z.string().max(2000).optional(),
         ...ExtractedPlacesPayloadSchema.shape,
         video_summary_ko: z.string().max(800).optional(),
       });
       ```
    NO `.js` extension on any import (Turbopack rule). Touch only these three field additions.
  </action>
  <verify>
    <automated>cd packages/core && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "summary_ko: z.string().max(500).nullable()" packages/core/src/schemas/place.ts` succeeds
    - `grep -q "summary_ko: z.string().max(500).optional()" packages/core/src/schemas/place.ts` succeeds
    - `grep -q "summary_ko: z.string().max(800).nullable()" packages/core/src/schemas/link.ts` succeeds
    - `grep -q "video_summary_ko: z.string().max(800).optional()" packages/core/src/schemas/extraction.ts` succeeds
    - `grep -rq "from '.*\.js'" packages/core/src/schemas/place.ts` FAILS (no .js imports)
    - `cd packages/core && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>place.ts has summary_ko on both persisted (nullable) and candidate (optional) schemas; link.ts has nullable summary_ko; extraction.ts mirror carries video_summary_ko optional; core typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: Extend PublicBoardView Pick unions — types/index.ts</name>
  <read_first>
    - packages/core/src/types/index.ts:25-43 (PublicBoardView)
    - 08-PATTERNS.md §6 + §CRITICAL link 3
  </read_first>
  <action>
    In PublicBoardView (packages/core/src/types/index.ts):
    - Add `| 'summary_ko'` to the `Pick<Link, ...>` union on the `links` line (currently ends `... | 'author_name'`).
    - Add `| 'summary_ko'` to the `Pick<Place, ...>` union on the `places` block (currently ends `... | 'confidence'`).
    This is the explicit opt-in to the public anon surface (Phase 5 discipline: each new field must be
    explicitly Pick'd; never spread the full Place/Link). No other change.
  </action>
  <verify>
    <automated>cd packages/core && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'summary_ko'" packages/core/src/types/index.ts` equals 2 (links Pick + places Pick)
    - The `links:` line in types/index.ts contains both `'author_name'` and `'summary_ko'`
    - The `places:` block contains both `'confidence'` and `'summary_ko'`
    - `cd packages/core && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Both Pick unions include summary_ko; core typecheck passes. The web (08-03) can now read view.places[i].summary_ko / view.links[i].summary_ko without a type error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon web client → public_board_view RPC | unauthenticated visitors read board jsonb; RPC is the only exposure path |
| DB schema → shared Zod/TS contract | column shape must match the type the web casts to |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | public_board_view RPC jsonb field list | mitigate | Explicit Pick + explicit jsonb_build_object — summary_ko is the ONLY new field exposed; no `select *`, so added_by/google_place_id/created_at stay private. RPC keeps `where p.hidden_at is null` and `where l.extraction_status='ready'` filters verbatim. |
| T-08-02 | Tampering | append-only migration history | mitigate | 0008 is new; acceptance criteria assert 0006/0007 byte-unchanged (`git status --porcelain` empty for them). |
| T-08-03 | Elevation of Privilege | security definer function | accept | RPC re-issued verbatim with `set search_path = public` + `security definer` unchanged from 0006 (already reviewed). No new authority added — only two read-only field projections. |
</threat_model>

<verification>
- `cd packages/core && npx tsc --noEmit` exits 0 (schemas + types compile).
- Migration 0008 present; 0001-0007 unmodified (`git status --porcelain supabase/migrations/000[1-7]*` empty).
- `grep -c "'summary_ko'" supabase/migrations/0008_extraction_summaries.sql` ≥ 2 and `grep -c "'summary_ko'" packages/core/src/types/index.ts` == 2.
- Note: the migration is NOT applied in this plan — that is the autonomous:false gate 08-04. The monorepo build does not depend on the applied column because web reads through the hand-maintained Pick type.
</verification>

<success_criteria>
- DB column contract (0008) + RPC explicit-field re-issue + Zod schemas + PublicBoardView type all carry summary_ko.
- Optional-vs-nullable trichotomy honored (persisted .nullable(), candidate .optional()).
- Append-only respected; core typecheck green.
- Backend (08-02) and web (08-03) can build against these contracts in Wave 2.
</success_criteria>

<output>
After completion, create `.planning/phases/08-extraction-depth/08-01-SUMMARY.md`
</output>
