---
phase: 20-affiliate-booking
plan: 05
type: execute
wave: 4
depends_on: ["20-03", "20-04"]
files_modified:
  - apps/ios/app.config.ts
  - apps/ios/lib/booking.ts
  - apps/ios/components/booking/compare-frame-card.tsx
  - apps/ios/__tests__/booking-open.test.ts
  - apps/ios/__tests__/compare-frame-card.test.tsx
autonomous: false
requirements: [ATTR-02, BOOK-03]
user_setup:
  - service: travelpayouts
    why: "제휴 어트리뷰션 실값 — marker는 공개값(모든 링크에 노출)이라 EXPO_PUBLIC_* 클라 노출 허용 (CONTEXT 판단)"
    env_vars:
      - name: EXPO_PUBLIC_TP_MARKER
        source: "Travelpayouts 대시보드 — 계정 marker (745749)"
      - name: EXPO_PUBLIC_TP_TRS
        source: "대시보드 Tools→Links에서 생성 링크의 trs= 값 (프로젝트 ID — A3 확인 겸)"
      - name: EXPO_PUBLIC_TP_KKDAY_P / EXPO_PUBLIC_TP_KKDAY_CAMPAIGN_ID
        source: "대시보드 Tools→Links에서 kkday.com 목적지 딥링크 1개 생성 → URL의 p=/campaign_id= 값 (Open Q1 — Task 3 체크포인트)"
      - name: EXPO_PUBLIC_TP_KKDAY_FALLBACK
        source: "대시보드 KKday tp.st 단축링크 (예: https://kkday.tp.st/gVbA69Yv) — 템플릿 획득 전 폴백"
must_haves:
  truths:
    - "[보기] 탭 → Linking.openURL이 즉시(로깅 await 없이) 호출된다 — 로깅 실패·지연이 브라우저 오픈을 절대 막지 않는다 (D-14, ATTR-02)"
    - "클릭 토큰이 expo-crypto CSPRNG로 발행되고 ClickTokenSchema를 통과한다 (16자 base62)"
    - "CompareFrameCard가 비교 구도(2행 나란히 + 정적 라벨 + 가격 자리 spacer)로 렌더된다 (D-06)"
    - "marker/trs가 env → Constants.expoConfig.extra로만 흐른다 (하드코딩 0)"
  artifacts:
    - "apps/ios/lib/booking.ts — mintClickToken + openBooking"
    - "apps/ios/components/booking/compare-frame-card.tsx — full + compact 변형"
    - "apps/ios/app.config.ts extra: tpMarker/tpTrs/tpKkdayP/tpKkdayCampaignId/tpKkdayFallback"
  key_links:
    - "openBooking → @moajoa/core buildAffiliateUrl (유일 조립 지점) → Linking.openURL → logBookingClick(@moajoa/api).catch(() => {})"
    - "CompareFrameCard onView 콜백 → openBooking (컴포넌트는 URL을 모름 — 조립 지식 격리)"
---

<objective>
iOS 예약 기반 계층: Travelpayouts env 배선 + 클릭 핸들러(mint→open→log, D-14 순서 엄수) + 비교 프레임 카드 컴포넌트. 20-06(plan 탭)·20-07(book 탭)이 이 위에 표면만 얹는다.

Purpose: ATTR-02의 본질 — 진짜 시스템 Safari(Linking.openURL)로 열어 제휴 쿠키를 크로스 세션 보존. SFSafariViewController 계열은 iOS 11+ per-app 격리 저장소라 탈락 (RESEARCH Pitfall 2, Expo 공식 문서 근거).
Output: lib/booking.ts + compare-frame-card + env 배선 + jest 오픈-선행 계약 테스트, KKday 템플릿 체크포인트.
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-affiliate-booking/20-RESEARCH.md
@.planning/phases/20-affiliate-booking/20-PATTERNS.md
@.planning/phases/20-affiliate-booking/20-UI-SPEC.md
@apps/ios/components/plan/plan-item-row.tsx
@apps/ios/components/boards/pin-sheet.tsx
</context>

## Artifacts this phase produces (this plan's share)

- `apps/ios/lib/booking.ts` exports:
  - `mintClickToken(): string` — expo-crypto getRandomValues 16바이트 → base62 16자 → `c_{body}` → ClickTokenSchema.parse
  - `openBooking(args: { program: 'klook'|'kkday'|'airalo'; destUrl: string; ctx: { tripId; userId; placeId? }; checklistItemId?: string; providerLabel: string }): Promise<void>` — travelpayouts 경로
  - `openDirectSearch(args: { provider: 'agoda'|'booking'; params: { city; checkIn; checkOut }; ctx; checklistItemId? }): Promise<void>` — D-05 비제휴 경로 (provider 'agoda_direct'/'booking_direct'로 로깅)
  - `kkdayAvailable(): boolean` — env에 (p+campaign_id) 또는 fallback 존재 여부 (없으면 KKday 버튼 숨김 — graceful hide)
- `apps/ios/components/booking/compare-frame-card.tsx` — `CompareFrameCard` (variant 'full'|'compact', rows: {providerLabel, labelKo, onView}[], footer flag)
- `apps/ios/app.config.ts` extra 키 5종 (EXPO_PUBLIC_TP_*)
- env 이름 계약: EXPO_PUBLIC_TP_MARKER, EXPO_PUBLIC_TP_TRS, EXPO_PUBLIC_TP_KKDAY_P, EXPO_PUBLIC_TP_KKDAY_CAMPAIGN_ID, EXPO_PUBLIC_TP_KKDAY_FALLBACK

<!-- planner-discipline-allow: Linking -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: env 배선 + lib/booking.ts (mint → open → log)</name>
  <files>apps/ios/app.config.ts, apps/ios/lib/booking.ts, apps/ios/__tests__/booking-open.test.ts</files>
  <read_first>
    - apps/ios/app.config.ts (L86-98 extra 블록 — EXPO_PUBLIC_* idiom; eas 블록 무수정)
    - apps/ios/components/boards/pin-sheet.tsx (L82-99 onOpenSource — Linking.openURL + .catch(() => {}) 하우스 idiom)
    - packages/core/src/booking.ts (20-03 산출물 — buildAffiliateUrl productParams 키 계약: program/marker/dest/trs/p/campaign_id/fallback_base)
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §Pattern 1 (mintClickToken 개념 코드 — 코드베이스에 expo-crypto 사용처 0건이라 이것이 정본)
  </read_first>
  <behavior>
    - mintClickToken: 반환값이 /^c_[0-9A-Za-z]{16}$/ 매치, 100회 호출 시 전부 유일, Math.random 미사용 (expo-crypto getRandomValues mock 경유 확인)
    - openBooking: (1) Linking.openURL이 logBookingClick보다 **먼저** 호출됨 (jest invocationCallOrder 단언), (2) logBookingClick이 영영 pending인 promise여도 openBooking은 resolve (오픈 논블로킹 — D-14), (3) logBookingClick reject여도 unhandled rejection 없음 (.catch 삼키기), (4) 열리는 URL이 buildAffiliateUrl 산출물 (트래킹 도메인 — 목적지 도메인 직접 오픈 금지: Klook 네이티브 앱 universal link 가로채기 방지, RESEARCH Anti-pattern 1)
    - kkdayAvailable: env 키 부재 시 false / fallback만 있어도 true
  </behavior>
  <action>
    1. app.config.ts extra에 5키 append (기존 키·eas 블록 무수정): tpMarker/tpTrs/tpKkdayP/tpKkdayCampaignId/tpKkdayFallback ← 대응 EXPO_PUBLIC_TP_* (default 없음 — undefined 허용). apps/ios/.env.local.example에 5키 placeholder 추가 — **이 파일이 권한 설정으로 쓰기 차단되면 우회하지 말고 추가할 5줄을 사용자에게 표면화**(직접 붙여넣도록).
    2. lib/booking.ts 신설: mintClickToken(RESEARCH Pattern 1 — Crypto.getRandomValues(new Uint8Array(16)) → BASE62 인덱싱 → ClickTokenSchema.parse); openBooking — 토큰 mint → Constants.expoConfig.extra에서 marker/trs 읽어(marker 부재 시 열지 않고 콘솔 warn — 미배선 dev 안전) buildAffiliateUrl('travelpayouts', productParams, token) 조립 → `Linking.openURL(url)` **즉시 발화, await로 로깅을 앞세우지 않음** → `logBookingClick(supabase, { trip_id, place_id, user_id, provider: program, click_token: token, checklist_item_id }).catch(() => {})` (D-14; 실패 침묵 — UI-SPEC Screen 4 "Error state NONE"). kkday는 kkdayAvailable 판정 후 p/campaign_id 또는 fallback_base를 productParams로 전달. openDirectSearch — buildDirectSearchUrl 경유 동일 순서, provider 문자열 'agoda_direct'/'booking_direct' (수익 없어도 '확인함' 전이에 필요 — RESEARCH).
    3. booking-open.test.ts: jest.mock('expo-linking')/('expo-crypto')/('expo-constants')/('@moajoa/api')/('@/lib/supabase') 스코프 mock (plan.test.tsx 하네스 결). behavior 4케이스 + mintClickToken 형식.
    인앱 브라우저 패키지(SFSafariViewController 계열)는 import 금지 — 쿠키 격리로 ATTR-02 불충족 (Pitfall 2).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- booking-open --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - booking-open 테스트 exit 0, 케이스 >= 5
    - `grep -c "Linking.openURL" apps/ios/lib/booking.ts` == 2 (openBooking + openDirectSearch 각 1) — 그 외 booking 신규 파일에 0
    - `grep -c "expo-web-browser" apps/ios/lib/booking.ts` == 0
    - `grep -c "Math.random" apps/ios/lib/booking.ts` == 0
    - `grep -c "745749" apps/ios/lib/booking.ts apps/ios/app.config.ts | grep -cv ":0$"` == 0 (marker 하드코딩 0 — env만)
    - `grep -c "tpMarker" apps/ios/app.config.ts` == 1
  </acceptance_criteria>
  <done>클릭 핸들러가 오픈-선행 계약으로 테스트 고정, env 배선 완료</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompareFrameCard (full + compact, 가격 자리 구조)</name>
  <files>apps/ios/components/booking/compare-frame-card.tsx, apps/ios/__tests__/compare-frame-card.test.tsx</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Component 0 (anatomy 정본 — 토큰·카피·터치 타깃 잠금)
    - apps/ios/components/plan/plan-item-row.tsx (ROW_SHADOW 상수 + LegPill stadium chip idiom + Props doc 스타일)
  </read_first>
  <behavior>
    - full: 헤더(아이콘 칩 + title + 우측 caption) + provider 2행(이름 + ─ + 정적 라벨 + flex-1 spacer + [보기]) 렌더
    - compact: 단일 행 — '예약 비교' 라벨 + Klook/KKday 미니 [보기] 2개
    - [보기] press → 행별 onView 콜백 호출 (컴포넌트는 URL 무지 — openBooking은 부모가 주입)
    - rows가 1개면 1행만 (유심/교통 단일 provider 변형)
    - footerVisible=false(기본)면 고지 문구 미렌더 / true면 '예약 시 수수료를 받을 수 있어요' 렌더 (D-16 flag-gated, RESEARCH Open Q2)
  </behavior>
  <action>
    UI-SPEC Component 0 anatomy를 문자 그대로 구현: container `bg-white rounded-2xl px-4 py-4`+cardShadow(standalone)/borderless(embedded prop), 아이콘 칩 `w-10 h-10 rounded-xl bg-neutral-100`+neutral-600 glyph(18), title `text-sm font-semibold text-neutral-900`, provider 행 `mt-3` 리듬 — 이름·`─`·라벨(`text-xs text-neutral-500`)·**가격 자리 `<View className="flex-1" />` spacer (D-06: 후일 가격이 끼어도 레이아웃 무변경)**·[보기] 버튼(`bg-brand-50 rounded-full px-3 py-1.5` + `text-xs font-semibold text-brand-600` + hitSlop ≥44px + accessibilityRole="button" + accessibilityLabel "{provider}에서 보기" + active:opacity-70, **로딩 상태 없음** — D-14 즉시 오픈). compact 변형: `bg-neutral-50 border border-neutral-100 rounded-xl px-3 py-2` + ticket-outline(14, neutral-400) + '예약 비교'(`text-xs text-neutral-500`) + spacer + 미니 [보기] 2개. Props: variant/icon/title/caption/rows({ providerName, labelKo, onView })/embedded/footerVisible(기본 false). 라벨 문자열은 부모가 @moajoa/core COMPARE_LABELS에서 주입 — 컴포넌트에 provider copy 하드코딩 0 (UI-SPEC Cross-Screen). 브랜드 컬러는 [보기]에만 — 카드 표면은 white/neutral (D-02 광고판 금지).
    테스트: RNTL — behavior 5케이스 (@expo/vector-icons 문자열 스텁 mock, plan.test.tsx 선례).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- compare-frame-card --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - compare-frame-card 테스트 exit 0, 케이스 >= 5
    - `grep -c "flex-1" apps/ios/components/booking/compare-frame-card.tsx` >= 2 (title 블록 + 가격 spacer)
    - `grep -c "Linking" apps/ios/components/booking/compare-frame-card.tsx` == 0 (URL 지식 격리 — onView 콜백만)
    - `grep -c "즉시확정\|한국어 가이드" apps/ios/components/booking/compare-frame-card.tsx` == 0 (라벨은 core 주입)
    - `grep -c "보기" apps/ios/components/booking/compare-frame-card.tsx` >= 1
  </acceptance_criteria>
  <done>비교 프레임이 D-06 구도(가격 자리 포함)로 재사용 가능하게 완성</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: KKday 딥링크 템플릿 + 실 env 값 (외부 준비물, Open Q1)</name>
  <files>(대시보드 작업 — repo 파일 변경 없음; 사용자 로컬 .env.local만)</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §Open Questions 1 (KKday 템플릿 획득 절차 정본)
  </read_first>
  <what-built>KKday의 동적 딥링크 프로그램 파라미터(p/campaign_id)는 공개 문서에 없음 — Travelpayouts 대시보드에서만 확인 가능 (RESEARCH Open Q1, 유일한 외부 준비물·약 5분). 코드는 이미 graceful — 값 미획득 시 KKday 버튼은 tp.st 폴백(목적지 타깃만 손해) 또는 숨김.</what-built>
  <action>사용자에게 아래 how-to-verify 절차(대시보드 5분 작업)를 제시하고 응답을 기다린다. "skip" 응답 시 폴백 유지가 확정 — 코드 변경 없이 후일 env만 채우면 활성화됨을 SUMMARY에 기록한다. 획득한 값은 A5(promo_id 4110 대조)·A3(trs 귀속) 검증 겸용.</action>
  <verify>사용자로부터 p/campaign_id/trs 값 또는 "skip" 수신</verify>
  <acceptance_criteria>KKday 파라미터 획득(SUMMARY 기록) 또는 폴백 확정 — 어느 쪽이든 KKday [보기] 동작 경로가 결정됨</acceptance_criteria>
  <done>외부 준비물 해소 (값 또는 폴백 확정)</done>
  <how-to-verify>
    Travelpayouts 대시보드(marker 745749 계정)에서:
    1. Tools → Links에서 kkday.com 아무 페이지나 목적지로 딥링크 1개 생성 → 생성된 URL의 `p=`와 `campaign_id=` 값 복사
    2. 같은 화면에서 Klook 딥링크도 1개 생성 → `promo_id=4110` 일치 확인 (A5 대조) + URL의 `trs=` 값 복사 (A3 — EXPO_PUBLIC_TP_TRS)
    3. 개발 머신 apps/ios/.env.local에 기입: EXPO_PUBLIC_TP_MARKER=745749, EXPO_PUBLIC_TP_TRS={trs}, EXPO_PUBLIC_TP_KKDAY_P={p}, EXPO_PUBLIC_TP_KKDAY_CAMPAIGN_ID={campaign_id}, EXPO_PUBLIC_TP_KKDAY_FALLBACK={KKday tp.st 링크}
  </how-to-verify>
  <resume-signal>p/campaign_id/trs 값 붙여넣기(SUMMARY에 기록용) 또는 "skip" (폴백 유지 — 후일 env만 채우면 활성화, 코드 변경 불필요)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → 시스템 Safari | 트래킹 URL이 OS 브라우저로 전달 — 목적지는 core 상수/파생값만, 사용자 입력 URL 없음 |
| env → 클라 번들 | EXPO_PUBLIC_TP_* 는 번들 포함 — marker는 공개값(모든 링크에 노출)이라 허용 (CONTEXT 판단) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-04 | Tampering | mintClickToken 예측 가능성 | medium | mitigate | expo-crypto getRandomValues CSPRNG 16자 base62(~95bit) — Math.random grep == 0 게이트 |
| T-20-12 | Spoofing | 오픈 리다이렉트 표면 | medium | mitigate | 리다이렉트 EF 미채택으로 공개 무인증 엔드포인트 자체 제거 (RESEARCH 아키텍처 결정); openBooking 목적지는 core 빌더 산출물만 |
| T-20-13 | Info Disclosure | 서비스롤/비밀키 노출 | high | mitigate | 이 설계는 서비스롤 자체를 안 씀 — anon key + RLS만. env는 공개 성격 값(marker/trs/프로그램 파라미터)만 EXPO_PUBLIC_* |
</threat_model>

<verification>
- ios suite green (`pnpm --filter @moajoa/ios test -- --watchman=false`) + typecheck 0
- 오픈-선행 계약 테스트가 D-14를 기계적으로 고정
</verification>

<success_criteria>
- [보기] → 즉시 Safari + 백그라운드 로깅 파이프라인 완성 (ATTR-02 코드 측면)
- 비교 프레임 컴포넌트 재사용 준비 (BOOK-03 시각 계약)
- KKday 파라미터 획득 또는 폴백 확정
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-05-SUMMARY.md` when done
</output>
