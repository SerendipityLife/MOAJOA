---
phase: 20-affiliate-booking
plan: 07
type: execute
wave: 5
depends_on: ["20-05"]
files_modified:
  - apps/ios/components/booking/checklist-row.tsx
  - apps/ios/app/trip/[id]/(tabs)/book.tsx
  - apps/ios/app/me.tsx
  - apps/ios/__tests__/checklist-row.test.tsx
  - apps/ios/__tests__/book.test.tsx
autonomous: true
requirements: [BOOK-02, BOOK-03]
must_haves:
  truths:
    - "book 탭이 예약 체크리스트 홈이다: 자동 파생 항목 + 수동 추가 + 3단 상태(미완료→확인함→완료)를 한 화면에서 진행·관리 (BOOK-02, D-03/D-10/D-11)"
    - "플랜에서 빠진 체크된 항목은 '플랜에 없음' 배지와 함께 보존된다 (D-13)"
    - "앱 복귀 시 팝업 없이 조용한 refetch — 클릭했던 행이 '확인함'으로 바뀌고 '예약했으면 체크해주세요' 인라인 힌트만 (D-15)"
    - "날짜 미정/플랜 없음 trip은 각각의 빈 상태 카피를 보여준다 (D-04)"
    - "설정(me)에 제휴 고지 섹션이 존재한다 (D-16)"
  artifacts:
    - "apps/ios/app/trip/[id]/(tabs)/book.tsx — 17-04 스텁 → 체크리스트 홈"
    - "apps/ios/components/booking/checklist-row.tsx"
    - "apps/ios/app/me.tsx — 제휴 안내 섹션"
  key_links:
    - "book.tsx load → listChecklist + deriveChecklistAutos(core) + reconcileChecklist(api) — plan 탭과 동일 데이터 (D-03)"
    - "행 확장 → CompareFrameCard(20-05) embedded — [보기]가 openBooking/openDirectSearch 경유"
    - "완료 해제 → listClickedChecklistItemIds로 clicked/todo 판정 (UI-SPEC Screen 3)"
---

<objective>
17-04 book 탭 스텁("예약은 곧 제공돼요")을 통합 예약 체크리스트 홈으로 교체 (BOOK-02) + me 화면 제휴 고지 (D-16). 체크리스트 = 관리 지점: 플랜 자동 파생 + 수동 추가 + 3단 상태 + 멤버 공동 조작(D-12) + 플랜 변경에도 체크 기록 보존(D-13) + 복귀 시 조용한 유도(D-15).

Purpose: "대표가 필요한 예약을 한 곳에서 진행"의 표면. plan 탭(발견)과 데이터는 하나.
Output: book.tsx 상태머신 + checklist-row + me.tsx 섹션 + jest, ios suite green.
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-affiliate-booking/20-UI-SPEC.md
@.planning/phases/20-affiliate-booking/20-PATTERNS.md
@apps/ios/app/trip/[id]/(tabs)/book.tsx
@apps/ios/components/plan/plan-item-row.tsx
</context>

## Artifacts this phase produces (this plan's share)

- `apps/ios/components/booking/checklist-row.tsx` — `ChecklistRow` (status control + kind 칩 + 배지 + 확장 + 삭제)
- `apps/ios/app/trip/[id]/(tabs)/book.tsx` — 상태머신: loading / error / empty-dateless / empty-no-plan / list(+row-expanded, add-sheet)
- `apps/ios/app/me.tsx` — '제휴 안내' 정적 섹션
- 카피 계약 (UI-SPEC 정본): '예약 체크리스트', '{done}/{total} 완료', '확인함', '플랜에 없음', '예약했으면 체크해주세요', '항목 추가'/'예약 항목 추가'/'추가하기', '이 항목을 삭제할까요?', 빈 상태 2종

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChecklistRow 컴포넌트 (3단 상태 + 배지 + 확장 + 삭제)</name>
  <files>apps/ios/components/booking/checklist-row.tsx, apps/ios/__tests__/checklist-row.test.tsx</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Screen 3 항목 2·3 (row anatomy 정본 — glyph/색/배지/힌트/삭제 규칙 전부 잠금)
    - apps/ios/components/plan/plan-item-row.tsx (ROW_SHADOW + row 골격 + 필수 태그 chip idiom + 44px 히트 컨트롤)
    - packages/core/src/checklist.ts (ChecklistItem type + isDesynced)
  </read_first>
  <behavior>
    - status 'todo' → ellipse-outline #D1D5DB / 'clicked' → ellipse-outline #2979FF + '확인함' 배지 + 힌트 '예약했으면 체크해주세요' / 'done' → checkmark-circle #10B981 + title text-neutral-400 (취소선 없음)
    - status control 탭 → onToggleDone 콜백 (확인 다이얼로그 없음 — D-11 완료의 원천은 사용자)
    - desynced=true → '플랜에 없음' neutral 칩 (D-13)
    - row 본문 탭 → onToggleExpand; expanded=true면 children(CompareFrameCard embedded) + '항목 삭제' 렌더
    - 삭제 숨김 규칙: source 'auto' && status 'done' → 삭제 버튼 미렌더 (돈 쓴 기록 보존 — D-13); manual done은 삭제 유지
  </behavior>
  <action>
    plan-item-row 골격 그대로 (`bg-white rounded-2xl mb-2.5 px-3 py-3 flex-row items-center` + ROW_SHADOW): 좌측 status control(`w-11 h-11` 히트, accessibilityRole="checkbox" + accessibilityState={{checked}}) → kind 아이콘 칩(`w-10 h-10 rounded-xl bg-neutral-100` + neutral-600 glyph: bed-outline stay / cellular-outline esim / train-outline transport / ticket-outline activity / create-outline manual) → 타이틀 블록(flex-1 px-3, `text-sm font-semibold`; 배지 inline — '확인함'은 필수 태그 idiom `bg-brand-50 text-[10px] text-brand-600`, '플랜에 없음'은 `bg-neutral-100 text-neutral-500 text-[10px]`; 서브라인 `text-xs text-neutral-500` kind 라벨, clicked면 힌트로 교체) → 확장 chevron(`w-11 h-11`, chevron-down/up 18 #D1D5DB). expanded 시 `border-t border-neutral-100 mt-3 pt-3` divider + children 슬롯 + '항목 삭제' 텍스트 버튼(`text-xs` #EF4444, 44px 히트) — behavior 숨김 규칙 적용. Props: item(ChecklistItem)/desynced/expanded/onToggleDone/onToggleExpand/onDelete/children. 상태 3색 진행(neutral→brand→green)이 한눈에 읽히게 (UI-SPEC Color — '완료'는 semantic green, 브랜드 아님).
    테스트: RNTL behavior 6케이스 (icons 문자열 스텁 mock).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- checklist-row --watchman=false</automated>
  </verify>
  <acceptance_criteria>
    - checklist-row 테스트 exit 0, 케이스 >= 6
    - `grep -c "확인함" apps/ios/components/booking/checklist-row.tsx` >= 1 && `grep -c "플랜에 없음" apps/ios/components/booking/checklist-row.tsx` == 1
    - `grep -c "checkmark-circle" apps/ios/components/booking/checklist-row.tsx` == 1
    - `grep -c "accessibilityRole" apps/ios/components/booking/checklist-row.tsx` >= 2
  </acceptance_criteria>
  <done>3단 상태·배지·보존 규칙이 컴포넌트 계약으로 고정</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: book.tsx 체크리스트 홈 (상태머신 + reconcile + 복귀 refetch + 수동 추가)</name>
  <files>apps/ios/app/trip/[id]/(tabs)/book.tsx</files>
  <read_first>
    - apps/ios/app/trip/[id]/(tabs)/book.tsx (현 17-04 스텁 — 빈 상태 레이아웃 재사용 대상)
    - apps/ios/app/trip/[id]/(tabs)/plan.tsx (early-return 상태머신 순서 L525-531 로딩 / L819-837 State F 에러 / @gorhom/bottom-sheet 시트 idiom L703-723 / destructive Alert idiom L297-305 — PATTERNS 발췌 지점)
    - apps/ios/app/_layout.tsx (L39-54 AppState 'active' 리스너 idiom — sub.remove() arrow-wrap 함정 포함)
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Screen 3·4 (레이아웃·카피·상태 정본)
  </read_first>
  <behavior>
    - 로딩 → ActivityIndicator #2979FF / 로드 실패 → '체크리스트를 불러오지 못했어요. 잠시 후 다시 시도해주세요.' + 다시 시도
    - dateless trip → heading '일정이 정해지면 예약을 시작할 수 있어요' / dated + plan null → '먼저 플랜을 만들어주세요' + '플랜 탭으로 가기' 링크 (D-04)
    - 활성: 타이틀 '예약 체크리스트' + '{done}/{total} 완료' caption + 행 정렬 싱글턴(숙소→유심→교통)→activity(플랜 순)→manual
    - status control 탭: todo/clicked → done UPDATE; done 해제 → 클릭 흔적 있으면 clicked, 없으면 todo (listClickedChecklistItemIds 판정)
    - '항목 추가' → bottom-sheet(제목 '예약 항목 추가', placeholder '예: 항공권, 레스토랑 예약', maxLength 80, 빈 입력 시 '추가하기' disabled) → addManualItem → 목록 반영
    - 삭제 → Alert '이 항목을 삭제할까요?' (삭제 destructive/취소) → deleteChecklistItem
    - AppState 'active' 전이 → 조용한 refetch (팝업·배너·토스트 0 — D-15)
  </behavior>
  <action>
    스텁 전체 교체. plan.tsx early-return 사슬 순서 미러: !loaded → 로딩 / error → State F 미러(다시 시도=load 재실행) / 게이트 아웃 → 빈 상태 2종(현 스텁의 centered 레이아웃 `flex-1 items-center justify-center px-8` + `w-20 h-20 rounded-full bg-neutral-100` glyph circle 재사용, '플랜 탭으로 가기'만 text-brand-600 + router push) / 활성 → ScrollView `px-6 pb-12` 목록.
    load(): Promise.all(getTrip, getPlanByTrip, listPlacesByTrip, listChecklist, listClickedChecklistItemIds) → 게이트(plan && trip.start_date) 시 deriveChecklistAutos(예약성 장소 = isBookableActivity 필터 × plan_items 배치분, BOOKING_REGION_MAP 커버리지) 계산 → diff 있으면 reconcileChecklist 후 listChecklist 재조회 (reconcile은 invisible — 스피너 없음, UI-SPEC). desynced 판정은 isDesynced(item, 현 예약성 place_id Set) 렌더 시 계산 (DB 상태 아님 — D-13).
    행 확장 children: kind별 CompareFrameCard embedded — activity=Klook/KKday full rows(장소 타깃 검색 — D-07), stay=Agoda/Booking(openDirectSearch + 날짜 프리필), esim/transport=단일 provider 행, manual=provider 행 없음. onView에 checklistItemId 전달 (클릭→'확인함' 전이 경로 — D-11). 완료 행도 [보기] 동작 유지 (re-open 허용 — UI-SPEC).
    상태 토글: setItemStatus 낙관 업데이트 + 실패 시 원복 (plan.tsx 낙관 idiom). AppState 리스너는 _layout.tsx idiom(inFlight ref 가드 + arrow-wrap sub.remove()). 수동 추가 시트는 @gorhom/bottom-sheet 19-03 확정 시트 idiom; 제출 전 ManualItemTitleSchema.parse (Zod 경계 검증 — CLAUDE.md §4.5).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "예약은 곧 제공돼요" "apps/ios/app/trip/[id]/(tabs)/book.tsx"` == 0 (스텁 소멸)
    - `grep -c "예약 체크리스트" "apps/ios/app/trip/[id]/(tabs)/book.tsx"` >= 1
    - `grep -c "AppState" "apps/ios/app/trip/[id]/(tabs)/book.tsx"` >= 2 (리스너 + cleanup)
    - `grep -c "이 항목을 삭제할까요?" "apps/ios/app/trip/[id]/(tabs)/book.tsx"` == 1
    - `grep -c "deriveChecklistAutos" "apps/ios/app/trip/[id]/(tabs)/book.tsx"` == 1 (파생은 core 단일 정의 호출)
    - `pnpm --filter @moajoa/ios typecheck` exit 0
  </acceptance_criteria>
  <done>체크리스트 홈이 D-03/10/11/12/13/15 전 계약으로 동작</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: me.tsx 제휴 안내 (D-16) + book.test.tsx</name>
  <files>apps/ios/app/me.tsx, apps/ios/__tests__/book.test.tsx</files>
  <read_first>
    - apps/ios/app/me.tsx (L179-208 카드 섹션 idiom — 기존 섹션 무수정, 하단 append)
    - apps/ios/__tests__/plan.test.tsx (mock 하네스 — book.test.tsx가 복사)
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Screen 5 (고지 카피 정본)
  </read_first>
  <behavior>
    - me 화면에 '제휴 안내' heading + 'MOAJOA는 일부 예약 링크에서 제휴 수수료를 받을 수 있어요. 결제 금액은 달라지지 않아요.' body
    - book.test.tsx: dateless → '일정이 정해지면…' / dated+plan null → '먼저 플랜을 만들어주세요' / 활성 → '예약 체크리스트' + 파생 항목 렌더 / clicked 항목 → '확인함' + 힌트 / 항목 추가 시트 오픈 → '예약 항목 추가'
  </behavior>
  <action>
    me.tsx: 기존 카드 섹션들 아래 신규 정적 섹션 surgical append — `bg-white rounded-3xl p-5 mb-4` + cardShadow, heading `text-base font-semibold text-neutral-900`, body `text-sm text-neutral-500 leading-relaxed` (UI-SPEC Screen 5 카피 그대로; 브랜드·링크 없음). D-16: 고지는 여기가 홈, 카드 footer 플래그는 기본 OFF 유지 (RESEARCH Open Q2 — TP 약관에 링크 인접 고지 강제 조항 미발견, 규정 이슈 판명 시 CompareFrameCard footerVisible만 켜면 됨).
    book.test.tsx 신설: plan.test.tsx mock 하네스 복사(@moajoa/api booking 함수 + @/lib/booking + bottom-sheet/icons/reanimated 스텁, 18-05 선례) + behavior 5케이스.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- book --watchman=false && pnpm --filter @moajoa/ios test -- --watchman=false</automated>
    <human-check>디바이스 UAT (phase verify 배치): 체크리스트에서 [보기] → Safari → 앱 복귀 → 해당 행 '확인함' 전환 + 힌트 노출 (팝업 없음, D-15); 완료 체크·해제; 수동 항목 추가·삭제; 플랜 재생성 후 체크된 항목 보존 + '플랜에 없음' 배지 (D-13)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "제휴 안내" apps/ios/app/me.tsx` == 1 && `grep -c "결제 금액은 달라지지 않아요" apps/ios/app/me.tsx` == 1
    - `git diff apps/ios/app/me.tsx | grep -c "^-"` <= 1 (기존 섹션 무손상 append)
    - book 테스트 exit 0, 케이스 >= 5
    - ios 전체 suite exit 0 (기존 87 + 신규 무회귀) + typecheck 0
  </acceptance_criteria>
  <done>D-16 고지 + book 탭 계약이 jest로 고정, 복귀 UX·보존 규칙은 UAT 항목 등록</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 수동 항목 title (사용자 입력) → DB | 자유 텍스트가 booking_checklist_items.title에 저장 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-15 | Tampering | 수동 항목 title 입력 | medium | mitigate | ManualItemTitleSchema(1..80) Zod 경계 검증 + 0021 DB CHECK 이중화. RN Text 렌더는 XSS 표면 없음 (웹 노출 없음 — 웹 예약카드 deferred) |
| T-20-05 | Elevation | 비편집 멤버의 체크/삭제 | high | mitigate | can_edit_trip RLS (0021, 매트릭스 검증) — 클라는 게이트 재구현 안 함 (D-12) |
| T-20-16 | Repudiation | 완료 상태의 임의 조작 | low | accept | D-11 설계상 완료의 원천이 사용자 — 조작 가능함이 곧 스펙 (전환 postback 부재) |
</threat_model>

<verification>
- ios 전체 suite green + typecheck 0
- 카피가 UI-SPEC Copywriting Contract와 문자 일치
</verification>

<success_criteria>
- BOOK-02 체크리스트 홈 완성 (파생+수동+3단 상태+보존+조용한 복귀)
- D-16 고지 홈 존재
- 디바이스 UAT 항목(복귀 전이·보존)이 phase verify에 등록
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-07-SUMMARY.md` when done
</output>
