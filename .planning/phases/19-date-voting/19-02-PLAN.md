---
phase: 19-date-voting
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - packages/core/src/schemas/date-poll.ts
  - packages/core/src/schemas/date-poll.test.ts
  - packages/core/src/schemas/index.ts
  - packages/core/src/constants.ts
  - packages/api/src/queries/date-polls.ts
  - packages/api/src/queries/date-polls.test.ts
  - packages/api/src/queries/index.ts
autonomous: true
requirements: [POLL-01, POLL-02, POLL-03]
must_haves:
  truths:
    - "pollChannelName(tripId) returns 'poll:{tripId}' from a single core builder"
    - "contiguousBlock picks the max-overlap contiguous window over per-day tally"
    - "date-polls.ts wrappers call the right RPCs with client-first signature + {error} throw"
    - "Anon vote writes go through rpc('cast_date_vote'), never a direct from('date_votes').insert"
  artifacts:
    - path: "packages/core/src/schemas/date-poll.ts"
      provides: "DatePoll/Option/Vote/Comment schemas + TripCreateDatelessSchema + contiguousBlock"
      contains: "contiguousBlock"
    - path: "packages/api/src/queries/date-polls.ts"
      provides: "pollByCode/castDateVote/getPollTally/postComment/deleteComment/confirmPollDate/createDatelessTrip"
      exports: ["castDateVote", "getPollTally", "confirmPollDate"]
  key_links:
    - from: "packages/api/src/queries/date-polls.ts"
      to: "supabase rpc('cast_date_vote' | 'poll_vote_tally' | 'confirm_poll_date' | 'create_dateless_trip_with_poll')"
      via: "client.rpc(...) wrappers"
      pattern: "client\\.rpc\\('cast_date_vote'"
    - from: "packages/core/src/constants.ts"
      to: "pollChannelName"
      via: "channel-name builder (single source of truth, mirror planChannelName)"
      pattern: "poll:\\$\\{tripId\\}"
---

<objective>
Lock the shared contract layer that both clients import: `@moajoa/core` Zod schemas (`date-poll.ts`), constants (channel builder, enums, limits, device-token key), the pure `contiguousBlock` recommender, and `@moajoa/api` typed RPC wrappers (`date-polls.ts`). This is the seam that lets iOS (Plan 03) and web (Plan 04) build in parallel against one definition.

Purpose: Define contracts first (interface-first ordering) so downstream plans receive the types/signatures in their plan text — no codebase scavenger hunt. Mirrors the Phase 18 18-01 (core) + 18-04 (api) split.
Output: TDD'd core schemas + recommender + constants + api wrappers, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-date-voting/19-RESEARCH.md
@.planning/phases/19-date-voting/19-PATTERNS.md

<interfaces>
<!-- Analog idioms (verified line refs, 2026-06-23). Copy from the analog, not from scratch. -->

packages/core/src/schemas/plan.ts L4-14 (row schema + import enum from constants), L29-36 (Request schema .default() idiom).
packages/core/src/schemas/trip.ts L17-19 (z.string().date().nullable()), L30-41 (TripCreateSchema — dateless variant drops start_date/end_date + the .refine).
packages/core/src/schemas/plan.test.ts L12 (UUID fixture '11111111-1111-4111-8111-111111111111'), L14-22 (.parse().toEqual default), L41-52 (.toThrow reject), L56-60 (channel-name block).
packages/core/src/constants.ts L8-22 (Limits object incl. VoteNoteMax:140), L186-197 (OnboardKeys/TripKeys namespace idiom), L200-203 (PLAN_CHANNEL_PREFIX + planChannelName), L214-215 (TravelMode = [...] as const + type).
packages/api/src/queries/votes.ts L62-68 (client.rpc idiom: `const { data, error } = await client.rpc('vote_counts_for_places', {...}); if (error) throw error;`), L8/L21 (client-first + {error} throw).
packages/api/src/queries/trips.ts L74-94 (createTrip insert two-step) + rpc idiom.
packages/api/src/queries/plans.test.ts L32-55 (makeChain/makeClient thenable mock; extend client.rpc = vi.fn(()=>Promise.resolve(result)) like invoke at L52), L57-72 (per-fn describe + 'throws when {error}' case).
packages/api/src/queries/index.ts current barrel (append `export * from './date-polls'`).
packages/api/src/types/database.ts — REGENERATED by Plan 01; the RPC arg/return types resolve here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: core date-poll schemas + constants + contiguousBlock (TDD)</name>
  <files>packages/core/src/schemas/date-poll.ts, packages/core/src/schemas/date-poll.test.ts, packages/core/src/schemas/index.ts, packages/core/src/constants.ts</files>
  <read_first>
    - packages/core/src/schemas/plan.ts (full) — the row-schema + Request-schema-with-.default() template.
    - packages/core/src/schemas/trip.ts L30-41 — TripCreateSchema (drop dates for the dateless variant).
    - packages/core/src/schemas/plan.test.ts (full) — the test structure to copy verbatim.
    - packages/core/src/constants.ts L186-215 — the namespace-keys + channel-builder + const-enum block to mirror (surgical APPEND only).
    - .planning/phases/19-date-voting/19-RESEARCH.md Pattern 3 note + A6 (contiguousBlock spec) + Pattern 1 (TripCreateDatelessSchema shape).
    - .planning/phases/19-date-voting/19-PATTERNS.md — the date-poll.ts + constants.ts sections.
  </read_first>
  <behavior>
    - DatePollSchema.parse(valid) → {id, trip_id, poll_code, mode, status, created_at}; bad mode → throw; non-uuid trip_id → throw.
    - TripCreateDatelessSchema.parse({title, city_code, poll_mode:'range'}) → ok; missing poll_mode → throw; poll_mode 'bad' → throw. NO start_date/end_date fields.
    - CastDateVoteRequestSchema: availability defaults to 'available'; bad availability → throw; nickname '' → throw (min 1).
    - pollChannelName('abc') === 'poll:abc'; POLL_CHANNEL_PREFIX === 'poll:'.
    - contiguousBlock(perDay, runLength): given per-day available counts, returns the [start,end] window of length runLength whose MINIMUM daily count is highest (max-overlap). Tie → earliest window. runLength > available days → null.
  </behavior>
  <action>
**constants.ts — surgical APPEND below the L200-215 plan block (do NOT edit existing exports):**
```typescript
export const POLL_CHANNEL_PREFIX = 'poll:';
export function pollChannelName(tripId: string): string {
  return `poll:${tripId}`;
}
export const DatePollMode = ['range', 'grid'] as const;
export type DatePollModeType = (typeof DatePollMode)[number];
export const DateAvailability = ['available', 'unavailable'] as const;
export type DateAvailabilityType = (typeof DateAvailability)[number];
export const POLL_RANGE_OPTIONS_MAX = 10;
export const POLL_GRID_WINDOW_MAX_DAYS = 60;
```
Add `PollKeys = { DeviceToken: 'moajoa:poll_device_token' } as const;` next to OnboardKeys/TripKeys (mirror that namespace idiom, L186-197). Reuse `Limits.VoteNoteMax = 140` for the comment cap (RESEARCH A7 — do NOT add a new constant; the 0018 CHECK is `1..140` matching this).

**date-poll.ts — new file.** Import `{ DatePollMode, DateAvailability }` from '../constants' (mirror plan.ts L2). Export:
- `DatePollSchema` (id/trip_id uuid, poll_code min(8), mode `z.enum(DatePollMode)`, status `z.enum(['open','closed'])`, created_at datetime).
- `DatePollOptionSchema` (id/poll_id uuid, start_date/end_date `z.string().date()`, created_at).
- `DateVoteSchema` (id/poll_id uuid, device_token, nickname, option_id uuid nullable, vote_date `z.string().date().nullable()`, availability `z.enum(DateAvailability)`).
- `DateCommentSchema` (id/poll_id uuid, device_token, nickname, body `z.string().min(1).max(Limits.VoteNoteMax)`, created_at datetime).
- `TripCreateDatelessSchema = z.object({ title: z.string().min(1).max(Limits.TripTitleMax), city_code: z.string().max(20), poll_mode: z.enum(DatePollMode) })` (mirror trip.ts L30-36, NO dates, NO .refine).
- `CastDateVoteRequestSchema` (code, deviceToken, nickname min(1), optionId uuid optional, voteDate `z.string().date()` optional, availability `z.enum(DateAvailability).default('available')` — mirror plan.ts .default idiom).
- `PostCommentRequestSchema` (code, deviceToken, nickname min(1), body min(1).max(Limits.VoteNoteMax)).
- `contiguousBlock(perDay: { date: string; count: number }[], runLength: number): { start: string; end: string } | null` — pure sliding window over the perDay array (assume already date-sorted; if not, sort by date first). For each window of size runLength, score = min(count) across the window; pick the window with the highest score (earliest on tie). Return its first/last date, or null if `perDay.length < runLength`.

**date-poll.test.ts — copy plan.test.ts structure verbatim.** UUID fixture; `.parse().toEqual()` for defaults; `.toThrow()` for rejects; a `describe('poll realtime channel')` asserting `pollChannelName('abc')==='poll:abc'`; **ADD** `describe('contiguousBlock')` — e.g. perDay `[d1:1, d2:3, d3:3, d4:1]`, runLength 2 → `{start:d2, end:d3}` (min 3 beats min 1); runLength 5 over 4 days → null; tie picks earliest.

**index.ts barrel:** append `export * from './date-poll';`.
  </behavior>
  <verify>
    <automated>pnpm --filter @moajoa/core test && pnpm --filter @moajoa/core typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/core test` exits 0 with date-poll.test.ts green (incl. contiguousBlock cases)
    - `pnpm --filter @moajoa/core typecheck` exits 0
    - `grep "export function pollChannelName" packages/core/src/constants.ts` → 1; `grep "poll:\${tripId}" constants.ts` → 1
    - `grep "export function contiguousBlock" packages/core/src/schemas/date-poll.ts` → 1
    - `grep "export \* from './date-poll'" packages/core/src/schemas/index.ts` → 1
    - constants.ts diff is ADDITIONS-only (no edit to the L200-215 plan block — `git diff` shows no removed lines in that range)
    - date-poll.ts has NO start_date/end_date in TripCreateDatelessSchema (`grep "start_date" date-poll.ts` matches only DateVote/Option, not TripCreateDateless)
  </acceptance_criteria>
  <done>core ships date-poll schemas + TripCreateDatelessSchema + contiguousBlock + pollChannelName/enums/PollKeys, all unit-green, constants appended surgically, barrel exported.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: @moajoa/api date-polls.ts RPC wrappers (TDD)</name>
  <files>packages/api/src/queries/date-polls.ts, packages/api/src/queries/date-polls.test.ts, packages/api/src/queries/index.ts</files>
  <read_first>
    - packages/api/src/queries/votes.ts L62-68 — the `client.rpc('...', {...})` + `{error} throw` idiom.
    - packages/api/src/queries/trips.ts L74-94 — createTrip insert path (for the optional dateless two-step fallback).
    - packages/api/src/queries/plans.test.ts (full) — the makeChain/makeClient mocked-chainer harness to copy; extend it for `client.rpc`.
    - packages/api/src/types/database.ts — regenerated by Plan 01; resolves the RPC arg/return types.
    - .planning/phases/19-date-voting/19-RESEARCH.md §Code Examples (castDateVote wrapper) + structure L198-201.
    - .planning/phases/19-date-voting/19-PATTERNS.md — the date-polls.ts section.
  </read_first>
  <behavior>
    - castDateVote(client, {code, deviceToken, nickname, optionId?, voteDate?, availability}) → calls rpc('cast_date_vote', {p_code, p_device_token, p_nickname, p_option_id: optionId ?? null, p_vote_date: voteDate ?? null, p_availability}); throws on {error}.
    - pollByCode → rpc('poll_view_by_code', {p_code}); getPollTally → rpc('poll_vote_tally', {p_code}); postComment → rpc('post_poll_comment', {...}); deleteComment → rpc('delete_poll_comment', {p_comment_id, p_device_token}); confirmPollDate → rpc('confirm_poll_date', {p_poll_id, p_start_date, p_end_date}); createDatelessTrip → rpc('create_dateless_trip_with_poll', {p_title, p_city_code, p_mode}).
    - Each wrapper: client-first arg, `if (error) throw error`. Vote/comment writes use rpc ONLY — zero `from('date_votes').insert(` / `from('date_comments').insert(`.
  </behavior>
  <action>
Create `packages/api/src/queries/date-polls.ts`. Mirror the votes.ts client-first + `{error} throw` house contract. RESEARCH §Code Examples gives the finished `castDateVote` — use it verbatim:
```typescript
export async function castDateVote(
  client: MoajoaSupabaseClient,
  input: { code: string; deviceToken: string; nickname: string; optionId?: string; voteDate?: string; availability: 'available' | 'unavailable' },
): Promise<void> {
  const { error } = await client.rpc('cast_date_vote', {
    p_code: input.code, p_device_token: input.deviceToken, p_nickname: input.nickname,
    p_option_id: input.optionId ?? null, p_vote_date: input.voteDate ?? null,
    p_availability: input.availability,
  });
  if (error) throw error;
}
```
Add: `pollByCode(client, code)` → `rpc('poll_view_by_code', { p_code: code })` returning data; `getPollTally(client, code)` → `rpc('poll_vote_tally', { p_code: code })` returning data; `postComment(client, {code, deviceToken, nickname, body})` → `rpc('post_poll_comment', {...})` returning data; `deleteComment(client, {commentId, deviceToken})` → `rpc('delete_poll_comment', { p_comment_id, p_device_token })`; `confirmPollDate(client, {pollId, startDate, endDate})` → `rpc('confirm_poll_date', { p_poll_id, p_start_date, p_end_date })`; `createDatelessTrip(client, {title, cityCode, mode})` → `rpc('create_dateless_trip_with_poll', { p_title, p_city_code, p_mode })` returning the `{trip_id, poll_id, poll_code}` jsonb. All `if (error) throw error`.

`date-polls.test.ts` — copy the plans.test.ts harness. **Extend makeClient** so `client.rpc = vi.fn(() => Promise.resolve(rpcResult))` (same treatment as `invoke` at plans.test.ts L52). Per-fn `describe`: assert `client.rpc` called with the exact RPC name + arg object, and a `'throws when rpc returns { error }'` case. Include a regression assertion: `from('date_votes')`/`from('date_comments')` never called for writes (count = 0).

Barrel: append `export * from './date-polls';` to `packages/api/src/queries/index.ts`.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api test && pnpm --filter @moajoa/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/api test` exits 0 with date-polls.test.ts green
    - `pnpm --filter @moajoa/api typecheck` exits 0 (resolves against regenerated database.ts from Plan 01)
    - `grep "client.rpc('cast_date_vote'" packages/api/src/queries/date-polls.ts` → 1
    - `grep -c "client.rpc('poll_view_by_code'\|client.rpc('poll_vote_tally'\|client.rpc('post_poll_comment'\|client.rpc('delete_poll_comment'\|client.rpc('confirm_poll_date'\|client.rpc('create_dateless_trip_with_poll'" date-polls.ts` → 6
    - `grep -c "from('date_votes').insert(\|from('date_comments').insert(" date-polls.ts` → 0 (writes via rpc only)
    - `grep "export \* from './date-polls'" packages/api/src/queries/index.ts` → 1
    - every exported fn has `if (error) throw error`
  </acceptance_criteria>
  <done>date-polls.ts exports 7 client-first RPC wrappers, all {error}-throwing, writes via rpc only; test harness extended for client.rpc; barrel exported; typecheck green against the regenerated types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client code → Supabase RPC | The api wrappers are the single call site that translates app intent to `rpc('...')`. They must never offer a direct table-write path for votes/comments (that would re-open T-19-01). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Elevation of Privilege | api offering a direct date_votes/date_comments insert | mitigate | date-polls.ts writes go through `client.rpc('cast_date_vote'|'post_poll_comment')` ONLY; test asserts 0 `from('date_votes').insert(` calls. Inherits the DB-level grant lockdown from Plan 01. |
| T-19-05 | Input Validation | unvalidated payloads reaching the RPC | mitigate | @moajoa/core Zod (CastDateVoteRequestSchema/PostCommentRequestSchema) narrows availability/nickname/body before the call; server re-validates in the DEFINER RPC (Plan 01) — defense in depth. |
| T-19-07 | Information Disclosure | wrapper exposing raw rows | accept | getPollTally returns only the shaped jsonb from poll_vote_tally (Plan 01 guarantees no device_token). The wrapper passes through, adds no raw-row query. |
</threat_model>

<verification>
- `pnpm --filter @moajoa/core test` + `typecheck` green (schemas + contiguousBlock).
- `pnpm --filter @moajoa/api test` + `typecheck` green (RPC wrappers against regenerated types).
- pollChannelName single source; no direct date_votes/date_comments insert in api.
</verification>

<success_criteria>
- core exports the poll schemas, TripCreateDatelessSchema, contiguousBlock, pollChannelName, enums, PollKeys.
- api exports 7 client-first RPC wrappers (vote/comment writes via rpc only).
- Both packages green; iOS + web can now build against these contracts in parallel (Plans 03/04).
</success_criteria>

<output>
After completion, create `.planning/phases/19-date-voting/19-02-SUMMARY.md`
</output>
