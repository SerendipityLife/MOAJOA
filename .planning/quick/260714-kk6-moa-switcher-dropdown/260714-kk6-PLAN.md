---
phase: quick-260714-kk6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/moa/[id]/_components/moa-switcher.tsx
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/app/moa/[id]/page.tsx
  - apps/web/app/moa/page.tsx
  - apps/web/__tests__/moa-switcher.test.tsx
  - apps/web/__tests__/moa-island.test.tsx
autonomous: true
requirements: [QUICK-260714-kk6]
must_haves:
  truths:
    - "지도 화면(/moa/[id]) 좌상단에 '현재 모아 이름 ▾' pill이 보인다 (뒤로 chevron 없음)"
    - "pill을 누르면 내 모아 목록 드롭다운이 pill 바로 아래(align=start) 열린다"
    - "드롭다운에서 다른 모아를 고르면 그 모아 지도로 즉시 전환된다 — router.push('/moa/{id}')"
    - "현재 모아 항목은 체크로 강조되고, 클릭해도 라우팅 없이 그냥 닫힌다"
    - "게스트 공유뷰(/t/[slug])에는 드롭다운 없이 정적 이름 pill만 보인다 (moas 미전달)"
    - "/moa 로 들어오면 리스트가 아니라 가장 최근 모아 지도로 바로 간다 (모아 0개면 /onboarding)"
    - "둘러보기(/discover) 진입이 드롭다운 하단 항목으로 보전된다"
  artifacts:
    - apps/web/app/moa/[id]/_components/moa-switcher.tsx
    - apps/web/__tests__/moa-switcher.test.tsx
  key_links:
    - "MoaIsland → MoaSwitcher: moas prop 옵셔널 — 게스트(guest-surface.tsx) 미전달이라 그쪽 diff 0"
    - "/moa/[id]/page.tsx listMyTrips(supabase) → MoaIsland.moas"
    - "/moa/page.tsx → redirect(`/moa/${trips[0].id}`) — 리스트 페이지가 리다이렉트 허브로 축소"
---

<objective>
웹 지도 화면 좌상단의 "뒤로 chevron → /moa 리스트" 진입을 **인플레이스 모아 스위처 드롭다운**으로 교체하고, 별도 모아 리스트 페이지를 리다이렉트 허브로 축소한다.

Purpose: 모아 간 전환에 화면 이동(지도 언마운트 → 리스트 → 다시 지도)을 강요하지 않는다. 지도 위에서 이름을 눌러 바로 갈아탄다.
Output: `moa-switcher.tsx`(신규) + moa-island 좌상단 교체 + /moa/[id] RSC의 moas seed + /moa 리다이렉트 허브 + jest 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@apps/web/app/moa/[id]/_components/moa-island.tsx
@apps/web/app/moa/[id]/page.tsx
@apps/web/app/moa/page.tsx
@apps/web/components/dropdown-menu.tsx
@apps/web/__tests__/moa-island.test.tsx
@apps/web/__tests__/moa-chat.test.tsx
</context>

<interfaces>
읽은 코드에서 확정한 사실 (재탐색 불필요):

- `apps/web/components/index.ts`가 `export * from './dropdown-menu'` — `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`(기본 `sideOffset=4`, Radix Portal 내부 렌더, `z-50`), `DropdownMenuItem`(Radix `onSelect`), `DropdownMenuSeparator` 전부 `@/components`에서 import 가능. **앱 코드 최초 사용처다** (지금까진 barrel export만 있었음).
- `moa-island.tsx`에서 `useRouter`/`router`의 **유일한 사용처는 551-559행 뒤로 버튼의 `onClick`**이다 (456행 `router.refresh 대신`은 주석 텍스트일 뿐 호출 아님). 즉 버튼 제거 시 `useRouter` import + `const router = ...` 는 내 변경이 만든 고아 → 제거 대상. `Plus`는 FAB(621-630행)가 계속 쓴다 → 유지.
- z-index 실측: PlaceSheet `z-40`, MoaTabBar `z-40`, FAB `z-[60]`, 기존 좌상단 버튼/[함께 정하기] `z-50`. Radix `DropdownMenuContent`는 **document.body 포털 + z-50** → 지도 컨테이너(`fixed inset-0`, z 없음) 위에 뜬다. FAB(z-60)는 우하단이라 좌상단 드롭다운과 시각 겹침 없음.
- `packages/api` `listMyTrips(client)` → `trips.select('*').order('updated_at', { ascending: false })` → `Trip[]`. **정렬이 updated_at desc라 `trips[0]`이 가장 최근 모아**. RLS가 내 trip만 통과시킨다.
- `guest-surface.tsx`는 `<MoaIsland {...moaSeed} hideHostControls />`로 마운트 — `moas`를 옵셔널로 추가하면 그 파일 diff 0.
- 테스트 하네스: vitest(`apps/web/vitest.config.ts`, include `__tests__/**`), `@testing-library/react` + `fireEvent`. **`@testing-library/user-event`는 미설치.**
- `moa-island.test.tsx`는 `vi.mock('@/components', ...)`로 `useToast`+`Button`만 노출한다 → MoaSwitcher가 같은 barrel에서 Dropdown* 을 import하는 순간 그 테스트에서 undefined 렌더 크래시. 무거운 자식은 모듈 스텁으로 격리하는 게 이 파일의 확립된 패턴이다(`moa-map`·`plan-section`·`add-sheet`·`share-sheet`·`moa-chat`·`moa-tab-bar` 전부 `vi.mock`). → moa-switcher도 같은 방식으로 스텁한다.
- `guest-surface.test.tsx`는 `vi.mock('@/app/moa/[id]/_components/moa-island')`로 MoaIsland 자체를 스텁 → **이번 변경으로 손댈 필요 없다.**
- `@/components` 패스스루 mock 선례: `moa-chat.test.tsx`가 `Chip`을 `<button {...props}>`로 통과시킨다.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: MoaSwitcher 컴포넌트 신규 작성</name>
  <files>apps/web/app/moa/[id]/_components/moa-switcher.tsx</files>
  <action>
`'use client'` 컴포넌트를 새로 만든다.

Props: `{ currentTripId: string; title: string; moas?: Trip[] }` (`import type { Trip } from '@moajoa/core'`). export는 `MoaSwitcher` + `MoaSwitcherProps`.

**pill 마크업 (기존 좌상단 버튼과 동일 위치·레이어):** 컨테이너 클래스 `absolute left-4 top-4 z-50 flex items-center gap-1 rounded-full bg-white px-3 py-2 shadow-md`. 안에 제목 `<span className="max-w-[180px] truncate text-sm font-semibold text-neutral-900">{title}</span>` + lucide `ChevronDown`(`className="size-4 shrink-0 text-neutral-500"`, `aria-hidden`). max-w-180px는 좁은 폰(375px)에서 우측 `[함께 정하기]`(right-4 top-4)와 겹치지 않는 상한이다.

**분기 1 — `moas`가 없거나 빈 배열이면 드롭다운을 아예 렌더하지 않는다.** 위 마크업을 `<div>`로 그대로 반환(정적 pill, 인터랙션 0). 게스트 `/t/[slug]` 경로가 여기다 — 남의 모아 열람이라 전환할 목록이 없다.

**분기 2 — 목록이 있으면** `@/components`에서 `DropdownMenu` / `DropdownMenuTrigger` / `DropdownMenuContent` / `DropdownMenuItem` / `DropdownMenuSeparator`를 import해 조립한다:
- `<DropdownMenuTrigger asChild>` 안에 `<button type="button" aria-label="모아 바꾸기">` + 위 pill 마크업(트리거가 곧 pill).
- `<DropdownMenuContent align="start" sideOffset={8} className="w-56">` — pill **바로 아래** 좌측 정렬 앵커.
- 항목: `moas.map(m => <DropdownMenuItem key={m.id} onSelect={() => onPick(m.id)}>)`. 각 행은 현재 모아면 lucide `Check`(`size-4 text-brand-600`), 아니면 같은 크기의 빈 스페이서(`<span className="size-4 shrink-0" />`)로 들여쓰기를 맞추고, 그 옆에 `<span className="truncate">{m.title}</span>`. 현재 모아 행에는 `aria-current="true"`를 준다(테스트 앵커).
- `onPick(id)`: `if (id === currentTripId) return;`(그냥 닫기) `else router.push(\`/moa/${id}\`)` — `useRouter`는 `next/navigation`.
- `<DropdownMenuSeparator />` 뒤에 항목 2개: `새 모아 만들기` → `router.push('/onboarding')`, `둘러보기` → `router.push('/discover')`. (리스트 페이지가 사라지며 유일하게 끊기는 /discover 진입 보전.)

새 색 hex 금지 — 기존 토큰 클래스(`brand-*`, `neutral-*`)만. 로그아웃/마이 등 다른 항목 추가 금지(하단 MoaTabBar `[마이]`가 이미 소유).
  </action>
  <verify>
    <automated>pnpm --filter ./apps/web typecheck &amp;&amp; grep -c "'use client'" "apps/web/app/moa/[id]/_components/moa-switcher.tsx" &amp;&amp; grep -c 'align="start"' "apps/web/app/moa/[id]/_components/moa-switcher.tsx" &amp;&amp; grep -c 'absolute left-4 top-4 z-50' "apps/web/app/moa/[id]/_components/moa-switcher.tsx" &amp;&amp; grep -c '둘러보기' "apps/web/app/moa/[id]/_components/moa-switcher.tsx"</automated>
  </verify>
  <done>moa-switcher.tsx가 존재하고 apps/web typecheck exit 0. moas 미전달 시 정적 pill, 전달 시 Radix 드롭다운을 렌더하는 두 분기가 코드에 있다.</done>
</task>

<task type="auto">
  <name>Task 2: moa-island 좌상단 교체 + moas seed + /moa 리다이렉트 허브 축소</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/app/moa/[id]/page.tsx, apps/web/app/moa/page.tsx</files>
  <action>
**(a) `moa-island.tsx` — surgical 3점 수정:**
1. `MoaIslandProps`에 `moas?: Trip[]` 추가(옵셔널 + JSDoc 한 줄: 게스트 마운트는 미전달 → 정적 pill). 구조분해 파라미터에도 `moas` 추가. `Trip`은 이미 import되어 있다.
2. 551-559행 뒤로 버튼 블록(주석 포함)을 통째로 `<MoaSwitcher currentTripId={trip.id} title={trip.title} moas={moas} />` 한 줄로 교체 + `./moa-switcher` import 추가.
3. **내 변경이 만든 고아만** 제거: (i) lucide import를 `Plus`만 남긴다 — 화살표 아이콘은 이제 안 쓴다. (ii) `next/navigation` import 라인과 95행의 라우터 지역변수 선언을 지운다(교체 후 파일 내 호출처 0 — 456행은 주석일 뿐). `Plus`(FAB)·`PlaceSheet` 헤더의 `{trip.title}`·나머지 전부 그대로.

<!-- planner-discipline-allow: useRouter -->
<!-- planner-discipline-allow: ChevronLeft -->
**PlaceSheet(`place-sheet.tsx`)는 한 줄도 수정 금지 (HC-5 — 제스처 소유권 회귀 방지).**

**(b) `apps/web/app/moa/[id]/page.tsx`:** `@moajoa/api` import에 `listMyTrips` 추가 → 기존 `Promise.all` 배열에 `listMyTrips(supabase)`를 append하고 구조분해에 `myTrips`를 받는다 → `<MoaIsland ... moas={myTrips} />`. 다른 라인 diff 0.

**(c) `apps/web/app/moa/page.tsx` — 리다이렉트 허브로 전면 축소:** 리스트 UI(header / ul / 카드 Link / 하단 고정 CTA)를 전부 지우고 아래 구조만 남긴다.
- 파일 상단 JSDoc을 새 역할로 갱신: 리스트 화면은 없어졌고(모아 전환은 지도 좌상단 스위처가 담당) 루트·/login·/auth/callback·bottom-nav '모아' 탭이 전부 여기로 보내므로 라우트는 남기고 리다이렉트만 한다는 사실을 적는다.
- 본문: 미로그인 → `redirect('/login?next=/moa')` → `const trips = await listMyTrips(supabase)` → `const latest = trips[0]` → `if (!latest) redirect('/onboarding')` → 마지막 문장 `redirect(\`/moa/${latest.id}\`)`. (`listMyTrips`가 updated_at desc라 `[0]`이 최근 모아. `listMyTripsWithPreview`는 places를 embed해 불필요하게 무겁다 — 쓰지 않는다.)
- 고아 import 제거: `next/link`의 Link, `@moajoa/core`의 CITY_KO_MAP, 그리고 preview 쿼리. `redirect`는 `never` 반환이라 함수 끝이 unreachable → TS가 `Promise<never>`로 추론(Next의 페이지 반환 계약 만족). 만약 typecheck가 반환형으로 불평하면 마지막 줄만 `return redirect(...)`로 바꾼다.

<!-- planner-discipline-allow: listMyTripsWithPreview -->
`packages/api`의 `listMyTripsWithPreview` 함수 자체는 **삭제하지 않는다**(다른 소비자 존재 가능 — 내 변경의 고아가 아님, CLAUDE.md §3.3).
`apps/web/components/bottom-nav.tsx`는 건드리지 않는다. **`apps/ios`는 절대 손대지 않는다(v2.1 전면 동결).**
  </action>
  <verify>
    <automated>pnpm --filter ./apps/web typecheck &amp;&amp; grep -c '&lt;MoaSwitcher' "apps/web/app/moa/[id]/_components/moa-island.tsx" &amp;&amp; test "$(grep -c 'aria-label="뒤로"' "apps/web/app/moa/[id]/_components/moa-island.tsx")" = "0" &amp;&amp; test "$(grep -c "router.push('/moa')" "apps/web/app/moa/[id]/_components/moa-island.tsx")" = "0" &amp;&amp; grep -c 'moas={myTrips}' "apps/web/app/moa/[id]/page.tsx" &amp;&amp; test "$(grep -c 'CITY_KO_MAP\|next/link' apps/web/app/moa/page.tsx)" = "0" &amp;&amp; test "$(grep -c 'redirect(' apps/web/app/moa/page.tsx)" -ge 3 &amp;&amp; test -z "$(git diff --name-only -- apps/ios apps/web/app/t 'apps/web/app/moa/[id]/_components/place-sheet.tsx' apps/web/components/bottom-nav.tsx supabase)"</automated>
  </verify>
  <done>지도 좌상단이 MoaSwitcher pill로 교체됨. /moa/[id] RSC가 listMyTrips를 seed. /moa는 리다이렉트 전용(리스트 마크업 0). apps/ios · /t · place-sheet · bottom-nav diff 0. typecheck exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 스위처 테스트 신규 + moa-island 테스트 스텁 배선 → 전체 그린</name>
  <files>apps/web/__tests__/moa-switcher.test.tsx, apps/web/__tests__/moa-island.test.tsx</files>
  <behavior>
    - moas 목록이 있으면: 현재 모아 title이 pill에 렌더되고, 메뉴에 내 모아 전부 + `새 모아 만들기` + `둘러보기`가 있다
    - 다른 모아 항목 클릭 → `router.push('/moa/t2')` 1회
    - 현재 모아 항목 클릭 → push 0회 (그냥 닫힘), 그 행에 `aria-current="true"`
    - `둘러보기` 클릭 → `router.push('/discover')`
    - moas 미전달(게스트) → title pill은 보이되 메뉴/트리거 버튼 없음
    - MoaIsland 렌더 시 좌상단이 스위처로 바뀌었다(옛 '뒤로' 버튼 부재)
  </behavior>
  <action>
**(a) `apps/web/__tests__/moa-switcher.test.tsx` 신규.** `@testing-library/user-event`가 미설치이고 Radix 메뉴는 jsdom에서 포인터캡처/ResizeObserver를 요구하므로, **이 레포의 확립된 패턴대로 `@/components`를 패스스루 mock한다**(moa-chat.test의 Chip 선례 — import 이전에 `vi.mock` 선언):

- `DropdownMenu`: `({children}) => <div>{children}</div>`
- `DropdownMenuTrigger`: `({children}) => <>{children}</>` (asChild 프로프는 무시)
- `DropdownMenuContent`: `({children}) => <div data-testid="moa-menu">{children}</div>`
- `DropdownMenuItem`: `({children, onSelect, ...rest}) => <button role="menuitem" onClick={() => onSelect?.()} {...rest}>{children}</button>`
- `DropdownMenuSeparator`: `() => <hr />`

라우터는 `const push = vi.fn(); vi.mock('next/navigation', () => ({ useRouter: () => ({ push }) }))`(moa-island.test 선례). Trip 픽스처는 최소 필드만 채운 뒤 `as unknown as Trip` 캐스트(moa-island.test의 픽스처 스타일을 따르되, 이미 있는 `Trip` 전체 픽스처를 복사해도 됨).

케이스 5개 — `<behavior>` 목록 그대로 1:1. 항목 클릭은 `fireEvent.click(screen.getByRole('menuitem', { name: /.../ }))`. 현재 모아 행은 `[aria-current="true"]`로 집는다. 게스트 케이스는 `moas` prop 자체를 넘기지 않고 `queryByTestId('moa-menu')`가 null + `getByText(title)`가 존재함을 단언.

**(b) `apps/web/__tests__/moa-island.test.tsx` — 최소 증분.** 기존 자식 스텁 블록(`moa-map`/`plan-section`/… `vi.mock`) 옆에 스위처 스텁을 추가한다:
`vi.mock('@/app/moa/[id]/_components/moa-switcher', () => ({ MoaSwitcher: ({ title, moas }: { title: string; moas?: { id: string }[] }) => (<div data-testid="moa-switcher" data-moas={String(moas?.length ?? 0)}>{title}</div>) }))`
— 없으면 `@/components` mock에 Dropdown* 이 없어서 크래시한다.
그리고 테스트 1개만 append: 렌더 후 `getByTestId('moa-switcher')`가 trip.title을 보여주고 `queryByLabelText('뒤로')`가 null임을 단언(옛 진입점 소멸 회귀 가드). **기존 케이스는 수정하지 않는다.**

기존에 /moa 리스트 렌더를 기대하는 테스트는 없다(확인 완료) — 새로 만들지도 말 것.
  </action>
  <verify>
    <automated>pnpm --filter ./apps/web test:run &amp;&amp; pnpm --filter ./apps/web typecheck &amp;&amp; pnpm --filter ./apps/web lint</automated>
  </verify>
  <done>apps/web vitest 전체 exit 0 (moa-switcher 5 신규 + moa-island 1 신규, 기존 케이스 무회귀). typecheck·lint exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → RSC(`/moa`, `/moa/[id]`) | 쿠키 세션. trips 조회는 anon 키 + RLS 게이트 |
| MoaSwitcher → router.push | 클라이언트 내비게이션. 목적지는 서버가 준 내 trip id 목록에서만 나온다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-KK6-01 | Information Disclosure | `moas` prop (남의 모아 제목 노출) | medium | mitigate | `listMyTrips`는 trips SELECT RLS(내 trip only) 통과분만 반환. 게스트 `/t/[slug]`에는 `moas` 미전달 → 정적 pill(목록 자체가 렌더되지 않음) |
| T-KK6-02 | Elevation of Privilege | `router.push('/moa/{id}')` 임의 id | low | accept | 클라이언트 내비게이션은 권한이 아니다. `/moa/[id]` RSC가 `getTrip`(RLS) null → `notFound()`로 이미 fail-closed(T-24-17) |
| T-KK6-SC | Tampering | 패키지 설치 | low | accept | 신규 의존성 0 — `@radix-ui/react-dropdown-menu`·`lucide-react` 전부 기설치 |
</threat_model>

<verification>
- `pnpm --filter ./apps/web test:run` exit 0 (신규 6케이스 포함 전체 그린)
- `pnpm --filter ./apps/web typecheck` exit 0
- `pnpm --filter ./apps/web lint` exit 0
- `git diff --name-only` 결과에 `apps/ios/**`, `apps/web/app/t/**`, `place-sheet.tsx`, `bottom-nav.tsx`, `supabase/migrations/**` 가 **없다**
- 워크스페이스 패키지 import에 `.js` 확장자 0
</verification>

<success_criteria>
- `/moa/[id]`에서 좌상단이 `{모아 제목} ▾` pill이고, 눌러 다른 모아를 고르면 그 지도로 전환된다
- `/t/[slug]` 게스트 화면은 정적 제목 pill만 (드롭다운 없음)
- `/moa` 진입 시 리스트 없이 최근 모아 지도(또는 모아 0개면 `/onboarding`)로 리다이렉트
- 변경된 모든 줄이 이 요청으로 추적 가능 (CLAUDE.md §3.3 surgical)
</success_criteria>

<output>
Create `.planning/quick/260714-kk6-moa-switcher-dropdown/260714-kk6-SUMMARY.md` when done
</output>
