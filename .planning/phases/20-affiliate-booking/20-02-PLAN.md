---
phase: 20-affiliate-booking
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - supabase/migrations/0021_booking.sql
  - packages/api/src/types/database.ts
autonomous: false
requirements: [BOOK-02, ATTR-02]
user_setup:
  - service: supabase (cloud project xfoauhsraguyrifingct)
    why: "이 Windows 머신은 Docker 미설치 — 로컬 supabase start/reset 불가. 클라우드 DB에 직접 적용해야 함"
    env_vars:
      - name: (세션 한정) Session pooler connection string
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string (Session pooler) — 사용자 보유, 체크포인트에서 제공"
must_haves:
  truths:
    - "라이브 DB에 booking_checklist_items 테이블이 존재하고 RLS가 can_read_trip/can_edit_trip 헬퍼로 게이트된다 (D-12)"
    - "booking_clicks에 authenticated INSERT(user_id=auth.uid() AND can_read_trip)와 멤버 SELECT 정책이 존재한다 — 멤버 클릭이 42501 없이 기록된다 (D-11)"
    - "재생성된 database.ts에 booking_checklist_items Row/Insert/Update 타입과 booking_clicks.click_token이 존재한다"
  artifacts:
    - "supabase/migrations/0021_booking.sql"
    - "packages/api/src/types/database.ts (재생성)"
  key_links:
    - "0021 정책 → 0016 can_read_trip/can_edit_trip SECURITY DEFINER 헬퍼 (42P17 가드 — 직접 cross-table EXISTS 0)"
    - "booking_clicks.checklist_item_id → booking_checklist_items(id) nullable FK (D-11 '확인함' 전이의 명시적 대상)"
    - "ChecklistItemSchema(20-03) enum ↔ 0021 CHECK 제약 (kind/status/source 값 완전 일치)"
---

<objective>
0021_booking.sql 작성 + **라이브 클라우드 DB 적용** + database.ts 타입 재생성 + RLS 매트릭스 검증. booking_clicks(0016 빈 테이블)에 클릭 토큰 컬럼·INSERT/멤버 SELECT 정책을 추가하고, trip-scoped 체크리스트 테이블 booking_checklist_items를 신설한다 (BOOK-02, ATTR-02 데이터 측면).

Purpose: 라이브 DB가 0021과 일치하지 않으면 booking_clicks INSERT와 체크리스트 읽기가 런타임에 42501/42P01로 터진다 — phase 검증의 전제조건. plan_items 확장은 불가(0017 draft 덮어쓰기가 rows 파괴 → D-13 위반, RESEARCH Pitfall 3)라 trip-scoped 신설이 구조적 근거.
Output: 0021 마이그레이션 파일 + 라이브 적용 + 재생성 타입 + psql-equivalent RLS 매트릭스 PASS.
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-affiliate-booking/20-CONTEXT.md
@.planning/phases/20-affiliate-booking/20-RESEARCH.md
@.planning/phases/20-affiliate-booking/20-PATTERNS.md
@supabase/migrations/0017_plans.sql
</context>

## Artifacts this phase produces (this plan's share)

- `supabase/migrations/0021_booking.sql` — 테이블 `booking_checklist_items` (columns: id, trip_id, place_id, kind, title, status, source, created_at, updated_at) + 인덱스 `checklist_trip_idx`, `checklist_singleton_uq`, `checklist_place_uq` + 트리거 `booking_checklist_items_set_updated_at` + RLS 정책 4종; `booking_clicks` alter — 컬럼 `click_token`, `checklist_item_id` + 정책 "booking_clicks: member can insert own", "booking_clicks: member can read"
- `packages/api/src/types/database.ts` — booking_checklist_items Row/Insert/Update + booking_clicks 확장 컬럼 반영 재생성

<tasks>

<task type="auto">
  <name>Task 1: 0021_booking.sql 작성 (append-only)</name>
  <files>supabase/migrations/0021_booking.sql</files>
  <read_first>
    - supabase/migrations/0016_trips_baseline.sql (L582-606 booking_clicks 원형 + L38-46 set_updated_at + can_read_trip/can_edit_trip 헬퍼 정의부)
    - supabase/migrations/0017_plans.sql (헤더 주석 + 테이블/RLS/트리거/partial unique idiom — 0021이 미러할 정본)
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §"0021 마이그레이션 골자"
    - .planning/phases/20-affiliate-booking/20-PATTERNS.md §0021 섹션
  </read_first>
  <action>
    새 파일 `supabase/migrations/0021_booking.sql` 작성. 0016~0020 기존 파일 절대 무수정 (append-only, CLAUDE.md §4.3). 0017 헤더 주석 스타일(phase/req/결정 인용 + 42P17 가드 명시) 미러. 내용 순서:

    1. **booking_checklist_items 테이블** (Phase 20 D-10/D-11/D-12/D-13, RESEARCH 골자 그대로):
       - id uuid pk default gen_random_uuid()
       - trip_id uuid not null references trips(id) on delete cascade
       - place_id uuid references places(id) on delete set null (nullable — 싱글턴·manual 항목)
       - kind text not null check (kind in ('stay','esim','transport','activity','custom'))
       - title text not null check (char_length(title) between 1 and 80)
       - status text not null default 'todo' check (status in ('todo','clicked','done')) — D-11 3단
       - source text not null default 'auto' check (source in ('auto','manual')) — D-10
       - created_at/updated_at timestamptz not null default now()
       - **플랜 테이블(plans/plan_items)로의 FK 컬럼은 어떤 이름으로도 만들지 않음** — 0017 draft 덮어쓰기(delete→insert)가 재생성마다 rows를 파괴해 D-13(체크 기록 보존)을 즉시 위반 (RESEARCH Pitfall 3). trip_id + place_id로만 참조.
    2. **인덱스**: `create index checklist_trip_idx on booking_checklist_items (trip_id);` + partial unique 2개 (plans_one_draft_per_trip idiom): `checklist_singleton_uq (trip_id, kind) where source='auto' and kind in ('stay','esim','transport')` / `checklist_place_uq (trip_id, place_id) where source='auto' and place_id is not null`
    3. **updated_at 트리거**: `booking_checklist_items_set_updated_at` — 0016 `set_updated_at()` 재사용, 재정의 금지 (0017 L58-61 idiom)
    4. **RLS**: `alter table booking_checklist_items enable row level security;` + 정책 4종 전부 헬퍼 직호출 (trip_id 직참조 테이블이라 EXISTS-경유 불필요 — PATTERNS Shared Patterns): SELECT to authenticated using (can_read_trip(trip_id)) / INSERT with check (can_edit_trip(trip_id)) / UPDATE using+with check (can_edit_trip(trip_id)) / DELETE using (can_edit_trip(trip_id)) — D-12 멤버 모두, 대표 독점 게이트 없음.
    5. **booking_clicks alter** (0016 원본 무수정, 0021에서 alter/새 policy만 — 0017 extraction_costs 선례):
       - `alter table booking_clicks add column click_token text check (click_token ~ '^c_[0-9A-Za-z]{8,30}$');` (NULLABLE — downtime 회피 §4.3; regex는 17-02 ClickTokenSchema와 동일)
       - `alter table booking_clicks add column checklist_item_id uuid references booking_checklist_items(id) on delete set null;` (테이블 생성 뒤에 배치)
       - INSERT 정책: `create policy "booking_clicks: member can insert own" on booking_clicks for insert to authenticated with check (user_id = auth.uid() and can_read_trip(trip_id));` — T-20-02 타 사용자 명의 클릭 위조 차단 (RESEARCH Pitfall 4)
       - 멤버 SELECT 정책: `create policy "booking_clicks: member can read" on booking_clicks for select to authenticated using (can_read_trip(trip_id));` — D-11 '확인함'은 멤버 모두의 클릭에 반응(D-12), 기존 owner-read 정책과 permissive 병존 (0016 정책 무수정)
       - `create index booking_clicks_checklist_item_idx on booking_clicks (checklist_item_id) where checklist_item_id is not null;`
    anon 정책은 0건 — 이 phase 표면은 authenticated 전용.
  </action>
  <verify>
    <automated>grep -c "can_edit_trip\|can_read_trip" supabase/migrations/0021_booking.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create policy" supabase/migrations/0021_booking.sql` == 6 (checklist 4 + clicks 2)
    - `grep -cE "plan_id|plan_item_id" supabase/migrations/0021_booking.sql` == 0 (Pitfall 3 가드)
    - `grep -c "exists" supabase/migrations/0021_booking.sql` == 0 — 직접 cross-table EXISTS 0건 (42P17 가드; 헬퍼 직호출만)
    - `grep -c "create function" supabase/migrations/0021_booking.sql` == 0 (set_updated_at 재정의 금지)
    - `git diff --stat supabase/migrations/0016_trips_baseline.sql supabase/migrations/0017_plans.sql supabase/migrations/0018_date_polls.sql` 빈 출력 (append-only)
    - `grep -c "to anon\|grant.*anon" supabase/migrations/0021_booking.sql` == 0
  </acceptance_criteria>
  <done>0021이 RESEARCH 골자·0017 idiom과 일치하고 구조 grep 게이트 전부 통과</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] 라이브 DB 접속 자격 제공 (Docker 부재 환경 게이트)</name>
  <files>(자격 제공 게이트 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §Environment Availability (Docker ✗ 이 Windows 머신)
  </read_first>
  <what-built>0021_booking.sql 완성. 이 Windows 머신은 Docker 미설치라 `supabase start`/`db reset` 불가, CLI `login`/`link`는 비대화 셸에서 hang — 기존 확립 패턴(0019/0020 적용 선례)대로 클라우드 프로젝트(ref xfoauhsraguyrifingct)에 Session pooler로 직접 적용해야 하며, pooler connection string은 사용자 보유.</what-built>
  <action>사용자에게 아래 how-to-verify의 두 경로를 제시하고 응답을 기다린다 — 응답 전 Task 3(적용)을 시작하지 않는다. 제공받은 connection string은 세션 환경변수로만 사용하고 어떤 파일·커밋에도 기록하지 않는다.</action>
  <verify>사용자로부터 pooler connection string 또는 "mac에서 적용 완료" 수신</verify>
  <acceptance_criteria>둘 중 한 경로가 확정됨 — Task 3의 실행 모드(직접 적용 vs 검증만)가 결정됨</acceptance_criteria>
  <done>라이브 DB 적용 수단 확보</done>
  <how-to-verify>
    다음 중 하나를 수행:
    1. (권장) Supabase Dashboard → Project Settings → Database → Session pooler connection string(비밀번호 포함)을 이 세션에 붙여넣기 — Claude가 Task 3(적용+타입재생성+RLS 매트릭스)를 실행. 문자열은 커밋·파일 저장 안 함(세션 환경변수로만 사용).
    2. (대안) 사용자가 Mac(colima)에서 직접: `supabase db push` 후 `pnpm supabase:types` 실행 → 재생성된 database.ts를 커밋했다고 알려주기 — Claude는 Task 3의 RLS 매트릭스만 pooler 없이 스킵하고 psql 매트릭스를 phase verify로 이관.
  </how-to-verify>
  <resume-signal>pooler connection string 붙여넣기, 또는 "mac에서 적용 완료" 입력</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 라이브 적용 + database.ts 재생성 + RLS 매트릭스</name>
  <files>packages/api/src/types/database.ts</files>
  <read_first>
    - supabase/migrations/0021_booking.sql (Task 1 산출물)
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §Security Domain (매트릭스 대상 위협)
    - package.json (root — supabase:types 스크립트 형태 확인)
  </read_first>
  <action>
    사용자 제공 pooler URL(환경변수 POOLER_URL로 노출, 어디에도 기록 금지)로 실행. 스크래치패드에 node 스크립트 작성(`pg` 클라이언트, `ssl: { rejectUnauthorized: false }` — 기확립 패턴):

    1. **적용**: 단일 트랜잭션으로 0021_booking.sql 전체 실행. 성공 시 `supabase_migrations.schema_migrations`에 version '0021' 행 insert (미래 `supabase db push`의 중복 적용 방지 — 테이블 shape가 예상과 다르면 중단하고 사용자에게 표면화). 실패 시 ROLLBACK + 에러 원문 보고.
    2. **타입 재생성**: `supabase gen types typescript --db-url "$POOLER_URL" --schema public > packages/api/src/types/database.ts` (Docker/login 불필요 경로). 이후 `pnpm --filter @moajoa/api typecheck` exit 0 확인.
    3. **RLS 매트릭스** (19-01 선례 — 전부 BEGIN…ROLLBACK 단일 트랜잭션 내, set_config is_local 소멸 함정 회피): postgres 롤로 테스트 fixture(auth.users 2명 + profiles + trip 1개 + owner/member membership) 생성 후, `set local role authenticated; select set_config('request.jwt.claims', '{"sub":"<uuid>"}', true);` 조합으로 단언:
       - (A) 멤버가 자기 user_id로 booking_clicks INSERT → 성공
       - (B) 멤버가 **타인 user_id**로 booking_clicks INSERT → RLS 거부 (T-20-02)
       - (C) 비멤버 authenticated가 booking_clicks INSERT → RLS 거부
       - (D) 멤버 SELECT booking_clicks → 행 보임 / 비멤버 SELECT → 0행 (T-20-03)
       - (E) 멤버 booking_checklist_items INSERT/UPDATE/DELETE → 성공 (D-12) / 비멤버 → 거부 (T-20-05)
       - (F) click_token CHECK: 'bad.token' INSERT → constraint 위반, 'c_aB3xY9zQ' → 통과
       전 케이스 결과를 SUMMARY에 기록. 마지막에 ROLLBACK — 라이브 데이터 무오염.
    Task 2에서 사용자가 "mac에서 적용 완료"를 선택한 경우: 1·2는 검증만(테이블 존재 확인 쿼리 불가 시 database.ts diff로 판정), 3은 phase verify로 이관 명시.
  </action>
  <verify>
    <automated>grep -c "booking_checklist_items" packages/api/src/types/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "booking_checklist_items" packages/api/src/types/database.ts` >= 3 (Row/Insert/Update)
    - `grep -c "click_token" packages/api/src/types/database.ts` >= 3
    - `pnpm --filter @moajoa/api typecheck` exit 0
    - RLS 매트릭스 A~F 전부 기대 결과 (SUMMARY에 케이스별 기록) — 42P17 recursion 0건
    - pooler URL이 커밋·파일 어디에도 없음 (`git grep -c "pooler.supabase" -- ':!*.md'` == 0)
  </acceptance_criteria>
  <done>라이브 DB가 0021과 일치, 타입 재생성 + api typecheck green, RLS 매트릭스 전부 PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS 클라(anon key + user JWT) → Postgres RLS | booking_clicks INSERT / 체크리스트 CRUD가 이 경계를 넘음 |
| 플래너 세션 → 라이브 DB (pooler) | postgres 자격으로 DDL 적용 — 자격 문자열 유출 금지 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02 | Spoofing | booking_clicks INSERT 정책 | high | mitigate | `with check (user_id = auth.uid() and can_read_trip(trip_id))` — 타인 명의·비멤버 trip 클릭 위조 구조적 차단. 매트릭스 케이스 B/C가 검증 |
| T-20-03 | Info Disclosure | booking_clicks/checklist SELECT | medium | mitigate | can_read_trip 게이트 — 비멤버 0행. 클릭 토큰은 opaque(PII 없음, 17-02 설계). 매트릭스 D |
| T-20-05 | Elevation | booking_checklist_items write | high | mitigate | can_edit_trip 3종 정책 (D-12 편집권한 멤버만). 매트릭스 E |
| T-20-07 | Tampering | 42P17 재귀 (cross-table 정책) | medium | mitigate | 전 정책 SECURITY DEFINER 헬퍼 직호출 — 직접 EXISTS 0건 grep 게이트 |
| T-20-08 | Info Disclosure | pooler connection string | high | mitigate | 세션 환경변수만, 파일/커밋 금지 acceptance 게이트, 작업 후 폐기 |
| T-20-09 | DoS | 멤버의 자기 명의 클릭 스팸 INSERT | low | accept | booking_clicks는 분석·'확인함' 용도(돈 경로 아님, 수익 원천은 TP 서버측 통계) — rate-limit 미도입 (19 A4 선례와 동일 판단) |
</threat_model>

<verification>
- 라이브 DB: booking_checklist_items 존재 + RLS enabled + 정책 6종
- database.ts 재생성 diff가 booking 관련 추가만 포함 (무관 테이블 diff 0)
</verification>

<success_criteria>
- 0021이 append-only로 커밋되고 라이브 DB에 적용됨 — 이후 wave의 INSERT/SELECT가 42501/42P01 없이 동작할 데이터 기반 확보
- RLS 매트릭스가 T-20-02/03/05를 실증
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-02-SUMMARY.md` when done
</output>
