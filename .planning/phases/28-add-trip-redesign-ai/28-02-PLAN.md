---
phase: 28-add-trip-redesign-ai
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/select-pill.tsx
  - apps/web/components/index.ts
  - apps/web/app/onboarding/_components/duration-pills.tsx
  - apps/web/lib/marker-svg.ts
  - apps/web/__tests__/select-pill.test.tsx
  - apps/web/__tests__/duration-pills.test.tsx
  - apps/web/__tests__/marker-svg.test.ts
autonomous: true
requirements: [SC-1, SC-6]

must_haves:
  truths:
    - "선택지 pill이 레퍼런스와 동일한 시각(비선택=연회색 채움 / 선택=흰 배경+파란 테두리+파란 텍스트)으로 렌더된다"
    - "기간 pill 6종이 한 벌로 존재하고 위저드·기간 게이트 시트가 같은 컴포넌트를 쓴다"
    - "지도 마커가 번호 라벨을 그릴 수 있고, 라벨 미전달 시 기존 출력이 바이트 단위로 동일하다"
  artifacts:
    - apps/web/components/select-pill.tsx
    - apps/web/app/onboarding/_components/duration-pills.tsx
    - "apps/web/lib/marker-svg.ts — buildMarkerIconUrl label 옵션"
  key_links:
    - "DurationPills ↔ SelectPill (pill 시각 단일 출처)"
    - "DurationPills ↔ step-dates(28-04) + duration-gate-sheet(28-05) — 한 벌 공유"
    - "buildMarkerIconUrl(label) ↔ moa-map 번호 핀(28-06)"
---

<objective>
28-04(위저드)·28-05(기간 게이트)·28-06(번호 핀)이 탐색 없이 import할 **순수 프레젠테이션 계약 3종**을 먼저 잠근다. 상태·mutation·DB 접촉 0 — 전부 props-driven이라 부모가 누구든 동일하게 동작한다.

Purpose: pill 시각(D-04)과 기간 옵션(D-06)의 단일 출처를 만들어 위저드와 D-13 게이트가 **한 벌을 공유**하게 한다(CONTEXT code_context 명시). 마커 번호 라벨은 D-16 Day 필터 지도의 전제다.
Output: `SelectPill` · `DurationPills` · `buildMarkerIconUrl`의 additive `label` 옵션.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md
@.planning/phases/28-add-trip-redesign-ai/28-PATTERNS.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
| 심볼 | 위치 | 소비자 |
|---|---|---|
| `SelectPill` (+ `SelectPillProps`) | `apps/web/components/select-pill.tsx` (배럴 export) | step-where·step-who·DurationPills(28-04) · DaySelectSheet(28-05) |
| `DurationPills` (+ `DURATION_OPTIONS`) | `apps/web/app/onboarding/_components/duration-pills.tsx` | step-dates(28-04) · DurationGateSheet(28-05) |
| `buildMarkerIconUrl({ label })` | `apps/web/lib/marker-svg.ts` (additive) | moa-map 번호 핀(28-06) |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SelectPill 신규 컴포넌트 (D-04 · UI-SPEC A-2)</name>
  <files>apps/web/components/select-pill.tsx, apps/web/components/index.ts, apps/web/__tests__/select-pill.test.tsx</files>

  <read_first>
    - `apps/web/components/chip.tsx` — **anatomy 참조만. 확장·수정 금지**(A-2: Chip은 12px/500 소형이라 대형 pill과 anatomy가 다름. 변형을 얹으면 기존 사용처가 회귀한다)
    - `apps/web/components/button.tsx` — 포커스 링 idiom(`focus-visible:outline-*`)을 여기서 복사
    - `apps/web/components/index.ts` — 배럴 export 패턴
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §SelectPill 상태표 · §Color(Accent reserved 3번) · §Accessibility Contract
  </read_first>

  <behavior>
    - selected=false → aria-pressed="false", 연회색 채움 클래스, 투명 테두리
    - selected=true → aria-pressed="true", 흰 배경 + brand 테두리 + brand 텍스트 클래스
    - onClick 콜백이 정확히 1회 발화한다
    - 두 상태 모두 border-2를 갖는다 (선택 시 레이아웃 시프트 0)
  </behavior>

  <action>
    `apps/web/components/select-pill.tsx` 신규 생성. `Chip`을 확장하지 말고 **독립 컴포넌트**로 만든다(A-2).

    계약:
    - props: `selected: boolean` · 나머지는 네이티브 `<button>` 속성 passthrough (`ButtonHTMLAttributes`) + `className` 병합. `cn()` 유틸은 `chip.tsx`가 쓰는 것과 동일한 것을 import.
    - 루트는 `<button type={type ?? 'button'} aria-pressed={selected}>`. **`aria-selected`가 아니라 `aria-pressed`** — Chip과 다른 접근성 롤이다(토글 버튼).
    - 크기·형태: `min-h-14`(56px — 터치 타깃 44px 상회) · `rounded-full` · `border-2` · `w-full`(2열 그리드 셀을 채움) · 중앙 정렬 · `text-base font-semibold`.
    - 상태 클래스(UI-SPEC 상태표 verbatim):
      - unselected → `bg-neutral-100 border-transparent text-neutral-600`
      - selected → `bg-white border-brand-500 text-brand-600`
      - 텍스트는 `brand-600`, 테두리는 `brand-500`이다(A-3 — brand-500 텍스트는 흰 배경 대비 3.72:1로 WCAG AA 미달, 테두리 같은 비텍스트에만 허용).
    - 전환: `transition-colors duration-150 ease-out` + 누름 피드백 `active:scale-[0.98]` (모션 예산 내 신규 허용분).
    - 포커스: `button.tsx`의 포커스 링 클래스 3종을 그대로 복사.
    - **색은 Tailwind 토큰 클래스만.** 색상 리터럴을 직접 쓰지 않는다(HC-2).

    `apps/web/components/index.ts`에 `export { SelectPill } from './select-pill';` + `export type { SelectPillProps } ...` 추가(기존 배럴 라인 무수정).

    테스트는 `apps/web/__tests__/select-pill.test.tsx`에 생성 — **반드시 이 디렉토리**(vitest include 글롭이 co-located를 못 잡는다, 19-04/25-02 실측).
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/select-pill.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/select-pill.test.tsx` → 4케이스 그린
    - `grep -c 'aria-pressed' apps/web/components/select-pill.tsx` → 1 이상
    - `grep -c 'min-h-14' apps/web/components/select-pill.tsx` → 1
    - `grep -c 'border-brand-500' apps/web/components/select-pill.tsx` → 1
    - `grep -c 'text-brand-600' apps/web/components/select-pill.tsx` → 1
    - `grep -Ec '#[0-9a-fA-F]{3,8}\b' apps/web/components/select-pill.tsx` → **0** (신규 hex 리터럴 0, HC-2)
    - `git diff --stat -- apps/web/components/chip.tsx` → **빈 출력** (Chip 무수정, A-2)
    - `grep -c 'SelectPill' apps/web/components/index.ts` → 1 이상
  </acceptance_criteria>

  <done>SelectPill이 배럴에서 export되고, 두 상태가 aria-pressed + 토큰 클래스로 구분되며, chip.tsx diff가 0이고 hex 리터럴이 0이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DurationPills — 기간 pill 6종 한 벌 (D-06)</name>
  <files>apps/web/app/onboarding/_components/duration-pills.tsx, apps/web/__tests__/duration-pills.test.tsx</files>

  <read_first>
    - `apps/web/app/onboarding/_components/step-who.tsx` L11-37 — 상수 배열 맵 렌더 + 단일 선택 analog (COMPANION_PRESETS)
    - 이 플랜 Task 1의 `SelectPill`
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Screen Contracts 2(기간 pill) · §Copywriting Contract(기간 pill 라벨)
  </read_first>

  <behavior>
    - 6개 pill이 렌더된다: 당일치기 · 1박 2일 · 2박 3일 · 3박 4일 · 4박 5일 · 5박 6일
    - '2박 3일' 클릭 시 onChange(3)이 호출된다 (라벨→dayCount 매핑: 당일치기=1, N박=N+1)
    - value=4일 때 '3박 4일' pill만 aria-pressed="true"이고 나머지 5개는 "false"
    - value=null이면 어떤 pill도 눌린 상태가 아니다
  </behavior>

  <action>
    `apps/web/app/onboarding/_components/duration-pills.tsx` 신규 생성. **한 벌 구현** — step-dates(28-04)와 DurationGateSheet(28-05)가 이 컴포넌트를 공유한다. 시트 쪽에 pill을 다시 만들지 않는다(CONTEXT code_context 명시).

    - 상수 `DURATION_OPTIONS`를 `as const` 배열로 export: `{ label: '당일치기', dayCount: 1 }` · `{ label: '1박 2일', dayCount: 2 }` · `{ label: '2박 3일', dayCount: 3 }` · `{ label: '3박 4일', dayCount: 4 }` · `{ label: '4박 5일', dayCount: 5 }` · `{ label: '5박 6일', dayCount: 6 }`. 라벨은 레퍼런스 표기 그대로 — 박/일 사이 **공백 포함**.
    - props: `value: number | null` · `onChange: (dayCount: number) => void`. **상태를 소유하지 않는다** — 부모(page.tsx 또는 게이트 시트)가 소유(Phase 24 D-02 구조 유지).
    - 레이아웃: `grid grid-cols-2 gap-2` (D-04 2열). 각 셀은 `SelectPill`, `selected={value === opt.dayCount}`.
    - DB·api·supabase import 0. 순수 프레젠테이션.

    테스트는 `apps/web/__tests__/duration-pills.test.tsx`.
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/duration-pills.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/duration-pills.test.tsx` → 4케이스 그린
    - `grep -c 'DURATION_OPTIONS' apps/web/app/onboarding/_components/duration-pills.tsx` → 1 이상 (export됨)
    - `grep -c 'grid-cols-2' apps/web/app/onboarding/_components/duration-pills.tsx` → 1
    - `grep -c '5박 6일' apps/web/app/onboarding/_components/duration-pills.tsx` → 1
    - `grep -Ec '@moajoa/api|getSupabaseBrowser|useState' apps/web/app/onboarding/_components/duration-pills.tsx` → **0** (상태·DB 미소유)
    - `grep -Ec '#[0-9a-fA-F]{3,8}\b' apps/web/app/onboarding/_components/duration-pills.tsx` → 0
  </acceptance_criteria>

  <done>DurationPills가 6개 옵션을 2열로 렌더하고 dayCount를 콜백하며, 상태·DB를 소유하지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: marker-svg label 옵션 (D-16 번호 핀 전제 · HC-6)</name>
  <files>apps/web/lib/marker-svg.ts, apps/web/__tests__/marker-svg.test.ts</files>

  <read_first>
    - `apps/web/lib/marker-svg.ts` 전문 — 특히 `fill?` 옵션의 additive 확장 선례(24-02: "미전달 시 출력 바이트 동일")와 `showQ` 물음표 배지 분기
    - `apps/web/__tests__/marker-svg.test.ts` — 기존 10케이스(8 + fill 2)
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Component Inventory > Modify > marker-svg
  </read_first>

  <behavior>
    - label 미전달 시 출력 문자열이 기존과 완전히 동일하다 (기존 10케이스 무회귀 + 바이트 동일성 단언)
    - label: 3 전달 시 SVG에 숫자 3이 흰색 text 요소로 들어간다
    - label과 물음표 배지 조건이 동시 성립하면 label이 우선하고 물음표는 렌더되지 않는다
    - label은 number 타입만 받는다 (문자열 전달은 타입 레벨에서 불가)
  </behavior>

  <action>
    `apps/web/lib/marker-svg.ts`의 `buildMarkerIconUrl` input 타입에 **additive optional** `label?: number` 추가. `fill?` 확장(24-02) 선례를 그대로 미러한다.

    - 라벨 렌더: 기존 물음표 배지 분기와 **같은 위치**에 `<text>` 요소를 삽입하되 조건을 `typeof input.label === 'number'`로 두고, 물음표 분기보다 **먼저** 평가해 label 우선을 성립시킨다. 폰트 크기·굵기·앵커는 기존 물음표 `<text>`의 속성을 기준으로 숫자 가독성에 맞게 잡되, 색은 흰색으로 기존 배지와 동일.
    - **HC-6 (인젝션 계약, T-24-04 승계):** 삽입값은 반드시 `String(input.label)`만. 사용자 문자열·템플릿 변수·장소명은 SVG에 **절대 삽입 금지**. label의 타입이 `number`인 것 자체가 이 계약의 강제 수단이다.
    - `label` 미전달 시 반환 문자열이 **기존과 바이트 단위로 동일**해야 한다 — 조건부 문자열 결합이 빈 문자열을 만들도록(기존 `showQ` 분기와 동일 형태).

    `apps/web/__tests__/marker-svg.test.ts`에 위 `<behavior>` 케이스를 append. 기존 케이스는 **무수정**(회귀 앵커).
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/marker-svg.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/marker-svg.test.ts` → 기존 케이스 전부 + 신규 3케이스 그린
    - `grep -c 'label?: number' apps/web/lib/marker-svg.ts` → 1
    - `grep -c 'String(input.label)' apps/web/lib/marker-svg.ts` → 1
    - `git diff -- apps/web/__tests__/marker-svg.test.ts | grep -c '^-[^-]'` → 0 (기존 테스트 삭제 라인 0)
    - `pnpm --filter @moajoa/web typecheck` → exit 0
  </acceptance_criteria>

  <done>buildMarkerIconUrl이 number label을 SVG 숫자 배지로 렌더하고, 미전달 시 기존 출력과 동일하며, 사용자 문자열 삽입 경로가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 데이터 → SVG data URI | `buildMarkerIconUrl`이 만든 문자열이 `<img src>`로 브라우저에 파싱된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-05 | Injection (XSS via SVG) | `buildMarkerIconUrl` | high | mitigate | `label`의 타입을 `number`로 고정하고 `String(n)`으로만 삽입. 장소명·사용자 입력 문자열은 SVG에 삽입하지 않는다(T-24-04 계약 승계). acceptance가 `String(input.label)` 존재를 grep으로 강제 |
| T-28-06 | Tampering | `Chip` 컴포넌트 (기존 사용처 회귀) | low | mitigate | SelectPill을 독립 컴포넌트로 만들어 Chip diff 0 — acceptance가 `git diff --stat` 빈 출력을 강제 |
| T-28-07 | — | SelectPill·DurationPills | low | accept | 상태·DB·네트워크 접촉 0인 순수 프레젠테이션. 공격면 없음 (acceptance가 api/supabase import 0을 grep으로 강제) |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web test -- --run` 전 스위트 그린 (기존 케이스 무회귀)
- `pnpm --filter @moajoa/web typecheck` exit 0
- `git diff --stat -- apps/ios apps/web/components/chip.tsx apps/web/components/add-content-tabs.tsx` → 빈 출력
- 신규 파일 3종에 hex 색 리터럴 0
</verification>

<success_criteria>
- [ ] SC-1(부분): 레퍼런스 pill 시각(선택/비선택)이 재사용 가능한 컴포넌트로 존재한다
- [ ] SC-6: iOS diff 0
- [ ] 28-04·28-05·28-06이 import할 3개 계약이 잠겼다
</success_criteria>

<output>
Create `.planning/phases/28-add-trip-redesign-ai/28-02-SUMMARY.md` when done
</output>
