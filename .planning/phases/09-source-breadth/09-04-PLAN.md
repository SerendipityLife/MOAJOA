---
phase: 09-source-breadth
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/boards/[id]/_components/add-link-form.tsx
autonomous: true
requirements: [SRC-01, SRC-02]
must_haves:
  truths:
    - "The web dev-tool add-link form triggers extraction for blog and instagram links (not only youtube)"
    - "The hint copy no longer says blog/insta go to a 큐레이션 대기열 — it says they are auto-analyzed"
    - "The form remains a dev tool gated by isDevToolsEnabled (no new create UI shipped to production)"
  artifacts:
    - path: "apps/web/app/boards/[id]/_components/add-link-form.tsx"
      provides: "Broadened trigger gate youtube → youtube|blog|instagram"
      contains: "triggerExtraction"
  key_links:
    - from: "add-link-form.tsx submit()"
      to: "triggerExtraction (Edge Function extract-youtube)"
      via: "detectSourceKind-based trigger for blog/insta"
      pattern: "triggerExtraction"
---

<objective>
Broaden the web dev-tool's add-link trigger so blog and instagram links also fire
`triggerExtraction` (today only youtube does; blog/insta just save with a "큐레이션 대기열"
notice). Update the hint copy accordingly. This is display/trigger-only on an existing
dev-tool form — NO new create UI (CLAUDE.md §5: web add-link is a DEV TOOL gated by
`isDevToolsEnabled`; we are not adding a new form).

Purpose: lets the developer fire blog/insta auto-extraction from the web dev tool to test
the Plan 03 router without iOS. iOS trigger broadening + live verification is the separate
morning gate (Plan 05).

Output: edited add-link-form.tsx. File-disjoint from the Edge Function plans (parallel-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-source-breadth/09-CONTEXT.md
@apps/web/app/boards/[id]/_components/add-link-form.tsx

<interfaces>
<!-- Current form. The youtube-only branch in submit() and the hint copy are the edit sites. -->
```typescript
// submit(): currently
if (link.source_kind === 'youtube') { triggerExtraction(...); toast('분석 중...') } else { toast('링크를 추가했어요.') }
// onChange(): hint says blog/insta → '큐레이션 대기열에 추가됩니다.'
// detectSourceKind(value) → 'youtube' | 'blog' | 'instagram' | null   (@moajoa/core)
// isDevToolsEnabled() gate at top stays — do NOT remove
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden trigger gate + update hint copy</name>
  <read_first>
    - apps/web/app/boards/[id]/_components/add-link-form.tsx (FULL — submit() youtube branch + onChange() hint)
    - 09-CONTEXT.md §"Implementation Decisions" E (web dev-tool trigger expansion, autonomous, reversible)
    - packages/core/src/schemas/link.ts (detectSourceKind return values)
  </read_first>
  <files>apps/web/app/boards/[id]/_components/add-link-form.tsx</files>
  <action>
    Surgical edit (Karpathy §3.3 — keep `isDevToolsEnabled()` gate, keep form structure):

    1. In `submit()`, change the trigger condition from `link.source_kind === 'youtube'` to fire for youtube, blog, AND instagram. Cleanest: trigger when `link.source_kind === 'youtube' || link.source_kind === 'blog' || link.source_kind === 'instagram'` (i.e. any auto-extractable kind; equivalently `link.source_kind !== 'manual'`). On trigger, keep the existing fire-and-forget `triggerExtraction(client, link.id).catch(...)` + success toast "링크를 추가했어요 — 장소를 분석 중이에요." The `else` (manual/unknown) keeps "링크를 추가했어요."
       - Note: instagram will resolve to `extraction_status='failed'` server-side (Plan 01 graceful fail); that's expected — the failure reason surfaces via existing status UI. The trigger still fires so the developer can observe the explicit failure path.

    2. In `onChange()`, update the hint copy so blog/insta no longer say "큐레이션 대기열에 추가됩니다." Instead:
       - blog → '블로그 — 본문을 분석해 장소를 추출합니다.'
       - instagram → '인스타 — 분석을 시도합니다 (캡션 추출이 제한될 수 있어요).'
       - youtube → unchanged '유튜브 — 분석을 자동 시작합니다.'
       - null → unchanged '링크 인식 불가 — 그래도 저장 가능합니다.'

    Do not add new inputs, buttons, or screens. Do not remove the dev-tool gate.
  </action>
  <acceptance_criteria>
    - `grep -q "source_kind === 'blog'\|source_kind !== 'manual'" apps/web/app/boards/[id]/_components/add-link-form.tsx` succeeds (blog now triggers).
    - The form no longer routes blog to a queue: `! grep -q "큐레이션 대기열" apps/web/app/boards/[id]/_components/add-link-form.tsx`.
    - `isDevToolsEnabled` gate still present: `grep -q "isDevToolsEnabled" apps/web/app/boards/[id]/_components/add-link-form.tsx`.
    - `pnpm --filter web typecheck` (or `pnpm --filter @moajoa/web exec tsc --noEmit`) exits 0.
    - `pnpm --filter web build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/wcb/Documents/MOAJOA && pnpm --filter web build</automated>
  </verify>
  <done>Web dev-tool triggers extraction for youtube/blog/instagram; hint copy reflects auto-analysis (no 큐레이션 대기열); dev-tool gate intact; typecheck + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web client → Edge Function | The dev tool fires `triggerExtraction` (invoke). Gated behind `isDevToolsEnabled` (not shipped to production main) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Elevation / scope creep (production exposure) | add-link-form trigger | mitigate | `isDevToolsEnabled()` double-gate retained (page redirect + component null per WEB-01/02); production main keeps the form hidden. Verifiable: `grep isDevToolsEnabled add-link-form.tsx`. |
| T-09-01 | SSRF (downstream) | n/a (client) | transfer | Actual fetch happens server-side; SSRF mitigated by Plan 01/03 (`assertFetchableUrl` + router allowlist). Client only passes a URL already Zod-validated at `addLink` insert. |
</threat_model>

<verification>
- `pnpm --filter web build` green; `pnpm --filter web typecheck` green.
- Dev-tool gate (`isDevToolsEnabled`) intact; no new UI surface.
- No `.js` workspace import extensions changed.
</verification>

<success_criteria>
- blog + instagram links trigger extraction from the web dev tool.
- Hint copy reflects auto-analysis; no "큐레이션 대기열" wording remains.
- Form stays a dev tool; typecheck + build green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-source-breadth/09-04-SUMMARY.md`
</output>
