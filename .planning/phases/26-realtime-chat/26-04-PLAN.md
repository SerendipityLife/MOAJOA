---
phase: 26-realtime-chat
plan: 04
type: execute
wave: 3
depends_on: [26-02, 26-03]
files_modified:
  - apps/web/app/moa/[id]/_components/place-list.tsx
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/__tests__/moa-island.test.tsx
autonomous: true
requirements: [CHAT-03]

must_haves:
  truths:
    - "Tapping a place row's 답장 switches to the 채팅 tab with reply_to_place_id prefilled"
    - "Sending that message writes reply_to_place_id and renders a #N 장소명 chip on the bubble"
    - "Tapping the chip switches to the 모으기 tab, scrolls to the place, and highlights it"
    - "A deleted place's chip silently disappears (reply_to_place_id set null)"
  artifacts:
    - path: "apps/web/app/moa/[id]/_components/place-list.tsx"
      provides: "답장 button → onReply(placeId) callback (replaces the toast stub)"
      contains: "onReply"
    - path: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      provides: "reply prefill from place-list + openPlaceFromChat chip nav (setActiveTab('moa')+openPlaceId+expanded)"
      contains: "openPlaceFromChat"
  key_links:
    - from: "place-list 답장 button"
      to: "island reply prefill"
      via: "onReply(p.id) → setReplyToPlaceId + setActiveTab('chat')"
      pattern: "onReply\\("
    - from: "moa-chat #N chip"
      to: "island openPlaceFromChat"
      via: "onChipTap → setActiveTab('moa') + setOpenPlaceId + expanded"
      pattern: "openPlaceFromChat"
---

<objective>
Close CHAT-03: the place-mention reply loop. The 답장 button on a place row jumps to the chat with that place prefilled as the reply target; the sent message carries a `#N 장소명` quote chip; tapping the chip jumps back to the map, scrolls to the place, and highlights it.

Purpose: D-10 — reuse the existing `openPlaceId` controlled path (place-list already has the scrollIntoView effect at L63-68 and rows carry `data-place-id`). The 답장 stub (place-list L218-229, currently a `toast('채팅은 곧 열려요')`) becomes the reply entry point. The chip round-trip reuses the same `openPlaceId` + `sheetAnchor` machinery the marker-tap path uses (moa-island L178-181).

Output: place-list `답장` callback, island reply-prefill + chip-nav wiring, extended island tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-realtime-chat/26-CONTEXT.md
@.planning/phases/26-realtime-chat/26-RESEARCH.md
@.planning/phases/26-realtime-chat/26-PATTERNS.md
@.planning/phases/26-realtime-chat/26-03-SUMMARY.md

Files to edit:
@apps/web/app/moa/[id]/_components/place-list.tsx
@apps/web/app/moa/[id]/_components/moa-island.tsx
@apps/web/__tests__/moa-island.test.tsx

<interfaces>
<!-- Existing openPlaceId path to REUSE (do not reinvent) -->
place-list.tsx L62-68: `useEffect(() => { if (!openPlaceId) return; document.querySelector('[data-place-id="${openPlaceId}"]')?.scrollIntoView({behavior:'smooth', block:'nearest'}) }, [openPlaceId])`.
place-list.tsx L141: each row has `data-place-id={p.id}`.
place-list.tsx L218-229: the current 답장 stub button (toast) to replace.
moa-island.tsx onMarkerTap (L178-181): `setOpenPlaceId(id); setSheetAnchor('expanded')` — the nav body openPlaceFromChat reuses.

<!-- From Plan 02 MoaChat contract (already built) -->
onChipTap: (placeId: string) => void;   // island supplies openPlaceFromChat
placesById: Record<string, { seqNo: number; name: string }>;  // island already passes (Plan 03)

<!-- From Plan 03 island state (already present) -->
const [activeTab, setActiveTab] = useState<'moa'|'chat'>('moa');
const [replyToPlaceId, setReplyToPlaceId] = useState<string | null>(null);
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: place-list.tsx — 답장 button → onReply callback</name>
  <files>apps/web/app/moa/[id]/_components/place-list.tsx</files>
  <action>
Replace the 답장 stub (L218-229) with a real callback (D-10). Surgical change only (Karpathy 3.3 — do not touch adjacent rows):
1. Add `onReply: (placeId: string) => void;` to `PlaceListProps` (L11-29) and to the destructured params (L46-59).
2. Rewrite the 답장 button: remove the `toast('채팅은 곧 열려요')` + `aria-disabled` + `opacity-50` stub; make it an enabled button whose onClick is `e.stopPropagation(); onReply(p.id);`. Keep it in the accordion (A-4 position, after the source link, before 삭제). Keep the label `답장` and the base text styling (drop the `opacity-50`).
3. If the `useToast` import becomes unused after removing the stub toast, remove that orphaned import (3.3 — clean up only what your change orphans; check whether any other place-list code still uses `toast` first — it does NOT currently, so remove `const { toast } = useToast();` and the import if nothing else references it).
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- place-list && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>답장 button calls `onReply(p.id)` (no toast stub, not disabled); PlaceListProps has `onReply`; existing place-list tests still pass; web typecheck exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: moa-island.tsx — reply prefill + chip-nav wiring</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx</files>
  <action>
Wire the two directions of the mention loop using state already declared in Plan 03 (`replyToPlaceId`, `activeTab`, `openPlaceId`, `sheetAnchor`):

1. **답장 → chat prefill** — pass `onReply` to `<PlaceList>`:
```typescript
onReply={(placeId) => { setReplyToPlaceId(placeId); setActiveTab('chat'); }}
```
2. **chip → map nav** — add `openPlaceFromChat` (mirrors onMarkerTap L178-181 + switches tab) and pass it as MoaChat's `onChipTap` (replacing the Plan 03 no-op placeholder):
```typescript
const openPlaceFromChat = (placeId: string) => {
  setActiveTab('moa');
  setOpenPlaceId(placeId);
  setSheetAnchor('expanded');
};
```
Set `onChipTap={openPlaceFromChat}` on `<MoaChat>`.
3. **Highlight (D-10 "하이라이트")** — the row already scrolls via place-list's openPlaceId effect. Add a transient highlight: keep it minimal — the opened row (`isOpen` accordion) already visually distinguishes; if a stronger cue is wanted, add a short-lived ring on the opened row keyed off `openPlaceId` (Claude's discretion — a `ring-2 ring-brand-300` that clears after ~1.5s is acceptable; do not over-build). Scroll + expand is the required behavior; the ring is the discretionary enhancement.

Deleted-place safety (Pitfall 9): no island change needed — 0025 FK is `on delete set null`, and MoaChat already skips a chip when `reply_to_place_id` is unresolvable in `placesById`. Confirm `placesById` (Plan 03) only contains non-hidden places (it is built from `places` state, which is hidden-filtered) so a removed place's chip vanishes.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- moa-island && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>`onReply` prefills reply + switches to chat; `openPlaceFromChat` switches to 모으기 + sets openPlaceId + expanded; wired to PlaceList/MoaChat; existing tests pass; typecheck exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: extend moa-island.test.tsx — mention loop</name>
  <files>apps/web/__tests__/moa-island.test.tsx</files>
  <action>
Add mention-loop cases (keep all prior tests green). Ensure the PlaceList is not fully mocked away (the existing island test renders the real PlaceList via the sheet — confirm; if PlaceList is stubbed, extend the stub to expose a 답장 button calling `onReply('p1')`). Extend the MoaChat stub (from Plan 03) to render a chip button calling `onChipTap('p1')` when given a message with `reply_to_place_id`.

1. **답장 → chat + prefill**: open p1's accordion (marker tap), click 답장 → assert the chat view is active (MoaChat stub visible) and the stub received `replyToPlaceId==='p1'`.
2. **chip → 모으기 + openPlaceId**: seed a message `{ id:'m1', reply_to_place_id:'p1', ... }` (via the trip_messages INSERT cb or initialMessages prop), switch to chat, click the chip → assert the 모으기 view is active and p1's row has `aria-expanded=true` (reuses the existing Test 5 aria-expanded assertion idiom).
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- moa-island</automated>
  </verify>
  <done>New cases green: 답장 flips to chat with prefilled reply target; chip tap flips to 모으기 and expands the referenced place row. Full moa-island suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chip render ← reply_to_place_id | chip resolves against island `placesById` (RLS-gated places); no new external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Information disclosure | #N chip for a deleted/hidden place | mitigate | 0025 FK `on delete set null` + placesById built from hidden-filtered places → unresolvable chip renders nothing (Pitfall 9) |
| T-26-10 | Tampering | reply_to_place_id in send | accept | validated as uuid-or-null by TripMessageCreateSchema (Plan 03 handleSend); a stale id simply yields no chip |
</threat_model>

<verification>
- `pnpm --filter web test:run` green (place-list + moa-island incl. new cases); web `tsc --noEmit` exit 0.
- `grep "채팅은 곧 열려요" apps/web` → 0 matches (stub removed).
- `grep "openPlaceFromChat" moa-island.tsx` present; `grep "onReply" place-list.tsx moa-island.tsx` present.
- No `.js` workspace imports; iOS untouched.
</verification>

<success_criteria>
- 답장 → 채팅 tab with reply target prefilled; sent message carries reply_to_place_id + renders #N chip.
- Chip tap → 모으기 tab + scroll + highlight of the referenced place (reuses openPlaceId path).
- Deleted-place chip vanishes; all web tests + typecheck green.
</success_criteria>

<output>
After completion, create `.planning/phases/26-realtime-chat/26-04-SUMMARY.md`.
</output>
