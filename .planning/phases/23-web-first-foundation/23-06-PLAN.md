---
phase: 23-web-first-foundation
plan: 06
type: execute
wave: 4
depends_on: ["23-04", "23-05"]
files_modified:
  - packages/api/src/queries/memberships.ts
  - packages/api/src/queries/memberships.test.ts
  - packages/api/src/queries/trips.ts
  - packages/api/src/queries/trips.test.ts
autonomous: true
requirements: [SHARE-01, SHARE-03] # api seam 기반 — e2e 검증은 Phase 24/25에 매핑
must_haves:
  truths:
    - "joinMoa가 join_moa RPC를 house 계약(첫 인자 client, error throw)으로 감싼다"
    - "shareMoa가 visibility='shared' + share_mode를 한 UPDATE로 설정하고 slug를 반환한다 (slug 생성은 0016 트리거 몫)"
    - "기존 joinSharedTrip·shareTrip은 무수정 (iOS 동결)"
  artifacts:
    - path: packages/api/src/queries/memberships.ts
      provides: "joinMoa(client, shareSlug) → trip_id"
      contains: "join_moa"
    - path: packages/api/src/queries/trips.ts
      provides: "shareMoa(client, tripId, shareMode) → share_slug"
      contains: "shareMoa"
  key_links:
    - from: packages/api/src/queries/memberships.ts
      to: "0025 join_moa RPC"
      via: "client.rpc('join_moa', { p_share_slug })"
      pattern: "rpc\\('join_moa'"
    - from: packages/api/src/queries/trips.ts
      to: "0016 ensure_share_slug 트리거"
      via: "visibility 'shared' UPDATE가 slug 생성 유발"
      pattern: "visibility: 'shared'"
---

<objective>
Phase 24·25가 호출할 typed query 2개를 잠근다: `joinMoa`(joinSharedTrip 미러 — RPC만 교체) + `shareMoa`(shareTrip 확장 — share_mode 동시 설정). TDD, ledger.test.ts mock chain 패턴.

Purpose: 웹 클라이언트가 RPC 이름·payload를 직접 알지 않게 하는 유일한 typed 경로 (기존 컨벤션).
Output: api 쿼리 2개 + 테스트, `pnpm --filter @moajoa/api test` 그린.

**Open Q3 결정 (기록 — 저비용 변경 가능):** share_mode **변경 허용** — shareMoa는 이미 공유된 모아에 재호출돼도 share_mode를 갱신한다 (shareTrip의 early-return을 제거한 단일 UPDATE 시맨틱). 날짜 확정 모아의 'dates' 숨김은 클라이언트 몫 (Phase 24 UI) — DB는 3값 CHECK만. 기존 멤버 role 자동 승격은 없음 (D-A4, join_moa의 on conflict do nothing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-web-first-foundation/23-PATTERNS.md

<interfaces>
<!-- 기존 house 계약 (PATTERNS 검증) — 미러 원본 -->
// memberships.ts L8~15
export async function joinSharedTrip(client: MoajoaSupabaseClient, shareSlug: string): Promise<string> {
  const { data, error } = await client.rpc('join_shared_trip', { p_share_slug: shareSlug });
  if (error) throw error;
  return data as string;
}

// trips.ts shareTrip — visibility 'shared' UPDATE 후 .select('share_slug').single(),
// slug 없으면 throw new Error('share_slug not generated')

<!-- 23-05 산출물 — shareMoa 시그니처가 import -->
import type { ShareModeType } from '@moajoa/core'; // ['dates','places','both']

<!-- api 테스트 mock 패턴 (ledger.test.ts L29~52) -->
makeChain(result): 모든 query-builder 메서드가 chain 반환 + thenable, .single() → Promise.resolve(result)
makeClient(): { from: vi.fn(() => chain), auth: { getUser } } as unknown as MoajoaSupabaseClient
→ rpc 테스트는 client에 rpc: vi.fn(() => Promise.resolve({ data, error })) 추가
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: joinMoa 쿼리 + 테스트 (TDD)</name>
  <files>packages/api/src/queries/memberships.ts, packages/api/src/queries/memberships.test.ts</files>
  <read_first>
    - packages/api/src/queries/memberships.ts (joinSharedTrip — 미러 원본, 무수정 보존 대상)
    - packages/api/src/queries/ledger.test.ts (mock client·uuid 픽스처 idiom — memberships.test.ts가 미러)
  </read_first>
  <behavior>
    - joinMoa(client, 'slug123') 호출 시 `client.rpc`가 `('join_moa', { p_share_slug: 'slug123' })`로 호출됨
    - rpc가 { data: TRIP_UUID, error: null } 반환 → joinMoa가 TRIP_UUID(string) 반환
    - rpc가 { data: null, error: {...} } 반환 → throw (rejects)
  </behavior>
  <action>
RED→GREEN. `memberships.test.ts` 신규 작성(behavior 3케이스, ledger.test.ts의 uuid 픽스처 상수 idiom + `rpc: vi.fn()` mock) 후 구현:

```typescript
/**
 * Self-join a moa by its share slug. Idempotent (already a member = no-op — no
 * role promotion, D-A4). Backed by the SECURITY DEFINER join_moa RPC (0025):
 * role is decided server-side by share_mode (places/both → editor, dates/null
 * → voter) and user_id = auth.uid() — the caller can only join as themselves.
 * Anonymous sessions work: they carry the authenticated role.
 */
export async function joinMoa(
  client: MoajoaSupabaseClient,
  shareSlug: string,
): Promise<string> {
  const { data, error } = await client.rpc('join_moa', { p_share_slug: shareSlug });
  if (error) throw error;
  return data as string;
}
```

기존 `joinSharedTrip`은 한 글자도 수정하지 않는다 (iOS 동결).
  </action>
  <acceptance_criteria>
    - `grep -q "rpc('join_moa'" packages/api/src/queries/memberships.ts` 매치
    - joinSharedTrip diff 0 (`git diff packages/api/src/queries/memberships.ts`에 기존 함수 본문 변경 없음)
    - memberships.test.ts에 `join_moa` + `p_share_slug` + rejects 케이스 존재
    - `pnpm --filter @moajoa/api test` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @moajoa/api test && grep -q "rpc('join_moa'" packages/api/src/queries/memberships.ts</automated>
  </verify>
  <done>Phase 25 닉네임 게이트 → 익명 인증 → joinMoa 흐름의 api seam이 테스트 가드와 함께 잠김.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shareMoa 쿼리 + 테스트 (TDD)</name>
  <files>packages/api/src/queries/trips.ts, packages/api/src/queries/trips.test.ts</files>
  <read_first>
    - packages/api/src/queries/trips.ts (shareTrip L123~153 — 미러 원본, 무수정 보존 대상; import 블록 스타일)
    - packages/api/src/queries/ledger.test.ts (mock chain — trips.test.ts가 미러)
  </read_first>
  <behavior>
    - shareMoa(client, TRIP, 'both') 호출 시 `from('trips')` → `.update({ visibility: 'shared', share_mode: 'both' })` → `.eq('id', TRIP)` → `.select('share_slug')` → `.single()` 체인 호출
    - single이 { data: { share_slug: 'abc123' }, error: null } → 'abc123' 반환
    - share_slug null → throw new Error('share_slug not generated')
    - error 반환 → throw (rejects)
  </behavior>
  <action>
RED→GREEN. `trips.test.ts` 신규 작성(behavior 4케이스, makeChain/makeClient 미러) 후 구현. Open Q3 결정 반영 — shareTrip의 "이미 slug 있으면 early return" 분기를 **의도적으로 제거**한 단일 UPDATE (모드 변경 허용, 재호출 멱등: visibility 'shared' 재설정은 no-op이고 ensure_share_slug는 slug가 이미 있으면 재생성하지 않음):

```typescript
import type { ShareModeType } from '@moajoa/core';

/**
 * Share a moa with an explicit share_mode ('dates' | 'places' | 'both') and
 * return its share_slug for the /t/{slug} link. Single UPDATE — re-calling
 * with a different mode UPDATES share_mode on an already-shared moa (Open Q3:
 * mode change allowed; hiding 'dates' for date-confirmed moas is client-side).
 * Slug minting stays with the 0016 ensure_share_slug trigger — no extra RPC.
 * Owner-only by trips UPDATE RLS. Existing members keep their role (D-A4).
 */
export async function shareMoa(
  client: MoajoaSupabaseClient,
  tripId: string,
  shareMode: ShareModeType,
): Promise<string> {
  const { data, error } = await client
    .from('trips')
    .update({ visibility: 'shared', share_mode: shareMode })
    .eq('id', tripId)
    .select('share_slug')
    .single();
  if (error) throw error;
  const slug = (data as { share_slug: string | null } | null)?.share_slug;
  if (!slug) throw new Error('share_slug not generated');
  return slug;
}
```

기존 `shareTrip`은 무수정 보존 (iOS 동결). `@moajoa/core` import에 `.js` 확장자 금지 (§4.5).
  </action>
  <acceptance_criteria>
    - `grep -q "share_mode: shareMode" packages/api/src/queries/trips.ts` 매치, `grep -q "ShareModeType" packages/api/src/queries/trips.ts` 매치
    - shareTrip 본문 diff 0
    - `grep -c "\.js'" packages/api/src/queries/trips.ts` = 0 (§4.5)
    - trips.test.ts에 `{ visibility: 'shared', share_mode: 'both' }` update 단언 + slug null throw 케이스 존재
    - `pnpm --filter @moajoa/api test && pnpm --filter @moajoa/api typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @moajoa/api test && pnpm --filter @moajoa/api typecheck && grep -q "share_mode: shareMode" packages/api/src/queries/trips.ts</automated>
  </verify>
  <done>Phase 24 [함께 정하기] 시트가 호출할 shareMoa seam이 모드 변경 시맨틱(Open Q3 결정 기록)과 함께 잠김.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 웹 클라이언트 → api 쿼리 → DB | 익명 키 + RLS만 신뢰 — api 레이어에 권한 로직 두지 않음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-16 | Elevation | joinMoa role 조작 | mitigate | 클라이언트 role 인자 자체가 없음 — role은 0025 RPC가 share_mode로 서버 결정. doc 주석에 명시 |
| T-23-17 | Elevation | shareMoa 비-owner 호출 | mitigate | trips UPDATE RLS(owner 전용)가 게이트 — api는 우회 경로 없음(익명 키). doc 주석에 명시 |
| T-23-18 | Tampering | shareMode 임의 문자열 | mitigate | 시그니처가 ShareModeType(3값 union) — TS 컴파일 게이트 + DB CHECK 최종 방어 |
</threat_model>

<verification>
- `pnpm --filter @moajoa/api test` 전체 그린 (기존 무회귀 + 신규), typecheck exit 0
- 기존 joinSharedTrip·shareTrip 무수정 (git diff)
</verification>

<success_criteria>
- 성공 기준 1 후반부("재생성된 타입 위에서 api 테스트 그린")의 api 절반 완결
- Phase 24/25 소비 seam 2개(joinMoa·shareMoa) 잠김 + Open Q3 시맨틱 기록
</success_criteria>

<output>
완료 후 `.planning/phases/23-web-first-foundation/23-06-SUMMARY.md` 생성.
</output>
