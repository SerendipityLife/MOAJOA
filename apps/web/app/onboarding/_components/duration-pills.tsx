'use client';

interface DurationPillsProps {
  value: number | null;
  onChange: (dayCount: number) => void;
}

// RED stub — 구현은 GREEN 커밋에서.
export function DurationPills(_props: DurationPillsProps) {
  return null;
}
