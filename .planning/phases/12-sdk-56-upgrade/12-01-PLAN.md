# Plan 12-01: SDK 56 lockstep 버전 상향

**Phase:** 12 (SDK 55 → 56)
**Wave:** 1
**autonomous:** true
**Requirements:** UPGRADE-01(완료)

## 목표

apps/ios를 SDK 56(RN 0.85/React 19.2, Hermes v1)으로 lockstep 상향. Phase 11 학습대로 직접 핀 + `expo install --fix` 포그라운드 1회.

## Tasks

- **T1** — `pnpm --filter @moajoa/ios add expo@~56.0.11` (메이저 직접 핀).
- **T2** — `pnpm --filter @moajoa/ios exec expo install --fix` (포그라운드 1회, 끝까지 대기). RN 0.85/react/expo-* 모듈 정렬.
- **T3** — `pnpm --filter @moajoa/ios add expo-share-intent@^7.0.0` (expo ^56 호환). plugin 옵션 shape 변경 확인.
- **T4** — `expo install --check` 잔여 불일치 0 확인. 남으면 수동 핀.
- **T5** — `tsc --noEmit` + `jest` 38/38 통과 (RN 0.85/React 19.2 타입·런타임 회귀 확인).

## Success Criteria (verify)

1. expo ~56.0.11 / react-native 0.85.x / expo-share-intent 7.x
2. `expo install --check` = up to date
3. tsc clean + jest 38/38

## 잠금 결정 (11-02 학습)

- `expo install expo@^56` 금지(메이저 미통과) → `pnpm add` 직접 핀
- `expo install --fix`는 포그라운드 1회 (좀비 프로세스 회피)
- 롤백: `git checkout HEAD -- package.json pnpm-lock.yaml` → `pnpm install --frozen-lockfile`
</content>
