---
phase: 24-host-flow
plan: 07
type: execute
wave: 4
depends_on: ["24-04", "24-06"]
files_modified:
  - apps/web/app/moa/[id]/_components/add-sheet.tsx
  - apps/web/app/moa/[id]/_components/share-sheet.tsx
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/__tests__/add-sheet.test.tsx
  - apps/web/__tests__/share-sheet.test.tsx
autonomous: true
requirements: [MOA-03, MOA-04, SHARE-01]
must_haves:
  truths:
    - "FAB [+] → 추가 시트에서 링크 붙여넣기로 자동 추출이 시작되고 '분석 중…' 행이 즉시 뜬다 (MOA-03, D-11/D-13)"
    - "장소 검색 탭에서 결과 탭 시 장소가 직접 추가된다 (MOA-04)"
    - "[함께 정하기]에서 날짜/장소/둘다 모드를 선택해 공유링크를 생성·복사할 수 있고, 날짜 확정 모아는 '날짜 정하기'가 숨는다 (SHARE-01, D-17)"
    - "재공유 시 현재 mode가 프리셋되고 다른 모드 선택 시 갱신 + 같은 slug 재복사된다 (D-19)"
  artifacts:
    - path: "apps/web/app/moa/[id]/_components/add-sheet.tsx"
      provides: "링크/검색 2탭 모달 시트 (AddContentTabs 재사용 — D-11)"
      exports: ["AddSheet"]
    - path: "apps/web/app/moa/[id]/_components/share-sheet.tsx"
      provides: "함께 정하기 모드 3택 + 복사/공유"
      exports: ["ShareSheet"]
  key_links:
    - from: "apps/web/app/moa/[id]/_components/add-sheet.tsx"
      to: "apps/web/components/add-content-tabs.tsx"
      via: "AddContentTabs 재사용 (D-11)"
      pattern: "AddContentTabs"
    - from: "apps/web/app/moa/[id]/_components/share-sheet.tsx"
      to: "shareMoa"
      via: "@moajoa/api — mode 갱신·slug 보존 계약"
      pattern: "shareMoa"
    - from: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      to: "add-sheet / share-sheet"
      via: "FAB·함께 정하기 버튼 open 상태 배선"
      pattern: "AddSheet"
---

<objective>
지도탭 마지막 조각 — FAB [+] 추가 시트(링크/검색, AddContentTabs 재사용)와 [함께 정하기] 공유 시트(모드 3택·클립보드·navigator.share)를 붙이고, phase 전체 게이트(3패키지 테스트 + build + grep 스윕)를 돌린다.

Purpose: MOA-03/04의 추가 진입점과 SHARE-01 완성 — Phase 25가 소비할 공유링크가 여기서 생성된다.
Output: add-sheet + share-sheet + island 배선 + 테스트 + phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-host-flow/24-CONTEXT.md (D-11, D-17, D-18, D-19)
@.planning/phases/24-host-flow/24-UI-SPEC.md (§함께 정하기 시트, §추가 시트, §FAB, Copywriting Contract)
@.planning/phases/24-host-flow/24-RESEARCH.md (Pitfall 5 navigator.share, §shareMoa Code Example)
@.planning/phases/24-host-flow/24-PATTERNS.md (§share-sheet, §add-sheet)
@apps/web/components/bottom-sheet.tsx (모달 시트 props: open·onClose·title)
@apps/web/components/add-content-tabs.tsx (24-04 산출물)
@apps/web/app/moa/[id]/_components/moa-island.tsx (24-06 산출물 — 배선 대상)

<interfaces>
From packages/api/src/queries/trips.ts (실존 — 23-06 계약):
```typescript
// 재호출 = share_mode 갱신 허용·slug 보존(ensure_share_slug), owner-only는 trips UPDATE RLS 게이트.
// 'dates' 숨김은 클라이언트 몫.
export async function shareMoa(client, tripId, shareMode: ShareModeType): Promise<string>  // → share_slug
```

From packages/core (실존):
```typescript
export const ShareMode = ['dates', 'places', 'both'] as const;
// Trip.share_mode: 'dates'|'places'|'both'|null, Trip.start_date: string|null
```

From 24-04:
```typescript
AddContentTabs { onAddLink(url), onPickPlace({id, name, address}), busy? }
```

공유 URL 조립 (D-18 — 브라우저 동일 오리진):
```typescript
const url = `${window.location.origin}/t/${slug}`;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: add-sheet + island FAB 배선 (MOA-03/04)</name>
  <files>apps/web/app/moa/[id]/_components/add-sheet.tsx, apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/__tests__/add-sheet.test.tsx</files>
  <read_first>
    - apps/web/components/add-content-tabs.tsx (24-04 산출물 — props 정확 shape)
    - apps/web/components/bottom-sheet.tsx (L13-20 props 계약)
    - apps/web/app/moa/[id]/_components/moa-island.tsx (24-06 산출물 — reconcile·상태 shape)
    - apps/web/app/boards/[id]/_components/add-link-form.tsx (L39-70 addLink+trigger 흐름 — isDevToolsEnabled 복사 금지)
  </read_first>
  <behavior>
    - Test 1 (MOA-03): 링크 탭 제출 → addLink({board_id, url}) 호출 + source_kind 'youtube'면 triggerExtraction fire-and-forget 호출 + onAdded(reconcile) 호출 + 시트 닫힘
    - Test 2 (MOA-04): 검색 탭 결과 행 탭 → addManualPlace({board_id, google_place_id}) 호출 + 시트 닫힘
    - Test 3: addLink reject → 에러 toast + 시트 유지
  </behavior>
  <action>
    1. `add-sheet.tsx` 신규 — `'use client'`:
    ```typescript
    export interface AddSheetProps {
      tripId: string;
      open: boolean;
      onClose: () => void;
      onAdded: () => void;   // island reconcile — 분석중 행 즉시 반영 (links INSERT는 구독 대상 아님)
    }
    ```
    `<BottomSheet open={open} onClose={onClose}>` 안에 `<AddContentTabs>`:
    - `onAddLink`: `const link = await addLink(client, { board_id: tripId, url });` → `if (link.source_kind !== 'manual') triggerExtraction(client, link.id).catch(console.error);` (fire-and-forget idiom) → `onAdded()` → `onClose()` — D-13: reconcile이 '분석 중…' 행을 즉시 띄움.
    - `onPickPlace`: `await addManualPlace(client, { board_id: tripId, google_place_id: place.id });` → `onClose()` — 장소 등장·토스트는 island의 places INSERT realtime 경로가 담당(D-16 중복 토스트 금지 — add-sheet 자체 성공 토스트 없음).
    - 실패 시 try/catch + toast('추가하지 못했어요. 다시 시도해 주세요', {variant:'error'}) + 시트 유지.
    2. `moa-island.tsx` 수정 (surgical — 24-06 구조 보존): `addOpen` 상태 + FAB 버튼 append — `brand-600` 56px 원형 + shadows.fab, lucide `Plus` 흰색, **aria-label="장소 추가"**, 우측 하단 collapsed 시트 상단에서 16px 위, z-order 시트보다 위(UI-SPEC §FAB). `<AddSheet tripId={trip.id} open={addOpen} onClose={...} onAdded={() => void reconcile()} />`.
    3. `__tests__/add-sheet.test.tsx` — behavior 3케이스. mock: `@moajoa/api`(addLink·triggerExtraction·addManualPlace)·`@/lib/supabase/browser`(functions.invoke 포함 — AddContentTabs 검색용)·`@/components`(BottomSheet pass-through·Tabs·useToast). 검색 결과 fixture는 functions.invoke resolve-place mock 반환으로 주입.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- add-sheet</automated>
  </verify>
  <acceptance_criteria>
    - `grep "AddContentTabs" apps/web/app/moa/[id]/_components/add-sheet.tsx` 1건 (D-11 재사용 — 자체 탭 재구현 금지)
    - `grep "triggerExtraction" apps/web/app/moa/[id]/_components/add-sheet.tsx` 1건 + `.catch` 존재 (fire-and-forget)
    - `grep "aria-label=\"장소 추가\"" apps/web/app/moa/[id]/_components/moa-island.tsx` 1건
    - `grep -c "isDevToolsEnabled" apps/web/app/moa/[id]/_components/add-sheet.tsx` == 0
    - add-sheet 3케이스 그린 + moa-island 테스트 무회귀
  </acceptance_criteria>
  <done>+ 버튼 → 링크 추출/장소 검색 추가의 정식 UI 경로 완성 (dev-tools 게이트 없는 첫 추가 표면)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: share-sheet + island 함께 정하기 배선 (SHARE-01)</name>
  <files>apps/web/app/moa/[id]/_components/share-sheet.tsx, apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/__tests__/share-sheet.test.tsx</files>
  <read_first>
    - packages/api/src/queries/trips.ts L163-190 (shareMoa 실계약 — 재호출 mode 갱신·slug 보존 doc comment)
    - .planning/phases/24-host-flow/24-UI-SPEC.md §함께 정하기 시트 (모드 카드 카피·스타일 정확값)
    - apps/web/app/moa/[id]/_components/moa-island.tsx (Task 1 수정본)
  </read_first>
  <behavior>
    - Test 1 (SHARE-01): 모드 카드 3개 렌더(날짜/장소/둘다 + 설명 카피) — '장소 정하기' 선택 후 "링크 복사하기" → shareMoa(client, tripId, 'places') 호출 + clipboard.writeText('{origin}/t/{slug}') + toast('링크를 복사했어요')
    - Test 2 (D-17): trip.start_date != null → '날짜 정하기' 카드 미렌더 (2택), '둘다 정하기'는 렌더
    - Test 3 (D-19): trip.share_mode 'both'로 열면 '둘다 정하기'가 선택 프리셋 상태
    - Test 4 (Pitfall 5): navigator.share 존재 시 clipboard 후 share({url}) 호출, share가 AbortError로 reject해도 에러 toast 없음
    - Test 5: shareMoa reject → toast('링크를 만들지 못했어요. 다시 시도해 주세요')
  </behavior>
  <action>
    1. `share-sheet.tsx` 신규 — `'use client'`:
    ```typescript
    export interface ShareSheetProps {
      trip: Trip;          // start_date(D-17 숨김)·share_mode(D-19 프리셋) 사용
      open: boolean;
      onClose: () => void;
      onShared?: (mode: ShareModeType) => void;   // island이 로컬 trip.share_mode 갱신(재열림 프리셋 일관성)
    }
    ```
    - `<BottomSheet open onClose title="함께 정하기">`. 모드 카드 세로 스택(카드 16px 패딩·radius xl, 제목 16/600 + 설명 14/400 neutral-500 한 줄) — Copywriting Contract verbatim:
      - "날짜 정하기" / "언제 갈지 투표로 정해요" (mode 'dates')
      - "장소 정하기" / "어디 갈지 찜으로 정해요" (mode 'places')
      - "둘다 정하기" / "날짜와 장소 모두 함께 정해요" (mode 'both')
    - **D-17:** `trip.start_date !== null`이면 'dates' 카드 **미렌더**(disabled 아님) → 2택.
    - 선택 스타일: 미선택 border neutral-200 / 선택 border brand-500 + bg brand-50. 초기 선택 = `trip.share_mode ?? (start_date ? 'places' : null)` — D-19 프리셋(null이면 미선택, CTA disabled).
    - CTA full-width primary "링크 복사하기":
    ```typescript
    try {
      const slug = await shareMoa(client, trip.id, mode);
      const url = `${window.location.origin}/t/${slug}`;
      await navigator.clipboard.writeText(url);
      toast('링크를 복사했어요');
      onShared?.(mode);
      if (navigator.share) {
        try { await navigator.share({ url }); }
        catch (e) { if ((e as Error).name !== 'AbortError') throw e; }   // Pitfall 5
      }
    } catch {
      toast('링크를 만들지 못했어요. 다시 시도해 주세요', { variant: 'error' });
    }
    ```
    (클립보드 복사가 기본 선행, share는 추가 제공 — D-18 순서. 데스크톱은 navigator.share 부재 → 복사만.)
    2. `moa-island.tsx` 수정 (surgical): `shareOpen` 상태 + 상단 바 우측 primary sm 버튼 "함께 정하기" 오버레이(UI-SPEC §상단 바) + `localShareMode` 상태(초기 trip.share_mode, onShared로 갱신 — 재열림 프리셋). `<ShareSheet trip={{...trip, share_mode: localShareMode}} ... />`.
    3. `__tests__/share-sheet.test.tsx` — behavior 5케이스. mock: `@moajoa/api`(shareMoa vi.fn → 'slug123')·`@/components`·clipboard(`Object.assign(navigator, { clipboard: { writeText: vi.fn() }, share: vi.fn() })` — beforeEach 재설정, share 부재 케이스는 delete).
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- share-sheet</automated>
  </verify>
  <acceptance_criteria>
    - `grep "shareMoa" apps/web/app/moa/[id]/_components/share-sheet.tsx` 1건 + `grep "/t/"` 1건 (URL 형식)
    - `grep "AbortError" apps/web/app/moa/[id]/_components/share-sheet.tsx` 1건
    - `grep "언제 갈지 투표로 정해요" apps/web/app/moa/[id]/_components/share-sheet.tsx` 1건 + 나머지 2 설명 카피 존재
    - `grep "함께 정하기" apps/web/app/moa/[id]/_components/moa-island.tsx` ≥ 1 (상단 바 버튼)
    - share-sheet 5케이스 그린 (D-17 2택·D-19 프리셋 포함)
  </acceptance_criteria>
  <done>SHARE-01 완성 — Phase 25가 소비할 /t/{slug} 공유링크 생성 경로 확립</done>
</task>

<task type="auto">
  <name>Task 3: Phase 24 전체 게이트 — 3패키지 테스트 + build + 안티패턴 grep 스윕</name>
  <files>(변경 없음 — 검증 전용. 실패 시 해당 파일 수정)</files>
  <read_first>
    - .planning/phases/24-host-flow/24-VALIDATION.md (§Sampling Rate — phase gate 정의)
  </read_first>
  <action>
    Phase 24 마감 게이트를 순차 실행 (**`pnpm -r test` 절대 금지 — watch hang**):
    1. `pnpm --filter @moajoa/core test:run` — 무회귀 (169+)
    2. `pnpm --filter @moajoa/api test:run && pnpm --filter @moajoa/api typecheck` — 81 + 신규
    3. `pnpm --filter web test:run && pnpm --filter web typecheck` — 기존 65 + 신규 7파일
    4. `pnpm --filter web build` — PASS + 라우트 테이블에 `/onboarding`·`/moa`·`/moa/[id]` 존재
    5. `pnpm --filter @moajoa/ios test` — ui-tokens 변경 무회귀 1회 (128, apps/ios 코드 무접촉 확인: `git diff --stat apps/ios` == 0)
    6. 안티패턴 grep 스윕 (전부 0건이어야 함):
       - `grep -rn "isDevToolsEnabled" apps/web/app/moa apps/web/app/onboarding apps/web/components/add-content-tabs.tsx`
       - `grep -rn "presence" apps/web/app/moa` (D-14 금지)
       - `grep -rn "seq_no:" apps/web/app/moa apps/web/app/onboarding | grep -i "insert\|payload"` (forge 경로)
       - `grep -rn "dangerouslySetInnerHTML" apps/web/app/moa apps/web/app/onboarding`
       - `grep -rn "from '@moajoa/core.js'\|from '@moajoa/api.js'" apps/web packages` (`.js` extension)
       - `grep -rn "react-day-picker/style.css" apps/web` (A-8)
    7. 실패 항목이 있으면 해당 파일을 수정 후 재실행 — 전부 그린일 때만 완료.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/core test:run && pnpm --filter @moajoa/api test:run && pnpm --filter web test:run && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - 3패키지 test:run + web build 전부 exit 0
    - build 출력 라우트 테이블에 `/onboarding`·`/moa`·`/moa/[id]` 3라우트
    - 안티패턴 grep 6종 전부 0건
    - `git diff --stat apps/ios` 변경 0 (iOS 동결)
  </acceptance_criteria>
  <done>Phase 24 자동 검증 전체 그린 — /gsd-verify-work(로컬 realtime 브라우저 스모크 + Vercel Preview 카카오 e2e)로 인계 가능</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공유링크 slug | bearer 자격 — 아는 사람은 누구나 /t/{slug} 접근 (Phase 25 표면) |
| 클립보드·navigator.share | 시스템 공유 표면으로 URL 유출 경로 |
| shareMoa UPDATE | visibility·share_mode 변경 권한 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-21 | Elevation | shareMoa (비소유자 공유 시도) | mitigate | owner-only는 trips UPDATE RLS가 게이트(23-06 T-23-17) — 클라이언트는 UI만, 권한 검사 서버 몫. 클라 role 파라미터 0 |
| T-24-22 | Info Disclosure | share_slug bearer 노출 | accept | 설계상 bearer 링크(0016 gen_random_bytes 엔트로피 트리거) — 카톡 공유가 제품 핵심 경로. 클라 slug 생성 경로 0 |
| T-24-23 | Spoofing | 공유 URL 조립 | mitigate | `window.location.origin` + shareMoa 반환 slug만 사용 — user 입력이 URL에 들어가는 경로 0 (Task 2) |
| T-24-24 | DoS | 추출 트리거 남용 (add-sheet) | accept→Phase 27 | 로그인 사용자만 UI 도달(RSC auth 게이트) — 멤버십 비용 게이트는 SEC-01(Phase 27) 범위로 명시 이관 |
</threat_model>

<verification>
- `pnpm --filter web test:run -- add-sheet`·`-- share-sheet` 그린
- Task 3 phase gate 전체 (3패키지 + build + grep 6종 + iOS diff 0)
</verification>

<success_criteria>
- 성공 기준 3 완성: 링크 추가·장소 검색 추가가 정식 UI로 동작
- 성공 기준 5 완성: 모드 3택(날짜 확정 시 2택) 공유링크 생성·복사 — D-18/19 포함
- Phase 24 전 자동 검증 그린 — verify-work 인계 준비
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-07-SUMMARY.md`
</output>
