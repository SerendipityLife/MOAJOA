---
phase: 26-realtime-chat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/moa/[id]/_components/moa-chat.tsx
  - apps/web/app/moa/[id]/_components/moa-tab-bar.tsx
  - apps/web/__tests__/moa-chat.test.tsx
autonomous: true
requirements: [CHAT-01, CHAT-02, CHAT-03]
user_setup: []

must_haves:
  truths:
    - "A messenger-style bubble list renders messages (nickname label, mine vs others alignment by user_id)"
    - "An input bar sends the draft (Enter or 보내기), clears on send, restores on failure"
    - "A '지금 N명 보는 중' strip renders from a viewers prop"
    - "A message with a resolvable reply_to_place_id renders a #N 장소명 chip; tapping it calls onChipTap"
    - "A reply-compose banner shows when replyToPlaceId is set, with a clear (x) affordance"
  artifacts:
    - path: "apps/web/app/moa/[id]/_components/moa-chat.tsx"
      provides: "Presentational chat surface (bubbles + input + presence strip + reply chip/banner)"
      min_lines: 90
    - path: "apps/web/app/moa/[id]/_components/moa-tab-bar.tsx"
      provides: "Fixed-bottom client-state 2-tab bar ([모으기][채팅]), extendable to 4"
      exports: ["MoaTabBar"]
    - path: "apps/web/__tests__/moa-chat.test.tsx"
      provides: "render/send/chip/presence unit coverage"
  key_links:
    - from: "moa-chat input"
      to: "onSend prop"
      via: "await onSend(body, replyToPlaceId)"
      pattern: "onSend\\("
    - from: "moa-chat #N chip"
      to: "onChipTap prop"
      via: "chip onClick"
      pattern: "onChipTap\\("
    - from: "moa-tab-bar button"
      to: "onTabChange prop"
      via: "button onClick (NOT next/link — D-02)"
      pattern: "onTabChange\\("
---

<objective>
The two net-new presentational components the island (Plan 03/04) will mount: a messenger-style chat surface and the moa-internal bottom tab bar. Both are pure/controlled — no realtime, no `client.channel()`, no `@moajoa/api` import. The island owns all state + the single channel; these receive props.

Purpose: RESEARCH Q3 state-lifting rule — because the `trip_messages` INSERT binding must live in the island's pre-subscribe chain, message state lives in the island and `moa-chat` is presentational (inverts poll-chat's "child owns bindings"). Copying poll's shape here would silently break live messages. D-02 requires the tab bar to be client-state (button onClick), NOT routes, so the island mounts once and the `moa:{tripId}` channel is never torn down.

Output: `moa-chat.tsx`, `moa-tab-bar.tsx`, `moa-chat.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-realtime-chat/26-CONTEXT.md
@.planning/phases/26-realtime-chat/26-RESEARCH.md
@.planning/phases/26-realtime-chat/26-PATTERNS.md

Analog files (read the cited ranges):
@apps/web/app/poll/[code]/_components/poll-chat.tsx
@apps/web/components/bottom-nav.tsx

<interfaces>
<!-- Executor: build to THESE contracts. Plan 03/04 wire against them. -->
From @moajoa/core:
```typescript
export type TripMessage = { id: string; trip_id: string; user_id: string; nickname: string; body: string; reply_to_place_id: string | null; created_at: string };
```

From @/components (apps/web/components/index.ts):
```typescript
export function Chip(props): JSX.Element;         // design-system chip (chip.tsx)
export function useToast(): { toast: (msg: string, opts?: { variant?: 'error' }) => void };
```

CONTRACT to implement — MoaChat (presentational, controlled by island):
```typescript
export interface MoaChatProps {
  messages: TripMessage[];               // island-owned; append-driven
  currentUserId: string;                 // mine-check: m.user_id === currentUserId
  viewers: number;                       // CHAT-02 presence count
  onSend: (body: string, replyToPlaceId: string | null) => Promise<void>; // throws → restore draft
  replyToPlaceId: string | null;         // CHAT-03 compose target
  onClearReply: () => void;
  placesById: Record<string, { seqNo: number; name: string }>; // chip resolution
  onChipTap: (placeId: string) => void;  // CHAT-03 chip → island nav
}
export function MoaChat(props: MoaChatProps): JSX.Element;
```

CONTRACT to implement — MoaTabBar:
```typescript
export type MoaTab = 'moa' | 'chat';
export interface MoaTabBarProps { activeTab: MoaTab; onTabChange: (tab: MoaTab) => void }
export function MoaTabBar(props: MoaTabBarProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: moa-chat.tsx — presentational messenger surface</name>
  <files>apps/web/app/moa/[id]/_components/moa-chat.tsx</files>
  <behavior>
    - Renders one bubble per message; `mine = m.user_id === currentUserId` → justify-end + brand-50, else justify-start + neutral-100; nickname label + whitespace-pre-wrap body + relTime.
    - Empty messages → "첫 메시지를 남겨보세요 💬" empty state.
    - Input: controlled draft, Enter or 보내기 → `await onSend(draft.trim(), replyToPlaceId)`; clears draft optimistically; on throw restores draft + error toast; disabled while sending; maxLength 140; blocks empty/whitespace.
    - viewers > 0 → presence strip ("지금 보는 중" at 1, "지금 N명 보는 중" at ≥2).
    - Message with `reply_to_place_id` set AND present in placesById → renders `#{seqNo} {name}` chip above the body; chip onClick → onChipTap(reply_to_place_id). Unresolvable (null or missing) → no chip (Pitfall 9).
    - replyToPlaceId set → reply-compose banner above input showing `#N 장소명` + clear (x) → onClearReply.
    - New message → auto-scroll to bottom (messagesEndRef.scrollIntoView).
  </behavior>
  <action>
Mirror `poll-chat.tsx` bubble/input/relTime idiom, but STRIP the poll-specific parts and adapt per PATTERNS §1:

KEEP from poll-chat: `relTime()` (L36-44), bubble markup (L150-181: rounded-2xl, nickname `text-xs font-semibold text-neutral-500`, body `whitespace-pre-wrap break-words`), compose row (L184-205: controlled input, `onKeyDown Enter`, `maxLength={140}`, `보내기` disabled).

DIFFERENCES (critical):
1. **NO `channel` prop, NO internal realtime, NO `initialMessages` seam.** `messages` is a controlled prop owned by the island. `moa-chat` never calls `client.channel()` or `.on('broadcast')`. Remove the poll-chat channel useEffect entirely.
2. **mine-check by `user_id`** (`m.user_id === currentUserId`), not `device_token`. Drop `getDeviceToken`.
3. **Send delegates to `onSend`.** Keep a local `draft` + `sending` state; `send()` = guard empty/sending → `setSending(true); setDraft('')` → `try { await onSend(body, replyToPlaceId) } catch { setDraft(body); toast('메시지를 보내지 못했어요. 다시 시도해주세요.', { variant: 'error' }) } finally { setSending(false) }`. The island does the query + append + clearReply; on success it clears reply, so no local reply state here.
4. **DROP delete UI** — no `pendingDelete`/`doDelete`/`Dialog` (범위 외, 26-CONTEXT domain). No 삭제 button.
5. **Presence strip** — copy the poll-vote-island strip markup verbatim (poll-vote-island L363-369): `{viewers > 0 && (<div className="mb-4 ...">…{viewers <= 1 ? '지금 보는 중' : \`지금 ${viewers}명 보는 중\`}…</div>)}`. Place at the top of the chat surface (Claude's discretion per D-10; chat-tab-top is fine).
6. **#N chip (CHAT-03)** — for a message where `m.reply_to_place_id` is non-null and `placesById[m.reply_to_place_id]` exists, render above the body: `<Chip onClick={() => onChipTap(m.reply_to_place_id!)}>#{p.seqNo} {p.name}</Chip>` (import Chip from `@/components`). Skip if unresolvable.
7. **Reply-compose banner** — when `replyToPlaceId` and resolvable, render a slim banner above the input: `답장 · #N 장소명` + an `x` button → `onClearReply()`.
8. **Auto-scroll** — `const endRef = useRef<HTMLDivElement>(null); useEffect(() => endRef.current?.scrollIntoView({ behavior: 'smooth' }), [messages.length]);` and render `<div ref={endRef} />` after the list.

`'use client'`. No `@moajoa/api` import. No `.js` workspace imports (§4.5). Use `Chip`/`useToast` from `@/components`.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- moa-chat</automated>
  </verify>
  <done>MoaChat renders bubbles (mine/other split), sends via onSend (clear + restore-on-throw), shows presence strip, renders + resolves #N chip → onChipTap, shows reply banner + clear. No channel/broadcast/getDeviceToken references.</done>
</task>

<task type="auto">
  <name>Task 2: moa-tab-bar.tsx + moa-chat.test.tsx</name>
  <files>apps/web/app/moa/[id]/_components/moa-tab-bar.tsx, apps/web/__tests__/moa-chat.test.tsx</files>
  <action>
**moa-tab-bar.tsx** — net-new fixed-bottom bar (PATTERNS §2, RESEARCH Q5). Copy the visual shell from `bottom-nav.tsx` L24-46 (`fixed inset-x-0 bottom-0 z-40 border-t border-neutral-200 bg-white/95 backdrop-blur`; `<ul>` with `max-w-lg` centered to match the island; per-item `flex-1`, active `text-brand-500` else `text-neutral-500`; inline SVG icon + `text-[11px] font-medium` label). DIFFERENCES:
- **Client state, NOT routing** (D-02): render `<button type="button" onClick={() => onTabChange(t.key)}>` — NO `next/link`, NO `usePathname`.
- Model tabs as an array so it is trivially extendable to 4 (D-01 — 플랜/예약/가계부 자리만): `const TABS = [{ key: 'moa', label: '모으기', Icon: MapPinIcon }, { key: 'chat', label: '채팅', Icon: ChatIcon }] as const;` render only these two now. Provide two small inline SVG icons (map-pin, chat-bubble) in the bottom-nav style (24×24, stroke currentColor 1.6).
- Drop the `isDevToolsEnabled` / `onTab` guards (this bar always shows inside /moa/[id]; the app-shell bottom-nav is already hidden here).
- Export `MoaTabBar`, `MoaTab`, `MoaTabBarProps`. `'use client'`.

**moa-chat.test.tsx** — mock `@/components` (Chip → passthrough button, `useToast` → `{ toast }` spy) BEFORE importing MoaChat (mirror moa-island.test.tsx mock-before-import). Seed `messages`/`viewers`/`placesById`/`replyToPlaceId` directly as props (controlled — no realtime seam needed). Cover:
1. Renders a message body + nickname; mine (user_id===currentUserId) aligns end, other aligns start.
2. Type + 보내기 (or Enter) → `onSend` called once with `(trimmed, replyToPlaceId)`; input clears.
3. onSend rejects → draft restored + error toast.
4. viewers=2 → "지금 2명 보는 중"; viewers=0 → strip absent.
5. Message with `reply_to_place_id` resolvable in placesById → `#N 장소명` chip present; click → `onChipTap(placeId)`.
6. `replyToPlaceId` set → reply banner shows `#N 장소명`; clicking x → `onClearReply`.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- moa-chat && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>MoaTabBar renders two buttons, onTabChange fires with 'moa'/'chat', no next/link/usePathname; moa-chat.test.tsx (6 cases) green; web typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (draft) → onSend | Untrusted text leaves the input; the island validates with Zod before the DB (Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Tampering | chat input body | mitigate | `maxLength={140}` + empty/whitespace guard here; authoritative `TripMessageCreateSchema.parse` (body 1–140) at the island boundary (Plan 03) + DB CHECK |
| T-26-05 | Information disclosure | #N chip / reply banner | accept | renders only from island-provided `placesById` (already RLS-gated places state); unresolvable ids render nothing |
</threat_model>

<verification>
- `pnpm --filter web test:run` green (moa-chat cases); web `tsc --noEmit` exit 0.
- `grep -n "client.channel\|broadcast\|getDeviceToken\|next/link\|usePathname" moa-chat.tsx moa-tab-bar.tsx` → 0 matches.
- No `@moajoa/api` import in either component; no `.js` workspace imports.
</verification>

<success_criteria>
- MoaChat is fully presentational (controlled `messages`, `onSend`, `viewers`, reply chip/banner) with no realtime/channel code.
- MoaTabBar is a client-state fixed-bottom 2-tab bar (button onClick), extendable to 4.
- Unit tests green; typecheck clean.
</success_criteria>

<output>
After completion, create `.planning/phases/26-realtime-chat/26-02-SUMMARY.md`.
</output>
