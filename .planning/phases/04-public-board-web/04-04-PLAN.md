---
phase: 04-public-board-web
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - apps/web/app/b/[slug]/opengraph-image.tsx
  - apps/web/lib/og/pretendard.ts
  - apps/web/public/fonts/Pretendard-Regular.subset.woff2
  - apps/web/public/fonts/Pretendard-SemiBold.subset.woff2
  - apps/web/__tests__/og-image.test.ts
  - apps/web/package.json
autonomous: false
requirements:
  - VIEW-03
tags:
  - opengraph-image
  - imageresponse
  - static-maps-fetch
  - pretendard-subset
user_setup:
  - service: google-cloud-platform
    why: "OG image의 미니맵을 Google Static Maps API로 fetch. 같은 GCP project + NEXT_PUBLIC_GOOGLE_MAPS_KEY 재사용이지만, Maps Static API가 별도로 enable되어 있어야 함 (RESEARCH §Pitfall 4 + A3)."
    env_vars: []
    dashboard_config:
      - task: "GCP Console → APIs & Services → Library → 'Maps Static API' 검색 → Enable 클릭"
        location: "https://console.cloud.google.com/apis/library — 같은 project (NEXT_PUBLIC_GOOGLE_MAPS_KEY가 속한)"
      - task: "(Optional v1) Maps Static API 호출량을 GCP Console → APIs & Services → Quotas에서 모니터링 — Phase 2 D-13의 $5/$20/$50 billing alert에 함께 잡힘"
        location: "GCP Console"

must_haves:
  truths:
    - "GET /b/{slug}/opengraph-image이 1200×630 PNG ImageResponse를 반환한다 (D-06)"
    - "Pretendard SemiBold + Regular 한글 폰트가 ArrayBuffer로 등록되어 한글이 system fallback 깨짐 없이 표시된다 (RESEARCH §Pitfall 3)"
    - "Pretendard 폰트 bundle이 500KB 미만이다 — woff2 + 한글 subset (RESEARCH §Pitfall 3, A1)"
    - "좌측 panel: 보드 제목 (48/600) + 도시 (28/400, 있을 때만) + 핀 수 (24/400) + 워드마크 (24/600 brand-500)"
    - "우측 panel: Static Maps PNG (places ≤10 markers, brand-500 color, OG_GRAYSCALE_STYLE) 또는 텍스트 fallback '지도 미리보기 준비 중'"
    - "places.length === 0 또는 NEXT_PUBLIC_GOOGLE_MAPS_KEY 없을 때 fallback 텍스트 렌더 (D-07)"
    - "view가 null이면 fallback 'MOAJOA 공유 보드' generic 카드 렌더"
    - "OG image도 BOARD_REVALIDATE_TAG로 묶여 동일 webhook으로 무효화 (Pitfall 8)"
  artifacts:
    - path: "apps/web/app/b/[slug]/opengraph-image.tsx"
      provides: "Next.js file-convention OG image route"
      contains: "ImageResponse"
      min_lines: 80
    - path: "apps/web/lib/og/pretendard.ts"
      provides: "Cached Pretendard ArrayBuffer loader"
      exports: ["loadPretendardFonts"]
    - path: "apps/web/public/fonts/Pretendard-Regular.subset.woff2"
      provides: "Korean-subset woff2 (target <120KB)"
      max_size_kb: 150
    - path: "apps/web/public/fonts/Pretendard-SemiBold.subset.woff2"
      provides: "Korean-subset woff2 (target <120KB)"
      max_size_kb: 150
    - path: "apps/web/__tests__/og-image.test.ts"
      provides: "Smoke test — default export returns ImageResponse without throw"
      min_lines: 30
  key_links:
    - from: "apps/web/app/b/[slug]/opengraph-image.tsx"
      to: "apps/web/lib/cache.ts getCachedPublicBoard"
      via: "shared cache + tag"
      pattern: "getCachedPublicBoard"
    - from: "apps/web/app/b/[slug]/opengraph-image.tsx"
      to: "apps/web/lib/og/static-maps.ts buildStaticMapsUrl"
      via: "URL builder for <img src=>"
      pattern: "buildStaticMapsUrl"
    - from: "apps/web/app/b/[slug]/opengraph-image.tsx"
      to: "apps/web/lib/og/pretendard.ts loadPretendardFonts"
      via: "fs.readFile + cached ArrayBuffer"
      pattern: "loadPretendardFonts"
---

<objective>
VIEW-03 완결: `/b/[slug]/opengraph-image.tsx` 파일 컨벤션으로 동적 OG image (1200×630) 생성. 좌측 텍스트 stack(보드 제목 + 도시 + 핀 수 + 워드마크), 우측 Google Static Maps PNG mini-map. Pretendard 한글 폰트는 woff2 + Korean subset으로 500KB 한도 내로 압축 후 ArrayBuffer 등록.

Purpose:
- 카톡 채팅창에 보드 URL 붙여넣을 때 미리보기 카드(보드 제목 + 미니맵 + 핀 수)가 한글 깨짐 없이 표시 — Phase 4 acquisition surface 핵심
- RESEARCH §Pitfall 3 (500KB bundle 초과) 대응: 실측 1.57MB → woff2 + KS X 1001 subset → ~110KB 예상
- RESEARCH §Pitfall 9 정정: D-06 "Edge runtime" 표기는 잔재 — Node runtime 사용 (readFile + process.cwd 안정)
- OG image도 04-01의 `getCachedPublicBoard` 재사용 → page와 동일 tag 묶음 (Pitfall 8 — RPC 호출 1번만)

Output:
- opengraph-image.tsx (1200×630 ImageResponse, Pretendard 등록, Static Maps embed, fallback 텍스트)
- lib/og/pretendard.ts (한 번만 readFile + 모듈 캐시)
- 2개 subset woff2 폰트 (Regular + SemiBold)
- 1 smoke test (default export가 throw 없이 ImageResponse 반환)
- 1 checkpoint (실제 production deploy 또는 dev에서 OG URL fetch 후 PNG 시각 확인)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-public-board-web/04-CONTEXT.md
@.planning/phases/04-public-board-web/04-RESEARCH.md
@.planning/phases/04-public-board-web/04-UI-SPEC.md
@.planning/phases/04-public-board-web/04-VALIDATION.md
@.planning/phases/04-public-board-web/04-01-PLAN.md
@.planning/phases/04-public-board-web/04-03-PLAN.md
@CLAUDE.md
@apps/web/public/fonts/

<interfaces>
<!-- From 04-01-PLAN -->

```typescript
// apps/web/lib/cache.ts
export function getCachedPublicBoard(slug: string): Promise<PublicBoardView | null>;

// apps/web/lib/og/static-maps.ts
export function buildStaticMapsUrl(opts: BuildStaticMapsOpts): string;
export const OG_GRAYSCALE_STYLE: string[];

// apps/web/lib/env.ts
export function getGoogleMapsKey(): string | undefined;

// packages/core
export const CITY_KO_MAP: Readonly<Record<string, string>>;
```

<!-- Current font file sizes (verified) — MUST be subset to fit 500KB ImageResponse limit -->
- Pretendard-Regular.otf: 1,574,352 bytes (~1.5 MB)
- Pretendard-SemiBold.otf: 1,583,704 bytes (~1.5 MB)
- Combined: ~3.0 MB — 6x over 500KB ImageResponse limit (RESEARCH §Pitfall 3)
- Target after subset: ~110 KB each (KS X 1001 — 2,350 commonly-used Korean syllables)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pretendard subset woff2 generation</name>
  <files>
    apps/web/public/fonts/Pretendard-Regular.subset.woff2,
    apps/web/public/fonts/Pretendard-SemiBold.subset.woff2,
    apps/web/package.json
  </files>
  <action>
RESEARCH §Pitfall 3 (A1) 대응: 현재 `.otf` 1.5MB × 2 = 3.0MB, ImageResponse 500KB 한도 6배 초과. woff2 + 한글 subset으로 ~110KB × 2 = ~220KB로 압축.

도구 선택: Python `fonttools`의 `pyftsubset` 또는 Node `subset-font` 패키지. 시스템에 Python이 가장 호환성 높음 (pyftsubset이 woff2 출력 + unicodes 옵션 둘 다 안정 지원).

검증된 명령:

```bash
# 1) pyftsubset 설치 확인
python3 -c "import fontTools" 2>/dev/null || pip3 install --user fonttools brotli zopfli

# 2) Unicode range for Korean — KS X 1001 commonly-used + ASCII + 한글 자모 + 숫자/문장부호
# Hangul Syllables (U+AC00–U+D7A3) + Hangul Compatibility Jamo (U+3130–U+318F) +
# Hangul Jamo (U+1100–U+11FF) + ASCII (U+0020–U+007E) + Latin-1 supplement basics +
# CJK punctuation (·, U+00B7; …, U+2026; · variants)
UNICODES="U+0020-007E,U+00A0-00FF,U+00B7,U+2026,U+1100-11FF,U+3130-318F,U+AC00-D7A3,U+0030-0039"

# 3) Subset Regular
python3 -m fontTools.subset \
  apps/web/public/fonts/Pretendard-Regular.otf \
  --output-file=apps/web/public/fonts/Pretendard-Regular.subset.woff2 \
  --flavor=woff2 \
  --unicodes="$UNICODES" \
  --layout-features='*' \
  --no-hinting

# 4) Subset SemiBold
python3 -m fontTools.subset \
  apps/web/public/fonts/Pretendard-SemiBold.otf \
  --output-file=apps/web/public/fonts/Pretendard-SemiBold.subset.woff2 \
  --flavor=woff2 \
  --unicodes="$UNICODES" \
  --layout-features='*' \
  --no-hinting

# 5) Verify size — each < 150KB
ls -la apps/web/public/fonts/Pretendard-*.subset.woff2

# 6) Combined size sanity check
COMBINED=$(stat -f%z apps/web/public/fonts/Pretendard-Regular.subset.woff2 2>/dev/null || stat -c%s apps/web/public/fonts/Pretendard-Regular.subset.woff2)
COMBINED=$((COMBINED + $(stat -f%z apps/web/public/fonts/Pretendard-SemiBold.subset.woff2 2>/dev/null || stat -c%s apps/web/public/fonts/Pretendard-SemiBold.subset.woff2)))
[ "$COMBINED" -lt 500000 ] && echo "✓ combined < 500KB ($COMBINED bytes)" || echo "✗ FAIL: $COMBINED bytes"
```

If pyftsubset is unavailable AND pip3 install fails, fallback to Node tool:
```bash
pnpm dlx subset-font@^2 \
  apps/web/public/fonts/Pretendard-Regular.otf \
  --output apps/web/public/fonts/Pretendard-Regular.subset.woff2 \
  --target-format woff2 \
  --text "$(printf ' -~가-힣' | head -c 99999)"
```

(Note: subset-font's text-based subset is less precise than unicode-range — pyftsubset preferred.)

Edge case handling:
- If subset output > 150KB per file: relax to broader Korean (drop CJK extension, focus on KS X 1001 2350 chars only) — strip `--layout-features='*'` and use default features.
- If `pyftsubset` exits non-zero with "missing brotli": `pip3 install --user brotli` then retry.
- Both `.otf` files must remain in `public/fonts/` (other parts of app may use them; do NOT delete originals — Karpathy §3.3 surgical).

Commit: `chore(04-04): generate Pretendard Korean-subset woff2 for OG image bundle`
  </action>
  <verify>
    <automated>test -f apps/web/public/fonts/Pretendard-Regular.subset.woff2 && test -f apps/web/public/fonts/Pretendard-SemiBold.subset.woff2 && [ $(($(stat -f%z apps/web/public/fonts/Pretendard-Regular.subset.woff2 2>/dev/null || stat -c%s apps/web/public/fonts/Pretendard-Regular.subset.woff2) + $(stat -f%z apps/web/public/fonts/Pretendard-SemiBold.subset.woff2 2>/dev/null || stat -c%s apps/web/public/fonts/Pretendard-SemiBold.subset.woff2))) -lt 500000 ]</automated>
  </verify>
  <done>
    - apps/web/public/fonts/Pretendard-Regular.subset.woff2 exists, < 150KB
    - apps/web/public/fonts/Pretendard-SemiBold.subset.woff2 exists, < 150KB
    - Combined size < 500KB (ImageResponse limit)
    - Original .otf files untouched (still used by next/font/local in layout.tsx)
    - 1 atomic commit
  </done>
</task>

<task type="auto">
  <name>Task 2: opengraph-image.tsx + Pretendard loader + smoke test</name>
  <files>
    apps/web/app/b/[slug]/opengraph-image.tsx,
    apps/web/lib/og/pretendard.ts,
    apps/web/__tests__/og-image.test.ts
  </files>
  <action>
1) **`apps/web/lib/og/pretendard.ts`** — cached font loader (per RESEARCH Pattern 3 + Pitfall 9):
   ```ts
   import { readFile } from 'node:fs/promises';
   import { join } from 'node:path';

   /**
    * Pretendard font loader for next/og ImageResponse.
    *
    * Uses Korean-subset woff2 (~110KB each) so combined fonts fit ImageResponse's
    * 500KB bundle limit (RESEARCH §Pitfall 3). Original .otf files (~1.5MB each)
    * remain on disk for next/font/local in layout.tsx — DO NOT delete.
    *
    * readFile happens once per process (module-level cache).
    * process.cwd() resolves to apps/web on Vercel deploy (verified Next.js docs).
    */
   let cached: { regular: Buffer; semibold: Buffer } | null = null;

   export async function loadPretendardFonts(): Promise<{ regular: Buffer; semibold: Buffer }> {
     if (cached) return cached;
     const [regular, semibold] = await Promise.all([
       readFile(join(process.cwd(), 'public/fonts/Pretendard-Regular.subset.woff2')),
       readFile(join(process.cwd(), 'public/fonts/Pretendard-SemiBold.subset.woff2')),
     ]);
     cached = { regular, semibold };
     return cached;
   }
   ```

2) **`apps/web/app/b/[slug]/opengraph-image.tsx`** — file-convention OG route (per RESEARCH Pattern 3 + UI-SPEC §7):
   ```tsx
   import { ImageResponse } from 'next/og';
   import { CITY_KO_MAP } from '@moajoa/core';
   import { getCachedPublicBoard } from '@/lib/cache';
   import { loadPretendardFonts } from '@/lib/og/pretendard';
   import { buildStaticMapsUrl, OG_GRAYSCALE_STYLE } from '@/lib/og/static-maps';
   import { getGoogleMapsKey } from '@/lib/env';

   export const alt = 'MOAJOA 공유 보드';
   export const size = { width: 1200, height: 630 };
   export const contentType = 'image/png';
   // Node runtime (Default since Next.js 15 for file conventions; D-06 'Edge' is a stale label
   // — RESEARCH §Pitfall 9: Edge can't readFile or use node:fs/promises reliably).
   export const runtime = 'nodejs';

   interface Props {
     params: Promise<{ slug: string }>;
   }

   export default async function Image({ params }: Props) {
     const { slug } = await params;
     const [view, fonts] = await Promise.all([
       getCachedPublicBoard(slug),
       loadPretendardFonts(),
     ]);

     const fontOptions = [
       { name: 'Pretendard', data: fonts.regular, weight: 400 as const, style: 'normal' as const },
       { name: 'Pretendard', data: fonts.semibold, weight: 600 as const, style: 'normal' as const },
     ];

     // Fallback: board not found
     if (!view) {
       return new ImageResponse(
         (
           <div
             style={{
               width: '100%',
               height: '100%',
               display: 'flex',
               alignItems: 'center',
               justifyContent: 'center',
               background: '#FFFFFF',
               fontFamily: 'Pretendard',
             }}
           >
             <div style={{ fontSize: 48, fontWeight: 600, color: '#F97316' }}>MOAJOA</div>
           </div>
         ),
         { ...size, fonts: fontOptions },
       );
     }

     const title = view.board.title;
     const cityKo = view.board.city_code ? CITY_KO_MAP[view.board.city_code] : null;
     const pinCount = view.places.length;
     const apiKey = getGoogleMapsKey();

     // Build Static Maps URL — only if we have key + at least 1 place
     let mapUrl: string | null = null;
     if (apiKey && view.places.length > 0) {
       try {
         mapUrl = buildStaticMapsUrl({
           places: view.places.map((p) => ({ lat: p.lat, lng: p.lng })),
           size: { width: 600, height: 630 },
           scale: 2,
           apiKey,
           styleParams: OG_GRAYSCALE_STYLE,
         });
       } catch {
         mapUrl = null;
       }
     }

     return new ImageResponse(
       (
         <div
           style={{
             width: '100%',
             height: '100%',
             display: 'flex',
             fontFamily: 'Pretendard',
             background: '#FFFFFF',
           }}
         >
           {/* Left 600×630 — text stack */}
           <div
             style={{
               width: 600,
               height: 630,
               display: 'flex',
               flexDirection: 'column',
               justifyContent: 'space-between',
               padding: 64,
             }}
           >
             <div style={{ display: 'flex', flexDirection: 'column' }}>
               <div
                 style={{
                   fontSize: 48,
                   fontWeight: 600,
                   color: '#0F172A',
                   lineHeight: 1.2,
                   // Satori 2-line clamp via display:-webkit-box not always reliable —
                   // substring fallback at ~40 chars per line × 2 = 80 chars.
                 }}
               >
                 {title.length > 80 ? `${title.slice(0, 79)}…` : title}
               </div>
               {cityKo && (
                 <div
                   style={{
                     fontSize: 28,
                     fontWeight: 400,
                     color: '#475569',
                     marginTop: 24,
                   }}
                 >
                   {cityKo}
                 </div>
               )}
               <div
                 style={{
                   fontSize: 24,
                   fontWeight: 400,
                   color: '#475569',
                   marginTop: cityKo ? 16 : 24,
                 }}
               >
                 핀 {pinCount}개
               </div>
             </div>
             <div style={{ fontSize: 24, fontWeight: 600, color: '#F97316' }}>MOAJOA</div>
           </div>

           {/* Right 600×630 — Static Maps PNG or fallback */}
           <div
             style={{
               width: 600,
               height: 630,
               display: 'flex',
               alignItems: 'center',
               justifyContent: 'center',
               background: '#F8FAFC',
             }}
           >
             {mapUrl ? (
               // eslint-disable-next-line @next/next/no-img-element
               <img
                 src={mapUrl}
                 width={600}
                 height={630}
                 style={{ objectFit: 'cover' }}
                 alt=""
               />
             ) : (
               <div style={{ fontSize: 20, fontWeight: 400, color: '#64748B' }}>
                 지도 미리보기 준비 중
               </div>
             )}
           </div>
         </div>
       ),
       { ...size, fonts: fontOptions },
     );
   }
   ```

3) **`apps/web/__tests__/og-image.test.ts`** — smoke test:
   ```ts
   import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';

   vi.mock('next/cache', () => ({
     unstable_cache: vi.fn((fn) => fn),
     revalidateTag: vi.fn(),
   }));
   vi.mock('@/lib/supabase/server', () => ({
     getSupabaseServer: vi.fn().mockResolvedValue({}),
   }));
   vi.mock('node:fs/promises', () => ({
     readFile: vi.fn().mockResolvedValue(Buffer.from('fake-font-bytes')),
   }));

   const getPublicBoardBySlugMock = vi.fn();
   vi.mock('@moajoa/api', () => ({
     getPublicBoardBySlug: (...args: unknown[]) => getPublicBoardBySlugMock(...args),
   }));

   // Stub ImageResponse to avoid Satori running on fake font bytes in jsdom.
   const imageResponseCtor = vi.fn();
   vi.mock('next/og', () => ({
     ImageResponse: class {
       constructor(...args: unknown[]) {
         imageResponseCtor(...args);
       }
     },
   }));

   describe('opengraph-image route', () => {
     beforeEach(() => {
       imageResponseCtor.mockClear();
       getPublicBoardBySlugMock.mockReset();
       vi.stubEnv('NEXT_PUBLIC_GOOGLE_MAPS_KEY', 'TEST_KEY');
     });
     afterEach(() => {
       vi.unstubAllEnvs();
       vi.resetModules();
     });

     it('returns ImageResponse with fallback when board not found', async () => {
       getPublicBoardBySlugMock.mockResolvedValue(null);
       const mod = await import('@/app/b/[slug]/opengraph-image');
       await mod.default({ params: Promise.resolve({ slug: 'missing01' }) });
       expect(imageResponseCtor).toHaveBeenCalledTimes(1);
       const [, opts] = imageResponseCtor.mock.calls[0] as [unknown, { fonts: unknown[]; width: number; height: number }];
       expect(opts.width).toBe(1200);
       expect(opts.height).toBe(630);
       expect(Array.isArray(opts.fonts)).toBe(true);
       expect(opts.fonts.length).toBe(2);
     });

     it('returns ImageResponse with mapUrl when board has places + key set', async () => {
       getPublicBoardBySlugMock.mockResolvedValue({
         board: { title: '도쿄', city_code: 'tokyo', description: null, cover_image_url: null, share_slug: 'abc12345', visibility: 'public', id: 'b1' },
         owner_display_name: '준',
         places: [{ lat: 35.68, lng: 139.69 }],
         links: [],
       });
       const mod = await import('@/app/b/[slug]/opengraph-image');
       await mod.default({ params: Promise.resolve({ slug: 'abc12345' }) });
       expect(imageResponseCtor).toHaveBeenCalledTimes(1);
     });

     it('exports size, alt, contentType, runtime', async () => {
       const mod = await import('@/app/b/[slug]/opengraph-image');
       expect(mod.size).toEqual({ width: 1200, height: 630 });
       expect(mod.alt).toBe('MOAJOA 공유 보드');
       expect(mod.contentType).toBe('image/png');
       expect(mod.runtime).toBe('nodejs');
     });

     it('does NOT throw when NEXT_PUBLIC_GOOGLE_MAPS_KEY missing (fallback to text)', async () => {
       vi.stubEnv('NEXT_PUBLIC_GOOGLE_MAPS_KEY', '');
       getPublicBoardBySlugMock.mockResolvedValue({
         board: { title: 'T', city_code: null, description: null, cover_image_url: null, share_slug: 'abc12345', visibility: 'public', id: 'b2' },
         owner_display_name: 'x',
         places: [{ lat: 1, lng: 2 }],
         links: [],
       });
       const mod = await import('@/app/b/[slug]/opengraph-image');
       await expect(mod.default({ params: Promise.resolve({ slug: 'abc12345' }) })).resolves.toBeDefined();
     });
   });
   ```

4) Run `pnpm --filter @moajoa/web test:run -- og-image` → 4 PASS.
5) Run `pnpm --filter @moajoa/web typecheck` → 0 errors.
6) Run `pnpm --filter @moajoa/web build` → build succeeds, no "page exceeds bundle size 500KB" warning (RESEARCH §Pitfall 3). If warning appears, return to Task 1 and tighten subset.
7) Commit: `feat(04-04): opengraph-image route with Pretendard subset + Static Maps embed`
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run -- og-image && pnpm typecheck && pnpm build 2>&1 | grep -E "(exceeds|500KB|opengraph-image)" | grep -v "exceeds" || true</automated>
  </verify>
  <done>
    - opengraph-image.tsx exports default async Image function + size/alt/contentType/runtime
    - runtime === 'nodejs' (NOT 'edge' — D-06 stale label corrected per Pitfall 9)
    - Pretendard fonts loaded via loadPretendardFonts() once per process
    - Static Maps URL built via buildStaticMapsUrl with OG_GRAYSCALE_STYLE
    - Fallback paths exercised: view null, key missing, places empty
    - CITY_KO_MAP imported from @moajoa/core (no `.js` extension)
    - All 4 og-image tests pass
    - typecheck passes
    - build succeeds without 500KB warning
    - 1 atomic commit (test + impl together since test is a smoke test, not RED→GREEN cycle)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual OG image visual verification + Kakao preview UAT</name>
  <what-built>
    - `/b/{slug}/opengraph-image` route generates 1200×630 PNG with:
      - Left panel: 보드 제목 (48/600) + 도시 (28/400) + 핀 수 (24/400) + MOAJOA wordmark (24/600 brand-500)
      - Right panel: Static Maps grayscale-tone PNG with up to 10 brand-color markers, or text fallback "지도 미리보기 준비 중"
    - Pretendard Korean characters render without system fallback breakage
    - Same `BOARD_REVALIDATE_TAG` invalidates both page and OG (single cache namespace)
  </what-built>
  <how-to-verify>
1. **Local dev preview:**
   ```bash
   pnpm --filter @moajoa/web dev
   ```

2. **Direct OG URL fetch (visual):**
   - Open `http://localhost:3000/b/{public-slug}/opengraph-image` in browser
   - PNG should display:
     - Left: 보드 제목 in Pretendard SemiBold 48px (한글 깨짐 X)
     - 도시명 (city_ko) — only if board has city_code; e.g. "도쿄" Pretendard Regular 28px
     - "핀 N개" Pretendard Regular 24px
     - "MOAJOA" Pretendard SemiBold 24px brand-500 (#F97316), 좌측 하단
     - Right panel: Google Static Maps PNG (grayscale tone, brand-color markers) OR "지도 미리보기 준비 중" text fallback (if Static Maps API not enabled / no key)
   - PNG dimensions = 1200×630 (Chrome DevTools → Inspect Element on image)

3. **Edge cases:**
   - URL with non-existent slug → fallback "MOAJOA" centered card
   - Board with city_code=null → 도시 줄 표시 X, only 핀 수
   - Board with places=0 → 우측에 "지도 미리보기 준비 중" 텍스트

4. **GCP Static Maps API enable 확인:**
   - GCP Console → APIs & Services → Library → "Maps Static API" → status가 "Enabled"인지 확인
   - 미적용 시: enable 클릭 → 재시도 (URL fetch)

5. **Kakao 카톡 미리보기 (production deploy 후):**
   - production URL `https://moajoa.app/b/{slug}` 을 카톡 본인 채팅창(나에게 보내기)에 붙여넣기
   - 미리보기 카드: 보드 제목 (한글) + 우측 mini-map 썸네일 + 핀 수 + MOAJOA wordmark
   - **검증 포인트:** 한글이 system fallback이 아닌 Pretendard로 표시되는가? (카톡 in-app browser는 OG image를 자체 cache하므로 처음 보낼 때 한 번만 fetch — 캐시 깨려면 `?v=2` query param 붙여서 재공유)

6. **Twitter/Slack 미리보기 (optional):**
   - Twitter Card Validator: https://cards-dev.twitter.com/validator (Twitter login 필요)
   - Slack 채널에 URL 붙여넣기 → unfurl 카드 확인

7. **Bundle size 회귀 가드:**
   ```bash
   pnpm --filter @moajoa/web build 2>&1 | grep -i "opengraph\|500KB\|exceeds"
   ```
   "exceeds 500KB" warning이 없어야 함.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (예: "한글 깨짐", "미니맵 fallback만 표시됨 — Static Maps enable 필요", "카톡 카드 텍스트 안 보임")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External scraper (Kakao, Slack, Twitter) → /opengraph-image | Anonymous GET, expected. No secrets in response. |
| process.cwd()/public/fonts/* → readFile | Path is hardcoded (no user input); reads are within app sandbox. |
| Static Maps URL fetch (Satori inside ImageResponse) → maps.googleapis.com | API key in URL is the same NEXT_PUBLIC_GOOGLE_MAPS_KEY (already client-exposed by design). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | D (Denial of Service) | OG image route hit by scraper flood → RPC + Static Maps cost spike | mitigate | Shared BOARD_REVALIDATE_TAG → RPC dedup with page (Pitfall 8). Static Maps response is cached by Kakao/Slack scrapers after first fetch (D-08 `revalidate: false`). |
| T-04-04-02 | I (Info disclosure) | OG image leaks private board content | mitigate | getCachedPublicBoard → getPublicBoardBySlug RPC enforces visibility='public' (SECURITY DEFINER). Private boards return null → fallback generic card. |
| T-04-04-03 | I (Info disclosure) | Google Maps API key visible in OG image's <img src> URL | accept | Same key already exposed client-side for interactive map (D-19 lock). HTTP referer restriction is the defense. |
| T-04-04-04 | T (Tampering) | readFile path traversal | mitigate | join(process.cwd(), 'public/fonts/Pretendard-*.subset.woff2') — hardcoded segments, no user input. |
| T-04-04-05 | D (Denial of Service) | Satori OOM on huge title | mitigate | title.slice(0, 79) substring fallback caps text length; ImageResponse 4MB output cap is Next.js default. |
| T-04-04-06 | I (Info disclosure) | extraction_costs not logged for Static Maps call (only LLM/Places) | accept | Static Maps spend rides on same GCP project; Phase 2 D-13 billing alerts cover it. EXTRACT-04 explicitly scopes "extraction" pipeline, not OG generation. |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web test:run -- og-image` → 4 tests pass
- `pnpm --filter @moajoa/web typecheck` exits 0
- `pnpm --filter @moajoa/web build` succeeds with no "exceeds 500KB" warning for opengraph-image
- `ls -la apps/web/public/fonts/Pretendard-*.subset.woff2` shows 2 files
- Combined subset font size < 500KB:
  ```bash
  python3 -c "import os; r=os.path.getsize('apps/web/public/fonts/Pretendard-Regular.subset.woff2'); s=os.path.getsize('apps/web/public/fonts/Pretendard-SemiBold.subset.woff2'); print(f'R:{r} S:{s} Total:{r+s}'); exit(0 if r+s < 500000 else 1)"
  ```
- `grep -n "runtime = 'nodejs'" apps/web/app/b/\\[slug\\]/opengraph-image.tsx` returns 1 hit
- `grep -n "loadPretendardFonts" apps/web/app/b/\\[slug\\]/opengraph-image.tsx` returns 1 hit
- `grep -n "buildStaticMapsUrl" apps/web/app/b/\\[slug\\]/opengraph-image.tsx` returns 1 hit
- `grep -n "OG_GRAYSCALE_STYLE" apps/web/app/b/\\[slug\\]/opengraph-image.tsx` returns 1 hit
- `grep -n "지도 미리보기 준비 중" apps/web/app/b/\\[slug\\]/opengraph-image.tsx` returns 1 hit
- `grep -rn "from '@moajoa/core/.*\\.js'" apps/web/app/b/\\[slug\\]/opengraph-image.tsx` returns 0 hits
- Human checkpoint signed ("approved" or remediation list)
- (Manual) `http://localhost:3000/b/{slug}/opengraph-image` returns 1200×630 PNG with Korean text rendered
</verification>

<success_criteria>
- 4 smoke tests pass
- Pretendard subset woff2 < 150KB each, combined < 500KB
- OG image route generates 1200×630 PNG without runtime error
- Korean text (보드 제목, 도시명, 핀 N개) renders in Pretendard (no system fallback breakage)
- Static Maps mini-map embedded when key set + places ≥ 1
- Text-only fallback when key missing or places empty
- Same BOARD_REVALIDATE_TAG → RPC dedup with page.tsx
- Build succeeds with no 500KB warning
- Human verification of /b/{slug}/opengraph-image PNG passes
- 2 atomic commits (font subset + OG route)
</success_criteria>

<output>
After completion, create `.planning/phases/04-public-board-web/04-04-SUMMARY.md` with:
- Files created/modified
- Final subset woff2 sizes (Regular + SemiBold)
- Bundle size impact ("opengraph-image" route size in `next build` output)
- Test count
- Manual UAT findings: GCP Maps Static API enable status, Kakao preview screenshot link
- A1 verified (subset solved Pitfall 3); A3 verified (Static Maps key reuse works); A5 verified (Satori loads Google Static Maps URL)
- Notable deviations from CONTEXT.md / RESEARCH.md with rationale
</output>
