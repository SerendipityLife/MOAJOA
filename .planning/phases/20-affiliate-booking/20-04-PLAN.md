---
phase: 20-affiliate-booking
plan: 04
type: execute
wave: 3
depends_on: ["20-02", "20-03"]
files_modified:
  - packages/api/src/queries/bookings.ts
  - packages/api/src/queries/bookings.test.ts
  - packages/api/src/queries/index.ts
autonomous: true
requirements: [BOOK-02, ATTR-02]
must_haves:
  truths:
    - "iOS가 import할 체크리스트 CRUD + 클릭 로깅 래퍼가 plans.ts house 계약(client-first, { error } throw, RLS-only)으로 존재한다"
    - "logBookingClick이 booking_clicks INSERT 후 checklist_item_id 존재 시 해당 항목을 todo→clicked로 전이시킨다 (D-11 '확인함' 자동)"
    - "reconcileChecklist가 core deriveChecklistAutos 계산 결과만 DB에 반영한다 (파생 규칙 재구현 0 — 단일 정의)"
  artifacts:
    - "packages/api/src/queries/bookings.ts"
    - "packages/api/src/queries/bookings.test.ts (makeChain 하네스)"
  key_links:
    - "bookings.ts → @moajoa/core deriveChecklistAutos/ChecklistItem (파생 로직 위임)"
    - "bookings.ts → database.ts booking_checklist_items/booking_clicks 타입 (20-02 재생성분)"
    - "iOS 클릭 핸들러(20-05) → logBookingClick — wrapper는 throw, 삼키기(.catch)는 호출부 책임 (D-14)"
---

<objective>
@moajoa/api에 `bookings.ts` 타입드 쿼리 계층 신설 — 체크리스트 CRUD(listChecklist/addManualItem/setItemStatus/deleteChecklistItem), reconcile 반영(reconcileChecklist), 클릭 로깅+'확인함' 전이(logBookingClick), 확인함-해제 판정용 클릭 조회(listClickedChecklistItemIds). iOS 두 표면(plan/book 탭)이 공유할 단일 데이터 seam (D-03 "데이터는 하나, 표면만 둘").

Purpose: BOOK-02 체크리스트와 ATTR-02 클릭 기록의 DB 접근을 house 계약으로 고정. RLS(can_read/can_edit_trip)가 유일한 게이트 — 클라 측 멤버십 중복 체크 금지 (T-18-17 선례).
Output: bookings.ts + 테스트 (plans.test.ts makeChain 하네스), api 전체 green.
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-affiliate-booking/20-RESEARCH.md
@.planning/phases/20-affiliate-booking/20-PATTERNS.md
@packages/api/src/queries/plans.ts
@packages/api/src/queries/plans.test.ts
</context>

## Artifacts this phase produces (this plan's share)

- `packages/api/src/queries/bookings.ts` exports:
  - `listChecklist(client, tripId): Promise<ChecklistItem[]>`
  - `addManualItem(client, input: { trip_id: string; title: string }): Promise<ChecklistItem>`
  - `setItemStatus(client, itemId: string, status: 'todo'|'clicked'|'done'): Promise<ChecklistItem>`
  - `deleteChecklistItem(client, itemId: string): Promise<void>`
  - `reconcileChecklist(client, tripId, derived: { toInsert; toDeleteIds }): Promise<void>`
  - `logBookingClick(client, input: { trip_id; place_id?; user_id; provider; click_token; checklist_item_id? }): Promise<void>`
  - `listClickedChecklistItemIds(client, tripId): Promise<Set<string>>`
- `packages/api/src/queries/index.ts` barrel append 1줄

<!-- planner-discipline-allow: deriveChecklistAutos -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — bookings.test.ts (makeChain 하네스, 쿼리 shape + throw 계약)</name>
  <files>packages/api/src/queries/bookings.test.ts</files>
  <read_first>
    - packages/api/src/queries/plans.test.ts (L30-55 makeChain/makeClient 하네스 — 복사 대상; upsert 필요 시 methods 배열에 'upsert' 추가)
    - packages/api/src/queries/plans.ts (house 계약 doc-comment 스타일)
    - packages/core/src/checklist.ts (20-03 산출물 — ChecklistItem shape, deriveChecklistAutos 시그니처)
  </read_first>
  <behavior>
    - listChecklist: from('booking_checklist_items') + .select('*') + .eq('trip_id', TRIP) + .order('created_at') 체인 단언; { error } → throw
    - addManualItem: .insert({ trip_id, title, kind: 'custom', source: 'manual', status: 'todo' }) + .select('*').single()
    - setItemStatus: .update({ status }) + .eq('id', ITEM) + .select('*').single()
    - deleteChecklistItem: .delete() + .eq('id', ITEM)
    - reconcileChecklist: toInsert 비었으면 insert 미호출 / toDeleteIds 비었으면 delete 미호출 / 있으면 .insert(rows 배열) 및 .delete().in('id', ids) 호출 단언 — **파생 계산 없음** (인자로 받은 diff만 반영)
    - logBookingClick: from('booking_clicks').insert({...}) 단언; checklist_item_id 존재 시 booking_checklist_items .update({ status:'clicked' }).eq('id', id).eq('status','todo') 후속 호출 / 부재(null) 시 update 미호출; { error } → throw (삼키기는 호출부)
    - listClickedChecklistItemIds: from('booking_clicks').select + .eq('trip_id') + .not('checklist_item_id','is',null) 체인 → Set 변환
    - 전 함수: 클라 측 멤버십 검사 코드 0 (RLS-only 계약)
  </behavior>
  <action>
    plans.test.ts의 makeChain/makeClient 하네스를 복사하되 methods 배열에 'in', 'not' 추가 (reconcile delete-batch·클릭 조회 체인용). UUID fixture 상수 idiom. behavior의 케이스를 전부 작성 — bookings.ts 미구현이므로 RED 확인 후 `test({phase}-{plan})` 커밋. per-fn 정확 테이블명·인자 단언 + { error } throw 단언 (19-02 date-polls.test.ts 결). D-11 인용: logBookingClick의 todo→clicked 조건부 전이가 '확인함' 자동 표시의 유일한 쓰기 경로.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api test -- bookings 2>&1 | grep -ci "fail"</automated>
  </verify>
  <acceptance_criteria>
    - RED 상태: bookings 테스트가 모듈 부재/실패로 non-zero (실패 확인 후 커밋)
    - 케이스 수 >= 14 (7함수 × shape+throw 최소 2)
    - `grep -c "makeChain" packages/api/src/queries/bookings.test.ts` >= 1
  </acceptance_criteria>
  <done>쿼리 계약이 테스트로 먼저 고정 (RED 커밋)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — bookings.ts 구현 + barrel</name>
  <files>packages/api/src/queries/bookings.ts, packages/api/src/queries/index.ts</files>
  <read_first>
    - packages/api/src/queries/bookings.test.ts (Task 1 산출물 — 구현 계약)
    - packages/api/src/queries/plans.ts (read: maybeSingle+null / write: insert+.select('*').single() / delete 패턴)
    - packages/api/src/queries/date-polls.ts (.order() 목록 + .delete().eq() 패턴)
    - packages/api/src/types/database.ts (20-02 재생성분 — booking_checklist_items/booking_clicks 타입)
  </read_first>
  <action>
    bookings.ts 신설 — plans.ts 헤더 doc-comment 스타일로 RLS 계약 명시 ("can_read_trip이 SELECT를, can_edit_trip이 write를 게이트 — wrapper는 추가 클라 측 멤버십 체크를 하지 않음", T-18-17 미러, D-12). 전 함수 client-first arg + `const { data, error } = await …; if (error) throw error;` shape:
    - listChecklist / addManualItem / setItemStatus / deleteChecklistItem — behavior 체인 그대로. addManualItem의 title은 호출부가 @moajoa/core ManualItemTitleSchema로 검증(여기선 재검증 안 함 — DB CHECK 1..80 이중화가 최후 방어).
    - reconcileChecklist(client, tripId, derived) — 인자로 받은 { toInsert, toDeleteIds }만 반영: toInsert 각 행에 trip_id/source 'auto'/status 'todo' 세팅해 일괄 insert (`checklist_singleton_uq`/`checklist_place_uq` 충돌 시 { error } throw — 동시성 드묾, 재조회로 자연 회복); toDeleteIds는 `.delete().in('id', toDeleteIds)`. 파생 규칙은 core deriveChecklistAutos 소유 — 이 함수에 조건 로직 금지 (단일 정의).
    - logBookingClick — insert 후 input.checklist_item_id 존재 시 `.update({ status: 'clicked' }).eq('id', id).eq('status', 'todo')` (이미 done이면 무변경 — D-11: 완료의 원천은 사용자). **wrapper는 throw** — fire-and-forget 삼키기는 iOS 호출부의 `.catch(() => {})` 책임 (D-14, PATTERNS 명시).
    - listClickedChecklistItemIds — booking_clicks에서 trip 스코프 non-null checklist_item_id를 Set으로 (book 탭의 완료 해제 시 clicked/todo 판정용, UI-SPEC Screen 3).
    barrel index.ts에 `export * from './bookings';` append. GREEN 확인 후 `feat(20-04)` 커밋.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api test && pnpm --filter @moajoa/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/api test` exit 0 (기존 35 + bookings 신규 전부 green)
    - `pnpm --filter @moajoa/api typecheck` exit 0 (재생성 database.ts 대상)
    - `grep -c "export \* from './bookings'" packages/api/src/queries/index.ts` == 1
    - `grep -c "can_read_trip\|can_edit_trip\|getMyTripRole" packages/api/src/queries/bookings.ts` == 0 — 코드 경로에 클라 측 권한 체크 0 (doc-comment 언급은 헬퍼명 아닌 설명으로만; 이 gate가 코멘트에 걸리면 코멘트에서 헬퍼 함수명 원문 대신 "RLS 헬퍼"로 reword)
    - `grep -c "deriveChecklistAutos" packages/api/src/queries/bookings.ts` == 0 (파생은 core, api는 반영만 — 호출부가 조합)
  </acceptance_criteria>
  <done>house 계약 쿼리 계층 완성 — iOS 20-05~07이 barrel에서 import 가능</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api 래퍼(anon key + user JWT) → Postgres RLS | 모든 CRUD가 0021 정책으로 게이트 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02 | Spoofing | logBookingClick user_id | high | mitigate | RLS `with check (user_id = auth.uid())`가 서버측 강제 (0021, 20-02 매트릭스 검증) — wrapper는 통과만, 위조 시 42501 throw |
| T-20-05 | Elevation | 체크리스트 write 래퍼 | high | mitigate | can_edit_trip RLS가 유일 게이트 — 클라 중복 체크 금지 계약이 오히려 우회 착각을 방지 (T-18-17) |
| T-20-11 | Tampering | reconcile의 파생 로직 드리프트 | medium | mitigate | api에 조건 로직 금지 grep 게이트 — 파생은 core 순수함수 단일 정의, D-13 보존 규칙이 두 곳에서 어긋날 표면 제거 |
</threat_model>

<verification>
- api 전체 suite green + typecheck 0
- bookings.ts에 직접 테이블 권한 로직 0 (RLS-only)
</verification>

<success_criteria>
- BOOK-02 데이터 접근 계층 + ATTR-02 클릭 기록 경로 완성
- '확인함' 전이(D-11)가 logBookingClick 한 곳에서만 일어남
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-04-SUMMARY.md` when done
</output>
