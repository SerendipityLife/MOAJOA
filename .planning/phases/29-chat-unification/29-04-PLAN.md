---
phase: 29-chat-unification
plan: 04
type: execute
wave: 3
depends_on: ["29-01", "29-03"]
files_modified:
  - apps/web/app/poll/[code]/_components/poll-guest-island.tsx
  - apps/web/__tests__/poll-guest-island.test.tsx
  - apps/web/app/poll/[code]/page.tsx
autonomous: false
requirements: [CHAT-05, CHAT-07]

user_setup:
  - service: supabase (원격 프로젝트 xfoauhsraguyrifingct)
    why: "0032 마이그레이션 원격 적용 — 라이브 /poll 채팅 join의 전제"
    dashboard_config:
      - task: "main push 시 Supabase↔GitHub 자동 적용 확인 또는 사용자 터미널에서 `supabase db push` 1회"
        location: "터미널 또는 Supabase 대시보드 (25-01/24-01 선례)"

must_haves:
  truths:
    - "/poll/[code] 방문자가 첫 채팅 전송(또는 첫 투표)에서 닉네임 게이트 1회를 거쳐 익명 멤버가 되고, 메시지가 trip_messages에 저장된다"
    - "/poll과 /t·/moa의 대화가 같은 저장소(trip_messages)·같은 채널(moa:{tripId})에서 수렴한다 — 호스트 채팅탭에 /poll 메시지가 보인다"
    - "재방문 멤버는 게이트 없이 이력을 본다 (private 레거시 poll trip 포함 — getTrip 미호출)"
    - "비멤버에겐 이력이 보이지 않고(RLS) 전용 빈 상태 카피가 보인다"
  artifacts:
    - path: "apps/web/app/poll/[code]/_components/poll-guest-island.tsx"
      provides: "게이트+채팅 채널-소유 래퍼 island (PollVoteIsland와 게이트 공유)"
      min_lines: 150
    - path: "apps/web/app/poll/[code]/page.tsx"
      provides: "PollGuestIsland 마운트 (SSR 셸 무수정)"
      contains: "PollGuestIsland"
  key_links:
    - from: "poll-guest-island.tsx"
      to: "join_moa_by_poll_code"
      via: "joinMoaByPollCode(client, code)"
      pattern: "joinMoaByPollCode"
    - from: "poll-guest-island.tsx"
      to: "moa:{tripId} 채널"
      via: "moaChannelName(tripId) + pre-subscribe 바인딩"
      pattern: "moaChannelName"
    - from: "poll-guest-island.tsx"
      to: "MoaChat"
      via: "controlled props (무수정 소비)"
      pattern: "<MoaChat"
---

<objective>
D-03: `/poll/[code]`에 통일 채팅 탑재 — 신규 채널-소유 래퍼 `poll-guest-island.tsx`가 PollVoteIsland와 채팅 섹션을 함께 감싸고(게이트 공유, A-7 "어느 쪽이 먼저든 1회"), `signInAnonymously`+`joinMoaByPollCode`로 익명 승격 후 `moa:{tripId}` 채널·`trip_messages`로 합류. page.tsx는 마운트 교체만.

Purpose: CHAT-05 완결 — /poll·/t·/moa 대화가 동일 메커니즘으로 수렴. MoaIsland 재사용은 기각(fixed inset-0 풀스크린·seed 9종 과잉 — RESEARCH Q2), 경량 래퍼가 정답. moa-chat/moa-island/moa-tab-bar는 무수정.
Output: poll-guest-island.tsx + 테스트 + page.tsx 마운트 교체 + 원격 0032 적용 human-action 게이트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-chat-unification/29-CONTEXT.md (D-03/D-03a)
@.planning/phases/29-chat-unification/29-RESEARCH.md (Q2 래퍼 책임 7종 · Pitfall 2/3/4/7 · Open Q2 authed 전환 권고)
@.planning/phases/29-chat-unification/29-PATTERNS.md (§4 (a)~(i) 합성 조각 전문 — 이 plan의 구현 사전)
@.planning/phases/29-chat-unification/29-UI-SPEC.md (Screen Contract 2 · Copywriting · A-6/A-7/A-8/A-9)

<interfaces>
<!-- MoaChat controlled props (moa-chat.tsx:17-32 — 무수정 소비) -->
```typescript
export interface MoaChatProps {
  messages: TripMessage[];
  currentUserId: string;
  viewers: number;
  onSend: (body: string, replyToPlaceId: string | null) => Promise<void>;
  replyToPlaceId: string | null;
  onClearReply: () => void;
  placesById: Record<string, { seqNo: number; name: string }>;
  onChipTap: (placeId: string) => void;
}
```
<!-- PollVoteIsland embed props (25-02 seam — 29-03 이후 embedded는 소멸) -->
```typescript
// code, tripId, mode, status, options (page.tsx :81-87 기존 전달분)
// + deviceToken?: string   — presence key·castDateVoteAuthed 인자 (uid)
// + nickname?: string      — state seed
// + onRequireMember?: () => Promise<{ uid: string; nickname: string }>
```
<!-- api / core seam -->
```typescript
import { joinMoaByPollCode, getMyTripRole, listTripMessages, sendTripMessage } from '@moajoa/api';
import { moaChannelName, TripMessageCreateSchema, type TripMessage } from '@moajoa/core';
import { getStoredNickname, setStoredNickname } from '@/lib/device-token';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poll-guest-island.tsx — 게이트+채팅 채널-소유 래퍼 (TDD RED→GREEN)</name>
  <files>apps/web/app/poll/[code]/_components/poll-guest-island.tsx, apps/web/__tests__/poll-guest-island.test.tsx</files>
  <read_first>
    - .planning/phases/29-chat-unification/29-PATTERNS.md §4 (a)~(i) — 합성할 검증 코드 조각 전문 (이 task의 구현 사전, 신규 로직 0)
    - apps/web/app/t/[slug]/_components/guest-surface.tsx (게이트 브리지 :92-95/:233-240 · ensureGuestMember :171-187 · handleConfirmNickname/handleCloseGate :242-272 · 세션 effect :99-126)
    - apps/web/app/moa/[id]/_components/moa-island.tsx (채널 effect :194-231 · appendMessage :131-133 · handleSend :466-476 · MoaChat 마운트 :662-671)
    - apps/web/app/moa/[id]/_components/moa-chat.tsx (props 계약 :17-32 · compose row :148-171 — 비멤버 compose 미러 원형 · 빈 상태 클래스 :92-95)
    - apps/web/__tests__/moa-island.test.tsx (:5-37 채널 스텁 하네스 — 그대로 재사용)
    - apps/web/app/poll/[code]/page.tsx (:81-87 — 래퍼가 받을 props 원형)
  </read_first>
  <behavior>
    - Test 1 (비멤버 렌더): 세션 없음 → MoaChat 스텁 미마운트, 빈 상태 카피 `참여하면 지금까지의 대화를 볼 수 있어요` 렌더, compose input 존재, `client.channel` **미호출** (Pitfall 4), PollVoteIsland 스텁에 `onRequireMember` 전달됨
    - Test 2 (첫 전송 게이트): compose 입력+보내기 → NicknameGateSheet open → confirm → `signInAnonymously({options:{data:{name}}})` → `joinMoaByPollCode(client, code)` → `setStoredNickname` 순서 호출 + `listTripMessages` hydrate + `sendTripMessage` 호출 (**`getTrip` 미호출 단언** — Pitfall 2)
    - Test 3 (재방문 멤버): `auth.getUser` uid + `getMyTripRole`→'voter' → 게이트 없이 `listTripMessages` hydrate + MoaChat 스텁 마운트
    - Test 4 (채널 소유): joined 시 `client.channel('moa:{tripId}', {config:{presence:{key: uid}}})` 정확 1회 + pre-subscribe 바인딩 2개(postgres_changes `trip_messages` filter `trip_id=eq.{tripId}` + presence sync) + SUBSCRIBED→track + 언마운트 시 removeChannel 동일 인스턴스
    - Test 5 (dedup): postgres_changes 콜백에 같은 id 메시지 2회 → messages 1건 (id-dedup append)
    - Test 6 (게이트 취소): 전송 → 게이트 open → onClose → draft 복원(입력값 유지) — reject 경로 (Pitfall 7 수용: 토스트 1회 허용)
  </behavior>
  <action>
**RED (commit `test(29-04): failing poll-guest-island cases`):** `apps/web/__tests__/poll-guest-island.test.tsx` 신규 — **반드시 `apps/web/__tests__/` 위치** (vitest include 제약, `_components/__tests__/`는 미수집 — 25-02/25-04 선례). moa-island.test.tsx :5-37 채널 스텁(바인딩 캡처+동기 SUBSCRIBED) verbatim 재사용 + `vi.mock('@moajoa/api')` 개별 vi.fn 위임 + MoaChat/PollVoteIsland/NicknameGateSheet 스텁(배선만 검증 — 프레젠테이션은 각자 스위트 몫). 위 6케이스.

**GREEN (commit `feat(29-04): poll-guest-island — unified chat on /poll (D-03)`):** `apps/web/app/poll/[code]/_components/poll-guest-island.tsx` 신규 `'use client'` island. Props: `{ code, tripId, mode, status, options }` (page.tsx :81-87 그대로 수령). PATTERNS §4 조각을 다음 구조로 합성 — **전 조각이 기존 검증 코드 미러, 발명 금지**:

1. **상태:** `userId`·`joined`·`nickname`·`messages: TripMessage[]`·`viewers`·`gateOpen` + `gateResolve`/`gateReject` refs (§4a).
2. **세션 effect (재방문, §4d):** `auth.getUser()` → uid 있으면 `getMyTripRole(client, tripId, uid).catch(() => null)` → role이면 `getStoredNickname()` 복원 + `listTripMessages(client, tripId)` hydrate + `setJoined(true)`. **`getTrip` 호출 금지** (Pitfall 2 — 레거시 dateless-poll trip은 `visibility='private'`: voter 멤버여도 trips SELECT 불가, join 성공 후 채팅 영구 빈 화면이 됨. trip_id는 SSR seed에서 이미 보유).
3. **requireMember 브리지 (§4a):** guest-surface :233-240 verbatim — PollVoteIsland `onRequireMember`와 채팅 첫 전송이 **같은 브리지** 공유 (A-7 "어느 쪽이 먼저든 1회").
4. **ensureGuestMember (§4b):** guest-surface :171-187 미러, `joinMoa(client, slug)` → `joinMoaByPollCode(client, code)`만 교체. `signInAnonymously({ options: { data: { name: nick } } })` (data.name 미주입 시 display_name='user' — Pitfall).
5. **handleConfirmNickname/handleCloseGate (§4c):** guest-surface :242-272 미러 — hydrate는 `listTripMessages`만. 순서 고정: ensureGuestMember → setUserId/setNickname → hydrate → `setJoined(true)` → `gateResolve`. 실패 토스트 `'참여하지 못했어요. 다시 시도해 주세요'` (기존 카피 무수정).
6. **채널 effect (§4e):** `if (!joined || !userId) return;` (Pitfall 4 — join 전 구독 금지: WALRUS 무음 0건 + presence 유령). `client.channel(moaChannelName(tripId), { config: { presence: { key: userId } } })` → `.on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'trip_messages', filter: \`trip_id=eq.${tripId}\` }, (payload) => appendMessage(payload.new as TripMessage))` → `.on('presence', { event: 'sync' }, () => setViewers(Object.keys(channel.presenceState()).length))` → `.subscribe(async (s) => { if (s === 'SUBSCRIBED') await channel.track({ user_id: userId, nickname, online_at: new Date().toISOString() }); })`. **전 바인딩 subscribe 이전 체이닝** (#1917 — 사후 추가 무음 no-op). cleanup `removeChannel`. 이 래퍼가 `moa:{tripId}` topic의 /poll 내 **유일 소유자** (같은 topic 2채널 = 배달 탈취).
7. **appendMessage (§4f):** id-dedup — `prev.some((m) => m.id === row.id) ? prev : [...prev, row]`.
8. **handleSend (§4g):** `!joined || !uid`면 `await requireMember()` → `TripMessageCreateSchema.parse({ trip_id: tripId, nickname: nick, body, reply_to_place_id: null })` (§4.5 UI 경계 Zod) → `sendTripMessage` → `appendMessage(row)` (echo는 dedup).
9. **렌더:**
   - `<PollVoteIsland code={code} tripId={tripId} mode={mode} status={status} options={options} deviceToken={userId ?? undefined} nickname={nickname || undefined} onRequireMember={requireMember} />` — guest-surface pollSection 미러(embedded 없음 — 29-03에서 소멸). **Open Q2 채택: /poll 투표를 authed 경로로 전환** (onRequireMember 존재 → castDateVoteAuthed). 재방문자의 구 device_token 투표행 + 신 uid 투표행 중복 엣지는 낮은 확률·낮은 피해(집계 +1)로 **수용 — 주석으로 기록** (RESEARCH Open Q2).
   - 채팅 섹션 (기존 한마디 위치 계승): `<section className="mt-8 border-t border-neutral-200 pt-6">` + `<h3 className="mb-3 text-sm font-semibold text-neutral-700">채팅</h3>` + `<div className="h-[400px]">` (A-6 — MoaChat은 flex h-full 전제).
   - **joined:** `<MoaChat messages={messages} currentUserId={userId ?? ''} viewers={viewers} onSend={handleSend} replyToPlaceId={null} onClearReply={() => {}} placesById={{}} onChipTap={() => {}} />` — placesById 빈 객체 = 칩 전부 미해석·미렌더 (A-9, Pitfall 9 기존 경로).
   - **비멤버 (A-7, planner 재량 확정):** MoaChat 미마운트 — `flex h-full flex-col` 래퍼에 (a) 빈 상태 `<p className="flex-1 py-10 text-center text-sm text-neutral-400">참여하면 지금까지의 대화를 볼 수 있어요</p>` (MoaChat 빈 상태 클래스 미러 — 멤버 카피 "첫 메시지를 남겨보세요 💬"와 이중 표시 충돌 회피, moa-chat diff 0 유지) + (b) compose row를 moa-chat :148-171 **verbatim 미러** (자체 draft/sending state·한글 IME 조합 가드 `!e.nativeEvent.isComposing`·maxLength 140·같은 클래스·`보내기`) — send는 handleSend 공유: 성공 시 joined 전환으로 MoaChat이 이력과 함께 인계, 실패/취소 시 draft 복원 + 에러 토스트 `'메시지를 보내지 못했어요. 다시 시도해주세요.'` (moa-chat 계약 미러 — 게이트 취소도 같은 경로, Pitfall 7 수용·명시적 비목표).
   - `<NicknameGateSheet open={gateOpen} onConfirm={handleConfirmNickname} onClose={handleCloseGate} />` — 카피 무수정 재사용.
10. **금지:** moa-chat/moa-island/moa-tab-bar/nickname-gate-sheet 수정 0 · reconcile(전체 refetch) 패턴 0 (append-only INSERT는 dedup이 규약) · 신규 hex/아이콘/모션 0 (HC-2) · 워크스페이스 import `.js` 0.
  </action>
  <verify>
    <automated>CI=true pnpm --filter web test -- __tests__/poll-guest-island.test.tsx __tests__/moa-chat.test.tsx __tests__/moa-island.test.tsx && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/poll/[code]/_components/poll-guest-island.tsx` 존재, `grep -n "joinMoaByPollCode\|moaChannelName\|TripMessageCreateSchema\|<MoaChat\|<NicknameGateSheet\|<PollVoteIsland"` 전부 존재
    - `! grep -n "getTrip\b" 'apps/web/app/poll/[code]/_components/poll-guest-island.tsx'` (Pitfall 2)
    - `grep -n "참여하면 지금까지의 대화를 볼 수 있어요" 'apps/web/app/poll/[code]/_components/poll-guest-island.tsx'` 1건 (A-7 카피)
    - `grep -c "channel(" 테스트에서 채널명 단언이 `moaChannelName` 산출값과 일치 · 바인딩 2개 · join 전 미구독
    - moa-chat.tsx / moa-island.tsx / moa-tab-bar.tsx / nickname-gate-sheet.tsx `git diff` 0줄 (이 plan)
    - `CI=true pnpm --filter web test` 신규 6케이스 + moa-chat 9·moa-island 기존 스위트 그린 · `pnpm --filter web typecheck` exit 0
    - `! grep -rn "from '.*\.js'" 'apps/web/app/poll/[code]/_components/poll-guest-island.tsx'`
  </acceptance_criteria>
  <done>/poll에 게이트 공유·채널 소유 래퍼 완성 — 비멤버 빈 상태·첫 전송 게이트·재방문 hydrate·realtime append 전부 단위 검증</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx 마운트 교체 + 풀 스위트·빌드 게이트</name>
  <files>apps/web/app/poll/[code]/page.tsx</files>
  <read_first>
    - apps/web/app/poll/[code]/page.tsx 전문 (:3-4 import · :81-87 PollVoteIsland 마운트 — 교체 대상 · generateMetadata/크롬/footer — 무수정 대상)
  </read_first>
  <action>
**commit `feat(29-04): mount unified chat island on /poll page`:** page.tsx surgical 2곳만:
1. import `PollVoteIsland` → `PollGuestIsland` (`./_components/poll-guest-island`).
2. :81-87 마운트 블록 교체 — props 동일 (`code={code} tripId={poll.trip_id} mode={poll.mode} status={poll.status} options={poll.options ?? []}`), 주석의 "chat hydrate" 서술은 래퍼 기준으로 갱신.

**무수정:** generateMetadata(:15-24)·SSR 크롬(:60-78)·footer(:89-99)·`getCachedPoll` 캐시 경로 — 채팅은 전부 client hydrate (SSR 캐시 무독성 유지, Pitfall 2 계승). page.tsx에 `'use client'` 추가 금지 (RSC 유지).

**게이트:** `CI=true pnpm -r test` + `pnpm -r typecheck` + `pnpm --filter web build` PASS + HC-7 grep 재확인 (29-03 이후 신규 파일이 은퇴 문자열을 재도입하지 않았는지).
  </action>
  <verify>
    <automated>! grep -n "PollVoteIsland" 'apps/web/app/poll/[code]/page.tsx' && grep -n "PollGuestIsland" 'apps/web/app/poll/[code]/page.tsx' && ! grep -rn "한마디\|투표가 마감되어 메시지를 남길 수 없어요\|이 메시지를 삭제할까요?" apps/web --include="*.ts" --include="*.tsx" --exclude-dir=.next && CI=true pnpm -r test && pnpm -r typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx에서 `PollVoteIsland` 0건 · `PollGuestIsland` ≥ 2건 (import + 마운트)
    - `! grep -n "'use client'" 'apps/web/app/poll/[code]/page.tsx'` (RSC 유지)
    - `git diff 'apps/web/app/poll/[code]/page.tsx'` 변경이 import·마운트 블록·해당 주석에 한정 (generateMetadata/크롬/footer diff 0)
    - `CI=true pnpm -r test` + `pnpm -r typecheck` exit 0 · `pnpm --filter web build` PASS (`ƒ /poll/[code]` 라우트 존재)
    - HC-7 grep 3종 0건 유지
  </acceptance_criteria>
  <done>/poll이 통일 채팅 래퍼를 마운트 — 전 스위트·빌드 그린, SSR 셸 무독성 유지</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: 원격 0032 적용 + 라이브 스팟 체크 (human-action)</name>
  <files>없음 — 원격 DB 상태만 (코드 변경 0)</files>
  <action>사용자에게 아래 how-to-verify 절차를 안내하고 대기: 원격 0032 적용(main push 자동 적용 또는 사용자 터미널 `supabase db push`) 후 라이브 /poll 채팅 스팟 체크. Claude가 대신 push하지 않는다 — 이 프로젝트에서 프로덕션 원격 push는 human-action 게이트 (24-01/25-01 선례).</action>
  <what-built>0032 `join_moa_by_poll_code` 마이그레이션(로컬 적용·smoke 그린)과 /poll 통일 채팅 코드 전체. 원격 DB 적용만 사람 몫 — 이 프로젝트에서 원격 push는 human-action 게이트 (25-01/24-01 선례: auto-mode가 프로덕션 배포를 거부).</what-built>
  <how-to-verify>
1. 원격 적용: `git push origin main` (Supabase↔GitHub 연동 자동 적용 관례) 또는 터미널에서 `supabase db push` 1회 → `supabase migration list`로 Remote에 0032 정합 확인.
2. 라이브 스팟 체크 (배포 후):
   a. 시크릿 브라우저에서 기존 `/poll/{code}` 링크 열기 → 투표 UI 아래 "채팅" 섹션 + "참여하면 지금까지의 대화를 볼 수 있어요" 확인 (한마디 없음).
   b. 메시지 입력 → 보내기 → 닉네임 게이트 → 시작하기 → 메시지가 목록에 나타남.
   c. 호스트 브라우저에서 해당 trip의 /moa 채팅탭 열기 → /poll에서 보낸 메시지가 보임 (같은 저장소 수렴 — SC2). 호스트가 답장 → /poll 쪽에 실시간 수신.
  </how-to-verify>
  <resume-signal>"approved" 입력 또는 발견한 문제 서술 (문제 시 gap으로 기록 — verify-work에서 소진)</resume-signal>
  <verify>
    <automated>supabase migration list | grep 0032</automated>
  </verify>
  <done>`supabase migration list`에서 Remote 컬럼에 0032 정합 + 라이브 스팟 체크 3항목(a/b/c) 사용자 확인</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /poll 익명 방문자 → signInAnonymously + join RPC | 무자격 방문자가 authenticated 멤버가 되는 승격 지점 |
| 익명 멤버 → trip_messages INSERT / moa:{tripId} 구독 | 채팅 쓰기·실시간 수신 권한 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-15 | Spoofing | 비멤버 presence 오염 (유령 카운트) | mitigate | `joined && userId`일 때만 채널 effect 실행 (Pitfall 4) — Test 1이 join 전 channel() 미호출 단언 |
| T-29-16 | Tampering | 클라이언트 trip_id로 타 trip 메시지 위조 | mitigate | trip_messages INSERT RLS `can_vote_trip` + user_id 트리거 서버 파생(0028) — 클라이언트 값은 RLS 최종 게이트 통과 필요. UI 경계는 TripMessageCreateSchema Zod |
| T-29-17 | Denial of Service (spam) | 익명 채팅 스팸 | mitigate(기존) | body 1~140 DB CHECK(0025) + Zod 이중화 + 멤버만 INSERT — 신규 완화 없음, 기존 계약 승계 |
| T-29-18 | Information Disclosure | 비멤버 이력 열람 | mitigate | RLS SELECT `can_read_trip`(멤버만) — 비멤버는 hydrate 자체 미실행 + A-7 카피로 정직 전달 |
| T-29-19 | Repudiation | 재방문자 투표 중복 행 (device_token→uid 신원 전환) | accept | Open Q2 채택 비용 — 낮은 확률(재방문+재투표 교집합)·낮은 피해(집계 +1). 코드 주석 기록 |
| T-29-20 | Information Disclosure | nickname PII 비정규화 저장 | accept(기존) | D-A2 기존 계약 — 자발 입력 닉네임만, 계정 정보 무연결 |
</threat_model>

<verification>
- `CI=true pnpm -r test && pnpm -r typecheck && pnpm --filter web build` 전부 exit 0
- HC-7 grep 3종 0건 (apps/web 소스)
- `git diff --stat` 기준 apps/ios 0파일 · moa-chat/moa-island/moa-tab-bar/nickname-gate-sheet 0파일(이 plan)
- 원격 0032 적용 + 라이브 스팟 체크 (Task 3 human-action)
</verification>

<success_criteria>
- /poll 방문자가 게이트 1회로 익명 멤버가 되어 채팅·투표 모두 authed 경로로 참여 (CHAT-05 — SC2)
- /poll·/t·/moa 대화가 trip_messages·moa:{tripId} 단일 메커니즘으로 수렴 — 라이브 스팟 체크로 확인
- moa-chat 무수정 소비 + 기존 채팅 스위트 그린 (CHAT-07 — SC4)
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-unification/29-04-SUMMARY.md`
</output>
