---
phase: 24-host-flow
plan: 05
type: execute
wave: 2
depends_on: ["24-02"]
files_modified:
  - apps/web/app/moa/[id]/_components/place-sheet.tsx
  - apps/web/app/moa/[id]/_components/place-list.tsx
  - apps/web/__tests__/place-list.test.tsx
autonomous: true
requirements: [MOA-02, MOA-03, MOA-05, MOA-06]
must_haves:
  truths:
    - "장소 리스트가 찜 desc·동률 seq_no asc로 정렬되되 순번 배지는 seq_no 그대로다 (MOA-02)"
    - "행 탭 시 아코디언(주소·구글맵·출처 타임스탬프·답장)이 펼쳐진다 (MOA-05)"
    - "행마다 추가자 색 순번 배지 + '{닉네임}님이 담음'이 표시된다 (MOA-06)"
    - "추출 중 링크는 '분석 중…' 행, 실패·0개 추출은 실패 행+재시도로 표시된다 (D-13/D-15, MOA-03)"
  artifacts:
    - path: "apps/web/app/moa/[id]/_components/place-list.tsx"
      provides: "정렬 리스트 + 아코디언 + 분석중/실패 행 (props-driven)"
      exports: ["PlaceList"]
      min_lines: 120
    - path: "apps/web/app/moa/[id]/_components/place-sheet.tsx"
      provides: "non-modal 2단 앵커 드래그 시트 (D-09)"
      exports: ["PlaceSheet"]
  key_links:
    - from: "apps/web/app/moa/[id]/_components/place-list.tsx"
      to: "apps/web/lib/place-sort.ts"
      via: "sortByLove import"
      pattern: "sortByLove"
    - from: "apps/web/app/moa/[id]/_components/place-list.tsx"
      to: "buildGoogleMapsPlaceUrl / buildYouTubeWatchUrl"
      via: "아코디언 액션 링크"
      pattern: "buildGoogleMapsPlaceUrl"
---

<objective>
지도탭의 시트 UI 2종을 만든다 — non-modal 드래그 바텀시트(place-sheet, D-09 물리)와 그 안의 장소 리스트(place-list: 정렬·아코디언·찜 하트·분석중/실패 행). 둘 다 props-driven 프레젠테이션 컴포넌트 — 상태·mutation은 24-06 island 소유.

Purpose: MOA-02/05/06의 시각 표면 + D-13/15 추출 진행 UX. island(24-06)보다 먼저 독립 빌드·테스트해 wave를 벌린다.
Output: place-sheet + place-list + RTL 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-host-flow/24-CONTEXT.md (D-09, D-13, D-15)
@.planning/phases/24-host-flow/24-UI-SPEC.md (§/moa/[id] 리스트 행 anatomy, §Interaction Contract, A-3·A-4·A-5·A-7)
@.planning/phases/24-host-flow/24-PATTERNS.md (§place-list — vote-island 재사용원, §place-sheet)
@apps/web/app/t/[slug]/_components/vote-island.tsx (아코디언·출처 액션·하트 analog)
@apps/web/components/bottom-sheet.tsx (시각 언어만 미러 — 컴포넌트 재사용 불가)
@apps/web/app/boards/[id]/_components/retry-extraction-button.tsx (재시도 로직)

<interfaces>
From packages/core (실존):
```typescript
// Place: id, board_id, link_id|null, added_by(uuid), google_place_id|null, name_local,
//        lat, lng, address|null, source_timestamp_sec|null, hidden_at|null,
//        source_kind: 'ai'|'manual', seq_no: number(int positive), ...
// Link:  id, board_id, added_by, source_kind, url, title|null,
//        extraction_status: 'pending'|'processing'|'ready'|'failed'|'manual_review', ...
```

From 24-02 (이 plan이 소비):
```typescript
sortByLove<T extends {id: string; seq_no: number}>(places: T[], loveCounts: Record<string, number>): T[]  // apps/web/lib/place-sort
```

From apps/web/lib (실존):
```typescript
buildGoogleMapsPlaceUrl(name: string, googlePlaceId: string | null): string   // lib/maps-url
buildYouTubeWatchUrl(url: string, timestampSec: number | null): string        // lib/youtube
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: place-sheet — non-modal 2단 앵커 드래그 시트</name>
  <files>apps/web/app/moa/[id]/_components/place-sheet.tsx</files>
  <read_first>
    - apps/web/components/bottom-sheet.tsx (전체 — L59-72 시각 계약: rounded-t-3xl·bg-white·핸들 h-1.5 w-10·250ms ease-out. 백드롭 L50-57은 복사 금지)
    - .planning/phases/24-host-flow/24-UI-SPEC.md §Interaction Contract (드래그 물리 스펙 전문)
  </read_first>
  <action>
    `place-sheet.tsx` 신규 — `'use client'`. 기존 BottomSheet는 modal(백드롭+닫힘)이라 재사용 불가, 시각 언어만 미러:
    ```typescript
    export type SheetAnchor = 'collapsed' | 'expanded';
    export interface PlaceSheetProps {
      anchor: SheetAnchor;                       // controlled — 마커 탭 시 island가 expanded로
      onAnchorChange: (a: SheetAnchor) => void;
      header: React.ReactNode;                   // 모아 이름 + 장소 N개
      children: React.ReactNode;                 // PlaceList
    }
    ```
    UI-SPEC Interaction Contract 그대로 구현:
    - 앵커 2단 (A-3): collapsed = 핸들+헤더 peek ~30vh, expanded = 85vh. 화면 하단 고정, max-w-lg 컬럼 내(A-10), `rounded-t-3xl bg-white` + 드래그 핸들 `h-1.5 w-10 rounded-full bg-neutral-300`(aria-hidden).
    - 드래그: 핸들/헤더 영역 `onPointerDown` → `setPointerCapture` → move 중 `translateY` 직접 세팅(이때 transition 클래스 off) → `onPointerUp`에 가까운 앵커로 snap(transition on, `duration-[250ms] ease-out`, bounce 없음).
    - 플릭: 마지막 pointermove delta 부호로 방향 바이어스 (아래로 플릭 → collapsed, 위로 → expanded).
    - 스크롤 충돌: 시트가 expanded이고 본문 리스트 `scrollTop > 0`이면 본문에서 드래그 시작 안 함 — 핸들 영역 드래그는 항상 허용.
    - 본문은 `overflow-y-auto` (expanded에서 리스트 스크롤).
    - 애니메이션 예산: snap 250ms만 — 그 외 모션 금지.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "setPointerCapture" apps/web/app/moa/[id]/_components/place-sheet.tsx` ≥ 1
    - `grep "rounded-t-3xl" apps/web/app/moa/[id]/_components/place-sheet.tsx` 1건
    - `grep -E "collapsed|expanded" apps/web/app/moa/[id]/_components/place-sheet.tsx | wc -l` ≥ 2 (2단 앵커)
    - `grep "backdrop\|onClose" apps/web/app/moa/[id]/_components/place-sheet.tsx` == 0건 (non-modal 상시 표시)
    - typecheck exit 0
  </acceptance_criteria>
  <done>드래그 시트 물리 완성 — 드래그·snap·플릭은 jsdom 검증 불가이므로 로컬 브라우저 UAT(verify-work) 항목, 계약은 controlled anchor prop으로 고정</done>
</task>

<task type="auto">
  <name>Task 2: place-list — 정렬·아코디언·하트·분석중/실패 행</name>
  <files>apps/web/app/moa/[id]/_components/place-list.tsx</files>
  <read_first>
    - apps/web/app/t/[slug]/_components/vote-island.tsx (L39-60 tsLabel·sourceAction, L244-378 아코디언 행 구조, L354-360 구글맵 딥링크 — 아코디언·액션 idiom 복사원. join/anon 분기 L153-176은 복사 금지)
    - apps/web/app/boards/[id]/_components/retry-extraction-button.tsx (L19-39 재시도 결과 분기 — dev-tools 문구 제거하고 로직만)
    - apps/web/lib/place-sort.ts (24-02 산출물 — 시그니처)
  </read_first>
  <action>
    `place-list.tsx` 신규 — `'use client'` props-driven (mutation 없음, 콜백만):
    ```typescript
    export interface PlaceListProps {
      places: Place[];                                  // hidden 제외 상태로 전달됨
      links: Link[];
      counts: Record<string, number>;                   // placeId → 찜 수
      myVotes: Record<string, boolean>;
      votePending: Record<string, boolean>;
      profileNames: Record<string, string>;             // userId → display_name
      colorFor: (userId: string) => string;             // island이 memberColor 바인딩해 전달
      openPlaceId: string | null;                       // controlled — 마커 탭 연동 (MOA-05)
      onOpenPlace: (id: string | null) => void;
      onToggleVote: (placeId: string) => void;
      onRetry: (linkId: string) => void;
    }
    ```
    1. **정렬 (MOA-02):** `sortByLove(places, counts)`로 렌더 순서만 결정. 순번 배지는 항상 `place.seq_no` — 정렬 인덱스 넘버링 금지 (Pitfall 9).
    2. **행 anatomy (UI-SPEC):** 상하 16px·좌우 24px·최소 44px. 좌: 20px 원 배지 `style={{ backgroundColor: colorFor(p.added_by) }}` + 흰 12px/600 `{p.seq_no}`. 중: `p.name_local` 16/600 truncate + `{profileNames[p.added_by] ?? '알 수 없음'}님이 담음` 12/400 neutral-500. 우: 하트 토글 버튼 **aria-label="찜"** — off: lucide `Heart` neutral-400 outline / on: brand-500 filled(`fill="currentColor"` + text-brand-500), 옆 count 12/400. `onClick={(e) => { e.stopPropagation(); onToggleVote(p.id); }}`, `disabled={votePending[p.id]}`.
    3. **아코디언 (MOA-05, A-7 순서):** 행 헤더 탭 → `onOpenPlace(open === p.id ? null : p.id)`. 한 번에 하나만(openPlaceId 단일). max-height transition 200ms ease-out, `aria-expanded`, `data-place-id={p.id}` 속성(마커 탭 스크롤 타깃). 내용 순서:
       1. 주소 — `p.address` 14/400 neutral-700 (null이면 생략)
       2. "구글맵에서 보기" text sm + ExternalLink 아이콘 — `<a href={buildGoogleMapsPlaceUrl(p.name_local, p.google_place_id)} target="_blank" rel="noopener noreferrer" onClick={(e) => e.stopPropagation()}>`
       3. 출처 — 해당 `link = links.find(l => l.id === p.link_id)`: youtube면 `출처 {tsLabel(p.source_timestamp_sec)}` → `buildYouTubeWatchUrl(link.url, p.source_timestamp_sec)`, blog면 "출처 보기" → link.url, manual(link_id null)이면 행 생략. tsLabel은 vote-island L39-44 복사 (`m:ss` zero-pad).
       4. "답장" — disabled(opacity 50%), 탭 시 `onReplyStub()` 대신 인라인 처리: 버튼에 `aria-disabled` + 클릭 시 toast('채팅은 곧 열려요') (A-4 — Phase 26이 핸들러만 교체). toast는 `useToast()` 직접 사용 허용(프레젠테이션 예외 1건).
    4. **openPlaceId 외부 변경 시 스크롤 (MOA-05 마커 탭):** `useEffect(() => { if (openPlaceId) document.querySelector(\`[data-place-id="${openPlaceId}"]\`)?.scrollIntoView({ behavior: 'smooth', block: 'nearest' }); }, [openPlaceId])`.
    5. **분석 중 행 (D-13):** `links.filter(l => ['pending','processing'].includes(l.extraction_status))` → 리스트 상단에 spinner(neutral-400 — 브랜드색 아님) + `l.title ?? l.url` 14/400 truncate + "분석 중…" 12/400 neutral-500.
    6. **실패 행 (D-15, Pitfall 8):** `l.extraction_status === 'failed' || l.extraction_status === 'manual_review' || (l.extraction_status === 'ready' && places.every(p => p.link_id !== l.id))` → lucide `AlertCircle` danger(#EF4444 시맨틱 토큰) + "장소를 찾지 못했어요" 14/400 neutral-700 + 우측 outline sm 버튼 "재시도" → `onRetry(l.id)`.
    7. **Empty state:** places 0 && 분석중/실패 행 0 → 중앙 정렬 "아직 담은 장소가 없어요"(18/600) + "아래 + 버튼으로 링크를 붙여넣거나 장소를 검색해 보세요"(14/400 neutral-500).
    8. `dangerouslySetInnerHTML` 사용 금지 — 장소명·링크 title은 React 텍스트 노드로만.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "sortByLove" apps/web/app/moa/[id]/_components/place-list.tsx` 1건
    - `grep "p.seq_no\|place.seq_no\|{p.seq_no}" apps/web/app/moa/[id]/_components/place-list.tsx` ≥ 1 + 배지에 정렬 인덱스(map index) 미사용
    - `grep "aria-label=\"찜\"" apps/web/app/moa/[id]/_components/place-list.tsx` 1건
    - `grep "님이 담음" apps/web/app/moa/[id]/_components/place-list.tsx` 1건, `grep "분석 중…"` 1건, `grep "장소를 찾지 못했어요"` 1건, `grep "재시도"` 1건, `grep "채팅은 곧 열려요"` 1건, `grep "아직 담은 장소가 없어요"` 1건
    - `grep "data-place-id" apps/web/app/moa/[id]/_components/place-list.tsx` ≥ 2 (속성 + querySelector)
    - `grep "dangerouslySetInnerHTML" apps/web/app/moa/[id]/_components/place-list.tsx` == 0건
    - `grep -E "castVote|retractVote|from\(" apps/web/app/moa/[id]/_components/place-list.tsx` == 0건 (mutation은 island 몫)
  </acceptance_criteria>
  <done>MOA-02/05/06 + D-13/15의 전체 리스트 표면 — 콜백 계약으로 island과 분리</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: place-list RTL 테스트</name>
  <files>apps/web/__tests__/place-list.test.tsx</files>
  <read_first>
    - apps/web/__tests__/vote-island.test.tsx (fixture factory makePlace + mock 배선 idiom)
    - apps/web/app/moa/[id]/_components/place-list.tsx (Task 2 산출물 — props 정확 shape)
  </read_first>
  <behavior>
    - Test 1 (MOA-02): counts {a:1, b:3} + seq {a:1, b:2} → 렌더 순서 b, a — 그러나 배지 텍스트는 각각 '2', '1' (순번 표기 불변)
    - Test 2 (MOA-02 동률): 같은 count, seq 5 vs 2 → seq 2 먼저
    - Test 3 (MOA-05): 행 클릭 → onOpenPlace 호출; openPlaceId 지정 렌더 시 아코디언에 주소·"구글맵에서 보기"·출처·"답장" 4요소 존재
    - Test 4 (MOA-06): "{닉네임}님이 담음" 렌더 + 배지 backgroundColor가 colorFor 반환값
    - Test 5 (하트): 하트 클릭 → onToggleVote(placeId) 호출 + 행 클릭 핸들러 미발화(stopPropagation)
    - Test 6 (D-13): extraction_status 'processing' 링크 → "분석 중…" 행
    - Test 7 (D-15): 'failed' 링크 → "장소를 찾지 못했어요" + "재시도" 클릭 → onRetry(linkId); 'ready' + 장소 0개 링크도 실패 행
    - Test 8 (empty): places·links 0 → "아직 담은 장소가 없어요"
  </behavior>
  <action>
    `apps/web/__tests__/place-list.test.tsx` 신규 — 순수 props 컴포넌트라 mock은 `@/components`(useToast)만 필요. makePlace/makeLink fixture factory(vote-island idiom, Partial overrides). props 기본값 헬퍼 `renderList(overrides)` 작성. behavior 8케이스.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- place-list</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test:run -- place-list` 그린 (8케이스)
    - Test 1이 배지 텍스트와 렌더 순서를 **둘 다** 단언 (getAllByTestId 또는 텍스트 순서 대조)
    - `pnpm --filter web test:run` 전체 무회귀
  </acceptance_criteria>
  <done>정렬 불변·아코디언 4요소·추출 상태 행이 자동 검증으로 고정</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 추출된 링크/장소 콘텐츠 → 렌더 | 유튜브·블로그에서 온 title·name_local·address가 DOM으로 (stored XSS 표면) |
| 외부 링크 액션 | 구글맵·유튜브·블로그 URL로 이탈 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-14 | Tampering (XSS) | place-list 렌더 (추출 콘텐츠) | mitigate | React 기본 이스케이프만 — dangerouslySetInnerHTML 0건을 acceptance grep으로 단언 (Task 2) |
| T-24-15 | Spoofing | 외부 링크 (출처·구글맵) | mitigate | `target="_blank" rel="noopener noreferrer"` 필수 + URL은 buildGoogleMapsPlaceUrl/buildYouTubeWatchUrl 헬퍼로만 조립 (Task 2) |
| T-24-16 | Tampering | 핀 색 스타일 주입 | mitigate | backgroundColor는 colorFor(=memberColor) 반환 토큰 리터럴만 — user 문자열 스타일 경로 0 (24-02 T-24-04 계승) |
</threat_model>

<verification>
- `pnpm --filter web test:run -- place-list` 그린 (8케이스)
- `pnpm --filter web typecheck` exit 0
- 카피 grep 스윕 (분석 중…·장소를 찾지 못했어요·님이 담음·채팅은 곧 열려요)
</verification>

<success_criteria>
- 성공 기준 4의 리스트 절반(정렬·순번 불변·아코디언·닉네임·색 배지)이 컴포넌트 레벨로 자동 검증됨
- 24-06 island이 props만 배선하면 되는 상태
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-05-SUMMARY.md`
</output>
