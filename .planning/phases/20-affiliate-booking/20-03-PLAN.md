---
phase: 20-affiliate-booking
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - packages/core/src/booking.ts
  - packages/core/src/booking.test.ts
  - packages/core/src/booking-map.ts
  - packages/core/src/booking-map.test.ts
  - packages/core/src/checklist.ts
  - packages/core/src/checklist.test.ts
  - packages/core/src/category.ts
  - packages/core/src/category.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [BOOK-01, BOOK-02, BOOK-03]
must_haves:
  truths:
    - "buildAffiliateUrl('travelpayouts', …)이 Klook은 click형(c137 호스트, shmarker=marker.token, promo_id 4110), Airalo는 media형(p 8310, campaign_id 541)의 라이브 실측 규격 URL을 반환한다"
    - "한글 장소명이 목적지 검색 URL에 정확히 1회 인코딩되어 통과한다 (이중/미인코딩 파손 없음 — Pitfall 7)"
    - "기존 booking.test.ts 15 케이스가 무파손 green (sub_id substring + 토큰 substring 계약 유지)"
    - "deriveChecklistAutos가 D-10 파생과 D-13 보존(체크됨/manual 불가침)을 순수함수로 보장한다"
    - "isBookableActivity가 관광명소·테마파크 계열만 true, 맛집·카페는 false (D-08)"
  artifacts:
    - "packages/core/src/booking.ts — 실규격 travelpayouts 분기 + buildSearchDestUrl + buildAiraloDestUrl + buildDirectSearchUrl"
    - "packages/core/src/booking-map.ts — BOOKING_REGION_MAP + COMPARE_LABELS"
    - "packages/core/src/checklist.ts — ChecklistItemSchema + deriveChecklistAutos"
    - "packages/core/src/category.ts — isBookableActivity (surgical append)"
  key_links:
    - "17-02 락: buildAffiliateUrl 시그니처·ClickTokenSchema.parse 첫 줄·stay22 분기 무수정"
    - "ChecklistItemSchema enum ↔ 0021 CHECK 값 완전 일치 (kind/status/source)"
    - "BOOKING_REGION_MAP 키 ↔ constants.ts CITY_KO_MAP 9개 city_code 정합 (D-09)"
---

<objective>
@moajoa/core에 Phase 20 도메인 계약을 락: (1) buildAffiliateUrl travelpayouts 분기를 marker 745749 라이브 실측 규격으로 채움 + 목적지 URL 빌더 + 숙소 비제휴 buildDirectSearchUrl (BOOK-01/03, D-05/D-07), (2) D-09 정적 매핑 + D-06 비교 라벨 상수, (3) 체크리스트 스키마 + D-10/D-13 순수 파생 규칙 + D-08 예약성 판정 (BOOK-02).

Purpose: 이후 api(20-04)·iOS(20-05~07)가 import할 단일 정의 seam. URL 조립은 booking.ts가 유일한 지점(17-02 Pitfall 1) — 손조립이 구조적으로 불가능해야 한다.
Output: core 신규 모듈 2 + 확장 2 + barrel, 전 core 테스트 green (기존 77 무회귀).
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-affiliate-booking/20-CONTEXT.md
@.planning/phases/20-affiliate-booking/20-RESEARCH.md
@.planning/phases/20-affiliate-booking/20-PATTERNS.md
@packages/core/src/booking.ts
@packages/core/src/category.ts
</context>

## Artifacts this phase produces (this plan's share)

- `packages/core/src/booking.ts` 확장 exports: `TP_PROGRAMS` (klook/airalo 프로그램 상수), `TpProgramKey` type, `buildSearchDestUrl(platform, query)`, `buildAiraloDestUrl(esimSlug)`, `buildDirectSearchUrl(provider, params)`, `DirectSearchProvider` (['agoda','booking'] as const) — 기존 `buildAffiliateUrl`/`ClickTokenSchema`/`BookingClickContextSchema`/`AffiliateProvider` 유지
- `packages/core/src/booking-map.ts` 신설: `BOOKING_REGION_MAP`, `COMPARE_LABELS`
- `packages/core/src/checklist.ts` 신설: `ChecklistKind`/`ChecklistStatus`/`ChecklistSource` const-enum, `ChecklistItemSchema`, `ChecklistItem` type, `ManualItemTitleSchema` (1..80), `deriveChecklistAutos`
- `packages/core/src/category.ts` append: `isBookableActivity(category)`
- `packages/core/src/index.ts` barrel append 2줄

<!-- planner-discipline-allow: travelpayouts.com -->
<!-- planner-discipline-allow: tp.media -->
<!-- planner-discipline-allow: tp.st -->
<!-- planner-discipline-allow: klook.com -->
<!-- planner-discipline-allow: kkday.com -->
<!-- planner-discipline-allow: airalo.com -->
<!-- planner-discipline-allow: agoda.com -->
<!-- planner-discipline-allow: booking.com -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: booking.ts 실규격 + 목적지/비제휴 빌더 (계약 테스트 무파손)</name>
  <files>packages/core/src/booking.ts, packages/core/src/booking.test.ts</files>
  <read_first>
    - packages/core/src/booking.ts (17-02 락 형태 전문 — 37줄)
    - packages/core/src/booking.test.ts (기존 15 케이스 — 특히 travelpayouts sub_id substring 단언 L73-77, invalid-token L91-95)
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §"검증된 딥링크 규격" + §"검색결과 딥링크 목적지 URL" + Pitfall 1/7
  </read_first>
  <behavior>
    - klook: buildAffiliateUrl('travelpayouts', { program:'klook', marker:'745749', dest: buildSearchDestUrl('klook','teamLab') }, 'c_aB3xY9zQ') → URL이 `https://c137.travelpayouts.com/click`으로 시작, `shmarker=745749.c_aB3xY9zQ`(dot 구분), `promo_id=4110`, `source_type=customlink`, `type=click`, `custom_url=`에 인코딩된 목적지, `sub_id=c_aB3xY9zQ` 포함
    - airalo: { program:'airalo', marker, trs:'123456', dest: buildAiraloDestUrl('japan-esim') } → `https://tp.media/r` 시작, `marker=745749.{token}`, `p=8310`, `campaign_id=541`, `trs=123456`, `u=` 인코딩 목적지, `sub_id` 포함
    - kkday(env 획득 후): { program:'kkday', marker, p:'…', campaign_id:'…', dest } → media형 조립
    - kkday(폴백): { program:'kkday', marker, fallback_base:'https://kkday.tp.st/gVbA69Yv' } → `{fallback_base}?sub_id={token}` (tp.st 동적 sub_id — 라이브 검증됨)
    - kkday(둘 다 없음): throw (호출부가 사전 게이트 — canBuildKkday 없이 params 존재로 판정)
    - 한글 장소명: buildSearchDestUrl('klook','팀랩 플래닛') 결과를 custom_url에 넣으면 **정확히 1회** 인코딩 — 목적지 URL 먼저 완성(내부 query 인코딩) 후 전체를 encodeURIComponent 1회 (Pitfall 7). decodeURIComponent 1회로 원 목적지 복원 단언
    - buildDirectSearchUrl('agoda', { city:'도쿄', checkIn:'2026-08-01', checkOut:'2026-08-05' }) → agoda.com/ko-kr/search + textToSearch/checkIn/checkOut/rooms=1/adults=2; 'booking' → booking.com/searchresults.ko.html + ss/checkin/checkout/group_adults=2/no_rooms=1
    - 기존 15 케이스 전부 무파손 (sub_id + 토큰 substring 계약 — marker-dot과 sub_id 동시 주입이 both 충족, RESEARCH 호환 전략)
    - invalid token('bad.token')은 여전히 throw (ClickTokenSchema.parse 첫 줄 유지)
  </behavior>
  <action>
    booking.ts 수정 — **buildAffiliateUrl 시그니처(provider, productParams: Record<string,string>, subId)·ClickTokenSchema.parse 첫 줄·stay22 분기는 무수정** (17-02 락, D-05 provider-스왑 전제). travelpayouts 분기만 교체:
    - `TP_PROGRAMS` 상수: klook = click형(호스트 'c137.travelpayouts.com', promoId '4110'), airalo = media형(p '8310', campaignId '541'). 프로그램 구조 상수는 계정 비밀이 아닌 공개 프로그램 ID라 core 상수 (RESEARCH Anti-pattern 5); **marker·trs는 상수화 금지** — productParams로 주입(env는 iOS 소유, CLAUDE.md §4.7).
    - travelpayouts 분기: productParams에서 program/marker/dest/trs/p/campaign_id/fallback_base를 읽어 click형은 `https://{host}/click?shmarker={marker}.{token}&promo_id={promoId}&source_type=customlink&type=click&custom_url={encodeURIComponent(dest)}&sub_id={token}`, media형은 `https://tp.media/r?marker={marker}.{token}&trs={trs}&p={p}&campaign_id={campaign_id}&u={encodeURIComponent(dest)}&sub_id={token}` 조립. kkday는 p+campaign_id 있으면 media형, 없고 fallback_base 있으면 `{fallback_base}?sub_id={token}`, 둘 다 없으면 throw. 조립은 URLSearchParams/encodeURIComponent만 — 수동 문자열 치환 금지 (T-20-01).
    - 목적지 빌더 신설 (제휴/플랫폼 URL 리터럴은 이 파일이 유일 소유 — Pitfall 1 grep 가드 유지): `buildSearchDestUrl('klook'|'kkday', query)` → klook `https://www.klook.com/ko/search/result/?query={q}` / kkday `https://www.kkday.com/ko/product/productlist?keyword={q}` (A1/A2 [ASSUMED] — 디바이스 UAT 판명 시 이 1곳만 수정); `buildAiraloDestUrl(esimSlug)` → `https://www.airalo.com/{slug}`.
    - `buildDirectSearchUrl(provider: 'agoda'|'booking', { city, checkIn, checkOut })` 신설 — D-05 숙소 비제휴 프리필: agoda `https://www.agoda.com/ko-kr/search?textToSearch={city}&checkIn={in}&checkOut={out}&rooms=1&adults=2` / booking `https://www.booking.com/searchresults.ko.html?ss={city}&checkin={in}&checkout={out}&group_adults=2&no_rooms=1`. buildAffiliateUrl 밖 손조립 방지를 위해 같은 파일 배치 (CONTEXT ⚠️ 주의).
    booking.test.ts: 기존 describe/it + UUID fixture idiom으로 behavior 케이스 추가. 기존 15 케이스는 한 줄도 수정하지 않음.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/core test -- booking</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/core test -- booking` exit 0, 케이스 수 >= 27 (기존 15 + 신규 12+)
    - `grep -c "buildDirectSearchUrl" packages/core/src/booking.ts` >= 1, `grep -c "buildSearchDestUrl" packages/core/src/booking.ts` >= 1
    - `grep -c "PLACEHOLDER" packages/core/src/booking.ts` == 1 (stay22 분기 잔존분만 — travelpayouts 분기의 자리표시자는 소멸)
    - `grep -c "745749" packages/core/src/booking.ts` == 0 (marker 하드코딩 금지 — 테스트 fixture에만 허용)
    - `git diff packages/core/src/booking.ts | grep -c "^-.*stay22"` == 0 (stay22 분기 무수정)
  </acceptance_criteria>
  <done>실규격 URL 조립 + 목적지/비제휴 빌더 완성, 기존 계약 테스트 무파손, marker/trs는 파라미터 주입만</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: booking-map.ts (D-09 정적 매핑 + D-06 라벨) + isBookableActivity (D-08)</name>
  <files>packages/core/src/booking-map.ts, packages/core/src/booking-map.test.ts, packages/core/src/category.ts, packages/core/src/category.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - packages/core/src/constants.ts (CITY_KO_MAP L140-158 — 9개 city_code 키 + why-주석 + as const idiom)
    - packages/core/src/category.ts (placeVibe/RULES/VIBE_META — 무수정 대상)
    - packages/core/src/category.test.ts (기존 22 케이스 스타일)
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Copywriting Contract (라벨 정본)
  </read_first>
  <behavior>
    - BOOKING_REGION_MAP: 정확히 CITY_KO_MAP의 9개 키. jp 6개(tokyo/osaka/kyoto/fukuoka/sapporo/okinawa)는 esimSlug 'japan-esim'; kr 3개(seoul/busan/jeju)는 esimSlug null + transport null (국내여행 유심 무의미 — D-09 억지 추천 금지)
    - transport: tokyo = { labelKo:'JR 패스', provider:'klook', searchQuery:'JR 패스' }; osaka/kyoto = { labelKo:'간사이 패스', provider:'klook', searchQuery:'간사이 패스' }; fukuoka/sapporo/okinawa = null (대표 패스 확신 없음 — 억지 추천 금지)
    - isBookableActivity: 'tourist_attraction'/'museum'/'temple' 등 culture 계열 → true; 'amusement_park'/'theme_park'/'aquarium'/'zoo'/'water_park' → true; 'ramen_restaurant'/'cafe'/'coffee_shop' → false; 'clothing_store' → false; null/undefined → false
  </behavior>
  <action>
    booking-map.ts 신설 (CITY_KO_MAP idiom — why-주석 + `Readonly<Record<...>>` + as const):
    - `BOOKING_REGION_MAP: Readonly<Record<string, { country:'jp'|'kr'; esimSlug: string|null; transport: { labelKo:string; provider:'klook'|'kkday'; searchQuery:string }|null }>>` — behavior의 9키 값 그대로. esimSlug는 경로 조각만(도메인 리터럴은 booking.ts 소유 — buildAiraloDestUrl이 조립). 매핑 부재 city → 호출부가 카드 숨김 (D-09 계약, CITY_KO_MAP "missing → omit" 미러).
    - `COMPARE_LABELS` 상수 — UI가 provider copy를 하드코딩하지 않게 core 소유 (UI-SPEC Cross-Screen note): klook '즉시확정·전세계 상품' / kkday '한국어 가이드 상품 강세' / agoda '아시아 숙소 강세' / booking '전 세계 숙소 폭넓게' / airalo 'eSIM 즉시 발급' (D-06 사용자 합의 결 — 한국인 일본여행 타깃).
    category.ts: 파일 하단에 `isBookableActivity(category: string | null | undefined): boolean` **append만** — 기존 RULES/placeVibe/VIBE_META 무수정 (Karpathy 3). 판정: placeVibe(category) === 'culture' 이면 true, 또는 소문자 category가 BOOKABLE_EXTRA needles(['amusement', 'theme_park', 'aquarium', 'zoo', 'water_park']) 중 하나를 포함하면 true (aquarium/zoo는 placeVibe상 nature로 빠지지만 예약성 — D-08 재량 판정). food/cafe/shopping은 구조적으로 false.
    index.ts barrel: `export * from './booking-map';` 1줄 append (checklist는 Task 3에서). 기존 줄 무수정.
    테스트: booking-map.test.ts 신설(9키 정합 + kr null 계약), category.test.ts에 isBookableActivity describe 추가 (기존 22 케이스 무수정).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/core test -- category && pnpm --filter @moajoa/core test -- booking-map</automated>
  </verify>
  <acceptance_criteria>
    - 두 filtered 테스트 exit 0; category 기존 케이스 무회귀
    - `grep -c "isBookableActivity" packages/core/src/category.ts` >= 1
    - `git diff packages/core/src/category.ts | grep -c "^-"` <= 1 (삭제 없는 append — diff 헤더 제외 삭제줄 0)
    - BOOKING_REGION_MAP 키 수 == 9 (테스트가 Object.keys 길이 + CITY_KO_MAP 키 일치 단언)
    - `grep -cE "https?://" packages/core/src/booking-map.ts` == 0 (URL 리터럴은 booking.ts만)
  </acceptance_criteria>
  <done>정적 매핑·라벨이 core에 락, 예약성 판정이 D-08 경계로 테스트됨</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: checklist.ts — 스키마 + deriveChecklistAutos 순수 파생 (D-10/D-13)</name>
  <files>packages/core/src/checklist.ts, packages/core/src/checklist.test.ts, packages/core/src/index.ts</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §Pattern 3 (reconcile 규칙 정본)
    - supabase/migrations/0021_booking.sql (20-02 Task 1 산출물 — CHECK 값과 enum 짝; wave 병렬이라 파일 부재 시 RESEARCH 골자 값 사용: kind/status/source 값은 잠금)
    - packages/core/src/booking.ts (Zod 모듈 shape idiom)
  </read_first>
  <behavior>
    - ChecklistItemSchema: id/trip_id uuid, place_id uuid nullable, kind enum ['stay','esim','transport','activity','custom'], title 1..80, status enum ['todo','clicked','done'] default 'todo', source enum ['auto','manual'] default 'auto'
    - deriveChecklistAutos({ covered:{ esimSlug, transportLabel }, bookablePlaces:[{placeId,title}], existing }) → { toInsert, toDeleteIds }:
      - 빈 existing + esim/transport 커버 + 예약성 장소 2개 → toInsert = stay('숙소 예약') + esim('여행 유심') + transport(transportLabel) + activity 2개(장소 title, place_id 세팅)
      - esimSlug null (kr 도시) → esim 항목 미파생
      - 기존 auto 항목과 중복 → toInsert에 미포함 (싱글턴은 kind, activity는 place_id로 매칭)
      - 플랜에서 빠진 auto+todo activity → toDeleteIds 포함
      - **status 'clicked'/'done' 항목은 플랜에서 빠져도 toDeleteIds에 절대 미포함** (돈 쓴 기록 — D-13)
      - **source 'manual' 항목은 어떤 경우에도 toInsert/toDeleteIds 무접촉** (D-10 수동 항목 보존)
      - '플랜에 없음' 배지는 파생 결과가 아님 — 렌더 시 place_id ∉ 현 예약성 집합으로 클라 계산 (D-13, 순수함수는 desync 판정 헬퍼 isDesynced(item, currentPlaceIds) 제공)
  </behavior>
  <action>
    checklist.ts 신설 — booking.ts Zod 모듈 shape 미러:
    - const-enum 3종: `ChecklistKind`/`ChecklistStatus`/`ChecklistSource` (`as const` + indexed-access type — TripVisibility idiom). **값은 0021 CHECK와 문자 단위 일치** (kind 5종, status 3종, source 2종).
    - `ChecklistItemSchema` + `ChecklistItem` type + `ManualItemTitleSchema`(z.string().min(1).max(80) — 0021 title CHECK 1..80 짝, D-10 수동 추가 입력 검증).
    - `deriveChecklistAutos(args)` 순수함수 — behavior 규칙 그대로. 시그니처: `{ covered: { esimSlug: string|null; transportLabel: string|null }, bookablePlaces: Array<{ placeId: string; title: string }>, existing: ChecklistItem[] }` → `{ toInsert: Array<{ kind: string; place_id: string|null; title: string }>; toDeleteIds: string[] }`. stay는 무조건 wanted(날짜 확정 trip 전제 — D-04 게이트는 호출부), esim/transport는 covered 값 존재 시. 구현 핵심: wanted 집합 계산 → existing auto와 diff. status!=='todo' 또는 source==='manual'은 읽기만 하고 절대 산출물에 넣지 않음 (D-13 불가침 규칙을 코드 주석으로 명시).
    - `isDesynced(item, currentBookablePlaceIds: Set<string>): boolean` — activity kind && place_id 존재 && 집합 밖.
    index.ts barrel: `export * from './checklist';` append.
    checklist.test.ts: behavior 케이스 전부 + 경계(existing에 clicked stay 있을 때 재파생 시 무접촉, manual 항목과 kind 충돌 시 auto 싱글턴은 여전히 파생 — partial unique가 auto만 걸리므로).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/core test -- checklist</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/core test -- checklist` exit 0, 케이스 >= 10
    - `grep -c "deriveChecklistAutos" packages/core/src/checklist.ts` >= 1
    - `pnpm --filter @moajoa/core test` 전체 exit 0 (기존 77 + 신규 — 무회귀)
    - `pnpm --filter @moajoa/core typecheck` exit 0
    - `grep -c "export \* from './checklist'" packages/core/src/index.ts` == 1
  </acceptance_criteria>
  <done>파생/보존 규칙이 순수함수로 테스트 고정 — api reconcile(20-04)이 이 계약 위에 DB 반영만 담당</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 장소명(외부 소스 유래 텍스트) → 제휴 URL 쿼리 | places.name_ko/name_local이 custom_url/u 중첩 쿼리에 삽입됨 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | buildAffiliateUrl / buildSearchDestUrl (URL 인젝션) | high | mitigate | URLSearchParams·encodeURIComponent만 사용, 문자열 연결 조립 금지. 단일 조립 지점(booking.ts) grep 게이트 + 한글/특수문자 1회-인코딩 계약 테스트 (Pitfall 7) |
| T-20-06 | Tampering | 조립 지점 우회 (booking.ts 밖 손조립) | medium | mitigate | 제휴·플랫폼 도메인 리터럴을 booking.ts에 격리 — phase verification의 repo-wide 도메인 grep 게이트 (17-02 Pitfall 1 가드를 agoda/booking 도메인까지 확장) |
| T-20-10 | Info Disclosure | marker/trs 하드코딩 | low | mitigate | core는 파라미터 주입만 — 745749 리터럴 grep == 0 게이트. 프로그램 구조 상수(4110/8310/541)는 공개 ID라 core 허용 (RESEARCH) |
</threat_model>

<verification>
- `pnpm --filter @moajoa/core test` 전체 green + typecheck 0
- 도메인 격리 게이트: `grep -rlE "travelpayouts\.com|tp\.media|klook\.com|kkday\.com|airalo\.com|agoda\.com" packages/core/src --include="*.ts" | grep -vE "booking\.(ts|test\.ts)$"` 빈 출력
</verification>

<success_criteria>
- 딥링크 실규격·비제휴 프리필·정적 매핑·체크리스트 파생이 전부 core에서 테스트로 고정
- 17-02 계약(시그니처·토큰 가드·기존 15 케이스) 무파손
- BOOK-01/02/03의 순수 로직 측면 충족
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-03-SUMMARY.md` when done
</output>
