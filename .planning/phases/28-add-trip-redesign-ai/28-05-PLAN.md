---
phase: 28-add-trip-redesign-ai
plan: 05
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx
  - apps/web/app/moa/[id]/_components/day-select-sheet.tsx
  - apps/web/app/moa/[id]/_components/plan-section.tsx
  - apps/web/app/moa/[id]/_components/place-list.tsx
  - apps/web/__tests__/duration-gate-sheet.test.tsx
  - apps/web/__tests__/day-select-sheet.test.tsx
  - apps/web/__tests__/plan-section.test.tsx
  - apps/web/__tests__/place-list.test.tsx
autonomous: true
requirements: [SC-4, SC-5]

must_haves:
  truths:
    - "일정이 없으면 [일정] 영역이 안내 카피와 '일정 만들기' 버튼을 보여준다"
    - "추출이 도는 중에는 '일정 만들기'가 비활성이고 몇 개 분석 중인지 보인다"
    - "기간이 미정이면 '일정 만들기'가 기간부터 물어본다 (owner에게만)"
    - "일정이 있으면 Day 1~N 탭 + 번호 타임라인 + 미배치 풀 + 하단 액션 3종이 보인다"
    - "미배치 장소를 버튼으로 특정 Day에 넣거나 '아직 모르겠다'로 풀에 남길 수 있다"
    - "'일정 다시 만들기' 아래에 수동 배치 보존 규칙(D-25)이 항상 보인다"
  artifacts:
    - apps/web/app/moa/[id]/_components/plan-section.tsx
    - apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx
    - apps/web/app/moa/[id]/_components/day-select-sheet.tsx
    - "apps/web/app/moa/[id]/_components/place-list.tsx — onAddToPlan additive prop"
  key_links:
    - "D-25 카피 ↔ D-21 계약(28-03) — 계약이 동작해야 카피가 진실이다"
    - "DurationPills(28-02) ↔ DurationGateSheet — 한 벌 공유"
    - "PlanSection ↔ moa-island(28-06) — 상태·mutation은 island 소유, 이 컴포넌트는 props-driven"
    - "place-sheet 제스처 소유권 ↔ Day 탭 스트립 — 회귀 최상위 위험(HC-5)"
---

<objective>
`/moa/[id]` place-sheet 안에 **[일정] 영역**을 만든다. 신규 라우트·신규 탭 없이 기존 시트의 children으로 들어간다(D-15/HC-4). iOS `plan.tsx`의 상태기계 A~F를 웹으로 이식하되 **iOS 파일은 읽기 전용 참고일 뿐 복사·import 대상이 아니다**.

이 플랜의 산출물은 전부 **props-driven 프레젠테이션**이다 — 상태와 mutation은 28-06의 moa-island가 소유한다(Shared Pattern 5). 시트 열림/닫힘 같은 순수 UI 상태만 로컬로 갖는다.

⚠ **D-25 카피 배포 게이트:** "직접 옮긴 장소는 그대로 두고 나머지만 다시 짜요"는 D-21 계약(28-03)이 **실제로 동작할 때만 진실**이다. 이 플랜이 28-03에 `depends_on`으로 걸린 이유다.

Purpose: SC-4(Day 탭 + 번호 타임라인 결과 화면) · SC-5(Day 배치 질문 + '모르겠다' + 가이드 카피).
Output: PlanSection · DurationGateSheet · DaySelectSheet · PlaceList의 풀 편입 진입점.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-add-trip-redesign-ai/28-CONTEXT.md
@.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md
@.planning/phases/28-add-trip-redesign-ai/28-PATTERNS.md
@.planning/phases/28-add-trip-redesign-ai/28-03-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
| 심볼 | 위치 | 소비자 |
|---|---|---|
| `PlanSection` (+ `PlanSectionProps`) | `apps/web/app/moa/[id]/_components/plan-section.tsx` | moa-island(28-06) |
| `DurationGateSheet` | `apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx` | PlanSection |
| `DaySelectSheet` | `apps/web/app/moa/[id]/_components/day-select-sheet.tsx` | PlanSection |
| `PlaceList` `onAddToPlan?` prop | `apps/web/app/moa/[id]/_components/place-list.tsx` (additive) | PlanSection(미배치 풀) |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DurationGateSheet + DaySelectSheet (D-13 · D-20 · A-7 · A-9)</name>
  <files>apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx, apps/web/app/moa/[id]/_components/day-select-sheet.tsx, apps/web/__tests__/duration-gate-sheet.test.tsx, apps/web/__tests__/day-select-sheet.test.tsx</files>

  <read_first>
    - `apps/web/app/moa/[id]/_components/add-sheet.tsx` 전문 — BottomSheet + 콘텐츠 + mutation 콜백 + 에러 토스트 + onClose의 정확한 analog
    - `apps/web/components/bottom-sheet.tsx` — `footer` 슬롯(고정 CTA가 스크롤에 안 잘림). 사용 선례는 `share-sheet.tsx`
    - 28-02의 `DurationPills` · `SelectPill`
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Screen Contracts 5 · §Copywriting Contract · Auto-Decisions A-7/A-9
  </read_first>

  <behavior>
    - DurationGateSheet: 기간 pill 6종이 렌더되고, 미선택 시 CTA가 disabled다
    - DurationGateSheet: '2박 3일' 선택 후 CTA 탭 시 onConfirm(3)이 정확히 1회 발화한다
    - DaySelectSheet: dayCount=4면 Day 1~4 pill이 렌더된다
    - DaySelectSheet: Day 2 선택 시 onSelectDay(1)이 발화한다 (표시는 1-based, 값은 0-based day_index)
    - DaySelectSheet: '아직 모르겠다' 탭 시 onSkip()이 발화한다 (풀 잔류, D-20)
  </behavior>

  <action>
    두 파일 모두 `add-sheet.tsx`의 셸 구조를 미러한다: `BottomSheet`(modal, 백드롭 있음) + 콘텐츠 + `footer` 슬롯 CTA + `onClose`. **mutation은 직접 하지 않는다** — 콜백만 호출하고 실제 api 호출은 moa-island(28-06)가 한다.

    **(a) `duration-gate-sheet.tsx` (D-13):**
    - 타이틀 `여행 기간을 알려주세요`, 서브카피 `며칠 일정으로 짤지 정해야 AI가 동선을 나눌 수 있어요`.
    - 본문은 **`DurationPills`(28-02) 재사용** — 시트용 pill을 새로 만들지 않는다(한 벌 공유가 이 컴포넌트의 존재 이유다).
    - footer CTA `이 기간으로 일정 만들기` — primary 풀폭, 미선택 시 disabled(비활성 시각은 위저드 CTA와 동일한 흰 글씨 오버라이드).
    - props: `open` · `onClose` · `onConfirm(dayCount: number)`. 선택된 dayCount는 시트 로컬 상태(순수 UI 상태).
    - ⚠ **A-9: 이 시트는 owner에게만 열린다.** 비-owner에게는 PlanSection이 버튼 자체를 disabled로 두고 시트를 열지 않는다(Task 3). 이유: `trips` UPDATE RLS가 owner 전용(0016)이라 editor의 day_count 저장은 조용히 실패한다. **신규 RLS로 editor에 여는 것은 이 phase 범위 밖**이다.

    **(b) `day-select-sheet.tsx` (A-7 · D-20):**
    - 타이틀 `며칠차에 넣을까요?`.
    - 본문: Day 1..N pill (N = `dayCount` prop). `SelectPill` 재사용, 2열 그리드. **표시는 1-based(`Day 1`), 콜백 값은 0-based `day_index`** — 이 변환을 이 컴포넌트가 책임진다(혼동 지점이므로 doc 주석에 명시).
    - 하단 `아직 모르겠다` text 버튼 → `onSkip()`. D-20: 풀에 남기고, 다음 재생성 때 AI가 배치하거나 사용자가 수동 이동. **즉시 재생성·자동 append 안 함.**
    - props: `open` · `onClose` · `dayCount: number` · `onSelectDay(dayIndex: number)` · `onSkip()`.
    - **드래그 앤 드롭 없음** — 버튼 + 시트가 이 phase의 계약이다(deferred).

    카피는 UI-SPEC Copywriting Contract verbatim. 색은 토큰 클래스만.
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/duration-gate-sheet.test.tsx __tests__/day-select-sheet.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/duration-gate-sheet.test.tsx __tests__/day-select-sheet.test.tsx` → 5케이스 그린
    - `grep -c 'DurationPills' apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx` → 1 이상 (한 벌 재사용)
    - `grep -c '이 기간으로 일정 만들기' apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx` → 1
    - `grep -c '아직 모르겠다' apps/web/app/moa/[id]/_components/day-select-sheet.tsx` → 1 (D-20)
    - `grep -c '며칠차에 넣을까요' apps/web/app/moa/[id]/_components/day-select-sheet.tsx` → 1
    - `grep -Ec '@moajoa/api|getSupabaseBrowser' apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx apps/web/app/moa/[id]/_components/day-select-sheet.tsx` → **0** (mutation 미소유 — 콜백만)
    - `grep -rEc '#[0-9a-fA-F]{3,8}\b' apps/web/app/moa/[id]/_components/duration-gate-sheet.tsx apps/web/app/moa/[id]/_components/day-select-sheet.tsx` → 0
  </acceptance_criteria>

  <done>두 시트가 BottomSheet 셸로 렌더되고 DurationPills를 재사용하며, api를 직접 호출하지 않고 콜백만 발화한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: place-list.tsx — onAddToPlan additive prop (미배치 풀 편입 진입점)</name>
  <files>apps/web/app/moa/[id]/_components/place-list.tsx, apps/web/__tests__/place-list.test.tsx</files>

  <read_first>
    - `apps/web/app/moa/[id]/_components/place-list.tsx` — 아코디언 액션 행 구조 · 추출 대기/실패 판별식(L96-111) · `onReply`·`onToggleVote` 등 기존 콜백 prop 패턴
    - `apps/web/__tests__/place-list.test.tsx` — 기존 18케이스
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Component Inventory > Modify > place-list
  </read_first>

  <behavior>
    - onAddToPlan 미전달 시 렌더가 기존과 동일하다 ('일정에 넣기' 버튼 부재 — 기존 18케이스 무회귀)
    - onAddToPlan 전달 시 행 아코디언 액션 행에 '일정에 넣기' 버튼이 렌더된다
    - '일정에 넣기' 탭 시 onAddToPlan(placeId)이 발화하고 행 토글은 발화하지 않는다 (이벤트 전파 차단)
  </behavior>

  <action>
    `apps/web/app/moa/[id]/_components/place-list.tsx`에 **additive optional prop** `onAddToPlan?: (placeId: string) => void` 추가.

    - 전달된 경우에만 각 행의 아코디언 액션 행에 `일정에 넣기` text 버튼을 렌더한다. **미전달 시 기존 렌더와 완전히 동일**해야 한다(기존 18케이스가 회귀 앵커).
    - 버튼 클릭 핸들러는 이벤트 전파를 막아 행 아코디언 토글이 같이 발화하지 않게 한다(기존 찜 하트·답장 버튼이 쓰는 것과 동일한 방식).
    - 기존 props(`onReply`·`currentUserId`·`ownerId`·`onToggleVote`·`onRetry` 등)와 기존 판별식(추출 대기·실패)은 **무수정**. 정렬·순번 배지(`seq_no`)·own-only 삭제 게이트(D-12, 25-02) 전부 무접촉.

    ⚠ **`seq_no`(담은 순 배지)와 타임라인 번호(`sort_order+1`)는 다른 체계다.** 이 파일은 `seq_no` 체계를 유지한다 — 번호 혼용 금지.

    `apps/web/__tests__/place-list.test.tsx`에 위 3케이스를 append. 기존 18케이스는 무수정.
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/place-list.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/place-list.test.tsx` → 기존 18 + 신규 3 = 21케이스 그린
    - `grep -c 'onAddToPlan' apps/web/app/moa/[id]/_components/place-list.tsx` → 2 이상 (props 타입 + 사용)
    - `grep -c '일정에 넣기' apps/web/app/moa/[id]/_components/place-list.tsx` → 1
    - `git diff -- apps/web/__tests__/place-list.test.tsx | grep -c '^-[^-]'` → 0 (기존 테스트 삭제 0)
    - `grep -c 'seq_no' apps/web/app/moa/[id]/_components/place-list.tsx` → 1 이상 (담은 순 배지 체계 유지)
    - `pnpm --filter @moajoa/web typecheck` → exit 0
  </acceptance_criteria>

  <done>PlaceList가 onAddToPlan 전달 시에만 '일정에 넣기'를 렌더하고, 미전달 시 기존 렌더가 바이트 동일하며, 기존 21케이스가 그린이다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: PlanSection — 상태기계 A~E + Day 탭 + 타임라인 + 미배치 풀 + 하단 액션 3종 (D-12~D-18, D-20, D-23, D-25)</name>
  <files>apps/web/app/moa/[id]/_components/plan-section.tsx, apps/web/__tests__/plan-section.test.tsx</files>

  <read_first>
    - `apps/ios/app/trip/[id]/(tabs)/plan.tsx` — **⚠️ READ-ONLY ANALOG. 읽기만 하고 절대 수정·복사·import하지 마라.** iOS는 v2.1 전면 동결이고 이 파일의 diff는 반드시 0이어야 한다(HC-1/SC-6). 참고할 것: 상태기계 A~F(L852-975) · 미배치 풀/Day 버킷 파생(L975-1061) · 연타 가드(L352-381) · 에러 카피. `@/components/plan/*`·`@/lib/realtime` 등 iOS 모듈은 웹에서 import 불가하다.
    - `apps/web/app/moa/[id]/_components/place-sheet.tsx` L116-133 — **제스처 소유권 계약**(핸들·헤더=드래그 전용, 본문=스크롤 전용). 이 파일은 **무수정**이다(HC-5)
    - `apps/web/app/moa/[id]/_components/place-list.tsx` L96-111 — 추출 대기/실패 판별식 (D-14 게이트가 이걸 그대로 재사용) · L115-124 빈 상태 카피 블록(D-23이 미러) · L180-218 배지+아코디언 anatomy
    - `packages/api/src/queries/plans.ts` — `PlanWithItems` 타입 형태(`getPlanByTrip` 반환)
    - `packages/core` — `PLAN_STEP_KO` 상수 · `PlanStepType` · `TravelMode`
    - 이 플랜 Task 1/2의 산출물 · 28-03의 D-21 계약
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Screen Contracts 3 (상태표 A~E + D 상태 레이아웃) · §Copywriting Contract · Auto-Decisions A-9~A-14
  </read_first>

  <behavior>
    - 장소 0개 → [일정] 섹션 자체가 렌더되지 않는다 (상태 A)
    - plan=null + 장소≥1 → 빈 상태 카피(D-23) + '일정 만들기' 버튼 (상태 B)
    - 추출 중 링크 2개 → '일정 만들기' disabled + '영상에서 장소를 찾고 있어요 · 2개 분석 중' (상태 B-1)
    - day_count·start_date 둘 다 null + owner → '일정 만들기' 탭 시 DurationGateSheet가 열리고 onGenerate는 발화하지 않는다 (상태 B-2)
    - day_count·start_date 둘 다 null + 비-owner → 버튼 disabled + '호스트가 여행 기간을 정하면...' 카피, 시트 안 열림 (상태 B-3, A-9)
    - generating=true → 버튼 disabled + '일정을 짜고 있어요…' + PLAN_STEP_KO 진행 카피 (상태 C)
    - error → 에러 행 + '다시 시도' 버튼 (상태 E)
    - plan 존재 → Day 탭 N개(= max day_index + 1) + 선택 Day의 타임라인이 sort_order 순으로 렌더되고 번호 배지가 sort_order+1이다 (상태 D)
    - 미배치 풀 = places − plan_items.place_id, 0개면 섹션 미렌더, N개면 '아직 안 넣은 곳 N'
    - '일정 다시 만들기' 아래에 D-25 보조 문구가 항상 렌더된다
    - 이동수단 세그먼트 변경 시 onTravelModeChange만 발화하고 재생성은 발화하지 않는다 (A-10)
  </behavior>

  <action>
    `apps/web/app/moa/[id]/_components/plan-section.tsx` 신규 생성. **props-driven** — 상태·mutation은 moa-island(28-06) 소유, 이 컴포넌트는 시트 열림/닫힘 같은 순수 UI 상태만 로컬로 갖는다.

    **props 계약 (28-06이 배선할 표면):**
    `plan: PlanWithItems | null` · `places: Place[]` · `links: Link[]` · `trip: Trip` · `currentUserId: string` · `generating: boolean` · `planStep: PlanStepType | null` · `error: string | null` · `selectedDay: number` · `onSelectDay(day: number)` · `onGenerate()` · `onSaveDuration(dayCount: number)` · `onMovePlaceToDay(placeId, dayIndex)` · `onMoveItemToDay(itemId, placeId, dayIndex)` · `onMoveToPool(itemId)` · `onTravelModeChange(mode: TravelModeType)` · `onShare()`.

    **파생 (쿼리 0 — 전부 클라 계산):**
    - Day 수 = `max(plan_items.day_index) + 1`. **클라이언트가 날짜에서 Day 수를 다시 계산하지 마라** — 결정자는 EF/plan_items다(iOS의 로컬 dayCount 헬퍼는 미러 금지).
    - Day 버킷 = plan_items를 day_index로 그룹 → sort_order 오름차순 정렬. place_id로 places를 lookup하고, **place가 없으면 그 항목은 건너뛴다**(FK-safety — 장소가 삭제된 경우).
    - 미배치 풀 = places(hidden 제외) − plan_items의 place_id 집합. 별도 테이블·쿼리 없음(D-17).

    **상태기계 A~E** — UI-SPEC §Screen Contracts 3 상태표를 그대로 구현. 게이트 판정은 이 컴포넌트가 props에서 파생한다:
    - **D-14 추출 대기:** `place-list.tsx`의 판별식을 **그대로 재사용** — 진행 중 = manual이 아니면서 추출 상태가 pending 또는 processing인 링크. 그 수가 N>0이면 버튼 disabled + `영상에서 장소를 찾고 있어요 · {N}개 분석 중`. 추출이 끝나면 links UPDATE → island reconcile로 자동 활성화되므로 **신규 realtime 배선이 0**이다.
    - **D-13 기간 미정:** `trip.day_count`와 `trip.start_date`가 **둘 다 null**이면 '일정 만들기' 탭이 `onGenerate()`가 아니라 `DurationGateSheet`를 연다. 시트가 dayCount를 확정하면 `onSaveDuration(dayCount)`를 부르고 시트를 닫는다(실제 생성은 island가 저장 성공 후 이어서 한다). **1일 기본 생성을 하지 않는다.**
    - **A-9 비-owner:** 위 조건 + `currentUserId !== trip.owner_id`이면 버튼을 disabled로 두고 `호스트가 여행 기간을 정하면 일정을 만들 수 있어요`를 버튼 바로 아래 텍스트로 병기한다. **시트를 열지 않는다.**
    - **C 생성 중:** `generating`이 true면 버튼 disabled + 라벨 `일정을 짜고 있어요…` + 진행 행(스피너 + `PLAN_STEP_KO[planStep]`). 진행 카피는 **core 상수를 쓴다 — 신규 문자열 금지.** 진행 영역에 `aria-live="polite"`. 연타 차단은 이 단일 boolean이 담당한다(유료 API 이중 지출 방지).

    **D 상태 레이아웃:**
    - 섹션 타이틀: `{도시}, {N}박 {N+1}일 추천 일정` — '추천 일정' 어절만 brand 색. 도시·기간을 모르면 `추천 일정`만.
    - **Day 탭 스트립 (⚠ HC-5 — 회귀 최상위 위험):** 시트 **본문(children) 스크롤 영역 안**에 둔다. `sticky top-0 z-10 bg-white` + 가로 스크롤만 허용(`overflow-x-auto touch-pan-x`) + 스크롤바 숨김.
      **금지:** 스트립에 포인터 이벤트 핸들러·포인터 캡처 API·터치 완전 차단 클래스를 얹지 마라. 셋 중 하나라도 들어가면 시트 드래그와 페이지 핀치줌이 회귀한다(커밋 3f32204이 청산한 버그). **`place-sheet.tsx` 자체는 한 줄도 수정하지 않는다.**
      **제스처 소유권:** 가로 스와이프=Day 스트립 · 세로 스크롤=시트 본문 · 시트 앵커 드래그=핸들/헤더 · 지도 팬줌=지도 표면. **한 표면이 두 제스처를 겸하지 않는다.**
      탭은 `role="tab"` + `aria-selected`, 부모는 `role="tablist"`. 선택 = brand 채움 + 흰 텍스트 / 비선택 = 흰 배경 + neutral 테두리.
    - **타임라인:** 각 행 = 번호 배지(24px, brand 채움, 흰 숫자) + 장소명 + 주소 + 이동시간 캡션 + `옮기기` text 버튼(→ DaySelectSheet).
      **번호 배지 값은 `sort_order + 1`** — 그날 방문 순서다. `place-list`의 `seq_no`(담은 순)와 **다른 체계이며 혼용하면 안 된다.**
      이동시간: `leg_travel_seconds`가 null이면 `이동시간 —`(iOS 관례 미러).
      **A-13: 타임라인 행에는 아코디언·찜을 노출하지 않는다**(명시적 비목표 — 장소 상세·찜은 풀 섹션/플랜 미생성 리스트에서 접근).
    - **미배치 풀 (D-17):** 0개면 섹션 미렌더. N개면 헤딩 `아직 안 넣은 곳 {N}` + `PlaceList`를 `places={pool}`로 **재사용**(분석중·실패 행 포함) + `onAddToPlan` 전달 → `DaySelectSheet` 오픈.
      **(0,0) 좌표 장소:** 풀 행에 `위치 정보 없음` 캡션을 병기한다(Pitfall 3 — AI가 영원히 배치 못 하는 장소라 "왜 안 들어가지?" 혼란을 흡수).
    - **하단 액션 3종 (D-18), 순서 고정:**
      1. `일정 다시 만들기` (outline 풀폭) → `onGenerate()`. **바로 아래에 D-25 보조 문구를 항상 렌더한다: `직접 옮긴 장소는 그대로 두고 나머지만 다시 짜요`.** 이 문구는 28-03의 D-21 계약이 동작하기 때문에 진실이다.
      2. **이동수단 세그먼트** (전철/도보/차, 기본 전철=transit) → `onTravelModeChange(mode)`만 호출. **A-10: 자동 재생성하지 않는다**(유료 API 이중 지출 방지). 변경 후에만 보조 문구 노출: `이동수단을 바꿨어요. 일정 다시 만들기를 누르면 경로를 새로 계산해요`.
      3. `일정 공유하기` (primary 풀폭) → `onShare()` (island이 기존 share-sheet를 연다 — 신규 공유 UI 0).
      레퍼런스의 '내 일정으로 담기'는 **만들지 않는다**(이미 내 모아다).
    - **파괴적 확인 다이얼로그 없음** — 재생성은 수동 배치를 보존하므로(D-21) 확인창을 두지 않는다.

    카피는 UI-SPEC Copywriting Contract verbatim. 색은 토큰 클래스만. 테스트는 `apps/web/__tests__/plan-section.test.tsx` (PlanWithItems 픽스처 + `@moajoa/core` PLAN_STEP_KO 실물 사용).
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/plan-section.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/plan-section.test.tsx` → 11케이스 그린 (상태 A/B/B-1/B-2/B-3/C/D/E + 풀 파생 + D-25 + 이동수단)
    - `pnpm --filter @moajoa/web test -- --run` → 전 스위트 그린 (무회귀)
    - `pnpm --filter @moajoa/web typecheck` → exit 0
    - **HC-5 제스처 게이트:** `grep -Ec 'onPointerDown|onPointerMove|setPointerCapture|touch-none' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → **0**
    - **HC-5 시트 무수정:** `git diff --stat -- 'apps/web/app/moa/[id]/_components/place-sheet.tsx'` → **빈 출력**
    - `grep -c 'touch-pan-x' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1 (가로 스크롤만 허용)
    - `grep -c 'role="tablist"' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1
    - **D-25 카피:** `grep -c '직접 옮긴 장소는 그대로 두고 나머지만 다시 짜요' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1
    - **D-23 카피:** `grep -c '링크를 넣거나 장소를 담고 일정 만들기를 누르면 AI가 동선을 짜드려요' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1
    - **D-14 카피:** `grep -c '영상에서 장소를 찾고 있어요' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1
    - **A-9 카피:** `grep -c '호스트가 여행 기간을 정하면' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1
    - `grep -c 'PLAN_STEP_KO' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1 (진행 카피는 core 상수)
    - `grep -c 'sort_order + 1' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 1 이상 (타임라인 번호 체계)
    - `grep -Ec '@moajoa/api|getSupabaseBrowser|\.channel\(' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → **0** (mutation·realtime 미소유 — island 몫)
    - **SC-6:** `git diff --stat -- apps/ios` → **빈 출력**
    - `grep -rEc '#[0-9a-fA-F]{3,8}\b' 'apps/web/app/moa/[id]/_components/plan-section.tsx'` → 0
  </acceptance_criteria>

  <done>PlanSection이 상태 A~E를 전부 렌더하고, Day 탭이 제스처 계약을 지키며, 번호 체계가 sort_order+1이고, D-23/D-25/D-14/A-9 카피가 전부 존재하며, api·realtime을 직접 건드리지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 UI → mutation 콜백 → moa-island → Supabase | 이 플랜의 컴포넌트는 콜백만 발화하고 직접 DB에 닿지 않는다 |
| 공유 링크 열람자(게스트) → /moa/[id] | 게스트는 `/t/[slug]` 표면을 쓴다 — 이 플랜의 컴포넌트는 `/moa/[id]`(멤버 전용, RLS 게이트) 안에만 마운트된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-18 | Elevation of Privilege | `DurationGateSheet` — editor가 day_count 저장 시도 | medium | mitigate | UI 게이트: 비-owner는 버튼 disabled + 안내 카피, 시트를 열지 않는다(A-9). DB 게이트: `trips` UPDATE RLS owner-only(0016)가 최종 방어. **심층방어 2겹** — UI만으로 막지 않는다 |
| T-28-19 | DoS (비용 — 유료 API 이중 지출) | '일정 만들기' / '다시 만들기' 연타 | high | mitigate | `generating` 단일 boolean으로 버튼 disabled + 라벨 교체(상태 C). 이동수단 토글은 저장만 하고 **자동 재생성하지 않는다**(A-10) — 토글 연타로 Claude+Routes가 반복 호출되는 경로를 원천 차단 |
| T-28-20 | Tampering | 게스트/비멤버의 플랜 변경 | high | accept | 이 컴포넌트는 `/moa/[id]`(RSC가 `getTrip` null → notFound로 비멤버 차단, T-24-17) 안에서만 마운트된다. 실제 쓰기는 `plan_items` RLS(`can_edit_trip`, 0017)가 게이트. 게스트 공유 표면(`/t/[slug]`)에는 이 컴포넌트가 없다 |
| T-28-21 | Information Disclosure | 시트 드래그 회귀로 인한 UI 오작동 | low | mitigate | 보안 이슈는 아니나 회귀 최상위 위험 — acceptance가 금지 핸들러 grep 0 + place-sheet diff 0을 강제(HC-5) |
| T-28-22 | Injection | 장소명·주소 렌더 | low | accept | React 기본 이스케이프. `dangerouslySetInnerHTML` 미사용(place-list의 T-24-14 계약 승계) |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web test -- --run` 전 스위트 그린
- `pnpm --filter @moajoa/web typecheck` exit 0
- **HC-5:** plan-section에 금지 제스처 핸들러 0건 + `place-sheet.tsx` diff 0
- **HC-1/SC-6:** `git diff --stat -- apps/ios` 빈 출력
- **HC-3:** `git diff --stat -- apps/web/components/add-content-tabs.tsx` 빈 출력
- 신규 파일 3종에 hex 색 리터럴 0 · 워크스페이스 import `.js` 확장자 0
</verification>

<success_criteria>
- [ ] SC-4(부분): Day 1~N 탭 + 번호 타임라인 결과 화면이 place-sheet 안에 렌더된다
- [ ] SC-5(부분): 미배치 장소를 Day로 넣거나 '아직 모르겠다'로 남길 수 있고, 규칙이 가이드 카피로 보인다
- [ ] D-25 카피가 D-21 계약(28-03) 위에서 진실이다
</success_criteria>

<output>
Create `.planning/phases/28-add-trip-redesign-ai/28-05-SUMMARY.md` when done
</output>
