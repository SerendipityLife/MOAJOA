---
phase: 21-travel-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0022_ledger.sql
  - packages/api/src/types/database.ts
autonomous: false
requirements: [LEDGER-01, LEDGER-03, LEDGER-04, LEDGER-05, LEDGER-06]
user_setup:
  - service: supabase (cloud project xfoauhsraguyrifingct 또는 로컬 colima)
    why: "0022 적용 대상 DB. 로컬은 colima Docker 가용(memory docker-runtime-on-this-mac). 원격 적용은 사용자 자격/승인 — 0021 선례"
    env_vars:
      - name: (세션 한정) POOLER_URL 또는 로컬 supabase
        source: "로컬 pnpm supabase:reset 또는 원격 Session pooler string (사용자 제공)"
must_haves:
  truths:
    - "라이브 DB에 ledger_entries 테이블이 존재하고 RLS SELECT가 trip_id NULL이면 owner_user_id=auth.uid(), 아니면 can_read_trip(trip_id)로 게이트된다 (D-04/D-05)"
    - "ledger_entries UPDATE/DELETE는 owner_user_id=auth.uid()만 허용된다 (행 소유자, D-04)"
    - "forwarding_addresses에 사용자당 opaque token이 발급되고 user_id=auth.uid()로만 읽힌다 (LEDGER-01/05)"
    - "5요소(amount_foreign/currency/fx_rate/fx_source/fx_as_of)가 컬럼으로 존재하고 fx_source CHECK가 email/frankfurter/unavailable로 제한된다 (LEDGER-03)"
    - "재생성된 database.ts에 ledger_entries + forwarding_addresses Row/Insert/Update 타입이 존재한다"
  artifacts:
    - "supabase/migrations/0022_ledger.sql"
    - "packages/api/src/types/database.ts (재생성)"
  key_links:
    - "0022 정책 → 0016 can_read_trip SECURITY DEFINER 헬퍼 (42P17 가드 — CASE 내 직접 EXISTS 0)"
    - "ensure_forwarding_token → 0016 ensure_share_slug / 0018 ensure_poll_code gen_random_bytes idiom"
    - "LedgerStatus/FxSource enum(21-02) ↔ 0022 CHECK 제약 값 완전 일치"
---

<objective>
0022_ledger.sql 작성 + DB 적용 + database.ts 재생성 + RLS 매트릭스 검증. 개인 전용 전달주소(forwarding_addresses opaque token)와 trip-scoped(nullable) 가계부(ledger_entries)를 신설하고, trip_id NULL 분기 RLS(미분류=본인·배정=멤버)와 환율 5요소 원자 저장을 구조화한다 (LEDGER-01/03/04/05/06 데이터 측면).

Purpose: 라이브 DB가 0022와 불일치하면 파이프라인 INSERT와 가계부 읽기가 런타임 42501/42P01로 터진다 — phase 검증 전제. 가계부는 plan/plan_items와 무관(앱 미경유 예약 LEDGER-04)하므로 독립 테이블이 구조적 근거.
Output: 0022 마이그레이션 + 적용 + 재생성 타입 + RLS 매트릭스 PASS(특히 trip_id NULL 분기).
</objective>

<execution_context>
GSD execute-plan 워크플로우 부재 환경 — 이 plan은 수동 실행. atomic commit(Conventional Commits), append-only 마이그레이션 규칙(CLAUDE.md §4.3) 엄수.
</execution_context>

<context>
@.planning/phases/21-travel-ledger/21-CONTEXT.md
@.planning/phases/21-travel-ledger/21-RESEARCH.md
@.planning/phases/21-travel-ledger/21-PATTERNS.md
@supabase/migrations/0021_booking.sql
</context>

## Artifacts this plan produces
- `supabase/migrations/0022_ledger.sql` — 테이블 `forwarding_addresses`(id, user_id, token, created_at) + `ledger_entries`(id, owner_user_id, trip_id nullable, status, platform, card_last4, merchant, amount_foreign, currency, fx_rate, fx_source, fx_as_of, amount_krw, paid_at, raw_mime, raw_expires_at, timestamps) + 인덱스 3 + 트리거 2(ensure_forwarding_token, ledger_entries_set_updated_at) + RLS 정책 5(ledger 3 + forwarding 2)
- `packages/api/src/types/database.ts` — 재생성

<tasks>

<task type="auto">
  <name>Task 1: 0022_ledger.sql 작성 (append-only)</name>
  <files>supabase/migrations/0022_ledger.sql</files>
  <read_first>
    - supabase/migrations/0016_trips_baseline.sql (ensure_share_slug L158-182 + set_updated_at L38-46 + can_read_trip/can_edit_trip 헬퍼 정의부)
    - supabase/migrations/0018_date_polls.sql (ensure_poll_code 트리거 idiom)
    - supabase/migrations/0021_booking.sql (헤더 주석 스타일 + 테이블/RLS/트리거/additive idiom — 0022가 미러할 정본)
    - .planning/phases/21-travel-ledger/21-PATTERNS.md §0022 섹션
    - .planning/phases/21-travel-ledger/21-RESEARCH.md §"Pattern 4: RLS trip_id NULL 분기"
  </read_first>
  <action>
    새 파일 `supabase/migrations/0022_ledger.sql`. 0016~0021 절대 무수정(append-only). 0021 헤더 주석 스타일(phase/req/결정 인용 + 42P17 가드 명시) 미러. 순서:

    1. **forwarding_addresses 테이블** (LEDGER-01/05):
       - id uuid pk default gen_random_uuid()
       - user_id uuid not null references auth.users(id) on delete cascade
       - token text not null unique
       - created_at timestamptz not null default now()
       - unique(user_id) — 사용자당 1개(재발급=UPDATE)
    2. **ensure_forwarding_token 트리거** — 0016 ensure_share_slug **verbatim 미러**(share_slug→token 리네임): before insert, token null이면 `lower(translate(encode(gen_random_bytes(8),'base64'),'+/=','abc'))` → `substr(regexp_replace(...,'[^a-z0-9]','','g'),1,12)`, <8자면 md5 패딩. unique 제약이 충돌 방어(retry 루프 없음 — ensure_share_slug와 동일).
    3. **ledger_entries 테이블** (LEDGER-02/03/04):
       - id uuid pk default gen_random_uuid()
       - owner_user_id uuid not null references auth.users(id) on delete cascade
       - trip_id uuid references trips(id) on delete set null  -- **nullable** 미분류(D-05)
       - status text not null default 'pending' check (status in ('pending','processing','ready','needs_review','failed'))
       - platform text / merchant text
       - card_last4 text check (card_last4 ~ '^[0-9]{4}$')  -- nullable
       - amount_foreign numeric(14,2)  -- 원통화 ①
       - currency text check (char_length(currency)=3)  -- ②
       - fx_rate numeric(18,8)  -- ③
       - fx_source text check (fx_source in ('email','frankfurter','unavailable'))  -- ④
       - fx_as_of date  -- ⑤ (Frankfurter 응답 date = 주말이면 직전영업일)
       - amount_krw numeric(14,2)  -- 파생(파이프라인 계산, generated 아님 — fx_rate null 허용 위해)
       - paid_at timestamptz / raw_mime text / raw_expires_at timestamptz  -- 최소저장+TTL(D-03)
       - created_at/updated_at timestamptz not null default now()
    4. **인덱스:** `ledger_owner_idx (owner_user_id)`, `ledger_trip_idx (trip_id) where trip_id is not null`, `ledger_status_idx (status) where status in ('pending','processing')`.
    5. **updated_at 트리거:** `ledger_entries_set_updated_at` — 0016 set_updated_at() 재사용, **재정의 금지**.
    6. **RLS ledger_entries** (RESEARCH Pattern 4 — 이 phase 핵심):
       - `enable row level security`
       - SELECT to authenticated using `(case when trip_id is null then owner_user_id = auth.uid() else can_read_trip(trip_id) end)` — 미분류 본인·배정 멤버(D-04/D-05)
       - UPDATE to authenticated using `(owner_user_id = auth.uid())` with check `(owner_user_id = auth.uid())` — 행 소유자만(D-04)
       - DELETE to authenticated using `(owner_user_id = auth.uid())`
       - **INSERT 정책 없음** — 앱 클라 INSERT 안 함(파이프라인 service-role만; RLS enabled + INSERT 정책 부재 = authenticated INSERT deny)
    7. **RLS forwarding_addresses:**
       - SELECT to authenticated using `(user_id = auth.uid())`
       - INSERT to authenticated with check `(user_id = auth.uid())` — getOrCreate(트리거가 token 생성)
       - (UPDATE는 재발급용 — 이번 scope 밖, 미포함. planner 필요시 추가)
    8. **anon 정책 0.** CASE 내 직접 cross-table EXISTS 0(can_read_trip 헬퍼만 — 42P17 가드).
  </action>
  <verify>
    <automated>grep -c "can_read_trip" supabase/migrations/0022_ledger.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create policy" supabase/migrations/0022_ledger.sql` == 5 (ledger 3 + forwarding 2)
    - `grep -cE "plan_id|plan_item_id" supabase/migrations/0022_ledger.sql` == 0 (LEDGER-04 가계부는 plan 무관)
    - `grep -c "exists" supabase/migrations/0022_ledger.sql` == 0 (CASE도 헬퍼 경유)
    - `grep -c "create function" supabase/migrations/0022_ledger.sql` == 1 (ensure_forwarding_token만 — set_updated_at 재정의 0)
    - `grep -c "case when trip_id is null" supabase/migrations/0022_ledger.sql` >= 1 (NULL 분기)
    - `git diff --stat supabase/migrations/0016_trips_baseline.sql supabase/migrations/0021_booking.sql` 빈 출력 (append-only)
    - `grep -c "to anon\|grant.*anon" supabase/migrations/0022_ledger.sql` == 0
  </acceptance_criteria>
  <done>0022가 RESEARCH 골자·0021 idiom과 일치, 구조 grep 게이트 전부 통과</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] DB 적용 수단 확정</name>
  <files>(자격/모드 게이트 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/21-travel-ledger/21-RESEARCH.md §Environment Availability
    - memory: docker-runtime-on-this-mac.md (colima 로컬 스택)
  </read_first>
  <what-built>0022_ledger.sql 완성. 이 Mac은 colima Docker 가용이라 로컬 apply가 기본 경로. 원격(ref xfoauhsraguyrifingct) 적용은 0021 선례대로 사용자 자격/승인 필요.</what-built>
  <action>사용자에게 두 경로 제시 후 응답 대기 — Task 3(적용) 전에 시작 안 함:
    1. (권장·로컬) `pnpm supabase:start`(colima) → `pnpm supabase:reset`로 0016~0022 클린 적용 → `pnpm supabase:types`. Claude가 실행.
    2. (원격) Session pooler string 붙여넣기 → Claude가 pg로 0022만 적용 + gen types. 문자열은 커밋·파일 금지.
  </action>
  <verify>사용자로부터 "로컬 적용 진행" 또는 pooler string 수신</verify>
  <acceptance_criteria>Task 3 실행 모드 결정</acceptance_criteria>
  <done>적용 수단 확보</done>
  <resume-signal>"로컬로 적용해" 또는 pooler connection string 붙여넣기</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 적용 + database.ts 재생성 + RLS 매트릭스</name>
  <files>packages/api/src/types/database.ts</files>
  <read_first>
    - supabase/migrations/0022_ledger.sql (Task 1 산출물)
    - package.json (supabase:types/supabase:reset 스크립트)
    - .planning/phases/19-date-voting/19-01-SUMMARY.md (RLS 매트릭스 BEGIN…ROLLBACK + set_config 소멸 함정 회피 선례)
  </read_first>
  <action>
    Task 2 경로대로:
    1. **적용:** 로컬이면 `pnpm supabase:reset`(0016~0022 클린) — 42P17 recursion 0 확인. 원격이면 단일 트랜잭션 0022 실행 + schema_migrations version '0022' insert(중복적용 방지).
    2. **타입 재생성:** `pnpm supabase:types`(로컬) 또는 `supabase gen types --db-url $POOLER_URL`. `pnpm --filter @moajoa/api typecheck` exit 0.
    3. **RLS 매트릭스**(19-01 선례 — BEGIN…ROLLBACK 단일 트랜잭션, set_config is_local 소멸 회피). fixture: auth.users 2명 + profiles + trip 1개(owner=A) + membership(B accepted member). `set local role authenticated; select set_config('request.jwt.claims','{"sub":"<uuid>"}',true);`로:
       - (A) A가 forwarding_addresses INSERT(user_id=A) → 성공 + 트리거가 token 생성(12자 `^[a-z0-9]+$`)
       - (B) A가 자기 user_id로 ledger_entries는 앱 INSERT 불가 확인 → **service-role로 INSERT**(파이프라인 경로) 후 아래 SELECT 검증
       - (C) trip_id=NULL 행: owner=A → A SELECT 보임, B SELECT 0행(미분류 본인-only, D-05)
       - (D) trip_id=trip(owner A, B member) 배정 행: A·B 둘 다 SELECT 보임(멤버 공유, D-04)
       - (E) B가 A 소유 행 UPDATE/DELETE → RLS 거부(행 소유자만, D-04)
       - (F) A가 자기 행 UPDATE(trip 배정) → 성공
       - (G) fx_source CHECK: 'bank' INSERT → 위반, 'email'/'frankfurter'/'unavailable' → 통과 / status CHECK: 'done' → 위반, 'ready' → 통과 / card_last4 '12ab' → 위반, '1234' → 통과
       - (H) B가 A의 forwarding token SELECT → 0행(user_id=auth.uid())
       전 케이스 SUMMARY 기록. 마지막 ROLLBACK.
  </action>
  <verify>
    <automated>grep -c "ledger_entries" packages/api/src/types/database.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ledger_entries" packages/api/src/types/database.ts` >= 3 (Row/Insert/Update)
    - `grep -c "forwarding_addresses" packages/api/src/types/database.ts` >= 3
    - `grep -c "fx_source\|amount_foreign" packages/api/src/types/database.ts` >= 2
    - `pnpm --filter @moajoa/api typecheck` exit 0
    - RLS 매트릭스 A~H 전부 기대 결과 — 42P17 recursion 0 (SUMMARY 케이스별 기록)
    - pooler URL이 커밋·파일 어디에도 없음 (`git grep -c "pooler.supabase" -- ':!*.md'` == 0)
  </acceptance_criteria>
  <done>DB가 0022와 일치, 타입 재생성 + api typecheck green, RLS 매트릭스(특히 trip_id NULL 분기 C/D) PASS</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| iOS 클라(user JWT) → Postgres RLS | ledger SELECT/UPDATE/DELETE, forwarding SELECT/INSERT가 이 경계 |
| 파이프라인 EF(service-role) → ledger INSERT | 앱은 INSERT 안 함 — service-role만(21-04) |
| 플래너 세션 → DB | DDL 적용, 자격 유출 금지 |

## STRIDE Threat Register
| Threat | Category | Severity | Mitigation |
|--------|----------|----------|------------|
| T-21-01 | Info Disclosure | high | 미분류(trip_id NULL) 행 타 사용자 열람 → CASE `owner_user_id=auth.uid()`. 매트릭스 C |
| T-21-02 | Tampering | high | 타 사용자 명의 행 수정/삭제 → UPDATE/DELETE `owner_user_id=auth.uid()`. 매트릭스 E |
| T-21-03 | Info Disclosure | medium | 타인 전달 token 조회(주소 탈취) → forwarding SELECT `user_id=auth.uid()`. 매트릭스 H |
| T-21-04 | Elevation | medium | 42P17 재귀(CASE cross-table) → can_read_trip DEFINER 헬퍼만, 직접 EXISTS 0 grep |
| T-21-05 | Spoofing | high | 앱 클라가 가짜 ledger INSERT → INSERT 정책 부재(service-role만). 매트릭스 B |
| T-21-06 | Info Disclosure | high | pooler string 유출 → 세션 환경변수만, 커밋 금지 게이트 |
</threat_model>

<verification>
- 라이브 DB: ledger_entries + forwarding_addresses 존재 + RLS enabled + 정책 5
- database.ts diff가 ledger/forwarding 추가만(무관 테이블 diff 0)
- RLS 매트릭스가 trip_id NULL 분기(C/D)와 행 소유자 write(E) 실증
</verification>

<success_criteria>
- 0022 append-only 커밋 + DB 적용 — 이후 wave의 INSERT/SELECT가 42501/42P01 없이 동작할 기반
- RLS 매트릭스가 T-21-01/02/03/05 실증(특히 미분류 본인-only ↔ 배정 멤버공유 전이)
</success_criteria>

<output>
Create `.planning/phases/21-travel-ledger/21-01-SUMMARY.md` when done
</output>
