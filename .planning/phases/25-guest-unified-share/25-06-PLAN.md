---
phase: 25-guest-unified-share
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/layout.tsx
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/app/t/[slug]/_components/guest-surface.tsx
  - apps/web/__tests__/moa-island.test.tsx
  - apps/web/__tests__/guest-surface.test.tsx
autonomous: true
gap_closure: true
requirements: [SHARE-02, SHARE-03]

must_haves:
  truths:
    - "게스트/호스트 클릭이 제휴 스크립트에 하이재킹되지 않는다 — 어떤 페이지에서도 emrldco 스크립트가 로드되지 않는다 (UAT Gap: TP 하이재킹)"
    - "모바일 폭 collapsed 시트에서 + FAB가 place-list 첫 행의 하트 탭 타깃을 덮지 않는다 (UAT Gap: SC3 찜 클릭 도달)"
    - "게스트 /t/[slug] 화면에는 [함께 정하기] 버튼이 노출되지 않고, 호스트 /moa/[id]에는 그대로 노출된다 (UAT Gap: 호스트 컨트롤 노출)"
  artifacts:
    - path: "apps/web/app/layout.tsx"
      provides: "TP 스크립트 없는 루트 레이아웃"
      not_contains: "emrldco"
    - path: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      provides: "collapsed 시트 위(calc(30vh+16px)) FAB + expanded 시 숨김 + hideHostControls prop"
      contains: "hideHostControls"
    - path: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      provides: "게스트 마운트에서 hideHostControls 전달 (D-08 재사용 유지, 병렬 컴포넌트 0)"
      contains: "hideHostControls"
  key_links:
    - from: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      to: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      via: "MoaIsland render 시 hideHostControls prop"
      pattern: "hideHostControls"
    - from: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      to: "FAB visibility"
      via: "sheetAnchor state 조건 렌더"
      pattern: "sheetAnchor !== 'expanded'"
---

<objective>
UAT(25-HUMAN-UAT.md Test 3)에서 진단된 클릭-도달 블로커 2건 + polish 1건을 닫는다:
(1) Travelpayouts Drive 스크립트 전면 제거(클릭 하이재킹 — 사용자 잠금 결정 "전면 제거"),
(2) + FAB가 place-list 첫 행 하트를 덮는 레이아웃 픽스,
(3) 게스트 컨텍스트에서 호스트 전용 [함께 정하기] 숨김(최소 prop — D-08 재사용 유지).

Purpose: 게스트 찜(SC3)의 실제 실패 원인은 API가 아니라 클릭 도달 실패 2종 — API 경로(201/204)·닉네임 게이트·SSR은 라이브 검증 통과(재수정 금지).
Output: layout.tsx에서 제휴 스크립트 제거, moa-island FAB 재배치 + hideHostControls prop, guest-surface 게스트 마운트 배선, 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-guest-unified-share/25-HUMAN-UAT.md
@.planning/phases/25-guest-unified-share/25-CONTEXT.md

<interfaces>
현재 코드 계약 (직접 읽어 확인된 것 — 탐색 불필요):

apps/web/app/layout.tsx L3, L46-49:
```tsx
import Script from 'next/script';
...
{/* Travelpayouts Drive — website-channel auto-affiliation (Phase 20 in-app
    deep links are separate and do NOT depend on this). ... */}
<Script src="https://emrldco.com/NTQ1NTU1.js?t=545555" strategy="afterInteractive" />
```
→ Script는 이 파일에서 이 한 곳만 사용. Phase 20 인앱 딥링크(SubID)는 주석 명시대로 무의존(다른 코드 grep 0건 확인됨).

apps/web/app/moa/[id]/_components/moa-island.tsx:
- L81: `const [sheetAnchor, setSheetAnchor] = useState<SheetAnchor>('collapsed');`
- L294-299: [함께 정하기] 버튼 — `<div className="absolute right-4 top-4 z-50"><Button size="sm" onClick={() => setShareOpen(true)}>함께 정하기</Button></div>`
- L333-344: FAB — `{!addOpen && !shareOpen && (<button ... aria-label="장소 추가" className="absolute bottom-[136px] right-4 z-[60] grid size-14 place-items-center rounded-full bg-brand-600 text-white shadow-fab">...)}` 주석: "collapsed 시트 상단에서 16px 위" (의도) — 실제 136px는 peek(30vh≈200px+)보다 작아 시트 안을 덮음(버그 원인)
- MoaIslandProps (L30-42): trip, currentUserId, initialPlaces, initialLinks, initialCounts, initialMyVotedPlaceIds, memberIdsInJoinOrder, initialProfileNames, initialMessages, currentUserNickname

apps/web/app/moa/[id]/_components/place-sheet.tsx L42: collapsed peek = `window.innerHeight * 0.3` (30vh) — FAB 오프셋 계산 근거.

apps/web/app/t/[slug]/_components/guest-surface.tsx — MoaIsland render 2곳:
- L336 (both): `{joined && moaSeed ? <MoaIsland {...moaSeed} /> : readOnlyPlaces}`
- L345 (places): `{joined && moaSeed ? <MoaIsland {...moaSeed} /> : readOnlyPlaces}`

apps/web/__tests__/moa-island.test.tsx:
- MoaMap 스텁이 `data-testid="marker-p1"` 버튼으로 onMarkerTap('p1') 노출 (L80-84) — 마커 탭 = sheetAnchor 'expanded' 전환 트리거로 테스트 가능
- share-sheet/add-sheet/moa-chat/moa-tab-bar 전부 vi.mock 스텁
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Travelpayouts Drive 스크립트 전면 제거 (Gap 2, ae52afe 역)</name>
  <files>apps/web/app/layout.tsx</files>
  <read_first>apps/web/app/layout.tsx</read_first>
  <action>
  사용자 잠금 결정 "전면 제거"대로:
  1. L49 `<Script src="https://emrldco.com/NTQ1NTU1.js?t=545555" ... />` 삭제.
  2. L46-48 Travelpayouts 주석 블록 삭제.
  3. 이 변경으로 고아가 된 L3 `import Script from 'next/script';` 삭제 (Script는 이 파일에서 유일 사용처 — 확인됨).
  4. 그 외 어떤 줄도 건드리지 말 것 (surgical). Phase 20 인앱 딥링크 코드는 별개 — 절대 접근 금지.
  </action>
  <verify>
    <automated>! grep -rn "emrldco" apps/web --include="*.tsx" --include="*.ts" && ! grep -n "next/script" apps/web/app/layout.tsx && CI=true pnpm --filter @moajoa/web test</automated>
  </verify>
  <done>apps/web 전체에서 emrldco 참조 0건, layout.tsx에 next/script import 없음, web 테스트 전체 그린(스크립트는 테스트 무관 — 회귀 0 확인용).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FAB를 collapsed 시트 위로 재배치 + expanded 시 숨김 (Gap 3)</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/__tests__/moa-island.test.tsx</files>
  <read_first>apps/web/app/moa/[id]/_components/moa-island.tsx (L80-90, L333-344), apps/web/__tests__/moa-island.test.tsx (마커 스텁·기존 렌더 헬퍼)</read_first>
  <behavior>
    - Test A: 초기 렌더(collapsed) → `aria-label="장소 추가"` FAB가 문서에 존재
    - Test B: `marker-p1` 클릭(onMarkerTap → sheetAnchor 'expanded') → FAB가 문서에서 사라짐
  </behavior>
  <action>
  moa-island.tsx FAB(L333-344) 수정 — 코드 주석의 원래 의도("collapsed 시트 상단에서 16px 위")를 실제로 구현:
  1. 클래스 `bottom-[136px]` → `bottom-[calc(30vh+16px)]` (place-sheet collapsed peek = 30vh + 16px 오프셋. UI-SPEC 토큰: FAB↔시트 오프셋 16px = spacing 4).
  2. 렌더 조건 `!addOpen && !shareOpen` → `!addOpen && !shareOpen && sheetAnchor !== 'expanded'` — expanded 시트는 리스트 풀스크린이라 우측 하트들 위 어디든 FAB가 겹침 → 숨김이 최소·안전 픽스.
  3. FAB 주석을 실제 동작에 맞게 갱신(collapsed 시트 위 16px, expanded 시 숨김 — 첫 행 하트 탭 타깃 겹침 해소, 25-06 gap fix).
  4. moa-island.test.tsx에 behavior의 Test A/B 추가 (기존 marker-p1 스텁 재사용, 신규 mock 불필요).
  다른 렌더 조건·z-order(z-[60])·크기(size-14)·색(brand-600)은 무변경.
  </action>
  <verify>
    <automated>grep -n "calc(30vh+16px)" "apps/web/app/moa/[id]/_components/moa-island.tsx" && ! grep -n "bottom-\[136px\]" "apps/web/app/moa/[id]/_components/moa-island.tsx" && CI=true pnpm --filter @moajoa/web test moa-island</automated>
  </verify>
  <done>FAB가 collapsed 시트 상단 16px 위(calc(30vh+16px))에 위치하고 expanded 시 렌더되지 않음. Test A/B 포함 moa-island 테스트 그린.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MoaIsland hideHostControls prop — 게스트에서 [함께 정하기] 숨김 (Gap 4)</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/app/t/[slug]/_components/guest-surface.tsx, apps/web/__tests__/moa-island.test.tsx, apps/web/__tests__/guest-surface.test.tsx</files>
  <read_first>apps/web/app/moa/[id]/_components/moa-island.tsx (L30-42 props, L294-299 버튼), apps/web/app/t/[slug]/_components/guest-surface.tsx (L336, L345), apps/web/__tests__/guest-surface.test.tsx (기존 mock·시딩 패턴)</read_first>
  <behavior>
    - Test C (moa-island): 기본 렌더(prop 미전달, 호스트) → "함께 정하기" 버튼 존재 (호스트 무회귀)
    - Test D (moa-island): `hideHostControls` 전달 → "함께 정하기" 버튼 부재
    - Test E (guest-surface): joined 게스트(test seam: initialJoined 등 기존 시딩)에서 MoaIsland가 hideHostControls로 마운트됨 — 기존 테스트의 MoaIsland mock이 있으면 prop 검증, 실 마운트면 버튼 부재 검증
  </behavior>
  <action>
  D-08(재사용) 유지 — 병렬 게스트 컴포넌트 금지. 최소 prop 1개만 추가:
  1. moa-island.tsx MoaIslandProps에 `/** 게스트 마운트(/t)에서 호스트 전용 컨트롤([함께 정하기]) 숨김 (25-06 Gap 4). */ hideHostControls?: boolean;` 추가(default 미전달 = false — 호스트 무변경).
  2. L294-299 [함께 정하기] 오버레이 div를 `{!hideHostControls && ( ... )}`로 감쌈. ShareSheet 마운트는 그대로 둠(shareOpen이 절대 true가 안 되므로 무해 — surgical).
  3. guest-surface.tsx의 MoaIsland render 2곳(L336 both, L345 places)을 `<MoaIsland {...moaSeed} hideHostControls />`로 변경. 그 외 guest-surface 로직(게이트·hydrate·poll fetch — 전부 라이브 검증 통과분) 절대 무변경.
  4. behavior의 Test C/D/E 추가.
  </action>
  <verify>
    <automated>grep -n "hideHostControls" "apps/web/app/moa/[id]/_components/moa-island.tsx" && test "$(grep -c "hideHostControls" "apps/web/app/t/[slug]/_components/guest-surface.tsx")" -ge 2 && CI=true pnpm --filter @moajoa/web test moa-island guest-surface</automated>
  </verify>
  <done>게스트 마운트 2곳 모두 hideHostControls 전달, 호스트 기본 렌더는 버튼 유지. Test C/D/E 포함 web 테스트 그린.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser ↔ third-party CDN | emrldco.com 제휴 스크립트가 전 페이지 클릭 이벤트에 접근하던 경계 (제거 대상) |
| guest(anon session) ↔ host controls | 게스트가 호스트 전용 공유 컨트롤을 조작하는 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25G-01 | Tampering (click hijack/popunder) | apps/web/app/layout.tsx 제3자 Script | mitigate | **위협 감소로 기록**: emrldco 스크립트 전면 제거(Task 1) — link_switcher/popunder에 의한 클릭 납치·Kiwi.com 리다이렉트 위협 소멸. 제3자 스크립트 재도입 시 게스트 인터랙티브 표면 제외 필수 |
| T-25G-02 | Elevation of Privilege | [함께 정하기] 게스트 노출 | mitigate | UI 숨김(hideHostControls, Task 3)은 UX 계층. DB 계층은 기존 trips UPDATE RLS(owner-only)가 shareMoa를 이미 차단 — 심층방어 2겹 유지 |
</threat_model>

<verification>
- `! grep -rn "emrldco" apps/web --include="*.tsx" --include="*.ts"` — TP 스크립트 0건
- `CI=true pnpm --filter @moajoa/web test` — web 전 스위트 그린 (bare vitest watch 금지 — CI=true 필수)
- `pnpm --filter @moajoa/web exec tsc --noEmit` 또는 기존 typecheck 스크립트 — 타입 0 에러
- 회귀 금지 확인: guest-surface의 게이트·hydrate·poll fetch 로직 diff 0 (MoaIsland render 2줄 + prop 외)
</verification>

<success_criteria>
- UAT Gap "게스트 클릭이 제휴 스크립트에 하이재킹되지 않는다": emrldco 로드 경로 소멸 (코드 레벨 확정)
- UAT Gap "게스트가 장소에 찜할 수 있다"의 도달 실패 (b)항: collapsed에서 FAB가 시트 밖(위 16px)·expanded에서 부재 → 첫 행 하트 탭 타깃 노출
- UAT Gap "게스트에게 호스트 전용 컨트롤 미노출": /t 게스트 마운트에서 [함께 정하기] 부재, /moa 호스트 무회귀
- 라이브 검증 통과분(찜 API 201/204·닉네임 게이트·재접속 동일신원·D-12·SSR) 코드 경로 무변경
</success_criteria>

<output>
After completion, create `.planning/phases/25-guest-unified-share/25-06-SUMMARY.md`
</output>
