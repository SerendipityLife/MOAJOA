---
phase: 01-build-unblock-hygiene
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/ios/app.config.ts
  - apps/ios/app/index.tsx
  - apps/ios/app/_layout.tsx
  - apps/ios/ios/
  - docs/SESSION-NOTES-2026-05-25.md
autonomous: false
requirements: [BUILD-01, BUILD-02, BUILD-03]

must_haves:
  truths:
    - "iOS 앱이 실기기(아이폰)에서 로컬 prebuild로 빌드되어 실행되고 첫 화면이 뜬다 (BUILD-01)"
    - "첫 화면의 NativeWind className(bg-brand-500, bg-white, text-brand-700)이 실기기에 시각적으로 적용된다 (BUILD-02, silent failure 없음)"
    - "실기기 홈에 MOAJOA app icon이 default Expo가 아닌 brand orange M-pin으로 표시된다 (BUILD-03 일부)"
    - "콜드 launch 시 흰 splash 배경 + 중앙 brand-orange 워드마크가 ~1초 표시된다 (BUILD-03 일부)"
    - "Pretendard 폰트 4 weight가 native bundle에 embed되어 iOS에서 사용 가능하다 (BUILD-03 일부)"
    - "4시간 timebox 안에 path A 성공 또는 path B(EAS) 전환 + SESSION-NOTES 기록 완료"
  artifacts:
    - path: "apps/ios/app.config.ts"
      provides: "Expo config — icon, splash plugin, expo-font plugin (4 Pretendard weights) 등록"
      contains: "expo-splash-screen"
    - path: "apps/ios/app/index.tsx"
      provides: "NativeWind smoke screen — BUILD-02 시각 검증 surface"
      contains: "bg-brand-500"
    - path: "apps/ios/ios/"
      provides: "Expo prebuild로 생성된 native iOS project (Xcode workspace)"
    - path: "docs/SESSION-NOTES-2026-05-25.md"
      provides: "4시간 timebox + 빌드 과정 timeline (D-14)"
      contains: "iOS build"
  key_links:
    - from: "apps/ios/app.config.ts"
      to: "apps/ios/assets/icon.png + assets/splash.png + assets/fonts/*.otf"
      via: "Expo config plugin 등록"
      pattern: "(icon|splash|fonts)"
    - from: "apps/ios/app/index.tsx"
      to: "packages/ui-tokens/src/tailwind.ts (brand colors)"
      via: "NativeWind className → tailwind preset → ui-tokens colors"
      pattern: "bg-brand-500"
    - from: "Xcode device build / EAS build"
      to: "실기기 iPhone"
      via: "`expo run:ios --device` 또는 EAS QR install"
      pattern: "MOAJOA app on home screen"
---

<objective>
Phase 1의 통과 게이트 — iOS 앱을 실기기에서 빌드·실행하고, NativeWind className이 시각적으로 적용됨을 smoke screen으로 검증하며, app.config.ts에 icon/splash/font plugin을 등록한다. Path A(local prebuild) 우선, 4시간 안에 막히면 06-PLAN(EAS fallback)으로 전환하고 SESSION-NOTES에 timeline 기록.

Purpose: BUILD-01·BUILD-02·BUILD-03의 실기기 표시 부분 충족. D-13 (Splash + index.tsx 렌더 + NativeWind 시각 적용) 도달점.
Output: app.config.ts 수정, smoke screen jsx, 실기기 launch 스크린샷, SESSION-NOTES timeline 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-build-unblock-hygiene/01-CONTEXT.md
@.planning/phases/01-build-unblock-hygiene/01-RESEARCH.md
@.planning/phases/01-build-unblock-hygiene/01-PATTERNS.md
@.planning/phases/01-build-unblock-hygiene/01-UI-SPEC.md
@.planning/phases/01-build-unblock-hygiene/01-01-SUMMARY.md

<interfaces>
<!-- 현재 apps/ios/app.config.ts plugins (확인된 lines 22-28): -->
plugins: [
  'expo-router',
  'expo-font',
  // expo-share-intent will be re-added in Phase 1.5 with a SDK 54+ compatible
  // version. We're not invoking its JS APIs yet, and it requires a native
  // build (not Expo Go) anyway.
],

<!-- 현재 apps/ios/app/index.tsx (확인됨, 6-35 lines): auth state 확인 후 Redirect to /(tabs)/boards 또는 /login -->

<!-- 현재 apps/ios/app/_layout.tsx (확인됨, 10-25 lines): useState(ready) + supabase.auth.getSession 후 SafeAreaProvider + Stack -->

<!-- Pretendard PostScript names (RESEARCH §"font name reference") = file basename: -->
- 'Pretendard-Regular', 'Pretendard-Medium', 'Pretendard-SemiBold', 'Pretendard-Bold'

<!-- ui-tokens가 정의한 NativeWind className에 매핑되는 토큰 (이미 등록됨, 변경 X): -->
- `bg-brand-500` → `#F97316` (warm orange)
- `bg-brand-700` → `#C2410C` (deep orange)
- `text-brand-700` → `#C2410C`
- `bg-white` → `#FFFFFF`
- `text-neutral-600` → `#475569`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: app.config.ts — icon/splash/expo-font plugin 와이어업 + smoke screen 추가</name>
  <files>
    apps/ios/app.config.ts (MOD),
    apps/ios/app/index.tsx (MOD — smoke screen으로 일시 교체)
  </files>
  <action>
**1. apps/ios/app.config.ts 수정 (PATTERNS §1.3 + RESEARCH §"app.config.ts 변경")**

현재 root level에서 `version` 다음 (line 7 자리, `// icon/splash assets to be added in design pass` 코멘트를 삭제하고):

```typescript
  version: '0.1.0',
  orientation: 'portrait',
  icon: './assets/icon.png',          // 추가 — Wave 1 산출물
  scheme: 'moajoa',
```

`ios` 블록은 변경하지 않음 (bundleIdentifier, googleMapsApiKey, infoPlist 그대로).

`plugins` 배열을 다음으로 교체 (string `'expo-font'`를 tuple form으로 확장 + `'expo-splash-screen'` 신규 entry):

```typescript
  plugins: [
    'expo-router',
    [
      'expo-splash-screen',
      {
        image: './assets/splash.png',
        backgroundColor: '#FFFFFF',     // UI-SPEC §"Splash Composition" — D-07의 의도적 refinement
        resizeMode: 'contain',
        imageWidth: 234,                // UI-SPEC: 60% of iPhone 15's 390pt = 234pt
      },
    ],
    [
      'expo-font',
      {
        fonts: [
          './assets/fonts/Pretendard-Regular.otf',
          './assets/fonts/Pretendard-Medium.otf',
          './assets/fonts/Pretendard-SemiBold.otf',
          './assets/fonts/Pretendard-Bold.otf',
        ],
      },
    ],
    // expo-share-intent will be re-added in Phase 3 (NOT Phase 1.5 — D-16 보정)
    // with a SDK 54+ compatible version. We're not invoking its JS APIs yet,
    // and it requires a native build (not Expo Go) anyway.
  ],
```

⚠️ **D-16 lock:** `expo-share-intent` 절대 plugins에 추가 금지 (Pitfall F). 코멘트만 유지.
⚠️ **Pitfall A:** `apps/ios/babel.config.js` 절대 수정 금지 — reanimated/plugin이 worklets 처리를 이미 내장.

**2. apps/ios/app/index.tsx — NativeWind smoke screen 일시 교체 (D-13)**

D-13: "실기기 입증 도달점 = Splash + app/index.tsx 렌더 + NativeWind 시각 적용. login.tsx 인증 흐름은 Phase 3로 이관."

현재 index.tsx의 auth-gated redirect 로직은 Phase 3 SAVE-01에서 재구성됨. Phase 1 동안은 smoke screen으로 교체. **현재 파일 내용 전체를 다음으로 교체:**

```tsx
// apps/ios/app/index.tsx
// Phase 1 (BUILD-02) smoke screen. Replaced by auth-gated redirect in Phase 3 (SAVE-01).
// D-13: visual verification gate = splash + this screen + NativeWind className applied.
import { View, Text } from 'react-native';

export default function Index() {
  return (
    <View className="flex-1 items-center justify-center bg-brand-500">
      <View className="px-6 py-4 bg-white rounded-2xl shadow-lg">
        <Text className="text-2xl font-bold text-brand-700">NativeWind OK</Text>
        <Text className="text-sm text-neutral-600 mt-2">
          오렌지 화면 + 흰 카드 + 진한 오렌지 텍스트가 보이면 className 적용됨
        </Text>
      </View>
    </View>
  );
}
```

기존 import (useEffect, useRouter, supabase 등)는 모두 제거 — 본 smoke 버전은 외부 의존 0개.

⚠️ Karpathy §3.3 (Surgical): 기존 index.tsx 로직은 Phase 3 SAVE-01에서 정확히 어떤 형태로 복원될지가 그 phase의 결정. 본 Phase 1에서 "임시로 _smoke/ 라우트로 분리"하는 옵션도 있었으나 (PATTERNS §1.5 권장), D-13이 "app/index.tsx 렌더 확인"을 명시했으므로 index.tsx 자체를 smoke로 둠. Phase 3 진입 시 git history에서 기존 코드 복원 + auth 패턴 재적용.

**3. apps/ios/app/_layout.tsx 변경 X (RESEARCH 명시)**

RESEARCH §"iOS — expo-font config plugin 셋업": "`app/_layout.tsx`: 변경 불필요. expo-font config plugin은 자동으로 native register." useFonts hook 추가 불요.

다만 Assumption A1 (weight 자동 매칭) falsify되면 Task 3에서 Option B로 fallback (이 task에서는 손대지 않음).

  </action>
  <verify>
    <automated>grep -q "icon: './assets/icon.png'" apps/ios/app.config.ts &amp;&amp; grep -q "'expo-splash-screen'" apps/ios/app.config.ts &amp;&amp; grep -q "Pretendard-Regular.otf" apps/ios/app.config.ts &amp;&amp; grep -q "Pretendard-Bold.otf" apps/ios/app.config.ts &amp;&amp; ! grep -qE "['\"]expo-share-intent['\"]" apps/ios/app.config.ts &amp;&amp; grep -q "bg-brand-500" apps/ios/app/index.tsx &amp;&amp; grep -q "NativeWind OK" apps/ios/app/index.tsx &amp;&amp; pnpm --filter @moajoa/ios typecheck</automated>
  </verify>
  <done>
    - `app.config.ts`에 icon field + expo-splash-screen plugin + expo-font tuple form 추가됨
    - `expo-share-intent`가 plugins 배열에 안 들어감 (D-16 lock, Pitfall F)
    - `app/index.tsx`가 NativeWind smoke screen으로 교체됨
    - typecheck 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: Local prebuild + pod install (Path A 시도) + 4시간 timer 시작</name>
  <files>
    apps/ios/ios/ (generated by prebuild),
    docs/SESSION-NOTES-2026-05-25.md (NEW or APPEND)
  </files>
  <action>
**시작 시 SESSION-NOTES 헤더 작성 (D-14 timeline 시작):**

```bash
# docs/SESSION-NOTES-2026-05-25.md에 다음 section 추가 (파일 없으면 신규 생성):
cat >> docs/SESSION-NOTES-2026-05-25.md <<'EOF'

## Phase 1 — iOS Build Path A 시도 (BUILD-01)

- 시작 시각: $(date '+%H:%M')
- Path A 시도: local prebuild + pod install + expo run:ios --device
- 4시간 timebox 종료 예상: (시작 + 4h)

### 시도 timeline
EOF
```

이후 각 단계 시각을 append 형태로 기록.

**1. Pretendard 설치 (Wave 1에서 했으나 lock 확인)**

```bash
# Wave 1 (01-PLAN)에서 이미 완료. 단순 sanity check:
test -d node_modules/pretendard/dist/public/static
test -d node_modules/react-native-worklets
test -d node_modules/sharp
```

**2. Expo prebuild — apps/ios/ios/ 디렉토리 생성**

```bash
cd apps/ios
npx expo prebuild --platform ios --clean
```

`--clean`은 기존 `ios/` 폴더가 있으면 삭제 후 재생성. 처음 실행이라면 신규 생성. RESEARCH §"명령 흐름" Step 5.

**예상 결과:**
- `apps/ios/ios/MOAJOA.xcworkspace`, `MOAJOA/`, `Podfile` 생성
- 자동으로 pod install 호출 (Expo SDK 54 default)
- 성공 시 "Pod installation complete!" 출력

**막힘 신호 (RESEARCH §"4-Hour Timebox Heuristic"):**
- `pod install`이 2회 연속 같은 podspec 에러 → SESSION-NOTES에 기록 + 4h 카운트
- `npx expo prebuild`가 예상치 못한 파일 변경 → 같이 기록
- Metro bundler가 @moajoa/* workspace 패키지 못 찾음 → hoist 적용 후 재시도

**3. (필요 시) pod install 수동 재시도**

prebuild가 자동으로 안 했거나 실패했을 때:

```bash
cd apps/ios/ios
pod install
cd ../..
```

각 시도 시각을 SESSION-NOTES에 추가:
```
- HH:MM: npx expo prebuild --platform ios --clean → success / blocked (error: ...)
- HH:MM: pod install → success / blocked
```

**4. .gitignore 정책 확인 (RESEARCH Open Question #1)**

```bash
cat apps/ios/.gitignore | grep -i "ios" || true
```

권장: `ios/` ignore 유지 (prebuild 매 머신 재실행). 본 task는 ios/ 디렉토리 commit하지 않음. 만약 commit 정책이 필요해지면 (Phase 3 Share Extension native patch) 그때 결정.

**5. 4시간 timebox 체크포인트**

각 시도 후 누적 경과 시간 계산. 4시간 초과 + 빌드 미통과 시:
- 본 task abort
- SESSION-NOTES에 "Path A 4h 도달 — Path B 전환" 기록
- 06-PLAN(EAS fallback) 실행

본 task는 path A가 끝나는 시점에 끝남 — 성공이든 timeout이든.

  </action>
  <verify>
    <automated>test -f docs/SESSION-NOTES-2026-05-25.md &amp;&amp; grep -q "iOS Build Path A" docs/SESSION-NOTES-2026-05-25.md &amp;&amp; ( test -d apps/ios/ios/MOAJOA.xcworkspace || ( echo "Path A blocked — proceed to 06-PLAN EAS fallback" &amp;&amp; grep -q "Path A.*blocked\|Path B" docs/SESSION-NOTES-2026-05-25.md ) )</automated>
  </verify>
  <done>
    - SESSION-NOTES 파일 존재 + 시작 시각·시도 timeline 기록
    - 둘 중 하나:
      (a) `apps/ios/ios/MOAJOA.xcworkspace` 존재 (Path A 성공) → Task 3로
      (b) Path A 4h 막힘 기록 → 본 plan 일시 정지, 06-PLAN 실행 후 복귀
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 실기기 install + smoke screen 시각 검증 + SESSION-NOTES 완료 기록</name>
  <what-built>
    - app.config.ts에 icon/splash/font plugin 와이어업
    - app/index.tsx NativeWind smoke screen
    - apps/ios/ios/ Xcode workspace (path A 성공 시)
    - SESSION-NOTES timeline
  </what-built>
  <how-to-verify>
    **A. 실기기에 install (D-04):**

    **Path A (prebuild 성공 시):**
    ```bash
    cd apps/ios
    npx expo run:ios --device
    # 대화형 device 목록에서 USB 연결된 iPhone 선택 → 빌드 후 자동 install
    ```
    또는 Xcode에서 `apps/ios/ios/MOAJOA.xcworkspace` 열기 → device 선택 → Play.

    첫 install 시: iPhone 잠금 해제 + "이 컴퓨터 신뢰" + Apple ID Developer Certificate (Xcode `Signing & Capabilities`).

    **Path B (06-PLAN EAS fallback 완료 시):**
    EAS QR 코드 또는 Expo Orbit으로 install (06-PLAN Task 4 참조). 본 task는 install 후 시각 검증만.

    **B. 실기기 시각 체크 (BUILD-01·02·03):**

    1. 홈 화면에 MOAJOA app 아이콘 확인 → **흰 배경 + brand orange 모양** (현재는 M-shape placeholder). 기본 Expo 흰 아이콘이면 BUILD-03 실패.

    2. 앱 tap → cold launch splash 화면 확인 → **흰 배경 + 중앙 brand orange 워드마크** (현재 placeholder는 6개 사각형). 기본 Expo splash가 보이면 BUILD-03 실패. ~1초 표시 후 main view.

    3. Main view에 NativeWind smoke screen 확인 (BUILD-02):
       - 전체 배경: brand orange (`#F97316`)
       - 중앙: 흰색 카드, 라운드 코너 + 그림자
       - 큰 텍스트 "NativeWind OK": 진한 orange (`#C2410C`), bold
       - 작은 텍스트 "오렌지 화면 + 흰 카드 ...": 회색 (`#475569`)
       - **Pretendard 폰트로 렌더링** (한글 잘림/네모 없음)

    Silent failure 진단 (Pitfall A):
    - 화면이 흰색이거나 기본 배경 → className 무시됨 → BUILD-02 FAIL
    - 텍스트만 보이고 카드 스타일 없음 → babel preset 누락
    - HMR 변경 안 받음 → `npx expo start -c` (Metro cache 초기화)

    **C. 스크린샷 저장 + SESSION-NOTES 마무리:**

    iPhone 스크린샷 1장 → `docs/screenshots/2026-05-25-phase1-smoke.png`로 저장.

    SESSION-NOTES에 append:
    ```
    - HH:MM: Path A/B 빌드 성공
    - HH:MM: 실기기 install + launch 성공
    - 시각 검증 결과: BUILD-01 ✓ / BUILD-02 ✓ / BUILD-03 ✓ (스크린샷: docs/screenshots/...)
    ```

    **D. Pretendard weight matching 검증 (Assumption A1):**

    smoke screen의 "NativeWind OK" 텍스트가 정상 bold로 보이면 PostScript name 매칭 OK. 만약 regular weight로 보이면 (font-bold className이 무시되면) — `_layout.tsx`에 useFonts hook 추가 (Option B, PATTERNS §1.4) 후 재빌드 필요. 이 경우 본 task 일시 정지 → useFonts hook 추가 task로 분기.

  </how-to-verify>
  <resume-signal>
    "approved" — 실기기 launch 성공 + smoke screen 시각 OK + 스크린샷 저장
    OR
    "smoke-failed" — className 미적용 또는 빌드 통과했지만 시각 실패. 추가 진단 필요 (구체 증상 명시)
    OR
    "switch-to-eas" — Path A 4h 막힘, 06-PLAN으로 전환
    OR
    "font-fallback-needed" — bold weight 매칭 실패, _layout.tsx에 useFonts hook 추가 task 분기
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → iPhone | Apple Developer signing (Personal Team or full account) — 본 phase는 본인 dogfooding이므로 Personal Team OK |
| Expo SDK 54 native autolinking → pnpm node_modules tree | hoisted scope 한정 (apps/ios/.npmrc) — pnpm symlink chain 회피 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | NativeWind silent failure (className 시각 무시) | mitigate | Wave 1에서 nativewind 4.2.4 upgrade + react-native-worklets peer install. babel.config.js 절대 수정 금지 (Pitfall A). Task 3 smoke screen 시각 검증 |
| T-02-02 | Denial of Service | 4시간 timebox 초과 후에도 시도 계속 | mitigate | D-01 lock: SESSION-NOTES 시각 기록 + 4h 초과 시 즉시 06-PLAN(EAS) 전환 |
| T-02-03 | Elevation of Privilege | iOS 빌드용 hoist 적용이 web에 새어 들어감 | mitigate | apps/ios/.npmrc 한 파일에 한정 (D-02). 본 task에서 .npmrc 추가 변경 금지 |
| T-02-04 | Information Disclosure | docs/SESSION-NOTES-*.md에 Apple ID / certificate 정보 포함 | mitigate | SESSION-NOTES에는 시각 timeline + decision만, secrets 미포함. .env.local 절대 commit 금지 (CLAUDE.md §5) |
| T-02-05 | Repudiation | Phase 3에서 index.tsx 복원 못 함 | mitigate | git history에 기존 index.tsx 코드 보존됨. Phase 3 SAVE-01 진입 시 git show <hash>:apps/ios/app/index.tsx로 복원 reference |
</threat_model>

<verification>
**V3 (RESEARCH §"V1..V8"): iOS 빌드 성공**
- Path A: `pod install` 완료 + `npx expo run:ios --device` 실기기 install 성공
- Path B: 06-PLAN 통과 후 EAS dashboard에 "finished" + .ipa install 성공

**V4: 실기기 NativeWind smoke screen 시각 확인**
- 스크린샷 `docs/screenshots/2026-05-25-phase1-smoke.png` 존재
- 시각: orange 배경 + 흰 카드 + brand-700 bold + neutral-600 한글

**Pitfall A (silent failure): Task 3에서 시각 검증으로 직접 진단**
**Pitfall G (lockfile): Wave 1에서 이미 freeze됨, 본 wave에서 추가 변경 없음**

**SESSION-NOTES 완성도 (D-14, BUILD-01 success criterion #3):**
- 시작 시각, 시도 timeline (시간 순), 결과 (성공 또는 4h 도달 + B 전환), 실기기 install 시각, 스크린샷 link
</verification>

<success_criteria>
- BUILD-01: 실기기에서 앱 launch 성공
- BUILD-02: NativeWind className 시각 적용 (orange bg + 흰 카드 등)
- BUILD-03 (절반): icon + splash + Pretendard 폰트가 native bundle에 embed되어 실기기에 표시. 자산 파일 존재는 01-PLAN, 와이어업은 본 plan.
- D-13 통과: Splash + index.tsx + NativeWind 적용 모두 시각 확인됨
- D-14: SESSION-NOTES에 timeline 기록 완료 (특히 path B 전환 시 timestamp + 사유)
</success_criteria>

<output>
완료 후 `.planning/phases/01-build-unblock-hygiene/01-02-SUMMARY.md` 생성. SUMMARY에:
- 빌드 경로 (A 또는 B), 총 소요 시간, 4h timebox 도달 여부
- 실기기 모델 + iOS 버전
- smoke screen 스크린샷 path
- Pretendard weight matching 결과 (assumption A1)
- 발견된 신규 pitfall (있다면 .planning/research/PITFALLS.md 후보)
- (Path B로 갔다면) EAS projectId, 첫 빌드 시간, EAS dashboard URL
</output>
