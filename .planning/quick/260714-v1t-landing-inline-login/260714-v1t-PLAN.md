---
phase: quick-260714-v1t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/social-auth-buttons.tsx
  - apps/web/app/login/page.tsx
  - apps/web/app/_components/landing-carousel.tsx
  - apps/web/__tests__/social-auth-buttons.test.tsx
  - apps/web/__tests__/landing-carousel.test.tsx
autonomous: true
requirements: [QUICK-260714-v1t]

must_haves:
  truths:
    - "랜딩 3번 슬라이드에서 카카오/구글/애플로 바로 로그인할 수 있다 (콜백 후 /moa 착지)"
    - "시작하기를 누르면 /login 으로 나가지 않고 3번 슬라이드로 스크롤한다"
    - "3번 슬라이드에 도달하면 시작하기는 사라지고 도트는 그대로 남는다"
    - "이메일 로그인이 필요한 사람은 3번 슬라이드의 작은 텍스트 링크로 /login 에 갈 수 있다"
    - "/login 페이지의 소셜 3종은 이전과 똑같이 동작한다 (?next= 보존 포함)"
    - "390×844 에서 3번 슬라이드 콘텐츠가 잘리지 않고, 모든 텍스트가 AA 를 통과한다"
  artifacts:
    - apps/web/components/social-auth-buttons.tsx
    - apps/web/__tests__/social-auth-buttons.test.tsx
  key_links:
    - "login/page.tsx → SocialAuthButtons 를 배럴(@/components)이 아닌 경로(@/components/social-auth-buttons)로 import — 배럴 경유 시 login.test.tsx 의 vi.mock('@/components') 가 컴포넌트를 undefined 로 만들어 테스트가 죽는다"
    - "landing-carousel → useToast (layout.tsx ToastProvider 범위 안임을 확인함)"
    - "landing oauth() → redirectTo = {base}/auth/callback (쿼리 없음) → callback route.ts:35 기본값 /moa"
---

<objective>
랜딩 캐러셀 3번째 슬라이드에 소셜 로그인 3종을 인라인으로 넣고, `시작하기` CTA 를 /login 네비게이션 대신 3번 슬라이드로의 스크롤로 바꾼다.

Purpose: 랜딩에서 페이지 이동 없이 로그인까지 도달하는 경로를 **추가**한다 (대체 아님).
Output: 공유 소셜 버튼 컴포넌트 1개 + 캐러셀 3번 슬라이드 인라인 로그인 + 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@apps/web/app/_components/landing-carousel.tsx
@apps/web/app/login/page.tsx
@apps/web/__tests__/login.test.tsx
@apps/web/__tests__/landing-carousel.test.tsx
@apps/web/components/toast.tsx
@apps/web/components/button.tsx
</context>

<research_facts>
계획 단계에서 코드를 직접 읽고 확인한 사실. 실행 중 재조사 불필요.

**F1 — 배럴 함정 (가장 중요).** `login.test.tsx:17` 이 `vi.mock('@/components', () => ({ Button, Input, useToast }))` 로 배럴 전체를 대체한다. 새 소셜 컴포넌트를 `components/index.ts` 에 export 하고 login/page.tsx 가 배럴에서 import 하면, 테스트에서 그 컴포넌트는 `undefined` 가 되어 렌더가 터진다 → "login.test.tsx 무수정 통과" 계약 위반. 따라서 **경로 직접 import** (`@/components/social-auth-buttons`) 를 쓴다. 이건 예외적 편법이 아니라 기존 관례다: `components/bottom-nav.tsx` 는 배럴에 없고 `layout.tsx:5` 가 경로로 직접 import 한다. 배럴에 추가하지 말 것.

**F2 — 토스트 범위.** `layout.tsx:41` 의 `<ToastProvider>` 가 `{children}` 을 감싸고, `app/page.tsx` 는 `<LandingCarousel />` 를 children 으로 렌더한다 → **런타임에서 캐러셀은 ToastProvider 안에 있다.** 따라서 /login 과 같은 `useToast()` 패턴을 쓸 수 있다. 단 `toast.tsx:39` 의 `useToast()` 는 provider 밖에서 **throw** 하므로, `<LandingCarousel />` 를 맨몸으로 렌더하는 `landing-carousel.test.tsx` 는 `@/components` 를 mock 해야 한다 (login.test.tsx 와 동일 패턴).

**F3 — 기존 랜딩 테스트 2개는 잠금 결정과 정면 충돌한다** (의도된 변경, 회귀 아님):
- `'exposes three accessible dots…'` → `getAllByRole('button')` 이 정확히 3개를 기대. 소셜 3종 + 시작하기(버튼화) 가 생기면 7개.
- `'points the 시작하기 CTA at /login'` → CTA 가 더 이상 링크가 아니다.
이 둘은 재작성한다. **`login.test.tsx` 는 한 글자도 건드리지 않는다.**

**F4 — 스크림 대비 모델 (검증됨).** 검정 스크림 알파 a 를 사진 위에 얹으면 합성 휘도 ≈ (1−a)^2.2 · L_photo. travel-photo 최악부(흰 책상) L≈0.83, a=0.55 → L=0.143 → 흰 텍스트 대비 **5.43:1** — 파일 주석의 실측치 5.42:1 과 일치하므로 이 모델을 신뢰해도 된다. 이 모델로 계산한 임계값:
- a=0.55 → 5.42:1 ✅ (목표)
- a=0.50 → 4.55:1 ✅ (AA 일반 텍스트 하한 4.5 를 겨우 통과)
- a=0.45 → 3.85:1 ❌ (일반 텍스트 실패)
스크림 그라디언트는 0%에서 0.35, 28%에서 0.55 로 선형 상승한다 → a ≥ 0.50 이 되는 지점은 **뷰포트 높이의 21% 지점**. 즉 **모든 텍스트의 최상단(워드마크)은 y ≥ 21%vh 에 있어야 하고, 28%vh 이상이면 기존 5.42:1 근거를 그대로 물려받는다.**
- 위험 방향은 아래가 아니라 **위**다. 슬라이드는 `justify-center` 라서 콘텐츠가 길어지면 워드마크가 **위로** 올라가 밝은(0.35) 구간으로 진입한다. 아래쪽(72%→0.78)은 더 어두워지므로 안전하다.
- banana-100(#fcf2ba, L=0.8775) 를 a=0.55 밴드에서 쓰면 4.81:1 로 AA 는 통과한다. 그래도 /login 진입 링크는 **흰색 + underline** 을 쓴다: 랜딩 카피가 전부 흰색이고 banana 는 랜딩 어디에도 없다 → 토큰 하나 덜 끌어오는 쪽이 단순하다(§3.2). (banana-100 도 유효하다는 계산은 남겨둠 — 디자인상 필요하면 근거 있음.)
- 애플 버튼은 검정 원이라 사진 위에서 **경계 대비(WCAG 1.4.11, 3:1)** 가 깨질 수 있다 (밝은 부분 위: 3.86:1 통과 / 어두운 부분 위: 1.2:1 실패). → 랜딩에서만 세 버튼에 흰 링을 얹는다 (아래 T1 의 `buttonClassName`).

**F5 — 레이아웃 예산 (390×844).** 콘텐츠 추정 ≈ 워드마크 40 + mt-5 20 + h1 2줄 75 + 로그인 블록 ~140 = **~285px**. 3번 슬라이드는 CTA 가 숨겨져 하단 chrome 이 도트만 남으므로 `pb-44`(176px) 를 줄일 수 있다. `pb-28`(112px) 기준: 박스 732 → 콘텐츠 top = (732−285)/2 = **223px = 26.4%vh → a≈0.539 → ~5.2:1** ✅, 콘텐츠 bottom = 508px 로 도트(≈760px)와 충분히 이격 → 잘림 없음. 1440×900 도 같은 계산으로 top ≈ 27.9% ✅. 이 수치는 추정이므로 T3 에서 실측으로 확정한다.

**F6 — 랜딩 OAuth 는 ?next= 를 붙이지 않는다.** callback route.ts:35 가 next 없으면 `/moa` 로 보낸다 = 원하는 동작. /login 의 `callbackUrl()` 은 window.location.search 에서 next 를 읽으므로 랜딩(`/`)에서 재사용해도 결과는 같지만, 랜딩은 next 를 **구조적으로 못 붙이는** 단순 함수를 따로 쓴다 (오픈리다이렉트 표면 축소, T-v1t-02).
</research_facts>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 소셜 3종 버튼을 공유 컴포넌트로 추출 (/login 동작·DOM 불변)</name>
  <files>apps/web/components/social-auth-buttons.tsx, apps/web/app/login/page.tsx, apps/web/__tests__/social-auth-buttons.test.tsx</files>
  <behavior>
    - 세 버튼의 접근성 이름은 정확히 `카카오로 시작하기`, `Google로 계속하기`, `Apple로 계속하기` (login.test.tsx 계약).
    - 각 버튼 클릭 → onProvider 가 각각 'kakao' | 'google' | 'apple' 로 정확히 1회 호출.
    - buttonClassName 을 주면 세 버튼 모두에 병합된다.
  </behavior>
  <action>
    `apps/web/components/social-auth-buttons.tsx` 를 새로 만든다. 'use client'. named export `SocialAuthButtons`.

    Props: `onProvider: (provider: 'kakao' | 'google' | 'apple') => void` 와 선택적 `buttonClassName?: string`.
    - onProvider 를 주입받는 이유: /login 은 ?next= 를 실어나르는 callbackUrl() 을 쓰고 랜딩은 쿼리 없는 콜백을 쓴다 (F6). 두 호출자의 redirectTo 가 구조적으로 다르므로 핸들러 주입이 맞다. supabase 클라이언트를 컴포넌트 안으로 넣지 말 것 — 넣는 순간 login.test.tsx 의 supabase mock 경계와 얽힌다.
    - buttonClassName 이 필요한 이유: 애플 버튼은 검정 원이라 사진 배경 위에서 경계 대비 3:1 이 깨질 수 있다 (F4). 랜딩만 흰 링을 얹는다. 투기적 설정 아님 — 두 번째 호출자가 접근성 때문에 실제로 요구하는 값이다.

    내용물은 login/page.tsx 의 `socialBlock` 에서 **구분선 + 원형 버튼 3개까지만** 옮긴다:
    - `간편 로그인` 구분선 (`h-px flex-1 bg-white/35` 양옆 + `text-xs text-white` 라벨)
    - 카카오(bg-[#FEE500]) / 구글(bg-white, 4색 공식 로고) / 애플(bg-black) 원형 버튼 3개. size-[52px], rounded-full, shadow-lg, focus-visible outline 까지 **현재 클래스 문자열 그대로** 복사한다. 공식 SVG path 는 그대로 옮긴다 (재작성 금지).
    - `계정이 없나요? 회원가입` 줄은 **옮기지 않는다** — signUp/pending/email/password 를 클로저로 잡는 /login 전용 로직이다. login/page.tsx 의 socialBlock 안, `<SocialAuthButtons/>` 바로 아래에 그대로 남긴다.

    클래스 병합은 이 레포가 이미 쓰는 유틸(button.tsx 가 import 하는 cn/clsx+tailwind-merge)을 그대로 쓴다. 새 유틸을 만들지 말 것.

    `login/page.tsx` 수정:
    - `import { SocialAuthButtons } from '@/components/social-auth-buttons';` — **경로 직접 import. `@/components` 배럴을 쓰지 말고, `components/index.ts` 에 export 도 추가하지 말 것** (F1: 배럴에 넣고 배럴로 import 하면 login.test.tsx 의 vi.mock 이 컴포넌트를 undefined 로 만든다). `bottom-nav.tsx` 가 이미 같은 방식이다.
    - socialBlock 안의 구분선+버튼 3개 JSX 를 `<SocialAuthButtons onProvider={oauth} />` 로 치환. buttonClassName 은 주지 않는다 → /login 의 렌더 결과는 이전과 동일해야 한다.
    - oauth(), callbackUrl(), signUp, 나머지 폼은 손대지 않는다 (§3.3).

    `apps/web/__tests__/social-auth-buttons.test.tsx` 를 새로 만든다 (mock 불필요 — 순수 프레젠테이션):
    - 세 버튼의 접근성 이름 3종 존재.
    - 각각 클릭 시 onProvider 가 'kakao'/'google'/'apple' 로 1회 호출.
    - buttonClassName 이 세 버튼 모두에 적용됨.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run 2>&1 | tail -20</automated>
    <automated>cd apps/web && pnpm typecheck</automated>
    <automated>cd /Users/test/Desktop/moajoa && git diff --exit-code -- apps/web/__tests__/login.test.tsx && echo "login.test.tsx UNMODIFIED (계약 OK)"</automated>
    <automated>cd /Users/test/Desktop/moajoa && grep -v '^#' apps/web/components/index.ts | grep -c social-auth-buttons | grep -qx 0 && echo "배럴 미오염 OK"</automated>
  </verify>
  <done>login.test.tsx 3개가 **무수정으로** 통과(실제 SocialAuthButtons 를 렌더하면서). social-auth-buttons.test.tsx 신규 통과. 배럴(index.ts) 에 social-auth-buttons 항목 없음. typecheck 통과.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 3번 슬라이드 인라인 로그인 + 시작하기 → goTo(2)</name>
  <files>apps/web/app/_components/landing-carousel.tsx, apps/web/__tests__/landing-carousel.test.tsx</files>
  <behavior>
    - `시작하기` 클릭 → scrollTo({ left: 2 × clientWidth, behavior: 'smooth' }) 1회. /login 으로 네비게이트하지 않는다.
    - index === 2 일 때 `시작하기` 는 DOM 에서 사라지고, 도트 3개는 남는다.
    - 소셜 3종은 3번 슬라이드에만 (문서 전체에서 각 1개씩) 렌더된다.
    - 랜딩 카카오 클릭 → signInWithOAuth({ provider: 'kakao', options.redirectTo 가 '/auth/callback' 을 포함하고 next 쿼리를 포함하지 않음 }).
    - signInWithOAuth 가 error 를 반환 → toast(message, { variant: 'error' }).
    - 3번 슬라이드에 href="/login" 텍스트 링크가 있다.
  </behavior>
  <action>
    `landing-carousel.tsx` 수정. 잠금 결정 3개(소셜 3종만·기존 카피 유지·CTA 는 스크롤)를 그대로 구현한다.

    1) OAuth. `getSupabaseBrowser` 를 import 하고, 랜딩 전용 콜백 URL 함수를 만든다: base = `process.env.NEXT_PUBLIC_APP_URL ?? window.location.origin`, 반환 `${base}/auth/callback` — **쿼리 문자열을 붙이는 경로가 아예 없어야 한다** (F6). oauth(provider) 는 /login 과 동일하게 `signInWithOAuth({ provider, options: { redirectTo } })` 를 부르고, error 가 오면(= 브라우저가 안 떠난 경우) `useToast()` 의 toast(error.message, { variant: 'error' }) 로 노출한다. 조용히 삼키지 말 것. 성공 시엔 브라우저가 떠나므로 후처리 없음. 랜딩에 새 에러 UI(배너/인라인 텍스트)를 만들지 말 것 — 콜백 실패는 기존대로 /login?error= 에서 표시된다.
       - useToast 는 `@/components` 배럴에서 import 해도 안전하다 (F2: 런타임에 ToastProvider 안이고, 랜딩 테스트는 이 배럴을 mock 한다). SocialAuthButtons 만 경로 직접 import.

    2) 3번 슬라이드 본문. `i === 2` 일 때 기존 워드마크 + h1 **아래에** 로그인 블록을 붙인다: `<SocialAuthButtons onProvider={oauth} buttonClassName="ring-2 ring-white/80" />` + 그 아래 `<Link href="/login">` 텍스트 링크(예: `이메일로 로그인`). 링크는 **흰색 + underline + text-xs~[13px]** (F4: banana-100 은 4.81:1 로 통과하지만 랜딩 팔레트에 banana 가 없어 흰색이 단순하다). 워드마크·h1 카피·object-position 은 건드리지 않는다.
       - ring 을 얹는 이유는 F4 (검정 애플 원의 1.4.11 경계 대비). 주석으로 한 줄 남긴다.

    3) 세로 여백. 3번 슬라이드는 CTA 가 숨겨져 하단 chrome 에 도트만 남는다 → 슬라이드 컨테이너의 `pb-44` 를 3번만 줄인다(예: `pb-28`). 목적은 두 가지: (a) 늘어난 콘텐츠가 `h-[100svh] overflow-hidden` 안에서 잘리지 않게 하고, (b) `justify-center` 때문에 워드마크가 위(밝은 0.35 구간)로 밀려 올라가는 걸 상쇄한다.
       - **경계 조건(F4):** 워드마크 상단이 `y ≥ 0.28 × vh` 면 기존 5.42:1 근거를 그대로 유지한다. `y < 0.21 × vh` 면 AA 실패다. 최종 padding 값은 T3 의 실측 boundingBox 로 확정한다.
       - `SCRIM` 상수와 슬라이드 1·2 는 **건드리지 말 것.** 만약 실측에서 3번 슬라이드가 21~28% 구간에 걸쳐 여유가 불안하면, `SCRIM` 을 수정하는 대신 3번 전용 상수를 추가해 **어둡게만** 조정하고(예: 상단 알파 상향), 주석에 새 실측치를 적는다. 밝게 하는 방향은 금지.
       - 어느 쪽이든 파일 상단 SCRIM 주석의 수치가 사실과 어긋나면 주석을 갱신한다.

    4) CTA. `<Link href="/login">시작하기</Link>` 를 `<button type="button" onClick={() => goTo(2)}>` 로 바꾼다 (기존 클래스 유지). `index !== 2` 일 때만 렌더하되, **CTA 를 감싸는 래퍼는 고정 높이를 유지**해서 3번 슬라이드에서 도트가 아래로 튀지 않게 한다 (CTA 는 도트 아래에 있고 chrome 이 bottom 앵커라, 그냥 제거하면 도트가 ~50px 내려앉는다). 래퍼에 CTA 실제 높이(py-3.5 + text-base ≈ 52px)만큼의 min-height 를 주고 안쪽만 조건부 렌더. 정확한 값은 T3 에서 도트 y좌표가 슬라이드 1과 3에서 동일한지로 검증한다.
       - 도트는 항상 3개 다 보인다. goTo/onScroll 인덱스 추적 로직은 그대로 둔다.
       - 슬라이드 안의 버튼 클릭이 스크롤 스냅을 깨지 않는지 T3 에서 확인한다.

    5) 트랙 div 에 `data-testid="carousel-track"` 를 붙인다 (jsdom 에서 scrollLeft 를 주입해 index 2 상태를 재현하려면 트랙 핸들이 필요).

    `landing-carousel.test.tsx` 수정 (F3: 기존 2개는 의도적 재작성):
    - 상단에 mock 추가: `vi.mock('@/lib/supabase/browser', ...)` (signInWithOAuth), `vi.mock('@/components', () => ({ useToast: () => ({ toast }) }))`. SocialAuthButtons 는 경로 import 라 mock 되지 않고 진짜가 렌더된다 — 그게 목적이다.
    - `'exposes three accessible dots…'`: `getAllByRole('button')` → 도트만 스코프하도록 `getAllByRole('button', { name: /^슬라이드 / })` 로 좁힌다. scrollTo 단언은 유지.
    - `'points the 시작하기 CTA at /login'` → 삭제하고 `시작하기` 클릭이 `scrollTo({ left: 2 * SLIDE_WIDTH, behavior: 'smooth' })` 를 부르는 테스트로 대체.
    - 신규: index 2 에서 CTA 소멸 — 트랙에 `Object.defineProperty(track, 'scrollLeft', { configurable: true, value: 2 * SLIDE_WIDTH })` 후 `fireEvent.scroll(track)` (jsdom 은 scrollLeft 대입을 무시하므로 defineProperty 필수) → `queryByRole('button', { name: '시작하기' })` 가 null, 도트는 여전히 3개.
    - 신규: 소셜 3종이 문서에 각 1개씩(3번 슬라이드에만) 렌더 — getByRole 로 단수 조회.
    - 신규: 카카오 클릭 → signInWithOAuth 가 provider 'kakao' + redirectTo 에 '/auth/callback' 포함 + next 쿼리 **미포함** 으로 1회 호출.
    - 신규: signInWithOAuth 가 error 반환 → toast(…, { variant: 'error' }).
    - 신규: href="/login" 텍스트 링크 존재.
    - 기존 이미지/카피/priority/alt 테스트 6개는 그대로 통과해야 한다.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run 2>&1 | tail -20</automated>
    <automated>cd apps/web && pnpm typecheck</automated>
    <automated>cd apps/web && pnpm build 2>&1 | tail -15</automated>
  </verify>
  <done>vitest 전체 통과 — 기존 303개 중 landing-carousel 의 의도적 재작성 2건 외 **회귀 0**, login.test.tsx 여전히 무수정 통과, 신규 테스트 전부 그린. typecheck + next build 통과.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright 라이브 검증 (데스크톱 1440 + 모바일 390)</name>
  <files>(검증 전용 — 스크립트는 scratchpad, 소스 변경은 T2 의 여백/스크림 미세조정만)</files>
  <action>
    `pnpm --filter @moajoa/web dev` 를 새 포트(예: 3001)로 띄우고, scratchpad 에 playwright-core 스크립트를 써서 아래를 실측한다 (기존 메모리의 2-browser UAT 패턴 재사용: Chrome for Testing executablePath + scratchpad 의 playwright-core). 스크립트/스크린샷은 레포에 커밋하지 않는다.

    데스크톱 1440×900 과 모바일 390×844 **양쪽에서**:
    1. `시작하기` 클릭 → 스크롤 정착 후 `track.scrollLeft === 2 * track.clientWidth`. URL 이 `/` 그대로일 것 (네비게이션 없음).
    2. 3번 슬라이드에서 `시작하기` 가 DOM 에 없고, 도트 3개는 보인다. **도트 컨테이너의 boundingBox.y 가 슬라이드 1일 때와 동일**(±1px) — 다르면 T2(4) 의 고정 높이 래퍼를 조정한다.
    3. **오버플로우 실측:** 워드마크·h1·소셜 버튼·텍스트 링크의 boundingBox 를 전부 수집 → (a) 모두 뷰포트 안(0 ≤ y, y+h ≤ vh), (b) 텍스트 링크 하단 < 도트 상단, (c) **워드마크 y / vh ≥ 0.28** (기존 5.42:1 근거 승계). 0.21~0.28 이면 통과이긴 하나 여유가 없으므로 T2(3) 의 padding 을 조정해 0.28 이상으로 올린다. **< 0.21 이면 AA 실패 — 반드시 고친다.** 최종 수치를 landing-carousel.tsx 주석에 실측치로 기록한다.
    4. 스크린샷 2장(1440/390 의 3번 슬라이드) 을 눈으로 확인 — 잘림·겹침 없음, 애플 검정 버튼이 사진 위에서 식별 가능(흰 링).
    5. **OAuth 인터셉트:** `**/auth/v1/authorize*` 를 route.abort() 로 가로채고 카카오/구글/애플을 각각 클릭 → 요청 URL 의 `provider` 가 각각 kakao/google/apple, `redirect_to` 가 `/auth/callback` 을 포함, **`next` 파라미터가 없음** 을 단언.
    6. **/login 회귀:** `/login` 으로 이동해 소셜 3종을 같은 방식으로 인터셉트 → provider 3종이 여전히 정상, redirect_to 에 `/auth/callback` 포함. (컴포넌트 추출 회귀 확인.)
    7. 전 과정 console error 0 (page.on('console') 수집).

    실측 결과로 T2 의 padding/스크림을 조정했다면 T2 의 automated verify(test:run·typecheck·build)를 다시 돌려 그린을 재확인한다.
  </action>
  <verify>
    <automated>cd apps/web && pnpm test:run 2>&1 | tail -5</automated>
    <automated>cd /Users/test/Desktop/moajoa && test -z "$(git status --porcelain apps/ios packages/ui-tokens)" && echo "apps/ios + ui-tokens CLEAN"</automated>
    <human-check>Playwright 리포트: 1440/390 각각 (1) scrollLeft==2×clientWidth (2) 3번서 CTA 없음·도트 y 불변 (3) 워드마크 y/vh 실측값 (4) provider 3종 + next 파라미터 부재 (5) /login 3종 정상 (6) console error 0 — 전부 PASS</human-check>
  </verify>
  <done>양 뷰포트에서 7개 항목 전부 PASS. 콘텐츠 잘림 없음. 워드마크 y/vh ≥ 0.28 (또는 스크림 보강 후 근거 기록). apps/ios·packages/ui-tokens diff 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 랜딩(비인증 브라우저) → Supabase OAuth | 사용자 제어 URL/쿼리가 redirectTo 로 흘러갈 수 있는 지점 |
| OAuth 제공자 → /auth/callback | 외부에서 돌아오는 code/error 를 서버가 교환 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v1t-01 | Tampering | 랜딩 oauth() redirectTo | medium | mitigate | redirectTo 를 `${NEXT_PUBLIC_APP_URL ?? origin}/auth/callback` 상수 조립으로만 만든다. 쿼리를 붙이는 코드 경로 자체를 두지 않음 (T2-1) |
| T-v1t-02 | Elevation of Privilege | 오픈 리다이렉트 (?next=) | medium | mitigate | 랜딩은 next 를 읽지도 쓰지도 않는다. callback route.ts:34 의 기존 상대경로 검증(`//` 차단)은 그대로 유효 — 변경 금지 |
| T-v1t-03 | Information Disclosure | OAuth 에러 메시지 토스트 | low | accept | /login 과 동일한 메시지 표면. 새 정보 노출 없음 |
| T-v1t-SC | Tampering | 패키지 설치 | low | accept | 신규 의존성 0 — 설치 태스크 없음 |
</threat_model>

<verification>
1. `pnpm --filter @moajoa/web test:run` — 회귀 0. login.test.tsx 무수정 통과.
2. `pnpm --filter @moajoa/web typecheck`
3. `pnpm --filter @moajoa/web build`
4. Playwright 라이브 (T3) — 1440 + 390.
5. `git status --porcelain apps/ios packages/ui-tokens` 비어 있음.
</verification>

<success_criteria>
- 랜딩 3번 슬라이드에서 카카오/구글/애플 로그인이 실제로 동작하고 /moa 로 착지한다 (next 파라미터 없음).
- `시작하기` 는 3번 슬라이드로 스크롤하고, 3번에서는 사라진다 (도트는 유지, 위치도 유지).
- 소셜 버튼 SVG/클래스가 두 곳에 복붙되지 않고 컴포넌트 1개를 공유한다.
- `/login` 은 삭제되지 않았고 소셜 3종 + ?next= 흐름이 그대로다.
- 390×844 에서 3번 슬라이드가 잘리지 않고 모든 텍스트가 AA 를 통과한다 (근거 주석에 기록).
- 하드코딩 hex 는 브랜드 강제색(#FEE500 · 구글 4색 · 애플 검정) + 스크림 알파뿐.
</success_criteria>

<output>
Create `.planning/quick/260714-v1t-landing-inline-login/260714-v1t-SUMMARY.md` when done.
</output>
