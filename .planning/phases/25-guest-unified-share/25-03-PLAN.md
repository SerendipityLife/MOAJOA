---
phase: 25-guest-unified-share
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx
  - apps/web/app/t/[slug]/_components/guest-surface.tsx
  - apps/web/app/t/[slug]/_components/__tests__/guest-surface.test.tsx
  - apps/web/app/t/[slug]/_components/__tests__/guest-mocks.ts
  - apps/web/app/t/[slug]/page.tsx
autonomous: true
requirements: [AUTH-08, SHARE-02, SHARE-03, SHARE-04]

must_haves:
  truths:
    - "공유링크(/t/[slug])가 비로그인 SSR로 즉시 렌더된다 (이름·지도·리스트, cookies() 미접근) — SHARE-02"
    - "첫 참여 액션 시 닉네임 시트 → signInAnonymously → join_moa 순으로 익명 세션이 발급되고 원래 액션이 이어진다 — AUTH-08/SHARE-03"
    - "같은 브라우저 재접속 시 getUser+getMyTripRole로 멤버면 게이트를 생략하고 곧장 MoaIsland를 마운트한다 — AUTH-08"
    - "share_mode가 places/dates/both 화면 구성을 결정하고, join 후 호스트 MoaIsland·PollVoteIsland를 재사용해 실시간·순번이 자동 통합된다 — SHARE-03/04"
  artifacts:
    - path: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      provides: "세션 lifecycle + lazy 게이트 + share_mode 분기 + join 후 MoaIsland/PollVoteIsland 마운트"
      contains: "signInAnonymously"
      min_lines: 120
    - path: "apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx"
      provides: "BottomSheet 닉네임 게이트 (C1)"
      contains: "시작하기"
    - path: "apps/web/app/t/[slug]/page.tsx"
      provides: "VoteIsland → GuestSurface 교체 (SSR 셸 유지)"
      contains: "GuestSurface"
  key_links:
    - from: "guest-surface ensureGuestMember"
      to: "join_moa(slug)"
      via: "signInAnonymously → joinMoa"
      pattern: "joinMoa\\("
    - from: "guest-surface (join 후)"
      to: "MoaIsland"
      via: "direct-read seed + 마운트"
      pattern: "<MoaIsland"
    - from: "page.tsx"
      to: "GuestSurface"
      via: "SSR seed props (share_mode 포함)"
      pattern: "<GuestSurface"
---

<objective>
이 phase의 **뼈대** — `/t/[slug]`를 호스트 컴포넌트 재사용 게스트 통합 화면으로 진화시킨다. (1) `nickname-gate-sheet.tsx` — poll 닉네임 게이트를 BottomSheet로 승격(C1). (2) `guest-surface.tsx` — 클라이언트 전용 세션 lifecycle(getUser→getMyTripRole, D-05 재접속) + lazy 게이트 `ensureGuestMember`(첫 액션→닉네임→signInAnonymously→join_moa→setStoredNickname→액션 재개, Pattern 1) + share_mode 분기(D-09: places→MoaIsland 재사용 / dates→PollVoteIsland 임베드 / both→둘 다). join 완료 후 direct-read seed를 클라이언트에서 로드해 호스트 `MoaIsland`를 실 데이터로 마운트 → 단일 `moa:{tripId}` 채널·순번·색·채팅이 자동 통합(SC4). (3) `page.tsx`에서 `VoteIsland`→`GuestSurface` 교체(surgical, SSR 셸·cookies() 무접근 유지).

Purpose: SC1~4 전부 이 파일에서 배선. 신규 realtime/투표/색/채팅 로직 작성 0 — 검증된 analog 조각(vote-island 세션 lifecycle + poll-vote-island 게이트 + moa-island 마운트) 조립.
Output: 게스트 통합 화면 + 재사용 검증 테스트 + 공유 mock 픽스처.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-guest-unified-share/25-RESEARCH.md
@.planning/phases/25-guest-unified-share/25-PATTERNS.md
@.planning/phases/25-guest-unified-share/25-UI-SPEC.md

<interfaces>
세션 lifecycle 미러 — `apps/web/app/t/[slug]/_components/vote-island.tsx` L106-137:
```typescript
let active = true;
(async () => {
  const client = getSupabaseBrowser();
  const { data } = await client.auth.getUser();
  if (!active) return;
  const uid = data.user?.id ?? null;
  setUserId(uid);
  if (uid) {
    const role = await getMyTripRole(client, tripId, uid).catch(() => null);
    if (!active) return;
    if (role) setJoined(true);   // D-05 재접속: 멤버면 게이트 스킵
  }
})();
return () => { active = false; };
```

lazy 게이트 — RESEARCH Pattern 1 (`ensureGuestMember`):
```typescript
async function ensureGuestMember(client, slug, nickname: string): Promise<string> {
  const { data: { user } } = await client.auth.getUser();
  let uid = user?.id ?? null;
  if (!uid) {
    const { data, error } = await client.auth.signInAnonymously({ options: { data: { name: nickname } } });
    if (error) throw error;
    uid = data.user!.id;
  }
  await joinMoa(client, slug);   // memberships.ts L24-31, role=share_mode
  setStoredNickname(nickname);   // device-token.ts L36 — D-05
  return uid;
}
```
**Pitfall 4:** signInAnonymously→SIGNED_IN→소켓 토큰 갱신 후(=join_moa 완료 후)에만 채널 구독. 비멤버 구독은 무음 0건.

join 후 seed 로딩 — `apps/web/app/moa/[id]/page.tsx` L36-53 (client에서 재현):
```typescript
const [places, links, members, initialMessages] = await Promise.all([
  listPlacesByTrip(client, tripId), listLinksByTrip(client, tripId),
  listTripMembers(client, tripId), listTripMessages(client, tripId),
]);
const placeIds = places.map((p) => p.id);
const [counts, myVotedPlaceIds] = await Promise.all([
  getVoteCounts(client, placeIds), getMyVotedPlaceIds(client, placeIds, uid),
]);
const nameIds = [...new Set([...places.map((p) => p.added_by), trip.owner_id, uid])];
const profileNames = await getProfileNames(client, nameIds);
const memberIdsInJoinOrder = members.map((m) => m.user_id);
```
join 후 full Trip = `getTrip(client, tripId)` (멤버 RLS 통과 — owner_id·share_mode 보유).

MoaIslandProps — `apps/web/app/moa/[id]/_components/moa-island.tsx` L30-42:
`{ trip, currentUserId, initialPlaces, initialLinks, initialCounts, initialMyVotedPlaceIds, memberIdsInJoinOrder, initialProfileNames, initialMessages, currentUserNickname }`

SSR seed — `apps/web/app/t/[slug]/page.tsx` L124-131:
```tsx
{view.board.id && (
  <VoteIsland slug={slug} tripId={view.board.id} places={view.places} links={view.links} />
)}
```
`view.board.share_mode`는 Plan 01(0029 public_trip_view 확장 + PublicBoardView.board Pick)로 노출됨.

닉네임 게이트 이식 소스 — `poll-vote-island.tsx` L198-206·L322-351 (confirmNickname + input/CTA). 카피는 UI-SPEC C1 (heading `닉네임을 정해주세요`, body `이 이름으로 담고 투표한 게 표시돼요.`, placeholder `닉네임` max20, 에러 `닉네임을 정해야 참여할 수 있어요.`, CTA `시작하기`).

poll-vote-island embed props (Plan 02): `deviceToken`·`nickname`·`onRequireMember`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: nickname-gate-sheet.tsx — BottomSheet 닉네임 게이트 (C1)</name>
  <read_first>
    apps/web/app/poll/[code]/_components/poll-vote-island.tsx L198-206·L322-351 (게이트 이식 소스) ·
    25-UI-SPEC.md C1 + Copywriting Contract · @/components BottomSheet 시그니처(add-sheet.tsx 참조).
  </read_first>
  <files>apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx</files>
  <behavior>
    - open=true 시 BottomSheet(title `닉네임을 정해주세요`) 렌더 + input(max20) + `시작하기` CTA.
    - 빈/공백 입력 시 에러 토스트 `닉네임을 정해야 참여할 수 있어요.` + onConfirm 미호출.
    - 유효 입력 확정(버튼/Enter) 시 onConfirm(trimmed) 호출.
    - 중복 검증 없음(D-06).
  </behavior>
  <action>
`@/components` BottomSheet 껍데기(white bg, `rounded-t-3xl`, 핸들 `h-1.5 w-10` — UI-SPEC Spacing) 안에 poll 게이트 입력+CTA를 이식. Props: `{ open: boolean; onConfirm: (nickname: string) => void; onClose?: () => void }`. 내부 `nicknameDraft` state + `confirmNickname`(poll-vote-island L198-206 미러 — trim·빈값 에러 토스트·onConfirm). input `maxLength={20}`·`onKeyDown` Enter=확정·`focus:border-brand-300`. CTA `시작하기` brand-600 semibold ≥40px. 카피는 UI-SPEC C1 확정값(poll verbatim 아님). 신규 hex/spacing 금지 — 토큰 클래스만.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web exec vitest run app/t/[slug]/_components/__tests__/guest-surface.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "닉네임을 정해주세요" apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx`
    - `grep -q "시작하기" apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx`
    - `grep -q "maxLength={20}" apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx`
    - `grep -q "onConfirm" apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx`
    - 신규 hex 리터럴 0: `! grep -Eq "#[0-9a-fA-F]{6}" apps/web/app/t/[slug]/_components/nickname-gate-sheet.tsx`
  </acceptance_criteria>
  <done>BottomSheet 닉네임 게이트·C1 카피·Enter 확정·빈값 에러·토큰 클래스만.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: guest-surface.tsx — 세션 lifecycle + lazy 게이트 + share_mode 분기 + 마운트</name>
  <read_first>
    vote-island.tsx L88-197 (세션·게이트·optimistic 전체) · moa/[id]/page.tsx L36-67 (seed) ·
    moa-island.tsx L30-42 (props) · poll-vote-island.tsx L41-53 (embed props) ·
    25-RESEARCH.md Pattern 1/2/3 + Pitfall 1/4/6 · 25-UI-SPEC.md C2/C3/C4 · 25-PATTERNS.md §guest-surface.
  </read_first>
  <files>apps/web/app/t/[slug]/_components/guest-surface.tsx, apps/web/app/t/[slug]/_components/__tests__/guest-surface.test.tsx, apps/web/app/t/[slug]/_components/__tests__/guest-mocks.ts</files>
  <behavior>
    - 마운트: getUser → uid 있고 getMyTripRole≠null이면 즉시 join된 상태로 MoaIsland/Poll 마운트(D-05 재접속). 아니면 read-only(SSR seed props + anon RPC 집계).
    - 첫 참여 액션(찜/추가/투표) → nickname-gate-sheet 오픈 → onConfirm → ensureGuestMember(signInAnonymously→join_moa→setStoredNickname) → join 후 direct-read seed 로드 → MoaIsland 마운트 → 원래 액션 재개.
    - share_mode='places' → MoaIsland 재사용. 'dates' → PollVoteIsland 임베드(deviceToken=uid·onRequireMember). 'both' → 모으기 탭 상단 poll 섹션 + 아래 장소.
    - 서버 컴포넌트 cookies() 미접근(guest-surface는 'use client'), 세션 해석 전량 클라이언트.
    - 테스트 seam: initialJoined/initialNickname/initialShareMode props로 네트워크 하이드레이션 스킵 가능.
  </behavior>
  <action>
`'use client'` 파일. Props: SSR seed(`slug`, `tripId`, `board`(share_mode 포함), `places`, `links`) + 테스트 seam(`initialJoined?`, `initialNickname?`, `initialUserId?`).

1. **세션 lifecycle** — vote-island.tsx L106-137 미러. `getUser`→uid→`getMyTripRole(client, tripId, uid)`. role 있으면 joined=true(D-05). `active` cleanup 가드.

2. **lazy 게이트** — RESEARCH Pattern 1 `ensureGuestMember(client, slug, nickname)`. 첫 참여 액션 핸들러가 nickname 미확정·미join이면 `setGateOpen(true)`로 시트 오픈, 대기 중이던 액션을 ref에 보관. onConfirm(nickname)→ensureGuestMember→join 후 seed 로드→pendingAction 재개. **Pitfall 4:** join_moa 완료 후에만 MoaIsland 마운트(=채널 구독).

3. **join 후 seed 로딩** — moa/[id]/page.tsx L36-53 client 재현: `getTrip`(full Trip) + listPlacesByTrip/listLinksByTrip/listTripMembers/listTripMessages + getVoteCounts/getMyVotedPlaceIds + getProfileNames(nameIds에 uid 포함 — Pitfall 6). MoaIslandProps 전부 구성해 `<MoaIsland currentUserId={uid} currentUserNickname={profileNames[uid] ?? nickname} .../>` 마운트.

4. **share_mode 분기**(D-09, UI-SPEC C2):
   - `places`: read-only 뷰(seed place-list, 찜 anon 집계) → 첫 액션 게이트 → join 후 MoaIsland.
   - `dates`: `getPublicTripPoll(client, slug)`(Plan 01)로 poll_code 획득 → `<PollVoteIsland code={pollCode} tripId mode status options deviceToken={uid ?? undefined} nickname={nickname ?? undefined} onRequireMember={() => ensureGuestMember(...).then(uid => ({uid, nickname}))} />`. 지도/리스트 없음.
   - `both`: 모으기 탭 상단 poll 섹션(위 dates 임베드) + 아래 MoaIsland 장소 리스트. 섹션 헤딩 `text-sm font-semibold text-neutral-700` + `mt-6`.

5. **read-only 비멤버 뷰**: join 전에는 direct-read 금지(Pitfall 1 — RLS 0건). seed props(place 이름·요약)·`getVoteCounts`(anon grant)만. 찜 어포던스 탭 시 게이트.

**공유 mock 픽스처** `__tests__/guest-mocks.ts` 작성 — vote-island.test.tsx L27-34 + login.test.tsx L1-43 미러. mutable `mockUser` + `authGetUser`/`signInAnonymously`/`getSupabaseBrowser` mock + @moajoa/api query(`joinMoa`·`getMyTripRole`·`listPlacesByTrip`·`getPublicTripPoll` 등) vi.fn. guest-surface.test.tsx가 import.

`guest-surface.test.tsx` 커버리지: SHARE-02(share_mode 3분기 렌더) · SHARE-03(첫 액션→닉네임→signInAnonymously→joinMoa 순서·1회) · AUTH-08(재접속: getMyTripRole≠null이면 게이트 스킵·MoaIsland 즉시 마운트) · dates 모드 getPublicTripPoll 호출.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web exec vitest run app/t/[slug]/_components/__tests__/guest-surface.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "signInAnonymously" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - `grep -q "joinMoa(" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - `grep -q "getMyTripRole" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - `grep -q "<MoaIsland" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - `grep -q "getPublicTripPoll" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - `grep -q "setStoredNickname" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - cookies 미접근: `! grep -q "cookies(" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - `.js` 워크스페이스 import 0: `! grep -Eq "from '@moajoa/[a-z]+/.*\\.js'" apps/web/app/t/[slug]/_components/guest-surface.tsx`
    - web typecheck 0: `pnpm --filter @moajoa/web exec tsc --noEmit`
  </acceptance_criteria>
  <done>세션 lifecycle·lazy 게이트(순서 검증)·share_mode 3분기·join 후 MoaIsland/Poll 마운트·재접속 스킵 테스트 그린.</done>
</task>

<task type="auto">
  <name>Task 3: page.tsx — VoteIsland → GuestSurface 교체 (surgical, SSR 셸 유지)</name>
  <read_first>apps/web/app/t/[slug]/page.tsx L1-131 (전체 — 교체 지점 L124-131).</read_first>
  <files>apps/web/app/t/[slug]/page.tsx</files>
  <action>
L7 import를 `VoteIsland`→`GuestSurface`로 교체. L124-131의 `<VoteIsland ... />`를 `<GuestSurface slug={slug} tripId={view.board.id} board={view.board} places={view.places} links={view.links} />`로 교체(share_mode는 `view.board.share_mode`). SSR 셸(브랜드 한 줄 L79-85·초대 카드 L98-111·빈상태 L113-117·MapSection·출처 섹션 L133-180·footer·`generateMetadata`·`getCachedPublicTrip`)·`cookies()` 무접근 **전부 유지**(§3.3 surgical). 초대 카드는 게스트 화면 최상단 컨텍스트로 유지(UI-SPEC C3). 다른 라인 diff 0.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web exec vitest run && pnpm --filter @moajoa/web exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "<GuestSurface" apps/web/app/t/[slug]/page.tsx`
    - `! grep -q "VoteIsland" apps/web/app/t/[slug]/page.tsx`
    - `! grep -q "cookies(" apps/web/app/t/[slug]/page.tsx` (캐시 무독성 유지)
    - `getCachedPublicTrip` 호출 유지: `grep -q "getCachedPublicTrip" apps/web/app/t/[slug]/page.tsx`
    - web build PASS: `pnpm --filter @moajoa/web build` (`/t/[slug]` 라우트)
  </acceptance_criteria>
  <done>GuestSurface 마운트·SSR 셸·cookies 무접근 유지·web build PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 비로그인 브라우저 → SSR 캐시 | 공유 링크 첫 페인트(쿠키리스 캐시) |
| 익명 세션 → direct-read + realtime 채널 | join 후 멤버 데이터 로드·구독 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Information Disclosure | SSR 캐시 오염(닉네임/찜 노출) | mitigate | guest-surface 'use client' — 세션 해석 전량 클라이언트. page.tsx cookies() 미접근(acceptance grep). getCachedPublicTrip 쿠키리스 유지(Pitfall 2) |
| T-25-09 | Spoofing | 비멤버가 join 전 채널 구독→타 trip 데이터 | mitigate | join_moa 완료 후에만 MoaIsland 마운트(=구독). WALRUS가 구독자 JWT로 RLS 재평가 — 비멤버 0건(Pitfall 4) |
| T-25-10 | Elevation of Privilege | 클라이언트 memberships 직접 INSERT 시도 | mitigate | joinMoa RPC(DEFINER, self-join auth.uid)만 사용. 직접 INSERT는 owner-only RLS로 차단 |
| T-25-11 | Repudiation | 익명 신원 재접속 식별 실패 | accept | 세션 지속=쿠키(같은 브라우저 동일 auth.uid). 시크릿/쿠키삭제 시 초기화는 정상(Pitfall 3) |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web exec vitest run` 그린 · web typecheck 0 · web build PASS(`/t/[slug]`)
- page.tsx cookies() 미접근 · getCachedPublicTrip 유지 · `.js` 워크스페이스 import 0 · iOS 무접촉
- 라이브 2-브라우저 게스트 참여(SHARE-04)는 원격 push(Plan 01 Task 3) 후 verify-work 몫
</verification>

<success_criteria>
게스트가 /t/[slug]에서 SSR 즉시 렌더를 보고, 첫 액션에 닉네임→익명인증→join으로 참여하며, 재접속 시 동일 신원으로 게이트 없이 진입하고, share_mode에 따라 MoaIsland·PollVoteIsland를 재사용해 찜·추가·투표·채팅·실시간이 통합된다.
</success_criteria>

<output>
완료 후 `.planning/phases/25-guest-unified-share/25-03-SUMMARY.md` 생성.
</output>
