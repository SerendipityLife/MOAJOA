---
phase: 23-web-first-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0025_web_share.sql
  - supabase/tests/web_share_smoke.sh
autonomous: true
requirements: [AUTH-08, SHARE-03, CHAT-01] # 백엔드 기반만 — e2e 검증은 Phase 25/26에 매핑 (ROADMAP 명시)
must_haves:
  truths:
    - "0025가 share_mode·companion 컬럼 + trip_messages 테이블(RLS 헬퍼-only) + join_moa RPC를 담는다"
    - "join_moa는 join_shared_trip의 안전장치 5종을 전부 유지하고 role만 share_mode로 분기한다"
    - "smoke 스크립트가 익명 세션 발급 → join_moa editor/voter 분기 → trip_messages RLS 런타임 프로브(200) → kakao authorize 시작을 단언한다"
  artifacts:
    - path: supabase/migrations/0025_web_share.sql
      provides: "trips.share_mode/companion + trip_messages + join_moa RPC"
      contains: "create or replace function join_moa"
    - path: supabase/tests/web_share_smoke.sh
      provides: "익명 세션 + join_moa role 분기 + trip_messages RLS 프로브 + kakao authorize smoke (실행은 23-04)"
      contains: "is_anonymous"
  key_links:
    - from: "join_moa(p_share_slug)"
      to: memberships
      via: "share_mode 분기 INSERT (editor/voter)"
      pattern: "case when v_share_mode in \\('places','both'\\) then 'editor' else 'voter' end"
    - from: trip_messages RLS
      to: "can_read_trip / can_vote_trip / am_trip_owner"
      via: "SECURITY DEFINER 헬퍼만 (42P17 가드)"
      pattern: "can_read_trip\\(trip_id\\)"
---

<objective>
웹 퍼스트 공유·참여·채팅의 DB 기반을 만든다: `0025_web_share.sql` — trips.share_mode('dates'|'places'|'both')·companion 컬럼, trip_messages 테이블(+RLS), join_moa RPC(share_mode 기반 editor/voter 분기) — 와 익명 세션·join_moa·trip_messages RLS 프로브·kakao authorize smoke 스크립트(Wave 0).

Purpose: Phase 25(게스트 통합 공유화면)·26(채팅)이 import할 스키마 seam. 전부 0016/0018의 검증된 idiom 미러 — 새 발명 0.
Output: `supabase/migrations/0025_web_share.sql`, `supabase/tests/web_share_smoke.sh` (적용·실행은 23-04 [BLOCKING]).

**Open Question 기본값 잠금 (RESEARCH 권장안 — 저비용 변경 가능):**
- **D-A1**: join_moa role 매핑 = `share_mode in ('places','both')` → `'editor'`, 그 외(`'dates'`·null 레거시) → `'voter'`
- **D-A2**: trip_messages는 nickname **비정규화** (date_comments 미러 — 익명 유저 purge 대비 + 조인 없는 히스토리 렌더 + "발화 시점 닉네임" 스냅샷)
- **D-A4**: 재-join 시 기존 role 유지 (`on conflict do nothing` — 모드 변경 후 자동 승격 없음)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-web-first-foundation/23-RESEARCH.md
@.planning/phases/23-web-first-foundation/23-PATTERNS.md

<interfaces>
<!-- 0016 기존 헬퍼 — 재정의 금지, 0025 RLS는 이것만 사용 -->
am_trip_owner(p_trip_id uuid) → boolean   -- 0016 L220
can_read_trip(p_trip_id uuid) → boolean   -- 0016 L291
can_edit_trip(p_trip_id uuid) → boolean   -- 0016 L313 (owner|editor)
can_vote_trip(p_trip_id uuid) → boolean   -- 0016 L338 (모든 accepted member)

<!-- 0016 memberships — role CHECK에 'editor' 이미 존재 (L203), unique (trip_id, user_id) 존재 -->
role text not null default 'editor' check (role in ('owner','editor','voter'))

<!-- 0016 ensure_share_slug 트리거 — visibility 변경 시 slug 자동 생성. 0025에서 손대지 않음 (재사용) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 익명 세션 + join_moa + trip_messages RLS 프로브 + kakao authorize smoke 스크립트 작성 (Wave 0)</name>
  <files>supabase/tests/web_share_smoke.sh</files>
  <read_first>
    - .planning/phases/23-web-first-foundation/23-RESEARCH.md (Code Example 3~4 — curl shape, JWT 디코드)
  </read_first>
  <action>
`supabase/tests/web_share_smoke.sh`를 아래 내용 그대로 작성한다. 전제(주석 명시): config.toml `enable_anonymous_sign_ins = true` 반영을 위한 스택 재시작 + 0025 적용 후 실행 (23-04). 익명 rate limit 30/hr/IP(Pitfall 6)가 있으므로 반복 실행 시 세션 재사용 유의 주석 포함.

```bash
#!/usr/bin/env bash
# Phase 23 smoke — (1) 익명 세션 is_anonymous 클레임 (2) join_moa share_mode 분기
# (3) trip_messages RLS 런타임 프로브 (4) kakao authorize 시작 가능.
# 전제: config.toml 반영 재시작 + 0025 적용 (23-04).
# 주의: 익명 signup은 IP당 30/hr rate limit — 반복 실행 시 유의.
set -euo pipefail
DB="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
API="http://127.0.0.1:54321"
ANON_KEY=$(supabase status -o env 2>/dev/null | grep ANON_KEY | cut -d= -f2 | tr -d '"')

# (1) 익명 세션 — data.name 주입 (Pitfall 4: 미주입 시 display_name='user')
RESP=$(curl -s -X POST "$API/auth/v1/signup" \
  -H "apikey: $ANON_KEY" -H "Content-Type: application/json" \
  -d '{"data":{"name":"게스트닉네임"}}')
JWT=$(printf '%s' "$RESP" | python3 -c "import json,sys; print(json.load(sys.stdin)['access_token'])")
CLAIMS=$(printf '%s' "$JWT" | python3 -c "
import json,base64,sys
p=sys.stdin.read().split('.')[1]
c=json.loads(base64.urlsafe_b64decode(p+'=='))
print(str(c['is_anonymous'])+'|'+c['role']+'|'+c['sub'])")
IS_ANON=$(echo "$CLAIMS" | cut -d'|' -f1)
ROLE=$(echo "$CLAIMS" | cut -d'|' -f2)
ANON_UID=$(echo "$CLAIMS" | cut -d'|' -f3)
[ "$IS_ANON" = "True" ] || { echo "FAIL: is_anonymous claim ($IS_ANON)"; exit 1; }
[ "$ROLE" = "authenticated" ] || { echo "FAIL: role claim ($ROLE, want authenticated)"; exit 1; }

# (2) 호스트 + shared trip 2개 셋업 (superuser psql — ensure_share_slug 트리거가 slug 생성)
EMAIL="sharehost@local.test"
curl -s -X POST "$API/auth/v1/signup" -H "apikey: $ANON_KEY" -H "Content-Type: application/json" \
  -d "{\"email\":\"$EMAIL\",\"password\":\"password-share-1\"}" > /dev/null || true
HOST=$(psql "$DB" -tAc "select id from auth.users where email='$EMAIL' limit 1")
[ -n "$HOST" ] || { echo "FAIL: host user missing"; exit 1; }
T_BOTH=$(psql "$DB" -tAc "insert into trips (owner_id, title) values ('$HOST','smoke-both-$(date +%s)') returning id")
T_DATES=$(psql "$DB" -tAc "insert into trips (owner_id, title) values ('$HOST','smoke-dates-$(date +%s)') returning id")
psql "$DB" -qc "update trips set visibility='shared', share_mode='both' where id='$T_BOTH'"
psql "$DB" -qc "update trips set visibility='shared', share_mode='dates' where id='$T_DATES'"
SLUG_BOTH=$(psql "$DB" -tAc "select share_slug from trips where id='$T_BOTH'")
SLUG_DATES=$(psql "$DB" -tAc "select share_slug from trips where id='$T_DATES'")
[ -n "$SLUG_BOTH" ] && [ -n "$SLUG_DATES" ] || { echo "FAIL: ensure_share_slug minted no slug"; exit 1; }

# (3) join_moa — both→editor, dates→voter (D-A1)
curl -s -X POST "$API/rest/v1/rpc/join_moa" -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" -d "{\"p_share_slug\":\"$SLUG_BOTH\"}" > /dev/null
curl -s -X POST "$API/rest/v1/rpc/join_moa" -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT" \
  -H "Content-Type: application/json" -d "{\"p_share_slug\":\"$SLUG_DATES\"}" > /dev/null
R_BOTH=$(psql "$DB" -tAc "select role from memberships where trip_id='$T_BOTH' and user_id='$ANON_UID'")
R_DATES=$(psql "$DB" -tAc "select role from memberships where trip_id='$T_DATES' and user_id='$ANON_UID'")
[ "$R_BOTH" = "editor" ] || { echo "FAIL: share_mode=both → '$R_BOTH' (want editor)"; exit 1; }
[ "$R_DATES" = "voter" ] || { echo "FAIL: share_mode=dates → '$R_DATES' (want voter)"; exit 1; }

# (4) trip_messages RLS 런타임 프로브 — 정책은 authenticated 쿼리 시점에만 평가됨.
#     db reset(superuser)·DEFINER RPC 어느 경로도 이 정책을 밟지 않으므로 익명 JWT GET으로 실증.
#     42P17 recursion 발화 시 200이 아닌 5xx → FAIL (성공 기준 1의 런타임 보강).
MSG_CODE=$(curl -s -o /dev/null -w '%{http_code}' \
  "$API/rest/v1/trip_messages?trip_id=eq.$T_BOTH&select=id" \
  -H "apikey: $ANON_KEY" -H "Authorization: Bearer $JWT")
[ "$MSG_CODE" = "200" ] || { echo "FAIL: trip_messages RLS probe HTTP $MSG_CODE (want 200 — 42P17 recursion?)"; exit 1; }

# (5) kakao authorize 시작 가능 — redirect_url에 kauth.kakao.com
LOC=$(curl -s -o /dev/null -w '%{redirect_url}' "$API/auth/v1/authorize?provider=kakao")
echo "$LOC" | grep -q "kauth.kakao.com" || { echo "FAIL: kakao authorize redirect ($LOC)"; exit 1; }

echo "PASS: anon(is_anonymous=true, role=authenticated) + join_moa(both→editor, dates→voter) + trip_messages RLS(200) + kakao authorize"
```

`chmod +x supabase/tests/web_share_smoke.sh`.
  </action>
  <acceptance_criteria>
    - `supabase/tests/web_share_smoke.sh` 존재 + 실행 권한, `bash -n` exit 0
    - 파일에 `is_anonymous`, `join_moa`, `kauth.kakao.com`, `editor`, `voter`, `data.*name` 주입(`게스트닉네임`) 전부 grep 매치
    - `rest/v1/trip_messages` 프로브 + HTTP `200` 단언 존재 (join_moa 이후 익명 JWT GET — RLS 런타임 평가 실증) — `grep -q "rest/v1/trip_messages"` 매치
    - rate limit 주의 주석 존재 (`30/hr`)
  </acceptance_criteria>
  <verify>
    <automated>bash -n supabase/tests/web_share_smoke.sh && grep -q "kauth.kakao.com" supabase/tests/web_share_smoke.sh && grep -q "is_anonymous" supabase/tests/web_share_smoke.sh && grep -q "rest/v1/trip_messages" supabase/tests/web_share_smoke.sh</automated>
  </verify>
  <done>성공 기준 3·4(로컬 절반) + trip_messages RLS 런타임 평가를 exit code로 단언하는 자기완결 smoke가 존재한다. 실행은 23-04.</done>
</task>

<task type="auto">
  <name>Task 2: 0025_web_share.sql 마이그레이션 작성</name>
  <files>supabase/migrations/0025_web_share.sql</files>
  <read_first>
    - supabase/migrations/0016_trips_baseline.sql L631~665 (join_shared_trip — join_moa의 원본, 안전장치 5종), L404~412 (links RLS — 헬퍼-only shape), L536~545 (votes INSERT — "본인 + 헬퍼" 결합 shape), L488~494 (places DELETE — "본인 or owner" shape)
    - supabase/migrations/0018_date_polls.sql L46~54 (date_comments — trip_messages의 테이블 shape)
    - supabase/migrations/0023_ledger_update_check.sql (헤더 스타일)
  </read_first>
  <action>
`supabase/migrations/0025_web_share.sql`을 새로 생성한다 (0016~0024 무수정). 0023 스타일 헤더(파일명 — Phase 23, AUTH-08/SHARE-03/CHAT-01 백엔드 기반 — D-A1/D-A2/D-A4 결정 기록 — 42P17 가드: 크로스 테이블은 헬퍼만 — Append-only: 0016..0024 are NEVER modified) 뒤에 아래 SQL을 그대로 넣는다.

```sql
-- 1) trips 확장 (nullable = 레거시 안전, §4.3)
alter table trips add column share_mode text
  check (share_mode in ('dates','places','both'));
alter table trips add column companion text
  check (char_length(companion) <= 20);

-- 2) trip_messages — 0018 date_comments shape 미러, device_token → user_id(익명도 profiles 행 보유).
--    nickname 비정규화 (D-A2): 발화 시점 스냅샷, 익명 유저 purge 후에도 히스토리 렌더 가능.
create table if not exists trip_messages (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references trips(id) on delete cascade,
  user_id uuid not null references profiles(id),
  nickname text not null,
  body text not null check (char_length(body) between 1 and 140),
  reply_to_place_id uuid references places(id) on delete set null,
  created_at timestamptz not null default now()
);
create index trip_messages_trip_id_idx on trip_messages (trip_id, created_at);

alter table trip_messages enable row level security;

-- RLS: 크로스 테이블은 SECURITY DEFINER 헬퍼만 (42P17, §4.4). 직접 EXISTS 0.
create policy "trip_messages: read if can read trip"
  on trip_messages for select
  to authenticated
  using (can_read_trip(trip_id));

create policy "trip_messages: insert own if can vote trip"
  on trip_messages for insert
  to authenticated
  with check (user_id = auth.uid() and can_vote_trip(trip_id));

create policy "trip_messages: delete own or trip owner"
  on trip_messages for delete
  to authenticated
  using (user_id = auth.uid() or am_trip_owner(trip_id));

-- 3) join_moa — 0016 join_shared_trip(L631~665) 미러. 안전장치 전부 유지:
--    bearer slug 검증 · self-join only(auth.uid) · owner self-join 가드 ·
--    on conflict do nothing 멱등(D-A4: 자동 승격 없음) · DEFINER + search_path 핀.
--    변경점: role 하드코딩 'voter' → share_mode 분기 (D-A1). 클라이언트 role 인자 없음.
--    기존 join_shared_trip은 삭제하지 않음 (iOS 동결).
create or replace function join_moa(p_share_slug text)
returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
  v_trip_id uuid;
  v_owner_id uuid;
  v_share_mode text;
begin
  select id, owner_id, share_mode into v_trip_id, v_owner_id, v_share_mode
  from trips
  where share_slug = p_share_slug
    and visibility in ('shared','public')
  limit 1;

  if v_trip_id is null then
    raise exception 'trip not found or not shared';
  end if;

  if v_owner_id = auth.uid() then
    return v_trip_id;
  end if;

  insert into memberships (trip_id, user_id, role, accepted_at)
  values (v_trip_id, auth.uid(),
          case when v_share_mode in ('places','both') then 'editor' else 'voter' end,
          now())
  on conflict (trip_id, user_id) do nothing;

  return v_trip_id;
end;
$$;

grant execute on function join_moa(text) to authenticated;
```

grant는 `to authenticated`만 — 익명 유저도 authenticated role이므로 anon grant 불필요 (RESEARCH Pattern 2). slug 생성 로직 재구현 금지 — `ensure_share_slug` 트리거(0016)가 visibility 변경 시 자동 발동.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/0025_web_share.sql` 존재, 0016~0024 무수정 (`git diff --name-only supabase/migrations/ | grep -v 0025 | grep -v 0024` 출력 0줄 — 0024는 23-01이 같은 커밋 트리에 있을 수 있음)
    - `share_mode in ('dates','places','both')` 문자 그대로 존재 (core ShareMode 상수와 문자 단위 잠금 대상)
    - join_moa에 `security definer`+`set search_path = public`+`visibility in ('shared','public')`+`on conflict (trip_id, user_id) do nothing`+`grant execute on function join_moa(text) to authenticated` 전부 grep 매치
    - trip_messages 정책 3개에 `can_read_trip(trip_id)` / `user_id = auth.uid() and can_vote_trip(trip_id)` / `user_id = auth.uid() or am_trip_owner(trip_id)` 각각 존재
    - 파일 내 `join ` 또는 다른 테이블 직접 `exists (select` 패턴 0건 (RLS 정책 본문 기준 — 42P17 가드)
    - `reply_to_place_id uuid references places(id) on delete set null` 존재 (CHAT-03 기반)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "share_mode in ('dates','places','both')" supabase/migrations/0025_web_share.sql && grep -q "on conflict (trip_id, user_id) do nothing" supabase/migrations/0025_web_share.sql && grep -q "can_vote_trip(trip_id)" supabase/migrations/0025_web_share.sql && grep -q "grant execute on function join_moa(text) to authenticated" supabase/migrations/0025_web_share.sql</automated>
  </verify>
  <done>0025가 share_mode·companion·trip_messages·join_moa를 검증된 idiom 미러로 담는다. 적용·42P17 검증은 23-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게스트(익명 JWT) → join_moa RPC | slug만 아는 미인가 사용자가 멤버십을 self-grant하는 경로 |
| 멤버 → trip_messages INSERT/SELECT | 크로스 trip 읽기/쓰기·사칭 가능 표면 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04 | Elevation | join_moa (임의 trip 잠입) | mitigate | bearer slug + `visibility in ('shared','public')` 게이트, role은 서버가 share_mode로 결정(클라이언트 role 인자 없음), `user_id = auth.uid()` self-join only — join_shared_trip 검증 shape 유지 |
| T-23-05 | Spoofing/Info Disclosure | join_moa slug brute-force | accept | slug = 0016 `gen_random_bytes` ~60bit opaque 토큰 — 브루트포스 비현실적. 기존 join_shared_trip과 동일 노출 수준, 신규 표면 아님 |
| T-23-06 | Tampering | trip_messages 사칭 INSERT (`user_id` 위조) | mitigate | WITH CHECK `user_id = auth.uid() and can_vote_trip(trip_id)` (0023 CR-01 교훈 — 신원+접근 동시 검사) |
| T-23-07 | Info Disclosure | trip_messages 크로스 trip SELECT | mitigate | `can_read_trip(trip_id)` 헬퍼-only — 23-04 reset(42P17=0) + smoke의 익명 JWT trip_messages 프로브(200)가 게이트 |
| T-23-08 | Elevation | 멱등 재-join으로 role 승격 시도 | mitigate | `on conflict do nothing` — 기존 role 절대 변경 없음 (D-A4). smoke가 editor/voter 분기 단언 |
| T-23-09 | DoS | RLS 42P17 재귀 | mitigate | 크로스 테이블 EXISTS 0, 헬퍼만 — acceptance grep + 23-04 `supabase db reset` 클린 + smoke trip_messages 프로브(런타임 평가)가 게이트 |
</threat_model>

<verification>
- 0025 정적 grep 전부 통과 (CHECK 값·DEFINER·헬퍼-only·grant)
- smoke 스크립트 문법 클린 + 단언 5종 포함 (익명 클레임·join_moa 분기·trip_messages RLS 200·kakao redirect·slug 발급)
- 적용·런타임 검증은 23-04 — 이 플랜 단독으로 "동작함"을 주장하지 않는다
</verification>

<success_criteria>
- 0025_web_share.sql이 D-A1/A2/A4 결정을 헤더에 기록하고 join_shared_trip 안전장치를 전부 유지
- web_share_smoke.sh가 성공 기준 3(익명+join_moa)·4(kakao 로컬 절반) + trip_messages RLS 런타임 프로브를 자동 단언
</success_criteria>

<output>
완료 후 `.planning/phases/23-web-first-foundation/23-02-SUMMARY.md` 생성.
</output>
