---
phase: 01-build-unblock-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ios/.npmrc
  - apps/ios/package.json
  - packages/ui-tokens/package.json
  - packages/ui-tokens/src/brand/wordmark.svg
  - packages/ui-tokens/src/brand/icon.svg
  - packages/ui-tokens/src/brand/LICENSE-Pretendard.txt
  - packages/ui-tokens/scripts/export-assets.mjs
  - apps/ios/assets/fonts/Pretendard-Regular.otf
  - apps/ios/assets/fonts/Pretendard-Medium.otf
  - apps/ios/assets/fonts/Pretendard-SemiBold.otf
  - apps/ios/assets/fonts/Pretendard-Bold.otf
  - apps/ios/assets/fonts/LICENSE-Pretendard.txt
  - apps/ios/assets/icon.png
  - apps/ios/assets/adaptive-icon.png
  - apps/ios/assets/splash.png
  - apps/web/public/fonts/Pretendard-Regular.otf
  - apps/web/public/fonts/Pretendard-Medium.otf
  - apps/web/public/fonts/Pretendard-SemiBold.otf
  - apps/web/public/fonts/Pretendard-Bold.otf
  - apps/web/public/fonts/LICENSE-Pretendard.txt
  - apps/web/public/apple-touch-icon.png
  - apps/web/public/og-default.png
  - apps/web/app/favicon.ico
  - apps/web/assets/Pretendard-Bold.ttf
  - pnpm-lock.yaml
autonomous: false
requirements: [BUILD-03]

must_haves:
  truths:
    - "단일 SVG source 2개(wordmark.svg, icon.svg)가 packages/ui-tokens/src/brand/에 존재한다"
    - "sharp 기반 export-assets.mjs 스크립트가 동일 머신 재실행 시 같은 PNG 산출물을 만든다 (V8 idempotency)"
    - "iOS bundle용 Pretendard 4 weight .otf 파일이 apps/ios/assets/fonts/에 존재한다"
    - "Web용 Pretendard 4 weight .otf + Bold .ttf(Phase 4 OG consumer)가 apps/web/{public/fonts,assets}에 존재한다"
    - "SIL OFL 1.1 LICENSE 파일이 폰트 파일과 같은 위치에 존재한다 (3곳)"
    - "iOS 빌드용 hoisting 범위가 apps/ios/.npmrc 한 파일에 한정된다 (D-02)"
  artifacts:
    - path: "apps/ios/.npmrc"
      provides: "RN native module을 위한 pnpm flat node_modules 셋업 (D-02)"
      contains: "node-linker=hoisted"
    - path: "packages/ui-tokens/src/brand/wordmark.svg"
      provides: "MOAJOA 워드마크 outline source (Pretendard Bold path 변환)"
      min_lines: 5
    - path: "packages/ui-tokens/src/brand/icon.svg"
      provides: "M-shape map pin icon source (1024x1024)"
      min_lines: 5
    - path: "packages/ui-tokens/scripts/export-assets.mjs"
      provides: "SVG → PNG/ICO/TTF 일괄 export (sharp + copyFile)"
      min_lines: 60
    - path: "apps/ios/assets/icon.png"
      provides: "iOS app icon (1024x1024)"
    - path: "apps/ios/assets/splash.png"
      provides: "iOS splash (1242x2688, 흰 배경 + 중앙 워드마크)"
    - path: "apps/web/app/favicon.ico"
      provides: "Next.js metadata convention favicon"
    - path: "apps/web/assets/Pretendard-Bold.ttf"
      provides: "Phase 4 Satori OG image consumer"
  key_links:
    - from: "packages/ui-tokens/scripts/export-assets.mjs"
      to: "packages/ui-tokens/src/brand/{wordmark,icon}.svg"
      via: "readFile + sharp pipeline"
      pattern: "readFile.*brand/(wordmark|icon)\\.svg"
    - from: "packages/ui-tokens/scripts/export-assets.mjs"
      to: "node_modules/pretendard/dist/public/static"
      via: "copyFile (npm pretendard package as font SOT)"
      pattern: "node_modules/pretendard"
---

<objective>
브랜드 자산 single source(SVG 2개)를 도입하고, sharp 기반 export 스크립트로 iOS·Web 모두에 필요한 PNG/ICO 산출물 + Pretendard 폰트 파일(.otf 4 weight × 2 위치, .ttf Bold × 1)을 일괄 생성한다. 동시에 iOS 빌드 prerequisite인 `apps/ios/.npmrc` (hoisting 한정)를 생성한다.

Purpose: 다음 wave(02-PLAN iOS 빌드, 03-PLAN app.config.ts 와이어업)가 의존하는 파일들을 만든다. BUILD-03(자산 파일 존재)의 절반을 충족(나머지 절반인 expo plugin 와이어업은 03-PLAN).
Output: SVG 2개, export 스크립트 1개, 생성된 PNG/ICO 7개, 폰트 .otf 8개(iOS 4 + Web 4), .ttf 1개, LICENSE 3개, .npmrc 1개, package.json 2개 수정, lockfile freeze.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-unblock-hygiene/01-CONTEXT.md
@.planning/phases/01-build-unblock-hygiene/01-RESEARCH.md
@.planning/phases/01-build-unblock-hygiene/01-PATTERNS.md
@.planning/phases/01-build-unblock-hygiene/01-UI-SPEC.md
@.planning/phases/01-build-unblock-hygiene/01-VALIDATION.md

<interfaces>
<!-- 기존 ui-tokens 노출 인터페이스 (변경 없음, 참고용) -->

From packages/ui-tokens/src/index.ts (확인됨):
- `colors.brand.500` = `#F97316`, `colors.brand.700` = `#C2410C`
- `colors.neutral.0` = `#FFFFFF`, `colors.neutral.900` = `#0F172A`
- `typography.fonts.sans = ['Pretendard', 'IBM Plex Sans KR', 'Noto Sans JP', 'system-ui', 'sans-serif']`

From packages/ui-tokens/src/tailwind.ts (확인됨, 변경 없음):
- exports `tailwindPreset` — 이미 brand.{50..900} color tokens + fontFamily.sans 등록
- iOS의 tailwind.config.js가 이미 이 preset import 중

Font SOT (npm pretendard@1.3.9 tarball, verified):
- `node_modules/pretendard/dist/public/static/Pretendard-Regular.otf` (1,574,352 bytes)
- `node_modules/pretendard/dist/public/static/Pretendard-Medium.otf` (1,584,068 bytes)
- `node_modules/pretendard/dist/public/static/Pretendard-SemiBold.otf` (1,583,704 bytes)
- `node_modules/pretendard/dist/public/static/Pretendard-Bold.otf` (1,576,660 bytes)
- `node_modules/pretendard/dist/public/static/alternative/Pretendard-Bold.ttf` (2,661,752 bytes)
- `node_modules/pretendard/dist/LICENSE.txt` (SIL OFL 1.1)

⚠️ UI-SPEC가 "총 ~1.6MB"로 적었으나 실제는 ~6.3MB (4 weight 합계). RESEARCH §"폰트 파일 확보" 검증값을 단일 진실로 사용.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: pnpm hoist 한정 + ui-tokens 의존성 + lockfile 갱신</name>
  <files>
    apps/ios/.npmrc (NEW),
    apps/ios/package.json (MOD),
    packages/ui-tokens/package.json (MOD),
    pnpm-lock.yaml (regenerated)
  </files>
  <action>
**1. apps/ios/.npmrc 신규 생성**

`apps/ios/.npmrc`를 신규 생성. 정확히 다음 3줄 (코멘트 + 빈 줄 + 설정):

```
# RN native modules expect flat node_modules paths (podspec resolution).
# Scoped to apps/ios only; root workspace stays isolated (see /.npmrc).
node-linker=hoisted
```

루트 `/.npmrc`는 절대 수정하지 않음 (D-02 위반, Pitfall B). 다른 워크스페이스에 `.npmrc` 추가하지 않음.

**2. apps/ios/package.json 수정 (in-place edit)**

- `dependencies.nativewind`: `"^4.1.23"` → `"^4.2.4"` (RESEARCH §"NativeWind 4.2 Upgrade" — npm view nativewind dist-tags.latest = 4.2.4 검증됨)
- `dependencies`에 신규 키 추가 (alphabetical 위치): `"react-native-worklets": "^0.8.3"` (RESEARCH §"react-native-worklets peer 설치" — Reanimated v4 worklets API peer)
- `devDependencies.tailwindcss`: `"^3.4.13"` → `"^3.4.17"`
- `devDependencies`에 신규 키 추가: `"pretendard": "^1.3.9"` (export-assets.mjs가 node_modules/pretendard/를 폰트 SOT로 사용)

**⚠️ NOT touch (Pitfall A):**
- `apps/ios/babel.config.js` 절대 수정 금지 — `react-native-worklets/plugin` 추가하면 reanimated/plugin과 중복으로 빌드 에러
- `expo-share-intent` 추가 금지 (D-16, Pitfall F)

**3. packages/ui-tokens/package.json 수정 (in-place edit)**

- `scripts`에 추가: `"export-assets": "node scripts/export-assets.mjs"`
- `devDependencies`에 추가: `"sharp": "^0.34.5"`

(pretendard는 apps/ios/package.json에 두므로 ui-tokens에는 두지 않음 — 단일 위치. export-assets.mjs는 ROOT 상대경로로 node_modules/pretendard/에 접근.)

**4. pnpm install + lockfile commit (Pitfall G)**

```bash
pnpm install
```

성공 후 `pnpm-lock.yaml` 변경분이 자동으로 staged 된다. 빌드 통과 시점이 아니라 *이 task 끝*에 즉시 commit 권장 — 다음 task부터 frozen lockfile로 진행해야 sharp/pretendard 버전 drift를 막음.

  </action>
  <verify>
    <automated>test -f apps/ios/.npmrc &amp;&amp; grep -q "node-linker=hoisted" apps/ios/.npmrc &amp;&amp; ! grep -q "node-linker" .npmrc &amp;&amp; node -e "const p=require('./apps/ios/package.json'); if(p.dependencies.nativewind!=='^4.2.4')throw 'nativewind'; if(p.dependencies['react-native-worklets']!=='^0.8.3')throw 'worklets'; if(p.devDependencies.tailwindcss!=='^3.4.17')throw 'tailwind'; if(!p.devDependencies.pretendard)throw 'pretendard'; console.log('ok')" &amp;&amp; node -e "const p=require('./packages/ui-tokens/package.json'); if(!p.scripts['export-assets'])throw 'script'; if(!p.devDependencies.sharp)throw 'sharp'; console.log('ok')" &amp;&amp; test -d node_modules/pretendard/dist/public/static &amp;&amp; test -d node_modules/sharp</automated>
  </verify>
  <done>
    - `apps/ios/.npmrc` 존재, 정확히 `node-linker=hoisted` 한 줄 (+ 코멘트) 포함
    - 루트 `/.npmrc`는 grep로 `node-linker` 미포함 (D-02 scope 검증)
    - `apps/ios/package.json` 4개 변경 모두 적용됨
    - `packages/ui-tokens/package.json` script + devDep 추가됨
    - `pnpm install` 성공 → `node_modules/pretendard/`, `node_modules/sharp/`, `node_modules/react-native-worklets/` 존재
    - `pnpm-lock.yaml` 갱신됨
  </done>
</task>

<task type="auto">
  <name>Task 2: Brand SVG source 2개 + LICENSE + export-assets.mjs 스크립트</name>
  <files>
    packages/ui-tokens/src/brand/wordmark.svg (NEW),
    packages/ui-tokens/src/brand/icon.svg (NEW),
    packages/ui-tokens/src/brand/LICENSE-Pretendard.txt (NEW),
    packages/ui-tokens/scripts/export-assets.mjs (NEW)
  </files>
  <action>
**1. packages/ui-tokens/src/brand/icon.svg (NEW)**

UI-SPEC §"Icon Mark" + RESEARCH §"icon.svg 최소 markup". 1024×1024 캔버스, 흰 배경 + brand orange M-shape map pin (inner 80% safe area = 102..922):

```svg
<?xml version="1.0" encoding="UTF-8"?>
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024">
  <rect width="1024" height="1024" fill="#FFFFFF"/>
  <path d="M 200 250 L 350 250 L 512 500 L 674 250 L 824 250 L 824 700 L 700 800 L 512 950 L 324 800 L 200 700 Z" fill="#F97316"/>
</svg>
```

(이 path는 첫 컷이다. 디자이너 review 후 정교화 가능. sharp는 valid SVG만 받으면 정확히 PNG로 변환한다 — Pitfall C 회피 위해 `<text>` 요소 절대 두지 말 것.)

**2. packages/ui-tokens/src/brand/wordmark.svg (NEW)**

UI-SPEC §"Wordmark Composition". viewBox ~5:1, "MOAJOA" outlined to paths in brand orange. 첫 컷은 단순 사각형 letter approximation으로 시작 (디자이너가 Pretendard Bold path로 교체):

```svg
<?xml version="1.0" encoding="UTF-8"?>
<!-- MOAJOA wordmark. Letters outlined from Pretendard Bold (no font dep at render time). -->
<!-- TODO: replace these placeholder rectangles with actual Pretendard Bold outline paths -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 500 100" fill="#F97316">
  <rect x="10"  y="10" width="60" height="80" rx="4"/>
  <rect x="90"  y="10" width="60" height="80" rx="4"/>
  <rect x="170" y="10" width="60" height="80" rx="4"/>
  <rect x="250" y="10" width="60" height="80" rx="4"/>
  <rect x="330" y="10" width="60" height="80" rx="4"/>
  <rect x="410" y="10" width="60" height="80" rx="4"/>
</svg>
```

**중요 (Karpathy §3.1/§3.4):** 이 placeholder는 검증 가능한 default 임. sharp가 받아 PNG 변환 가능. Phase 1 동안 splash·OG에 표시되는 워드마크는 "정사각형 6개" 모양이지만 export 파이프라인 자체는 검증된다. **실제 outlined paths는 디자인 검토 후 same file 교체** — Wave 2(02-PLAN) 실기기 검증과 병렬로 디자이너 작업.

**3. packages/ui-tokens/src/brand/LICENSE-Pretendard.txt (NEW)**

`node_modules/pretendard/dist/LICENSE.txt` 내용을 그대로 복사. 워드마크 outline의 출처(Pretendard Bold)를 명시하기 위해 source 디렉토리에도 함께 둠.

```bash
cp node_modules/pretendard/dist/LICENSE.txt packages/ui-tokens/src/brand/LICENSE-Pretendard.txt
```

**4. packages/ui-tokens/scripts/export-assets.mjs (NEW)**

RESEARCH §"export-assets.mjs — sharp 정확한 API 호출" lines 432-540 패턴을 그대로 사용. 단, 폰트 source는 `node_modules/pretendard/` (apps/ios/assets/fonts/가 아직 비어있을 수 있으므로). 전체 내용:

```javascript
// packages/ui-tokens/scripts/export-assets.mjs
// 실행: pnpm --filter @moajoa/ui-tokens run export-assets
// SOT: packages/ui-tokens/src/brand/{wordmark,icon}.svg
//      node_modules/pretendard/dist/public/static/{Regular,Medium,SemiBold,Bold}.otf
//      node_modules/pretendard/dist/public/static/alternative/Pretendard-Bold.ttf
//      node_modules/pretendard/dist/LICENSE.txt

import sharp from 'sharp';
import { readFile, copyFile, mkdir } from 'node:fs/promises';
import { dirname, join, resolve } from 'node:path';
import { fileURLToPath } from 'node:url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const ROOT = resolve(__dirname, '../../..');
const BRAND = resolve(__dirname, '../src/brand');
const PRETENDARD = join(ROOT, 'node_modules/pretendard/dist');

const iconSvg = await readFile(join(BRAND, 'icon.svg'));
const wordmarkSvg = await readFile(join(BRAND, 'wordmark.svg'));

const PNG_OPTS = { compressionLevel: 9, palette: false, progressive: false };

async function ensureDir(p) {
  await mkdir(dirname(p), { recursive: true });
}

async function writePng(svgBuf, width, height, outPath, opts = {}) {
  await ensureDir(outPath);
  await sharp(svgBuf, { density: 384 })
    .resize(width, height, {
      fit: opts.fit ?? 'contain',
      background: opts.background ?? { r: 255, g: 255, b: 255, alpha: 1 },
    })
    .png(PNG_OPTS)
    .toFile(outPath);
  console.log('  ✓', outPath.replace(ROOT + '/', ''));
}

console.log('→ iOS assets');
await writePng(iconSvg, 1024, 1024,
  join(ROOT, 'apps/ios/assets/icon.png'),
  { background: { r: 255, g: 255, b: 255, alpha: 1 } });
await writePng(iconSvg, 1024, 1024,
  join(ROOT, 'apps/ios/assets/adaptive-icon.png'),
  { background: { r: 0, g: 0, b: 0, alpha: 0 } });

// splash: 1242×2688 흰 캔버스에 워드마크 60% width composite
const wordmarkForSplash = await sharp(wordmarkSvg, { density: 384 })
  .resize({ width: Math.round(1242 * 0.6) })
  .png()
  .toBuffer();
const splashOut = join(ROOT, 'apps/ios/assets/splash.png');
await ensureDir(splashOut);
await sharp({
  create: { width: 1242, height: 2688, channels: 4, background: { r: 255, g: 255, b: 255, alpha: 1 } },
})
  .composite([{ input: wordmarkForSplash, gravity: 'center' }])
  .png(PNG_OPTS)
  .toFile(splashOut);
console.log('  ✓', splashOut.replace(ROOT + '/', ''));

console.log('→ Web assets');
await writePng(iconSvg, 32, 32,
  join(ROOT, 'apps/web/app/favicon.ico'));
await writePng(iconSvg, 180, 180,
  join(ROOT, 'apps/web/public/apple-touch-icon.png'),
  { background: { r: 255, g: 255, b: 255, alpha: 1 } });

const wordmarkForOg = await sharp(wordmarkSvg, { density: 384 })
  .resize({ width: 600 })
  .png()
  .toBuffer();
const ogOut = join(ROOT, 'apps/web/public/og-default.png');
await ensureDir(ogOut);
await sharp({
  create: { width: 1200, height: 630, channels: 4, background: { r: 255, g: 255, b: 255, alpha: 1 } },
})
  .composite([{ input: wordmarkForOg, gravity: 'center' }])
  .png(PNG_OPTS)
  .toFile(ogOut);
console.log('  ✓', ogOut.replace(ROOT + '/', ''));

console.log('→ Pretendard fonts (iOS .otf × 4)');
const IOS_FONTS = join(ROOT, 'apps/ios/assets/fonts');
await mkdir(IOS_FONTS, { recursive: true });
for (const w of ['Regular', 'Medium', 'SemiBold', 'Bold']) {
  const src = join(PRETENDARD, `public/static/Pretendard-${w}.otf`);
  const dst = join(IOS_FONTS, `Pretendard-${w}.otf`);
  await copyFile(src, dst);
  console.log('  ✓', dst.replace(ROOT + '/', ''));
}
await copyFile(join(PRETENDARD, 'LICENSE.txt'), join(IOS_FONTS, 'LICENSE-Pretendard.txt'));
console.log('  ✓', join(IOS_FONTS, 'LICENSE-Pretendard.txt').replace(ROOT + '/', ''));

console.log('→ Pretendard fonts (Web .otf × 4)');
const WEB_FONTS = join(ROOT, 'apps/web/public/fonts');
await mkdir(WEB_FONTS, { recursive: true });
for (const w of ['Regular', 'Medium', 'SemiBold', 'Bold']) {
  const src = join(PRETENDARD, `public/static/Pretendard-${w}.otf`);
  const dst = join(WEB_FONTS, `Pretendard-${w}.otf`);
  await copyFile(src, dst);
  console.log('  ✓', dst.replace(ROOT + '/', ''));
}
await copyFile(join(PRETENDARD, 'LICENSE.txt'), join(WEB_FONTS, 'LICENSE-Pretendard.txt'));
console.log('  ✓', join(WEB_FONTS, 'LICENSE-Pretendard.txt').replace(ROOT + '/', ''));

console.log('→ Pretendard Bold .ttf for Phase 4 OG (Satori)');
const WEB_ASSETS = join(ROOT, 'apps/web/assets');
await mkdir(WEB_ASSETS, { recursive: true });
await copyFile(
  join(PRETENDARD, 'public/static/alternative/Pretendard-Bold.ttf'),
  join(WEB_ASSETS, 'Pretendard-Bold.ttf')
);
console.log('  ✓', join(WEB_ASSETS, 'Pretendard-Bold.ttf').replace(ROOT + '/', ''));

console.log('\n✓ All brand assets exported');
```

이 스크립트는 idempotent (같은 머신, 같은 sharp 버전이면 같은 PNG bytes — V8 검증). `withMetadata()` 호출 안 함 (EXIF/timestamp 자동 strip).

⚠️ workspace import에 .js extension 금지 (CLAUDE.md §4.5) — 본 파일은 node ESM이라 .mjs를 쓰고 외부 패키지만 import하므로 해당 사항 없음.

  </action>
  <verify>
    <automated>test -f packages/ui-tokens/src/brand/icon.svg &amp;&amp; test -f packages/ui-tokens/src/brand/wordmark.svg &amp;&amp; test -f packages/ui-tokens/src/brand/LICENSE-Pretendard.txt &amp;&amp; test -f packages/ui-tokens/scripts/export-assets.mjs &amp;&amp; grep -q "viewBox=\"0 0 1024 1024\"" packages/ui-tokens/src/brand/icon.svg &amp;&amp; grep -q "viewBox=\"0 0 500 100\"" packages/ui-tokens/src/brand/wordmark.svg &amp;&amp; ! grep -q "<text" packages/ui-tokens/src/brand/wordmark.svg &amp;&amp; ! grep -q "<text" packages/ui-tokens/src/brand/icon.svg &amp;&amp; grep -q "SIL Open Font License" packages/ui-tokens/src/brand/LICENSE-Pretendard.txt &amp;&amp; grep -q "import sharp" packages/ui-tokens/scripts/export-assets.mjs &amp;&amp; node -e "import('./packages/ui-tokens/scripts/export-assets.mjs').then(()=>{}).catch(e=>{if(e.message.includes('SyntaxError'))throw e})"</automated>
  </verify>
  <done>
    - icon.svg 존재, valid SVG (`<rect>` + `<path>` with brand `#F97316`), `<text>` 요소 없음 (Pitfall C)
    - wordmark.svg 존재, viewBox 500×100, `<text>` 요소 없음
    - LICENSE-Pretendard.txt에 "SIL Open Font License" 문자열 포함
    - export-assets.mjs가 syntactically valid한 ES module이며 sharp import 포함
    - 스크립트는 이 task에서 실행하지 않음 — 다음 task가 실행
  </done>
</task>

<task type="auto">
  <name>Task 3: export-assets 실행 → PNG/ICO/폰트 산출물 git commit</name>
  <files>
    apps/ios/assets/icon.png (generated),
    apps/ios/assets/adaptive-icon.png (generated),
    apps/ios/assets/splash.png (generated),
    apps/ios/assets/fonts/Pretendard-{Regular,Medium,SemiBold,Bold}.otf (copied),
    apps/ios/assets/fonts/LICENSE-Pretendard.txt (copied),
    apps/web/app/favicon.ico (generated),
    apps/web/public/apple-touch-icon.png (generated),
    apps/web/public/og-default.png (generated),
    apps/web/public/fonts/Pretendard-{Regular,Medium,SemiBold,Bold}.otf (copied),
    apps/web/public/fonts/LICENSE-Pretendard.txt (copied),
    apps/web/assets/Pretendard-Bold.ttf (copied)
  </files>
  <action>
**1. export-assets 스크립트 1회 실행**

```bash
pnpm --filter @moajoa/ui-tokens run export-assets
```

콘솔에 각 산출물 경로가 ✓로 표시되어야 함 (총 15개 출력 라인).

**2. V8 idempotency 검증 (RESEARCH §"sharp 결정성")**

같은 머신에서 한 번 더 실행하고 sha256 비교 — 다르면 sharp 버전 / OS-level 캐시 문제 의심:

```bash
shasum -a 256 apps/ios/assets/*.png apps/web/public/*.png apps/web/public/apple-touch-icon.png apps/web/app/favicon.ico > /tmp/run1.sha
pnpm --filter @moajoa/ui-tokens run export-assets
shasum -a 256 apps/ios/assets/*.png apps/web/public/*.png apps/web/public/apple-touch-icon.png apps/web/app/favicon.ico > /tmp/run2.sha
diff /tmp/run1.sha /tmp/run2.sha   # exit 0 expected (same machine)
```

(다른 머신에서는 byte-identical 보장 안 됨 — pixel-identical로 폴백. CI 부재라 문제 안 됨.)

**3. 산출물 파일 크기 sanity check**

- `apps/ios/assets/icon.png`: 1024×1024, 흰 배경 + orange shape → ~10-50KB
- `apps/ios/assets/splash.png`: 1242×2688, 대부분 흰색 → ~20-100KB
- `apps/web/assets/Pretendard-Bold.ttf`: 2,661,752 bytes (정확히 npm 패키지의 alternative/ 사본)
- `apps/ios/assets/fonts/Pretendard-Bold.otf`: 1,576,660 bytes (정확히 npm 패키지 사본)

모두 size > 0 확인. **byte-equal-to-source 폰트 사본**은 git LFS 없이도 OK (각 1.5MB).

**4. .gitignore 확인**

```bash
cat apps/ios/.gitignore 2>/dev/null
cat apps/web/.gitignore 2>/dev/null
cat .gitignore | grep -E "assets|fonts|\.png|\.otf|\.ttf" || true
```

만약 `assets/`나 `*.png`가 ignore된다면 본 task의 의도(committed assets)와 충돌. 발견 시 ignore 라인 제거 또는 더 좁은 패턴으로 수정. **현재 검증:** RESEARCH Open Question #1에서 `apps/ios/.gitignore`에 `ios/`가 있을 수 있다고 언급(이건 prebuild 산출물용). `assets/`는 ignore되지 않아야 함.

⚠️ NOT touch (D-16, Pitfall F): `apps/ios/app.config.ts`는 03-PLAN에서 다룸. 본 task는 자산 파일만.

  </action>
  <verify>
    <automated>test -f apps/ios/assets/icon.png &amp;&amp; test -f apps/ios/assets/adaptive-icon.png &amp;&amp; test -f apps/ios/assets/splash.png &amp;&amp; test -f apps/ios/assets/fonts/Pretendard-Regular.otf &amp;&amp; test -f apps/ios/assets/fonts/Pretendard-Medium.otf &amp;&amp; test -f apps/ios/assets/fonts/Pretendard-SemiBold.otf &amp;&amp; test -f apps/ios/assets/fonts/Pretendard-Bold.otf &amp;&amp; test -f apps/ios/assets/fonts/LICENSE-Pretendard.txt &amp;&amp; test -f apps/web/app/favicon.ico &amp;&amp; test -f apps/web/public/apple-touch-icon.png &amp;&amp; test -f apps/web/public/og-default.png &amp;&amp; test -f apps/web/public/fonts/Pretendard-Regular.otf &amp;&amp; test -f apps/web/public/fonts/Pretendard-Medium.otf &amp;&amp; test -f apps/web/public/fonts/Pretendard-SemiBold.otf &amp;&amp; test -f apps/web/public/fonts/Pretendard-Bold.otf &amp;&amp; test -f apps/web/public/fonts/LICENSE-Pretendard.txt &amp;&amp; test -f apps/web/assets/Pretendard-Bold.ttf &amp;&amp; [ "$(stat -f %z apps/ios/assets/fonts/Pretendard-Bold.otf)" = "1576660" ] &amp;&amp; [ "$(stat -f %z apps/web/assets/Pretendard-Bold.ttf)" = "2661752" ] &amp;&amp; file apps/ios/assets/icon.png | grep -q "PNG image data, 1024 x 1024"</automated>
  </verify>
  <done>
    - V5 검증 통과: 9 PNG/ICO 산출물 + 9 폰트 파일 + 3 LICENSE = 모두 존재
    - Pretendard-Bold.otf 정확히 1,576,660 bytes (npm 패키지 byte-equal)
    - Pretendard-Bold.ttf 정확히 2,661,752 bytes
    - icon.png is "PNG image data, 1024 x 1024"
    - 같은 머신 재실행 시 PNG sha256 일치 (V8 idempotency, manual verify 권장)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: 워드마크 SVG 디자인 검토 + 산출물 시각 승인</name>
  <what-built>
    - SVG single source 2개 (icon.svg / wordmark.svg)
    - 자동 생성된 9개 PNG/ICO 산출물
    - 9개 Pretendard 폰트 파일 + 3개 LICENSE 사본
  </what-built>
  <how-to-verify>
    1. `apps/ios/assets/icon.png`을 macOS Finder 또는 Preview로 연다 → 흰 배경 + 가운데 brand orange "M" 모양 (현재는 placeholder geometry — 디자이너 정교화 필요여부 결정).
    2. `apps/ios/assets/splash.png`을 연다 → 흰 배경 + 중앙에 워드마크 (현재는 6개 orange 사각형 placeholder).
    3. `apps/web/public/og-default.png`을 연다 → 1200×630 흰 배경 + 중앙 워드마크.
    4. **결정 포인트:** 워드마크 placeholder를 Phase 1 안에서 정교화할지(별도 task 추가) vs Phase 1은 파이프라인만 검증하고 placeholder 그대로 commit 한 채 Phase 1 종료 후 디자이너 작업으로 교체할지.
       - **권장:** 후자. 본 Phase 1의 검증 대상은 "파일 존재 + 파이프라인 작동". 디자인 정교화는 같은 wordmark.svg를 교체하고 `pnpm export-assets` 재실행만 하면 모든 산출물 자동 업데이트 (이게 SVG single source 도입의 목적).
    5. **확인:** icon mark의 16×16 favicon에서 가독성을 보고 싶다면 `apps/web/app/favicon.ico`를 Finder QuickLook 또는 브라우저 도구로 확인.

    Pitfall C 검증: SVG 안에 `<text>` 요소 절대 없어야 함 (font-dependent → 머신마다 다른 PNG):
    ```bash
    grep -c "<text" packages/ui-tokens/src/brand/*.svg
    # 출력: 0 (양쪽 파일 모두)
    ```
  </how-to-verify>
  <resume-signal>
    "approved" — 산출물 OK, placeholder 그대로 Phase 1 종료하고 디자이너 작업 별도 처리 (Wave 2 진행)
    OR
    "refine" — 워드마크 outline path를 본 Phase 1 안에서 정교화 (Task 5로 추가, 디자이너가 작업한 SVG content를 제공해주세요)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → git → other machines | 본 task가 만든 PNG/폰트가 다른 머신 빌드에서 동일하게 작동해야 함 (lockfile + committed 산출물) |
| node_modules/pretendard → apps/* | 외부 패키지 (SIL OFL 라이선스) source가 우리 bundle에 들어감 — 라이선스 동봉 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | export-assets.mjs idempotency | mitigate | sharp 옵션 고정 (`compressionLevel: 9, palette: false, progressive: false`), `<text>` 요소 SVG 금지 (font-dep 제거 — Pitfall C) |
| T-01-02 | Information Disclosure | EXIF/timestamp in committed PNG | mitigate | sharp default가 metadata strip; `withMetadata()` 호출 금지 |
| T-01-03 | Repudiation | Pretendard license attribution | mitigate | LICENSE-Pretendard.txt를 3 위치(iOS/Web/ui-tokens)에 동봉 (SIL OFL 1.1 §"Reserved Font Name" 준수) |
| T-01-04 | Elevation of Privilege | pnpm hoisting scope creep | mitigate | apps/ios/.npmrc 한 파일에 한정 (D-02), 루트 .npmrc unchanged grep 검증 (Pitfall B) |
| T-01-05 | Denial of Service | bundle size bloat (~6.3MB iOS) | accept | UI-SPEC "~1.6MB" 표기는 부정확이나 ~6.3MB도 acceptable (전체 앱 < 30MB 예상), 4 weight 모두 startup 필요 (UI-SPEC contract) |
</threat_model>

<verification>
**Phase 종합 검증 (이 plan 끝):**
- V1: `pnpm -r --filter "@moajoa/*" typecheck` → exit 0
- V5: `ls -la apps/ios/assets/{icon,adaptive-icon,splash}.png apps/web/app/favicon.ico apps/web/public/{apple-touch-icon,og-default}.png apps/web/assets/Pretendard-Bold.ttf` → 모든 파일 size > 0
- V8: `pnpm --filter @moajoa/ui-tokens run export-assets` 2회 실행 + sha256 diff exit 0 (same machine)
- Pitfall B: `grep "node-linker" .npmrc` → 출력 없음
- Pitfall C: `grep "<text" packages/ui-tokens/src/brand/*.svg` → 출력 없음 (count 0)
- Pitfall F: `grep "expo-share-intent" apps/ios/package.json` → 출력 없음 (D-16 lock)
- Pitfall G: `git status pnpm-lock.yaml` → modified or clean (커밋된 상태)
</verification>

<success_criteria>
- 모든 산출물 파일 17개 (PNG 6 + ICO 1 + OTF 8 + TTF 1 + LICENSE 3 + SVG 2) 존재
- export-assets.mjs는 manually invokable + idempotent (same machine)
- 다음 plan들이 의존하는 prerequisite 충족:
  - 02-PLAN(iOS 빌드): `apps/ios/.npmrc` 존재 + nativewind 4.2.4 + worklets peer 설치됨
  - 03-PLAN(app.config.ts wire-up): `apps/ios/assets/{icon,adaptive-icon,splash}.png` + `apps/ios/assets/fonts/*.otf` 존재
  - 05-PLAN(web dev-tool gate): `apps/web/public/fonts/*.otf` 존재 (next/font/local source)
  - Phase 4 (consumer): `apps/web/assets/Pretendard-Bold.ttf` 존재
- lockfile freeze 완료 (Pitfall G 방어)
</success_criteria>

<output>
완료 후 `.planning/phases/01-build-unblock-hygiene/01-01-SUMMARY.md` 생성. SUMMARY에 다음 기록:
- 실제 PNG sha256 (다음 머신 검증용 reference)
- placeholder wordmark가 committed된 상태인지 / 정교화된 path로 교체된 상태인지
- `pnpm install` 출력에서 어떤 추가 transitive deps이 들어왔는지 (worklets / sharp)
- `apps/ios/.gitignore` 안의 `assets/` 관련 패턴 여부
</output>
