---
phase: 29-chat-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0032_join_moa_by_poll_code.sql
  - packages/api/src/types/database.ts
  - supabase/tests/web_share_smoke.sh
  - packages/api/src/queries/memberships.ts
  - packages/api/src/queries/memberships.test.ts
autonomous: true
requirements: [CHAT-04, CHAT-05]

must_haves:
  truths:
    - "poll_code 보유자가 익명 세션으로 trip에 voter로 합류할 수 있다 (slug 없이, private 레거시 trip 포함)"
    - "voter role 멤버가 trip_messages를 읽고(200) 쓸 수 있다(201) — DB 레이어 실증"
    - "0016~0031 기존 마이그레이션은 단 한 바이트도 변하지 않는다"
  artifacts:
    - path: "supabase/migrations/0032_join_moa_by_poll_code.sql"
      provides: "join_moa_by_poll_code DEFINER RPC (poll_code bearer → voter 멤버십)"
      contains: "security definer"
    - path: "packages/api/src/queries/memberships.ts"
      provides: "joinMoaByPollCode typed wrapper"
      exports: ["joinMoaByPollCode"]
    - path: "packages/api/src/types/database.ts"
      provides: "typegen 재생성 — join_moa_by_poll_code Functions 타입"
      contains: "join_moa_by_poll_code"
  key_links:
    - from: "packages/api/src/queries/memberships.ts"
      to: "join_moa_by_poll_code RPC"
      via: "client.rpc"
      pattern: "rpc\\('join_moa_by_poll_code'"
    - from: "supabase/tests/web_share_smoke.sh"
      to: "trip_messages RLS (voter)"
      via: "curl REST 프로브"
      pattern: "join_moa_by_poll_code"
---

<objective>
Phase 29 백엔드 전제를 한 append-only 마이그레이션(0032)으로 봉합: `/poll/[code]` 방문자가 poll_code만으로(slug 없이) trip에 voter로 self-join하는 `join_moa_by_poll_code` DEFINER RPC + api typed wrapper + voter의 trip_messages RLS 통과 smoke 실증.

Purpose: CHAT-05(/poll 방문자가 통일 채팅 저장소에 합류)의 유일한 신규 백엔드. 기존 RPC로는 poll_code→멤버십 경로가 없고(RESEARCH Q1 — poll_view_by_code는 slug 미반환, slug 노출은 bearer 스코프 붕괴), 레거시 dateless-poll trip은 `visibility='private'`·slug null이라 join_moa 경유가 구조적으로 불가.
Output: 0032 SQL + database.ts 재생성 + `joinMoaByPollCode` 래퍼 + web_share_smoke.sh (7) 케이스.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/29-chat-unification/29-CONTEXT.md
@.planning/phases/29-chat-unification/29-RESEARCH.md (Q1 — 0032 SQL 전문 스펙)
@.planning/phases/29-chat-unification/29-PATTERNS.md (§5 마이그레이션 analog, §6 래퍼 analog, §10 smoke analog)

<interfaces>
<!-- 기존 joinMoa 래퍼 (packages/api/src/queries/memberships.ts:17-31) — 신규 함수의 verbatim 미러 원형 -->
```typescript
export async function joinMoa(
  client: MoajoaSupabaseClient,
  shareSlug: string,
): Promise<string> {
  const { data, error } = await client.rpc('join_moa', { p_share_slug: shareSlug });
  if (error) throw error;
  return data as string;
}
```
<!-- 기존 테스트 하네스 (packages/api/src/queries/memberships.test.ts:13-35) -->
```typescript
function makeRpcClient(result: { data: unknown; error: unknown }) {
  const rpc = vi.fn(() => Promise.resolve(result));
  const client = { rpc } as unknown as MoajoaSupabaseClient;
  return { client, rpc };
}
// expect(rpc).toHaveBeenCalledWith('join_moa', { p_share_slug: SLUG });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 0032_join_moa_by_poll_code.sql 생성 (append-only)</name>
  <files>supabase/migrations/0032_join_moa_by_poll_code.sql</files>
  <read_first>
    - supabase/migrations/0025_web_share.sql (join_moa 본문 :64-99 — 안전장치 5종 미러 원형)
    - supabase/migrations/0029_public_trip_poll.sql (헤더 주석 관례 :1-10 · grant 스코프 idiom :156)
    - .planning/phases/29-chat-unification/29-RESEARCH.md의 Q1 섹션 (SQL 전문)
  </read_first>
  <action>
`supabase/migrations/0032_join_moa_by_poll_code.sql` 신규 생성. 본문은 RESEARCH Q1 스펙 verbatim (D-03a — poll_code 자체를 bearer로 사용, slug 미경유):

```sql
create or replace function join_moa_by_poll_code(p_code text)
returns uuid
language plpgsql
security definer
set search_path = public
as $$
declare
  v_trip_id uuid;
  v_owner_id uuid;
begin
  -- bearer 코드 검증: poll_code 보유 = 이 trip의 dates-scope 참여 자격.
  -- status 게이트 없음: 채팅은 trip 소속(A-8) — poll 마감 후에도 대화 유지 (Assumptions A1).
  -- visibility 게이트 없음: 레거시 dateless-poll trip은 private·slug null (RESEARCH Q1).
  select p.trip_id, t.owner_id into v_trip_id, v_owner_id
  from date_polls p join trips t on t.id = p.trip_id
  where p.poll_code = p_code
  limit 1;

  if v_trip_id is null then
    raise exception 'poll not found';
  end if;

  if v_owner_id = auth.uid() then
    return v_trip_id;
  end if;

  -- poll_code는 dates 시맨틱 → role 고정 'voter' (join_moa D-A1 dates 분기 미러).
  insert into memberships (trip_id, user_id, role, accepted_at)
  values (v_trip_id, auth.uid(), 'voter', now())
  on conflict (trip_id, user_id) do nothing;  -- 멱등, 자동 승격 없음 (D-A4)

  return v_trip_id;
end;
$$;

grant execute on function join_moa_by_poll_code(text) to authenticated;
-- anon grant 없음: 익명이라도 세션 필수 (0029 cast_date_vote_authed 선례)
```

파일 헤더에 0029:1-10 관례대로 결정 근거 주석 추가(append-only 선언·§4.4 준수·D-03a/CHAT-05 참조). 기존 0016~0031 파일은 무접촉. 신규 RLS 정책 0 — DEFINER 본문 내 조인은 42P17 무관. date_comments DROP 문 금지(D-02a).
  </action>
  <verify>
    <automated>test -f supabase/migrations/0032_join_moa_by_poll_code.sql && grep -q "security definer" supabase/migrations/0032_join_moa_by_poll_code.sql && grep -q "set search_path = public" supabase/migrations/0032_join_moa_by_poll_code.sql && grep -q "'voter'" supabase/migrations/0032_join_moa_by_poll_code.sql && grep -q "on conflict (trip_id, user_id) do nothing" supabase/migrations/0032_join_moa_by_poll_code.sql && ! grep -qi "to anon" supabase/migrations/0032_join_moa_by_poll_code.sql && ! grep -qiE "drop table|alter table date_comments" supabase/migrations/0032_join_moa_by_poll_code.sql && git diff --name-only supabase/migrations/ | grep -v 0032 | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/0032_join_moa_by_poll_code.sql` 존재, `create or replace function join_moa_by_poll_code(p_code text)` 포함
    - `security definer` + `set search_path = public` + `grant execute on function join_moa_by_poll_code(text) to authenticated` 전부 존재
    - `to anon` grant 0건 · `create policy` 0건 · `drop` 0건
    - `git status`상 supabase/migrations/ 변경 파일은 0032 신규 1개뿐 (0016~0031 무수정)
    - role 리터럴은 `'voter'` 고정 (share_mode 분기 없음 — poll_code는 dates 시맨틱)
  </acceptance_criteria>
  <done>0032 파일이 join_moa 안전장치 5종(bearer 검증·self-join auth.uid·owner 가드·멱등·search_path 핀+authenticated grant)을 전부 계승한 상태로 존재</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] 로컬 적용 — db reset(0016→0032) + typegen + smoke (7) append·실행</name>
  <files>packages/api/src/types/database.ts, supabase/tests/web_share_smoke.sh</files>
  <read_first>
    - supabase/tests/web_share_smoke.sh 전문 (섹션 (3) :42-50 join_moa dates→voter 프로브 · (4) :52-58 trip_messages RLS 프로브 · (6) :65-127 게스트 익명 idiom — curl RPC :91-92, trip_messages POST :114-117, psql 시드 :120-122)
    - .planning/phases/29-chat-unification/29-PATTERNS.md §10 (smoke append 코드 excerpt)
  </read_first>
  <action>
**선행 확인 (env 제약):** colima+docker 기동 필요 — `docker info` 실패 시 `colima start` 후 `supabase start`. (프로젝트 메모리: supabase 로컬 스택은 colima 필수.)

**(a) 적용:** `supabase db reset` — 0016→0032 클린 적용 확인 (출력에 `0032_join_moa_by_poll_code` 라인 존재, `42P17` 문자열 0건).

**(b) typegen:** `pnpm supabase:types` → `packages/api/src/types/database.ts` 재생성. diff는 `join_moa_by_poll_code` Functions 항목 additive만이어야 함 (스키마 변경 없음 — 테이블 diff 0).

**(c) smoke append:** `supabase/tests/web_share_smoke.sh`에 섹션 `(7) join_moa_by_poll_code — poll_code bearer voter join + trip_messages RLS (CHAT-04/05)` append-only 추가. 기존 (1)~(6) 라인 무수정. 내용 3프로브 (기존 idiom 재사용):
  1. **poll_code 시드:** 기존 (d) 블록(:120-122)의 psql 패턴으로 T_DATES(또는 신규 dates trip)의 `date_polls.poll_code` 획득 (`psql -q` — INSERT RETURNING 캡처 시 -q 필수, 23-04 학습). poll이 없으면 psql로 date_polls 행 시드(ensure_poll_code 트리거가 poll_code 자동 발급).
  2. **fresh 익명 join:** 신규 익명 세션 발급(섹션 (1) idiom) → `curl -X POST "$API/rest/v1/rpc/join_moa_by_poll_code" ... -d "{\"p_code\":\"$PCODE\"}"` → HTTP 200 + psql로 `memberships.role='voter'` 단언 (:47-50 idiom — boolean 비교는 text 캐스트 주의). 잘못된 코드(`"nope"`)로 재호출 → HTTP 400 (poll not found) 단언.
  3. **voter trip_messages 프로브:** 같은 세션으로 `POST /rest/v1/trip_messages` (trip_id·nickname·body 3키 — user_id는 0028 트리거 몫) → **201** + `GET /rest/v1/trip_messages?trip_id=eq....` → **200** + 본문 비어있지 않음 단언. 이것이 CHAT-04의 DB 측 실증(voter role RLS 통과 — RESEARCH Q3).

**(d) 실행:** `bash supabase/tests/web_share_smoke.sh` → exit 0 (기존 (1)~(6) 무회귀 포함).

**주의:** 원격 push는 이 task 범위 밖 — human-action 게이트(29-04 Task 3). 라이브 /poll join은 배포 후 동작(25-01 선례).
  </action>
  <verify>
    <automated>grep -q "join_moa_by_poll_code" packages/api/src/types/database.ts && grep -q "join_moa_by_poll_code" supabase/tests/web_share_smoke.sh && bash supabase/tests/web_share_smoke.sh</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` 출력에 `0032_join_moa_by_poll_code` 적용 라인 존재 · `42P17` 0건
    - `packages/api/src/types/database.ts`에 `join_moa_by_poll_code` 존재 (Functions 타입)
    - `web_share_smoke.sh` 기존 (1)~(6) 라인 diff 0 (append-only — `git diff`로 확인), 신규 (7) 섹션 존재
    - `bash supabase/tests/web_share_smoke.sh` exit 0 — voter trip_messages POST 201 + GET 200 + join role='voter' + bad code 400 전부 포함
  </acceptance_criteria>
  <done>로컬 DB에 0032 적용·typegen 반영·smoke (7) 그린 — Wave 3(/poll 래퍼)의 false-positive 위험 해소</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: joinMoaByPollCode api 래퍼 (TDD RED→GREEN)</name>
  <files>packages/api/src/queries/memberships.ts, packages/api/src/queries/memberships.test.ts</files>
  <read_first>
    - packages/api/src/queries/memberships.ts (joinMoa :17-31 — verbatim 미러 원형 + JSDoc 관례)
    - packages/api/src/queries/memberships.test.ts (makeRpcClient :13-35 + joinMoa describe — 케이스 3종 원형)
  </read_first>
  <behavior>
    - Test 1: `joinMoaByPollCode(client, CODE)` → `rpc('join_moa_by_poll_code', { p_code: CODE })` 정확 1회 호출
    - Test 2: rpc data가 uuid string이면 그대로 반환 (`data as string`)
    - Test 3: rpc error면 그 error를 throw
  </behavior>
  <action>
**RED (commit `test(29-01): add failing joinMoaByPollCode cases`):** memberships.test.ts에 `join_moa_by_poll_code` describe append — makeRpcClient 재사용, 위 3케이스. memberships.ts에는 아직 미구현(또는 throw 스텁)이라 behavioral 실패 확인 (임포트 에러가 아닌 단언 실패가 되도록 필요 시 미구현 스텁 export — 26-01 RED idiom).

**GREEN (commit `feat(29-01): joinMoaByPollCode wrapper`):** memberships.ts에 joinMoa(:24-31) verbatim 미러 append — RPC명 `'join_moa_by_poll_code'`·인자 `{ p_code: code }`만 교체. JSDoc에 명시: poll_code bearer → role 고정 'voter'(D-A1 dates 미러)·멱등(D-A4)·visibility 게이트 없음(레거시 private dateless-poll trip 커버)·grant authenticated만(익명도 세션 필수). 워크스페이스 import `.js` extension 금지. 기존 joinMoa/joinSharedTrip/getMyTripRole/listTripMembers 함수 본문 diff 0 (surgical §3.3).
  </action>
  <verify>
    <automated>CI=true pnpm --filter @moajoa/api test && pnpm --filter @moajoa/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rpc('join_moa_by_poll_code', { p_code" packages/api/src/queries/memberships.ts` 1건
    - `grep -c "join_moa_by_poll_code" packages/api/src/queries/memberships.test.ts` ≥ 2 (describe + 인자 단언)
    - `CI=true pnpm --filter @moajoa/api test` exit 0 (기존 memberships 케이스 무회귀)
    - `pnpm --filter @moajoa/api typecheck` exit 0
    - `git diff packages/api/src/queries/memberships.ts`에서 기존 함수 본문 삭제 라인 0
    - `! grep -n "from '.*\.js'" packages/api/src/queries/memberships.ts` (`.js` extension 0)
  </acceptance_criteria>
  <done>api 스위트 그린 + typecheck 0 — 29-04 래퍼가 import할 typed seam 완비</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon 클라이언트 → PostgREST RPC | poll_code(bearer)가 유일한 자격 증명으로 넘어오는 지점 |
| RPC → memberships INSERT | DEFINER 권한으로 RLS를 우회해 멤버십을 만드는 지점 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Elevation of Privilege | join_moa_by_poll_code (poll_code → trip 스코프 확장) | mitigate | role 고정 `'voter'`(editor 아님)·share_slug 미반환/미노출(bearer 스코프 분리 유지, RESEARCH Q1)·`on conflict do nothing`(재-join 승격 불가, D-A4) |
| T-29-02 | Tampering | 클라이언트 trip_id 신뢰 | mitigate | RPC가 poll_code에서 trip을 서버 파생 — 클라이언트 trip_id 인자 자체가 없음 (0018 idiom) |
| T-29-03 | Spoofing | self-join 대상 사칭 | mitigate | INSERT user_id는 `auth.uid()` 서버값만 — 사용자 인자 0. grant authenticated만(익명도 세션 필수) |
| T-29-04 | Information Disclosure | voter 멤버십의 trip 전체 read(places/links/messages) | accept | dates 시맨틱 trip은 통상 장소 0 — /t dates 공유와 동일 노출 상한 (RESEARCH A3, 사용자 D-03a 잠금) |
| T-29-05 | Denial of Service | RLS 재귀 42P17 | mitigate | 신규 정책 0 — DEFINER 함수 본문 내 조인만. db reset에서 42P17=0 게이트 |
| T-29-06 | Information Disclosure | closed poll의 코드 보유자 합류 지속 | accept | 채팅은 trip 소속(A-8) — 마감은 신규 참여 차단이 아님 (RESEARCH A1, 설계 판단 기록) |
</threat_model>

<verification>
- `bash supabase/tests/web_share_smoke.sh` exit 0 — (1)~(6) 무회귀 + (7) 신규 그린
- `CI=true pnpm --filter @moajoa/api test && pnpm --filter @moajoa/api typecheck` exit 0
- `git diff --stat`에 apps/ios 0파일 · 0016~0031 마이그레이션 0파일
</verification>

<success_criteria>
- poll_code만으로 익명 세션이 voter 멤버가 되는 서버 경로 존재·smoke 실증 (CHAT-05 백엔드)
- voter role trip_messages INSERT 201/SELECT 200 실증 (CHAT-04 DB 측 — RESEARCH Q3 "마이그레이션 불필요" 판정의 런타임 확인)
- database.ts에 신규 RPC 타입 반영 — 29-04가 탐색 없이 import 가능
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-unification/29-01-SUMMARY.md`
</output>
