---
phase: 23-web-first-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0024_place_seq.sql
  - supabase/tests/place_seq_concurrency.sh
autonomous: true
requirements: [MOA-01]
must_haves:
  truths:
    - "0024 마이그레이션이 advisory-lock DEFINER 트리거 + trips 카운터 + backfill을 한 파일에 담는다"
    - "동시성 하네스가 무중복·무결번 + hard-delete 무재사용 + soft-delete 순번 유지 + forge 차단을 exit code로 단언한다"
  artifacts:
    - path: supabase/migrations/0024_place_seq.sql
      provides: "places.seq_no + trips.last_place_seq + backfill + assign_place_seq() 트리거"
      contains: "pg_advisory_xact_lock"
    - path: supabase/tests/place_seq_concurrency.sh
      provides: "MOA-01 동시성 검증 하네스 (실행은 23-04)"
      contains: "xargs -P 8"
  key_links:
    - from: supabase/migrations/0024_place_seq.sql
      to: "places BEFORE INSERT"
      via: "create trigger places_seq_before_insert"
      pattern: "before insert on places"
    - from: "assign_place_seq()"
      to: "trips.last_place_seq"
      via: "UPDATE ... RETURNING → NEW.seq_no"
      pattern: "returning last_place_seq into new.seq_no"
---

<objective>
MOA-01(순번 영구 채번)의 DB 기반을 만든다: `0024_place_seq.sql` — places.seq_no 컬럼 + trips.last_place_seq 단조증가 카운터 + 기존 행 backfill + advisory-lock SECURITY DEFINER 트리거 — 와 이를 검증할 SQL 동시성 하네스(Wave 0 산출물).

Purpose: 웹·iOS·EF 3개 insert 경로 어디서 동시 추가돼도 순번이 중복·결번 없이 채번되고, 어떤 삭제 경로에서도 번호가 재부여되지 않는 유일한 관문은 DB 트리거다.
Output: `supabase/migrations/0024_place_seq.sql`, `supabase/tests/place_seq_concurrency.sh` (실행·적용은 23-04 [BLOCKING] 플랜).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-web-first-foundation/23-RESEARCH.md
@.planning/phases/23-web-first-foundation/23-PATTERNS.md

핵심 결정 (이 플랜에 잠김):
- `max(seq_no)+1` 금지 — places hard DELETE 정책(0016 L488~494)이 실존하므로 최고 순번 행 hard delete 시 번호 재사용 → MOA-01 위반. 카운터 방식만 허용 (RESEARCH Pitfall 1).
- 트리거는 `security definer set search_path = public` 필수 — editor/익명 멤버는 trips UPDATE RLS가 없어 invoker면 insert가 실패 (RESEARCH Pitfall 2).
- 네이밍(A7): `seq_no` / `last_place_seq` / `assign_place_seq` / `places_seq_before_insert` / `places_trip_seq_key`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: MOA-01 동시성 하네스 스크립트 작성 (Wave 0)</name>
  <files>supabase/tests/place_seq_concurrency.sh</files>
  <read_first>
    - .planning/phases/23-web-first-foundation/23-RESEARCH.md (Code Example 2 — 하네스 원형, 접속 문자열)
    - supabase/migrations/0016_trips_baseline.sql L426~452 (places 컬럼: trip_id·added_by·name_local·lat·lng·hidden_at — INSERT 문 컬럼 정합 검증용)
  </read_first>
  <action>
`supabase/tests/` 디렉토리를 새로 만들고(레포 최초 bash SQL 하네스 — analog 없음, RESEARCH Code Example 2가 레퍼런스) 아래 스크립트를 그대로 작성한다. superuser psql이라 RLS는 우회하지만 트리거는 동일하게 발화한다는 점이 이 하네스의 전제다.

```bash
#!/usr/bin/env bash
# MOA-01 동시성 하네스 — 0024 assign_place_seq 트리거 검증.
# 전제: supabase local 가동 + 0024 적용 (23-04 [BLOCKING] 이후 실행).
# 단언: (1) 동시 40건 무중복·무결번 (2) soft-delete 복원 시 순번 유지
#       (3) hard-delete 후 번호 무재사용 (4) 클라이언트 seq_no forge 차단
set -euo pipefail
DB="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
API="http://127.0.0.1:54321"

ANON_KEY=$(supabase status -o env 2>/dev/null | grep ANON_KEY | cut -d= -f2 | tr -d '"')

# 테스트 유저 확보 (email signup — 로컬 autoconfirm. 이미 있으면 signup은 무시)
EMAIL="seqtest@local.test"
curl -s -X POST "$API/auth/v1/signup" \
  -H "apikey: $ANON_KEY" -H "Content-Type: application/json" \
  -d "{\"email\":\"$EMAIL\",\"password\":\"password-seqtest-1\"}" > /dev/null || true
USER_ID=$(psql "$DB" -tAc "select id from auth.users where email='$EMAIL' limit 1")
[ -n "$USER_ID" ] || { echo "FAIL: test user missing"; exit 1; }

TRIP=$(psql "$DB" -tAc "insert into trips (owner_id, title) values ('$USER_ID','seq-test-$(date +%s)') returning id")

# (1) 동시 40건 삽입, 8-way 병렬
seq 1 40 | xargs -P 8 -I{} psql "$DB" -qc \
  "insert into places (trip_id, added_by, name_local, lat, lng)
   values ('$TRIP','$USER_ID','p{}',35.0,139.0)"

RES=$(psql "$DB" -tAc "select count(*)||'|'||count(distinct seq_no)||'|'||max(seq_no) from places where trip_id='$TRIP'")
[ "$RES" = "40|40|40" ] || { echo "FAIL: concurrency — got $RES (want 40|40|40)"; exit 1; }

# (2)(3) soft delete·복원 + 최고번호 hard delete 후 재삽입
psql "$DB" -qc "update places set hidden_at = now() where trip_id='$TRIP' and seq_no = 3"
psql "$DB" -qc "delete from places where trip_id='$TRIP' and seq_no = 40"
psql "$DB" -qc "insert into places (trip_id, added_by, name_local, lat, lng) values ('$TRIP','$USER_ID','after-delete',35,139)"
psql "$DB" -qc "update places set hidden_at = null where trip_id='$TRIP' and seq_no = 3"

NEWSEQ=$(psql "$DB" -tAc "select seq_no from places where trip_id='$TRIP' and name_local='after-delete'")
[ "$NEWSEQ" = "41" ] || { echo "FAIL: hard-delete reuse — got seq $NEWSEQ (want 41, 40 재사용 금지)"; exit 1; }
KEPT=$(psql "$DB" -tAc "select seq_no||'|'||(hidden_at is null) from places where trip_id='$TRIP' and seq_no=3")
[ "$KEPT" = "3|t" ] || { echo "FAIL: soft-delete restore — got $KEPT (want 3|t)"; exit 1; }

# (4) forge 차단: 클라이언트가 seq_no=999를 보내도 트리거가 덮어씀
FORGE=$(psql "$DB" -tAc "insert into places (trip_id, added_by, name_local, lat, lng, seq_no) values ('$TRIP','$USER_ID','forge',35,139,999) returning seq_no")
[ "$FORGE" = "42" ] || { echo "FAIL: forge guard — got seq $FORGE (want 42)"; exit 1; }

echo "PASS: place_seq concurrency (40|40|40, hard-delete→41, soft-restore 3|t, forge→42)"
```

`chmod +x supabase/tests/place_seq_concurrency.sh` 실행 권한 부여.
  </action>
  <acceptance_criteria>
    - `supabase/tests/place_seq_concurrency.sh` 파일 존재 + 실행 권한 (`test -x` 통과)
    - `bash -n supabase/tests/place_seq_concurrency.sh` exit 0 (문법 클린)
    - 파일에 `xargs -P 8`, `40|40|40`, `hard-delete`, `seq_no=3`, `999` 전부 grep 매치
    - `set -euo pipefail` 존재
  </acceptance_criteria>
  <verify>
    <automated>bash -n supabase/tests/place_seq_concurrency.sh && test -x supabase/tests/place_seq_concurrency.sh && grep -q "xargs -P 8" supabase/tests/place_seq_concurrency.sh</automated>
  </verify>
  <done>MOA-01 4개 시나리오(동시성·soft-delete·hard-delete·forge)를 exit code로 단언하는 자기완결 하네스가 존재한다. 실행은 23-04.</done>
</task>

<task type="auto">
  <name>Task 2: 0024_place_seq.sql 마이그레이션 작성</name>
  <files>supabase/migrations/0024_place_seq.sql</files>
  <read_first>
    - supabase/migrations/0023_ledger_update_check.sql (append-only 패치 헤더 스타일 — 파일명·Phase/REQ·Why 단락·"Append-only: 0016..0023 are NEVER modified" 마무리)
    - supabase/migrations/0016_trips_baseline.sql L78~98 (handle_new_auth_user — DEFINER+search_path 함수 shape), L466~468 (BEFORE INSERT 트리거 부착 shape), L488~494 (places hard DELETE 정책 — 카운터 방식 채택의 직접 근거)
    - .planning/phases/23-web-first-foundation/23-RESEARCH.md (Code Example 1 — 완성 SQL)
  </read_first>
  <action>
`supabase/migrations/0024_place_seq.sql`을 새로 생성한다 (0016~0023 무수정 — append-only §4.3). 0023 스타일 헤더 주석(파일명 — Phase 23 Web-First Foundation, MOA-01 — Why: hard DELETE 정책이 실존하므로 max+1은 번호 재사용, 카운터는 단조증가라 재부여 불가 — Append-only: 0016..0023 are NEVER modified) 뒤에 아래 SQL을 그대로 넣는다. 순서가 곧 정합성이다(Pitfall 3): ①nullable 추가 → ②backfill(tiebreak=id) → ③not null 승격 → ④unique 인덱스 → ⑤카운터 동기화 → ⑥트리거.

```sql
alter table trips add column last_place_seq int not null default 0;
alter table places add column seq_no int;

-- backfill: created_at 순, 동률(같은 추출 배치)은 id로 tiebreak. hidden 행 포함(순번 영구성).
with numbered as (
  select id, row_number() over (partition by trip_id order by created_at, id) as rn
  from places
)
update places p set seq_no = n.rn from numbered n where p.id = n.id;

update trips t set last_place_seq = coalesce(
  (select max(seq_no) from places where trip_id = t.id), 0);

alter table places alter column seq_no set not null;
create unique index places_trip_seq_key on places (trip_id, seq_no);

-- 채번 트리거. security definer 필수: places INSERT는 editor 허용(0016 L477~480)이지만
-- trips UPDATE는 owner 전용(0016 L187~191) — invoker면 editor insert 시 트리거 실패 (Pitfall 2).
create or replace function assign_place_seq()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  -- 클라이언트 제공 seq_no는 신뢰하지 않음 — 항상 트리거가 채번 (forge 차단)
  perform pg_advisory_xact_lock(hashtextextended('place_seq:' || new.trip_id::text, 0));
  update trips set last_place_seq = last_place_seq + 1
    where id = new.trip_id
    returning last_place_seq into new.seq_no;
  if new.seq_no is null then
    raise exception 'trip % not found for seq assignment', new.trip_id;
  end if;
  return new;
end;
$$;

create trigger places_seq_before_insert
  before insert on places
  for each row execute function assign_place_seq();
```

주의: `grant execute`는 불필요 — 트리거 함수는 직접 호출되지 않는다. 기존 마이그레이션 파일은 단 한 줄도 수정하지 않는다.
  </action>
  <acceptance_criteria>
    - `supabase/migrations/0024_place_seq.sql` 존재, 0016~0023 파일 diff 없음 (`git diff --name-only supabase/migrations/ | grep -v 0024` 출력 0줄)
    - 파일에 `pg_advisory_xact_lock`, `hashtextextended`, `security definer`, `set search_path = public`, `last_place_seq`, `places_trip_seq_key`, `before insert on places` 전부 grep 매치
    - backfill이 `row_number() over (partition by trip_id order by created_at, id)` 정확 포함 (tiebreak=id)
    - `alter column seq_no set not null`이 backfill UPDATE **뒤**에 위치 (`grep -n`으로 라인 순서 확인)
    - 헤더에 `Append-only` 문구 존재
  </acceptance_criteria>
  <verify>
    <automated>grep -q "pg_advisory_xact_lock" supabase/migrations/0024_place_seq.sql && grep -q "set search_path = public" supabase/migrations/0024_place_seq.sql && grep -q "order by created_at, id" supabase/migrations/0024_place_seq.sql && test "$(git diff --name-only supabase/migrations/ | grep -cv 0024_place_seq)" = "0"</automated>
  </verify>
  <done>0024가 카운터 채번 + backfill + DEFINER 트리거를 한 파일에 담고, 기존 마이그레이션은 무수정. 실제 적용·트리거 발화 검증은 23-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → places INSERT | web/iOS/EF 3개 경로 — 클라이언트가 임의 seq_no를 실어 보낼 수 있음 |
| 트리거 → trips UPDATE | editor/익명 멤버 세션이 owner 전용 RLS를 넘어 카운터를 갱신해야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | places.seq_no (클라이언트 forge) | mitigate | 트리거가 클라이언트 값 무시하고 항상 재채번 + `unique (trip_id, seq_no)` — 하네스 (4)가 단언 |
| T-23-02 | Elevation | assign_place_seq() DEFINER | mitigate | `set search_path = public` 핀(0019/0020 선례) + 함수는 trips 카운터 UPDATE 단일 문장만 수행, 인자 없는 트리거 함수라 호출 표면 없음 |
| T-23-03 | DoS | advisory lock (trip 단위 직렬화) | accept | 락 키가 trip_id 스코프 — 다른 trip은 무경합, 같은 trip 직렬화는 MOA-01의 의도된 동작. 트랜잭션 종료 시 자동 해제라 leak 없음 |
</threat_model>

<verification>
- `bash -n` 하네스 문법 클린 + 실행 권한
- 0024 정적 grep 전부 통과 (advisory lock·DEFINER·search_path·backfill 순서)
- 기존 마이그레이션 0016~0023 무수정 (git diff)
- 런타임 검증(reset·하네스 실행)은 23-04에서 — 이 플랜 단독으로 "적용됨"을 주장하지 않는다
</verification>

<success_criteria>
- 0024_place_seq.sql이 RESEARCH Code Example 1 shape 그대로 존재
- place_seq_concurrency.sh가 4개 시나리오를 exit code로 단언하는 자기완결 스크립트로 존재
- append-only 룰 위반 0
</success_criteria>

<output>
완료 후 `.planning/phases/23-web-first-foundation/23-01-SUMMARY.md` 생성.
</output>
