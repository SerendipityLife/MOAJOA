---
phase: 25-guest-unified-share
plan: 04
type: execute
wave: 3
depends_on: ["25-03"]
files_modified:
  - apps/web/app/t/[slug]/_components/guest-promote.tsx
  - apps/web/app/t/[slug]/_components/__tests__/guest-promote.test.tsx
  - apps/web/app/t/[slug]/page.tsx
  - supabase/config.toml
autonomous: false
requirements: [AUTH-08]
user_setup:
  - service: supabase
    why: "linkIdentity(D-03)는 Manual linking 활성화 전제 — 미활성 시 런타임 에러"
    dashboard_config:
      - task: "Authentication → Manual linking 토글 ON (원격 대시보드) — config.toml enable_manual_linking=true와 짝"
        location: "Supabase Dashboard → Authentication → Settings"

must_haves:
  truths:
    - "게스트 화면에 '로그인하고 내 여행에 담기' 진입점이 렌더되고, 클릭 시 linkIdentity({provider:'kakao'})가 1회 호출된다 (D-03 최소 심)"
    - "linkIdentity가 익명 auth.uid를 보존한 채 카카오 identity를 부착해 찜·추가·멤버십 이력이 유지된다"
    - "config.toml에 enable_manual_linking=true가 설정된다 (로컬), 원격 대시보드 토글은 human-action"
  artifacts:
    - path: "apps/web/app/t/[slug]/_components/guest-promote.tsx"
      provides: "계정 승격 진입점 (linkIdentity 최소 심)"
      contains: "linkIdentity"
    - path: "supabase/config.toml"
      provides: "enable_manual_linking 활성화"
      contains: "enable_manual_linking"
  key_links:
    - from: "guest-promote 버튼"
      to: "supabase.auth.linkIdentity"
      via: "onClick"
      pattern: "linkIdentity\\(\\{ provider: 'kakao'"
---

<objective>
D-03 **계정 승격 최소 심** — 게스트가 카카오로 로그인하면 익명 `auth.uid`를 그대로 정식 계정으로 전환(linkIdentity)해 찜·추가·멤버십 이력을 유지한다. 별도 병합 화면 없음(deferred). (1) `guest-promote.tsx` — `로그인하고 내 여행에 담기` 진입점 1개(secondary 스타일, C6). 클릭→`supabase.auth.linkIdentity({ provider: 'kakao' })`(login.tsx `oauth('kakao')` L105-115 미러). (2) `config.toml`에 `enable_manual_linking = true` + 원격 대시보드 토글(human-action, A1 — 미활성 시 런타임 에러). (3) page.tsx 초대 카드 하단(C6)에 마운트.

Purpose: 게스트가 나중에 정식 로그인해도 이력을 잃지 않게 하는 승격 seam. UI는 진입점만 — 충돌(identity_already_exists) 처리·전체 승격 UX는 deferred.
Output: guest-promote 진입점 + config.toml manual_linking + 진입점 렌더/호출 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-guest-unified-share/25-RESEARCH.md
@.planning/phases/25-guest-unified-share/25-PATTERNS.md
@.planning/phases/25-guest-unified-share/25-UI-SPEC.md

<interfaces>
승격 진입점 미러 — `apps/web/app/login/page.tsx` L105-115:
```typescript
async function oauth(provider: 'google' | 'apple' | 'kakao') {
  ...
  const { error } = await getSupabaseBrowser().auth.signInWithOAuth({ provider, options: {...} });
}
```
→ `signInWithOAuth`를 `linkIdentity`로 교체:
```typescript
// Source: supabase.com/docs/reference/javascript/auth-linkidentity
await getSupabaseBrowser().auth.linkIdentity({ provider: 'kakao' });
```
전제: 프로젝트 Auth **Manual linking 활성화**(config.toml `[auth] enable_manual_linking = true` + 원격 대시보드).

카피(UI-SPEC C6): `로그인하고 내 여행에 담기` (secondary — brand text/outline, primary CTA와 경합 방지).

config.toml `[auth]` 블록 L37~ (L48 `enable_anonymous_sign_ins = true` 이미 있음). append 위치.

테스트 mock 패턴 — `apps/web/__tests__/login.test.tsx` L1-43 (signInWithOAuth mock) → linkIdentity로 미러.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: guest-promote.tsx — linkIdentity 승격 진입점 + 테스트</name>
  <read_first>
    apps/web/app/login/page.tsx L105-115 · apps/web/__tests__/login.test.tsx L1-55 ·
    25-UI-SPEC.md C6 · 25-RESEARCH.md §Code Ex(linkIdentity) + A1/A4.
  </read_first>
  <files>apps/web/app/t/[slug]/_components/guest-promote.tsx, apps/web/app/t/[slug]/_components/__tests__/guest-promote.test.tsx</files>
  <behavior>
    - 버튼 `로그인하고 내 여행에 담기` 렌더(secondary 스타일).
    - 클릭 시 `supabase.auth.linkIdentity({ provider: 'kakao' })` 정확히 1회 호출.
    - 에러 시 토스트(error variant) — 충돌 상세 처리는 deferred(진입점만).
  </behavior>
  <action>
`'use client'` 컴포넌트. `getSupabaseBrowser().auth.linkIdentity({ provider: 'kakao' })` onClick(login.tsx oauth 미러 — signInWithOAuth→linkIdentity). 버튼 카피 `로그인하고 내 여행에 담기`, secondary 스타일(brand text 또는 outline — primary와 경합 방지, UI-SPEC C6). 에러 catch→`useToast` error variant(문구는 기존 로그인 에러 스타일). 신규 hex 금지 — 토큰 클래스만. `aria-label` 불필요(텍스트 버튼).

테스트(`guest-promote.test.tsx`, login.test.tsx mock 미러): (a) 버튼 렌더, (b) 클릭→linkIdentity가 `{ provider: 'kakao' }`로 1회 호출, (c) 에러→toast error variant. Import AFTER mocks(login.test.tsx L31-32 규율).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web exec vitest run app/t/[slug]/_components/__tests__/guest-promote.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "linkIdentity({ provider: 'kakao'" apps/web/app/t/[slug]/_components/guest-promote.tsx`
    - `grep -q "로그인하고 내 여행에 담기" apps/web/app/t/[slug]/_components/guest-promote.tsx`
    - `! grep -Eq "#[0-9a-fA-F]{6}" apps/web/app/t/[slug]/_components/guest-promote.tsx`
    - web typecheck 0: `pnpm --filter @moajoa/web exec tsc --noEmit`
  </acceptance_criteria>
  <done>승격 진입점 렌더·linkIdentity 1회 호출·에러 토스트·테스트 그린.</done>
</task>

<task type="auto">
  <name>Task 2: page.tsx 초대 카드 하단에 guest-promote 마운트 (C6)</name>
  <read_first>apps/web/app/t/[slug]/page.tsx L98-111 (초대 카드) — Plan 03 반영본.</read_first>
  <files>apps/web/app/t/[slug]/page.tsx</files>
  <action>
초대 카드 섹션(L98-111) 하단에 `<GuestPromote />` 마운트(C6 — OQ-3 초대 카드 하단 채택). import 추가. 다른 라인 diff 0(surgical). SSR 셸이 client 컴포넌트를 렌더하는 형태(page.tsx는 RSC, GuestPromote는 'use client' island).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "<GuestPromote" apps/web/app/t/[slug]/page.tsx`
    - `! grep -q "cookies(" apps/web/app/t/[slug]/page.tsx` (캐시 무독성 유지)
    - web build PASS(`/t/[slug]`)
  </acceptance_criteria>
  <done>초대 카드 하단 승격 진입점 마운트·build PASS.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: config.toml enable_manual_linking + 원격 대시보드 토글 (human-action)</name>
  <read_first>supabase/config.toml L37-48 ([auth] 블록) · 25-RESEARCH.md A1 + Environment Availability.</read_first>
  <files>supabase/config.toml</files>
  <action>
`supabase/config.toml` `[auth]` 블록에 `enable_manual_linking = true` 추가(로컬 — linkIdentity 전제). **원격 적용(human-action):** Supabase Dashboard → Authentication → Settings에서 Manual linking 토글 ON(사용자). config.toml 변경만으론 원격 미반영 — 대시보드 토글 필수(A1). 미활성 시 linkIdentity 런타임 에러(진입점은 렌더되나 클릭 시 실패). **A4 확인:** linkIdentity kakao provider 지원은 문서 발췌 불완전 — 사용자가 실 카카오 승격으로 배포 전 확인 권장.
  </action>
  <verify>
    <automated>grep -q "enable_manual_linking = true" supabase/config.toml; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "enable_manual_linking = true" supabase/config.toml`
  </acceptance_criteria>
  <what-built>config.toml manual_linking 활성화 (로컬)</what-built>
  <how-to-verify>
    1. Supabase Dashboard → Authentication → Settings → Manual linking 토글 ON
    2. 게스트 익명 세션에서 승격 진입점 클릭 → 카카오 로그인 → linkIdentity 성공(익명 이력 유지) 확인
  </how-to-verify>
  <resume-signal>대시보드 Manual linking ON 완료 시 "enabled"로 응답 (또는 배포 전 검증 예정)</resume-signal>
  <done>config.toml enable_manual_linking=true·원격 대시보드 토글 사용자 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 익명 세션 → linkIdentity(kakao) | 익명 uid에 정식 OAuth identity 부착 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-12 | Spoofing | linkIdentity identity 충돌(이미 존재하는 카카오 계정) | accept (defer) | 최소 심 — 충돌 처리 deferred(Phase 25는 진입점만). identity_already_exists 에러는 토스트로 노출, 병합 UX는 다음 단계 |
| T-25-13 | Elevation of Privilege | Manual linking 미활성 상태 linkIdentity 호출 | mitigate | config.toml + 대시보드 토글(human-action 게이트). 미활성 시 런타임 에러로 fail-closed(권한 상승 없음) |
| T-25-14 | Tampering | 승격 후 이력 재소유 오류 | accept | linkIdentity가 uid 보존 전환 — added_by/votes/memberships 자동 보존(Supabase 위임, 커스텀 재소유 로직 0) |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web exec vitest run` 그린 · web typecheck 0 · web build PASS
- `grep enable_manual_linking supabase/config.toml` 존재 · page.tsx cookies() 미접근 유지
- `.js` 워크스페이스 import 0 · iOS 무접촉 · 라이브 카카오 승격은 대시보드 토글 후 verify-work 몫
</verification>

<success_criteria>
게스트 화면에 승격 진입점이 렌더되고, 클릭 시 linkIdentity가 카카오 provider로 호출되며(대시보드 활성 후 익명 이력 유지 전환), config.toml에 manual_linking이 설정된다.
</success_criteria>

<output>
완료 후 `.planning/phases/25-guest-unified-share/25-04-SUMMARY.md` 생성.
</output>
