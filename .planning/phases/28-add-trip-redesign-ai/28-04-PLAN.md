---
phase: 28-add-trip-redesign-ai
plan: 04
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified:
  - apps/web/app/onboarding/_lib/build-draft.ts
  - apps/web/app/onboarding/_components/step-dates.tsx
  - apps/web/app/onboarding/_components/step-where.tsx
  - apps/web/app/onboarding/_components/step-who.tsx
  - apps/web/app/onboarding/_components/step-seed.tsx
  - apps/web/app/onboarding/page.tsx
  - apps/web/__tests__/build-draft.test.ts
  - apps/web/__tests__/onboarding.test.tsx
autonomous: true
requirements: [SC-1, SC-2, SC-3]

must_haves:
  truths:
    - "위저드 스텝이 뒤로가기 chevron + N/4 카운터 + 중앙 아이콘 + 큰 타이틀 + 2열 pill + 하단 고정 CTA로 렌더된다"
    - "기간 pill을 고르면 그 Day 수가 새 모아에 저장된다"
    - "정확한 날짜를 원하면 별도 버튼으로 기존 캘린더에 도달할 수 있다"
    - "긴 여행(장기 range)을 골라도 모아 생성이 실패하지 않는다 — 상한을 넘으면 선택 자체가 막히고 안내가 뜬다"
    - "날짜를 안 정하고도 위저드를 통과할 수 있다 (ONBOARD-04 회귀 없음)"
    - "링크 붙여넣기·장소 검색이 기존과 완전히 동일하게 동작한다"
  artifacts:
    - "apps/web/app/onboarding/page.tsx — 헤더·타이틀 앙상블·CTA 리스타일 + canProceed 상한 게이트"
    - "apps/web/app/onboarding/_components/step-dates.tsx — 기간 pill + 캘린더 escape hatch + range 상한·안내 카피"
    - "apps/web/app/onboarding/_lib/build-draft.ts — day_count 매핑 + deriveDayCount/isDayCountWithinLimit"
  key_links:
    - "DurationPills(28-02) ↔ step-dates"
    - "buildDraft(day_count) ↔ TripCreateDraftSchema(28-01) ↔ createMoaDraft"
    - "Limits.TripDayCountMax(28-01) ↔ 캘린더 max ↔ canProceed 게이트 ↔ 0031 CHECK — 네 숫자가 같아야 장기 여행이 INSERT에서 거부되지 않는다"
    - "AddContentTabs ↔ step-seed — 무수정 재사용(SC-3의 검증은 diff 0)"
---

<objective>
`/onboarding` 4단계 위저드를 레퍼런스(IMG_2918~2921)와 동일한 룩으로 리스타일하고, 날짜 스텝을 기간 pill 1차 + 캘린더 escape hatch 구조로 바꾼다.

**상태 소유 구조는 절대 건드리지 않는다** — `page.tsx`가 모든 상태를 소유하고 스텝 컴포넌트는 props/콜백만 받는 Phase 24 D-02 구조가 그대로 유지된다. 렌더 트리만 교체한다.

Purpose: SC-1(레퍼런스 레이아웃) · SC-2(기간 pill → day_count) · SC-3(AddContentTabs 재사용).
Output: 리스타일된 위저드 셸 + 기간 pill 스텝 + day_count가 실린 draft.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-add-trip-redesign-ai/28-CONTEXT.md
@.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md
@.planning/phases/28-add-trip-redesign-ai/28-PATTERNS.md
@.planning/phases/28-add-trip-redesign-ai/28-01-SUMMARY.md
@.planning/phases/28-add-trip-redesign-ai/28-02-SUMMARY.md
@CLAUDE.md
</context>

<artifacts_this_phase_produces>
| 심볼 | 위치 | 소비자 |
|---|---|---|
| `buildDraft({ dayCount })` → `TripCreateDraft.day_count` | `apps/web/app/onboarding/_lib/build-draft.ts` | 위저드 제출(page.tsx) |
| `deriveDayCount(from, to)` | 동상 | step-dates(안내 카피) · page.tsx(`canProceed`) |
| `isDayCountWithinLimit(n)` | 동상 | 동상 — 상한 판정 한 벌 공유 |
| `StepDates` 새 props (`dayCount` · `onDayCountChange` · 모드 3택) | `apps/web/app/onboarding/_components/step-dates.tsx` | page.tsx |
| `HEADINGS` → `{icon, title, subtitle}` Record | `apps/web/app/onboarding/page.tsx` | 위저드 셸 |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build-draft.ts — day_count 매핑 + 정합 규칙 확정 (D-08 · Pitfall 7)</name>
  <files>apps/web/app/onboarding/_lib/build-draft.ts, apps/web/__tests__/build-draft.test.ts</files>

  <read_first>
    - `apps/web/app/onboarding/_lib/build-draft.ts` 전문 — 로컬 타임존 `fmt` 헬퍼(UTC 시프트 회피)와 `TripCreateDraftSchema.parse` 제출 게이트(T-24-10)
    - `apps/web/__tests__/build-draft.test.ts` — 기존 5케이스
    - 28-01의 `TripCreateDraftSchema.day_count` 계약
    - `.planning/phases/28-add-trip-redesign-ai/28-RESEARCH.md` Pitfall 7 (day_count vs 날짜 우선순위 드리프트)
  </read_first>

  <behavior>
    - 기간 pill 경로(dayCount=3, 날짜 없음) → day_count: 3, start_date: null, end_date: null
    - 캘린더 경로(6/14~6/16 범위) → start_date/end_date가 로컬 YYYY-MM-DD로 채워지고 **day_count는 3으로 파생 저장**된다
    - 미정 경로('나중에 정할게요') → day_count: null, start_date: null, end_date: null
    - 캘린더 단일 날짜(from만, to 없음) → day_count: 1
    - `deriveDayCount`: 같은 날 = 1, 6/14~6/16 = 3, 35일 범위 = 35 (상한 클램프 없이 **실제 값**을 반환한다 — 판정은 호출부가 한다)
    - `isDayCountWithinLimit`: 30 → true, 31 → false (상한 = `Limits.TripDayCountMax`)
    - **상한 초과 캘린더 범위(35일)를 buildDraft에 넣으면 `TripCreateDraftSchema.parse`가 throw한다 → INSERT가 발생하지 않는다** (마지막 방어선. 정상 UI 경로에서는 여기까지 오지 않는다 — Task 2/3이 앞단에서 막는다)
    - 기존 5케이스(title 파생·city 커스텀·companion·로컬 타임존)가 전부 무회귀
  </behavior>

  <action>
    **정합 규칙 확정 (D-08의 플래너 재량 지점 — 이 한 문장이 계약이다):**
    > **day_count는 항상 채운다. 캘린더로 정확한 날짜를 정하면 day_count를 그 범위에서 파생시켜 함께 저장한다.** 날짜를 아예 안 정하면 day_count만 있고 start/end는 null이다. 셋 다 안 정하면 전부 null이다.

    근거: EF fallback이 `trip.day_count ?? computeDayCount(start,end)`로 **day_count를 항상 우선**하므로(28-03), 캘린더로 5일 범위를 정했는데 이전 pill 선택의 day_count=3이 남아 있으면 5일 날짜가 무시되는 드리프트가 생긴다(Pitfall 7). 파생 저장이 그 드리프트를 0으로 만든다 — 단일 fallback 순서를 유지하면서.

    ⚠ **상한 계약 (BLOCKER 수습 — 이 파생이 상한을 만나는 지점이다):**
    캘린더 range는 길이 상한이 없다. 파생된 `day_count`가 `Limits.TripDayCountMax`(=30, 28-01)를 넘으면 0031 CHECK가 INSERT를 거부해 **위저드 제출이 통째로 실패**한다. 장기 여행은 D-07이 전제하는 **정상 경로**이므로 이걸 런타임 에러로 두면 안 된다.
    방어는 3겹이고, 이 태스크는 **판정에 쓸 순수 함수**를 제공한다:
    1. (Task 2) 캘린더가 31일 이상 범위를 **애초에 선택할 수 없게** 한다 — react-day-picker `max` 제약.
    2. (Task 3) `canProceed`가 상한 초과 범위를 통과시키지 않는다 + 안내 카피 노출.
    3. (이 태스크) `TripCreateDraftSchema.parse`가 마지막 방어선 — 위 둘을 뚫고 온 값은 throw로 INSERT를 막는다.

    `apps/web/app/onboarding/_lib/build-draft.ts` 수정:
    - 입력 타입에 `dayCount: number | null` 추가.
    - `dateMode` 모델을 3택으로 확장: 기간 pill 선택 / 정확한 날짜(캘린더) / 미정.
    - **`deriveDayCount(from, to)`를 export**한다 — 시작·종료가 모두 있으면 두 날짜 사이의 **포함 일수**(같은 날이면 1, 6/14~6/16이면 3), `from`만 있고 `to`가 없으면 1. 파생 계산은 로컬 `Date` 기준으로 하되 **기존 `fmt` 헬퍼의 로컬 타임존 관례를 깨지 않는다**(UTC 변환 금지 — 24-04에서 잡은 함정). **상한 클램프를 여기서 하지 않는다** — 실제 값을 반환해야 호출부가 "35일을 골랐다"는 사실을 알고 정확한 안내를 띄울 수 있다. 조용한 클램프는 사용자 의도를 말없이 바꾸는 것이라 금지.
    - **`isDayCountWithinLimit(n)`을 export**한다 — `n <= Limits.TripDayCountMax`. Task 2(캘린더 안내)·Task 3(`canProceed`)이 이 한 함수를 공유해 판정 로직이 두 벌로 갈라지지 않게 한다. 상한 리터럴 하드코딩 금지 — `@moajoa/core`의 상수를 import한다.
    - 캘린더 경로의 반환 객체에 파생된 `day_count`를 실는다. `TripCreateDraftSchema.parse` 게이트는 **그대로 유지**(제출 전 마지막 방어선 — 이제 상한도 함께 검사한다).
    - 순수 매퍼 유지 — DB·api·supabase import 0. (`@moajoa/core` import는 기존에도 있다 — 스키마 출처.)

    `apps/web/__tests__/build-draft.test.ts`에 위 `<behavior>` 신규 케이스를 append. 기존 5케이스는 무수정.
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/build-draft.test.ts</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/build-draft.test.ts` → **기존 5케이스 무회귀** + 신규 케이스 그린 (**상한 초과 범위 → parse throw 케이스 필수**)
    - `grep -c 'day_count' apps/web/app/onboarding/_lib/build-draft.ts` → 1 이상
    - `grep -c 'export function deriveDayCount' apps/web/app/onboarding/_lib/build-draft.ts` → 1
    - `grep -c 'export function isDayCountWithinLimit' apps/web/app/onboarding/_lib/build-draft.ts` → 1
    - **상한 단일 소스:** `grep -c 'TripDayCountMax' apps/web/app/onboarding/_lib/build-draft.ts` → 1 이상 (리터럴 30 하드코딩 금지)
    - `grep -c 'TripCreateDraftSchema.parse' apps/web/app/onboarding/_lib/build-draft.ts` → 1 (제출 게이트 유지)
    - `grep -Ec 'toISOString|@moajoa/api|getSupabaseBrowser' apps/web/app/onboarding/_lib/build-draft.ts` → **0** (로컬 타임존 유지 · 순수 매퍼 유지)
    - `git diff -- apps/web/__tests__/build-draft.test.ts | grep -c '^-[^-]'` → 0
  </acceptance_criteria>

  <done>buildDraft가 3경로(pill·캘린더·미정) 전부에서 day_count를 올바르게 채우고, `deriveDayCount`·`isDayCountWithinLimit`을 상한 상수 기반으로 export하며, 상한 초과 값은 parse 게이트가 throw로 막고, 순수 매퍼로 남아 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: step-dates — 기간 pill 1차 + 캘린더 escape hatch (D-06 · D-07 · A-8)</name>
  <files>apps/web/app/onboarding/_components/step-dates.tsx, apps/web/__tests__/onboarding.test.tsx</files>

  <read_first>
    - `apps/web/app/onboarding/_components/step-dates.tsx` 전문 — `ModeCard`(L20-43) · `DAY_PICKER_CLASS_NAMES`(L47-68) · DayPicker 블록(L84-95) · `StepDatesProps`(L13-18)
    - 28-02의 `DurationPills` + `DURATION_OPTIONS`
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Screen Contracts 2 · §Copywriting Contract
  </read_first>

  <behavior>
    - 기간 pill 6종이 렌더되고, '2박 3일' 탭 시 onDayCountChange(3)이 발화한다
    - '정확한 날짜 고르기' 버튼 탭 시 캘린더(react-day-picker)가 노출된다
    - 캘린더가 열리면 기간 pill 선택이 해제된다 (상호 배타)
    - '나중에 정할게요' 버튼이 존재하고 탭 시 미정 모드로 전환된다 (ONBOARD-04)
    - 초기 상태(아무것도 안 고름)에서는 캘린더가 렌더되지 않는다
    - **캘린더에 `max` 상한이 걸려 있다 (= `Limits.TripDayCountMax`) — 31일 이상 range를 선택할 수 없다**
    - **상한을 넘는 range가 주어지면(props로 들어온 경우) 안내 카피 `여행 기간은 최대 30일까지 정할 수 있어요. 날짜를 다시 골라주세요`가 렌더된다**
    - 상한 이내(30일) range에서는 안내 카피가 렌더되지 않는다
  </behavior>

  <action>
    `apps/web/app/onboarding/_components/step-dates.tsx` 재구성. **캘린더를 폐기하지 않는다 — 진입점만 뒤로 옮긴다**(D-07).

    - `DurationPills`(28-02)를 1차 UI로 배치.
    - pill 그리드 아래 24px 간격에 가로 2버튼 행:
      - `정확한 날짜 고르기` — outline sm. 탭 시 캘린더를 인라인 노출.
      - `나중에 정할게요` — text sm. **A-8: 레퍼런스에는 없지만 ONBOARD-04(날짜 미정 통과)가 기존 요구사항이므로 존치한다.** 제거하면 요구사항 회귀다.
    - **`DAY_PICKER_CLASS_NAMES` 상수와 DayPicker 블록은 그대로 유지**(share-sheet와의 클래스 미러 관계 보존). 렌더 조건만 기존 모드 분기에서 '정확한 날짜' 모드로 옮긴다. `locale={ko}` · `disabled={{ before: new Date() }}` · `mode="range"` 전부 유지.
    - ⚠ **캘린더 range 상한 (BLOCKER 수습 — 1차 방어):** DayPicker에 `max` prop을 **`Limits.TripDayCountMax`(=30, 28-01)로 건다.** react-day-picker 9.14.0의 `mode="range"`는 `max?: number`("The maximum number of days to include in the range")를 지원한다 — 실측 확인됨. 현재 이 캘린더에는 `disabled={{ before: new Date() }}`(과거 차단)만 있고 **길이 상한이 전혀 없어서**, 사용자가 35일 범위를 고르면 파생 day_count=35가 0031 CHECK(1..30)에 걸려 **모아 생성이 통째로 실패**한다. 상한을 여기 걸면 애초에 선택이 불가능해진다.
      상한 숫자를 리터럴로 쓰지 마라 — `@moajoa/core`의 상수를 import한다(0031 CHECK·core Zod와 같은 숫자여야 한다).
    - **상한 안내 카피:** 캘린더 아래에 `deriveDayCount`/`isDayCountWithinLimit`(Task 1)로 현재 range를 판정해, 상한을 넘으면 `여행 기간은 최대 30일까지 정할 수 있어요. 날짜를 다시 골라주세요`를 **caption 텍스트로 노출**한다(`text-sm text-danger-600` 계열 토큰 — 신규 hex 0). `max` prop이 정상 경로를 이미 막으므로 이 카피는 방어적 표시이지만, RDP의 `max` 동작이 버전에 따라 range를 리셋하는 대신 잘라낼 여지가 있어 **사용자가 왜 원하는 만큼 못 고르는지**를 설명하는 역할을 한다. 판정 로직을 이 파일에 다시 쓰지 말고 Task 1의 함수를 import해 한 벌로 쓴다.
    - 세 경로는 **상호 배타**: 캘린더를 열면 pill 선택이 해제되고, pill을 고르면 캘린더가 닫힌다.
    - props를 확장: 기간 값(`dayCount`)·기간 콜백·모드·모드 콜백·range·range 콜백. **상태는 여전히 page.tsx가 소유한다** — 이 컴포넌트는 useState를 갖지 않는다(Phase 24 D-02).
    - `ModeCard` 로컬 컴포넌트는 pill로 대체되어 orphan이 된다 → **제거한다**(내 변경이 만든 orphan만 치운다 — CLAUDE.md §3.3).
    - 카피는 UI-SPEC Copywriting Contract verbatim.

    테스트는 `apps/web/__tests__/onboarding.test.tsx`에 append (step-dates 전용 파일을 새로 만들지 않는다 — 위저드 통합 흐름에서 검증하는 것이 기존 관례).
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/onboarding.test.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/onboarding.test.tsx` → **기존 4케이스 무회귀** + 신규 케이스 그린 (실측: 현 파일 4케이스)
    - `grep -c 'DurationPills' apps/web/app/onboarding/_components/step-dates.tsx` → 1 이상
    - `grep -c 'DAY_PICKER_CLASS_NAMES' apps/web/app/onboarding/_components/step-dates.tsx` → 2 이상 (상수 선언 + DayPicker 적용 — 캘린더 보존)
    - `grep -c 'DayPicker' apps/web/app/onboarding/_components/step-dates.tsx` → 1 이상 (캘린더 폐기 안 함)
    - **상한 제약(BLOCKER):** `grep -Ec 'max=\{' apps/web/app/onboarding/_components/step-dates.tsx` → 1 이상 (DayPicker range 길이 상한)
    - **상한 단일 소스:** `grep -c 'TripDayCountMax' apps/web/app/onboarding/_components/step-dates.tsx` → 1 이상 (리터럴 30 하드코딩 금지)
    - **상한 안내 카피:** `grep -c '여행 기간은 최대 30일까지 정할 수 있어요' apps/web/app/onboarding/_components/step-dates.tsx` → 1
    - `grep -c '정확한 날짜 고르기' apps/web/app/onboarding/_components/step-dates.tsx` → 1
    - `grep -c '나중에 정할게요' apps/web/app/onboarding/_components/step-dates.tsx` → 1 (ONBOARD-04 존치)
    - `grep -c 'ModeCard' apps/web/app/onboarding/_components/step-dates.tsx` → 0 (orphan 제거)
    - `grep -c 'useState' apps/web/app/onboarding/_components/step-dates.tsx` → 0 (상태는 page.tsx 소유)
    - `grep -Ec '#[0-9a-fA-F]{3,8}\b' apps/web/app/onboarding/_components/step-dates.tsx` → 0
  </acceptance_criteria>

  <done>기간 pill이 1차 UI이고 캘린더는 버튼 뒤에 보존되며, 캘린더 range에 상한(`Limits.TripDayCountMax`)이 걸려 31일 이상을 고를 수 없고 초과 시 안내 카피가 뜨며, '나중에 정할게요'가 존치하고, 컴포넌트가 상태를 소유하지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 위저드 셸 리스타일 — 헤더·타이틀·CTA·2열 pill·D-22 카피 (D-01~D-05, D-10, D-11, D-22)</name>
  <files>apps/web/app/onboarding/page.tsx, apps/web/app/onboarding/_components/step-where.tsx, apps/web/app/onboarding/_components/step-who.tsx, apps/web/app/onboarding/_components/step-seed.tsx, apps/web/__tests__/onboarding.test.tsx</files>

  <read_first>
    - `apps/web/app/onboarding/page.tsx` 전문 — `HEADINGS` Record(L24-29) · 상태 선언(L35-44) · pushState/popstate(L46-66) · `canProceed`(L68-75) · `handleSubmit`(L77-106) · 헤더/점 인디케이터(L110-134) · 타이틀(L138-140) · CTA 컨테이너(L186-196)
    - `apps/web/app/onboarding/_components/step-where.tsx` · `step-who.tsx` — 현재 `Chip` + `flex-wrap` 렌더
    - `apps/web/app/onboarding/_components/step-seed.tsx` — AddContentTabs 마운트 지점과 현재 안내 문구
    - 28-02의 `SelectPill`
    - `.planning/phases/28-add-trip-redesign-ai/28-UI-SPEC.md` §Screen Contracts 1 · §CTA 상태표 · §Copywriting Contract · §Accessibility Contract
  </read_first>

  <behavior>
    - step 2~4에서 뒤로가기 chevron이 렌더되고 step 1에서는 렌더되지 않는다
    - 각 스텝에 N/4 카운터가 렌더되고 스크린리더 라벨("4단계 중 N단계")을 갖는다
    - 4-dot 점 인디케이터가 더 이상 렌더되지 않는다
    - step 1에서 도시를 안 고르면 '다음' CTA가 disabled다
    - step 2에서 기간 pill / 캘린더 range / '나중에 정할게요' 중 하나만 만족해도 CTA가 활성화된다
    - **step 2에서 캘린더 range의 파생 day_count가 상한을 넘으면 CTA가 disabled다 → 제출이 불가능하므로 INSERT가 발생하지 않는다** (BLOCKER 수습)
    - step 4의 서브카피가 D-22 문구다
    - 기존 제출 흐름(createMoaDraft 1회 → addLink/addManualPlace → /moa/{id} 리다이렉트)이 무회귀다
  </behavior>

  <action>
    **(a) `page.tsx` 헤더 (D-02):** 4-dot 점 인디케이터를 **제거**하고 우측 `N/4` 카운터로 교체한다. 좌측 `ChevronLeft`(이미 존재, `aria-label="뒤로"`, step>1에서만)는 **그대로 유지**. 카운터는 12px/600 `text-brand-600` + `tabular-nums` + `aria-label`로 "4단계 중 N단계"를 전달한다(A-5: 표기는 슬래시 붙임, 공백 없음). 총 스텝은 **4 고정**.
    **pushState/popstate 로직(L46-66)은 무수정** — 카운터는 `step` 상태에서 파생되므로 추가 배선이 0이다.

    **(b) `page.tsx` 타이틀 앙상블 (D-03):** `HEADINGS` Record를 `{icon, title, subtitle}` Record로 확장한다(스텝별 if 분기 없이 유지). 렌더 순서: 중앙 이모지(`aria-hidden`, `text-4xl`) → 중앙 큰 타이틀(`text-2xl font-semibold text-center leading-tight`) → 중앙 회색 서브카피(`text-sm text-neutral-500 text-center`). 이모지는 스텝별 1 🌏 · 2 📅 · 3 😎 · 4 📍 (A-6 — 신규 에셋·아이콘 라이브러리 0). 좌측정렬 `text-lg` 타이틀은 이 앙상블로 대체된다.

    **(c) `page.tsx` CTA (D-05):** **`Button` 컴포넌트 자체는 무수정**(전 화면 공유 — A-4). 기존 `sticky bottom-0` 컨테이너도 무수정. 비활성 시각("연한 파랑 bg + 흰 글씨")은 primary Button에 `className`으로 흰 글씨 오버라이드만 얹어 충족한다 — Button primary는 이미 연한 파랑 채움 disabled 스타일을 갖고 있고, 기본 글씨가 반투명이라 그것만 불투명 흰색으로 덮으면 된다. **신규 Button variant 추가 금지.**

    **(d) `page.tsx` `canProceed` (D-06 + 상한 게이트):** step 2 분기를 확장 — 기간 pill 선택 ∨ 캘린더 range 시작일 존재 ∨ 미정 모드, 셋 중 하나면 통과. step 1/3/4 분기는 무수정.
    ⚠ **단, 캘린더 경로는 상한 검사를 통과해야 한다 (BLOCKER 수습 — 2차 방어):** range가 있고 그 파생 day_count가 상한을 넘으면 `canProceed`는 **false**다. Task 1의 `deriveDayCount` + `isDayCountWithinLimit`을 import해 판정한다 — 판정 로직을 여기 다시 쓰지 마라(step-dates와 한 벌 공유). 이 게이트가 있으면 상한 초과 상태에서 '다음'/'모아 만들기'가 눌리지 않아 **`createMoaDraft` INSERT가 물리적으로 발생하지 않는다.** 안내 카피는 step-dates가 캘린더 밑에 이미 띄우므로 page.tsx에서 중복 렌더하지 않는다.
    `handleSubmit`은 `buildDraft` 호출 인자에 `dayCount`만 추가하고 나머지(createMoaDraft 1회 → 일괄 addLink+triggerExtraction+addManualPlace → `router.replace('/moa/{id}')`)는 **무수정**(D-11 — 위저드 완료 후 지도 보드로 이동, AI 일정 자동 생성 없음).

    **(e) `step-where.tsx` · `step-who.tsx` (D-04):** `Chip` → `SelectPill`, `flex-wrap` → `grid grid-cols-2 gap-2`. 홀수 개면 마지막 pill이 1열만 점유한다(레퍼런스와 동일). '기타' 직접입력 `Input`과 그 로직(maxLength·custom 플래그)은 **무수정**.

    **(f) `step-seed.tsx` (D-22 · D-10):** 안내 문구를 D-22 카피로 **교체**한다(기존 문구 대체 — 신규 UI 0). **`AddContentTabs`는 단 한 줄도 건드리지 않는다** — 링크/장소 2탭은 as-is 재사용이다(HC-3). 스테이징 리스트·'모아 만들기' CTA·'건너뛰기'도 무수정.

    **(g) D-01:** 히어로 인트로 화면은 **만들지 않는다.** `/onboarding` 진입 = 곧장 스텝 1 (기존 동작 유지).

    `apps/web/__tests__/onboarding.test.tsx`에 위 `<behavior>` 케이스를 append.
  </action>

  <verify>
    <automated>pnpm --filter @moajoa/web test -- --run __tests__/onboarding.test.tsx && pnpm --filter @moajoa/web typecheck</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm --filter @moajoa/web test -- --run __tests__/onboarding.test.tsx` → 그린. **기존 4케이스 무회귀**(실측: 현 파일 `it(`/`test(` 4개) + Task 2 신규 케이스 + 이 태스크 신규 케이스(**상한 초과 range → CTA disabled 케이스 필수**)
    - `pnpm --filter @moajoa/web test -- --run` → **전 스위트 그린** (무회귀)
    - `pnpm --filter @moajoa/web typecheck` → exit 0
    - `pnpm --filter @moajoa/web build` → PASS, `/onboarding` 라우트 존재
    - **상한 게이트(BLOCKER):** `grep -c 'isDayCountWithinLimit' apps/web/app/onboarding/page.tsx` → 1 이상 (`canProceed`가 상한을 검사 — 판정 로직 재작성 금지)
    - **SC-3 게이트:** `git diff --stat -- apps/web/components/add-content-tabs.tsx` → **빈 출력** (AddContentTabs 무수정 재사용)
    - `grep -c 'SelectPill' apps/web/app/onboarding/_components/step-where.tsx` → 1 이상
    - `grep -c 'SelectPill' apps/web/app/onboarding/_components/step-who.tsx` → 1 이상
    - `grep -c 'grid-cols-2' apps/web/app/onboarding/_components/step-where.tsx` → 1
    - `grep -c '4단계 중' apps/web/app/onboarding/page.tsx` → 1 (카운터 a11y 라벨)
    - `grep -c 'aria-label="뒤로"' apps/web/app/onboarding/page.tsx` → 1 (chevron 유지)
    - `grep -c '유튜브·블로그 링크를 넣으면' apps/web/app/onboarding/_components/step-seed.tsx` → 1 (D-22 카피)
    - `git diff --stat -- apps/web/components/button.tsx apps/web/components/chip.tsx` → 빈 출력 (공유 컴포넌트 무수정, A-4/A-2)
    - `git diff --stat -- apps/ios` → 빈 출력 (SC-6)
    - `grep -rEc '#[0-9a-fA-F]{3,8}\b' apps/web/app/onboarding/page.tsx` → 0
  </acceptance_criteria>

  <done>위저드가 chevron+N/4 헤더, 중앙 아이콘+타이틀+서브카피, 2열 SelectPill 그리드, 하단 고정 CTA로 렌더되고, AddContentTabs·Button·Chip diff가 0이며, 제출 흐름이 무회귀다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 위저드 입력(도시·기간·동행·링크) → `createMoaDraft` INSERT | 사용자 입력이 DB에 도달하는 경로 |
| 사용자 붙여넣기 URL → `addLink` → 추출 파이프라인 | 외부 URL이 서버 처리로 들어간다 (기존 표면, 무변경) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-14 | Tampering / Input Validation | 위저드 제출 페이로드 | medium | mitigate | `TripCreateDraftSchema.parse`를 제출 게이트로 유지(T-24-10) — day_count는 정수 1~`Limits.TripDayCountMax`(=30) 또는 null만 통과. ⚠ **정정:** 0031 CHECK(1..30)는 "2차 방어"가 아니라 **정상 입력 경로의 1차 계약**이다. 캘린더로 31일 이상을 고르는 것은 공격이 아니라 D-07이 전제하는 정상 사용이며, 세 상한(캘린더 `max` → Zod → DB CHECK)이 **같은 숫자**여야 정상 입력이 INSERT에서 거부되지 않는다. 상한 불일치는 보안 이슈가 아니라 **기능 파손**이다 |
| T-28-15 | Tampering | `AddContentTabs` 포크로 인한 URL 검증 우회 | medium | mitigate | AddContentTabs **무수정 재사용**(HC-3) — `new URL()` 검증·`resolve-place` EF 계약이 그대로 유지된다. acceptance가 `git diff --stat` 빈 출력을 강제 |
| T-28-16 | Elevation of Privilege | 신규 모아 소유권 | low | accept | `createMoaDraft`는 기존 경로 무변경 — owner_id는 DB 기본값/RLS가 결정. 이 플랜은 필드 하나만 추가 |
| T-28-17 | Spoofing (시각) | 비활성 CTA가 활성처럼 보임 | low | mitigate | 비활성 상태를 색만이 아니라 `disabled` 속성으로도 전달(A11y Contract) — 스크린리더·키보드 사용자에게 정확히 전달된다 |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web test -- --run` 전 스위트 그린
- `pnpm --filter @moajoa/web typecheck` exit 0
- `pnpm --filter @moajoa/web build` PASS
- **SC-3:** `git diff --stat -- apps/web/components/add-content-tabs.tsx` 빈 출력
- `git diff --stat -- apps/ios apps/web/components/button.tsx apps/web/components/chip.tsx` 빈 출력
- 워크스페이스 import에 `.js` 확장자 0건
</verification>

<success_criteria>
- [ ] SC-1: `/onboarding` 스텝이 레퍼런스 레이아웃(뒤로가기 + N/총, 중앙 아이콘, 큰 타이틀+서브카피, 2열 pill, 하단 고정 CTA)으로 렌더된다
- [ ] SC-2: 기간 pill 선택이 `trips.day_count`에 저장되고, 캘린더 버튼으로 정확한 날짜도 고를 수 있다
- [ ] SC-3: 링크 붙여넣기·장소 검색이 기존과 동일하게 동작한다 (AddContentTabs diff 0)
</success_criteria>

<output>
Create `.planning/phases/28-add-trip-redesign-ai/28-04-SUMMARY.md` when done
</output>
