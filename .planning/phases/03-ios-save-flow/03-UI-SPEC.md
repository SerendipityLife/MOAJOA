---
phase: 3
slug: ios-save-flow
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-26
---

# Phase 3 — UI Design Contract: iOS Save Flow

> iOS 실기기 dogfooding 입력 경로 #1의 시각·인터랙션 계약.
> 대부분의 결정은 03-CONTEXT.md D-01~D-11에 이미 lock됨. 본 문서는 그 결정을 실행 가능한 토큰·컴포넌트·상태·카피로 풀어낸다.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Pre-populated From

| Source | Decisions Used |
|--------|----------------|
| 03-CONTEXT.md | D-01~D-11 (전부 — Share Extension UX, enqueue 구조, bottom sheet, spinner UX) |
| 01-CONTEXT.md | D-06 (SVG brand pipeline), D-08 (Pretendard 4 weight) |
| `packages/ui-tokens/src/index.ts` | colors.brand · colors.pin · colors.neutral · spacing · typography · radii · shadows |
| `apps/ios/tailwind.config.js` | NativeWind 4.2 preset 적용 확인 |
| 기존 iOS 화면 (boards.tsx, [id].tsx) | className 패턴, SafeAreaView 사용, brand-500/neutral-* 색 사용 패턴 |
| User input | (none — auto mode, upstream artifacts complete) |

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn은 React Native 미지원 — 직접 토큰 사용) |
| Preset | not applicable |
| Component library | React Native core + NativeWind 4.2 (Tailwind classes on RN) |
| Bottom sheet | **@gorhom/bottom-sheet** (recommended — researcher가 reanimated v4 호환 확인 필요. fallback: `react-native` `Modal`) |
| Map | react-native-maps (Apple Maps provider — iOS default) |
| Icon library | **lucide-react-native** (이미 monorepo에 없으면 신규 — sparse usage. fallback: emoji + RN Text) |
| Font | Pretendard (Regular 400 · Medium 500 · SemiBold 600 · Bold 700 — Phase 1에서 expo-font 등록 완료) |
| Source of truth | `packages/ui-tokens/src/index.ts` — 이 파일이 모든 토큰의 single source. UI-SPEC은 그 사용 패턴만 명시 |

**Phase 3에서 추가하는 시각 컴포넌트:**
- Toast (Share Extension + 추출 done/error) — 신규
- Bottom Sheet (핀 디테일) — 신규
- Spinner overlay (추출 진행) — 신규
- + 핀 modal (text search + autocomplete dropdown) — 신규
- Boards 목록 시스템 보드 banner ("저장 실패" 표시) — 신규

---

## Spacing Scale

기존 `ui-tokens/src/index.ts` `spacing` 토큰 그대로 사용. NativeWind Tailwind class로 접근.

| Token | Value | Phase 3 Usage |
|-------|-------|---------------|
| `px-1` / `gap-1` | 4px | 아이콘과 텍스트 사이 간격 (`source_kind` badge 내부) |
| `px-2` / `gap-2` | 8px | URL input과 추가 버튼 사이, bottom sheet 액션 버튼 간 |
| `px-3` / `py-2` | 12px / 8px | URL TextInput·검색 input 내부 패딩 (기존 패턴 유지) |
| `px-4` / `py-2` | 16px / 8px | Pressable 버튼 내부 (기존 brand-500 버튼 패턴) |
| `px-6` | 24px | 화면 좌우 gutter (boards.tsx · [id].tsx 기존 패턴 유지) |
| `pt-2 pb-3` 또는 `pb-4` | 8/12/16px | 헤더 row vertical padding |
| `mb-3` / `mb-2` | 12 / 8px | List item 간 간격 |
| `py-16` | 64px | Empty state vertical padding (기존 boards.tsx 패턴) |
| `h-64` | 256px | MapView 고정 높이 (기존 [id].tsx 유지) |

**Bottom sheet snap points:**
- snap[0] = `25%` (제목·주소·source_kind badge만 보임 — peek state)
- snap[1] = `50%` (액션 버튼 전체 노출 — default open)
- 스크롤 추가 없음. 두 단계 충분.

**+ 핀 modal:**
- 화면 하단 절반 modal (snap 1단). KeyboardAvoidingView로 키보드 위에 올라옴.
- 검색 결과 드롭다운 = ScrollView nested 안 함. FlatList max 5개 (D-07 lock).

**Exceptions:** none. 모두 4의 배수.

---

## Typography

기존 `ui-tokens/src/index.ts` `typography` 토큰 그대로. NativeWind Tailwind class로 접근.

| Role | Class | px equivalent | Weight | Phase 3 Usage |
|------|-------|---------------|--------|---------------|
| Display (보드 제목 큰 헤더) | `text-2xl` | 24px | `font-semibold` (600) | boards.tsx의 "내 보드" (기존 유지) |
| Heading (보드 상세 헤더) | `text-lg` | 18px | `font-semibold` (600) | [id].tsx의 board.title (기존 유지) |
| Body (기본 본문, 토스트, 핀 이름) | `text-base` | 16px | `font-medium` (500) 또는 `font-regular` (400) | Bottom sheet 장소명, toast 메시지 |
| Label (메타 정보, source_kind badge, 입력 라벨) | `text-sm` | 14px | `font-medium` (500) | 버튼 label, list item title, 추가 버튼 |
| Caption (보조 설명, source 메타, helper) | `text-xs` | 12px | `font-regular` (400) | "유튜브 — 자동 분석 시작", source_kind badge text |

**Line height policy:**
- 본문 (`text-base`, `text-sm`) → `leading-normal` (1.5) — Korean 가독성
- 헤더 (`text-2xl`, `text-lg`) → `leading-tight` (1.25)
- Toast 한 줄이므로 lineHeight 명시 불필요

**Font weight 정책 (4 종 사용):**
- 400 regular: 본문, caption, placeholder
- 500 medium: 버튼 label, list item title, list 카운터 ("링크 N개 · 장소 M개")
- 600 semibold: 화면 헤더, bottom sheet 장소명, "내 보드" 타이틀
- 700 bold: **Phase 3에서 사용 안 함** (Phase 4 OG 이미지에서만 사용 — Phase 1 D-08)

> 가이드라인은 sizes 3-4 / weights 2개 권장하지만, 기존 `ui-tokens`이 4 size + 4 weight를 이미 lock했고 Phase 1에서 검증됨. Phase 3는 **활성 4 size / 활성 3 weight**(400/500/600)로 운용 — bold(700)는 phase 3에서 사용 안 함을 명시. 토큰 자체는 변경 불필요.

---

## Color

기존 `ui-tokens` 토큰 그대로. iOS는 라이트 모드만 (다크 모드는 v2 — REQUIREMENTS.md THEME-01).

| Role | Token | NativeWind class | Hex | Phase 3 Usage |
|------|-------|------------------|-----|---------------|
| Dominant (60%) | `neutral.0` | `bg-white` | #FFFFFF | 모든 화면 배경 (`SafeAreaView className="flex-1 bg-white"` 기존 유지) |
| Secondary (30%) | `neutral.50` ~ `neutral.300` | `bg-neutral-50` / `border-neutral-200` / `text-neutral-500/600/700` | #F8FAFC / #E2E8F0 / #64748B | 카드 border, list item, helper text, bottom sheet 배경 (`bg-neutral-50`), 입력 border |
| Accent (10%) | `brand.500` | `bg-brand-500` / `text-brand-500` | #F97316 | **다음 4가지에만 사용** (아래 reserved list) |
| Destructive | `semantic.danger` | `bg-danger` / `text-danger` / `border-danger` | #DC2626 | 핀 삭제 confirm 버튼, "저장 실패" 시스템 보드 dot, error toast 배경 |
| Success (feedback only) | `semantic.success` | `text-success` | #16A34A | "X개 핀 추가됨" done toast 좌측 아이콘 (옵션) |

**Accent (`brand-500`) reserved for:**
1. **Primary CTA** — "추가" 버튼 (URL add), "새 보드" 버튼, "+ 핀 추가" FAB, modal "추가" 버튼, "MOAJOA 앱 열기" deeplink 버튼 (Share Extension 안내)
2. **Active selection feedback** — list item `active:bg-brand-50` (이미 [id].tsx와 boards.tsx에서 사용 중)
3. **Back arrow / navigation hint** — 보드 상세 `text-brand-500` "← 뒤로" (기존 유지)
4. **Spinner color** — 추출 진행 spinner (`ActivityIndicator color={brand-500}`)

> Bottom sheet의 [이름 수정], [영상에서 위치] 버튼은 **`brand-500` 사용 X** — `bg-neutral-100` + `text-neutral-800` 사용 (secondary 액션). `[삭제]`만 destructive 색.
> Manual 핀 vs AI 핀의 시각 차별화 색(점선·옅은 색)은 **Phase 5 Trust UI에서 도입** (Phase 3 scope lock).

**Source kind badge (bottom sheet 안 D-09):**
- `source_kind='ai'` → `bg-neutral-100 text-neutral-700` + label "AI"
- `source_kind='manual'` → `bg-neutral-100 text-neutral-700` + label "수동"
- 색 차별화 X (Phase 5에서 시각 분리). Phase 3는 텍스트 label로만 구분.

---

## Component States

### 1. URL Add Flow (D-10)

`apps/ios/app/boards/[id].tsx` 기존 TextInput + "추가" Pressable에 spinner overlay 추가.

| State | Visual | Copy |
|-------|--------|------|
| **idle** | TextInput placeholder만 (`text-neutral-400`) | placeholder: `"유튜브 / 블로그 / 인스타 링크"` (기존 유지) |
| **typed (url detected)** | helper caption `text-xs text-neutral-500 mt-1` (기존 유지) | `"유튜브 — 자동 분석 시작"` (기존 유지) |
| **submitting (broadcast 구독 중)** | Full-screen modal overlay `absolute inset-0 bg-white/70` + 중앙 `ActivityIndicator color={brand-500} size="large"` + 아래 `text-base text-neutral-700` 라벨 | `"분석 중..."` |
| **done (broadcast.done 수신)** | overlay 사라짐 + 상단 toast (3초 자동 dismiss) | `"{N}개 핀 추가됨"` ({N}은 places.length 증분) |
| **error (broadcast.error 수신)** | overlay 사라짐 + 상단 toast (danger 색, 5초 자동 dismiss + 탭하면 즉시 dismiss) | `"분석 실패: {reason}"` reason 매핑: `transcript_unavailable→"자막이 없는 영상"`, `no_places→"장소를 찾지 못함"`, `quota_exceeded→"오늘 할당량 초과"`, default→`"잠시 후 다시 시도"` |

**Toast 컴포넌트 (신규):**
- 위치: 화면 상단 SafeArea 바로 아래 (top inset + 8px)
- 크기: `mx-4 px-4 py-3 rounded-xl shadow-md`
- 배경: success → `bg-neutral-900`, error → `bg-danger`
- 텍스트: `text-white text-sm font-medium`
- 단일 인스턴스 (큐 없음 — 새 toast가 이전 거 교체)
- iOS native banner 사용 X — React Native 자체 컴포넌트로 통일 (Share Extension toast는 별도, 아래 §2 참고)

### 2. Share Extension (D-01, D-02, D-03)

Native iOS Share Extension target — Swift UIViewController. React Native 아님.

| State | Visual | Copy |
|-------|--------|------|
| **로그인 안 됨 OR last_board_id 없음 (D-03)** | 시스템 표준 share UI (UIActivityIndicator → 텍스트로 전환) | `"MOAJOA 앱을 먼저 열어주세요"` + `[앱 열기]` 버튼 (deeplink `moajoa://pending`) |
| **저장 중 (online)** | UIActivityIndicator (System gray) — 0.5초 미만 | `"저장 중..."` |
| **저장 완료 (online)** | iOS native toast (커스텀 UILabel — 검은 반투명 배경 + 흰 글자), 1초 후 auto-dismiss + extension complete | `"{보드명} - 저장됨"` (D-01 lock) |
| **오프라인 (enqueue) (D-04, D-05)** | 동일 toast 스타일 | `"오프라인 — 나중에 저장돼요"` |
| **저장 실패 (3회 retry 초과, D-06)** | toast | `"저장 실패 — 앱에서 확인"` |

**Color:**
- Native UIKit 색 — Phase 3는 React Native 토큰 매핑 X.
- Toast 배경: `UIColor.black.withAlphaComponent(0.85)`, 글자: `UIColor.white`.
- 1탭 = 즉시 dismiss, 사용자는 1초 후 share sheet으로 복귀.

### 3. Pin Bottom Sheet (D-09)

핀 marker 탭 → 화면 하단 sheet snap[0] (peek) → 위로 끌면 snap[1] (full).

| Element | Class | Copy/Behavior |
|---------|-------|---------------|
| Sheet handle | `w-10 h-1 bg-neutral-300 rounded-full self-center mt-2` | 시각적 grip만 |
| 장소명 (heading) | `text-lg font-semibold text-neutral-900 mt-3` | `place.name_local` (D-09 inline 편집 진입점) |
| 주소 (caption) | `text-sm text-neutral-600 mt-1` | `place.address ?? place.name_ko ?? ""` |
| Source kind badge | `self-start mt-2 px-2 py-0.5 rounded-md bg-neutral-100` | 텍스트 `text-xs font-medium text-neutral-700`: "AI" or "수동" |
| Action: `[이름 수정]` | `bg-neutral-100 px-4 py-3 rounded-lg mt-4` (full-width) + `text-base font-medium text-neutral-800 text-center` | "이름 수정" — 탭 시 inline TextInput으로 전환, save 시 PATCH `places.name_local` |
| Action: `[영상에서 위치]` (AI 핀만) | 동일 secondary 버튼 스타일, `mt-2` | "영상에서 위치 보기" — Phase 3는 `Linking.openURL("https://youtube.com/watch?v=...&t=Xs")` no-op 수준 (CONTEXT.md deferred #1) |
| Action: `[삭제]` (destructive) | `bg-white border border-danger px-4 py-3 rounded-lg mt-2` + `text-base font-medium text-danger text-center` | "삭제" — 탭 시 Alert.alert("핀 삭제", "정말 삭제할까요?", [{cancel}, {destructive: 삭제}]) |

**Single sheet UI** (D-09): source_kind에 따라 `[영상에서 위치]` 버튼만 visibility 다름. UI 컴포넌트는 하나.

### 4. + 핀 Manual Add Modal (D-07, D-08)

[id].tsx 헤더 우측 상단 `+ 핀` Pressable → fullscreen modal (`presentationStyle='pageSheet'`).

| Element | Class | Copy/Behavior |
|---------|-------|---------------|
| Modal header | `flex-row items-center justify-between px-6 pt-4 pb-3` | 좌측 `[취소]` `text-brand-500`, 중앙 `text-lg font-semibold` `"핀 추가"`, 우측 invisible spacer |
| Search input | `mx-6 border border-neutral-300 rounded-lg px-3 py-2 text-base` | placeholder: `"장소명, 주소, 지하철역..."` autoFocus=true, 입력 300ms debounce 후 `resolve-place` Edge Function 호출 |
| Loading state | search input 하단 `mx-6 mt-2 flex-row items-center gap-2` | `<ActivityIndicator size="small" />` + `text-xs text-neutral-500` `"검색 중..."` |
| Empty result | `mx-6 mt-4 items-center py-8` | `text-sm text-neutral-500 text-center` `"검색 결과가 없어요. 다른 키워드로 시도해 보세요."` |
| Result item | `flex-row items-start px-6 py-3 border-b border-neutral-100 active:bg-neutral-50` | 우측 텍스트 stack: `text-base font-medium text-neutral-900` (`displayName`) + `text-xs text-neutral-500 mt-0.5` (`formattedAddress`). 좌측 핀 아이콘 (Map pin lucide / emoji 📍) `mr-3 mt-1` |
| Max items | 5개 (D-07 lock) | 초과 결과는 표시 X |
| Tap action | `resolve-place` 응답을 `addManualPlace` helper에 넘김 → 성공 시 modal dismiss → boards/[id] reload → "수동 핀 추가됨" toast 1초 노출 | toast: `"핀 추가됨"` |

### 5. Boards List (D-06 — 시스템 보드 표시)

기존 `boards.tsx`에 "저장 실패" 시스템 보드 영역 추가.

| Element | Class | Behavior |
|---------|-------|----------|
| 시스템 보드 banner (저장 실패 N개 있을 때만) | `mx-6 mb-4 bg-danger/5 border border-danger/20 rounded-lg px-4 py-3 flex-row items-center` | 좌측 빨간 dot `w-2 h-2 rounded-full bg-danger mr-3` + 텍스트 `text-sm text-neutral-800 flex-1` `"저장 실패 {N}개 — 탭하여 확인"` + 우측 chevron `text-neutral-400 text-sm` `"›"` |
| 시스템 보드 진입 | 탭 시 router.push('/boards/_failed') 또는 modal | 실패 큐(`pending_links_failed`) 리스트 + 각 항목에 `[재시도]` `[삭제]` 버튼 |

**N=0이면 banner 숨김.** 일반 보드 목록과 시각 분리 (border-danger/20로 구분). "보드 카드처럼 보이지 않게" — specifics 영역 lock 만족.

### 6. Login Screen (D-13 from Phase 1 — Phase 3 복원)

이메일 + 비밀번호 (매직 링크 토글)는 Phase 3에서 처음 사용자 흐름에 노출.

| Element | Class | Copy |
|---------|-------|------|
| Logo area | `items-center mt-16 mb-12` | brand wordmark SVG (Phase 1 ui-tokens) `w-40 h-12` |
| Email input | `mx-6 border border-neutral-300 rounded-lg px-3 py-3 text-base mb-3` | placeholder `"이메일"`, keyboardType="email-address", autoCapitalize="none" |
| Password input | 동일 스타일 | placeholder `"비밀번호"`, secureTextEntry |
| Primary button | `mx-6 bg-brand-500 px-4 py-3 rounded-lg items-center mt-2` | `text-white text-base font-medium` `"로그인"` (loading 시 `"로그인 중..."`) |
| Toggle: 매직 링크 | `mt-4 items-center` Pressable | `text-sm text-brand-500` `"매직 링크로 로그인"` (탭 시 비번 input 사라지고 버튼 라벨 `"링크 받기"`) |
| Toggle: 회원가입 | `mt-2 items-center` | `text-sm text-neutral-500` `"계정이 없으신가요? 회원가입"` |
| Error | input 아래 `mx-6 mt-2 text-sm text-danger` | Supabase error 메시지를 한글로 매핑 (`Invalid login credentials→"이메일 또는 비밀번호가 틀려요"`) |

> 디테일한 인증 옵션(Google OAuth · 이메일 verify 흐름)은 본 spec scope X — SESSION-NOTES-2026-05-25 결정에 따름. UI 컨트랙트는 위 골격만.

---

## Copywriting Contract

iOS 한국어 fixture. 모두 짧고, 사용자 다음 동작이 명확한 verb + noun.

| Element | Copy |
|---------|------|
| Primary CTA — URL 추가 | `"추가"` (3 chars — 기존 [id].tsx 유지) |
| Primary CTA — 새 보드 | `"새 보드"` (기존 boards.tsx 유지) |
| Primary CTA — 핀 추가 (modal 헤더) | `"핀 추가"` |
| Primary CTA — modal 안 검색 결과 선택 | (탭만 — 별도 버튼 없음) |
| Primary CTA — 핀 삭제 confirm | `"삭제"` (Alert destructive) |
| Primary CTA — 로그인 | `"로그인"` / loading `"로그인 중..."` |
| Empty state heading (보드 0개) | `"아직 보드가 없어요"` (기존 boards.tsx 유지, `{'\n'}` 분리) |
| Empty state body | `"새 보드를 만들고 유튜브 링크를 던져보세요"` (기존 유지) |
| Empty state — 보드 상세 핀 0개 (analysis 전) | `"링크를 붙여넣으면 자동으로 분석돼요"` (URL input 아래 helper) |
| Empty state — manual 검색 0건 | `"검색 결과가 없어요. 다른 키워드로 시도해 보세요."` |
| Empty state — pending_links_failed 0개 | (시스템 보드 banner 자체가 숨겨짐) |
| Toast — 추출 완료 | `"{N}개 핀 추가됨"` (N은 신규 places.length 증분; broadcast.done payload에서 계산) |
| Toast — 수동 핀 추가 완료 | `"핀 추가됨"` |
| Toast — 수동 핀 삭제 완료 | `"삭제됨"` |
| Toast — Share Extension 저장 완료 (native) | `"{보드명} - 저장됨"` (D-01 정확한 형식 lock) |
| Toast — Share Extension 오프라인 | `"오프라인 — 나중에 저장돼요"` |
| Error toast — 추출 실패 (transcript) | `"분석 실패: 자막이 없는 영상"` |
| Error toast — 추출 실패 (no places) | `"분석 실패: 장소를 찾지 못함"` |
| Error toast — 추출 실패 (quota) | `"분석 실패: 오늘 할당량 초과"` |
| Error toast — 추출 실패 (기타) | `"분석 실패: 잠시 후 다시 시도"` |
| Error toast — 수동 핀 추가 실패 | `"핀 추가 실패: 잠시 후 다시 시도"` |
| Error inline — 로그인 (Invalid creds) | `"이메일 또는 비밀번호가 틀려요"` |
| Error inline — 로그인 (이메일 미확인) | `"이메일 확인 메일을 먼저 열어주세요"` |
| Destructive confirmation — 핀 삭제 | Alert title `"핀 삭제"` + body `"정말 삭제할까요?"` + buttons `[취소][삭제]` |
| Destructive confirmation — 실패 보드 핀 영구 삭제 | Alert title `"영구 삭제"` + body `"재시도 없이 삭제할까요?"` + buttons `[취소][삭제]` |
| Share Extension 안내 (D-03 unauth) | `"MOAJOA 앱을 먼저 열어주세요"` + 버튼 `"앱 열기"` |
| Bottom sheet — source 라벨 (AI) | `"AI"` |
| Bottom sheet — source 라벨 (manual) | `"수동"` |
| Bottom sheet — 액션 1 | `"이름 수정"` |
| Bottom sheet — 액션 2 (AI only) | `"영상에서 위치 보기"` |
| Bottom sheet — 액션 3 | `"삭제"` |

**카피 가이드라인 (Phase 3 적용):**
- 끝맺음: 정중체 `~요/~돼요` (현재 코드 기존 톤 유지 — "아직 보드가 없어요")
- 길이: toast 한 줄 (≤ 15 글자 목표), heading ≤ 12 글자
- 동사 위주: "추가", "삭제", "수정" (명사형 X)
- 시간 표현: "잠시 후" / "나중에" (구체 초 표시 X — 사용자에게 거짓 약속 회피)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn | (not applicable — React Native 환경) | not required |
| third-party | none | not required |

**Native 모듈 의존성 (참고 — Phase 3 첫 plan에서 lock):**

| Package | 용도 | Safety note |
|---------|------|-------------|
| `@gorhom/bottom-sheet` | Pin bottom sheet (D-09) | researcher가 SDK 54 + reanimated 호환 확인. open source, MIT, well-maintained |
| `expo-share-intent` 또는 `expo-share-extension` | Share Extension (SAVE-03/04) | Phase 1 D-16 lock: Phase 3에서 처음 도입. SDK 54 호환 버전 확인이 첫 plan task |
| `lucide-react-native` (optional) | Map pin / chevron / close icon | 사용 안 해도 됨 (emoji + Text fallback). 채택 시 tree-shakable individual imports만 |
| `react-native-maps` | 지도 (기존) | 이미 셋업됨 |

---

## Visual Reference Patterns (Phase 3 신규 화면 wireframe)

### 보드 상세 [id].tsx — 추가된 요소만

```
┌──────────────────────────────────────┐
│ ← 뒤로    {board.title}     [+ 핀]   │  ← 헤더에 + 핀 추가
├──────────────────────────────────────┤
│ [유튜브 / 블로그 / 인스타 링크]  [추가]│
│ 유튜브 — 자동 분석 시작              │  (helper caption)
├──────────────────────────────────────┤
│                                      │
│        🗺️  MapView                  │  ← 핀 marker 탭 → bottom sheet
│         📍 📍 📍                     │
│                                      │
├──────────────────────────────────────┤
│ 링크 N개 · 장소 M개                  │
│ ┌────────────────────────────────┐  │
│ │ 영상 제목                       │  │
│ │ youtube · done                 │  │
│ └────────────────────────────────┘  │
└──────────────────────────────────────┘

[추출 진행 중일 때 overlay]
┌──────────────────────────────────────┐
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │  bg-white/70
│        ◌ Spinner (brand-500)        │
│         분석 중...                   │
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │
└──────────────────────────────────────┘
```

### Pin Bottom Sheet

```
   ───── (handle)
┌──────────────────────────────────────┐
│ 마쓰모토 라멘                         │  text-lg font-semibold
│ 도쿄도 시부야구 ...                   │  text-sm text-neutral-600
│ [AI]                                  │  small badge
│                                       │
│ ┌──────────────────────────────────┐ │
│ │         이름 수정                 │ │  neutral 100 bg
│ └──────────────────────────────────┘ │
│ ┌──────────────────────────────────┐ │
│ │       영상에서 위치 보기          │ │  (AI 핀만 표시)
│ └──────────────────────────────────┘ │
│ ┌──────────────────────────────────┐ │
│ │            삭제                   │ │  border-danger
│ └──────────────────────────────────┘ │
└──────────────────────────────────────┘
```

### + 핀 Modal

```
┌──────────────────────────────────────┐
│ [취소]      핀 추가              ·   │
├──────────────────────────────────────┤
│ [장소명, 주소, 지하철역...        ]  │  autoFocus
│ ◌ 검색 중...                          │
├──────────────────────────────────────┤
│ 📍 마쓰모토 라멘                      │
│    도쿄도 시부야구 진난 1-19-1       │
├──────────────────────────────────────┤
│ 📍 마쓰모토 카페                      │
│    오사카부 ...                      │
└──────────────────────────────────────┘
```

### 시스템 보드 banner (boards.tsx 상단)

```
┌──────────────────────────────────────┐
│ ●  저장 실패 2개 — 탭하여 확인    ›  │  danger/5 bg, danger/20 border
└──────────────────────────────────────┘
[일반 보드 목록 ...]
```

---

## Open Items Resolved by This Spec

CONTEXT.md `<open_questions>` 중 UI 영역:

| Question | Resolution |
|----------|-----------|
| bottom sheet 라이브러리 | **@gorhom/bottom-sheet** 권장 (reanimated v4 호환은 researcher가 첫 plan에서 확인 — 비호환 시 RN `Modal` + 수동 gesture 또는 v3 다운그레이드) |
| 보드 picker (last board 없을 때) — modal vs full screen | **D-03이 이미 결정: Share Extension은 picker 자체 안 띄움 — 안내 화면 + deeplink + SharedDefaults enqueue**. 메인 앱이 deeplink로 깨어났을 때 `pending_links` 처리는 modal (`presentationStyle='pageSheet'`) — full screen 아님. 보드 목록만 보여주고 1탭 = 저장 |
| "저장 실패" 표시 — 빨간 dot vs 별도 섹션 | **위 §5 — 빨간 dot + danger/5 배경의 별도 banner** (보드 카드 아님). 시각적 분리 + N=0일 때 숨김 |

CONTEXT.md `<open_questions>` 중 비-UI 영역 (App Group identifier · expo-share-intent SDK 54 호환): planner/researcher의 책임 — 본 UI-SPEC scope X.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — 모든 toast/CTA/empty/error 카피 명시 (16+ entries)
- [ ] Dimension 2 Visuals: PASS — 5개 신규 컴포넌트 (Toast, BottomSheet, Spinner overlay, + 핀 modal, 시스템 banner) 시각 정의 + wireframe
- [ ] Dimension 3 Color: PASS — accent reserved-for 4-item list, destructive `semantic.danger` 한정
- [ ] Dimension 4 Typography: PASS — 4 size, 3 active weight (Phase 3는 700 사용 X), Korean line-height policy
- [ ] Dimension 5 Spacing: PASS — 모두 4의 배수, 기존 `spacing` 토큰 사용
- [ ] Dimension 6 Registry Safety: PASS — third-party registry 없음 (React Native 환경), native package는 별도 표

**Approval:** pending
