---
phase: 24-host-flow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/moa/page.tsx
  - apps/web/app/login/page.tsx
  - apps/web/app/auth/callback/route.ts
  - apps/web/__tests__/login.test.tsx
autonomous: true
requirements: [AUTH-07, ONBOARD-03]
must_haves:
  truths:
    - "카카오 버튼 탭이 signInWithOAuth({provider: 'kakao'})를 기존 PKCE 콜백 경유로 호출한다 (AUTH-07)"
    - "로그인 직후 모아 0개→/onboarding, 1개→/moa/[id], 2개+→/moa 리스트로 분기한다 (D-01)"
    - "/moa 리스트가 모아 카드(이름·도시·날짜·장소 수) + 새 모아 CTA만 보여준다 (D-12)"
  artifacts:
    - path: "apps/web/app/moa/page.tsx"
      provides: "D-01 진입 분기 + D-12 미니멀 카드 리스트 (RSC)"
      min_lines: 40
    - path: "apps/web/app/login/page.tsx"
      provides: "kakao provider union + 카카오로 시작하기 버튼"
      contains: "kakao"
  key_links:
    - from: "apps/web/app/login/page.tsx"
      to: "/auth/callback"
      via: "signInWithOAuth redirectTo (기존 PKCE 무수정)"
      pattern: "signInWithOAuth"
    - from: "apps/web/app/moa/page.tsx"
      to: "listMyTripsWithPreview"
      via: "@moajoa/api import + count 분기 redirect"
      pattern: "listMyTripsWithPreview"
---

<objective>
진입 경로를 연다 — /login에 카카오 버튼(AUTH-07), 로그인 후 기본 목적지를 /moa로, /moa RSC가 D-01 분기(0개→온보딩, 1개→지도탭, 2개+→리스트)와 D-12 미니멀 카드 리스트를 담당한다.

Purpose: 분기 로직을 /moa RSC 한 곳에 모아 login·callback은 URL만 알게 한다 (RESEARCH Pattern 3). 카카오 provider·PKCE 콜백은 Phase 23-07에서 프로덕션 실증 완료 — 웹 UI만 추가하면 된다.
Output: /moa 라우트 + 카카오 버튼 + 목적지 변경 + login 단위 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-host-flow/24-CONTEXT.md (D-01, D-12)
@.planning/phases/24-host-flow/24-PATTERNS.md (§login, §callback, §/moa)
@.planning/phases/24-host-flow/24-UI-SPEC.md (§/moa, §/login 카카오 버튼, Copywriting Contract)
@apps/web/app/login/page.tsx
@apps/web/app/auth/callback/route.ts
@apps/web/app/boards/page.tsx (RSC 리스트 analog — dev-tools 게이트는 복사 금지)

<interfaces>
From packages/api/src/queries/trips.ts (실존):
```typescript
export type TripPreview = Trip & { place_count: number /* + preview 필드 */ };
export async function listMyTripsWithPreview(client: MoajoaSupabaseClient): Promise<TripPreview[]>
```

From packages/core/src/constants.ts (실존):
```typescript
export const CITY_KO_MAP: Readonly<Record<string, string>> = { tokyo: '도쿄', osaka: '오사카', kyoto: '교토', seoul: '서울', busan: '부산', jeju: '제주', fukuoka: '후쿠오카', sapporo: '삿포로', okinawa: '오키나와' };
```

From apps/web/app/login/page.tsx L106-116 (실코드 — union만 확장):
```typescript
async function oauth(provider: 'google' | 'apple') {
  setError(null);
  const { error } = await getSupabaseBrowser().auth.signInWithOAuth({
    provider, options: { redirectTo: callbackUrl() },
  });
  if (error) toast(error.message, { variant: 'error' });
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /moa RSC — D-01 진입 분기 + D-12 카드 리스트</name>
  <files>apps/web/app/moa/page.tsx</files>
  <read_first>
    - apps/web/app/boards/page.tsx (전체 — auth 게이트·리스트 렌더·animate-fade-up 패턴. L19-43 isDevToolsEnabled 게이트는 절대 복사 금지)
    - apps/web/app/me/page.tsx (redirect 게이트 idiom L5-9)
    - packages/api/src/queries/trips.ts L20-66 (TripPreview shape — place_count 필드명 확인)
  </read_first>
  <action>
    `apps/web/app/moa/page.tsx` 신규 (RSC — 'use client' 없음):
    1. auth 게이트: `getSupabaseServer()` → `auth.getUser()` → 미로그인 시 `redirect('/login?next=/moa')`.
    2. `const trips = await listMyTripsWithPreview(supabase);`
    3. D-01 분기 (정확히 이 순서):
       - `trips.length === 0` → `redirect('/onboarding')`
       - `trips.length === 1` → `redirect(\`/moa/${trips[0].id}\`)`
       - 2개+ → 카드 리스트 렌더
    4. D-12 카드 (UI-SPEC §/moa): `<Link href={\`/moa/${trip.id}\`}>` 카드 —
       - 이름: `trip.title` text-base(16px) font-semibold
       - 도시+날짜: `CITY_KO_MAP[trip.city_code] ?? trip.city_code` + ` · ` + (`trip.start_date && trip.end_date ? \`${trip.start_date} ~ ${trip.end_date}\` : '날짜 미정'`) — text-sm neutral-500
       - `장소 ${trip.place_count}개` — text-xs neutral-500
       - 배경 `surface.background`(#F5F6F8 — 기존 bg 클래스 관례), 카드 white raised, 좌우 16px 패딩, boards/page.tsx의 animate-fade-up stagger 미러
    5. 하단 고정 primary CTA "새 모아 만들기" → `<Link href="/onboarding">` (Button primary md full-width, 좌우 16px, safe-area 하단 패딩).
    6. 관리 기능(이름 변경·삭제) 없음 — D-12 미니멀. `isDevToolsEnabled` 게이트 없음.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "redirect('/onboarding')" apps/web/app/moa/page.tsx` 1건
    - `grep "listMyTripsWithPreview" apps/web/app/moa/page.tsx` 1건
    - `grep "날짜 미정" apps/web/app/moa/page.tsx` 1건, `grep "새 모아 만들기"` 1건
    - `grep -c "isDevToolsEnabled" apps/web/app/moa/page.tsx` == 0
    - `grep "'use client'" apps/web/app/moa/page.tsx` == 0건 (RSC)
    - `pnpm --filter web build` PASS — 라우트 테이블에 `/moa` 존재
  </acceptance_criteria>
  <done>D-01 분기·D-12 리스트가 /moa 한 곳에 — build 그린</done>
</task>

<task type="auto">
  <name>Task 2: 카카오 버튼 + 로그인 후 목적지 /moa</name>
  <files>apps/web/app/login/page.tsx, apps/web/app/auth/callback/route.ts</files>
  <read_first>
    - apps/web/app/login/page.tsx (전체 — oauth() L106-116, postLoginDestination L17-21, OAuth 버튼 블록 L227-240)
    - apps/web/app/auth/callback/route.ts (전체 — next 기본값 L34-40, PKCE exchange L61-80은 무수정 확인)
  </read_first>
  <action>
    Surgical diff 4곳만 — PKCE exchange·쿠키 배선·기존 이메일/구글/애플 경로 전부 무수정:
    1. `oauth()` union 확장: `async function oauth(provider: 'google' | 'apple')` → `'google' | 'apple' | 'kakao'`. 함수 본문 무변경.
    2. Apple 버튼 아래 카카오 버튼 1개 append (UI-SPEC A-9 — 카카오 브랜드 가이드용 토큰 외 색 유일 허용):
    ```tsx
    <button
      onClick={() => oauth('kakao')}
      className="w-full rounded-lg bg-[#FEE500] py-3 font-medium text-neutral-900 transition-colors hover:bg-[#FDD800]"
    >
      카카오로 시작하기
    </button>
    ```
    (기존 Apple 버튼의 className 구조 미러 — 색만 #FEE500/검정)
    3. `postLoginDestination()` (login/page.tsx L17-21): open-redirect 가드(`startsWith('/') && !startsWith('//')`)와 dev-tools `/boards` 분기는 그대로, 최종 fallback `'/'`만 `'/moa'`로.
    4. `auth/callback/route.ts` next 기본값 (L34-40): 동일하게 최종 fallback `'/'`만 `'/moa'`로. rawNext 가드 무수정.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'google' | 'apple' | 'kakao'" apps/web/app/login/page.tsx` 1건
    - `grep "카카오로 시작하기" apps/web/app/login/page.tsx` 1건 + `grep "#FEE500"` 1건
    - `grep "'/moa'" apps/web/app/login/page.tsx` 1건, `grep "'/moa'" apps/web/app/auth/callback/route.ts` 1건
    - `grep -c "startsWith('//')" apps/web/app/login/page.tsx` ≥ 1, `apps/web/app/auth/callback/route.ts` ≥ 1 (open-redirect 가드 보존)
    - `git diff apps/web/app/auth/callback/route.ts | grep "^-" | grep -v "'/'"` — exchange 로직 삭제 라인 0
  </acceptance_criteria>
  <done>카카오 OAuth 진입 + 기본 목적지 /moa — 기존 인증 경로 diff 최소</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: login 단위 테스트 (AUTH-07 자동 검증)</name>
  <files>apps/web/__tests__/login.test.tsx</files>
  <read_first>
    - apps/web/__tests__/vote-island.test.tsx (mock 4종 배선 — supabase browser·next/navigation·@/components·import-after-mocks 순서)
    - apps/web/app/login/page.tsx (Task 2 수정본 — 마운트 시 부수효과·사용 훅 확인)
  </read_first>
  <behavior>
    - Test 1: "카카오로 시작하기" 버튼이 렌더된다
    - Test 2: 버튼 클릭 시 signInWithOAuth가 정확히 `{ provider: 'kakao', options: { redirectTo: expect.stringContaining('/auth/callback') } }`로 1회 호출
    - Test 3: signInWithOAuth가 {error} 반환 시 toast 에러 variant 호출 (리다이렉트 없음 경로)
  </behavior>
  <action>
    `apps/web/__tests__/login.test.tsx` 신규 — vote-island.test.tsx의 mock idiom verbatim:
    - `vi.mock('@/lib/supabase/browser')` — `getSupabaseBrowser: () => ({ auth: { signInWithOAuth: mockSignIn, /* LoginPage가 마운트 시 쓰는 다른 auth 메서드가 있으면 read_first에서 확인해 no-op stub */ } })`
    - `vi.mock('next/navigation')` — useRouter no-op
    - `vi.mock('@/components')` — Button pass-through·useToast spy (vote-island L75-85 미러)
    - mock 선언 **뒤에** `import LoginPage from '@/app/login/page'` (import-after-mocks 규칙)
    - 테스트 파일은 `apps/web/__tests__/`에만 (co-located는 vitest include가 못 잡음 — 19-04 실측)
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- login</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test:run -- login` 그린 (3케이스)
    - 테스트에 `provider: 'kakao'` 정확 단언 존재 (`grep "provider: 'kakao'" apps/web/__tests__/login.test.tsx`)
    - `pnpm --filter web test:run` 전체 무회귀 그린
  </acceptance_criteria>
  <done>AUTH-07이 unit 레벨로 자동 검증됨 — 카카오 실로그인 e2e는 Vercel Preview UAT 몫(로컬은 이메일 대체, 23-07 잠금)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → Kakao OAuth → /auth/callback | 외부 IdP 리다이렉트 왕복 (PKCE) |
| ?next= 쿼리 파라미터 | 사용자 제어 리다이렉트 목적지 |
| /moa 데이터 로드 | 로그인 사용자의 trips만 노출되어야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Spoofing | ?next= open redirect | mitigate | 기존 가드 `startsWith('/') && !startsWith('//')` 를 login·callback 양쪽에서 보존 — acceptance grep으로 단언 (Task 2) |
| T-24-08 | Spoofing | OAuth code 교환 | mitigate | 기존 서버사이드 PKCE exchange(/auth/callback) 무수정 재사용 — 23-07 프로덕션 실증 완료. diff에 exchange 삭제 라인 0 단언 |
| T-24-09 | Info Disclosure | /moa 리스트 (타인 trips) | mitigate | listMyTripsWithPreview는 RLS(owner/member)로 필터 — RSC는 anon 키 + 사용자 쿠키 세션만 사용, 서비스 롤 0 |
</threat_model>

<verification>
- `pnpm --filter web test:run -- login` 그린
- `pnpm --filter web build` PASS + 라우트 테이블 `/moa`
- open-redirect 가드 2곳 보존 grep
</verification>

<success_criteria>
- 카카오 버튼 → signInWithOAuth('kakao') 단위 검증 (성공 기준 1의 자동화 절반; 실로그인은 Preview UAT)
- 로그인 직후 D-01 분기가 /moa 한 곳에서 동작
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-03-SUMMARY.md`
</output>
