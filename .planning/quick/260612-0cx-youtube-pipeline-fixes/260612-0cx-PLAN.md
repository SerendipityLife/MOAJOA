---
quick_id: 260612-0cx
slug: youtube-pipeline-fixes
description: 유튜브 추출 파이프라인 코드 리뷰 픽스 #1~#6
date: 2026-06-12
mode: quick
---

# Quick Task 260612-0cx: 유튜브 추출 파이프라인 리뷰 픽스

## Source

Fable 5 코드 리뷰(2026-06-12 세션)에서 발견한 High/Medium 이슈 6건. 각 픽스 = atomic commit.

## Tasks

1. **fix(extract): claude.ts max_tokens 2048→8192 + stop_reason 잘림 가드**
   - Phase 8의 summary_ko/video_summary_ko 추가로 2048 토큰 초과 시 JSON 잘림 → 전체 failed
   - `data.stop_reason === 'max_tokens'`면 명시적 에러 throw
   - Verify: `deno check` + 기존 claude.test.ts 통과 (프롬프트 스냅샷 무변경)

2. **feat(db): migration 0010 — links.extraction_started_at (nullable)**
   - stale-processing 탈출용 타임스탬프. append-only, NULLABLE (downtime 회피)
   - 적용은 모닝 게이트 `supabase db push` 배치(0008+0009+0010)

3. **fix(extract): anon JWT 거부**
   - Bearer 접두사 체크만으로는 공개 anon 키가 통과 (verify_jwt도 anon 키 JWT 수용)
   - `admin.auth.getUser(token)`으로 실제 유저 세션만 허용
   - public_board_view가 link id를 익명에 노출하므로 비용 어뷰즈 차단 필수

4. **fix(extract): processing 클레임 원자화 + 상태 게이트 + stale 탈출**
   - check-then-update 레이스 제거: 조건부 UPDATE 단일 쿼리 + 영향 행 확인
   - 클레임 허용: `pending|failed|manual_review`, 또는 `processing`인데 extraction_started_at이 10분 초과/NULL
   - `ready` 재추출 차단 (비용 어뷰즈 + 의미상 retry는 실패 건만)

5. **fix(extract): languageCode 하드코딩 제거 + cityHint 연결**
   - board.city_code를 link 로드 직후 조회 → cityHint로 LLM 프롬프트에 전달 (기존 TODO 해소)
   - languageCode: 한국 도시(seoul/busan/jeju) → 'ko', 그 외 'ja' (기존 기본값 유지)
   - Places textQuery에 cand.inferred_city 붙여 동명 장소 오해석 완화
   - revalidate 웹훅의 boards 중복 조회 제거 (이미 로드한 row 재사용)

6. **fix(extract): YOUTUBE_API_KEY 미설정 경고 로깅**
   - oEmbed 폴백은 description이 빈 문자열 → 추출 품질 급감. 운영 가시성 확보

## Out of scope

- Low 이슈들 (already-processing 응답 status 의미, 실패 시 메타데이터 저장, SSRF 가드 강화 등)
- structured outputs 전환 (v1.2 과제)
- 멤버십 검증 (RLS 헬퍼 경유) — 후속 과제
