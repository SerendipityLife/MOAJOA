---
phase: 20-affiliate-booking
plan: 06
type: execute
wave: 5
depends_on: ["20-05"]
files_modified:
  - apps/ios/app/trip/[id]/(tabs)/plan.tsx
  - apps/ios/components/plan/day-section.tsx
  - apps/ios/__tests__/plan.test.tsx
autonomous: true
requirements: [BOOK-01, BOOK-03]
must_haves:
  truths:
    - "날짜 확정 + 플랜 초안 존재 trip의 plan 탭에 '여행 준비' 클러스터(숙소 큰 카드 1 + 유심/교통 컴팩트 행 ≤2)가 TravelModeToggle과 Day 목록 사이에 뜬다 (D-01/D-02/D-04)"
    - "플랜 없음·날짜 미정 trip에서는 클러스터·strip 모두 렌더 0 (D-04 — 19-03 투표 관리카드와 상호배타)"
    - "예약성 장소 항목 아래에만 '예약 비교' 컴팩트 strip(Klook·KKday [보기])이 뜨고, 맛집·카페엔 절대 없다 (D-08)"
    - "매핑 미커버 도시(kr)는 유심/교통 행이 존재하지 않는다 — disabled/준비중 아님 (D-09)"
  artifacts:
    - "apps/ios/app/trip/[id]/(tabs)/plan.tsx — 여행 준비 클러스터 (surgical 삽입)"
    - "apps/ios/components/plan/day-section.tsx — 예약성 항목 하단 strip 슬롯"
  key_links:
    - "클러스터 게이트: plan !== null && trip.start_date — 19 관리카드(isDateless && poll open)와 상호배타 (PATTERNS 검증)"
    - "[보기] → openBooking/openDirectSearch(20-05) — checklist item id 연결로 plan 탭 클릭도 동일 항목을 '확인함' 전이 (D-03 데이터는 하나)"
    - "strip 게이트: isBookableActivity(place.category) (20-03)"
---

<objective>
plan 탭에 맥락형 인라인 예약 카드 삽입 (BOOK-01의 직해 — D-01): Day 1 시작 전 '여행 준비' 클러스터(숙소 비제휴 비교 카드 + Airalo 유심 행 + 교통 패스 행)와 예약성 장소 항목 아래 Klook/KKday 비교 strip (BOOK-03). 18 상태머신(States A–F)·19 투표 관리카드 분기 무수정 — 검증된 surgical 조건 분기 선례 그대로.

Purpose: plan 탭 = 발견 지점 (D-03). 일정 흐름 안에서 맥락으로 예약을 발견하되 광고판이 되지 않게 — 큰 카드 1장 + 컴팩트 행만.
Output: plan.tsx 클러스터 + day-section strip + plan.test.tsx 확장, ios suite green.
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-affiliate-booking/20-UI-SPEC.md
@.planning/phases/20-affiliate-booking/20-PATTERNS.md
@apps/ios/app/trip/[id]/(tabs)/plan.tsx
@apps/ios/components/plan/day-section.tsx
</context>

## Artifacts this phase produces (this plan's share)

- plan.tsx 내: `showBookingCluster` 게이트 변수, '여행 준비' 섹션 렌더 블록, checklist 상태(`checklist` state + load 시 `listChecklist`/`reconcileChecklist` 배선), 클릭 콜백(openBooking/openDirectSearch 호출부)
- day-section.tsx: `renderBookingStrip?: (placeId: string) => React.ReactNode` optional prop (기존 드래그/reorder 무수정)
- plan.test.tsx 신규 케이스 4+

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 여행 준비 클러스터 (plan.tsx surgical) + 체크리스트 배선</name>
  <files>apps/ios/app/trip/[id]/(tabs)/plan.tsx</files>
  <read_first>
    - apps/ios/app/trip/[id]/(tabs)/plan.tsx 전문 — 특히 L170-186 load Promise.all, L188-190 isDateless 게이트, L533-544 19-03 관리카드 분기, L904-921 State D/E 렌더의 TravelModeToggle→DaySection seam (PATTERNS가 지정한 삽입 지점)
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Screen 1 (레이아웃·카피·밀도 계약 정본)
    - apps/ios/lib/booking.ts + packages/core/src/booking-map.ts + packages/core/src/checklist.ts (20-03/05 산출물 — import 계약)
  </read_first>
  <behavior>
    - dated + plan 존재 → '여행 준비' 섹션 렌더 (숙소 예약 카드 + jp 도시면 여행 유심 행 + tokyo면 JR 패스 행)
    - dateless 또는 plan null → '여행 준비' 문자열 미렌더
    - kr 도시(seoul) → 유심/교통 행 미렌더 (D-09)
    - 숙소 카드 caption에 `{도시} · {MM.DD–MM.DD}` 프리필 컨텍스트
  </behavior>
  <action>
    기존 상태머신·19 분기 무수정 (Karpathy 3 — 삽입만). 구현:
    1. **게이트**: `const showBookingCluster = !!plan && !!trip?.start_date;` (D-04 — isDateless 관리카드와 상호배타, PATTERNS 검증). 
    2. **데이터**: load()의 Promise.all에 `listChecklist(supabase, id)` 추가 + showBookingCluster 조건에서 reconcile 실행 — 예약성 장소 집합(`places.filter(p => isBookableActivity(p.category))` × plan_items 배치분)과 BOOKING_REGION_MAP[trip.city_code] 커버리지로 `deriveChecklistAutos` 계산 → diff 있을 때만 `reconcileChecklist` 후 재조회 (plan 탭 클릭이 checklist item id를 갖고 로깅되게 — D-03/D-11; 실패는 console.warn 보조 데이터 idiom, 카드 렌더는 계속).
    3. **렌더**: TravelModeToggle 블록과 Day sections map 사이(L907↔L910 seam)에 `mt-6`로: 섹션 라벨 '여행 준비'(`text-sm font-semibold text-neutral-500 mb-2`) → CompareFrameCard full(icon bed-outline, title '숙소 예약', caption `{CITY_KO_MAP[city]} · {MM.DD–MM.DD}`, rows Agoda/Booking — COMPARE_LABELS 라벨, onView가 openDirectSearch(city는 CITY_KO_MAP 한글명, checkIn/checkOut은 trip.start_date/end_date, checklistItemId는 stay 항목 id)) → 유심 행(esimSlug 존재 시: cellular-outline + '여행 유심' + Airalo 라벨 + [보기] onView=openBooking({program:'airalo', destUrl: buildAiraloDestUrl(esimSlug), checklistItemId: esim 항목 id})) → 교통 행(transport 존재 시: train-outline + labelKo + [보기] onView=openBooking({program: transport.provider, destUrl: buildSearchDestUrl(provider, transport.searchQuery)})). 행 hidden 규칙: 매핑 null이면 **아예 미렌더** (빈 셸·준비중 금지 — D-09/UI-SPEC).
    밀도 계약(D-02): 큰 카드 정확히 1장 + 컴팩트 행 ≤2, trip당 1회, Day 목록 위. 브랜드 컬러는 [보기]에만.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "여행 준비" "apps/ios/app/trip/[id]/(tabs)/plan.tsx"` == 1
    - `grep -c "showBookingCluster" "apps/ios/app/trip/[id]/(tabs)/plan.tsx"` >= 2 (선언 + 렌더 게이트)
    - `grep -c "reconcileChecklist" "apps/ios/app/trip/[id]/(tabs)/plan.tsx"` == 1
    - 19 분기 무손상: `grep -c "날짜 투표 진행 중" "apps/ios/app/trip/[id]/(tabs)/plan.tsx"` == 1 (기존 값 유지)
    - `pnpm --filter @moajoa/ios typecheck` exit 0
  </acceptance_criteria>
  <done>클러스터가 D-01/02/04/09 계약대로 삽입, 기존 상태머신 diff 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 액티비티 컴팩트 strip (day-section 슬롯)</name>
  <files>apps/ios/components/plan/day-section.tsx, apps/ios/app/trip/[id]/(tabs)/plan.tsx</files>
  <read_first>
    - apps/ios/components/plan/day-section.tsx 전문 (드래그 Gesture.Pan reorder 로직 — 무수정 대상)
    - .planning/phases/20-affiliate-booking/20-UI-SPEC.md §Screen 2 (strip 시각 계약 — ml-11 정렬, mb-2.5 리듬)
  </read_first>
  <behavior>
    - 예약성 category 장소의 plan-item-row 바로 아래 '예약 비교' 컴팩트 strip (Klook [보기] · KKday [보기])
    - 맛집(ramen_restaurant)·카페 항목엔 strip 0
    - kkdayAvailable() false면 KKday 미니 버튼 숨김(Klook만) — graceful hide
  </behavior>
  <action>
    day-section.tsx에 optional prop `renderBookingStrip?: (placeId: string) => React.ReactNode` 추가 — 각 항목 row 렌더 직후(다음 항목 전) 호출부 삽입만, 드래그/reorder/기존 props 무수정. plan.tsx가 이 prop으로 주입: `isBookableActivity(place.category)`일 때 CompareFrameCard compact(rows: Klook + KKday(kkdayAvailable 시), 라벨 생략 — compact 계약, onView가 openBooking({program, destUrl: buildSearchDestUrl(program, place.name_ko ?? place.name_local), ctx: {placeId}, checklistItemId: 해당 activity 항목 id}) — D-07 장소명 검색결과 직행), 아니면 null. 들여쓰기 `ml-11`(drag-handle 칼럼 클리어) + `mb-2.5` 리듬 (UI-SPEC Screen 2). plan 탭 strip에는 확장 상태 없음 (full frame은 book 탭 — D-03).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "renderBookingStrip" apps/ios/components/plan/day-section.tsx` >= 2 (prop 선언 + 호출)
    - `grep -c "isBookableActivity" "apps/ios/app/trip/[id]/(tabs)/plan.tsx"` >= 1
    - `git diff apps/ios/components/plan/day-section.tsx | grep -c "Gesture\|reorder"` == 0 (드래그 로직 무접촉)
    - `pnpm --filter @moajoa/ios typecheck` exit 0
  </acceptance_criteria>
  <done>strip이 D-07/D-08 계약으로 예약성 항목에만 부착</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: plan.test.tsx 게이트 케이스 (positive/negative 쌍)</name>
  <files>apps/ios/__tests__/plan.test.tsx</files>
  <read_first>
    - apps/ios/__tests__/plan.test.tsx (mock 하네스 L17-106 + 19-03 관리카드 케이스 L210-299 — positive/negative 쌍 선례)
  </read_first>
  <behavior>
    - dated trip + plan draft + tokyo → '여행 준비' + '숙소 예약' + '여행 유심' 렌더
    - dated + plan + seoul → '여행 준비'는 렌더되나 '여행 유심' 미렌더 (D-09)
    - plan null (State B) → queryByText('여행 준비') null (D-04)
    - dateless + open poll → 관리카드 렌더 + '여행 준비' null (상호배타)
    - 예약성 장소(tourist_attraction) 포함 plan → '예약 비교' 렌더 / 전부 맛집이면 null (D-08)
  </behavior>
  <action>
    기존 mock 하네스에 @moajoa/api booking 함수 mock(listChecklist → [], reconcileChecklist → resolve) + @/lib/booking mock(openBooking/openDirectSearch/kkdayAvailable=true) 추가. behavior 5 케이스를 19-03 케이스 shape(waitFor + getByText/queryByText 쌍)로 작성. 기존 케이스 무수정.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/ios test -- plan --watchman=false</automated>
    <human-check>디바이스 UAT (phase verify로 배치): 실기기에서 tokyo trip [보기] 탭 → 시스템 Safari가 열리고 Klook/KKday 검색결과에 장소명이 착지하는지 (A1/A2 [ASSUMED] — 봇차단으로 사람 눈이 유일 게이트, RESEARCH Pitfall 8) + TP 대시보드에 클릭·SubID 집계 확인 (A4)</human-check>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/ios test -- plan --watchman=false` exit 0 (기존 7 + 신규 5)
    - `pnpm --filter @moajoa/ios test -- --watchman=false` 전체 exit 0 (13+ suites 무회귀)
  </acceptance_criteria>
  <done>게이트·밀도·상호배타가 jest로 고정, 착지 검증은 UAT 항목 등록</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| places.name_ko/name_local (외부 유래) → 검색 딥링크 쿼리 | 장소명이 buildSearchDestUrl을 거쳐 URL에 삽입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | 장소명 → URL 인젝션 | high | mitigate | plan.tsx는 원문자열을 core 빌더에 넘길 뿐 — 인코딩은 booking.ts 단일 지점 (20-03 계약 테스트가 한글/특수문자 고정). 화면 측 문자열 연결 조립 0 |
| T-20-14 | Info Disclosure | 클릭 컨텍스트(userId) 로깅 | low | accept | 자기 trip 멤버 대상 자기 클릭 기록 — RLS can_read_trip 스코프 내 (0021 매트릭스 검증됨) |
</threat_model>

<verification>
- ios 전체 suite green + typecheck 0
- UI-SPEC Screen 1/2 밀도·카피 준수 (큰 카드 1 + 컴팩트 ≤2, 브랜드는 [보기]만)
</verification>

<success_criteria>
- BOOK-01 인라인 카드(숙소/유심/교통/액티비티 슬롯) 코드+테스트 완성
- BOOK-03 비교 구도가 plan 탭 발견 지점에서 동작
- 검색 착지·쿠키 보존은 phase UAT 항목으로 등록
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-06-SUMMARY.md` when done
</output>
