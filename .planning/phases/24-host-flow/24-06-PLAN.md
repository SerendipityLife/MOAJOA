---
phase: 24-host-flow
plan: 06
type: execute
wave: 3
depends_on: ["24-01", "24-02", "24-05"]
files_modified:
  - apps/web/app/moa/[id]/page.tsx
  - apps/web/app/moa/[id]/_components/moa-map.tsx
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/__tests__/moa-island.test.tsx
autonomous: true
requirements: [MOA-03, MOA-05, MOA-06]
must_haves:
  truths:
    - "링크 추출이 완료되면 postgres_changes 이벤트로 리스트·지도에 핀이 자동 반영된다 (MOA-03, D-14)"
    - "마커 탭 시 해당 행으로 스크롤+아코디언 펼침, 시트가 collapsed면 expanded로 (MOA-05)"
    - "핀이 추가자별 색(호스트=브랜드)으로 렌더된다 (MOA-06)"
    - "새 핀 생성 시 fitBounds 재조정 + '장소 N개 추가됨' 토스트, 지도 re-init 없음 (D-16)"
  artifacts:
    - path: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      provides: "상태·realtime 구독·optimistic 찜·reconcile 허브"
      exports: ["MoaIsland"]
      min_lines: 120
    - path: "apps/web/app/moa/[id]/_components/moa-map.tsx"
      provides: "persistent map ref + 마커 diff (re-init 금지)"
      exports: ["MoaMap"]
    - path: "apps/web/app/moa/[id]/page.tsx"
      provides: "auth 게이트 + 초기 데이터 로드 RSC"
  key_links:
    - from: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      to: "moaChannelName(tripId)"
      via: "postgres_changes 단일 채널 (places INSERT + links UPDATE)"
      pattern: "postgres_changes"
    - from: "apps/web/app/moa/[id]/_components/moa-map.tsx"
      to: "buildMarkerIconUrl"
      via: "fill=memberColor 마커 아이콘"
      pattern: "buildMarkerIconUrl"
    - from: "apps/web/app/moa/[id]/page.tsx"
      to: "listPlacesByTrip / listLinksByTrip / getVoteCounts / listTripMembers / getProfileNames"
      via: "@moajoa/api RSC 초기 로드"
      pattern: "listPlacesByTrip"
---

<objective>
`/moa/[id]` 지도탭을 조립한다 — RSC 초기 로드, persistent 지도(마커 diff·fitBounds·추가자 색), 상태 허브 island(realtime postgres_changes 단일 채널·reconcile·optimistic 찜·마커↔행 연동).

Purpose: MOA-03(추출 완료 실시간 반영)·MOA-05(마커 탭 연동)·MOA-06(핀 색)의 배선 완성. 24-05의 place-sheet/place-list를 소비한다. FAB·함께 정하기 버튼은 24-07이 붙인다.
Output: 지도탭 라우트 + moa-map + moa-island + 채널 lifecycle 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-host-flow/24-CONTEXT.md (D-09, D-14, D-16)
@.planning/phases/24-host-flow/24-RESEARCH.md (Pattern 1 realtime, Pitfall 4 지도 re-init, 안티패턴)
@.planning/phases/24-host-flow/24-PATTERNS.md (§moa-map, §Realtime, §moa/[id] RSC)
@apps/web/app/t/[slug]/_components/public-board-map.tsx (스크립트 로딩·맵 옵션·마커 생성만 — useEffect 재init 구조 복사 금지)
@apps/web/app/poll/[code]/_components/poll-vote-island.tsx (L143-174 채널 lifecycle idiom)
@apps/web/app/t/[slug]/_components/vote-island.tsx (L177-197 optimistic 찜 rollback)

<interfaces>
From packages/core (실존):
```typescript
moaChannelName(tripId)  // → `moa:${tripId}` — ONE channel per screen (Phase 19/20 교훈)
```

From packages/api (실존 + 24-02):
```typescript
getTrip(client, id): Promise<Trip | null>
listPlacesByTrip(client, tripId): Promise<Place[]>       // hidden_at null 필터·created_at desc
listLinksByTrip(client, tripId): Promise<Link[]>
getVoteCounts(client, placeIds): Promise<Record<string, number>>
getMyVotedPlaceIds(client, placeIds, uid): Promise<string[]>
castVote(client, { place_id, kind: 'love' }) / retractVote(client, placeId)
triggerExtraction(client, linkId): Promise<ExtractionResult>
listTripMembers(client, tripId): Promise<{user_id: string; created_at: string}[]>   // 24-02
getProfileNames(client, userIds): Promise<Record<string, string>>                    // 24-02
```

From 24-02 / 24-05 (이 plan이 소비):
```typescript
memberColor(userId, ownerId, memberIdsInJoinOrder): string   // apps/web/lib/member-color
buildMarkerIconUrl({ source_kind, confidence, fill? }): string  // fill = 토큰 리터럴만
PlaceSheet { anchor, onAnchorChange, header, children }
PlaceList { places, links, counts, myVotes, votePending, profileNames, colorFor, openPlaceId, onOpenPlace, onToggleVote, onRetry }
```

realtime 구독 계약 (RESEARCH Pattern 1 — 0026이 publication 등록 완료):
```typescript
const channel = client.channel(moaChannelName(tripId))
  .on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'places', filter: `trip_id=eq.${tripId}` }, () => void reconcile())
  .on('postgres_changes', { event: 'UPDATE', schema: 'public', table: 'links', filter: `trip_id=eq.${tripId}` }, () => void reconcile())
  .subscribe();
// cleanup: client.removeChannel(channel) — presence 사용 금지 (D-14)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: moa-map — persistent map + 마커 diff + fitBounds</name>
  <files>apps/web/app/moa/[id]/_components/moa-map.tsx</files>
  <read_first>
    - apps/web/app/t/[slug]/_components/public-board-map.tsx (전체 — L85-101 스크립트 로딩 idiom·L38-45 맵 옵션·L59-68 마커 생성은 복사, places 의존 재init 구조는 복사 금지)
    - apps/web/lib/marker-svg.ts (24-02 fill 확장판 시그니처)
  </read_first>
  <action>
    `moa-map.tsx` 신규 — `'use client'`:
    ```typescript
    export interface MoaMapProps {
      places: Place[];
      colorFor: (userId: string) => string;
      onMarkerTap: (placeId: string) => void;
    }
    ```
    1. 스크립트 로딩: public-board-map.tsx L85-101 idiom verbatim (`script[data-moajoa-gmaps]` dedupe, `libraries=marker`, `NEXT_PUBLIC_GOOGLE_MAPS_KEY`). 키 부재 시 정적 fallback 문구 "지도를 불러올 수 없어요" (기존 innerHTML 정적 리터럴 패턴 — user 문자열 삽입 금지).
    2. 맵 옵션: `disableDefaultUI: true, zoomControl: true, gestureHandling: 'greedy', clickableIcons: false` (계승).
    3. **Pitfall 4 구조 (신규 발명):** `mapRef = useRef<google.maps.Map>`·`markersRef = useRef<Map<string, google.maps.Marker>>` — 맵은 1회만 생성. places 변경 useEffect에서 **마커 diff만**: markersRef에 없는 place id → 마커 생성(icon: `buildMarkerIconUrl({ source_kind: p.source_kind, confidence: p.confidence, fill: colorFor(p.added_by) })`, scaledSize 32×40, anchor 16,40, `marker.addListener('click', () => onMarkerTap(p.id))`), places에 없는 id → `marker.setMap(null)` + 삭제. 지도 인스턴스 재생성 0.
    4. **fitBounds (D-16):** `prevCountRef`로 직전 장소 수 유지 — **수가 증가했을 때만** 전체 마커 LatLngBounds로 `map.fitBounds(bounds)` (사용자 팬 중 강제 리셋 금지). places 0→N 초기 로드도 이 경로.
    5. 컨테이너: 풀 컬럼·풀 하이트 div (`h-full w-full`) — 레이아웃은 island 몫.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "data-moajoa-gmaps" apps/web/app/moa/[id]/_components/moa-map.tsx` ≥ 1 (스크립트 dedupe)
    - `grep "gestureHandling: 'greedy'" apps/web/app/moa/[id]/_components/moa-map.tsx` 1건 + `grep "clickableIcons: false"` 1건
    - `grep "buildMarkerIconUrl" apps/web/app/moa/[id]/_components/moa-map.tsx` 1건 + fill에 `colorFor(` 전달
    - `grep "fitBounds" apps/web/app/moa/[id]/_components/moa-map.tsx` 1건 + 증가 조건 가드 (`prevCount` 류 ref 존재)
    - `grep -c "new g.Map\|new google.maps.Map" apps/web/app/moa/[id]/_components/moa-map.tsx` == 1 (1회 생성 경로만)
  </acceptance_criteria>
  <done>realtime 이벤트마다 깜빡임 없는 persistent 지도 — 마커 diff·색·탭 콜백 완비</done>
</task>

<task type="auto">
  <name>Task 2: moa-island — 상태 허브 + realtime + optimistic 찜 + RSC page</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/app/moa/[id]/page.tsx</files>
  <read_first>
    - apps/web/app/poll/[code]/_components/poll-vote-island.tsx (L143-174 채널 subscribe/removeChannel lifecycle)
    - apps/web/app/t/[slug]/_components/vote-island.tsx (L177-197 optimistic rollback 패턴 verbatim 복사원)
    - apps/web/app/t/[slug]/page.tsx (RSC → island props 구조) + apps/web/app/me/page.tsx (auth redirect 게이트)
    - apps/web/app/moa/[id]/_components/place-sheet.tsx·place-list.tsx (24-05 산출물 — props 정확 shape)
  </read_first>
  <action>
    1. `moa-island.tsx` 신규 — `'use client'`:
    ```typescript
    export interface MoaIslandProps {
      trip: Trip;
      currentUserId: string;
      initialPlaces: Place[];
      initialLinks: Link[];
      initialCounts: Record<string, number>;
      initialMyVotedPlaceIds: string[];
      memberIdsInJoinOrder: string[];
      initialProfileNames: Record<string, string>;
    }
    ```
       - 상태: places, links, counts, myVotes(Record), votePending, profileNames, openPlaceId, sheetAnchor('collapsed' 초기).
       - `colorFor = (uid) => memberColor(uid, trip.owner_id, memberIdsInJoinOrder)`.
       - **reconcile():** `Promise.all([listPlacesByTrip, listLinksByTrip])` → placeIds로 `getVoteCounts` → 새 added_by 중 profileNames에 없는 id는 `getProfileNames` 추가 fetch → 상태 일괄 set. **payload 패치 금지 — refetch reconcile** (RESEARCH 안티패턴). 장소 수가 직전보다 증가하면 `toast(\`장소 ${delta}개 추가됨\`)` (D-16 잠금 형식).
       - **realtime effect:** interfaces의 구독 계약 그대로 — `moaChannelName(trip.id)` 단일 채널에 places INSERT + links UPDATE 바인딩, cleanup `client.removeChannel(channel)`. **presence·broadcast 바인딩 금지(D-14), 한 토픽 채널 2개 금지.**
       - **찜 토글 (A-5):** vote-island L177-197 optimistic+rollback verbatim (flip → castVote('love')/retractVote → 실패 시 rollback + toast('투표를 저장하지 못했어요.', {variant:'error'})). join/anon 분기는 복사하지 않음(호스트 화면 — 로그인 전제).
       - **마커 탭 (MOA-05):** `onMarkerTap = (id) => { setOpenPlaceId(id); setSheetAnchor('expanded'); }` — 스크롤은 place-list의 openPlaceId effect가 수행.
       - **재시도 (D-15):** `onRetry = (linkId) => triggerExtraction(client, linkId).then(() => reconcile()).catch(() => toast('장소를 찾지 못했어요', {variant:'error'}))` — retry-extraction-button 로직 미러, router.refresh 대신 reconcile.
       - 레이아웃: 풀스크린 relative 컨테이너(max-w-lg 중앙 고정 — A-10, 바깥 거터 neutral-100) — MoaMap 절대 채움 + 상단 좌측 back chevron 오버레이(흰 원형 40px + shadow, lucide `ChevronLeft`, **aria-label="뒤로"**, `router.push('/moa')`) + PlaceSheet(header: 모아 이름 18/600 + `장소 ${places.length}개` 14/400 neutral-500, children: PlaceList 전체 props 배선). 불투명 상단 바 금지(지도 감성 — UI-SPEC).
    2. `page.tsx` 신규 — RSC:
       - Next 15: `const { id } = await params;`
       - auth 게이트: `getSupabaseServer()` → 미로그인 `redirect(\`/login?next=/moa/${id}\`)`
       - `getTrip(supabase, id)` null → `notFound()` (접근 권한은 RLS가 자연 게이트 — RESEARCH Open Q4: 로그인 + can_read 기준)
       - `Promise.all([listPlacesByTrip, listLinksByTrip, listTripMembers])` → placeIds로 `getVoteCounts`·`getMyVotedPlaceIds(uid)` → `getProfileNames([...new Set([...places.map(p => p.added_by), trip.owner_id])])`
       - `<MoaIsland trip={trip} currentUserId={user.id} ... />` — 캐시 없음(로그인 화면).
  </action>
  <verify>
    <automated>pnpm --filter web typecheck && pnpm --filter web build</automated>
  </verify>
  <acceptance_criteria>
    - `grep "moaChannelName" apps/web/app/moa/[id]/_components/moa-island.tsx` 1건 + `grep -c "postgres_changes"` == 2 (places INSERT + links UPDATE)
    - `grep "presence" apps/web/app/moa/[id]/_components/moa-island.tsx` == 0건 (D-14)
    - `grep "removeChannel" apps/web/app/moa/[id]/_components/moa-island.tsx` 1건
    - `grep "개 추가됨" apps/web/app/moa/[id]/_components/moa-island.tsx` 1건 (토스트 D-16)
    - `grep "aria-label=\"뒤로\"" apps/web/app/moa/[id]/_components/moa-island.tsx` 1건
    - `grep "notFound()" apps/web/app/moa/[id]/page.tsx` 1건 + `grep "redirect"` 1건 + `grep "'use client'" apps/web/app/moa/[id]/page.tsx` == 0건
    - `pnpm --filter web build` PASS — 라우트 테이블 `/moa/[id]`
  </acceptance_criteria>
  <done>지도탭 전체 배선 — 추출 완료가 realtime으로 리스트·지도·토스트에 반영되는 경로 완성</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: moa-island 채널 lifecycle + reconcile 테스트</name>
  <files>apps/web/__tests__/moa-island.test.tsx</files>
  <read_first>
    - apps/web/__tests__/poll-vote-island.test.tsx (채널 fake mock 선례 — on/subscribe 체인 캡처 방식)
    - apps/web/app/moa/[id]/_components/moa-island.tsx (Task 2 산출물)
  </read_first>
  <behavior>
    - Test 1: 마운트 시 `client.channel('moa:{tripId}')` 1회 + `.on('postgres_changes', {table:'places', event:'INSERT', ...})`·`.on('postgres_changes', {table:'links', event:'UPDATE', ...})` 바인딩 2건 + subscribe 호출
    - Test 2: 언마운트 시 `removeChannel`이 동일 채널 인스턴스로 호출
    - Test 3: 캡처한 places INSERT 콜백 발화 → listPlacesByTrip refetch 호출 + 장소 수 +1이면 toast('장소 1개 추가됨')
    - Test 4: 하트 토글 → castVote 호출·count 즉시 +1, castVote reject 시 count 원복 + 에러 toast (optimistic rollback)
    - Test 5: 마커 탭(onMarkerTap) → place-list에 openPlaceId 전달되어 해당 행 aria-expanded=true
  </behavior>
  <action>
    `apps/web/__tests__/moa-island.test.tsx` 신규:
    - `vi.mock('@/app/moa/[id]/_components/moa-map', ...)` — MoaMap을 `onMarkerTap` 노출하는 스텁 div로 (Google Maps는 jsdom 불가 — 실지도는 manual UAT).
    - `vi.mock('@/lib/supabase/browser')` — fake channel: `{ on: vi.fn().mockReturnThis(), subscribe: vi.fn().mockReturnThis() }`, `channel: vi.fn(() => fakeChannel)`, `removeChannel: vi.fn()` — on 호출 인자에서 콜백 캡처(poll-vote-island.test 선례).
    - `vi.mock('@moajoa/api')` — listPlacesByTrip·listLinksByTrip·getVoteCounts·getProfileNames·castVote·retractVote·triggerExtraction vi.fn.
    - `vi.mock('@/components')` — useToast spy. `vi.mock('next/navigation')`.
    - initial props fixture로 네트워크 hydration 불필요(seed prop seam — vote-island 설계 미러).
    - 배치는 `apps/web/__tests__/`만.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- moa-island</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web test:run -- moa-island` 그린 (5케이스)
    - Test 1이 채널명 문자열 `moa:` prefix를 정확 단언
    - `pnpm --filter web test:run` 전체 무회귀
  </acceptance_criteria>
  <done>D-14 구독 계약(단일 채널·2바인딩·cleanup)과 D-16 토스트·optimistic 찜이 자동 검증으로 고정</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Realtime WAL → 브라우저 | places/links 변경 이벤트 수신 (WALRUS RLS 필터) |
| URL 파라미터 [id] → 데이터 로드 | 임의 trip id 접근 시도 (IDOR 표면) |
| 추출 콘텐츠 → 지도 마커 | name_local이 marker title로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-17 | Info Disclosure | /moa/[id] IDOR | mitigate | getTrip·listPlacesByTrip 등 전부 RLS(can_read_trip 계열) 경유 — 비멤버는 getTrip null → notFound. anon 키 + 쿠키 세션만, 서비스 롤 0 (Task 2) |
| T-24-18 | Info Disclosure | postgres_changes 이벤트 누출 | mitigate | WALRUS가 구독자 JWT로 RLS 평가 — 24-01 스모크가 비멤버 0건 수신을 이미 실증. payload를 상태에 쓰지 않고 refetch reconcile(RLS 재평가) (Task 2) |
| T-24-19 | Tampering | 이벤트 payload 신뢰 | mitigate | payload 무시·refetch reconcile만 — hard-delete(PK만)·hidden_at 엣지에서 드리프트 차단 (RESEARCH 안티패턴 준수) |
| T-24-20 | Info Disclosure | Google Maps 키 노출 | accept | NEXT_PUBLIC 브라우저 키는 설계상 공개(기존 public-board-map과 동일) — referrer 제한은 GCP 콘솔 기존 설정 몫 |
</threat_model>

<verification>
- `pnpm --filter web test:run -- moa-island` 그린
- `pnpm --filter web build` PASS + 라우트 `/moa/[id]`
- presence 0건·postgres_changes 2건·removeChannel 1건 grep
- 로컬 브라우저 스모크(verify-work 이관): 링크 추가 → 분석 중 행 → 추출 완료 시 핀 등장 + 토스트, 마커 탭 → 행 스크롤+펼침
</verification>

<success_criteria>
- 성공 기준 3·4의 지도 측면: 추출 완료 실시간 핀 반영(D-13→D-16 전 사이클), 마커 탭 연동, 추가자 색
- Phase 25/26이 재사용할 moa:{tripId} 단일 채널 인프라 확립
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-06-SUMMARY.md`
</output>
