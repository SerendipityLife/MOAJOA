---
phase: 27-hardening-wrapup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/t/[slug]/_components/guest-surface.tsx
  - apps/web/app/t/[slug]/page.tsx
  - docs/WORKSTREAMS.md
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [NAME-01]

must_haves:
  truths:
    - "/t/[slug] 게스트 표면(하트 버튼 aria-label·텍스트)과 SSR 안내 문구에 '가고싶어'가 없고 '찜'으로 표기된다"
    - "apps/web 라이브 표면 전체에서 '가고싶어'·'보드' 유저 대면 잔여가 0이다 (dead code vote-island·개발자 주석 제외)"
    - "web 테스트 스위트가 같은 커밋에서 그린이다 (D-05 — 실측상 단언 변경 0 예상)"
    - "docs 2종의 역할 기술이 v2.1 현행(웹=입력·저장·편집 풀 서피스, iOS=전면 동결)을 반영한다"
  artifacts:
    - path: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      provides: "찜 하트 버튼 (aria-label + 텍스트)"
      contains: "aria-label=\"찜\""
    - path: "apps/web/app/t/[slug]/page.tsx"
      provides: "찜 안내 문구 SSR 카드"
      contains: "찜"
    - path: "docs/WORKSTREAMS.md"
      provides: "웹=풀 서피스·iOS=동결 역할 기술"
    - path: "docs/ARCHITECTURE.md"
      provides: "역할 반전 헤딩 + trip_id·/t/[slug] 어휘 data flow"
  key_links:
    - from: "guest-surface.tsx 하트 버튼"
      to: "place-list.tsx 호스트 하트 버튼"
      via: "동일 어휘 aria-label=\"찜\" (analog 정합)"
      pattern: "aria-label=\"찜\""
    - from: "docs/WORKSTREAMS.md·ARCHITECTURE.md"
      to: "CLAUDE.md §4.1·§5"
      via: "기준 어휘 일치 (Pitfall 7 — 충돌 금지)"
      pattern: "입력·저장·편집"
---

<objective>
NAME-01 카피 스윕 완성 + 문서 마감. 라이브 유저 대면 표면에 남은 "가고싶어" 정확히 2파일 3곳을 "찜"으로 치환하고(D-04), WORKSTREAMS·ARCHITECTURE의 v2.1 피봇 이전 역할 기술(웹=열람 전용)을 현행으로 갱신한다 (Claude 재량 항목 — 역할 기술 수정 수준, 전면 재작성 아님).

Purpose: 외부 노출 전 유저 대면 어휘 통일 (보드→모아는 이미 완료 실측, 가고싶어→찜 잔여 정리) + 문서가 실제 아키텍처를 반영하도록.
Output: 카피 치환 2파일 + docs 2파일. 코드 식별자(love, vote 등)·개발자 주석·dead code(vote-island) 무접촉 (D-04, D-06).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-hardening-wrapup/27-CONTEXT.md
@.planning/phases/27-hardening-wrapup/27-PATTERNS.md
@apps/web/app/t/[slug]/_components/guest-surface.tsx
@apps/web/app/t/[slug]/page.tsx
@docs/WORKSTREAMS.md
@docs/ARCHITECTURE.md
@CLAUDE.md

<interfaces>
<!-- 목표 어휘 analog — place-list.tsx L213-217 (호스트 표면, 이미 "찜" 사용 중) -->
```tsx
{/* 우: 찜 하트 토글 — 행 토글과 분리(stopPropagation). */}
<button
  type="button"
  aria-label="찜"
  aria-pressed={voted}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: "가고싶어"→"찜" 라이브 표면 치환 (2파일 3곳) + 전수 grep + 테스트 동커밋</name>
  <files>apps/web/app/t/[slug]/_components/guest-surface.tsx, apps/web/app/t/[slug]/page.tsx</files>
  <read_first>
    - apps/web/app/t/[slug]/_components/guest-surface.tsx (L317-327 하트 버튼 — 현재 상태 확인)
    - apps/web/app/t/[slug]/page.tsx (L114-117 안내 문구)
    - apps/web/app/moa/[id]/_components/place-list.tsx L213-217 (목표 어휘 analog — 읽기만, 무변경)
  </read_first>
  <action>
    실측된 3곳만 치환 (D-04 — 라이브 유저 대면 문자열만):

    **(1) guest-surface.tsx L319:** `aria-label="가고싶어"` → `aria-label="찜"` (aria-label은 스크린리더 유저 대면 — NAME-01 대상, Anti-Pattern 3. `data-testid="guest-vote-..."`는 코드 식별자 — 유지)

    **(2) guest-surface.tsx L325:** 버튼 텍스트 노드 `가고싶어` → `찜` (Heart 아이콘·count span·className 무변경)

    **(3) page.tsx L115:** (재량 결정: RESEARCH Open Q2 문안 확정)
    ```tsx
    // 변경 전
    가고 싶은 곳에 <span className="font-semibold">가고싶어!</span>를 눌러주세요.
    // 변경 후
    가고 싶은 곳에 <span className="font-semibold">찜</span>을 눌러주세요.
    ```
    (조사 를→을 — "찜" 받침 정합. 뒷문장 "많이 모인 곳으로 같이 정해요." 무변경)

    **변경 금지 (근거):** `vote-island.tsx`·`vote-island.test.tsx` — dead code, D-06 잠금 (삭제도 금지 — 수술적 변경 원칙, deferred 정리 항목). `map-section.tsx:11` — 개발자 주석, D-04 유지. 앞 문장 "가고 싶은 곳에"의 "가고 싶은"은 서술어이지 기능명 아님 — 치환 대상 아님.

    **D-05 테스트 동커밋:** 실측상 `guest-surface.test.tsx`는 `data-testid`로만 조회하고 page.tsx 문구 단언 테스트도 없음 — 단언 변경 0 예상. 재확인: `grep -rn "가고싶어" apps/web/__tests__ | grep -v vote-island` → 0건이면 테스트 수정 불필요, 히트가 있으면 같은 커밋에서 "찜"으로 동기화.

    커밋: `feat(27-02): sweep 가고싶어→찜 on live guest surfaces (NAME-01)`
  </action>
  <verify>
    <automated>bash -c '[ -z "$(grep -rn "가고싶어" apps/web --include="*.tsx" | grep -v vote-island | grep -v map-section)" ]' && pnpm --filter @moajoa/web test:run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "가고싶어" apps/web --include="*.tsx" | grep -v vote-island | grep -v map-section` → 0줄
    - `grep -c 'aria-label="찜"' "apps/web/app/t/[slug]/_components/guest-surface.tsx"` = 1
    - `grep -c "guest-vote-" "apps/web/app/t/[slug]/_components/guest-surface.tsx"` ≥ 1 (testid 유지)
    - `grep -n "찜" "apps/web/app/t/[slug]/page.tsx"` ≥ 1건 + `grep -c "가고싶어" 동파일` = 0
    - "보드" 라이브 잔여 재확인: `grep -rn "보드" apps/web --include="*.tsx" | grep -vE "클립보드|대시보드|vote-island"` → 0줄
    - `pnpm --filter @moajoa/web test:run` → 전체 그린 (기준선 267 — bare `test`는 watch 함정, `test:run` 필수)
    - `git diff --stat apps/ios packages supabase` → 0파일
  </acceptance_criteria>
  <done>라이브 표면 "가고싶어" 잔여 0, "찜" 표기 적용, web 테스트 같은 커밋 그린 (SC-2 충족 — 단언 변경 필요 시 동커밋 동기화 완료).</done>
</task>

<task type="auto">
  <name>Task 2: WORKSTREAMS·ARCHITECTURE 역할 기술 v2.1 갱신</name>
  <files>docs/WORKSTREAMS.md, docs/ARCHITECTURE.md</files>
  <read_first>
    - docs/WORKSTREAMS.md (전문 — 특히 §1 iOS L33-37, §2 Web L46-67)
    - docs/ARCHITECTURE.md (전문 — 특히 L19-22 헤딩, L24-50 data flow)
    - CLAUDE.md §4.1·§5 (기준 어휘 — Pitfall 7: 이 서술과 충돌 금지)
  </read_first>
  <action>
    재량 항목 실행 (CONTEXT Claude's Discretion — 역할 기술 수정 수준, 전면 재작성 아님). 기준 어휘는 CLAUDE.md §4.1 "web/ Next.js 15 (입력·저장·편집 풀 서피스 — v2.1 웹 퍼스트. 공유 열람·투표 포함)" · §5 "v2.1 웹 퍼스트 동안 iOS는 전면 동결".

    **WORKSTREAMS.md:**
    - §2 헤딩 `## 2️⃣ Web — 공개 보드 열람 (Owner: 프론트엔드)` → `## 2️⃣ Web — 입력·저장·편집 풀 서피스 (Owner: 프론트엔드)`
    - §2 상태 문장 `**상태:** 작동. 다만 *현재는 dev tool 성격의 폼*도 포함 (보드 생성·링크 추가). 본래 web의 역할은 **열람·공유 랜딩**만.` → v2.1 피봇(Phase 23, 2026-07 공식 반전) 반영: 웹이 입력·저장·편집 풀 서피스이며 공유 열람·투표·채팅 포함. 현행 라우트 `/onboarding`(추가 위저드)·`/moa/[id]`(지도 보드·채팅)·`/t/[slug]`(공유 열람+게스트 참여)·`/poll/[code]` 명시
    - §2 할 일 목록(L55-67)의 `/b/[slug]`·`/boards`·dev-tools 격리 항목 → 완료/대체됨 표시 또는 현행 라우트 기준으로 갱신 (역사 기록이 필요한 항목은 ~~취소선~~+사유 한 줄, 통째 삭제보다 우선)
    - §1 iOS(L33-37): 구 라우트(`apps/ios/app/boards/[id].tsx`) 기반 할 일 위에 **"v2.1 웹 퍼스트 동안 iOS 전면 동결 (Phase 23 피봇)"** 명시 — 트립 4탭 구조는 v2.0 산출물임을 한 줄로

    **ARCHITECTURE.md:**
    - L19 헤딩 `### Next.js for Web (열람), Expo for iOS (저장)` → `### Next.js for Web (입력·저장·편집 풀 서피스), Expo for iOS (v2.1 동결)`
    - 헤딩 아래 불릿: 공유 링크 비로그인 열람 서술은 유지하되(여전히 사실), iOS Share Extension 서술에 v2.1 동결 상태 부기
    - Data flow 2개(L24-50): `[iOS] Share Sheet` 기점 → 웹 기점(`/onboarding` 위저드·`/moa` add-sheet)으로, `board_id` → `trip_id`, `/b/<slug>` → `/t/[slug]`, RLS 문구 "board editor" → "trip editor (can_edit_trip)" 어휘 갱신. 흐름 구조 자체는 유지 — 어휘·기점만 수정

    **금지:** CLAUDE.md·`.planning/` 문서 수정 (범위 외), 코드 파일 접촉, 문서 전면 재구성.

    커밋: `docs(27-02): update WORKSTREAMS·ARCHITECTURE role descriptions to v2.1 web-first reality`
  </action>
  <verify>
    <automated>bash -c 'grep -q "입력·저장·편집" docs/WORKSTREAMS.md && grep -q "전면 동결" docs/WORKSTREAMS.md && grep -q "입력·저장·편집" docs/ARCHITECTURE.md && ! grep -qE "board_id|/b/<slug>" docs/ARCHITECTURE.md && ! grep -q "열람·공유 랜딩" docs/WORKSTREAMS.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "입력·저장·편집" docs/WORKSTREAMS.md` ≥ 1, `grep -c "전면 동결" docs/WORKSTREAMS.md` ≥ 1
    - `grep -n "열람·공유 랜딩" docs/WORKSTREAMS.md` → 0건, `grep -n "공개 보드 열람" docs/WORKSTREAMS.md` → 0건
    - `grep -nE "board_id|/b/<slug>" docs/ARCHITECTURE.md` → 0건, `grep -c "trip_id" docs/ARCHITECTURE.md` ≥ 1
    - `grep -n "Web (열람)" docs/ARCHITECTURE.md` → 0건
    - `git diff --stat` 이 커밋 docs/ 2파일만
  </acceptance_criteria>
  <done>docs 2종이 웹=풀 서피스·iOS=동결 현행을 반영하고 구 어휘(board_id·/b/slug·열람 전용) 잔여 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (신규 없음) | 카피·문서 텍스트 변경만 — 신규 신뢰 경계·데이터 흐름 0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06 | T | guest-surface 하트 버튼 — 카피 변경이 게이트 로직(openGate)·testid를 건드릴 위험 | mitigate | aria-label·텍스트 노드만 치환, onClick·data-testid diff 0 (acceptance grep으로 강제) |
| T-27-07 | I | docs 갱신 중 비밀값·env 노출 | accept | 텍스트 역할 기술만 — env·secret 서술 무접촉 (기존 문서에 비밀값 없음) |
</threat_model>

<verification>
- `grep -rn "가고싶어" apps/web --include="*.tsx" | grep -v vote-island | grep -v map-section` → 0줄
- `pnpm --filter @moajoa/web test:run` → 그린 (동커밋)
- docs grep 세트 통과 (Task 2 acceptance)
- `git diff main --stat -- apps/ios supabase packages` → 0파일
</verification>

<success_criteria>
- SC-2 충족: 유저 대면 카피 전반 보드→"모아"(잔여 0 재확인)·가고싶어→"찜", 테스트 동커밋 그린, 코드 식별자 유지
- 문서 역할 기술이 CLAUDE.md 현행 서술과 정합 (Pitfall 7)
- vote-island(dead code) 무접촉 — deferred 항목 유지 (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/27-hardening-wrapup/27-02-SUMMARY.md`
</output>
