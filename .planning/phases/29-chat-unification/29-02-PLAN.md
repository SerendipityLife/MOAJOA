---
phase: 29-chat-unification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/__tests__/moa-island.test.tsx
  - apps/web/app/t/[slug]/_components/guest-surface.tsx
  - apps/web/__tests__/guest-surface.test.tsx
autonomous: true
requirements: [CHAT-04, CHAT-07]

must_haves:
  truths:
    - "dates 공유 게스트가 join 후 both과 동일하게 MoaIsland(채팅탭 포함)를 본다"
    - "dates 재방문 멤버도 hydrate 후 곧장 MoaIsland가 마운트된다 (poll-only 화면에 안 갇힘)"
    - "dates 게스트(voter)에게 장소 추가 FAB가 노출되지 않는다 (실패하는 버튼 금지, F-2)"
    - "호스트 /moa와 both/places 게스트의 기존 동작은 무회귀 (기존 moa-island 테스트 무수정 그린)"
  artifacts:
    - path: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      provides: "dates 분기의 both 미러 (D-01) — MoaIsland + pollSlot + hidePlaceAdd"
      contains: "hidePlaceAdd"
    - path: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      provides: "hidePlaceAdd optional prop (미전달=기존 렌더 동일)"
      contains: "hidePlaceAdd?: boolean"
  key_links:
    - from: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      to: "MoaIsland (dates 분기)"
      via: "joined && moaSeed 조건 마운트"
      pattern: "hidePlaceAdd"
    - from: "guest-surface handleConfirmNickname"
      to: "hydrateMember"
      via: "shareMode 무관 무조건 호출"
      pattern: "await hydrateMember\\(uid, nick\\)"
---

<objective>
D-01: dates 공유를 both 경로로 수렴 — `shareMode !== 'dates'` 가드 2곳 제거, dates 분기를 both 미러(joined 시 `MoaIsland + hideHostControls + pollSlot`)로 재작성, `embedded` 전달 라인 은퇴. F-2: MoaIsland에 `hidePlaceAdd` additive optional prop을 추가해 voter 게스트에게 장소 추가 FAB를 숨긴다.

Purpose: CHAT-04 — dates 게스트가 채팅탭(trip_messages)에 접근·참여. 신규 컴포넌트 0, 채팅탭·presence·멘션 전부 기존 MoaIsland 무료 재사용. DB 변경 0 (voter role은 이미 trip_messages RLS 통과 — RESEARCH Q3).
Output: guest-surface dates→both 수렴 + moa-island hidePlaceAdd + 테스트 갱신.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-chat-unification/29-CONTEXT.md (D-01)
@.planning/phases/29-chat-unification/29-RESEARCH.md (Q4 — 변경 지점 전수 + 깨질 지점 4종, Pitfall 5/6/8)
@.planning/phases/29-chat-unification/29-PATTERNS.md (§1 guest-surface 수렴 excerpt)
@.planning/phases/29-chat-unification/29-UI-SPEC.md (Screen Contract 1 — dates 비join 현행 유지 A-2, F-2 계약)

<interfaces>
<!-- moa-island.tsx 현재 seam (실측) — hidePlaceAdd는 이 옆에 append -->
```typescript
// MoaIslandProps (:63-65)
hideHostControls?: boolean;
pollSlot?: ReactNode;
// FAB 렌더 조건 (:621경 — 25-06 gap fix 주석 아래)
{!addOpen && !shareOpen && sheetAnchor !== 'expanded' && (
  <button type="button" aria-label="장소 추가" ... />
)}
```
<!-- guest-surface.tsx both 분기 (:345-378) — dates가 미러할 원형. pollSection은 :280-304 (PollVoteIsland embed) -->
```tsx
{joined && moaSeed ? (
  <MoaIsland
    {...moaSeed}
    hideHostControls
    pollSlot={
      pollMeta != null ? (
        <section>
          <h3 className="text-sm font-semibold text-neutral-700">날짜 정하기</h3>
          {pollSection}
        </section>
      ) : undefined
    }
  />
) : ( /* 비join sibling */ )}
{gate}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoaIsland `hidePlaceAdd` additive optional prop (F-2)</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/__tests__/moa-island.test.tsx</files>
  <read_first>
    - apps/web/app/moa/[id]/_components/moa-island.tsx (props 선언 :60-95 · FAB 블록 :617-631)
    - apps/web/__tests__/moa-island.test.tsx 전문 (기존 15+ 케이스 — 무수정 보존 대상, 채널/컴포넌트 mock 하네스 재사용)
  </read_first>
  <behavior>
    - Test A (신규 append): `hidePlaceAdd` 전달 시 `screen.queryByLabelText('장소 추가')`가 null
    - Test B (신규 append): 미전달 시 FAB(`aria-label="장소 추가"`) 렌더 유지 (기존 렌더 동일 — 회귀 앵커)
  </behavior>
  <action>
**RED (commit `test(29-02): failing FAB hide cases`):** moa-island.test.tsx **하단에 append-only**로 위 2케이스 추가 — 기존 케이스 본문 무수정 (CHAT-07 회귀 앵커: "기존 테스트 무수정 그린"이 additive prop의 무회귀 증명, RESEARCH CHAT-07 행). Test A가 실패함을 확인.

**GREEN (commit `feat(29-02): MoaIsland hidePlaceAdd prop`):** moa-island.tsx 3줄 surgical:
1. `MoaIslandProps`의 `hideHostControls?: boolean;`(:63) 옆에 `/** F-2: 쓰기 권한 없는 role(dates voter 게스트)에겐 장소 추가 FAB 미노출 — 실패하는 버튼 금지. 미전달=기존 렌더 동일. */ hidePlaceAdd?: boolean;` append
2. destructure(:92-93 영역)에 `hidePlaceAdd,` 추가
3. FAB 조건을 `{!hidePlaceAdd && !addOpen && !shareOpen && sheetAnchor !== 'expanded' && (`로 변경 (조건 맨 앞 append — 나머지 조건·본문 diff 0)

그 외 라인(채널·reconcile·PlaceSheet·AddSheet·ShareSheet·plan) 일절 무접촉. HC-6: moa-island 계약 무변경 — additive optional prop만.
  </action>
  <verify>
    <automated>CI=true pnpm --filter web test -- __tests__/moa-island.test.tsx && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hidePlaceAdd" 'apps/web/app/moa/[id]/_components/moa-island.tsx'` = 3 (prop 선언·destructure·FAB 가드)
    - moa-island.test.tsx 기존 케이스 삭제/수정 라인 0 (`git diff`가 append-only)
    - `CI=true pnpm --filter web test -- __tests__/moa-island.test.tsx` exit 0 (기존 + 신규 2)
    - `pnpm --filter web typecheck` exit 0
  </acceptance_criteria>
  <done>hidePlaceAdd 전달 시 FAB 미렌더·미전달 시 기존 동일 — 기존 moa-island 스위트 무수정 그린</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: guest-surface dates→both 수렴 (가드 2곳 제거 + dates 분기 재작성 + embedded 라인 은퇴)</name>
  <files>apps/web/app/t/[slug]/_components/guest-surface.tsx, apps/web/__tests__/guest-surface.test.tsx</files>
  <read_first>
    - apps/web/app/t/[slug]/_components/guest-surface.tsx 전문 (세션 effect :99-126 · handleConfirmNickname :242-264 · pollSection :280-304 · dates 분기 :336-343 · both 분기 :345-378)
    - apps/web/__tests__/guest-surface.test.tsx 전문 (PollVoteIsland 스텁 :99-104의 embedded 속성 · dates/embedded 단언 :200, :213 — 재작성 대상)
    - .planning/phases/29-chat-unification/29-PATTERNS.md §1 (변경 좌표·JSX 원형)
  </read_first>
  <behavior>
    - Test 1 (재작성): dates + 재방문 멤버(세션 uid + getMyTripRole=role) → hydrate 후 MoaIsland 스텁 마운트 + `hidePlaceAdd` 전달됨 (poll-only 화면에 안 갇힘 — Pitfall 5)
    - Test 2 (재작성): dates + 비join → MoaIsland 미마운트, pollSection(PollVoteIsland 스텁)+게이트만 (A-2 현행 유지)
    - Test 3 (재작성): dates 첫 join(handleConfirmNickname) → ensureGuestMember 후 hydrateMember 호출 → MoaIsland 마운트, pollSlot에 "날짜 정하기" 섹션 (pollSection이 sibling에 중복 렌더되지 않음 — Pitfall 8)
    - Test 4 (기존 both 케이스): both joined → MoaIsland에 `hidePlaceAdd` **미전달** (editor는 FAB 유지)
  </behavior>
  <action>
**RED (commit `test(29-02): rewrite dates-branch cases for both-convergence`):** guest-surface.test.tsx 갱신 —
- PollVoteIsland 스텁(:99-104)에서 `embedded` prop·`data-embedded` 속성 제거 (prop 자체가 29-03에서 소멸)
- :213 "both 임베드는 embedded=true, dates 전용 화면은 embedded=false (한마디·presence 유지)" 케이스 삭제 → 위 Test 1~3으로 대체
- :200 케이스 이름의 "한마디" 문구 재서술 (예: "빈 집계 노이즈 제거" — HC-7 grep 앵커, 24-05/26-02 주석 재서술 선례)
- MoaIsland 스텁이 `hidePlaceAdd`를 data 속성으로 노출하도록 확장 (기존 스텁 하네스 방식 유지)
현행 구현에서 Test 1~3 실패 확인.

**GREEN (commit `feat(29-02): converge dates share onto the both MoaIsland path (D-01)`):** guest-surface.tsx 4곳 — **가드 2곳은 반드시 같은 커밋에서 제거 (Pitfall 5: 한 곳만 제거하면 반쪽 회귀)**:
1. **세션 effect (:112-118):** `if (shareMode !== 'dates') { await hydrateMember(uid, stored); if (!active) return; }` → 가드 제거, 무조건 `await hydrateMember(uid, stored); if (!active) return;`
2. **handleConfirmNickname (:248):** `if (shareMode !== 'dates') await hydrateMember(uid, nick);` → `await hydrateMember(uid, nick);` — **순서 불변: hydrateMember 완료 → setJoined(true) → gateResolve** (RESEARCH Q4-2: 순서 바꾸면 게이트 resolve와 MoaIsland 마운트 경합)
3. **pollSection (:299-301):** 주석 2줄(한마디 언급)과 `embedded={shareMode === 'both'}` 라인 삭제 — deviceToken/nickname/onRequireMember 전달은 무변경
4. **dates 분기 (:336-343):** both 분기(:345-378) 미러로 재작성:
```tsx
if (shareMode === 'dates') {
  // D-01: dates도 join 후 both과 동일하게 MoaIsland(채팅탭 포함) 마운트 — pollSlot에
  // 날짜 정하기, 장소는 빈 상태(PlaceList 기존 empty). 비join은 현행 유지(A-2).
  // hidePlaceAdd: dates join role은 voter(D-A1) — places INSERT 불가라 FAB 숨김(F-2).
  return (
    <>
      {joined && moaSeed ? (
        <MoaIsland
          {...moaSeed}
          hideHostControls
          hidePlaceAdd
          pollSlot={
            pollMeta != null ? (
              <section>
                <h3 className="text-sm font-semibold text-neutral-700">날짜 정하기</h3>
                {pollSection}
              </section>
            ) : undefined
          }
        />
      ) : (
        pollSection
      )}
      {gate}
    </>
  );
}
```
**금지:** joined 시 pollSection을 pollSlot과 sibling 양쪽에 렌더 (Pitfall 8 — 같은 `poll:{tripId}` topic 2채널 = 투표 broadcast 배달 탈취). both/places 분기·hydrateMember 본문·ensureGuestMember·readOnlyPlaces는 무접촉 (hydrateMember의 getTrip은 /t 경로에서 안전 — shareMoa가 visibility='shared' 설정, RESEARCH Q4-1). both 분기에는 hidePlaceAdd **미전달** (both join=editor, FAB 유지).

**참고(과도기 상태 — 의도됨):** 이 plan 후 PollVoteIsland는 embedded 미전달로 pollSlot 내부에 자체 한마디를 일시 렌더할 수 있다 — 29-03(Wave 2)이 PollChat 자체를 제거하며 소멸. 중간 커밋은 스위트 그린이며 배포는 phase 완료 후.
  </action>
  <verify>
    <automated>CI=true pnpm --filter web test -- __tests__/guest-surface.test.tsx __tests__/moa-island.test.tsx && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "shareMode !== 'dates'" 'apps/web/app/t/[slug]/_components/guest-surface.tsx'` (가드 2곳 전부 소멸)
    - `! grep -n "embedded" 'apps/web/app/t/[slug]/_components/guest-surface.tsx'` (전달 라인 은퇴)
    - `! grep -n "한마디" 'apps/web/app/t/[slug]/_components/guest-surface.tsx' apps/web/__tests__/guest-surface.test.tsx` (HC-7 앵커 — 이 두 파일 몫 선소진)
    - `grep -c "hidePlaceAdd" 'apps/web/app/t/[slug]/_components/guest-surface.tsx'` = 1 (dates 분기만 — both은 미전달, 주석 카운트 제외 위해 JSX prop 라인 기준으로 확인)
    - dates 분기에 `hideHostControls`와 `pollSlot` 존재 (both 미러)
    - `CI=true pnpm --filter web test` 해당 두 파일 exit 0 · `pnpm --filter web typecheck` exit 0
    - `git diff --stat`에 apps/ios 0파일 · moa-chat.tsx/moa-tab-bar.tsx/nickname-gate-sheet.tsx 0파일
  </acceptance_criteria>
  <done>dates 게스트: 비join=현행(poll+게이트), join 후=MoaIsland(채팅탭·pollSlot·FAB 숨김) — 재방문 멤버 포함. 기존 both/places 경로 diff 최소</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dates 게스트(voter) → places/votes/trip_messages 쓰기 표면 | UI가 넓어지며 voter가 닿는 클라이언트 표면 확대 지점 |
| 게스트 브라우저 → moa:{tripId} realtime 채널 | join 후 구독 — 이벤트 수신 권한 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07 | Elevation of Privilege | dates voter의 장소 추가 시도 (FAB) | mitigate | `hidePlaceAdd`로 UI 미노출(F-2) + DB `can_edit_trip` RLS(0016)가 최종 방어 — 심층방어 2겹 (25-06 hideHostControls 선례). dates→both 모드 변경 trip의 기존 voter 엣지는 RLS 거부+실패 토스트로 수용(기존 동일 엣지) |
| T-29-08 | Information Disclosure | 비멤버 realtime 도청 | mitigate | 기존 경로 무변경 — join 후에만 MoaIsland(=채널) 마운트 (guest-surface Pitfall 4 계승), WALRUS가 구독자 JWT로 RLS 재평가 (25-05 실증) |
| T-29-09 | Spoofing | 채팅 user_id 위조 | mitigate | 기존 해소 — 0028 BEFORE-INSERT 트리거가 `auth.uid()` 서버 파생, INSERT RLS가 user_id=auth.uid() 검증 |
| T-29-10 | Tampering | poll 이중 채널(배달 탈취)로 투표 UI 교란 | mitigate | joined 시 pollSection을 pollSlot에만 렌더 (Pitfall 8) — 테스트로 sibling 중복 렌더 0 단언 |
</threat_model>

<verification>
- `CI=true pnpm --filter web test` 전체 그린 (guest-surface 신규 + moa-island 기존 무수정 + 나머지 무회귀)
- `pnpm --filter web typecheck` exit 0
- `pnpm --filter web build` PASS (`ƒ /t/[slug]` 라우트 유지)
</verification>

<success_criteria>
- dates 공유 게스트가 join 후 채팅탭(trip_messages 영속 이력·presence·멘션)에 접근·참여 (CHAT-04 웹 측)
- dates 재방문 멤버 MoaIsland 마운트 (Pitfall 5 봉합 — 테스트 단언)
- 기존 moa-island 테스트 무수정 그린 = additive prop 무회귀 증명 (CHAT-07)
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-unification/29-02-SUMMARY.md`
</output>
