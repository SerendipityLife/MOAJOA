---
phase: 24-host-flow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ui-tokens/src/index.ts
  - packages/api/src/queries/trips.ts
  - packages/api/src/queries/memberships.ts
  - packages/api/src/queries/profiles.ts
  - packages/api/src/queries/index.ts
  - packages/api/src/queries/__tests__ or packages/api/src (기존 테스트 배치 위치 미러)
  - apps/web/lib/member-color.ts
  - apps/web/lib/place-sort.ts
  - apps/web/lib/marker-svg.ts
  - apps/web/__tests__/member-color.test.ts
  - apps/web/__tests__/place-sort.test.ts
  - apps/web/__tests__/marker-svg.test.ts
autonomous: true
requirements: [MOA-02, MOA-06, ONBOARD-03]
must_haves:
  truths:
    - "member 팔레트 6색이 ui-tokens에 정의되어 웹·iOS가 공유 가능하다 (D-20)"
    - "memberColor가 호스트=brand.500 고정, 참여자=join순 순환 배정을 낸다 (MOA-06)"
    - "sortByLove가 찜 desc·동률 seq_no asc로 정렬하되 입력을 변형하지 않는다 (MOA-02)"
    - "createMoaDraft가 companion 포함 trips INSERT를 수행한다 (ONBOARD-03, 기존 createTrip 무수정)"
  artifacts:
    - path: "packages/ui-tokens/src/index.ts"
      provides: "colors.member 6색 팔레트 (UI-SPEC A-2 확정값)"
      contains: "#FF7043"
    - path: "apps/web/lib/member-color.ts"
      provides: "join순 순환 배정 순수함수"
      exports: ["memberColor"]
    - path: "apps/web/lib/place-sort.ts"
      provides: "찜 정렬 comparator 순수함수"
      exports: ["sortByLove"]
    - path: "packages/api/src/queries/profiles.ts"
      provides: "added_by → display_name 매핑 쿼리"
      exports: ["getProfileNames"]
  key_links:
    - from: "apps/web/lib/member-color.ts"
      to: "packages/ui-tokens"
      via: "colors.member / colors.brand[500] import"
      pattern: "colors\\.member"
    - from: "packages/api/src/queries/trips.ts"
      to: "trips 테이블"
      via: "companion 포함 insert"
      pattern: "companion"
---

<objective>
Phase 24 전체가 소비하는 순수 계약 레이어를 잠근다 — ui-tokens member 팔레트(D-20), memberColor·sortByLove 순수함수(MOA-06·MOA-02), marker-svg fill 확장, packages/api 신규 typed query 3종(createMoaDraft·listTripMembers·getProfileNames).

Purpose: 온보딩(24-04)·리스트(24-05)·지도(24-06)가 병렬로 import할 단일 정의 seam. 순수함수는 이 plan에서 단위 테스트로 고정한다.
Output: 토큰 append + lib 순수함수 2종 + api 쿼리 3종 + 테스트 그린.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-host-flow/24-CONTEXT.md (D-20)
@.planning/phases/24-host-flow/24-UI-SPEC.md (§Member Pin Palette — A-2 확정 6색)
@.planning/phases/24-host-flow/24-PATTERNS.md (§member-color, §place-sort, §marker-svg, §createMoaDraft)
@packages/ui-tokens/src/index.ts
@packages/api/src/queries/trips.ts
@packages/api/src/queries/memberships.ts
@apps/web/lib/marker-svg.ts

<interfaces>
<!-- 실측 완료 계약 — executor는 탐색 없이 그대로 사용 -->

From packages/core/src/schemas/trip.ts (실존):
```typescript
export const TripCreateDraftSchema = z.object({
  title: z.string().min(1).max(Limits.TripTitleMax),
  city_code: z.string().max(20),
  start_date: z.string().date().nullable(),
  end_date: z.string().date().nullable(),
  companion: z.string().max(20).nullable(),
}).refine(/* both-or-null */).refine(/* end>=start */);
export type TripCreateDraft = z.infer<typeof TripCreateDraftSchema>;
```

From packages/api/src/queries/trips.ts (실존 — 무수정, analog):
```typescript
export async function createTrip(client: MoajoaSupabaseClient, input: TripCreate): Promise<Trip>
// insert({ title, city_code, start_date, end_date }).select('*').single(), {error} throw
```

From packages/api/src/queries/memberships.ts (실존 — accepted 필터 idiom):
```typescript
// getMyTripRole이 쓰는 accepted 판별: .not('accepted_at', 'is', null)
// memberships 컬럼: id, trip_id, user_id, role, invited_by, accepted_at, created_at
```

profiles 테이블 (0016 실존): id uuid PK, display_name text not null(1..60) — RLS "read all authenticated"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ui-tokens member 팔레트 + member-color 순수함수</name>
  <files>packages/ui-tokens/src/index.ts, apps/web/lib/member-color.ts, apps/web/__tests__/member-color.test.ts</files>
  <read_first>
    - packages/ui-tokens/src/index.ts (colors 객체 전체 — category 팔레트의 append 형태와 as const 스타일 확인)
    - apps/web/lib/marker-svg.ts (token-only 순수함수 doc comment 스타일 analog)
  </read_first>
  <behavior>
    - Test 1: memberColor(ownerId, ownerId, []) === colors.brand[500] (호스트 = 브랜드 고정)
    - Test 2: memberColor('u1', 'owner', ['u1','u2']) === colors.member[0] (join순 첫 번째)
    - Test 3: 7번째 멤버(index 6)는 colors.member[0]으로 순환 (% 6)
    - Test 4: 목록에 없는 userId는 colors.member[0] (방어적 기본값)
    - Test 5: colors.member.length === 6 && colors.member[0] === '#FF7043' (A-2 잠금 회귀 가드, .toEqual 전체 배열 대조)
  </behavior>
  <action>
    1. `packages/ui-tokens/src/index.ts`의 colors 객체에 **append만** (기존 키 무수정 — 충돌 위험 영역, CLAUDE.md §4.2). UI-SPEC A-2 확정 6색 그대로:
    ```typescript
    /** Phase 24 D-20 — 참여자 핀 팔레트. 호스트는 brand[500] 고정(팔레트 밖).
     *  join순(created_at asc) 인덱스 % 6 순환 배정 — apps/web/lib/member-color.ts가 소비. */
    member: ['#FF7043', '#AB47BC', '#26A69A', '#FFB300', '#EC407A', '#7CB342'],
    ```
    (기존 colors 객체의 as const/타입 스타일에 맞춰 배치 — category 팔레트 옆)
    2. `apps/web/lib/member-color.ts` 신규:
    ```typescript
    import { colors } from '@moajoa/ui-tokens';

    /**
     * D-20 pin color assignment. Owner has NO memberships row (join_moa owner guard),
     * so callers pass participants only, sorted by created_at asc (join order).
     * Colors come from ui-tokens ONLY — never interpolate user strings (T-05-05-01).
     */
    export function memberColor(
      userId: string,
      ownerId: string,
      memberIdsInJoinOrder: string[],
    ): string {
      if (userId === ownerId) return colors.brand[500];
      const i = memberIdsInJoinOrder.indexOf(userId);
      return colors.member[(i < 0 ? 0 : i) % colors.member.length];
    }
    ```
    3. `apps/web/__tests__/member-color.test.ts` — behavior 5케이스. mock 불필요(순수함수 직접 import). 워크스페이스 import에 `.js` extension 금지.
    4. iOS는 ui-tokens를 import하므로 토큰 변경 후 `pnpm --filter @moajoa/ios test` 1회 무회귀 확인 (VALIDATION Phase gate 규칙 — apps/ios 코드는 무접촉).
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- member-color</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'#FF7043', '#AB47BC', '#26A69A', '#FFB300', '#EC407A', '#7CB342'" packages/ui-tokens/src/index.ts` ≥ 1
    - `git diff packages/ui-tokens/src/index.ts`에 삭제 라인 0 (append-only)
    - `grep "export function memberColor" apps/web/lib/member-color.ts` 1건
    - `pnpm --filter web test:run -- member-color` 그린 (5케이스)
    - `pnpm --filter @moajoa/ios test` 무회귀 (128 tests)
  </acceptance_criteria>
  <done>팔레트 6색 토큰 + memberColor 순수함수 + 테스트 그린 + iOS 무회귀</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sortByLove comparator + marker-svg fill 파라미터 확장</name>
  <files>apps/web/lib/place-sort.ts, apps/web/__tests__/place-sort.test.ts, apps/web/lib/marker-svg.ts, apps/web/__tests__/marker-svg.test.ts</files>
  <read_first>
    - apps/web/lib/marker-svg.ts (전체 — buildMarkerIconUrl 시그니처·SVG 조립·fill 분기 현행)
    - apps/web/__tests__/marker-svg.test.ts (기존 케이스 — 무수정 보존 대상)
  </read_first>
  <behavior>
    - place-sort Test 1: 찜 수 내림차순 정렬 (counts {a:2, b:5} → [b, a])
    - place-sort Test 2: 동률 시 seq_no 오름차순 (둘 다 3표, seq 7 vs 2 → seq 2 먼저)
    - place-sort Test 3: counts에 없는 id는 0 취급
    - place-sort Test 4: 입력 배열 비변형 (원본 순서 유지 — spread copy 검증)
    - place-sort Test 5: 반환 요소의 seq_no는 입력과 동일 객체 참조 (순번 표기 불변 — Pitfall 9)
    - marker-svg Test 6: fill '#FF7043' 전달 시 인코딩된 URL에 해당 색 포함
    - marker-svg Test 7: fill 미전달 시 기존 출력과 동일 (기존 테스트 전부 무수정 그린)
  </behavior>
  <action>
    1. `apps/web/lib/place-sort.ts` 신규 (RESEARCH Pattern 5 그대로 — 구조 타입으로 Place 의존 회피):
    ```typescript
    /** MOA-02 — 찜 desc, 동률 seq_no asc. 정렬은 배열 순서만 바꾼다.
     *  순번 배지는 항상 place.seq_no 표기 — 정렬 인덱스 넘버링 금지 (Pitfall 9). */
    export function sortByLove<T extends { id: string; seq_no: number }>(
      places: T[],
      loveCounts: Record<string, number>,
    ): T[] {
      return [...places].sort(
        (a, b) => (loveCounts[b.id] ?? 0) - (loveCounts[a.id] ?? 0) || a.seq_no - b.seq_no,
      );
    }
    ```
    2. `apps/web/lib/marker-svg.ts` — buildMarkerIconUrl input에 `fill?: string` 옵션 추가. 기존 분기 결과를 기본값으로: `const resolvedFill = input.fill ?? <기존 fill 분기식>`. SVG 조립 구조는 무변경 — **user 문자열을 SVG에 삽입하지 않는 계약(T-05-05-01) 유지, fill은 ui-tokens 리터럴만 받는다는 doc comment 추가**. 기존 호출부 무영향(옵션 파라미터).
    3. `apps/web/__tests__/place-sort.test.ts` 신규 — behavior 1~5. `apps/web/__tests__/marker-svg.test.ts`에 fill 케이스 2개 **append** (기존 케이스 무수정).
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- place-sort && pnpm --filter web test:run -- marker-svg</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export function sortByLove" apps/web/lib/place-sort.ts` 1건
    - `grep "fill?: string" apps/web/lib/marker-svg.ts` 1건
    - `git diff apps/web/__tests__/marker-svg.test.ts`에 삭제 라인 0 (append-only)
    - place-sort 5케이스 + marker-svg 전체(기존+2) 그린
  </acceptance_criteria>
  <done>정렬 comparator·fill 확장 잠금 — MOA-02 정렬 계약과 MOA-06 핀 색 렌더 경로가 단위 테스트로 고정</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: api typed query 3종 — createMoaDraft·listTripMembers·getProfileNames</name>
  <files>packages/api/src/queries/trips.ts, packages/api/src/queries/memberships.ts, packages/api/src/queries/profiles.ts, packages/api/src/queries/index.ts, packages/api/src/queries/trips.test.ts 또는 기존 테스트 파일 위치, packages/api/src/queries/memberships.test.ts, packages/api/src/queries/profiles.test.ts</files>
  <read_first>
    - packages/api/src/queries/trips.ts (createTrip L74-94 — insert shape·{error} throw 하우스 계약, shareMoa 옆 append 선례)
    - packages/api/src/queries/memberships.ts (getMyTripRole의 accepted_at not-null 필터 idiom)
    - packages/api의 기존 테스트 파일 위치·makeChain mock 하네스 (`ls packages/api/src/**/*.test.ts` 후 trips 테스트의 makeChain 패턴)
  </read_first>
  <behavior>
    - Test 1: createMoaDraft가 from('trips').insert에 title·city_code·start_date·end_date·**companion** 5필드를 전달하고 select('*').single() 후 Trip 반환
    - Test 2: createMoaDraft — {error} 존재 시 throw (하우스 계약)
    - Test 3: listTripMembers가 select('user_id, created_at') + eq('trip_id') + not('accepted_at','is',null) + order('created_at', {ascending: true}) 체인 호출
    - Test 4: getProfileNames([]) → from 미호출 + {} 반환 (short-circuit)
    - Test 5: getProfileNames(['u1'])가 from('profiles').select('id, display_name').in('id', ids) → {u1: '이름'} 매핑
  </behavior>
  <action>
    1. `packages/api/src/queries/trips.ts` — 기존 함수 전부 무수정(23-06 선례), `createTrip` 아래 append:
    ```typescript
    /**
     * Phase 24 온보딩 draft 생성 (ONBOARD-03, D-03). createTrip과 별도 —
     * TripCreateDraft는 dates nullable + companion(0025)을 나른다. 기존 createTrip(iOS) 무수정.
     * seq_no·visibility·representative_id·share_slug는 전부 서버 몫(트리거) — 클라이언트 미전송.
     */
    export async function createMoaDraft(
      client: MoajoaSupabaseClient,
      input: TripCreateDraft,
    ): Promise<Trip> {
      const { data, error } = await client
        .from('trips')
        .insert({
          title: input.title,
          city_code: input.city_code,
          start_date: input.start_date,
          end_date: input.end_date,
          companion: input.companion,
        })
        .select('*')
        .single();
      if (error) throw error;
      return data as Trip;
    }
    ```
    (`TripCreateDraft` 타입은 `@moajoa/core`에서 import — `.js` extension 금지)
    2. `packages/api/src/queries/memberships.ts` — 기존 함수 무수정, append:
    ```typescript
    export interface TripMember {
      user_id: string;
      created_at: string;
    }

    /**
     * Phase 24 D-20 — 핀 색 join순 배정의 데이터 소스. accepted 멤버만,
     * created_at asc (join order). owner는 memberships 행이 없다(설계상).
     */
    export async function listTripMembers(
      client: MoajoaSupabaseClient,
      tripId: string,
    ): Promise<TripMember[]> {
      const { data, error } = await client
        .from('memberships')
        .select('user_id, created_at')
        .eq('trip_id', tripId)
        .not('accepted_at', 'is', null)
        .order('created_at', { ascending: true });
      if (error) throw error;
      return (data ?? []) as TripMember[];
    }
    ```
    3. `packages/api/src/queries/profiles.ts` 신규:
    ```typescript
    import type { MoajoaSupabaseClient } from '../client';

    /**
     * Phase 24 MOA-06 — "{닉네임}님이 담음". places.added_by → display_name.
     * profiles RLS는 read-all-authenticated (0016) — 로그인 화면 전용 소비.
     */
    export async function getProfileNames(
      client: MoajoaSupabaseClient,
      userIds: string[],
    ): Promise<Record<string, string>> {
      if (userIds.length === 0) return {};
      const { data, error } = await client
        .from('profiles')
        .select('id, display_name')
        .in('id', userIds);
      if (error) throw error;
      return Object.fromEntries(
        ((data ?? []) as { id: string; display_name: string }[]).map((p) => [p.id, p.display_name]),
      );
    }
    ```
    4. `packages/api/src/queries/index.ts`에 `export * from './profiles';` append.
    5. 테스트: 기존 api 테스트의 makeChain/makeClient 하네스 미러(23-06 trips.test.ts 선례) — behavior 5케이스. 기존 테스트 파일이 있으면 append, profiles는 신규 파일.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api test:run && pnpm --filter @moajoa/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function createMoaDraft" packages/api/src/queries/trips.ts` 1건 + insert 블록에 `companion: input.companion` 존재
    - `git diff packages/api/src/queries/trips.ts`에서 기존 createTrip·shareMoa 함수 본문 삭제/수정 라인 0
    - `grep "not('accepted_at', 'is', null)" packages/api/src/queries/memberships.ts` ≥ 2 (기존 getMyTripRole + 신규)
    - `grep "export \* from './profiles'" packages/api/src/queries/index.ts` 1건 (`.js` extension 없음)
    - `grep -rn "seq_no" packages/api/src/queries/trips.ts | grep -v "^.*//"` — createMoaDraft insert에 seq_no 부재
    - api test:run 그린 (기존 81 + 신규 ≥5) + typecheck exit 0
  </acceptance_criteria>
  <done>온보딩 제출(24-04)·지도탭 RSC(24-06)가 import할 typed query seam 완비 — 기존 함수 무수정</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → trips INSERT | createMoaDraft 입력이 DB로 (RLS + Zod 게이트) |
| user 문자열 → SVG 마커 | buildMarkerIconUrl fill 파라미터 표면 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | buildMarkerIconUrl fill 파라미터 | mitigate | fill은 ui-tokens 리터럴만 받는 계약을 doc comment로 고정 + 호출부(24-06)는 memberColor 반환값만 전달 — user 문자열 SVG 삽입 경로 0 (T-05-05-01 계승, Task 2) |
| T-24-05 | Tampering | createMoaDraft (seq_no·visibility forge) | mitigate | insert 필드를 5개로 명시 제한 — seq_no·visibility·share_slug 미전송, acceptance grep으로 단언 (Task 3) |
| T-24-06 | Info Disclosure | getProfileNames (display_name 노출) | accept | profiles RLS가 read-all-authenticated로 설계됨(0016) — 로그인 사용자 간 닉네임 공개는 제품 요구(MOA-06) |
</threat_model>

<verification>
- `pnpm --filter web test:run -- member-color`, `-- place-sort`, `-- marker-svg` 그린
- `pnpm --filter @moajoa/api test:run` + typecheck 그린 (기존 81 무회귀 + 신규)
- `pnpm --filter @moajoa/ios test` 무회귀 (ui-tokens 변경 영향 확인)
- 기존 함수(createTrip·shareMoa·getMyTripRole) diff 0
</verification>

<success_criteria>
- MOA-02 정렬·MOA-06 색 배정이 순수함수 + 단위 테스트로 고정
- ONBOARD-03 제출 경로(createMoaDraft)가 companion 포함으로 준비됨
- 이후 wave가 탐색 없이 import만 하면 되는 계약 완성
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-02-SUMMARY.md`
</output>
