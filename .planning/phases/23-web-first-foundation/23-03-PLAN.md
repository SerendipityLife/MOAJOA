---
phase: 23-web-first-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/.env.local.example
  - CLAUDE.md
autonomous: true
requirements: [AUTH-07, AUTH-08] # 스위치·룰 기반만 — e2e 검증은 Phase 24/25에 매핑
user_setup:
  - service: kakao-developers
    why: "카카오 OAuth provider (성공 기준 4 — 프로덕션 절반)"
    env_vars:
      - name: KAKAO_REST_API_KEY
        source: "Kakao Developers → 내 애플리케이션 → 앱 키 → REST API 키"
      - name: KAKAO_CLIENT_SECRET
        source: "Kakao Developers → 카카오 로그인 → 보안 → Client Secret (활성화 필요)"
    dashboard_config:
      - task: "Redirect URI 등록 (https://<project-ref>.supabase.co/auth/v1/callback) + 동의항목 profile_nickname·profile_image"
        location: "Kakao Developers → 카카오 로그인"
  - service: supabase-dashboard
    why: "프로덕션 GoTrue 설정 (config.toml은 로컬 전용)"
    dashboard_config:
      - task: "Anonymous sign-ins 토글 ON + Kakao provider client_id/secret 입력"
        location: "Supabase Dashboard → Authentication → Providers"
must_haves:
  truths:
    - "로컬 config.toml에서 익명 sign-in이 켜지고 kakao provider 블록이 존재한다"
    - "KAKAO 시크릿은 placeholder만 커밋된다 (실값 0)"
    - "CLAUDE.md §5 D26 금지 불릿이 반전되어 이후 세션이 웹 생성·편집 UI 작업을 거부하지 않는다"
  artifacts:
    - path: supabase/config.toml
      provides: "enable_anonymous_sign_ins = true + [auth.external.kakao]"
      contains: "[auth.external.kakao]"
    - path: supabase/.env.local.example
      provides: "KAKAO_REST_API_KEY / KAKAO_CLIENT_SECRET placeholder"
      contains: "KAKAO_REST_API_KEY"
    - path: CLAUDE.md
      provides: "§5 D26 반전 + §4.1 web 역할 정합"
  key_links:
    - from: supabase/config.toml
      to: supabase/.env.local.example
      via: "env() 치환"
      pattern: "env\\(KAKAO_REST_API_KEY\\)"
---

<objective>
인증 스위치와 프로젝트 룰을 뒤집는다: config.toml 익명 sign-in ON + `[auth.external.kakao]` 블록(env() 치환), `.env.local.example`에 KAKAO placeholder, CLAUDE.md §5 D26("웹 생성 UI 금지") 불릿 공식 반전 + §4.1 web 역할 한 줄 정합.

Purpose: 성공 기준 3·4의 로컬 절반 + 성공 기준 5. config.toml은 로컬 전용이므로(파일 헤더 주석 L2~3) 프로덕션 대시보드·Kakao console은 23-07 human-action으로 분리.
Output: config.toml·.env.local.example·CLAUDE.md 수정 (스택 재시작·검증은 23-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-web-first-foundation/23-RESEARCH.md
@.planning/phases/23-web-first-foundation/23-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: config.toml 익명+카카오 스위치 + env placeholder</name>
  <files>supabase/config.toml, supabase/.env.local.example</files>
  <read_first>
    - supabase/config.toml (전체 — L48 `enable_anonymous_sign_ins = false` 현재값, L55~63 apple/google 블록 idiom, L2~3 "로컬 전용" 헤더 주석)
    - supabase/.env.local.example (기존 key 형식 — 주석 한 줄 + `KEY=예시값` shape)
  </read_first>
  <action>
1. `supabase/config.toml` L48을 `enable_anonymous_sign_ins = false` → `enable_anonymous_sign_ins = true`로 변경 (이 한 줄만 — 다른 auth 설정 무수정).
2. 기존 `[auth.external.google]` 블록(L60~63) 바로 뒤에 apple/google idiom 미러로 추가:

```toml
[auth.external.kakao]
enabled = true
client_id = "env(KAKAO_REST_API_KEY)"
secret = "env(KAKAO_CLIENT_SECRET)"
```

3. `supabase/.env.local.example` 맨 끝에 기존 형식(주석 한 줄 + placeholder) 미러로 추가:

```
# Kakao OAuth — config.toml [auth.external.kakao] env() substitution.
# REST API key = client_id. Client Secret은 Kakao console에서 활성화 필요.
KAKAO_REST_API_KEY=...
KAKAO_CLIENT_SECRET=...
```

실값 절대 커밋 금지 (§4.7) — placeholder `...`만. `supabase/.env.local`(실파일)은 건드리지 않는다.
  </action>
  <acceptance_criteria>
    - `grep -c "enable_anonymous_sign_ins = true" supabase/config.toml` = 1, `= false` 버전 0건
    - `grep -A3 "\[auth.external.kakao\]" supabase/config.toml`에 `enabled = true`·`env(KAKAO_REST_API_KEY)`·`env(KAKAO_CLIENT_SECRET)` 포함
    - `grep -c "KAKAO_REST_API_KEY" supabase/.env.local.example` ≥ 1, `KAKAO_CLIENT_SECRET` ≥ 1
    - 실값 미커밋: `.env.local.example`의 KAKAO 값이 `...` placeholder (`grep "KAKAO_REST_API_KEY=..." supabase/.env.local.example` 매치)
    - 기존 apple/google 블록 무수정 (`git diff supabase/config.toml`에 해당 라인 없음)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "enable_anonymous_sign_ins = true" supabase/config.toml && grep -q "env(KAKAO_REST_API_KEY)" supabase/config.toml && grep -q "KAKAO_CLIENT_SECRET=" supabase/.env.local.example</automated>
  </verify>
  <done>로컬 GoTrue 스위치 2개가 켜졌고 시크릿은 placeholder만 존재. 실제 로딩 검증(재시작+smoke)은 23-04.</done>
</task>

<task type="auto">
  <name>Task 2: CLAUDE.md §5 D26 불릿 반전 + §4.1 정합</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md §5 (현재 D26 불릿 4줄 — `❌ Web에 *새로운* "보드 생성"·"링크 추가" UI 추가 — 그건 iOS 전용` + dev-tool 괄호 설명), §4.1 (web 역할 한 줄: `Next.js 15 (열람·공개 보드 + 투표 참여)`)
  </read_first>
  <action>
1. §5의 D26 불릿 전체(4줄 — `❌ Web에 *새로운* "보드 생성"·"링크 추가" UI 추가 — 그건 iOS 전용`부터 `apps/web/.env.local.example`.)` 닫는 괄호까지)를 아래로 교체 (❌ 접두 리스트 포맷 유지):

```markdown
- ❌ iOS 코드 변경 — v2.1 웹 퍼스트 동안 iOS는 전면 동결
  (구 룰 "Web 생성·링크 추가 UI 금지"는 v2.1 웹 퍼스트 피봇[Phase 23, 2026-07]으로
  **공식 반전** — 이제 웹이 입력·저장·편집 풀 서피스다. 웹 생성·편집 UI 작업 허용.
  기존 dev-tool 폼 격리 `NEXT_PUBLIC_ENABLE_DEV_TOOLS`는 정식 UI로 대체될 때까지 유지.)
```

2. §4.1 모노레포 구조의 web 줄을 교체:
   - 현재: `│   ├── web/           Next.js 15 (열람·공개 보드 + 투표 참여)`
   - 변경: `│   ├── web/           Next.js 15 (입력·저장·편집 풀 서피스 — v2.1 웹 퍼스트. 공유 열람·투표 포함)`

3. 파일 끝 `**버전:**` 줄에 반영 이력 한 줄 추가는 하지 않는다 (Surgical Changes — 요청 범위는 두 지점만).
  </action>
  <acceptance_criteria>
    - `grep -c '보드 생성.*UI 추가' CLAUDE.md` = 0 (금지 불릿 부재 — ROADMAP 성공 기준 5의 검증식)
    - `grep -c '웹 퍼스트' CLAUDE.md` ≥ 1 그리고 `grep -c '공식 반전' CLAUDE.md` ≥ 1
    - `grep -c 'iOS.*전면 동결' CLAUDE.md` ≥ 1
    - §4.1 web 줄에 `입력·저장·편집` 포함 (`grep '입력·저장·편집' CLAUDE.md` 매치)
    - diff가 §5 불릿 1개 + §4.1 한 줄 외 변경 0 (`git diff CLAUDE.md` 육안 확인 — 다른 섹션 무수정)
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c '보드 생성.*UI 추가' CLAUDE.md)" = "0" && grep -q '공식 반전' CLAUDE.md && grep -q '입력·저장·편집' CLAUDE.md</automated>
  </verify>
  <done>이후 세션의 Claude가 웹 생성·편집 UI 작업을 §5 위반으로 거부하지 않고, iOS 동결이 새 가드레일로 명문화됨 (성공 기준 5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git 커밋 → 공개 이력 | KAKAO 시크릿 실값이 커밋되면 영구 노출 |
| 익명 sign-in 개방 → GoTrue | 미인증 인터넷 사용자가 세션(auth.uid)을 발급받는 표면이 열림 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-10 | Info Disclosure | KAKAO_* 시크릿 | mitigate | config.toml은 `env()` 치환만, `.env.local.example`은 `...` placeholder만 커밋 (§4.7) — acceptance grep이 단언 |
| T-23-11 | DoS | 익명 계정 대량 생성 | accept | GoTrue 기본 rate limit 30/hr/IP 유지. 프로덕션 CAPTCHA(Turnstile)는 v2.1 범위 외로 defer 기록 (RESEARCH Pitfall 6) |
| T-23-12 | Elevation/DoS | 익명 세션의 추출 EF 호출 표면 개방 | transfer | 의도된 시퀀스 — SEC-01(Phase 27) 멤버십 게이트가 닫음. 그 전 외부 공유 미시작이 마일스톤 전제 (ROADMAP) |
</threat_model>

<verification>
- config.toml 스위치 2개 grep 통과, 시크릿 실값 0
- CLAUDE.md 금지 불릿 부재 + 반전 문구 존재 (성공 기준 5 검증식)
- 로컬 GoTrue 실로딩(재시작 후 smoke)은 23-04
</verification>

<success_criteria>
- 성공 기준 5 완결 (CLAUDE.md D26 반전)
- 성공 기준 3·4의 로컬 config 절반 완결 — 나머지 절반은 23-04(검증)·23-07(프로덕션 human-action)
</success_criteria>

<output>
완료 후 `.planning/phases/23-web-first-foundation/23-03-SUMMARY.md` 생성.
</output>
