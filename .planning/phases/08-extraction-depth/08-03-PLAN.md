---
phase: 08-extraction-depth
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/app/b/[slug]/page.tsx
  - apps/web/app/b/[slug]/_components/place-summary-list.tsx
  - apps/web/__tests__/place-summary-list.test.tsx
autonomous: true
requirements: [VIEW-08]
must_haves:
  truths:
    - "The public board page shows each place's name + Korean commentary when summary_ko is present"
    - "The video TL;DR (links[i].summary_ko) renders under the title/author in the 영상 출처 list when present"
    - "Legacy rows / rows with null summary_ko render NO summary block and the layout does not break (conditional render)"
    - "summary_ko renders as escaped text (React default) — no dangerouslySetInnerHTML, no XSS surface"
  artifacts:
    - path: "apps/web/app/b/[slug]/_components/place-summary-list.tsx"
      provides: "RTL-testable place list section rendering name_ko ?? name_local + conditional summary"
      contains: "summary_ko"
    - path: "apps/web/app/b/[slug]/page.tsx"
      provides: "Renders PlaceSummaryList + conditional link TL;DR in the 영상 출처 list"
      contains: "summary_ko"
    - path: "apps/web/__tests__/place-summary-list.test.tsx"
      provides: "vitest: summary present renders / absent (null) hides / escaping"
      contains: "describe"
  key_links:
    - from: "page.tsx (view.places / view.links)"
      to: "place-summary-list.tsx + 영상 출처 <li> conditional <p>"
      via: "{x.summary_ko && (...)} conditional render"
      pattern: "summary_ko &&"
---

<objective>
Surface the Korean commentary on the public web board (VIEW-08): each place shows its 1~2 sentence
summary, each video its 2~3 sentence TL;DR — both behind a conditional render so legacy rows
(summary_ko null) never break the layout. No new "create/add" UI (CLAUDE.md hard rule) — display only,
reusing the Phase 4 link-card Tailwind tokens.

Purpose: closes the 4th link of the data-flow chain (08-PATTERNS.md §CRITICAL) — the column now
reaches the browser. The web currently renders ZERO per-place text detail, so the place commentary
is a NET-NEW read-only section (mirroring the existing 영상 출처 list), not an edit to a detail panel.
Output: a small RTL-testable PlaceSummaryList component + a conditional <p> in the existing link list +
vitest covering present/absent/escaping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extraction-depth/08-CONTEXT.md
@.planning/phases/08-extraction-depth/08-PATTERNS.md
@.planning/phases/08-extraction-depth/08-01-SUMMARY.md

<project_rules>
CLAUDE.md §5: NO new "보드 생성"/"링크 추가" UI on web — this is display-only (표시만 추가, 허용).
CLAUDE.md §4.5: workspace imports NO `.js` extension.
CONTEXT line 85: legacy rows without summary MUST NOT break layout → every summary <p> conditional.
</project_rules>

<interfaces>
After 08-01, PublicBoardView['places'][number] includes `summary_ko: string | null` and
PublicBoardView['links'][number] includes `summary_ko: string | null`. The web casts the RPC payload
straight through (getPublicBoardBySlug raw cast), so view.places[i].summary_ko / view.links[i].summary_ko
are typed and may be null.

Existing page.tsx 영상 출처 list item (page.tsx:120-127) — author conditional is the exact pattern to copy:
```tsx
<div className="min-w-0">
  <p className="text-base font-semibold text-neutral-900 line-clamp-2">{link.title ?? link.url}</p>
  {link.author_name && (
    <p className="text-sm text-neutral-500 mt-1">{link.author_name}</p>
  )}
</div>
```

Token idiom (Phase 4): container card `p-3 border border-neutral-200 rounded-lg`; primary text
`text-base font-semibold text-neutral-900`; muted text `text-sm text-neutral-500`; commentary body
`text-sm text-neutral-600 mt-1`.

vitest+RTL idiom (apps/web/__tests__/marker-svg.test.ts + vitest.config.ts): `import { describe, expect, it } from 'vitest'`,
RTL available (@testing-library/react 16, jsdom env, jsx:'automatic', include covers *.test.tsx).
Use `import { render, screen } from '@testing-library/react'` for the component.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PlaceSummaryList component + vitest</name>
  <read_first>
    - apps/web/app/b/[slug]/page.tsx:97-134 (link-card list — token + conditional-render analog)
    - apps/web/__tests__/marker-svg.test.ts (vitest idiom) + apps/web/vitest.config.ts
    - 08-PATTERNS.md §8 (b) (recommend option ii — place list section)
  </read_first>
  <behavior>
    New file apps/web/app/b/[slug]/_components/place-summary-list.tsx exporting
    `PlaceSummaryList({ places }: { places: PublicBoardView['places'] })` — a server-safe (no 'use client')
    presentational section. For each place: heading `{p.name_ko ?? p.name_local}`, and CONDITIONALLY
    `{p.summary_ko && <p ...>{p.summary_ko}</p>}`. Renders nothing (return null) when places.length === 0.
    vitest (place-summary-list.test.tsx, RTL):
    - "renders commentary when summary_ko present" → render with `[{ ...place, name_local:'스시집', summary_ko:'여기 라멘이 유명해요' }]` → `screen.getByText('여기 라멘이 유명해요')` is in document.
    - "hides summary block when summary_ko is null (legacy row)" → render with `[{ ...place, name_local:'스시집', summary_ko: null }]` → place name '스시집' present, but `screen.queryByTestId('place-summary')` is null (no summary <p>). Give the conditional <p> `data-testid="place-summary"`.
    - "renders name_ko over name_local when present" → `{ name_ko:'스시야', name_local:'寿司屋', summary_ko:null }` → getByText('스시야') present, queryByText('寿司屋') null.
    - "escapes HTML in summary_ko (no XSS)" → `summary_ko: '<img src=x onerror=alert(1)>'` → the literal string is rendered as text (`screen.getByText('<img src=x onerror=alert(1)>')` present) and `container.querySelector('img')` is null. Asserts React default escaping; component MUST NOT use dangerouslySetInnerHTML.
    Build a minimal place fixture inline satisfying PublicBoardView['places'][number] (id/link_id/
    name_local/name_ko/name_en/lat/lng/category/source_timestamp_sec/source_kind/confidence/summary_ko).
  </behavior>
  <action>
    Create _components/place-summary-list.tsx:
    ```tsx
    import type { PublicBoardView } from '@moajoa/core';

    export function PlaceSummaryList({ places }: { places: PublicBoardView['places'] }) {
      if (places.length === 0) return null;
      return (
        <section className="mt-8">
          <h2 className="text-lg font-semibold text-neutral-900 mb-3">장소 {places.length}곳</h2>
          <ul className="space-y-2">
            {places.map((p) => (
              <li key={p.id} className="p-3 border border-neutral-200 rounded-lg">
                <p className="text-base font-semibold text-neutral-900 line-clamp-2">
                  {p.name_ko ?? p.name_local}
                </p>
                {p.summary_ko && (
                  <p data-testid="place-summary" className="text-sm text-neutral-600 mt-1">
                    {p.summary_ko}
                  </p>
                )}
              </li>
            ))}
          </ul>
        </section>
      );
    }
    ```
    NO 'use client' (SSR-friendly). NO dangerouslySetInnerHTML. NO `.js` import extension.
    Create apps/web/__tests__/place-summary-list.test.tsx with the four behaviors above.
  </action>
  <verify>
    <automated>cd apps/web && pnpm vitest run __tests__/place-summary-list.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/app/b/[slug]/_components/place-summary-list.tsx` succeeds
    - `grep -q "p.summary_ko &&" apps/web/app/b/[slug]/_components/place-summary-list.tsx` succeeds (conditional render)
    - `grep -q "dangerouslySetInnerHTML" apps/web/app/b/[slug]/_components/place-summary-list.tsx` FAILS (no raw HTML injection — XSS-safe)
    - `grep -q "p.name_ko ?? p.name_local" apps/web/app/b/[slug]/_components/place-summary-list.tsx` succeeds
    - `cd apps/web && pnpm vitest run __tests__/place-summary-list.test.tsx` reports 4 passing tests, 0 failures
  </acceptance_criteria>
  <done>PlaceSummaryList renders names + conditional escaped commentary; vitest covers present/absent(null)/name-preference/escaping.</done>
</task>

<task type="auto">
  <name>Task 2: Wire page.tsx — PlaceSummaryList + link TL;DR conditional</name>
  <read_first>
    - apps/web/app/b/[slug]/page.tsx (entire — header 73-84, map 86-95, 영상 출처 list 97-134, footer 136-144)
    - apps/web/app/b/[slug]/_components/place-summary-list.tsx (from Task 1)
    - 08-PATTERNS.md §8 (a)(b)
  </read_first>
  <action>
    1. Import the new component at top of page.tsx:
       `import { PlaceSummaryList } from './_components/place-summary-list';`
    2. Place TL;DR list — render PlaceSummaryList. Insert it AFTER the map block (after line 95,
       i.e. after the `)}` closing the `view.places.length === 0 ? ... : (...)` ternary) and BEFORE
       the 영상 출처 `{view.links.length > 0 && (` section (line 97):
       ```tsx
       <PlaceSummaryList places={view.places} />
       ```
    3. Video TL;DR — inside the existing 영상 출처 `<li>` inner `<div className="min-w-0">`
       (page.tsx:120-127), add a conditional <p> AFTER the `{link.author_name && (...)}` block,
       copying that exact conditional idiom:
       ```tsx
       {link.author_name && (
         <p className="text-sm text-neutral-500 mt-1">{link.author_name}</p>
       )}
       {link.summary_ko && (
         <p className="text-sm text-neutral-600 mt-1 line-clamp-3">{link.summary_ko}</p>
       )}
       ```
    Surgical: do NOT restyle the header, map, footer, or existing link card structure. No new
    "create/add" affordance — display only. No `.js` import extension.
  </action>
  <verify>
    <automated>cd apps/web && pnpm tsc --noEmit && pnpm vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { PlaceSummaryList }" apps/web/app/b/[slug]/page.tsx` succeeds
    - `grep -q "<PlaceSummaryList places={view.places} />" apps/web/app/b/[slug]/page.tsx` succeeds
    - `grep -q "link.summary_ko && (" apps/web/app/b/[slug]/page.tsx` succeeds (video TL;DR conditional)
    - `grep -q "dangerouslySetInnerHTML" apps/web/app/b/[slug]/page.tsx` FAILS
    - `cd apps/web && pnpm tsc --noEmit` exits 0 (PublicBoardView.summary_ko types resolve via 08-01)
    - `cd apps/web && pnpm vitest run` — full suite green (place-summary-list 4 new + all prior, 0 regressions)
    - `cd apps/web && pnpm build` exits 0 (server component compiles)
  </acceptance_criteria>
  <done>Public board page renders place commentary section + per-video TL;DR, both conditionally; typecheck/build/test green; no new create UI; no XSS surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM-generated summary_ko → public web render | generated text rendered to unauthenticated visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering (stored XSS) | summary_ko rendered in page.tsx / place-summary-list.tsx | mitigate | Rendered via JSX text interpolation `{p.summary_ko}` → React default HTML-escapes. NO dangerouslySetInnerHTML (asserted by grep-FAILS acceptance + the escaping vitest case which confirms `<img>` payload renders as inert text). Low severity (text-only, length-capped at schema layer) but addressed by construction. |
| T-08-07 | Information Disclosure | new place list exposes place fields | accept | Only fields already in the PublicBoardView Pick (08-01) render; summary_ko is the lone new field and was explicitly opted in. No added_by/google_place_id leak. |
</threat_model>

<verification>
- `cd apps/web && pnpm vitest run` — full web suite green incl. 4 new place-summary-list tests.
- `cd apps/web && pnpm tsc --noEmit` exits 0; `pnpm build` exits 0.
- `grep -q dangerouslySetInnerHTML` FAILS across both web files (XSS-safe by construction).
- Live browser verification (real RPC payload with applied 0008 column) is DEFERRED to the
  autonomous:false gate 08-04.
</verification>

<success_criteria>
- VIEW-08: place commentary + video TL;DR visible on the public board, conditional on summary_ko.
- Legacy/null rows render no summary block; layout intact (vitest-proven).
- Display-only (no new create/add UI); escaped render (no XSS).
- File-disjoint from 08-02 → ran in parallel (Wave 2).
</success_criteria>

<output>
After completion, create `.planning/phases/08-extraction-depth/08-03-SUMMARY.md`
</output>
