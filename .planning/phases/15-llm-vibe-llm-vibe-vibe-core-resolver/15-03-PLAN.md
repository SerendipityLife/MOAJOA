---
phase: 15-llm-vibe-llm-vibe-vibe-core-resolver
plan: 03
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - apps/ios/lib/category.ts
  - apps/web/lib/category-icon.ts
autonomous: true
requirements: [EXTRACT-12]
must_haves:
  truths:
    - "iOS vibeOf delegates to core placeVibe and returns one of the 6 canonical vibes"
    - "iOS VIBE_STYLE has exactly the 6 canonical keys (cafe added, wellness removed) and keeps its Ionicons icon + labelKo, sourcing color from core VIBE_META"
    - "Web categoryVisual delegates to core placeVibe and keeps only its lucide icon map (now keyed by the 6 vibes)"
    - "Existing callers (boards.tsx, place-list.tsx, vote-island.tsx) compile unchanged against the new exports"
  artifacts:
    - path: "apps/ios/lib/category.ts"
      provides: "vibeOf → placeVibe delegation; VIBE_STYLE icon/label map over 6 vibes"
      contains: "placeVibe"
    - path: "apps/web/lib/category-icon.ts"
      provides: "categoryVisual → placeVibe delegation; lucide icon+tone map over 6 vibes"
      contains: "placeVibe"
  key_links:
    - from: "apps/ios/lib/category.ts"
      to: "@moajoa/core placeVibe"
      via: "import + call inside vibeOf"
      pattern: "placeVibe"
    - from: "apps/web/lib/category-icon.ts"
      to: "@moajoa/core placeVibe"
      via: "import + call inside categoryVisual"
      pattern: "placeVibe"
---

<objective>
Replace the two duplicated, vibe-key-blind client mappers with thin wrappers over the core `placeVibe` resolver (D2). Each client keeps ONLY its own icon map (iOS Ionicons / web lucide — different libraries), and pulls the shared color from core `VIBE_META`. The taxonomy unifies on the 6 canonical keys: `cafe` is added, `wellness` is removed (→ other).

Purpose: Single source of truth for vibe bucketing; map-link places (which now store a vibe key in `category`, see 15-02) get recognized — previously these mappers only matched Google primaryType substrings and returned grey for bare vibe keys.
Output: `apps/ios/lib/category.ts` and `apps/web/lib/category-icon.ts` rewritten to delegate; existing callers unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-CONTEXT.md
@.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-01-SUMMARY.md

<interfaces>
<!-- From @moajoa/core (Plan 15-01): -->
export type Vibe = 'food' | 'cafe' | 'nature' | 'culture' | 'shopping' | 'other';
export function placeVibe(category: string | null | undefined): Vibe;
export const VIBE_META: Record<Vibe, { color: string; labelKo: string }>;

<!-- iOS callers (must stay source-compatible — do NOT change these files):
  apps/ios/app/(tabs)/boards.tsx:20   import { vibeOf, VIBE_STYLE } from '@/lib/category';
  apps/ios/app/(tabs)/boards.tsx:249  const vibe = VIBE_STYLE[vibeOf(item.top_category)];
  apps/ios/components/boards/place-list.tsx:13  import { vibeOf, VIBE_STYLE } from '@/lib/category';
  apps/ios/components/boards/place-list.tsx:61  const vibe = VIBE_STYLE[vibeOf(item.category)];
  → vibeOf(string|null|undefined): Vibe  and  VIBE_STYLE[vibe]  must keep working.
  VIBE_STYLE entries are consumed for: .color, .textOn, .tint, .icon, .labelKo (grep the call sites to confirm which fields). -->

Current iOS VIBE_STYLE shape (apps/ios/lib/category.ts): { color, textOn, tint, icon (Ionicons glyph), labelKo }.
Current iOS Vibe: food|nature|culture|wellness|shopping|other  → REPLACE with the 6 canonical keys (add cafe, drop wellness).

<!-- web caller (must stay source-compatible — do NOT change this file):
  apps/web/app/b/[slug]/_components/vote-island.tsx:16  import { categoryVisual } from '@/lib/category-icon';
  apps/web/app/b/[slug]/_components/vote-island.tsx:245 const cat = categoryVisual(p.category);  // uses cat.icon, cat.tone -->

Current web categoryVisual returns { icon: LucideIcon, tone: string }. Keep that exact return shape.
Current web buckets: 7 (restaurant/cafe/bar/shopping/nature/culture/lodging) → collapse to the 6 vibes
  (bar→food, lodging→other).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS — vibeOf delegates to placeVibe; VIBE_STYLE over 6 vibes</name>
  <files>apps/ios/lib/category.ts</files>
  <action>
    Rewrite apps/ios/lib/category.ts to delegate (D2):
    - Remove the local `RULES` substring table and the local `Vibe` type. Import from core: `import { placeVibe, VIBE_META, type Vibe } from '@moajoa/core';` (NO `.js` extension — CLAUDE.md §4.5). Re-export the type for callers: `export type { Vibe };`.
    - `export function vibeOf(category: string | null | undefined): Vibe { return placeVibe(category); }` (thin wrapper — keeps the call-site name `vibeOf` stable so boards.tsx/place-list.tsx don't change).
    - Rebuild `VIBE_STYLE: Record<Vibe, VibeStyle>` with EXACTLY the 6 canonical keys. Keep the `VibeStyle` shape the callers use (color, textOn, tint, icon, labelKo). Source `color` and `labelKo` from `VIBE_META[vibe]` (color = shared token); keep `textOn`, `tint`, and `icon` (Ionicons glyph) defined locally per vibe — icons stay per-client (D2). Add the new `cafe` entry (Ionicons `cafe` glyph, coffee tint/textOn) and DROP `wellness`. Keep food/nature/culture/shopping/other icons as-is.
    - Confirm `keyof typeof Ionicons.glyphMap` still types the icon field. `cafe` is a valid Ionicons glyph; verify in the typecheck.
    Touch ONLY this file. Do not edit boards.tsx or place-list.tsx (they remain source-compatible).
  </action>
  <verify>
    <automated>cd apps/ios && (pnpm typecheck 2>&1 || npx tsc --noEmit 2>&1) | tail -15</automated>
  </verify>
  <done>`vibeOf` returns `placeVibe(...)`; `VIBE_STYLE` has the 6 canonical keys with per-client Ionicons icons and core-sourced color; iOS typecheck clean; callers unchanged. Commit: `refactor(15-03): iOS vibeOf delegates to core placeVibe`.</done>
</task>

<task type="auto">
  <name>Task 2: Web — categoryVisual delegates to placeVibe; lucide map over 6 vibes</name>
  <files>apps/web/lib/category-icon.ts</files>
  <action>
    Rewrite apps/web/lib/category-icon.ts to delegate (D2):
    - Remove the local `BUCKETS` substring table. Import `import { placeVibe, type Vibe } from '@moajoa/core';` (NO `.js` extension).
    - Define a local `const VIBE_VISUAL: Record<Vibe, { icon: LucideIcon; tone: string }>` keyed by the 6 canonical vibes — icons + Tailwind tone classes stay per-client (web lucide). Map: food→Utensils (orange tone), cafe→Coffee (amber tone), nature→TreePine (green tone), culture→Landmark (violet tone), shopping→ShoppingBag (emerald tone), other→MapPin (brand tone). Drop the Beer (bar) and Building2 (lodging) buckets — bar→food, lodging→other now collapse via placeVibe. Remove now-unused lucide imports (Beer, Building2) per CLAUDE.md §3.3 (clean up only orphans your change creates).
    - `export function categoryVisual(category: string | null | undefined): { icon: LucideIcon; tone: string } { return VIBE_VISUAL[placeVibe(category)]; }` — keep the exact return shape vote-island.tsx consumes (cat.icon, cat.tone).
    Touch ONLY this file. Do not edit vote-island.tsx.
  </action>
  <verify>
    <automated>cd apps/web && (pnpm typecheck 2>&1 || npx tsc --noEmit 2>&1) | tail -15</automated>
  </verify>
  <done>`categoryVisual` returns `VIBE_VISUAL[placeVibe(...)]` with the same `{ icon, tone }` shape; no orphaned lucide imports; web typecheck clean; vote-island.tsx unchanged. Commit: `refactor(15-03): web categoryVisual delegates to core placeVibe`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored category → client display | DB string → placeVibe → icon/color (display only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Tampering | category display value | accept | Pure presentational mapping with an 'other' default; no DOM injection (icon is a component reference, tone is a static class string). Wrong input → neutral icon. Low-stakes per D5. |
</threat_model>

<verification>
- iOS `vibeOf` and web `categoryVisual` both delegate to `placeVibe` (grep confirms no local substring table remains).
- iOS `VIBE_STYLE` and web `VIBE_VISUAL` each have exactly the 6 canonical keys (cafe present, wellness/bar/lodging absent as buckets).
- `pnpm typecheck` clean in both apps/ios and apps/web (project-wide vitest is known-broken — rely on typecheck per phase verification note).
- Caller files (boards.tsx, place-list.tsx, vote-island.tsx) are NOT in the diff.
</verification>

<success_criteria>
- Both client mappers are thin wrappers over core `placeVibe`; duplication removed (D2).
- A bare vibe key like `'cafe'` now resolves to the correct icon/color on both clients (previously grey).
- No `.js` extension on the new core imports; no orphaned imports; surgical diffs only.
</success_criteria>

<output>
After completion, create `.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-03-SUMMARY.md`
</output>
