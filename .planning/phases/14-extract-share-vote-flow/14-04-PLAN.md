---
phase: 14-extract-share-vote-flow
plan: 04
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - apps/ios/app/boards/[id].tsx
  - apps/ios/lib/share-board.ts
autonomous: false
must_haves:
  truths:
    - "보드 상단에 상시 공유 아이콘이 있고, 추출 완료(장소≥1)면 '친구와 정하기' CTA가 등장한다."
    - "둘 중 무엇을 눌러도 shareBoard(boardId)로 보드를 shared 전환·share_slug를 받아 네이티브 Share.share로 ${webUrl}/b/{slug}를 공유한다."
    - "공유 시트의 URL을 비로그인으로 web에서 열면 기존 vote-island 투표 화면이 그대로 뜬다(회귀 0)."
  artifacts:
    - path: "apps/ios/lib/share-board.ts"
      provides: "shareCurrentBoard(boardId): shareBoard api → Share.share(${webUrl}/b/{slug})"
      exports: ["shareCurrentBoard"]
    - path: "apps/ios/app/boards/[id].tsx"
      provides: "상단 상시 공유 아이콘 + 추출 후 '친구와 정하기' CTA, 둘 다 shareCurrentBoard 호출"
      contains: "shareCurrentBoard"
---

<objective>
빠진 다리 완성. `lib/share-board.ts`의 `shareCurrentBoard(boardId)`: `shareBoard`(14-01 api) 호출로 slug 확보 → `webUrl = Constants.expoConfig?.extra?.webUrl` → `Share.share({ message: `${webUrl}/b/${slug}` })`(friends.tsx 패턴 재사용). `boards/[id].tsx`에 (1) 헤더 우측 상시 공유 아이콘, (2) 장소≥1일 때 등장하는 "친구와 정하기" CTA 버튼 — 둘 다 `shareCurrentBoard(id)` 호출. 실패 시 toast.

**autonomous:false** — 공유 시트는 시뮬레이터/실기기에서 사람 확인 필요. 또한 14-01의 `share_board` RPC가 `db push`되어 있어야 라이브 동작(게이트).
</objective>

## Steps
1. `lib/share-board.ts` 신규: `shareCurrentBoard(boardId)` — `shareBoard(supabase, boardId)` → URL 조립 → `Share.share`. webUrl null/placeholder 방어.
2. `boards/[id].tsx`:
   - 헤더 우측에 공유 아이콘(현 `+ 핀` 옆) → `shareCurrentBoard(id)`.
   - 장소≥1이면 지도 아래/리스트 위에 "친구와 정하기" CTA(primary) → 동일 호출. (14-02의 화면 구조 위에 얹음 — depends 14-02.)
   - 호출 실패 시 `showToast('공유 준비 실패', 'error')`.
3. 라이브 게이트: 14-01 마이그레이션 `supabase db push` + types 재생성 후 동작.

## Verify
- `grep -q 'shareCurrentBoard' apps/ios/lib/share-board.ts apps/ios/app/boards/[id].tsx`
- `grep -q 'Share.share' apps/ios/lib/share-board.ts`
- `pnpm --filter @moajoa/ios typecheck` green
- (UAT) 보드에서 공유/CTA 탭 → 공유 시트에 `${webUrl}/b/{slug}` → 그 URL을 비로그인 web에서 열면 투표 화면(❤️ 동작)
