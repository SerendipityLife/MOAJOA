---
phase: 24-host-flow
plan: 04
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - apps/web/app/onboarding/page.tsx
  - apps/web/app/onboarding/_components/step-where.tsx
  - apps/web/app/onboarding/_components/step-dates.tsx
  - apps/web/app/onboarding/_components/step-who.tsx
  - apps/web/app/onboarding/_components/step-seed.tsx
  - apps/web/app/onboarding/_lib/build-draft.ts
  - apps/web/components/add-content-tabs.tsx
  - apps/web/components/index.ts
  - apps/web/__tests__/onboarding.test.tsx
  - apps/web/__tests__/build-draft.test.ts
autonomous: true
requirements: [ONBOARD-03, ONBOARD-04, ONBOARD-05]
must_haves:
  truths:
    - "4단계(어디로→날짜→누구랑→봐둔 곳)를 거쳐 마지막 단계 완료 시 모아가 한 번에 생성된다 (ONBOARD-03, D-03)"
    - "날짜 미정 선택 시 안내 한 줄 후 통과되어 dates null 모아가 만들어지고, 확정 선택 시 캘린더 range로 입력한다 (ONBOARD-04, D-06)"
    - "마지막 단계에서 링크 붙여넣기·장소 검색으로 담아두거나 건너뛸 수 있고, 생성 직후 일괄 addLink+추출 트리거된다 (ONBOARD-05, D-08)"
  artifacts:
    - path: "apps/web/app/onboarding/page.tsx"
      provides: "단일 라우트 클라이언트 위저드 (D-02)"
      min_lines: 80
    - path: "apps/web/app/onboarding/_lib/build-draft.ts"
      provides: "위저드 상태 → TripCreateDraft 순수 매퍼 (title 파생 포함)"
      exports: ["buildDraft"]
    - path: "apps/web/components/add-content-tabs.tsx"
      provides: "링크/장소검색 2탭 공유 컴포넌트 (D-11 — 24-07 add-sheet가 재사용)"
      exports: ["AddContentTabs"]
  key_links:
    - from: "apps/web/app/onboarding/page.tsx"
      to: "createMoaDraft"
      via: "TripCreateDraftSchema.parse 후 @moajoa/api 호출"
      pattern: "createMoaDraft"
    - from: "apps/web/app/onboarding/page.tsx"
      to: "triggerExtraction"
      via: "addLink 후 fire-and-forget"
      pattern: "triggerExtraction"
    - from: "apps/web/components/add-content-tabs.tsx"
      to: "resolve-place EF"
      via: "client.functions.invoke('resolve-place')"
      pattern: "resolve-place"
---

<objective>
`/onboarding` 4단계 위저드를 완성한다 — 도시 칩(D-05)·캘린더 range/미정 통과(D-06)·누구랑 칩(D-07)·봐둔 곳 스테이징(D-08), 완료 시 TripCreateDraftSchema 한 번 생성 + 일괄 시드(D-03), 링크/검색 탭은 공유 컴포넌트로 만들어 24-07 add-sheet가 재사용한다(D-11).

Purpose: ONBOARD-03/04/05의 전체 표면. 중간 저장 없음(D-04) — 빈 모아 잔여 0.
Output: 위저드 + step 4종 + AddContentTabs 공유 컴포넌트 + buildDraft 순수 매퍼 + 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-host-flow/24-CONTEXT.md (D-02~D-08, D-11)
@.planning/phases/24-host-flow/24-UI-SPEC.md (§/onboarding, Copywriting Contract, A-6, A-8)
@.planning/phases/24-host-flow/24-PATTERNS.md (§onboarding, §add-sheet/step-seed)
@.planning/phases/24-host-flow/24-RESEARCH.md (§Calendar, Pitfall 6·7)
@apps/web/app/login/page.tsx (클라이언트 단일 라우트 + useState 모드 전환 analog)
@apps/web/components/chip.tsx
@apps/web/app/boards/[id]/_components/add-link-form.tsx (링크 추가 패턴 — isDevToolsEnabled 게이트 복사 금지)

<interfaces>
From packages/core (실존):
```typescript
TripCreateDraftSchema  // { title: 1..TripTitleMax, city_code: ≤20, start_date/end_date: date|null (both-or-null, end>=start), companion: ≤20|null }
CITY_KO_MAP            // 9개: tokyo 도쿄, osaka 오사카, kyoto 교토, seoul 서울, busan 부산, jeju 제주, fukuoka 후쿠오카, sapporo 삿포로, okinawa 오키나와
detectSourceKind(url)  // 'youtube'|'blog'|'instagram'|null
```

From packages/api (24-02가 제공 + 실존):
```typescript
createMoaDraft(client, input: TripCreateDraft): Promise<Trip>        // 24-02 신규
addLink(client, { board_id, url }): Promise<Link>                    // 실존 — 입력 필드명은 board_id (내부에서 trip_id로 매핑)
triggerExtraction(client, linkId): Promise<ExtractionResult>         // 실존 — fire-and-forget 호출
addManualPlace(client, { board_id, google_place_id }): Promise<...>  // 실존 — 좌표는 서버 해석
```

resolve-place EF 계약 (supabase/functions/resolve-place/index.ts 실존):
```typescript
// POST body { query: string(1..200), language: 'ko' } → { places: ResolvedPlace[] } (max 5)
// ResolvedPlace: { id(구글 place_id), name, address, ... } — Bearer JWT 필수(anon 허용)
const { data, error } = await client.functions.invoke('resolve-place', { body: { query, language: 'ko' } });
```

react-day-picker 9.14.0 (24-01이 설치):
```typescript
import { DayPicker, type DateRange } from 'react-day-picker';
import { ko } from 'react-day-picker/locale';
// mode="range" selected={range} onSelect={setRange} disabled={{ before: new Date() }}
// A-8: default style.css 미사용 — classNames prop으로 Tailwind 직접 매핑
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 위저드 셸 + step-where + step-who (칩 스텝 2종)</name>
  <files>apps/web/app/onboarding/page.tsx, apps/web/app/onboarding/_components/step-where.tsx, apps/web/app/onboarding/_components/step-who.tsx</files>
  <read_first>
    - apps/web/components/chip.tsx (selected 스타일 계약)
    - apps/web/app/login/page.tsx L34-42 (클라이언트 useState 모드 전환 analog)
    - apps/web/components/index.ts (barrel — Chip·Button·Input export 확인)
  </read_first>
  <action>
    1. `apps/web/app/onboarding/page.tsx` — `'use client'` 단일 라우트 위저드 (D-02):
       - 상태: `step: 1|2|3|4`, `city: string|null`(칩 코드 또는 기타 텍스트), `cityCustom: boolean`, `dateMode: 'fixed'|'unset'|null`, `range: DateRange|undefined`, `companion: string|null`, `seedLinks: string[]`, `seedPlaces: {id: string; name: string; address: string|null}[]`, `submitting: boolean`
       - 상단 중앙 4-dot 스텝 인디케이터 (A-6): 현재 dot `bg-brand-600 w-6` pill, 나머지 `bg-neutral-300 w-1.5` 원. 숫자 없음. 전환 애니메이션 없음(즉시 교체).
       - step 2~4에서 좌상단 뒤로 chevron (lucide `ChevronLeft`, 44px 히트영역, **aria-label="뒤로"**) → step-1.
       - 브라우저 뒤로가기 (Pitfall 6 최소 구현): step 전진 시 `history.pushState({ step })`, `popstate` 리스너에서 state.step으로 후퇴, step 1에서의 back은 이탈 허용 (D-04 소실 허용 — localStorage 저장 금지). 20줄 이내, 과설계 금지.
       - 헤딩(18px/600, 아래 32px 간격) — Copywriting Contract 그대로: step1 "어디로 떠나요?", step2 "언제 가요?", step3 "누구랑 가요?", step4 "봐둔 곳이 있나요?"
       - 하단 고정 full-width primary CTA: step1~3 "다음"(해당 스텝 값 미선택 시 disabled), step4는 Task 3에서.
    2. `_components/step-where.tsx`: `CITY_KO_MAP` entries를 `<Chip selected onClick>` 9개로 + "기타" 칩. 기타 선택 시 `<Input>` 노출(maxLength 20 — city_code ≤20 스키마 정합). props: `{ value, custom, onChange(cityCode: string, custom: boolean) }`.
    3. `_components/step-who.tsx`: 칩 5개 `혼자/연인/친구/가족/동료` + "기타" 칩 → Input(maxLength 20 — trips.companion ≤20, D-07). step-where와 동일 칩 패턴(시각 일관성 — CONTEXT specifics).
    4. import는 `@/components` barrel — 워크스페이스 import `.js` extension 금지. 배경 surface.background, 콘텐츠 좌우 16px, 온보딩 상단 여백 48px (UI-SPEC spacing).
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'use client'" apps/web/app/onboarding/page.tsx` 1건
    - `grep "어디로 떠나요?" apps/web/app/onboarding/_components/step-where.tsx apps/web/app/onboarding/page.tsx | wc -l` ≥ 1 (헤딩 카피 정확)
    - `grep "aria-label=\"뒤로\"" apps/web/app/onboarding/page.tsx` 1건
    - `grep "CITY_KO_MAP" apps/web/app/onboarding/_components/step-where.tsx` 1건
    - `grep -E "혼자|연인|친구|가족|동료" apps/web/app/onboarding/_components/step-who.tsx | wc -l` ≥ 1
    - `grep "localStorage" apps/web/app/onboarding/page.tsx` == 0건 (D-04 소실 허용)
    - typecheck exit 0
  </acceptance_criteria>
  <done>위저드 골격 + 칩 스텝 2종 — 스텝 전환·뒤로가기·인디케이터 동작</done>
</task>

<task type="auto">
  <name>Task 2: step-dates (캘린더) + AddContentTabs 공유 컴포넌트 + step-seed</name>
  <files>apps/web/app/onboarding/_components/step-dates.tsx, apps/web/components/add-content-tabs.tsx, apps/web/components/index.ts, apps/web/app/onboarding/_components/step-seed.tsx</files>
  <read_first>
    - apps/web/app/boards/[id]/_components/add-link-form.tsx (L39-70 링크 추가 흐름 — L13 isDevToolsEnabled return-null은 절대 복사 금지)
    - apps/web/components/tabs.tsx (Radix Tabs props 계약)
    - apps/web/components/index.ts (barrel append 위치)
    - supabase/functions/resolve-place/index.ts L1-37 (요청/응답 계약)
  </read_first>
  <action>
    1. `_components/step-dates.tsx` (D-06, ONBOARD-04):
       - 2택 카드: "날짜 정했어요" / "아직 미정이에요" (선택 시 border brand-500 — UI-SPEC step2 계약)
       - "정했어요" 선택 → 카드 아래 `<DayPicker mode="range" locale={ko} selected={range} onSelect={onRangeChange} disabled={{ before: new Date() }} />`. **A-8: `react-day-picker/style.css` import 금지 — `classNames` prop으로 Tailwind 매핑**: 선택 endpoints `bg-brand-600 text-white rounded-full`, 사이 날짜 `bg-brand-100`, 오늘 이전 `text-neutral-300`, 셀 44px 히트영역. 한 캘린더에서 시작·종료 탭 2번(같은 날 재탭 = 당일치기 — range.to fallback range.from).
       - "미정이에요" 선택 → 안내 한 줄 "날짜는 나중에 친구들과 함께 정할 수 있어요" (14px/400 neutral-500) 노출 후 CTA 활성.
       - props: `{ mode, range, onModeChange, onRangeChange }` — 상태는 page.tsx 소유.
    2. `apps/web/components/add-content-tabs.tsx` 신규 — **온보딩 step4와 지도탭 추가 시트(24-07)가 공유하는 D-11 컴포넌트**:
    ```typescript
    export interface PickedPlace { id: string; name: string; address: string | null }
    export interface AddContentTabsProps {
      onAddLink: (url: string) => void | Promise<void>;   // 유효 URL만 전달됨
      onPickPlace: (place: PickedPlace) => void | Promise<void>;
      busy?: boolean;
    }
    export function AddContentTabs({ onAddLink, onPickPlace, busy }: AddContentTabsProps)
    ```
       - `Tabs` 2탭: "링크 붙여넣기" / "장소 검색" (Copywriting Contract)
       - 링크 탭: `Input` placeholder "유튜브·블로그 링크를 붙여넣어 주세요" + primary CTA **"링크 추가하기"** (checker FLAG 반영 — bare "추가하기" 금지). 제출 시 `new URL(url)` 시도 — 실패하면 toast('올바른 링크가 아니에요', {variant:'error'}) 후 미전달, 성공 시 `onAddLink(url.trim())` 후 입력 클리어.
       - 검색 탭: `Input` placeholder "장소 이름을 검색해 보세요" + 검색 실행 시 `getSupabaseBrowser().functions.invoke('resolve-place', { body: { query, language: 'ko' } })` → `data.places` 최대 5행(장소명 16/400 + 주소 12/400 neutral-500, 행 최소 44px) — 행 탭 시 `onPickPlace({ id, name, address })`. 결과 0건이면 "검색 결과가 없어요. 다른 이름으로 찾아보세요". 검색 실패 시 toast 에러.
       - **`isDevToolsEnabled` 게이트 절대 금지** (RESEARCH 안티패턴 — 신규 정식 UI).
       - `apps/web/components/index.ts` barrel에 `export * from './add-content-tabs';` append.
    3. `_components/step-seed.tsx` (D-08, ONBOARD-05): 안내 문구 "유튜브·블로그 링크를 붙여넣거나 장소를 검색해 담아 두세요" + `<AddContentTabs onAddLink={링크 스테이징 push} onPickPlace={장소 스테이징 push} />` + 담은 항목 스테이징 리스트(링크 URL truncate + 장소명, 항목별 제거 X 버튼). **스테이징은 로컬 상태만 — DB 접촉 0 (D-03: 생성은 완료 시 일괄)**. props: `{ seedLinks, seedPlaces, onAdd..., onRemove... }` — 상태는 page.tsx 소유.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep "mode=\"range\"" apps/web/app/onboarding/_components/step-dates.tsx` 1건 + `grep "react-day-picker/locale"` 1건
    - `grep "react-day-picker/style.css" apps/web -r` == 0건 (A-8)
    - `grep "날짜는 나중에 친구들과 함께 정할 수 있어요" apps/web/app/onboarding/_components/step-dates.tsx` 1건
    - `grep "링크 추가하기" apps/web/components/add-content-tabs.tsx` 1건 + `grep "장소 이름을 검색해 보세요"` 1건 + `grep "resolve-place"` 1건
    - `grep -c "isDevToolsEnabled" apps/web/components/add-content-tabs.tsx apps/web/app/onboarding/_components/step-seed.tsx` == 0
    - `grep "add-content-tabs" apps/web/components/index.ts` 1건 (`.js` extension 없음)
    - `grep -E "addLink|addManualPlace|createMoaDraft" apps/web/app/onboarding/_components/step-seed.tsx` == 0건 (스테이징만 — DB 접촉 0)
  </acceptance_criteria>
  <done>캘린더·공유 추가 탭·시드 스테이징 완성 — 24-07이 AddContentTabs를 그대로 재사용 가능</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: buildDraft 매퍼 + 제출 흐름 + 온보딩 테스트</name>
  <files>apps/web/app/onboarding/_lib/build-draft.ts, apps/web/app/onboarding/page.tsx, apps/web/__tests__/build-draft.test.ts, apps/web/__tests__/onboarding.test.tsx</files>
  <read_first>
    - apps/web/app/onboarding/page.tsx (Task 1·2 산출물 — 상태 shape)
    - apps/web/__tests__/vote-island.test.tsx (mock 배선 idiom)
    - packages/core/src/schemas/trip.ts L49-63 (TripCreateDraftSchema 정확 계약)
  </read_first>
  <behavior>
    - build-draft Test 1: 확정 날짜 → { start_date: 'YYYY-MM-DD', end_date: 'YYYY-MM-DD' } (range.to 없으면 from = 당일치기)
    - build-draft Test 2: 미정 → start_date·end_date 둘 다 null (ONBOARD-04)
    - build-draft Test 3: 칩 도시 → title '도쿄 모아', city_code 'tokyo'; 기타 입력 '나고야' → title '나고야 모아', city_code '나고야'
    - build-draft Test 4: companion null 허용(스킵 없음 — step3은 필수 선택이지만 매퍼는 null 방어)
    - build-draft Test 5: 산출물이 TripCreateDraftSchema.parse를 통과
    - onboarding Test 6: 4단계 완주(미정 경로) → createMoaDraft가 parse된 draft로 1회 호출 + router.replace('/moa/{id}') (ONBOARD-03)
    - onboarding Test 7: step4에서 링크 1개 스테이징 후 완료 → addLink 호출 + triggerExtraction 호출 (ONBOARD-05, D-03 일괄)
    - onboarding Test 8: step4 "건너뛰기" → addLink·addManualPlace 미호출, createMoaDraft는 호출 (ONBOARD-05)
  </behavior>
  <action>
    1. `_lib/build-draft.ts` 순수 매퍼:
    ```typescript
    import { CITY_KO_MAP, TripCreateDraftSchema, type TripCreateDraft } from '@moajoa/core';
    import type { DateRange } from 'react-day-picker';

    /** 위저드 상태 → TripCreateDraft. title 파생 규칙(재량 확정):
     *  칩 도시 = `${CITY_KO_MAP[code]} 모아` (예: "도쿄 모아"), 기타 입력 = `${입력값} 모아`.
     *  날짜는 로컬 타임존 기준 YYYY-MM-DD (toISOString 금지 — UTC 시프트 버그). */
    export function buildDraft(input: {
      city: string; cityCustom: boolean;
      dateMode: 'fixed' | 'unset'; range?: DateRange;
      companion: string | null;
    }): TripCreateDraft {
      const cityLabel = input.cityCustom ? input.city : (CITY_KO_MAP[input.city] ?? input.city);
      const fmt = (d: Date) =>
        `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
      const start = input.dateMode === 'fixed' && input.range?.from ? fmt(input.range.from) : null;
      const end = input.dateMode === 'fixed' && input.range?.from ? fmt(input.range.to ?? input.range.from) : null;
      return TripCreateDraftSchema.parse({
        title: `${cityLabel} 모아`,
        city_code: input.city,
        start_date: start,
        end_date: end,
        companion: input.companion,
      });
    }
    ```
    2. page.tsx 제출 핸들러 (step4 "모아 만들기" / "건너뛰기" 둘 다 이 경로 — 건너뛰기는 시드 0개로 동일 제출):
    ```typescript
    const draft = buildDraft({ city, cityCustom, dateMode, range, companion });
    const trip = await createMoaDraft(client, draft);
    for (const url of seedLinks) {
      const link = await addLink(client, { board_id: trip.id, url });
      if (link.source_kind !== 'manual') {
        triggerExtraction(client, link.id).catch(console.error);   // fire-and-forget idiom
      }
    }
    for (const g of seedPlaces) {
      await addManualPlace(client, { board_id: trip.id, google_place_id: g.id });
    }
    router.replace(`/moa/${trip.id}`);
    ```
    실패 시 try/catch + toast('모아를 만들지 못했어요. 다시 시도해 주세요', {variant:'error'}) + submitting 해제. **seq_no·visibility 미전송.** step4 CTA: primary "모아 만들기" + 그 위 중앙 text button "건너뛰기".
    3. `__tests__/build-draft.test.ts` — behavior 1~5 (mock 불필요, 순수함수).
    4. `__tests__/onboarding.test.tsx` — behavior 6~8. vote-island.test.tsx mock idiom: `@moajoa/api`(createMoaDraft·addLink·triggerExtraction·addManualPlace vi.fn), `@/lib/supabase/browser`, `next/navigation`(replace spy), `@/components`(pass-through). 캘린더 상호작용은 테스트하지 않음(확정 경로 매퍼는 build-draft 단위 테스트가 커버) — 위저드 테스트는 미정 경로로 완주. import-after-mocks 순서, `apps/web/__tests__/` 배치.
  </action>
  <verify>
    <automated>pnpm --filter web test:run -- build-draft && pnpm --filter web test:run -- onboarding</automated>
  </verify>
  <acceptance_criteria>
    - `grep "TripCreateDraftSchema.parse" apps/web/app/onboarding/_lib/build-draft.ts` 1건
    - `grep "toISOString" apps/web/app/onboarding/_lib/build-draft.ts` == 0건 (로컬 날짜 포맷)
    - `grep "triggerExtraction" apps/web/app/onboarding/page.tsx` 1건 + `.catch` fire-and-forget 형태
    - `grep "건너뛰기" apps/web/app/onboarding/page.tsx apps/web/app/onboarding/_components/step-seed.tsx | wc -l` ≥ 1
    - `grep "seq_no" apps/web/app/onboarding -r` == 0건
    - build-draft 5케이스 + onboarding 3케이스 그린, `pnpm --filter web test:run` 전체 무회귀
  </acceptance_criteria>
  <done>ONBOARD-03/04/05 전 경로 자동 검증 — 미정/확정/시드/건너뛰기 전부 커버</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사용자 입력(도시 기타·누구랑 기타·링크 URL·검색어) → DB/EF | 자유 텍스트가 trips INSERT·links INSERT·resolve-place EF로 |
| 브라우저 → resolve-place EF | 검색어가 Google Places API로 전달 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering | 위저드 입력 → trips INSERT | mitigate | TripCreateDraftSchema.parse가 제출 게이트 (city_code ≤20·companion ≤20·dates refine) — buildDraft 안에서 parse 강제 (Task 3) |
| T-24-11 | Tampering | 링크 URL 입력 | mitigate | AddContentTabs가 `new URL()` 검증 후에만 전달 + addLink 서버 normalize + LinkSchema url zod (Task 2) |
| T-24-12 | DoS | resolve-place 검색 남용 | mitigate | EF RequestSchema(query 1..200) + Bearer JWT 게이트 기존 — UI는 명시 검색 실행(입력마다 자동 발사 금지). 비용 게이트 본격화는 Phase 27 SEC-01 범위 |
| T-24-13 | Tampering | 좌표 위조 | mitigate | addManualPlace는 google_place_id만 전송 — 좌표는 add_manual_place RPC가 서버 해석 (기존 계약 유지) |
</threat_model>

<verification>
- `pnpm --filter web test:run -- build-draft`·`-- onboarding` 그린
- `pnpm --filter web typecheck` + `pnpm --filter web build` PASS (라우트 `/onboarding`)
- isDevToolsEnabled·localStorage·style.css·seq_no grep 0건 스윕
</verification>

<success_criteria>
- 성공 기준 2 충족: 로그인 직후(0개 분기 — 24-03) 4단계를 거쳐 모아 생성, 미정/확정 양 날짜 경로 + 시드/건너뛰기 동작
- AddContentTabs가 24-07 add-sheet 재사용 가능한 계약으로 존재
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-04-SUMMARY.md`
</output>
