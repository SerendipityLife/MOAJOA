---
phase: 02-extraction-pipeline-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/api/src/types/database.ts
autonomous: false
requirements:
  - EXTRACT-06

must_haves:
  truths:
    - "Database schema includes extraction_costs table and places.source_kind/inferred_city columns on remote"
    - "TypeScript database types reflect the new schema (extraction_costs table type, places source_kind/inferred_city fields)"
    - "GCP billing alerts are active at $5/$20/$50 thresholds"
  artifacts:
    - path: "packages/api/src/types/database.ts"
      provides: "Regenerated Supabase types with extraction_costs and updated places"
      contains: "extraction_costs"
  key_links:
    - from: "packages/api/src/types/database.ts"
      to: "supabase/migrations/0004_extraction_hardening.sql"
      via: "pnpm supabase:types regeneration"
      pattern: "extraction_costs"
---

<objective>
Push the database schema to the remote Supabase project and regenerate TypeScript types, then set up GCP billing alerts (EXTRACT-06 per D-13/D-14).

Purpose: The migration from Plan 01 and the Edge Function code from Plan 02 are ready but the remote database has not been updated. This plan pushes the schema, regenerates types so downstream TypeScript code (iOS/Web) sees the new table and columns, and sets up the billing safety net that prevents runaway Google Cloud costs.

Output: Updated `packages/api/src/types/database.ts` with new schema types. GCP billing alerts active.
</objective>

<execution_context>
@/Users/a15139/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a15139/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-extraction-pipeline-hardening/02-CONTEXT.md
@.planning/phases/02-extraction-pipeline-hardening/02-01-SUMMARY.md
@.planning/phases/02-extraction-pipeline-hardening/02-02-SUMMARY.md

@supabase/migrations/0004_extraction_hardening.sql
@packages/api/src/types/database.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Push schema + regenerate TypeScript types</name>
  <files>packages/api/src/types/database.ts</files>
  <read_first>
    supabase/migrations/0004_extraction_hardening.sql
    packages/api/src/types/database.ts
  </read_first>
  <action>
This is the [BLOCKING] schema push task. The phase cannot pass verification without it.

Step 1 -- Verify Supabase project is linked:
Run `supabase projects list` to confirm a project is linked. If not linked, run `supabase link --project-ref <ref>` (the project ref should be in `.env.local` or the user will need to provide it).

Step 2 -- Push the migration:
Run `supabase db push`. This applies `0004_extraction_hardening.sql` to the remote database.

If the push requires interactive confirmation (e.g., "Do you want to push these migrations?"), try setting `SUPABASE_ACCESS_TOKEN` env var first, or use `supabase db push --include-all` if available. If interactive prompts cannot be suppressed, note the issue and let the user run the command manually.

If the push fails with an error about existing columns (e.g., `source_kind` already exists), the `IF NOT EXISTS` clause in the migration should handle it. If not, diagnose and fix.

Step 3 -- Regenerate TypeScript types:
Run `pnpm supabase:types` from the project root. This regenerates `packages/api/src/types/database.ts` to reflect the new `extraction_costs` table and updated `places` table columns.

Step 4 -- Verify the generated types:
Confirm the regenerated file contains `extraction_costs` table type with all expected columns and `places` table type includes `source_kind` and `inferred_city` fields.
  </action>
  <verify>
    <automated>grep -c 'extraction_costs' packages/api/src/types/database.ts | grep -q '[1-9]' && grep -c 'source_kind' packages/api/src/types/database.ts | grep -q '[1-9]' && grep -c 'inferred_city' packages/api/src/types/database.ts | grep -q '[1-9]' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes without errors
    - `pnpm supabase:types` completes without errors
    - `packages/api/src/types/database.ts` contains an `extraction_costs` table definition with columns: id, link_id, provider, model, input_tokens, output_tokens, cost_usd, duration_ms, created_at
    - `packages/api/src/types/database.ts` contains `source_kind` field in the places table Row type
    - `packages/api/src/types/database.ts` contains `inferred_city` field in the places table Row type
    - No TypeScript compilation errors in `packages/api`
  </acceptance_criteria>
  <done>Remote database schema updated with 0004 migration. TypeScript types regenerated and include extraction_costs table and places.source_kind/inferred_city columns.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set up GCP billing alerts at $5/$20/$50</name>
  <what-built>The extraction pipeline now calls Google Places API with per-call cost tracking ($0.032/call). EXTRACT-06 requires billing alerts as a safety net against cost overruns.</what-built>
  <how-to-verify>
    Set up billing alerts in Google Cloud Console:

    1. Go to https://console.cloud.google.com/billing
    2. Select your billing account
    3. Navigate to "Budgets & alerts" in the left sidebar
    4. Click "CREATE BUDGET"
    5. Set budget name: "MOAJOA Places API"
    6. Set budget amount: $50
    7. Set alert thresholds:
       - 10% ($5) -- warning
       - 40% ($20) -- elevated
       - 100% ($50) -- critical
    8. Configure notification channels:
       - Email to your billing account email
       - (Optional) Pub/Sub for programmatic alerting
    9. Click "Finish"

    Verification:
    - Confirm the budget appears in the "Budgets & alerts" list
    - Confirm at least one notification channel is active
    - (Optional) If you can trigger a test alert, confirm email receipt

    Document the setup:
    - Take a screenshot or note the budget ID for records
    - The billing alert is infrastructure config, not code (per D-14: no IaC/Terraform)
  </how-to-verify>
  <resume-signal>Type "alerts-configured" after setting up the billing alerts, or describe any issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local -> Remote Supabase | Schema push applies DDL to production database |
| Google Cloud Billing | Cost monitoring for Places API usage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Denial of Service | Google Places API cost overrun | mitigate | GCP billing alerts at $5/$20/$50 thresholds (EXTRACT-06). Per-call cost logging in extraction_costs table for monitoring. Max 30 candidates per extraction (Zod schema limit). |
| T-02-09 | Tampering | Schema push to production | accept | Migration uses IF NOT EXISTS for idempotency. Supabase CLI validates migration syntax before push. Append-only migration policy prevents modification of existing schema. |
</threat_model>

<verification>
- `packages/api/src/types/database.ts` reflects the 0004 migration schema
- GCP billing alerts confirmed active at 3 thresholds
- No regression in existing database tables or functions
</verification>

<success_criteria>
- Remote Supabase database has extraction_costs table and places.source_kind/inferred_city columns
- TypeScript types regenerated and include all new schema elements
- GCP billing alerts active at $5/$20/$50 (EXTRACT-06)
</success_criteria>

<output>
Create `.planning/phases/02-extraction-pipeline-hardening/02-03-SUMMARY.md` when done
</output>
