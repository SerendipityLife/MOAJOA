---
phase: 06-dogfooding-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/dogfooding/pre-dogfooding-checklist.md
  - docs/manual-uat-phase3.md
autonomous: true
requirements: [EXTRACT-07]
user_setup:
  - service: supabase
    why: "Apply migration 0006 to prod + regenerate types (Plan 05-01 deferred)"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens"
    dashboard_config:
      - task: "supabase link --project-ref <ref> (if not already linked)"
        location: "https://supabase.com/dashboard/project/<ref>/settings/general"
      - task: "supabase db push (apply migration 0006)"
        location: "local CLI after link"
      - task: "pnpm supabase:types (regenerate database.ts)"
        location: "local CLI after push"
  - service: vercel
    why: "Register REVALIDATE_SECRET + GOOGLE_MAPS_KEY + REVALIDATE_BASE_URL for Phase 4 webhook"
    env_vars:
      - name: REVALIDATE_SECRET
        source: "Generate via openssl rand -hex 32; set in Vercel + Supabase Edge Function env"
      - name: NEXT_PUBLIC_GOOGLE_MAPS_KEY
        source: "GCP Console → APIs & Services → Credentials (existing key reused)"
      - name: REVALIDATE_BASE_URL
        source: "Vercel deployment URL (https://moajoa.vercel.app or custom domain)"
    dashboard_config:
      - task: "Set env vars in Vercel Production environment"
        location: "Vercel → Project → Settings → Environment Variables"
      - task: "Set REVALIDATE_SECRET in Supabase Edge Function secrets"
        location: "supabase secrets set REVALIDATE_SECRET=<value> --project-ref <ref>"
      - task: "Enable Maps Static API in GCP"
        location: "GCP Console → APIs & Services → Library → 'Maps Static API' → Enable"
      - task: "supabase functions deploy extract-youtube (after secret set)"
        location: "local CLI"
  - service: ios-build
    why: "Phase 3 Share Extension prebuild + real-device install for share-sheet UAT"
    dashboard_config:
      - task: "pnpm --filter @moajoa/ios prebuild + cd ios && pod install"
        location: "local machine with Xcode 16+"
      - task: "Install on real iPhone via Xcode or EAS"
        location: "Connected iPhone or EAS Build dashboard"

must_haves:
  truths:
    - "Pre-dogfooding 체크리스트가 100% 통과되어 dogfooding을 깨끗한 환경에서 시작할 수 있다"
    - "Migration 0006이 prod에 적용되어 places.confidence + public_board_view + first_board trigger가 동작한다"
    - "REVALIDATE_SECRET이 Vercel + Supabase Edge Function 양쪽에 동일 값으로 set되어 webhook이 작동한다"
    - "iOS Share Extension이 실기기에서 prebuild → install → share-sheet 1탭 저장이 동작한다"
    - "Phase 3 manual UAT 5 시나리오 + N1 + N2 모두 close된다"
    - "Phase 4 실브라우저 UAT(모바일 Safari + 카톡 OG)가 통과된다"
    - "Phase 5 iOS UAT(step indicator + low-conf sheet + first board + onboard card)가 통과된다"
  artifacts:
    - path: ".planning/dogfooding/pre-dogfooding-checklist.md"
      provides: "단일 체크리스트 — 모든 deferred 작업 + UAT 시나리오 + commit SHA/스크린샷 경로 inline 기록"
      contains: "T1.Phase 5 인프라"
      contains_2: "T1.Phase 4 웹 인프라"
      contains_3: "T1.Phase 3 prebuild + real-device UAT"
      contains_4: "T1.Phase 4 실브라우저 UAT"
      contains_5: "T1.Phase 5 iOS UAT"
      min_lines: 100
    - path: "docs/manual-uat-phase3.md"
      provides: "Phase 3 UAT 시나리오 1~5 + N1 + N2 체크박스 update (수정)"
  key_links:
    - from: ".planning/dogfooding/pre-dogfooding-checklist.md"
      to: "supabase/migrations/0006_trust_ui_onboarding.sql"
      via: "수동 push 후 commit SHA 또는 push 시각 기록"
    - from: ".planning/dogfooding/pre-dogfooding-checklist.md"
      to: "apps/ios/ios/MOAJOA/MOAJOA.entitlements"
      via: "prebuild 후 entitlement 파일 inline grep 결과"
    - from: ".planning/dogfooding/pre-dogfooding-checklist.md"
      to: ".planning/dogfooding/screenshots/"
      via: "각 manual UAT 항목당 스크린샷 경로 기록"
---

<objective>
Phase 3/4/5에서 "end-of-phase batch"로 미뤘던 모든 deferred 작업(인프라 push, env 등록, prebuild, manual UAT)을 단일 체크리스트로 집계하고, 본인이 dogfooding 시작 전에 모두 close한다.

Purpose: 7일 dogfooding을 깨끗한 환경에서 시작하기 위한 prerequisite. Migration이 안 올라가거나 webhook이 안 굴러가면 dogfooding 측정 자체가 오염됨.
Output: `.planning/dogfooding/pre-dogfooding-checklist.md` — 모든 체크박스 ☑️ + commit SHA/스크린샷 경로 inline 기록. D-01/D-02 lock.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dogfooding-gate/06-CONTEXT.md

# Phase 3 UAT
@.planning/phases/03-ios-save-flow/03-VALIDATION.md
@docs/manual-uat-phase3.md

# Phase 4 deferred
@.planning/phases/04-public-board-web/deferred-items.md
@.planning/phases/04-public-board-web/04-04-SUMMARY.md

# Phase 5 deferred (Plan 05-01 type regen + migration push)
@.planning/phases/05-trust-ui-onboarding/05-01-SUMMARY.md

# CLAUDE.md guardrails
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pre-dogfooding-checklist.md 작성 (단일 골든 문서)</name>
  <files>.planning/dogfooding/pre-dogfooding-checklist.md</files>
  <action>
파일 생성. D-01의 5개 그룹 + D-02 sign-off 규칙을 그대로 옮긴 master checklist. 각 항목은 다음 4-column 표 형식:

| # | 항목 | 검증 명령/관찰 | 상태 + 증거 |

각 항목의 "상태 + 증거" 칸은 처음엔 빈칸 — 사용자가 항목을 수행할 때 commit SHA (`git rev-parse HEAD`), 스크린샷 경로, 또는 관찰 결과를 inline 기록한다.

**섹션 구성 (D-01 순서 그대로 — 의존성 순):**

## A. Phase 5 → 인프라 (먼저 — Edge Function/타입 안정화)

- [ ] A-1: `supabase login` (one-time) → `supabase link --project-ref <ref>` (linked 상태 확인은 `supabase projects list`로) — *증거: 링크 확인 출력 paste*
- [ ] A-2: `supabase db push` → migration 0006_trust_ui_onboarding.sql apply (per D-01 Phase 5 group). 검증: `psql "$SUPABASE_DB_URL" -c "\d places"`로 `confidence` 컬럼 존재 확인 — *증거: psql output paste + push 시각*
- [ ] A-3: 트리거 backfill 검증 SQL (05-01-SUMMARY.md User Setup #3 그대로) — `select count(*) from profiles p where not exists (select 1 from boards where owner_id = p.id);` → 0이어야 함 — *증거: 결과 행 수*
- [ ] A-4: `pnpm supabase:types` (또는 `supabase gen types typescript --linked > packages/api/src/types/database.ts`) → `grep -c confidence packages/api/src/types/database.ts` > 0 확인. **WARN:** 05-01-SUMMARY.md Rule 1에 따라 redirect가 실패 시 파일을 0바이트로 만들 수 있음 — 실패하면 `git checkout HEAD -- packages/api/src/types/database.ts` 후 재시도 — *증거: grep -c 결과 + 변경 라인 수*
- [ ] A-5: `pnpm typecheck`(루트) → 0 에러 — *증거: exit code 또는 "All good" 메시지*
- [ ] A-6: `deno check supabase/functions/extract-youtube/index.ts` → Phase 4 deferred-items.md의 9 errors가 type regen으로 해소되었는지 확인. 잔여 에러 발생 시 helper generic widening 직접 fix (시간박스 30분, 초과 시 v2-backlog.md로 이동) — *증거: error count before/after*

## B. Phase 4 → 웹 인프라

- [ ] B-1: `openssl rand -hex 32` → REVALIDATE_SECRET 값 생성 (한 번 생성 후 양쪽에 동일 값) — *증거: 첫 8글자만 paste (full secret 보안 위해 X)*
- [ ] B-2: Vercel Production env에 REVALIDATE_SECRET set — Vercel Dashboard → Project → Settings → Environment Variables. 확인: `vercel env ls production | grep REVALIDATE_SECRET` — *증거: vercel env ls output*
- [ ] B-3: Supabase Edge Function secret에 동일 값 set — `supabase secrets set REVALIDATE_SECRET=<value> --project-ref <ref>`. 확인: `supabase secrets list | grep REVALIDATE_SECRET` — *증거: secrets list output*
- [ ] B-4: Vercel에 추가 env 확인: `NEXT_PUBLIC_GOOGLE_MAPS_KEY`, `SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `WEB_BASE_URL` (또는 `REVALIDATE_BASE_URL`) 모두 production에 set — *증거: vercel env ls grep*
- [ ] B-5: GCP Console → APIs & Services → Library → "Maps Static API" → **Enable**. 동일 API key에 referrer restriction이 `*.vercel.app`와 production 도메인 모두 허용 확인 — *증거: GCP UI 스크린샷 경로 (e.g., screenshots/maps-static-api-enabled.png)*
- [ ] B-6: `supabase functions deploy extract-youtube` (REVALIDATE_SECRET 환경 반영) — *증거: deploy output 또는 commit SHA*
- [ ] B-7: Webhook 작동 sanity — 임의의 public 보드에 URL 추가하여 추출 트리거 → Vercel logs에서 `/api/revalidate` 호출 1건 확인 — *증거: Vercel log entry 경로 또는 paste*

## C. Phase 3 → iOS prebuild + 실기기 install

- [ ] C-1: `pnpm --filter @moajoa/ios prebuild` (Path A) — 첫 native build. expo-share-intent plugin이 entitlement wiring 했는지 검증 — *증거: prebuild 시각 + exit code*
- [ ] C-2: `cat apps/ios/ios/MOAJOA/MOAJOA.entitlements | grep -A2 com.apple.security.application-groups` → `group.com.serendipitylife.moajoa` 한 줄 inline 확인 (Phase 3 D-?? + 06-CONTEXT Known Pitfall "prebuild 후 entitlement drift") — *증거: grep output paste*
- [ ] C-3: `cd apps/ios/ios && pod install && cd -` → exit 0 — *증거: exit code*
- [ ] C-4: Xcode 또는 EAS로 실기기 install — 첫 화면 뜨고 NativeWind 적용됨 (Phase 1 BUILD-01/02 회귀 확인 겸용) — *증거: 첫 화면 스크린샷 (screenshots/cold-launch.png)*

## D. Phase 3 → 실기기 manual UAT (docs/manual-uat-phase3.md 시나리오)

각 시나리오는 `docs/manual-uat-phase3.md`에서 정의됨. 본 체크리스트는 status만 mirror — 실제 진행은 docs/manual-uat-phase3.md의 체크박스로 한다 (Task 2에서 update).

- [ ] D-1: Scenario 1 — 로그인 → boards → 보드 상세 진입 (SAVE-01) — *증거: docs/manual-uat-phase3.md 체크박스 ✓ + 스크린샷 경로*
- [ ] D-2: Scenario 2 — URL 추가 → 30s 내 핀 (SAVE-02, 3 timed runs max-of-3) — *증거: 3개 시각 측정값 + 최대값*
- [ ] D-3: Scenario 3 — Safari/카톡 share sheet → MOAJOA 저장 (SAVE-03, dismiss < 1.5s) — *증거: 스크린샷 + dismiss 시각 추정*
- [ ] D-4: Scenario 4 — airplane mode → 추가 → online drain (SAVE-04) — *증거: 핀 표시 확인*
- [ ] D-5: Scenario 5 — 수동 핀 + 검색 → resolve → rename → delete (SAVE-05) — *증거: 각 단계 스크린샷*
- [ ] D-6: N1 — retry > 3 → failed banner — *증거: banner 스크린샷 또는 unit test 결과 인용 (Plan 03-04 pending.test.ts)*
- [ ] D-7: **N2 (REQUIRED — binary gate)** — SQL RLS 회피 시도: `psql "$SUPABASE_DB_URL" -c "select set_config('request.jwt.claim.sub', '00000000-0000-0000-0000-000000000099', true); insert into places (board_id, name_local, lat, lng) values ('<my-board-id>', 'rls-test', 0, 0);"` → expect `ERROR: 42501` (insufficient_privilege) — *증거: 정확한 에러 메시지 paste*

## E. Phase 4 → 실브라우저 UAT

- [ ] E-1: iPhone Safari로 `https://<prod-host>/b/<my-public-slug>` 열람 — 핀치줌, 핀 탭, gestureHandling 'greedy' 정상 — *증거: 스크린샷 (screenshots/ios-safari-public-board.png)*
- [ ] E-2: 핀 탭 → YouTube 앱/웹에서 정확한 `?t=Xs` timestamp로 영상 재생 — *증거: 짧은 화면 녹화 또는 스크린샷*
- [ ] E-3: 카톡 "나에게 보내기"에 production URL 붙여넣기 → OG 카드 한글 정상 (Pretendard) + 미니맵 thumbnail + 핀 N개 — *증거: 카톡 스크린샷 (screenshots/kakao-og-preview.png)*
- [ ] E-4: Vercel Analytics에서 `/b/[slug]` SSR TTFB 측정 → p90 < 800ms 확인 (VIEW-01 manual gate, 표본 ≥ 10 요청) — *증거: Vercel Analytics 스크린샷 + 측정값*

## F. Phase 5 → iOS UAT

- [ ] F-1: 5단계 step indicator 시각 확인 — 보드 상세에서 URL 추가 → overlay에 '메타데이터' → '자막' → 'AI 분석' → '장소 찾기' → '완료' 순서로 카피 표시 (D-09 EXTRACT_STEP_KO fixture) — *증거: 5장 스크린샷 또는 화면 녹화*
- [ ] F-2: Low confidence 핀 marker (opacity 0.5 + amber 'CYR' badge) — 추출 결과 중 `confidence < 0.7`인 핀이 시각적으로 약하게 표시됨 — *증거: 지도 스크린샷*
- [ ] F-3: PinBottomSheet에서 low-conf 핀 탭 → `[확인]` / `[잘못됨]` 버튼이 이름 수정 위에 표시됨 → `[확인]` 탭 → source_kind='manual' + confidence=null로 변경되어 marker가 normal로 바뀜 — *증거: before/after 스크린샷*
- [ ] F-4: 첫 보드 자동 생성 trigger 검증 — 신규 가입 또는 기존 profile에 대해 `select * from boards where owner_id = '<my-uuid>'` → '내 첫 여행' 보드 1개 이상 — *증거: SQL 결과*
- [ ] F-5: OnboardCard 표시 + dismiss 영구 — 첫 보드 상세 진입 시 amber-50 카드 1회 표시, × 탭 후 재진입 시 미표시 — *증거: 2장 스크린샷 (보임/안 보임)*

## Sign-off (D-02)

- [ ] **모든 체크박스 ☑️ + 각 항목에 commit SHA 또는 스크린샷 경로 inline 기록 완료**
- [ ] **P0 발견 시 fix → 체크박스 close (별도 hotfix commit 허용, GSD 우회 per 06-CONTEXT scope lock)**
- [ ] **P1 이하 발견 시 `.planning/dogfooding/v2-backlog.md`에 1행 등록 (close 안 해도 dogfooding 시작 가능)**
- [ ] **Sign-off 시각 + 본인 이니셜 기록 → dogfooding Day 1 시작 가능 신호**

---
*Pre-dogfooding gate per Phase 6 D-01/D-02. 본 체크리스트가 모두 close되어야 7일 dogfooding 시작.*
*Document created: <YYYY-MM-DD>*
  </action>
  <verify>
    <automated>test -f .planning/dogfooding/pre-dogfooding-checklist.md && grep -q "A-1" .planning/dogfooding/pre-dogfooding-checklist.md && grep -q "## D. Phase 3" .planning/dogfooding/pre-dogfooding-checklist.md && grep -q "N2 (REQUIRED" .planning/dogfooding/pre-dogfooding-checklist.md && grep -q "Sign-off" .planning/dogfooding/pre-dogfooding-checklist.md && wc -l .planning/dogfooding/pre-dogfooding-checklist.md | awk '{exit ($1 >= 80) ? 0 : 1}'</automated>
  </verify>
  <done>File exists, contains all 6 sections (A~F + Sign-off), N2 SQL substitute test marked REQUIRED, ≥ 80 lines.</done>
</task>

<task type="auto">
  <name>Task 2: docs/manual-uat-phase3.md 체크박스 status 동기화 + N2 SQL 명령 inline 보강</name>
  <files>docs/manual-uat-phase3.md</files>
  <action>
기존 `docs/manual-uat-phase3.md` (Plan 03-01 산출물)를 read한 후, 다음 2개 변경만 적용한다 (Karpathy §3.3 surgical — 다른 부분 건드리지 X):

1. **N2 시나리오에 정확한 SQL 명령 inline 보강** — 03-VALIDATION.md L106에 정의된 명령을 N2 시나리오 본문 안에 code block으로 옮긴다:
   ```sql
   -- N2: non-member RLS denial (REQUIRED, binary gate)
   -- Run against linked Supabase prod via psql "$SUPABASE_DB_URL"
   select set_config('request.jwt.claim.sub', '00000000-0000-0000-0000-000000000099', true);
   insert into places (board_id, name_local, lat, lng)
     values ('<my-board-id>', 'rls-test', 0, 0);
   -- Expected: ERROR: 42501 insufficient_privilege
   ```
   "Expected" 라인까지 포함. 이 SQL을 그대로 copy해서 dogfooding 시점에 실행할 수 있게.

2. **각 시나리오(1~5, N1, N2) 끝에 "Evidence:" 라인 추가** — 사용자가 dogfooding 시점에 스크린샷 경로 또는 commit SHA를 inline으로 채울 수 있게 `Evidence: ` 한 줄 (빈 값) append. 형식:
   ```
   - [ ] Scenario 1 ... (existing content)
     - Evidence: 
   ```

기존 시나리오 본문, 체크박스 상태, 다른 메타데이터는 절대 수정 X.
  </action>
  <verify>
    <automated>test -f docs/manual-uat-phase3.md && grep -q "set_config" docs/manual-uat-phase3.md && grep -q "42501" docs/manual-uat-phase3.md && grep -c "Evidence:" docs/manual-uat-phase3.md | awk '{exit ($1 >= 7) ? 0 : 1}'</automated>
  </verify>
  <done>N2 SQL command inline in body, "42501" expected outcome present, Evidence: line on each of 7 scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CLI → Supabase prod | `supabase db push`로 prod 스키마 변경. service-role 권한 |
| Vercel env → Supabase Edge Function secret | REVALIDATE_SECRET 양쪽 동일 값 등록. 한 쪽만 set되면 webhook 무한 401 |
| GCP Console → Maps Static API | API key referrer restriction이 너무 좁으면 OG image 빈 박스, 너무 넓으면 quota 도용 |
| Local jwt.claim.sub override (N2 test) | psql session에서 다른 user uuid 가장. SECURITY DEFINER 헬퍼 우회 시도 — RLS deny가 확인되어야 함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | T | `supabase db push` (migration 0006 apply) | mitigate | linked project 확인 (`supabase projects list`) 후 push. CLAUDE.md §4.3 append-only — 기존 0001~0005 수정 X 확인 |
| T-06-01-02 | I | REVALIDATE_SECRET in Vercel env logs | mitigate | 체크리스트에 첫 8글자만 기록. full secret을 .planning/ md에 paste 금지. Vercel은 env value를 UI에서 mask 처리 |
| T-06-01-03 | E | GCP API key referrer too-permissive | accept | dogfooding 단계 1차 한국어 사용자만, 미공개. v2 외부 사용자 받기 전 referrer 좁힘 |
| T-06-01-04 | T | iOS entitlement drift after prebuild | mitigate | C-2 task에 entitlement grep으로 명시 검증. 06-CONTEXT Known Pitfall로 기록 |
| T-06-01-05 | E | N2 test가 false-positive (RLS deny된 게 아니라 다른 이유로 fail) | mitigate | 정확한 error code 42501 확인 필수. 다른 에러(다른 code) 발생 시 RLS 검증 안 된 것으로 처리 |
</threat_model>

<verification>
- `.planning/dogfooding/pre-dogfooding-checklist.md` 파일 존재 + 6 섹션 + Sign-off
- `docs/manual-uat-phase3.md`에 N2 SQL command + 42501 expected + 7개 Evidence: 라인
- Dogfooding 시점 (이후): 모든 체크박스 ☑️ + 각 항목 inline 증거 기록
</verification>

<success_criteria>
- Pre-dogfooding 체크리스트 마스터 문서가 작성되어 있다 (Task 1)
- Phase 3 UAT 시나리오 문서가 N2 SQL + Evidence 라인으로 보강되어 있다 (Task 2)
- 본인이 모든 체크박스를 close할 수 있는 명확한 명령/관찰 기준이 inline 기록되어 있다
- D-01의 5개 그룹(A~E + F) + D-02 sign-off 규칙이 모두 반영되어 있다
</success_criteria>

<output>
After completion, create `.planning/phases/06-dogfooding-gate/06-01-SUMMARY.md` per template, including:
- Checklist sections written + lines created
- N2 SQL command inline in manual-uat-phase3.md
- User-side actions enumerated (db push, vercel env, GCP enable, prebuild) — explicit deferred list
- Note that체크박스 close 자체는 dogfooding 시작 직전 본인이 손으로 (이 plan은 골든 문서 작성까지가 scope)
</output>
