---
phase: 25-guest-unified-share
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0029_public_trip_poll.sql
  - packages/api/src/queries/date-polls.ts
  - packages/api/src/queries/date-polls.test.ts
  - packages/api/src/queries/trips.ts
  - packages/api/src/queries/places.ts
  - packages/api/src/queries/places.test.ts
  - packages/api/src/types/database.ts
  - packages/core/src/types/index.ts
  - supabase/tests/public_trip_poll_smoke.sh
autonomous: false
requirements: [SHARE-02, SHARE-03, AUTH-08]
user_setup:
  - service: supabase
    why: "0029 마이그레이션 원격 적용 — 라이브 dates/both 게스트 투표·share_mode SSR 분기·D-12 own-only 소프트삭제 게이트"
    dashboard_config:
      - task: "원격 DB에 0029 적용 (git push main 시 Supabase↔GitHub 자동 적용, 0028 선례) 또는 `supabase db push` 1회"
        location: "사용자 터미널 `supabase db push` (colima+docker 가동) 또는 main push"

must_haves:
  truths:
    - "비멤버 익명/비로그인 세션이 slug로 poll_code·mode·status·options를 얻을 수 있다 (public_trip_poll anon RPC)"
    - "SSR 공유 seed에 share_mode가 포함되어 게스트 화면이 places/dates/both를 분기할 수 있다"
    - "익명 세션이 서버파생 device_token(auth.uid)으로 날짜투표를 저장할 수 있고 spoof이 불가하다"
    - "장소 소프트삭제가 hide_place_as_member DEFINER RPC로만 이뤄져 호스트(owner)만 전체 hide하고 게스트-에디터 포함 그 외는 자기 장소(added_by=auth.uid)만 숨길 수 있다 (D-12 DB-airtight, Pitfall 5 봉합)"
    - "supabase db reset이 0016→0029 클린 통과하고(42P17=0) 재생성 타입 위에서 api 테스트가 그린이다"
  artifacts:
    - path: "supabase/migrations/0029_public_trip_poll.sql"
      provides: "public_trip_poll(slug) anon RPC + public_trip_view share_mode 추가 + cast_date_vote_authed 서버파생 래퍼 + hide_place_as_member own-only 소프트삭제 RPC"
      contains: "create or replace function hide_place_as_member"
    - path: "packages/api/src/queries/date-polls.ts"
      provides: "getPublicTripPoll · castDateVoteAuthed 래퍼"
      contains: "public_trip_poll"
    - path: "packages/api/src/queries/places.ts"
      provides: "hidePlace가 hide_place_as_member RPC 경유(D-12 DB-airtight own-only)"
      contains: "hide_place_as_member"
    - path: "packages/core/src/types/index.ts"
      provides: "PublicBoardView.board.share_mode 노출"
      contains: "share_mode"
  key_links:
    - from: "apps/web/lib/public-trip-cache.ts (getCachedPublicTrip)"
      to: "public_trip_view(slug)"
      via: "getPublicTripBySlug → board.share_mode"
      pattern: "share_mode"
    - from: "packages/api/src/queries/date-polls.ts (getPublicTripPoll)"
      to: "public_trip_poll(slug)"
      via: "client.rpc"
      pattern: "rpc\\('public_trip_poll'"
    - from: "packages/api/src/queries/places.ts (hidePlace)"
      to: "hide_place_as_member(place_id)"
      via: "client.rpc"
      pattern: "rpc\\('hide_place_as_member'"
---

<objective>
게스트 통합 화면의 **백엔드 전제 4종**을 한 append-only 마이그레이션(0029)으로 봉합한다: (1) 비멤버 익명이 slug로 poll을 얻을 anon-grant DEFINER RPC `public_trip_poll`(dates/both 임베드 게이트 — RESEARCH A2/Pattern 2), (2) SSR 게스트 화면이 D-09 모드 분기에 쓸 `share_mode`를 `public_trip_view`에 추가(현재 미노출 — 확인됨), (3) 날짜투표 spoof 차단 서버파생 래퍼 `cast_date_vote_authed`(device_token := auth.uid, Open Q2 채택), (4) **D-12를 DB에서 airtight하게 강제**하는 own-only 소프트삭제 RPC `hide_place_as_member`(Pitfall 5 봉합 — 현재 `hidePlace`가 `can_edit_trip` UPDATE라 게스트가 API 직타로 남의 장소도 숨김 가능). 이어서 api 래퍼·core 타입·hidePlace 전환·[BLOCKING] 로컬 적용·타입 재생성·0029 스모크.

Purpose: 이 phase의 모든 게스트 참여(SHARE-02/03, AUTH-08)와 D-12 권한 경계가 이 backend seam 위에 배선된다. 프론트(Plan 03)·삭제 UI(Plan 02)가 탐색 없이 import·의존할 계약을 먼저 잠근다.
Output: 0029 마이그레이션 + `getPublicTripPoll`/`castDateVoteAuthed` 래퍼 + `hidePlace`(RPC 경유) + `PublicBoardView.board.share_mode` + 재생성된 `database.ts` + 0029 스모크.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-guest-unified-share/25-RESEARCH.md
@.planning/phases/25-guest-unified-share/25-PATTERNS.md

<interfaces>
<!-- 실행자가 탐색 없이 쓸 계약. 아래 라인 참조는 진실원본. -->

기존 idiom (mirror 대상):
- `supabase/migrations/0016_trips_baseline.sql` L689-760 — `public_trip_view(p_slug)` DEFINER RPC. **L710-717의 'trip' jsonb_build_object에 share_mode가 없음(확인됨)** → 0029에서 `create or replace`로 `'share_mode', v_trip.share_mode` 한 줄 추가.
- `supabase/migrations/0016_trips_baseline.sql` L313-336 — `can_edit_trip(p_trip_id uuid)` DEFINER 헬퍼(owner or role in owner/editor). L220-233 — `am_trip_owner(p_trip_id uuid)` DEFINER 헬퍼 — hide_place_as_member가 재사용(호스트만 전체 hide, 게스트-에디터 포함 그 외 own-only).
- `supabase/migrations/0016_trips_baseline.sql` L448-494 — `places`(hidden_at timestamptz L449) · UPDATE 정책 `can_edit_trip`(L482-486, **own 아님 — Pitfall 5**) · DELETE 정책 `added_by=auth.uid or am_trip_owner`(L488-494).
- `supabase/migrations/0018_date_polls.sql` L125-144 (`cast_date_vote` 본문) · L147-164 (`poll_view_by_code` 옵션 집계 idiom).
- `cast_date_vote` 본문(L132-142): open poll 조회 → nickname/option/date/availability 검증 → `insert ... on conflict (poll_id, device_token, option_id, vote_date) do update`.

기존 api 계약 (mirror 대상):
- `packages/api/src/queries/date-polls.ts` L38-43 `pollByCode`: `client.rpc('poll_view_by_code', { p_code })`, `{ data, error } throw`.
- `packages/api/src/queries/date-polls.ts` L16-36 `castDateVote`: `client.rpc('cast_date_vote', { p_code, p_device_token, p_nickname, ... })`.
- `packages/api/src/queries/trips.ts` L222-230 `getPublicTripBySlug`: `return { ...rest, board: trip } as PublicBoardView;` (public_trip_view 결과 매핑).
- `packages/api/src/queries/places.ts` L58-64 `hidePlace`: 현재 `client.from('places').update({ hidden_at }).eq('id', id)` (raw UPDATE — RLS=can_edit_trip). L112 `deletePlace = hidePlace`, L143 `rejectAiPlace = hidePlace` 별칭 — hidePlace 전환 시 자동 승계.
- `packages/api/src/queries/places.test.ts` L9-13 `makeClient`: `rpc = vi.fn(...)` mock (hidePlace는 현재 미테스트).

core 타입:
- `packages/core/src/types/index.ts` L25-50 `PublicBoardView` — `board: Pick<Trip, 'id'|'title'|'description'|'city_code'|'cover_image_url'|'updated_at'>` (share_mode 없음). `Trip.share_mode`는 `packages/core/src/schemas/trip.ts` L22에 이미 `z.enum(ShareMode).nullable()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 0029 마이그레이션 — public_trip_poll + share_mode + cast_date_vote_authed + hide_place_as_member</name>
  <read_first>
    supabase/migrations/0016_trips_baseline.sql L220-233 (am_trip_owner) · L313-336 (can_edit_trip) · L448-494 (places 정책) · L689-760 (public_trip_view 전체) ·
    supabase/migrations/0018_date_polls.sql L125-164 (cast_date_vote + poll_view_by_code) ·
    25-PATTERNS.md §0029 (anon-grant DEFINER idiom).
  </read_first>
  <files>supabase/migrations/0029_public_trip_poll.sql</files>
  <action>
새 append-only 파일 `supabase/migrations/0029_public_trip_poll.sql` 생성. 기존 0016~0028 **절대 수정 금지**(CLAUDE.md §4.3). 파일 상단 주석에 `-- BREAKING DB CHANGE` 표기(§4.6). 네 개의 `create or replace function` 포함:

1. **public_trip_view 확장** — 0016 L689-760 본문을 verbatim 복사하되 'trip' jsonb_build_object(L710-717)에 `'share_mode', v_trip.share_mode` 한 줄만 추가. 나머지 필드·grant(`to authenticated, anon`)·`security definer set search_path = public` 동일. (additive — 기존 소비자 무영향.)

2. **public_trip_poll(p_slug text)** 신규 — 25-RESEARCH.md §Pattern 2 / 25-PATTERNS.md §0029의 코드 그대로. `stable security definer set search_path = public`. slug→trips(visibility in ('public','shared')) 조회, poll 없으면 `return null`, 있으면 `date_poll_options` 집계(0018 L154-157 idiom verbatim) 후 `jsonb_build_object('poll_code', v_poll.poll_code, 'mode', v_poll.mode, 'status', v_poll.status, 'options', v_options)`. `grant execute on function public_trip_poll(text) to authenticated, anon;`

3. **cast_date_vote_authed(...)** 신규 (Open Q2 — spoof 차단) — 0018 `cast_date_vote`(L125-144) 본문 미러하되 `p_device_token` 인자 제거하고 본문 insert에서 `auth.uid()::text`를 device_token 자리에 사용. 시그니처: `cast_date_vote_authed(p_code text, p_nickname text, p_option_id uuid default null, p_vote_date date default null, p_availability text default 'available')`. `security definer set search_path = public`. **grant는 `to authenticated`만**(anon 아님 — 익명이라도 세션 필요, 서버가 auth.uid 파생). `/poll` 레거시 `cast_date_vote`는 **무변경**(별도 함수).

4. **hide_place_as_member(p_place_id uuid)** 신규 (D-12 DB-airtight own-only 소프트삭제 — Pitfall 5 봉합). `returns void language plpgsql volatile security definer set search_path = public`. 본문:
   ```sql
   declare v_place places%rowtype;
   begin
     select * into v_place from places where id = p_place_id;
     if not found then raise exception 'place not found'; end if;
     if am_trip_owner(v_place.trip_id) or v_place.added_by = auth.uid() then
       update places set hidden_at = now() where id = p_place_id;
     else
       raise exception 'not authorized to hide this place';
     end if;
   end;
   ```
   0016 `am_trip_owner`(L220) DEFINER 헬퍼 재사용(직접 EXISTS 금지 §4.4). **grant는 `to authenticated`만**: `grant execute on function hide_place_as_member(uuid) to authenticated;`. 이 RPC가 소프트삭제 유일 경로가 되어(Task 2에서 hidePlace 전환) API가 raw `places UPDATE hidden_at`(RLS=can_edit_trip, 남의 것도 가능)를 더는 쓰지 않게 한다 — 호출자가 am_trip_owner(호스트)면 전체 hide, 그 외(게스트-에디터 포함)는 자기 장소(added_by=auth.uid)만 — D-12 게스트-에디터까지 airtight.
  </action>
  <verify>
    <automated>grep -c "create or replace function" supabase/migrations/0029_public_trip_poll.sql   # 4</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "'share_mode', v_trip.share_mode" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "function public_trip_poll(p_slug text)" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "public_trip_poll(text) to authenticated, anon" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "cast_date_vote_authed" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "auth.uid()::text" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "cast_date_vote_authed(text, text, uuid, date, text) to authenticated" supabase/migrations/0029_public_trip_poll.sql` (anon 미포함)
    - `grep -q "function hide_place_as_member(p_place_id uuid)" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "v_place.added_by = auth.uid()" supabase/migrations/0029_public_trip_poll.sql` (own-only 조건)
    - `grep -q "am_trip_owner(v_place.trip_id)" supabase/migrations/0029_public_trip_poll.sql`
    - `grep -q "hide_place_as_member(uuid) to authenticated" supabase/migrations/0029_public_trip_poll.sql` (anon 미포함)
    - 기존 0016~0028 파일 diff 0: `git diff --name-only supabase/migrations/ | grep -v 0029` 결과 없음
  </acceptance_criteria>
  <done>0029에 4 함수(public_trip_view+share_mode·public_trip_poll·cast_date_vote_authed·hide_place_as_member), grant 정확, 기존 마이그레이션 무수정.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: api 래퍼 2종 + hidePlace RPC 전환 + PublicBoardView.board.share_mode</name>
  <read_first>
    packages/api/src/queries/date-polls.ts L16-43 · packages/api/src/queries/trips.ts L216-230 ·
    packages/api/src/queries/places.ts L58-64·L112·L143 · packages/api/src/queries/places.test.ts L9-13 ·
    packages/core/src/types/index.ts L25-50 · packages/api/src/queries/date-polls.test.ts (기존 mock 패턴).
  </read_first>
  <files>packages/api/src/queries/date-polls.ts, packages/api/src/queries/date-polls.test.ts, packages/api/src/queries/places.ts, packages/api/src/queries/places.test.ts, packages/api/src/queries/trips.ts, packages/core/src/types/index.ts</files>
  <behavior>
    - getPublicTripPoll(client, slug) → `client.rpc('public_trip_poll', { p_slug: slug })` 1회 호출, error 시 throw, data 반환.
    - castDateVoteAuthed(client, {code, nickname, optionId?, voteDate?, availability}) → `client.rpc('cast_date_vote_authed', { p_code, p_nickname, p_option_id, p_vote_date, p_availability })` — **p_device_token 인자 없음**, error 시 throw.
    - hidePlace(client, id) → `client.rpc('hide_place_as_member', { p_place_id: id })` 1회 호출, error 시 throw, void. (raw `from('places').update` 제거 — D-12 DB-airtight.)
    - PublicBoardView.board Pick에 'share_mode' 포함되어 타입 통과.
  </behavior>
  <action>
`date-polls.ts`에 `pollByCode`(L38-43) house 계약 미러로 두 wrapper append:
- `getPublicTripPoll(client, slug: string): Promise<unknown>` — 25-PATTERNS.md §date-polls.ts 그대로.
- `castDateVoteAuthed(client, input: { code; nickname; optionId?; voteDate?; availability })` — `castDateVote`(L16-36) 미러하되 `p_device_token` 인자 제거, RPC명 `cast_date_vote_authed`.

`places.ts` `hidePlace`(L58-64)를 raw `client.from('places').update({ hidden_at }).eq('id', id)`에서 `client.rpc('hide_place_as_member', { p_place_id: id })`로 전환(D-12 DB-airtight own-only). `{ error } throw` 계약 유지, `Promise<void>` 시그니처 무변경. `deletePlace`(L112)·`rejectAiPlace`(L143) 별칭은 hidePlace를 참조하므로 자동 승계 — 모든 소프트삭제가 own-only RPC 경유. `unhidePlace`(L66-69, 언하이드)는 이 phase 범위 밖, 무변경.

`packages/core/src/types/index.ts` L26 `PublicBoardView.board` Pick 유니온에 `'share_mode'` 추가. `packages/api/src/queries/trips.ts` `getPublicTripBySlug`는 `{ ...rest, board: trip }` 스프레드라 0029가 share_mode를 반환하면 자동 통과 — 매핑 코드 변경 불필요, 단 반환 캐스팅이 새 타입과 정합하는지만 확인.

`date-polls.test.ts`에 두 wrapper 케이스 추가(기존 rpc mock chain 스타일): getPublicTripPoll이 정확한 인자로 rpc 1회 호출·castDateVoteAuthed가 `cast_date_vote_authed`를 device_token 없이 호출. `places.test.ts`에 hidePlace 케이스 추가(기존 `makeClient` rpc mock L9-13 재사용): hidePlace가 `hide_place_as_member`를 `{ p_place_id: id }`로 rpc 1회 호출·{error} 시 throw.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api exec vitest run src/queries/date-polls.test.ts src/queries/places.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "rpc('public_trip_poll'" packages/api/src/queries/date-polls.ts`
    - `grep -q "cast_date_vote_authed" packages/api/src/queries/date-polls.ts`
    - `grep -q "rpc('hide_place_as_member'" packages/api/src/queries/places.ts`
    - hidePlace 본문에서 raw update 제거: `! grep -q "from('places')" <(sed -n '/export async function hidePlace/,/^}/p' packages/api/src/queries/places.ts)`
    - `grep -q "share_mode" packages/core/src/types/index.ts`
    - `castDateVoteAuthed` 정의에 `p_device_token` 문자열 부재: `! grep -q "p_device_token" <(sed -n '/castDateVoteAuthed/,/^}/p' packages/api/src/queries/date-polls.ts)`
    - api typecheck 0: `pnpm --filter @moajoa/api exec tsc --noEmit`
  </acceptance_criteria>
  <done>두 wrapper + hidePlace RPC 전환 그린 테스트·PublicBoardView.board.share_mode 노출·api typecheck 0.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] 스키마 적용 — 로컬 reset + typegen + 0029 스모크 (원격 push는 human-action)</name>
  <read_first>supabase/tests/web_share_smoke.sh (기존 anon signup/RPC 프로브 스타일) · 프로젝트 메모리(supabase는 colima 필요).</read_first>
  <files>packages/api/src/types/database.ts, supabase/tests/public_trip_poll_smoke.sh</files>
  <action>
**로컬 적용(autonomous — colima+docker 가동 확인 후):**
1. `supabase db reset` — 0016→0029 클린 적용, 42P17(recursion) 0건 확인.
2. `pnpm supabase:types` → `packages/api/src/types/database.ts` 재생성. 0029는 함수만 추가(스키마-중립)이나 함수 시그니처(public_trip_poll·cast_date_vote_authed·hide_place_as_member)가 타입에 반영되는지 확인 후 커밋.
3. `supabase/tests/public_trip_poll_smoke.sh` 신규 작성 — 기존 `web_share_smoke.sh` psql 프로브 스타일 미러: (a) 시드 trip(share_mode='dates') + date_poll 생성, (b) `select public_trip_poll('<slug>')`가 anon role로 poll_code·mode·options 반환(not null) 단언, (c) `select public_trip_view('<slug>')->>'share_mode'`가 'dates' 반환 단언, (d) **hide_place_as_member own-only 프로브**: 시드 place(added_by=other user) 후 `select hide_place_as_member('<other_place_id>')`를 게스트-에디터(비호스트) 세션에서 호출 시 예외(not authorized) 단언 + 자기 장소(added_by=self) hide는 성공 단언, (e) exit code 0.

**원격 적용(human-action — checkpoint):** 원격 DB 0029 적용은 사용자 몫. 두 경로: main push 시 Supabase↔GitHub 자동 적용(0028 선례) 또는 사용자 터미널 `supabase db push`. **라이브 dates/both 게스트 투표·D-12 own-only 소프트삭제는 이 적용 후에만 동작** — Plan 02/03/05의 라이브 검증 전제.
  </action>
  <verify>
    <automated>bash supabase/tests/public_trip_poll_smoke.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` 출력에 42P17 0건 (로컬)
    - `bash supabase/tests/public_trip_poll_smoke.sh` exit 0
    - `grep -q "public_trip_poll" packages/api/src/types/database.ts` (typegen 반영)
    - `grep -q "hide_place_as_member" packages/api/src/types/database.ts` (typegen 반영)
    - api 전 스위트 그린: `pnpm --filter @moajoa/api exec vitest run`
  </acceptance_criteria>
  <what-built>0029 로컬 적용 + database.ts 재생성 + public_trip_poll anon-read·hide_place_as_member own-only 스모크</what-built>
  <how-to-verify>
    1. colima+docker 가동 상태에서 `supabase db reset` 재실행 → 42P17 0 확인
    2. `bash supabase/tests/public_trip_poll_smoke.sh` → exit 0 (poll_code·share_mode 반환·own-only hide 강제)
    3. 원격 적용: 사용자 터미널 `supabase db push` 실행 (또는 main push 시 자동)
  </how-to-verify>
  <resume-signal>원격 push 완료 시 "pushed" 또는 "main 자동적용 예정"으로 응답</resume-signal>
  <done>로컬 0029 클린 적용·typegen 커밋·0029 스모크 exit 0·원격 push 게이트 사용자 확인.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 비로그인/익명 브라우저 → anon-grant RPC | slug만 아는 외부 게스트가 DEFINER RPC로 RLS 우회 열람 |
| 익명 세션 → cast_date_vote_authed | 인증된 익명 세션이 날짜투표 write |
| 익명 세션 → hide_place_as_member | 게스트가 장소 소프트삭제 write (own-only 경계) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | public_trip_poll(slug) | mitigate | DEFINER가 poll_code/mode/status/options만 반환 — device_token·voter PII 미노출(0018 poll_vote_tally idiom). visibility in ('public','shared') 게이트로 비공유 trip 차단 |
| T-25-02 | Tampering | 날짜투표 device_token spoof | mitigate | cast_date_vote_authed가 `auth.uid()::text` 서버파생 — 클라이언트가 device_token 위조 불가. anon 미grant(세션 필수) |
| T-25-03 | Elevation of Privilege | RLS 크로스테이블 재귀(42P17) | mitigate | 직접 EXISTS 0, DEFINER 헬퍼(can_edit_trip)만. `supabase db reset` 42P17=0 스모크로 실증(CLAUDE.md §4.4) |
| T-25-04 | Information Disclosure | public_trip_view share_mode 노출 | accept | share_mode는 공유 링크 소지자에게 이미 의도된 화면 구성 정보 — 민감정보 아님 |
| T-25-08 | Tampering | 게스트가 API 직타로 남의 장소 소프트삭제(hidden_at) | mitigate | `hide_place_as_member` DEFINER RPC가 소프트삭제 유일 경로 — 호스트(am_trip_owner)만 전체 hide, 게스트-에디터 포함 그 외는 added_by=auth.uid 자기 장소만, 아니면 raise. API가 raw `places UPDATE`(can_edit_trip, 남의 것도 가능)를 더는 쓰지 않음. D-12 DB-airtight(Pitfall 5 봉합, `hide_place_as_member(uuid) to authenticated`만 grant) |
</threat_model>

<verification>
- `supabase db reset` 42P17=0 · `pnpm --filter @moajoa/api exec vitest run` 그린 · api typecheck 0
- `bash supabase/tests/public_trip_poll_smoke.sh` exit 0 (anon poll read + own-only hide)
- 기존 마이그레이션 0016~0028 diff 0 · `.js` 워크스페이스 import 0 · iOS 무접촉
</verification>

<success_criteria>
public_trip_poll이 anon으로 slug→poll을 반환하고, public_trip_view가 share_mode를 노출하며, cast_date_vote_authed가 서버파생 device_token으로 저장하고, hide_place_as_member가 호스트 외(게스트-에디터 포함)를 own-only로 강제하며(D-12), 로컬 reset이 0029 포함 클린(42P17=0) 통과한다.
</success_criteria>

<output>
완료 후 `.planning/phases/25-guest-unified-share/25-01-SUMMARY.md` 생성.
</output>
</content>
</invoke>
