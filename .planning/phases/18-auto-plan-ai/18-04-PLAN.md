---
phase: 18-auto-plan-ai
plan: 04
type: tdd
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - packages/api/src/queries/plans.ts
  - packages/api/src/queries/plans.test.ts
  - packages/api/src/queries/index.ts
  - packages/api/package.json
autonomous: true
requirements: [PLAN-02, PLAN-05]
must_haves:
  truths:
    - "getPlanByTrip returns the trip's draft plan joined with its plan_items"
    - "generatePlan invokes the 'generate-plan' Edge Function with the GeneratePlanRequest body and returns GeneratePlanResult"
    - "reorderPlanItem updates a plan_item's day_index/sort_order (RLS via can_edit_trip — no extra client check)"
    - "setTravelMode updates plans.travel_mode; moveToPool/moveToDay add/remove a plan_item (placed↔unplaced, D-13)"
    - "setCollaborative flips plans.collaborative=true then reuses shareTrip to surface the share slug (D-14 — flag + share only, no new vote query)"
  artifacts:
    - path: "packages/api/src/queries/plans.ts"
      provides: "typed plan queries + generate-plan invoke"
      contains: "functions.invoke('generate-plan'"
    - path: "packages/api/src/queries/plans.test.ts"
      provides: "vitest coverage with a mocked Supabase client"
  key_links:
    - from: "packages/api/src/queries/index.ts"
      to: "./plans"
      via: "barrel re-export"
      pattern: "export \\* from './plans'"
    - from: "packages/api/src/queries/plans.ts"
      to: "shareTrip (trips.ts)"
      via: "setCollaborative reuses shareTrip"
      pattern: "shareTrip"
---

<objective>
Add the `@moajoa/api` typed query layer for plans: read the trip's draft plan + items, invoke the `generate-plan` EF, reorder/move items, toggle travel mode, and flip collaborative (reusing the existing `shareTrip`). These are the functions the iOS plan tab (Plan 05) calls. RLS (Plan 02) enforces access — the wrappers do no redundant client-side checks, exactly like `renamePlace`/`confirmAiPlace`.

Purpose: PLAN-02 (reorder, add/remove, "초안" persistence) + PLAN-05 (collaborative toggle = flag + share only). TDD because each query is a `expect(mockClient.from).toHaveBeenCalledWith(...)` / invoke-shape assertion writable before implementation, mirroring the core booking.test.ts idiom.

Output: `packages/api/src/queries/plans.ts` (+ test) + barrel re-export. Depends on Plan 01 (GeneratePlanRequest/Result types) and Plan 02 (regenerated database.ts with plans/plan_items types).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-auto-plan-ai/18-CONTEXT.md
@.planning/phases/18-auto-plan-ai/18-PATTERNS.md

<interfaces>
<!-- api query analogs. Extracted from codebase — mirror these exactly. -->

From packages/api/src/queries/places.ts (CRUD shape — every fn takes client first; .update().eq().select().single()):
```typescript
import type { Place, PlaceAddManual } from '@moajoa/core';
import type { MoajoaSupabaseClient } from '../client';
export async function listPlacesByTrip(client, tripId): Promise<Place[]> {
  const { data, error } = await client.from('places').select('*').eq('trip_id', tripId).is('hidden_at', null)...;
}
export async function renamePlace(client, id, newName): Promise<Place> {
  const { data, error } = await client.from('places').update({ name_ko: trimmed }).eq('id', id).select('*').single();
  if (error) throw error; return data as Place;
}
```

From packages/api/src/queries/links.ts L61-70 (EF invoke — mirror for generatePlan EXACTLY):
```typescript
export async function triggerExtraction(client, linkId): Promise<ExtractionResult> {
  const { data, error } = await client.functions.invoke('extract-youtube', { body: { link_id: linkId } });
  if (error) throw error;
  return data as ExtractionResult;
}
```

From packages/api/src/queries/trips.ts L130-153 (shareTrip — read-then-flip; REUSE in setCollaborative, do NOT reimplement):
```typescript
export async function shareTrip(client, tripId): Promise<string> {
  // returns existing share_slug or flips visibility='shared' to generate one
}
```

From packages/api/src/queries/index.ts (barrel — 5 lines, append ./plans).

From @moajoa/core (Plan 01): Plan, PlanItem, GeneratePlanRequest, GeneratePlanResult, TravelMode.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — plans.test.ts with a mocked Supabase client</name>
  <files>packages/api/src/queries/plans.test.ts</files>
  <read_first>
    - packages/core/src/booking.test.ts (vitest accept/reject + mock idiom)
    - packages/api/src/queries/places.ts (the CRUD shapes being asserted)
    - packages/api/src/queries/links.ts L61-70 (invoke shape)
    - packages/api/src/client.ts (MoajoaSupabaseClient type for the mock)
  </read_first>
  <behavior>
    Build a minimal chainable mock of `MoajoaSupabaseClient` (from/select/eq/is/order/update/insert/delete/single returning `{ data, error }`; functions.invoke returning `{ data, error }`). Assert:
    - Test 1: `getPlanByTrip(mock, TRIP)` calls `from('plans')`, `select` includes `plan_items`, `eq('trip_id', TRIP)`.
    - Test 2: `generatePlan(mock, { trip_id: TRIP, travel_mode: 'walk', anchor_place_ids: [], removed_place_ids: [] })` calls `functions.invoke('generate-plan', { body })` and returns the data as GeneratePlanResult.
    - Test 3: `reorderPlanItem(mock, ITEM, { day_index: 1, sort_order: 2 })` calls `from('plan_items').update({ day_index:1, sort_order:2 }).eq('id', ITEM)`.
    - Test 4: `setTravelMode(mock, PLAN, 'drive')` calls `from('plans').update({ travel_mode:'drive' }).eq('id', PLAN)`.
    - Test 5: `moveToPool(mock, ITEM)` deletes the plan_item (`from('plan_items').delete().eq('id', ITEM)`).
    - Test 6: `moveToDay(mock, { plan_id, place_id, day_index, sort_order })` inserts a plan_item with is_anchor false.
    - Test 7: `setCollaborative(mock, PLAN, TRIP)` updates `from('plans').update({ collaborative:true }).eq('id', PLAN)` AND calls shareTrip (assert the trips read-then-flip path is invoked / returns a slug). No votes table touched.
    - Test 8: each query throws when the mock returns `{ error }`.
  </behavior>
  <action>
    Create `packages/api/src/queries/plans.test.ts`. `import { describe, it, expect, vi } from 'vitest'`. Use uuid fixtures. If the api package has no existing test, mirror the core booking.test.ts structure and add `"test": "vitest run"` to `packages/api/package.json` ONLY if absent (check first; Phase 17 wired vitest for core — confirm api has a test script, add minimally if missing — surgical). Run `pnpm --filter @moajoa/api test` — MUST FAIL (plans.ts not implemented). Commit RED.
  </action>
  <verify>
    <automated>cd packages/api && pnpm vitest run src/queries/plans.test.ts 2>&1 | grep -E "(FAIL|Cannot find module|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/api/src/queries/plans.test.ts` exists with ≥8 `it(...)` cases.
    - `pnpm --filter @moajoa/api test` shows plans.test.ts FAILING (red) due to missing plans.ts exports.
    - Test 7 asserts NO votes query is made (collaborative = flag + share only, D-14).
  </acceptance_criteria>
  <done>plans.test.ts exists with ≥8 cases and FAILS red because plans.ts is not yet implemented; Test 7 asserts no votes query.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement plans.ts + barrel</name>
  <files>packages/api/src/queries/plans.ts, packages/api/src/queries/index.ts</files>
  <read_first>
    - packages/api/src/queries/places.ts (CRUD shapes)
    - packages/api/src/queries/links.ts L61-70 (invoke)
    - packages/api/src/queries/trips.ts L130-153 (shareTrip to reuse)
    - packages/api/src/queries/index.ts (barrel)
    - .planning/phases/18-auto-plan-ai/18-PATTERNS.md "packages/api/src/queries/plans.ts" section
  </read_first>
  <action>
    Create `packages/api/src/queries/plans.ts`:
    - `import type { Plan, PlanItem, GeneratePlanRequest, GeneratePlanResult, TravelModeType } from '@moajoa/core';` + `import type { MoajoaSupabaseClient } from '../client';` + `import { shareTrip } from './trips';`
    - `getPlanByTrip(client, tripId)` → `from('plans').select('*, plan_items(*)').eq('trip_id', tripId).eq('status','draft').maybeSingle()`; return typed plan+items (null if none).
    - `generatePlan(client, body: GeneratePlanRequest): Promise<GeneratePlanResult>` → mirror triggerExtraction: `functions.invoke('generate-plan', { body })`, throw on error, return `data as GeneratePlanResult`.
    - `reorderPlanItem(client, itemId, patch: { day_index: number; sort_order: number }): Promise<PlanItem>` → `from('plan_items').update(patch).eq('id', itemId).select('*').single()`.
    - `setTravelMode(client, planId, mode: TravelModeType): Promise<Plan>` → `from('plans').update({ travel_mode: mode }).eq('id', planId).select('*').single()`.
    - `moveToPool(client, itemId): Promise<void>` → `from('plan_items').delete().eq('id', itemId)` (D-13: removing a placed item returns it to the pool, since unplaced = no plan_item row).
    - `moveToDay(client, input: { plan_id, place_id, day_index, sort_order }): Promise<PlanItem>` → `from('plan_items').insert({ ...input, is_anchor:false, leg_travel_seconds:null }).select('*').single()`.
    - `setAnchor(client, itemId, isAnchor: boolean): Promise<PlanItem>` → update is_anchor (필수 표시; re-cluster happens on regenerate via generatePlan anchor_place_ids — D-10).
    - `setCollaborative(client, planId, tripId): Promise<{ collaborative: true; share_slug: string }>` → update `{ collaborative: true }` on the plan, then `const slug = await shareTrip(client, tripId);` return `{ collaborative: true, share_slug: slug }`. NO votes query (D-14).
    All wrappers: throw on `error`; no redundant client-side membership check (RLS via can_edit_trip — mirror renamePlace doc-comment).

    Append `export * from './plans';` to `packages/api/src/queries/index.ts`.
    Run `pnpm --filter @moajoa/api test` (GREEN) + `pnpm --filter @moajoa/api typecheck`. Commit GREEN.
  </action>
  <verify>
    <automated>cd packages/api && pnpm vitest run src/queries/plans.test.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @moajoa/api test` GREEN including all plans.test.ts cases.
    - `pnpm --filter @moajoa/api typecheck` exits 0 (against the Plan-02 regenerated database.ts types).
    - grep plans.ts matches `functions.invoke('generate-plan'`, `shareTrip`, `collaborative: true`, `travel_mode`.
    - grep `packages/api/src/queries/index.ts` matches `export \* from './plans'`.
    - plans.ts contains NO `from('votes')` (D-14 flag + share only).
  </acceptance_criteria>
  <done>plans.ts implemented + GREEN + api typecheck clean; generatePlan invoke mirrors triggerExtraction; setCollaborative reuses shareTrip; no votes coupling; barrel re-exports plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client app → @moajoa/api queries → Supabase | Queries run with the user's anon-key session; RLS (Plan 02) is the enforcement, not these wrappers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-15 | Tampering | reorder/move on another trip's plan | mitigate | RLS `can_edit_trip` on plans/plan_items (Plan 02) denies non-members; wrappers add no false sense of security and no bypass (V4). |
| T-18-16 | Elevation | client forging a collaborative flip | accept | `setCollaborative` only flips a boolean + reuses shareTrip; RLS gates the update; no privilege beyond what trip edit rights already grant. |
| T-18-17 | Info disclosure | over-broad select leaking other trips' items | mitigate | `getPlanByTrip` scoped `.eq('trip_id', ...)`; RLS additionally filters rows the user can't read. |
</threat_model>

<verification>
- `pnpm --filter @moajoa/api test` GREEN; `pnpm --filter @moajoa/api typecheck` exit 0.
- generatePlan invoke shape matches triggerExtraction; setCollaborative reuses shareTrip; no votes query.
- Barrel re-exports plans (importable by iOS Plan 05).
</verification>

<success_criteria>
- All plan queries implemented + tested with a mocked client.
- "초안" persistence (getPlanByTrip draft), reorder/move (PLAN-02), collaborative flag+share (PLAN-05) covered.
- RLS-only enforcement; no redundant checks; no votes coupling.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auto-plan-ai/18-04-SUMMARY.md`
</output>
