---
phase: 14
slug: extract-share-vote-flow
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-14
---

# Phase 14 — UI Design Contract

> Visual and interaction contract for the iOS 추출→공유→투표 자연 흐름 (홈 · 보드 상세/지도 · 공유/투표).
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> **Primary visual source:** `docs/design/2026-06-14-core3-ui-upgrade.md` (LOCKED) + mockups `docs/design/mockups/{home-variants,detail-and-vote}.html`.
> **Token source of truth:** `packages/ui-tokens/src/index.ts` (zero new tokens this phase).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn N/A — Expo / React Native target, not web React) |
| Preset | not applicable |
| Component library | NativeWind 4 + `@gorhom/bottom-sheet` (already in use) + `react-native-maps` + new `react-native-webview` |
| Icon library | inline glyphs / existing icon usage (mockups reference Tabler; iOS uses existing emoji + vector glyphs already in code — no new icon dep) |
| Font | Pretendard (`typography.fonts.sans`) — already loaded via expo-font |

**Design-system state:** Mature. `packages/ui-tokens` defines colors / spacing / radii / typography / shadows consumed by both apps. This phase reuses them via the NativeWind preset — **add zero tokens** (locked decision). The shadcn initialization gate does not apply: shadcn is a web-React/Tailwind generator and the Phase 14 surface is the Expo app (`apps/ios`). The web vote surface (`vote-island.tsx`) already exists and is reused as-is (CONTEXT 재사용), so no web component generation is in scope.

---

## Spacing Scale

Declared from `ui-tokens` `spacing` (rem→px @ 16px base). Phase 14 uses this subset:

| Token | Value | Usage |
|-------|-------|-------|
| `1` | 4px | Icon gaps, badge inner padding, avatar overlap nudge |
| `2` | 8px | Compact element spacing (chip gaps, row inner gap) |
| `3` | 12px | Card inner padding, vote-option card padding, list row padding |
| `4` | 16px | Default element spacing, screen content gap, card radius reference |
| `6` | 24px | Screen horizontal padding (`px-6` — matches existing `place-list.tsx`) |
| `8` | 32px | Section breaks (above vote list, header→content) |
| `12` | 48px | List bottom inset (`pb-12` — matches existing `place-list.tsx`) |

Exceptions:
- **Touch targets:** every tappable control (heart vote, share icon, category filter chip, place row) must present **≥ 44×44px hit area** (Apple HIG). Visual size may be smaller; pad the `Pressable` / use `hitSlop` to reach 44.
- **Avatar stack overlap:** `-6px` / `-5px` negative margin for participant avatar overlap (from mockup) — a deliberate visual overlap, not a spacing-scale value.
- **Bottom-sheet drag handle:** `36×4px` pill (mockup) — fixed control dimension, not on the scale.

---

## Typography

iOS sizes map to `ui-tokens.typography.sizes` (rem→px). Korean line-heights use `lineHeights.tight` (1.25) for headings, `lineHeights.normal` (1.5) for body — Korean readability rule already in tokens. **2 weights used** — regular 400 (body / prose / meta / caption / labels) and semibold 600 (place names, headings, and the home greeting display). Headings are distinguished from body by weight (600) at the shared 16px size, not by a separate size. No medium 500 and no bold 700.

| Role | Size | Weight | Line Height | Where |
|------|------|--------|-------------|-------|
| Display (home greeting) | 24px (`2xl`) | semibold (600) | 1.25 | `boards.tsx` 인사말 헤더 — 24px size alone carries display prominence |
| Heading / body / place name | 16px (`base`) | semibold (600) for headings + names, regular (400) for prose | 1.5 (1.25 when used as a heading) | "어디부터 갈까?", board-detail title, place card name, vote option name |
| Label / meta | 14px (`sm`) | regular (400) | 1.5 | "장소 N · 참여 M명", board subtitle, address |
| Caption / chip | 12px (`xs`) | regular (400) | 1.25 | category chips, D-카운트 chip, 출처 ▶칩, 표수, 메달 number |

Rules:
- Place names always `numberOfLines={1}` on cards, `numberOfLines={2}` allowed in sheets.
- Never exceed these 4 roles. No new font sizes.
- Only 2 weights: 400 and 600. Headings/names render at 600; everything else at 400.
- Korean copy never uppercased (no `text-transform`); rely on weight + size for hierarchy.

---

## Color

60/30/10 split mapped onto `ui-tokens.colors`. Brand blue is the accent and is **rationed** — it is not the default for every interactive element (the heart/category/medal palettes carry most of the meaning).

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `surface.raised` #FFFFFF / `surface.background` #F5F6F8 | White cards & sheets on a tinted #F5F6F8 scaffold |
| Secondary (30%) | `neutral.{100..300}` (#F3F4F6 / #E5E7EB / #D1D5DB) | Card borders, inactive filter chips, progress-bar track, dividers, neutral text |
| Accent (10%) | `brand.500` #2979FF / `brand.600` #2563EB | See reserved-for list below |
| Destructive | `semantic.danger` #EF4444 | Error toasts only — there is no in-screen destructive button this phase (delete is swipe-only and out of scope) |

**Accent (`brand`) reserved for:**
1. Primary CTA — "친구와 정하기" button and "초대 링크 복사" bar (`brand.600` fill, white text).
2. Active state of a cast ❤️ vote (filled heart + `brand.500`), and the loved-pin color on the map (`pin.loved` = `brand.500`).
3. Active category-filter chip background ("전체" / selected category) and the header share icon (`brand.500`).
4. In-progress voting bar fill for a leading-but-not-confirmed option (`brand.500`).

**Domain palettes (carry meaning — NOT accent, do not substitute brand blue):**

| Purpose | Token | Values |
|---|---|---|
| Category dot / pin color | `colors.category` | nature #43A047 · food #FF8F00 · culture #5C6BC0 · wellness #26A69A · shopping #8D6E63 |
| Pin / vote state | `colors.pin` | candidate #94A3B8 (gray, 무득표) · loved #2979FF · confirmed #16A34A · hidden #D1D5DB |
| Vote ranking medal | `colors.medal` | gold #FFD700 · silver #C0C0C0 · bronze #CD7F32 |
| 결정됨 / confirmed | `pin.confirmed` #16A34A + `semantic.success` #10B981 | 2px green border + "결정됨" chip (`#DCFCE7` bg / `#15803D` text) |
| 마감 임박 (D-카운트) | `semantic.warning` #F59E0B | warning chip (`#FEF3C7` bg / `#B45309` text) |

**Card radius:** `radii.xl` 16px for all place / vote-option / board cover cards. Bottom-sheet top corners `radii.3xl` 28px (existing). Chips `radii.full` (stadium). Buttons `radii.lg` 12px.
**Shadows:** flat — shadows only on floating elements (`shadows.fab` for any FAB, `shadows.nav` for bottom bar). Cards use 1px `neutral.200` border, no shadow.

---

## Copywriting Contract

All copy Korean, 토스 톤 (warm, plain, no exclamation spam). Reuses existing app strings where they already exist (toast copy from `vote-island.tsx`).

| Element | Copy |
|---------|------|
| Primary CTA (boards detail → share) | **친구와 정하기** |
| Secondary share (always-on, header) | share icon (no visible label) → native `Share.share` sheet (friends.tsx pattern). **`accessibilityLabel="공유하기"`** (icon-only control). |
| Invite-copy bar (vote screen) | **초대 링크 복사** (success toast: `링크를 복사했어요.`) |
| Vote screen heading | **어디부터 갈까?** |
| Vote screen subhead | `{N}명 참여 중 · {M}표 더 모이면 확정` (when no deadline) |
| Vote affordance hint | `{N}표 · 탭해서 투표` |
| Confirmed badge | **결정됨** (with check glyph) |
| Deadline chip | `D-{n}` (warning tone); on due day `D-DAY`; past `마감` |
| Empty state heading (board detail, no places yet) | **아직 장소가 없어요** |
| Empty state body | `유튜브·블로그 링크를 공유하면 영상 속 장소가 여기 모여요.` |
| Empty state (analysis running) | inline `분석 중…` on the board cover / detail header (reuses extraction-store) |
| Error — extraction failed (existing TRUST-03 path) | reuse existing toast + failed-row retry (`재시도`) — unchanged this phase |
| Error — share RPC failed | toast: `공유 링크를 만들지 못했어요. 다시 시도해 주세요.` |
| Error — vote save failed (web reuse) | `투표를 저장하지 못했어요.` (existing) |
| Error — join failed (web reuse) | `참여하지 못했어요.` (existing) |
| Error — video embed failed to load | inline in sheet: `영상을 불러오지 못했어요.` + `▶ 유튜브에서 보기` fallback link (opens `buildYouTubeWatchUrl`) |
| Loading — video embed | sheet shows a spinner over the player area until `onLoadEnd` |
| Loading — board detail map | map renders immediately; `fitToCoordinates` runs once places resolve (no full-screen spinner — map is the hero) |

**Icon-only control accessibility labels (Dimension 2):** every glyph-only button carries an `accessibilityLabel`.
- Detail-screen header share icon → `accessibilityLabel="공유하기"`.
- Vote-screen secondary share glyph button → `accessibilityLabel="메시지로 공유"`.

**Destructive actions:** none surfaced in-screen this phase. Board 수정/삭제 is swipe-revealed only (locked: "파괴적 액션 전면 배치 제거") and the swipe-delete UI itself is **out of scope** for Phase 14 (CONTEXT 비범위). No destructive confirmation dialog is added here.

---

## Screen Contracts

### Screen 1 — 내 여행 (홈) · `apps/ios/app/(tabs)/boards.tsx`
- Keep existing greeting header + date-chip tone. Greeting renders at 24px/600 semibold (display role).
- Board = **cover card** (`radii.xl`, white, 1px `neutral.200` border): title (16/600) + place-name chips (top 3 + `+N`) + `장소 N · 참여 M명` meta badge (14/400).
- **Hybrid cover:** when a representative place photo exists → hero photo with a translucent bottom bar overlaying title/meta; otherwise the place-name-chip layout is the default identifier. Never a mini-map thumbnail (locked rationale: maps look alike).
- 수정/삭제 never shown on the card face → swipe-revealed (UI not built this phase).
- Analyzing board shows inline `분석 중…`.

### Screen 2 — 보드 상세 / 지도 · `apps/ios/app/boards/[id].tsx`
- **Map is the hero** (full-bleed) + draggable bottom sheet (`@gorhom/bottom-sheet`, 28px top radius, 36×4px handle).
- Map: `fitToCoordinates` to all places on load (replaces the hard-coded 도쿄 `initialRegion`).
- Pins: color = `colors.category` (by place category); state = `colors.pin` (candidate gray / loved blue / confirmed green); top-3 by votes carry a `colors.medal` numbered badge.
- Header: back chevron · board title (16/600) · participant avatar stack (overlapping circles) · share icon (`brand.500`, icon-only → `accessibilityLabel="공유하기"`).
- Sheet top: category filter chips (active = `brand.500` fill / inactive = `neutral.100`). Each place card: medal rank (if top 3) + category dot + name (16/600) + meta (14/400 `neutral.400`) + ❤️ count.
- Tapping a place card opens the existing `PinBottomSheet`, extended with the embedded video player (Screen 4 detail).
- Primary CTA "친구와 정하기" lives here (entry into share/vote flow).

### Screen 3 — 공유 / 투표 (vote screen)
- Heading "어디부터 갈까?" (16/600) + subhead participation/deadline line (14/400).
- Each option = card (`radii.xl`): name (16/600) + ❤️ (one-tap toggle) + live horizontal bar + participant avatar stack + 표수 (12/400).
- Leading option bar = `brand.500`; **majority → confirmed**: 2px `pin.confirmed` border + "결정됨" green chip + green bar.
- Trailing options bar = `neutral.300` track with `brand.500`/`neutral.300` fill by share.
- Deadline chip top-right (`semantic.warning`).
- Bottom bar: **초대 링크 복사** (`brand.600` fill, white) + a secondary share glyph button (`neutral.100`, icon-only → `accessibilityLabel="메시지로 공유"`).
- **Reuse:** the web `vote-island.tsx` already implements ❤️ cast/retract + counts + join. iOS does not re-implement vote tallying; it links into the web vote surface via `shareBoard()` slug (`Constants.expoConfig.extra.webUrl` + `/b/{slug}`). iOS in-app vote-result/confirmed viewing is **out of scope** (CONTEXT 비범위).

### Screen 4 — 임베드 영상 플레이어 (in `PinBottomSheet`)
- In-app `react-native-webview` player, seeked to `source_timestamp_sec` (CONTEXT lock). 16:9 area, `radii.lg` clipped, spinner until loaded, inline error + 유튜브 폴백 link on failure.
- Source ▶칩 on flat place cards uses `▶ {m:ss}` when youtube timestamp exists, else `▶ 영상` (matches existing `place-list.tsx`).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (no shadcn — Expo target) |
| third-party | none | not applicable |

No component registries are used. New runtime dependency `react-native-webview` is an Expo-ecosystem native module (config-plugin install per CONTEXT), not a UI block registry — outside the registry-vetting scope. No registry vetting gate required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
