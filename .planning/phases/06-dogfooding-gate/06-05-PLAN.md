---
phase: 06-dogfooding-gate
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - .planning/dogfooding/pass-evaluator.md
  - .planning/dogfooding/extraction-baseline-TEMPLATE.md
  - .planning/dogfooding/PASS-TEMPLATE.md
  - .planning/research/PITFALLS.md
autonomous: true
requirements: [EXTRACT-07]

must_haves:
  truths:
    - "Phase 6 Pass / Fail 평가가 D-20/D-21 lock 그대로 단일 문서에서 가능 (6 pass criteria + 4 fail conditions 표 형식)"
    - "Baseline 측정 결과를 작성할 표준 markdown 양식이 D-09 구조 그대로 준비되어 있다"
    - "Pass 시 PASS.md sign-off 양식이 D-22 그대로 준비되어 있다 (날짜, 수치, 친구 피드백, P0 incidents)"
    - "신규 pitfall 추가 채널이 PITFALLS.md에 정해진 섹션으로 명시되어 있다 (D-19, Success Criterion 5)"
  artifacts:
    - path: ".planning/dogfooding/pass-evaluator.md"
      provides: "D-20/D-21 평가 체크리스트 + 각 criterion의 측정 데이터 source 명시 (어느 파일/SQL에서 가져오는지)"
      contains: "recall"
      contains_2: "precision"
      contains_3: "P0"
      min_lines: 60
    - path: ".planning/dogfooding/extraction-baseline-TEMPLATE.md"
      provides: "D-09 5-part 구조 그대로 (메타 / 영상별 표 / 집계 / Top 5 failure modes / v2 시드)"
      contains: "Top 5"
      min_lines: 50
    - path: ".planning/dogfooding/PASS-TEMPLATE.md"
      provides: "D-22 sign-off 양식 (날짜, baseline 수치, 7일 핀 수, 친구 피드백, P0 건수)"
      contains: "sign-off"
      min_lines: 20
    - path: ".planning/research/PITFALLS.md"
      provides: "v6 dogfooding 신규 pitfall 섹션 anchor (PITFALLS.md 자체는 기존 — Phase 6 섹션 헤더 추가)"
      contains: "Phase 6"
  key_links:
    - from: ".planning/dogfooding/pass-evaluator.md"
      to: ".planning/dogfooding/pre-dogfooding-checklist.md (Plan 06-01)"
      via: "Criterion 1 — 모든 체크박스 ☑️ 확인"
    - from: ".planning/dogfooding/pass-evaluator.md"
      to: ".planning/dogfooding/extraction-baseline-YYYY-MM-DD.md (Plan 06-02 + 본 plan template)"
      via: "Criterion 2 — overall recall ≥ 0.70 AND precision ≥ 0.75"
    - from: ".planning/dogfooding/pass-evaluator.md"
      to: ".planning/dogfooding/daily-log-template.md (Plan 06-03)"
      via: "Criterion 3 — 7일 commit + ≥ 10 핀"
    - from: ".planning/dogfooding/pass-evaluator.md"
      to: ".planning/dogfooding/friend-share-checklist.md (Plan 06-04)"
      via: "Criterion 4 — 친구 2명 5/5 ☑️"
    - from: ".planning/dogfooding/pass-evaluator.md"
      to: ".planning/dogfooding/incidents.md (Plan 06-03)"
      via: "Criterion 5 — P0 ≤ 1 AND P1 모두 v2 백로그"
    - from: ".planning/dogfooding/pass-evaluator.md"
      to: ".planning/research/PITFALLS.md"
      via: "Criterion 6 — 신규 pitfall ≥ 1 append"
---

<objective>
Phase 6 종료 시점에 본인이 Pass / Fail 판정과 sign-off을 단일 문서에서 결정 가능하게 evaluator + 결과 template + PITFALLS 추가 anchor를 준비한다. D-20/D-21/D-22 lock.

Purpose: 7일 dogfooding이 끝났을 때 "이게 통과인가?" 판단을 ad-hoc하지 않게 — 6개 criteria 표 + 4개 fail 조건 lock + sign-off 양식이 미리 있어야 dogfooding 마지막 날 1시간 안에 close.
Output: pass-evaluator.md (체크리스트) + baseline-TEMPLATE.md (D-09 양식) + PASS-TEMPLATE.md (D-22 sign-off) + PITFALLS.md Phase 6 섹션 anchor — Phase 6 완료의 최종 산출물.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-dogfooding-gate/06-CONTEXT.md

# Plans 06-01 ~ 06-04 산출물이 본 plan의 입력
# 본 plan은 dogfooding 시점에 본인이 채워서 close하는 최종 evaluator + sign-off 양식 작성까지
</context>

<tasks>

<task type="auto">
  <name>Task 1: pass-evaluator.md + extraction-baseline-TEMPLATE.md (평가 체크리스트 + baseline 양식)</name>
  <files>.planning/dogfooding/pass-evaluator.md, .planning/dogfooding/extraction-baseline-TEMPLATE.md</files>
  <action>
2개 파일 생성. dogfooding 마지막 날 본인이 모든 입력 (06-01 ~ 06-04 산출물 + 7일 daily log + baseline 측정 결과)을 한 곳에서 종합 평가.

### 파일 1: `.planning/dogfooding/pass-evaluator.md`

D-20/D-21 그대로 + 각 criterion의 데이터 source 명시 + Fail 분기 시 다음 phase 추천.

구조:

```markdown
# Phase 6 Pass / Fail Evaluator

**Lock:** D-20 (Pass 조건 6개 모두 충족) + D-21 (Fail 조건 4개 중 하나만 충족 시 fail)
**Run when:** dogfooding Day 7 종료 후 (마지막 daily-log commit 직후)
**Output:** Pass → `.planning/dogfooding/PASS.md` 작성 → Phase 1.5 진입 가능 / Fail → 어떤 보강 phase 필요한지 본 문서가 결정

---

## Pass Criteria (D-20)

| # | Criterion | Threshold | Data source | Status |
|---|-----------|-----------|-------------|--------|
| 1 | Pre-dogfooding 체크리스트 100% 통과 | 모든 ☑️ | `.planning/dogfooding/pre-dogfooding-checklist.md` (Plan 06-01) | ⬜ |
| 2a | Baseline overall recall | ≥ 0.70 | `.planning/dogfooding/extraction-baseline-YYYY-MM-DD.md` §"집계" overall recall (Plan 06-02 ground truth + Plan 06-03 measure-accuracy.sql) | ⬜ |
| 2b | Baseline overall precision | ≥ 0.75 | 동일 파일 overall precision | ⬜ |
| 3a | 7일 daily-log commit | ≥ 7 (`git log --grep "dogfooding day" --oneline \| wc -l`) | `.planning/dogfooding/daily-log-YYYY-MM-DD.md` (Plan 06-03 양식) | ⬜ |
| 3b | 핀 누적 ≥ 10 (D-13) | Day 7 places_cumulative ≥ 10 | `scripts/dogfooding/daily-aggregate.sql` 출력 | ⬜ |
| 4a | Friend A 5/5 ☑️ | 모든 체크박스 | `.planning/dogfooding/friend-share-checklist.md` Friend A 블록 (Plan 06-04) | ⬜ |
| 4b | Friend B 5/5 ☑️ | 모든 체크박스 | 동일 파일 Friend B 블록 | ⬜ |
| 5a | P0 incidents ≤ 1 (해당 일 fix 완료) | Count | `.planning/dogfooding/incidents.md` (Plan 06-03 양식) "End-of-Week Tally" P0 | ⬜ |
| 5b | P1 모두 v2 backlog 등록 | 100% mapped | 동일 파일 P1 행의 "v2-backlog ref" 칸 모두 채워짐 | ⬜ |
| 6 | 신규 pitfall ≥ 1 PITFALLS.md append | Count | `.planning/research/PITFALLS.md` Phase 6 섹션 (본 plan Task 2 anchor) | ⬜ |

**모든 ⬜ → ☑️로 close되어야 Pass.**

---

## Fail Conditions (D-21)

다음 중 **하나라도 발생하면 Fail**:

| Condition | Trigger | Next Phase (자동 추천) |
|-----------|---------|------------------------|
| recall < 0.70 OR precision < 0.75 | Criterion 2a/2b 미달 | **Phase 6.1: 추출 보강** — failure label top 1~2 카테고리에 집중. v2 EXTRACT-08 (자동 eval framework) 시드는 보존 |
| 7일 중 P0 ≥ 2 | Criterion 5a > 1 | **Phase 6.2: 안정성 보강** — P0 incidents 원인 분석 + hotfix를 GSD plan으로 정형화 |
| 친구 1명이라도 OG/모바일 실패 | Criterion 4a 또는 4b 5/5 X | **Phase 4 잔여작업 재진입** — VIEW-* 중 fail한 항목 (e.g., OG kakao 캐시 vs 실제 buggy?) revisit |
| 7일 미달성 (중도 포기) | Criterion 3a < 7 commit | **STATE 재점검** — 동기 부족 또는 core value 미작동. roadmap reassess |

Fail 시:
1. 본 문서 "Conclusion" 섹션에 어느 fail condition 발동인지 기록
2. 추천된 next phase가 ROADMAP.md에 Phase 6.x로 추가
3. `.planning/dogfooding/PASS.md`는 작성 X — 대신 `.planning/dogfooding/FAIL-YYYY-MM-DD.md`로 dogfooding 종료 상태 stamp

---

## Decision Tree (visual)

```
Run pass-evaluator.md
   │
   ├─ All 11 Pass criteria ☑️?
   │       ├─ YES → write PASS.md (D-22) → Phase 1.5 진입 가능
   │       └─ NO → check Fail conditions
   │
   └─ Any Fail condition?
           ├─ YES → write FAIL-YYYY-MM-DD.md + recommend next phase
           └─ NO (criteria 미달이지만 fail trigger도 아님) → 모호 → 수동 판단 + 본인 sign-off로 reclassify (대부분 P1 incident가 P0 borderline인 case)
```

---

## Conclusion (dogfooding 종료 시 본인이 채움)

- **Date:** YYYY-MM-DD
- **Verdict:** Pass / Fail (선택)
- **Pass criteria summary:** N / 11 ☑️
- **Failed criteria (if any):** list
- **Next action:**
  - If Pass: `.planning/dogfooding/PASS.md` 작성 + ROADMAP.md Phase 1.5 unlocked 표시
  - If Fail: recommend phase + FAIL-YYYY-MM-DD.md 작성

본 문서 작성 + 결과 commit (`docs(06): pass-evaluator verdict`) → Phase 6 close.
```

### 파일 2: `.planning/dogfooding/extraction-baseline-TEMPLATE.md`

D-09 5-part 구조 그대로:

```markdown
# Extraction Baseline — YYYY-MM-DD

**Lock:** D-09 (5-part 구조) + D-06 (precision/recall) + D-07 (6 failure labels)
**Sample size:** 12 videos (D-03)
**Authoring:** 수동 (D-08) — Plan 06-03 measure-accuracy.sql + ground-truth/<sample-id>.json (Plan 06-02) 비교

---

## Part 1: Meta + Sampling decisions

- **Date measured:** YYYY-MM-DD (within dogfooding Day 1~7 or 직후)
- **Samples used:** 12 (`.planning/dogfooding/samples.json` v <hash> as of <date>)
- **Sampling matrix (D-04):** 3 categories × 2 cities × 2 lengths × 2 transcript sources × langs
- **Matching method (D-06):** primary google_place_id, fallback normalized name+city
- **Failure label vocabulary (D-07):** hallucination / wrong_place / wrong_city / missing_lowconf / missing_dropped / transcript_fail

---

## Part 2: Per-video results

| sample_id | url | category | city | gt_count | extracted_count | matched_count | recall | precision | failure_labels |
|-----------|-----|----------|------|----------|-----------------|---------------|--------|-----------|----------------|
| sample-01 | https://youtu.be/XXXX | vlog | seoul | 8 | 7 | 6 | 0.75 | 0.86 | hallucination:1, missing_dropped:2 |
| sample-02 | ... | | | | | | | | |
| ... (12 rows) | | | | | | | | | |

`recall = matched_count / gt_count`
`precision = matched_count / extracted_count`
`failure_labels` = comma-separated `<label>:<count>` for the row's mismatches

---

## Part 3: Aggregate

### Overall (PASS criteria — D-20 #2)

| Metric | Value | Threshold | Status |
|--------|-------|-----------|--------|
| Overall recall | `SUM(matched_count) / SUM(gt_count)` = ? | ≥ 0.70 | ⬜ |
| Overall precision | `SUM(matched_count) / SUM(extracted_count)` = ? | ≥ 0.75 | ⬜ |
| Overall F1 | `2*r*p/(r+p)` = ? | (bonus) | — |

### By category

| Category | n | recall | precision | dominant failure |
|----------|---|--------|-----------|------------------|
| vlog | 4 | | | |
| food | 4 | | | |
| walk | 4 | | | |

### By city

| City | n | recall | precision | dominant failure |
|------|---|--------|-----------|------------------|
| seoul | 6 | | | |
| tokyo | 6 | | | |

### By transcript source

| Source | n | recall | precision | dominant failure |
|--------|---|--------|-----------|------------------|
| auto | 6 | | | |
| creator | 6 | | | |

---

## Part 4: Top 5 Failure Modes

각 항목 = label + count + 1~2 representative example (sample_id reference + 추출 결과의 wrong/missing place 인용).

1. **<label-1>** (N건) — 예시: sample-XX의 "<place_name>" 케이스. <왜 발생했는지 한 줄>
2. **<label-2>** (N건) — 예시: ...
3. **<label-3>** (N건) — 예시: ...
4. **<label-4>** (N건) — 예시: ...
5. **<label-5>** (N건) — 예시: ...

---

## Part 5: v2 EXTRACT-08 Seeds

본 baseline에서 도출된 v2 evaluation framework 시드:

- [ ] 본 12 samples.json을 v2 EXTRACT-08의 regression test set의 초기 세트로 보존
- [ ] failure label top 1~2를 v2 EXTRACT-09 prompt tuning의 우선 ablation target
- [ ] (예: wrong_city가 top이면) v2에서 `inferred_city` 기반 region bias 도입 우선
- [ ] (예: missing_dropped가 top이면) Phase 2 D-04 citation 강제의 false-negative 분석 우선
- [ ] 자동 매칭 스크립트 (D-08 deferred) 우선순위 1순위

본 문서가 closed되면 `.planning/dogfooding/pass-evaluator.md` Criterion 2 ☑️ 가능.
```
  </action>
  <verify>
    <automated>test -f .planning/dogfooding/pass-evaluator.md && grep -q "recall" .planning/dogfooding/pass-evaluator.md && grep -q "0.70" .planning/dogfooding/pass-evaluator.md && grep -q "0.75" .planning/dogfooding/pass-evaluator.md && grep -q "Friend A" .planning/dogfooding/pass-evaluator.md && grep -q "Fail Conditions" .planning/dogfooding/pass-evaluator.md && test -f .planning/dogfooding/extraction-baseline-TEMPLATE.md && grep -q "Top 5 Failure Modes" .planning/dogfooding/extraction-baseline-TEMPLATE.md && grep -q "hallucination" .planning/dogfooding/extraction-baseline-TEMPLATE.md && grep -q "Part 5" .planning/dogfooding/extraction-baseline-TEMPLATE.md</automated>
  </verify>
  <done>pass-evaluator.md contains 6 pass criteria + 4 fail conditions; extraction-baseline-TEMPLATE.md has all 5 parts (D-09) including hallucination label and v2 seeds section.</done>
</task>

<task type="auto">
  <name>Task 2: PASS-TEMPLATE.md + PITFALLS.md Phase 6 섹션 anchor</name>
  <files>.planning/dogfooding/PASS-TEMPLATE.md, .planning/research/PITFALLS.md</files>
  <action>
2개 파일 처리:

### 파일 1 (생성): `.planning/dogfooding/PASS-TEMPLATE.md`

D-22 그대로. dogfooding Pass 시 본인이 이 template를 copy하여 `.planning/dogfooding/PASS.md`로 저장.

구조:

```markdown
# Phase 6 Dogfooding Gate — PASS

**Sign-off date:** YYYY-MM-DD
**Dogfooder:** <본인 이니셜>
**Verdict:** PASS (per `.planning/dogfooding/pass-evaluator.md`)

---

## Sign-off (D-22)

| Field | Value |
|-------|-------|
| Dogfooding 기간 | YYYY-MM-DD ~ YYYY-MM-DD (7일) |
| Baseline recall | 0.XX (≥ 0.70 ✓) |
| Baseline precision | 0.XX (≥ 0.75 ✓) |
| Baseline F1 | 0.XX (참고) |
| Day 7 핀 누적 | NN개 (≥ 10 ✓, hidden_at IS NULL) |
| P0 incidents | 0 또는 1 (fix complete ✓) |
| P1 v2 backlog 등록 | N건 → v2-backlog.md / GitHub issues |
| 신규 pitfalls PITFALLS.md append | N건 |
| Friend A device + verdict | iOS 18.x ko-KR / 5-5 ✓ |
| Friend B device + verdict | Android 14 ko-KR / 5-5 ✓ |
| Friend A 한 줄 피드백 | "..." |
| Friend B 한 줄 피드백 | "..." |

---

## Phase 1.5 Unlock

- [ ] ROADMAP.md에 Phase 1.5 (협업·투표) 항목 status를 "Not started → Unlocked"로 update
- [ ] STATE.md "Current Position" 섹션에 Phase 6 complete + 1.5 ready 기록
- [ ] PROJECT.md §"Dogfooding Gate" status를 ✓로 update
- [ ] `/gsd-extract-learnings`로 Phase 6 패턴/결정/실수 추출

---

## Artifacts Index (Phase 6 종합)

- Pre-dogfooding checklist: `.planning/dogfooding/pre-dogfooding-checklist.md`
- Sample matrix: `.planning/dogfooding/sample-videos.md` + `samples.json`
- Ground truth: `.planning/dogfooding/ground-truth/sample-*.json` (12 files)
- Daily log: `.planning/dogfooding/daily-log-YYYY-MM-DD.md` + 7 commits
- Baseline: `.planning/dogfooding/extraction-baseline-YYYY-MM-DD.md`
- Friend share: `.planning/dogfooding/friend-share-checklist.md` + `screenshots/friend-{A,B}/`
- Incidents: `.planning/dogfooding/incidents.md`
- Evaluator: `.planning/dogfooding/pass-evaluator.md`
- New pitfalls: `.planning/research/PITFALLS.md` §Phase 6
```

### 파일 2 (수정): `.planning/research/PITFALLS.md`

먼저 파일 존재 여부 확인. 존재하면 Phase 6 섹션 헤더 append (file end), 없으면 minimal file 생성:

**Step A:** `ls .planning/research/PITFALLS.md` 확인. 없으면 다음 minimal content로 생성:

```markdown
# MOAJOA Pitfalls Log

이 파일은 phase 진행 중 발견된 신규 pitfall들의 운영 archive. v2 백로그 시드.

각 phase 종료 시 해당 phase 섹션에 append.

---

## Phase 6 — Dogfooding Gate

(Dogfooding 중 발견된 신규 pitfall append — D-19, Success Criterion 5)

- _Empty until dogfooding starts_
```

**Step B:** 존재하면 (Phase 1~5에서 이미 만들어진 경우): Read한 후 file end에 다음 anchor section만 append (기존 섹션 수정 X):

```markdown
---

## Phase 6 — Dogfooding Gate

(Dogfooding 중 발견된 신규 pitfall append — D-19, Success Criterion 5)

- _Empty until dogfooding starts_
```

이미 "## Phase 6" 섹션이 있다면 skip (idempotent).
  </action>
  <verify>
    <automated>test -f .planning/dogfooding/PASS-TEMPLATE.md && grep -q "Sign-off" .planning/dogfooding/PASS-TEMPLATE.md && grep -q "D-22" .planning/dogfooding/PASS-TEMPLATE.md && grep -q "Friend A" .planning/dogfooding/PASS-TEMPLATE.md && grep -q "Phase 1.5" .planning/dogfooding/PASS-TEMPLATE.md && test -f .planning/research/PITFALLS.md && grep -q "Phase 6" .planning/research/PITFALLS.md</automated>
  </verify>
  <done>PASS-TEMPLATE.md contains D-22 sign-off fields + Phase 1.5 unlock checklist; PITFALLS.md has Phase 6 anchor section (idempotent — created or appended).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pass-evaluator.md → PASS.md 작성 | self-attestation. 본인이 모든 criterion ☑️ 검증 책임 |
| baseline-TEMPLATE.md ground truth 비교 | 사람 manual diff (D-08). bias 가능성 — high/medium/low confidence_label로 보조 weighting (Plan 06-02) |
| PITFALLS.md append → 공개 surface | dogfooding 중 발견 P1을 일반화하여 기록. personal info redact는 본인 책임 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | T | Self-attestation으로 Pass criteria 우회 | accept | dogfooding 자체가 self-driven. external review는 v2 closed beta 단계 |
| T-06-05-02 | R | PASS.md 작성 시점에 PSQL 결과 misread (recall/precision 잘못 계산) | mitigate | extraction-baseline-TEMPLATE.md "Overall" 섹션에 계산식 inline (`SUM(matched)/SUM(gt)`) — 본인이 손계산 가능 |
| T-06-05-03 | I | PITFALLS.md에 친구 device meta 식별 정보 leak | mitigate | Phase 6 섹션에 pitfall만 일반화 — friend-A/B 라벨 X, 일반 description "일부 Android 사용자에서 ..." |
| T-06-05-04 | E | Fail 시 fail-recovery phase 추천이 잘못 (e.g., recall fail인데 안정성 보강 추천) | mitigate | Fail Conditions 표가 condition → next phase 1-1 mapping. 본인이 표 따라 판단 |
</threat_model>

<verification>
- `.planning/dogfooding/pass-evaluator.md` 11 criteria + 4 fail conditions + decision tree + conclusion 슬롯
- `.planning/dogfooding/extraction-baseline-TEMPLATE.md` D-09 5-part (메타 / 영상별 / 집계 / Top 5 / v2 시드)
- `.planning/dogfooding/PASS-TEMPLATE.md` D-22 sign-off 필드 + Phase 1.5 unlock 체크
- `.planning/research/PITFALLS.md` Phase 6 섹션 헤더 존재 (idempotent)
</verification>

<success_criteria>
- dogfooding 마지막 날 본인이 1시간 안에 pass-evaluator.md 모든 ☑️ 또는 fail 분기 결정 가능
- baseline 측정 결과를 5-part 양식 그대로 채울 수 있음
- Pass 시 PASS.md sign-off로 Phase 1.5 진입 신호 명확
- 신규 pitfall이 PITFALLS.md Phase 6 섹션에 자연스럽게 추가 가능 (anchor 존재)
</success_criteria>

<output>
After completion, create `.planning/phases/06-dogfooding-gate/06-05-SUMMARY.md` per template, including:
- 4 artifacts (pass-evaluator + baseline-TEMPLATE + PASS-TEMPLATE + PITFALLS.md anchor)
- D-20/D-21/D-22 lock 그대로 반영 확인
- Phase 6 전체 plan 흐름 (06-01 → 06-04 산출물 → dogfooding 실행 → 06-05로 종합) inline 명시
- Phase 6 종료 시점에 ROADMAP.md/STATE.md/PROJECT.md update 필요 항목 list (본 plan은 template까지 — 실제 update는 dogfooding 종료 시 본인)
</output>
