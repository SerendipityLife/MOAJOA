---
phase: 08-extraction-depth
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - packages/api/src/types/database.ts
autonomous: false
requirements: [EXTRACT-12, EXTRACT-13, EXTRACT-14, VIEW-08]
must_haves:
  truths:
    - "Migration 0008 is applied to the linked Supabase project (places.summary_ko + links.summary_ko columns exist live)"
    - "public_board_view RPC live-returns summary_ko in both jsonb objects"
    - "packages/api/src/types/database.ts regenerated and includes summary_ko on places + links"
    - "A live extraction of a transcript-rich travel video yields non-empty places.summary_ko + links.summary_ko with no hallucination (spot-check)"
    - "The public board page shows the commentary in a real browser; a legacy board (null summary_ko) still renders without layout break"
  artifacts:
    - path: "packages/api/src/types/database.ts"
      provides: "Regenerated DB types carrying summary_ko (NOT hand-edited)"
      contains: "summary_ko"
  key_links:
    - from: "supabase db push (0008 applied)"
      to: "live public_board_view payload → web render"
      via: "applied column + regenerated types + live RPC"
      pattern: "summary_ko"
---

<objective>
[BLOCKING — autonomous: false — MORNING / USER-SIDE GATE]

Apply migration 0008 to the linked Supabase project, regenerate the typed DB schema, and live
spot-check the full chain end-to-end. This is the ONLY plan that requires live Supabase + an
Anthropic-keyed extraction + a real browser — it is deliberately last so all autonomous code/unit
work (08-01/02/03) lands and passes WITHOUT the migration applied.

Why this can wait until morning without blocking the autonomous work: the monorepo build/typecheck/
tests pass WITHOUT the push because the web reads through the hand-maintained `PublicBoardView` Pick
type (08-01), not the generated `database.ts`. The unapplied migration does not gate the unit work.

Purpose: prove Success Criteria 1-4 of ROADMAP Phase 8 against the live system, and regenerate
database.ts so future code sees summary_ko.
Output: applied 0008 + regenerated database.ts + a recorded spot-check result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extraction-depth/08-CONTEXT.md
@.planning/phases/08-extraction-depth/08-01-SUMMARY.md
@.planning/phases/08-extraction-depth/08-02-SUMMARY.md
@.planning/phases/08-extraction-depth/08-03-SUMMARY.md

<known_hazard>
From STATE.md Decisions (Phase 5 05-01): `pnpm supabase:types` shell-redirect
(`supabase gen typescript --local > packages/api/src/types/database.ts`) CREATES the empty target
file BEFORE the command runs — if `gen` fails (Docker down, not linked), only the empty file remains
and the 1500+ line database.ts is wiped. RECOVERY: `git checkout HEAD -- packages/api/src/types/database.ts`.
Verify the regenerated file is non-trivial (line count >> 100) before committing.
</known_hazard>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Apply migration 0008 (supabase db push)</name>
  <what-built>Migration supabase/migrations/0008_extraction_summaries.sql (08-01): two nullable
  columns + public_board_view RPC re-issue with summary_ko.</what-built>
  <how-to-verify>
    Prereq: linked project + `SUPABASE_ACCESS_TOKEN` (the Supabase CLI is NOT authenticated in the
    overnight session). From repo root:
    1. `supabase db push` — applies 0008 to the linked project.
    2. Confirm columns live:
       `supabase db query "select column_name from information_schema.columns where table_name in ('places','links') and column_name='summary_ko';"`
       → expect TWO rows (places.summary_ko, links.summary_ko).
    3. Confirm the RPC emits the field — pick any public board slug and run:
       `supabase db query "select (public_board_view('<known-public-slug>') -> 'places' -> 0) ? 'summary_ko';"`
       → expect `t` (the key is present, even if value is null for legacy rows).
  </how-to-verify>
  <resume-signal>Type "applied" once `db push` succeeds and both columns + the RPC key are confirmed, or paste the error.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Regenerate database.ts (pnpm supabase:types)</name>
  <what-built>Generated DB types consumed by @moajoa/api typed queries.</what-built>
  <how-to-verify>
    1. `pnpm supabase:types` (regenerates packages/api/src/types/database.ts).
    2. GUARD against the known wipe hazard: `wc -l packages/api/src/types/database.ts` should be
       MANY hundreds of lines (the prior file was ~1545). If it is near-empty → the gen failed;
       run `git checkout HEAD -- packages/api/src/types/database.ts` and re-run after fixing the
       linked/Docker prerequisite. Do NOT commit an empty file.
    3. Confirm the new field landed:
       `grep -c "summary_ko" packages/api/src/types/database.ts` → expect ≥ 2 (places row + links row, Row/Insert/Update may add more).
    4. `pnpm -w typecheck` (or `cd packages/api && pnpm typecheck`) exits 0.
  </how-to-verify>
  <resume-signal>Type "regenerated" once database.ts is non-empty, contains summary_ko, and typecheck passes; or paste the error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live extraction spot-check + browser render (Success Criteria 1-4)</name>
  <what-built>The full widened pipeline (08-02) + web render (08-03), now against the applied schema.</what-built>
  <how-to-verify>
    A. Extraction (Success Criteria 1-3, EXTRACT-12/13/14):
    1. Through the normal save flow (or a direct extract-youtube invoke with a valid link_id),
       extract ONE transcript-rich Japanese or Korean travel video.
    2. Query the result:
       `supabase db query "select name_local, summary_ko from places where link_id='<link_id>';"`
       → expect each place to have a NON-empty 1~2 sentence KOREAN summary_ko (Korean even if the
       video is JP/EN). Spot-check 2-3 summaries against the actual transcript — they must NOT
       invent facts not present in the subtitles/description (Success Criterion 2). A place with no
       transcript grounding may legitimately have a short/empty summary.
       `supabase db query "select summary_ko from links where id='<link_id>';"`
       → expect a 2~3 sentence Korean TL;DR.
    3. EXTRACT-14: extract a video with a SPARSE/empty transcript → confirm the link still reaches
       'ready'/'manual_review' as before and places are still extracted; summary_ko may be NULL/short
       but extraction did NOT fail because of missing commentary.
    B. Browser (Success Criterion 4, VIEW-08):
    4. Open the public board `/b/<slug>` for the just-extracted board in a real browser → confirm the
       장소 section shows each place's commentary and the 영상 출처 list shows the video TL;DR.
    5. Open a LEGACY public board (one extracted before Phase 8, summary_ko null) → confirm the page
       renders with NO summary blocks and NO layout break (conditional render works on real null data).
  </how-to-verify>
  <resume-signal>Type "verified" with a one-line note of the test video + whether any hallucination was spotted; or describe issues (which become gap-closure input).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → linked production Supabase | migration apply + type gen touch the live project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Tampering | database.ts wipe on failed gen | mitigate | Task 2 guards with `wc -l` line-count check + documented `git checkout HEAD --` recovery before any commit (STATE.md known hazard). |
| T-08-09 | Repudiation / data quality | hallucinated commentary reaching public web | mitigate | Task 3 mandates a spot-check of 2-3 summaries against the transcript; hallucination found → feed to gap closure (`/gsd-plan-phase 8 --gaps`) to tighten the prompt grounding clause. |
</threat_model>

<verification>
- Migration 0008 applied; both columns + RPC key confirmed live.
- database.ts regenerated, non-empty, contains summary_ko, typecheck green.
- Live extraction: non-empty Korean place summaries + video TL;DR, no hallucination on spot-check.
- Sparse-transcript video: extraction still succeeds with NULL/short summary (EXTRACT-14).
- Browser: commentary visible on a fresh board; legacy null board renders intact (VIEW-08).
</verification>

<success_criteria>
- All four ROADMAP Phase 8 success criteria proven against the live system.
- database.ts carries summary_ko (regenerated, not hand-edited).
- No hallucination on spot-check (or gap recorded for prompt tightening).
</success_criteria>

<output>
After completion, create `.planning/phases/08-extraction-depth/08-04-SUMMARY.md`
recording the test video, the observed summaries, and the hallucination spot-check result.
</output>
