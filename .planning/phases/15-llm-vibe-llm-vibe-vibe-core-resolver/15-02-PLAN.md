---
phase: 15-llm-vibe-llm-vibe-vibe-core-resolver
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/extract-youtube/pipeline/claude.ts
  - supabase/functions/extract-youtube/pipeline/claude.test.ts
  - supabase/functions/extract-youtube/index.ts
autonomous: false
requirements: [EXTRACT-12]
must_haves:
  truths:
    - "Claude PlaceCandidate schema accepts an optional `vibe` enum of the 6 canonical keys"
    - "The extraction prompt instructs the LLM to assign one of the 6 vibe buckets per place"
    - "Map-link places (which are NOT LLM candidates) borrow a vibe by matching their normalized name to an LLM candidate"
    - "Inserted places store `category = primaryType ?? vibe ?? null` (Google type for text-search, vibe key for map-link)"
    - "buildPrompt regression snapshot is updated so the prompt-drift test passes"
  artifacts:
    - path: "supabase/functions/extract-youtube/pipeline/claude.ts"
      provides: "vibe field on PlaceCandidate + prompt schema/constraint line"
      contains: "vibe"
    - path: "supabase/functions/extract-youtube/index.ts"
      provides: "map-link vibe borrow + category insert = primaryType ?? vibe"
      contains: "vibe"
  key_links:
    - from: "supabase/functions/extract-youtube/index.ts (map-link seed loop ~289)"
      to: "validPlaces (LLM candidates)"
      via: "normalizeName name match → borrow cand.vibe"
      pattern: "normalizeName"
    - from: "supabase/functions/extract-youtube/index.ts (insert row ~363)"
      to: "places.category"
      via: "primaryType ?? vibe ?? null"
      pattern: "primaryType \\?\\? .*vibe"
---

<objective>
Give the extraction pipeline a coarse `vibe` for every place at zero added API cost (D4). The LLM assigns a vibe per place (single existing Claude call — no new call/latency). Map-link places (seeded keyless from description Google-map links, NOT LLM candidates) borrow a vibe by matching their normalized name to an LLM candidate. The insert stores `places.category = primaryType ?? vibe ?? null` (D3) so display normalization (`placeVibe`) always has something to color.

Purpose: Map-link places currently land with `primaryType: null` → `category` empty → grey cards. This is the forward fix that makes category color "always" alive (D5: no confidence gating — vibe is just color).
Output: Edited `claude.ts` (schema + prompt + updated regression snapshot in `claude.test.ts`) and `index.ts` (vibe borrow + insert category).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-CONTEXT.md

<interfaces>
<!-- The Edge function (Deno) does NOT import @moajoa/core — confirmed: no @moajoa/core / packages/core
     references in supabase/functions/extract-youtube. So the vibe enum is defined LOCALLY in claude.ts
     and kept in sync with core's Vibe type by comment. (D2 note: resolver lives in core; the enum literal
     is duplicated here only because Deno can't import the workspace package.) -->

The 6 canonical vibe keys (MUST match @moajoa/core Vibe — keep in sync):
  'food' | 'cafe' | 'nature' | 'culture' | 'shopping' | 'other'

PlaceCandidate (claude.ts ~line 11), current shape — add `vibe` as optional:
  z.object({
    name_local, name_ko?, summary_ko?, source_timestamp_sec?, source_quote,
    confidence (default 0.5), inferred_city?,
    // ADD: vibe: z.enum(['food','cafe','nature','culture','shopping','other']).optional(),
  })

buildPrompt (claude.ts ~line 116) is snapshot-tested in claude.test.ts via YOUTUBE_PROMPT_REGRESSION_0
(an exact byte-for-byte string). ANY prompt change here MUST update that constant in the test file or the
regression test fails. Two edits to the prompt:
  1. Output schema block: add a "vibe" line to the per-place object.
  2. Constraints block: add ONE line describing the 6 buckets.

index.ts map-link seed loop (~289): each map-link `mlp` pushes a `resolved` entry with
  cand: { name_ko, source_timestamp_sec, source_quote, inferred_city, confidence: 0.95, summary_ko }
  place: { placeId, displayName, lat, lng, primaryType: null, ... }
`mapLinkNames` Set is built from normalizeName(mlp.label) + normalizeName(mlp.name).
`validPlaces` = LLM candidates that survived the source_quote filter (has .name_local, .name_ko?, .vibe?).
normalizeName(s: string): string  — from ./pipeline/maplinks.ts (already imported).

index.ts insert row (~363): `category: r.place.primaryType ?? null` → change to
  `category: r.place.primaryType ?? r.cand.vibe ?? null`
For text-search places r.cand IS an LLM candidate (has .vibe). For map-link places r.cand is the seeded
object — add a `vibe` field to it (borrowed below), else it's undefined → null → resolver returns 'other'.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vibe to PlaceCandidate + prompt + update regression snapshot</name>
  <files>supabase/functions/extract-youtube/pipeline/claude.ts, supabase/functions/extract-youtube/pipeline/claude.test.ts</files>
  <action>
    In claude.ts (D4):
    1. Add to `PlaceCandidate` (~line 11): `vibe: z.enum(['food','cafe','nature','culture','shopping','other']).optional(),`. Add a one-line `// keep in sync with @moajoa/core Vibe (Deno can't import the workspace pkg)` comment.
    2. In `buildPrompt` Output schema block, add a vibe line to the per-place object, e.g.:
       `      "vibe": "<one of: food, cafe, nature, culture, shopping, other>",`
       Place it adjacent to inferred_city for readability.
    3. In the Constraints block, add ONE line:
       `- vibe: classify each place into exactly one of food(맛집·식당·바·이자카야) / cafe(카페·베이커리·디저트) / nature(공원·자연·해변) / culture(명소·박물관·신사·랜드마크) / shopping(쇼핑·상점·마켓) / other(그 외). Coarse color hint only.`
    Then update claude.test.ts: regenerate `YOUTUBE_PROMPT_REGRESSION_0` so it includes the two new lines byte-for-byte (run the suite to see the diff, then paste the new expected string). Also add a positive test: `LLMOutput.parse({ places: [{ name_local:'x', source_quote:'q', vibe:'cafe' }] })` yields `places[0].vibe === 'cafe'`, and an omitted-vibe case yields `undefined`, and an invalid vibe (`vibe:'bar'`) throws.
    Do NOT change SYSTEM_PROMPT, max_tokens, model, or any other field (surgical, CLAUDE.md §3.3).
  </action>
  <verify>
    <automated>cd supabase/functions/extract-youtube && deno test pipeline/claude.test.ts --allow-none 2>&1 | tail -20</automated>
  </verify>
  <done>`deno test` passes including the updated regression snapshot and the new vibe parse cases. Commit: `feat(15-02): LLM vibe field + prompt + regression snapshot`.</done>
</task>

<task type="auto">
  <name>Task 2: Map-link vibe borrow + insert category = primaryType ?? vibe</name>
  <files>supabase/functions/extract-youtube/index.ts</files>
  <action>
    Two surgical edits (D4 + D3):
    1. Map-link vibe borrow — in the map-link seed loop (~289), before/while pushing each `resolved` entry, find a matching LLM candidate by normalized name and borrow its vibe. Build a lookup once (outside the loop) from `validPlaces`:
       `const llmVibeByName = new Map<string, 'food'|'cafe'|'nature'|'culture'|'shopping'|'other'>();`
       for each `c of validPlaces` with `c.vibe`, set both `normalizeName(c.name_local)` and (if present) `normalizeName(c.name_ko)` → `c.vibe`. Then in the seed loop, set the seeded `cand.vibe = llmVibeByName.get(normalizeName(mlp.name)) ?? (mlp.label ? llmVibeByName.get(normalizeName(mlp.label)) : undefined)`. No match → leave undefined (resolver → 'other', per D4).
    2. Insert category — change the insert row (~363) from `category: r.place.primaryType ?? null` to `category: r.place.primaryType ?? r.cand.vibe ?? null` (D3: text-search places keep richer Google primaryType; map-link places fall back to the borrowed vibe key).
    Note: text-search `resolved` entries already carry the full LLM `cand` (with `.vibe`), so they're covered automatically. Touch nothing else (surgical). Keep `place.primaryType: null` for map-link seeds unchanged — the vibe lives on `cand`, not `place`.
  </action>
  <verify>
    <automated>cd supabase/functions/extract-youtube && deno check index.ts 2>&1 | grep -v "pre-existing" | tail -20</automated>
  </verify>
  <done>`deno check index.ts` introduces no NEW type errors (pre-existing ones logged earlier in deferred-items.md are out of scope). Map-link seeds get `cand.vibe` when a same-named LLM candidate exists; insert writes `primaryType ?? vibe ?? null`. Commit: `feat(15-02): map-link vibe borrow + category = primaryType ?? vibe`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Edge Function `extract-youtube` now emits a coarse vibe per place and stores it in `places.category` for map-link places (Google primaryType still wins for text-search places). This is NOT locally screenshot-verifiable — it requires deploy + a real extraction (project pattern: Phase 14 plans 03/04 deferred Edge-deploy/UAT to a human gate).</what-built>
  <how-to-verify>
    1. Deploy: `supabase functions deploy extract-youtube`
    2. In the iOS app (or via the dev tool), extract a YouTube video whose description contains Google-map links with Korean-transliterated place names (the동료 video class that previously came out grey).
    3. In Supabase SQL editor, inspect the new rows:
       `select name_local, category, google_place_id from places where link_id = '<the link>' order by created_at desc;`
       Expect: map-link places (google_place_id present, was previously `category` null) now show a vibe key like `food`/`cafe`/`culture`; text-search places still show a Google primaryType (e.g. `ramen_restaurant`).
    4. Open the board card in iOS / public board on web — cards should now wear a non-grey color (this completes once Plan 15-03 ships the client resolver; if 15-03 isn't merged yet, only the stored category needs to look right here).
    5. Confirm no extraction regressed (places still appear, summaries intact).
  </how-to-verify>
  <resume-signal>Type "approved" once stored categories look right, or describe what came out grey/wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → DB | Claude-supplied `vibe` string crosses into `places.category` |
| description map links → name match | Untrusted video description text drives the vibe-borrow name match |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Tampering | LLM `vibe` value | mitigate | `z.enum([...6 keys]).optional()` in PlaceCandidate rejects any out-of-set value at parse time before it can reach the insert. |
| T-15-04 | Tampering | map-link name match | accept | A mismatched/poisoned name only yields a wrong-or-absent color; resolver defaults to 'other'. Low-stakes per D5, no privilege or data exposure. |
| T-15-05 | Information disclosure | category column | accept | Stores only a coarse public taxonomy key or Google type — no PII/secret. |
</threat_model>

<verification>
- `PlaceCandidate` parse rejects vibe values outside the 6 keys (enum guard).
- buildPrompt regression snapshot updated; `deno test pipeline/claude.test.ts` green.
- `deno check index.ts` adds no new type errors.
- After deploy + real extraction: map-link places store a vibe key in `category`; text-search places keep their Google primaryType.
</verification>

<success_criteria>
- LLM emits a vibe per place; map-link places borrow a matching candidate's vibe (zero extra API calls).
- `places.category` insert = `primaryType ?? vibe ?? null`.
- Edge-deploy + real-video UAT confirms stored categories are populated for previously-grey map-link places.
</success_criteria>

<output>
After completion, create `.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-02-SUMMARY.md`
</output>
