---
phase: 03-ios-save-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0005_extraction_costs_link_id_nullable.sql
  - packages/core/src/constants.ts
  - apps/ios/jest.config.js
  - apps/ios/jest-setup.ts
  - apps/ios/package.json
  - apps/ios/__mocks__/shared-defaults.ts
  - apps/ios/__tests__/.gitkeep
  - docs/manual-uat-phase3.md
autonomous: true
requirements:
  - SAVE-04
  - SAVE-05
must_haves:
  truths:
    - "Migration 0005 exists and drops NOT NULL on extraction_costs.link_id"
    - "packages/core exports APP_GROUP_ID = 'group.com.serendipitylife.moajoa'"
    - "packages/core exports PENDING_LINKS_KEY / PENDING_LINKS_FAILED_KEY / LAST_BOARD_ID_KEY"
    - "jest-expo + @testing-library/react-native installed in apps/ios"
    - "pnpm --filter @moajoa/ios test runs (even if no tests yet — exits 0)"
    - "docs/manual-uat-phase3.md exists with 5 numbered scenarios (SAVE-01..05)"
  artifacts:
    - path: "supabase/migrations/0005_extraction_costs_link_id_nullable.sql"
      provides: "ALTER TABLE to allow link_id IS NULL for resolve-place manual searches"
      contains: "alter column link_id drop not null"
    - path: "packages/core/src/constants.ts"
      provides: "APP_GROUP_ID + SharedDefaults keys as exported constants"
      contains: "APP_GROUP_ID"
    - path: "apps/ios/jest.config.js"
      provides: "jest-expo preset config"
      contains: "jest-expo"
    - path: "docs/manual-uat-phase3.md"
      provides: "Manual real-device UAT checklist for SAVE-01..05"
      min_lines: 30
  key_links:
    - from: "supabase/functions/resolve-place (Wave 2)"
      to: "supabase/migrations/0005"
      via: "extraction_costs INSERT with link_id=null relies on migration"
      pattern: "drop not null"
    - from: "apps/ios/lib/shared-defaults.ts (Wave 3)"
      to: "packages/core/src/constants.ts"
      via: "imports APP_GROUP_ID and keys"
      pattern: "APP_GROUP_ID"
---

<objective>
Lay Wave 1 foundation for Phase 3: a migration that unblocks resolve-place cost logging, shared constants every other plan imports (App Group ID + SharedDefaults keys), RN test infrastructure (so SAVE-04 drain logic and SAVE-05 resolve-place can have unit/integration tests), and a manual UAT checklist (since SAVE-01/03/04 ultimately gate on real-device verification).

Purpose: All four artifacts are blockers for Wave 2/3/4. Doing them as a single foundation plan keeps the rest parallel-safe and lets every other plan reference these constants by name (no string drift — see Pitfall 2 "App Group ID mismatch").
Output: Migration 0005, packages/core constants additions, apps/ios jest setup, manual UAT doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ios-save-flow/03-CONTEXT.md
@.planning/phases/03-ios-save-flow/03-RESEARCH.md
@CLAUDE.md
@supabase/migrations/0004_extraction_hardening.sql
@packages/core/src/constants.ts

<interfaces>
<!-- Current state of packages/core/src/constants.ts (extracted from codebase) -->
<!-- This plan APPENDS new exports — do NOT remove existing Limits/SourceKind/VoteKind/BoardVisibility -->

Existing exports (keep intact):
```typescript
export const Limits = { BoardsPerUser, LinksPerBoard, MembersPerBoard, PlacesPerLink, BoardTitleMax, BoardDescMax, VoteNoteMax } as const;
export const SourceKind = ['youtube', 'blog', 'instagram', 'manual'] as const;
export type SourceKindType = (typeof SourceKind)[number];
export const VoteKind = ['love'] as const;
export const BoardVisibility = ['private', 'shared', 'public'] as const;
```

Existing 0004 migration (line 28):
```sql
link_id uuid not null references links(id) on delete cascade,
```
This NOT NULL is what 0005 must drop.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0005 (extraction_costs.link_id nullable) + add Phase 3 constants to packages/core</name>
  <read_first>
    - supabase/migrations/0004_extraction_hardening.sql (verify exact NOT NULL constraint phrasing)
    - packages/core/src/constants.ts (current shape — append, do not rewrite)
    - .planning/phases/03-ios-save-flow/03-RESEARCH.md §"Pattern 5", §"Pitfall 9" (rationale)
    - CLAUDE.md §4.3 (migrations append-only — new numbered file, never edit 0001~0004)
  </read_first>
  <files>
    supabase/migrations/0005_extraction_costs_link_id_nullable.sql,
    packages/core/src/constants.ts
  </files>
  <action>
**A. Create `supabase/migrations/0005_extraction_costs_link_id_nullable.sql`** with exact content:

```sql
-- Migration 0005: Allow extraction_costs.link_id to be NULL.
-- Why: Phase 3 introduces a new Edge Function `resolve-place` (manual pin search)
-- that performs Google Places API calls outside the per-link extraction pipeline.
-- Per CONTEXT.md D-08, these calls MUST be logged in extraction_costs for
-- aggregated cost monitoring (Phase 2 D-09 pattern). Such rows have no link_id.
--
-- Existing 0004 declared link_id NOT NULL with FK on links(id) on delete cascade.
-- We keep the FK + cascade; we only relax the NOT NULL.
--
-- Append-only per CLAUDE.md §4.3. Idempotent (uses IF NOT NULL guard via DO block).

do $$
begin
  if exists (
    select 1 from information_schema.columns
    where table_schema = 'public'
      and table_name = 'extraction_costs'
      and column_name = 'link_id'
      and is_nullable = 'NO'
  ) then
    alter table public.extraction_costs alter column link_id drop not null;
  end if;
end $$;

-- Replace strict link-only index with partial index so aggregations stay fast.
drop index if exists extraction_costs_link_idx;
create index extraction_costs_link_idx
  on public.extraction_costs (link_id)
  where link_id is not null;
```

**B. Append to `packages/core/src/constants.ts`** (do NOT modify existing exports — add at the bottom of the file, before any trailing newline):

```typescript

/**
 * iOS App Group identifier shared between the main app target and the
 * Share Extension target. MUST exactly match:
 *   - apps/ios/app.config.ts entitlements + plugin option (Plan 03-02)
 *   - apps/ios/ios/MOAJOA.entitlements after prebuild
 *   - SharedDefaults(suiteName: APP_GROUP_ID) in Swift (Plan 03-04)
 *
 * Mismatch = silent nil from UserDefaults — no error, just data loss
 * (Phase 3 RESEARCH Pitfall 2).
 */
export const APP_GROUP_ID = 'group.com.serendipitylife.moajoa' as const;

/**
 * Keys written to App Group SharedDefaults. Single source of truth so
 * Share Extension Swift code and main-app TS code never drift.
 */
export const SharedDefaultsKeys = {
  /** JSON array of {url, board_id, queued_at, retry_count} — D-05. */
  PendingLinks: 'pending_links',
  /** JSON array of {...PendingLink, failed_at, reason} — D-06 (retry_count > 3). */
  PendingLinksFailed: 'pending_links_failed',
  /** UUID string — last board the user saved to. Used by Share Extension default. */
  LastBoardId: 'last_board_id',
  /** Boolean string ('1' / '0') — Share Extension uses this to decide D-03 fallback. */
  AuthStatus: 'auth_status',
} as const;

/**
 * Supabase Realtime channel name builder for extraction broadcast.
 * Phase 2 sends; Phase 3 subscribes.
 */
export function extractChannelName(linkId: string): string {
  return `extract:${linkId}`;
}
```

**C. Verify TypeScript still compiles:**
```bash
pnpm --filter @moajoa/core typecheck
```

DO NOT add a `.js` extension on any import (CLAUDE.md §4.5).
Implements: D-02 (App Group ID), D-05 (pending_links shape — key constant), D-08 (resolve-place requires nullable link_id), Phase 2 D-09 pattern reuse.
  </action>
  <verify>
    <automated>grep -q "drop not null" supabase/migrations/0005_extraction_costs_link_id_nullable.sql &amp;&amp; grep -q "APP_GROUP_ID = 'group.com.serendipitylife.moajoa'" packages/core/src/constants.ts &amp;&amp; grep -q "PendingLinks: 'pending_links'" packages/core/src/constants.ts &amp;&amp; grep -q "extractChannelName" packages/core/src/constants.ts &amp;&amp; pnpm --filter @moajoa/core typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0005_extraction_costs_link_id_nullable.sql` exists
    - `grep -c "alter column link_id drop not null" supabase/migrations/0005_extraction_costs_link_id_nullable.sql` returns 1
    - `grep -c "extraction_costs_link_idx" supabase/migrations/0005_extraction_costs_link_id_nullable.sql` returns 2 (drop + create)
    - `grep -c "where link_id is not null" supabase/migrations/0005_extraction_costs_link_id_nullable.sql` returns 1
    - 0004 file unchanged: `git diff --name-only supabase/migrations/0004_extraction_hardening.sql` returns empty
    - `grep -q "^export const APP_GROUP_ID = 'group.com.serendipitylife.moajoa'" packages/core/src/constants.ts`
    - `grep -q "^export const SharedDefaultsKeys" packages/core/src/constants.ts`
    - `grep -q "PendingLinks: 'pending_links'" packages/core/src/constants.ts`
    - `grep -q "PendingLinksFailed: 'pending_links_failed'" packages/core/src/constants.ts`
    - `grep -q "LastBoardId: 'last_board_id'" packages/core/src/constants.ts`
    - `grep -q "AuthStatus: 'auth_status'" packages/core/src/constants.ts`
    - `grep -q "export function extractChannelName" packages/core/src/constants.ts`
    - Existing `Limits` / `SourceKind` / `VoteKind` / `BoardVisibility` exports still present (`grep -c "^export const Limits" packages/core/src/constants.ts` returns 1)
    - `pnpm --filter @moajoa/core typecheck` exits 0
    - No `.js` extension in any import added by this task: `grep -n "from '.*\.js'" packages/core/src/constants.ts` returns no matches
  </acceptance_criteria>
  <done>Migration 0005 file exists, constants exported, typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap apps/ios test infra (jest-expo + RNTL) + write manual UAT checklist</name>
  <read_first>
    - apps/ios/package.json (current deps + scripts — append, do not rewrite)
    - apps/ios/tsconfig.json (path aliases — jest must resolve `@/*` to `apps/ios/`)
    - .planning/phases/03-ios-save-flow/03-RESEARCH.md §"Validation Architecture" → §"Wave 0 Gaps" (this task closes those gaps)
    - .planning/phases/03-ios-save-flow/03-CONTEXT.md (5 manual UAT scenarios = SAVE-01..05)
  </read_first>
  <files>
    apps/ios/package.json,
    apps/ios/jest.config.js,
    apps/ios/jest-setup.ts,
    apps/ios/__mocks__/shared-defaults.ts,
    apps/ios/__tests__/.gitkeep,
    docs/manual-uat-phase3.md
  </files>
  <action>
**A. Install test deps in `apps/ios`** (Phase 1 D-02 hoist pattern — direct dep declaration in apps/ios/package.json so pnpm hoist surfaces them):

```bash
pnpm --filter @moajoa/ios add -D jest@^29.7.0 jest-expo@~54.0.0 @testing-library/react-native@^12.7.0 @testing-library/jest-native@^5.4.3 @types/jest@^29.5.12 babel-jest@^29.7.0
```

Then add a `test` script to `apps/ios/package.json` `scripts`:
```json
"test": "jest --passWithNoTests",
"test:watch": "jest --watch --passWithNoTests"
```
(Insert next to existing `typecheck` script. Preserve every other field exactly.)

**B. Create `apps/ios/jest.config.js`** (CommonJS — Expo SDK 54 jest-expo preset):

```js
/** @type {import('jest').Config} */
module.exports = {
  preset: 'jest-expo',
  setupFiles: ['./jest-setup.ts'],
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/$1',
    '^@moajoa/core$': '<rootDir>/../../packages/core/src/index.ts',
    '^@moajoa/api$': '<rootDir>/../../packages/api/src/index.ts',
    '^@moajoa/ui-tokens$': '<rootDir>/../../packages/ui-tokens/src/index.ts',
  },
  transformIgnorePatterns: [
    'node_modules/(?!((jest-)?react-native|@react-native(-community)?|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@unimodules/.*|unimodules|sentry-expo|native-base|react-native-svg))',
  ],
  testPathIgnorePatterns: ['/node_modules/', '/ios/', '/android/'],
};
```


**C. Create `apps/ios/jest-setup.ts`** — single setup file for `setupFiles` (loads `@testing-library/jest-native` matchers before each test file):

```ts
import '@testing-library/jest-native/extend-expect';
```

**D. Create stub mock `apps/ios/__mocks__/shared-defaults.ts`** (Plan 03-04 will USE this; we create the contract now so 03-04's tests can mock):

```ts
// Jest auto-mock target for apps/ios/lib/shared-defaults.ts (Plan 03-04).
// Provides an in-memory Map standing in for App Group UserDefaults.
const store = new Map<string, string>();

export const SharedDefaults = {
  get<T>(key: string): T | null {
    const raw = store.get(key);
    if (!raw) return null;
    try { return JSON.parse(raw) as T; } catch { return null; }
  },
  set<T>(key: string, value: T): void {
    store.set(key, JSON.stringify(value));
  },
  remove(key: string): void {
    store.delete(key);
  },
  __clear(): void {
    store.clear();
  },
};
```

**E. Create empty marker file `apps/ios/__tests__/.gitkeep`** (so the dir is tracked; 03-04 and 03-05 will populate it).

**F. Create `docs/manual-uat-phase3.md`** — real-device UAT checklist. Each scenario MUST be numbered, MUST cite the requirement ID, and MUST list explicit pass/fail criteria. Use exactly this structure:

```markdown
# Phase 3 Manual UAT — iOS Save Flow

> 실기기(아이폰) 필수. 시뮬레이터는 Share Extension 동작이 부정확함 (03-RESEARCH §"Environment Availability").
> 본 체크리스트는 자동화 불가능한 부분만 다룬다. drainPendingLinks 로직과 resolve-place Edge Function은 unit/integration 테스트로 별도 검증.

## Prerequisites

- iPhone (iOS 16+) connected via USB or paired wirelessly
- `apps/ios/.env.local` 설정됨 (`EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY`)
- `pnpm --filter @moajoa/ios prebuild --clean` + `pnpm --filter @moajoa/ios ios` 으로 실기기 install 완료
- Supabase 프로젝트 (`apps/ios/lib/supabase.ts` 가 가리키는) 에서 본인 계정으로 가입 완료
- 적어도 1개 보드 존재 (없으면 시나리오 1에서 생성)

## Scenario 1 — SAVE-01: 로그인 → 보드 목록 → 보드 상세 진입

1. 앱을 cold launch (홈스크린에서 MOAJOA 아이콘 탭)
2. 로그인 화면이 표시되는지 확인
3. 본인 이메일/비밀번호로 로그인
4. 보드 목록 화면(`/(tabs)/boards`)이 표시되는지 확인
5. 보드 카드를 탭하여 보드 상세(`/boards/[id]`)로 진입
6. 막힘 / 흰 화면 / crash 없이 진입 완료

**Pass:** 1~6 모두 성공, 각 화면 전환 < 1초
**Fail 조건:** 어느 단계든 crash, 흰 화면, "Login required" loop

## Scenario 2 — SAVE-02: URL → 30초 안에 핀 (p90)

1. Scenario 1 완료 후 보드 상세 화면에서 시작
2. URL TextInput에 YouTube URL 붙여넣기 (예: 일본 카페 vlog)
3. "추가" 버튼 탭
4. 화면에 spinner overlay + "분석 중..." 텍스트 표시 확인
5. 시간 기록 시작 (t0)
6. spinner가 사라지고 "{N}개 핀 추가됨" toast 표시될 때까지 대기
7. 시간 기록 종료 (t1). 경과 = t1 - t0
8. MapView에 새 marker가 N개 추가되었는지 시각 확인

**Pass:** 경과 ≤ 30초 (3회 반복 중 최소 2회 통과 = p90 추정). 핀 marker가 MapView에 표시됨.
**Fail 조건:** spinner 60초+ 지속, error toast 표시, MapView marker 미표시.
**p90 정밀 측정:** Phase 6 dogfooding 7일치 `extraction_costs.duration_ms` SQL aggregate (D-11)로 최종 확정.

## Scenario 3 — SAVE-03: 카톡/사파리 Share Sheet → 1탭 저장

1. 사파리에서 YouTube 비디오 URL 열기 (어떤 비디오든 무관)
2. 사파리 하단 share 버튼 (네모+위쪽 화살표) 탭
3. Share sheet 두 번째 줄에서 "MOAJOA 저장" 아이콘 탭
4. Share Extension UI가 0.5~1초 안에 자동으로 닫히고 "{보드명} - 저장됨" 토스트 표시 (D-01 정확한 형식)
5. MOAJOA 메인 앱 열기 → 마지막 사용 보드(last_board_id) 진입 → 새 link 카드 + spinner 또는 새 핀 확인

**Pass:** Share Extension dismiss < 1.5초, toast 표시 정확히 "{보드명} - 저장됨", 메인 앱 진입 시 해당 URL의 link/place 존재
**Fail 조건:** Share Extension에 BoardPicker 표시 (D-01 위반), toast 누락, dismiss > 3초, link 미저장.

> 카톡 안에서의 share는 카카오톡 사용 시 동일 절차. 카카오 앱이 share sheet에서 MOAJOA 노출 안 하면 (카카오 정책), 사파리만으로 확인 충분.

## Scenario 4 — SAVE-04: Offline enqueue + 메인 앱 launch 시 drain

1. iPhone 설정 → 비행기 모드 ON (또는 Wi-Fi/셀룰러 모두 OFF)
2. 사파리에서 YouTube URL 열기 → share → MOAJOA 저장
3. Share Extension 토스트 = "오프라인 — 나중에 저장돼요" (D-04, §UI-SPEC §2)
4. 비행기 모드 OFF (네트워크 복구)
5. MOAJOA 메인 앱 열기 (cold launch — 앱 swipe out 후 다시 열기)
6. 마지막 보드 자동 진입 → 그 보드에 share했던 URL이 link로 추가됨 + 추출 spinner 또는 핀 표시
7. (추가) 두 번째 검증: 앱이 background 상태에서 share → 비행기 모드 OFF → 앱 foreground 복귀 (홈스크린 → MOAJOA 탭) → 같은 drain 동작 확인

**Pass:** offline share 후 online 복구 시 cold launch AND foreground 복귀 둘 다에서 enqueue된 link가 자동 추출됨 (D-04). drain은 silent — 사용자 별도 action 없음.
**Fail 조건:** drain 안 일어남 / 같은 URL 중복 link 생성 (Pitfall 7 retry storm) / app crash on launch with pending queue.

## Scenario 5 — SAVE-05: 수동 핀 추가/편집/삭제

1. 보드 상세 화면에서 우측 상단 "+ 핀" 탭
2. modal이 화면 하단에서 올라옴 (pageSheet 스타일). 키보드 자동 노출.
3. 검색창에 "스타벅스 리저브 도쿄" 입력 (300ms debounce 후 검색)
4. 결과 dropdown에 최대 5개 (D-07) 검색 결과 표시
5. 첫 번째 결과 탭 → modal dismiss → "핀 추가됨" toast → MapView에 새 marker
6. 그 marker 탭 → bottom sheet 표시 (D-09): 장소명 / 주소 / "수동" badge / [이름 수정] / [삭제] 버튼 (영상에서 위치 보기는 manual이라 숨김)
7. [이름 수정] 탭 → 장소명 inline TextInput으로 전환 → "내가 좋아하는 곳"으로 수정 → blur 또는 save
8. bottom sheet 닫고 다시 그 marker 탭 → 수정된 이름이 반영됨
9. [삭제] 탭 → Alert.alert("핀 삭제", "정말 삭제할까요?") → [삭제] 탭 → bottom sheet dismiss → MapView marker 사라짐 → "삭제됨" toast

**Pass:** 1~9 모두 성공, RLS 차단 없음 (본인이 board owner / member이므로)
**Fail 조건:** 검색 결과 빈 응답, marker 미표시, RLS 거부 (`new row violates row-level security policy`), 삭제 후 marker 재출현.

## 부정 시나리오 (Negative)

### N1 — SAVE-04 retry > 3 fallback
1. apps/ios/lib/pending.ts 의 addLink mock을 강제 실패하도록 한 상태에서 drain → retry_count = 1, 2, 3, 4 진행 → 4번째에 `pending_links_failed`로 이동 확인
2. 보드 목록 상단에 "저장 실패 N개 — 탭하여 확인" banner 표시 (UI-SPEC §5)

**Pass:** retry > 3 시 pending_links_failed로 이동 + banner 표시
(N1은 unit test로도 검증 — `__tests__/pending.test.ts` from Plan 03-04)

### N2 — SAVE-05 non-member RLS
1. 두 번째 계정 (board member 아님) 으로 로그인 → 보드 ID를 알아도 + 핀 추가 시도 → Edge Function 또는 DB RLS 거부 응답
**Pass:** 401 또는 RLS error 응답, 핀 INSERT 안 됨

---

## 실행 기록

각 시나리오 실행 시 commit message에 결과 기록: `test(03): UAT scenario {N} PASS — {date}`

마지막 모든 시나리오 PASS 시점에 Phase 3 종료 가능.
```

Implements: All SAVE-01..05 manual-only verification gates (research §"Validation Architecture"). Closes Wave 0 gaps.
  </action>
  <verify>
    <automated>cd apps/ios &amp;&amp; cat package.json | grep -q "\"jest\":" &amp;&amp; cat package.json | grep -q "\"jest-expo\":" &amp;&amp; cat package.json | grep -q "\"@testing-library/react-native\":" &amp;&amp; cat package.json | grep -q "\"test\":" &amp;&amp; test -f jest.config.js &amp;&amp; test -f jest-setup.ts &amp;&amp; test -f __mocks__/shared-defaults.ts &amp;&amp; test -f __tests__/.gitkeep &amp;&amp; cd ../.. &amp;&amp; test -f docs/manual-uat-phase3.md &amp;&amp; pnpm --filter @moajoa/ios test --passWithNoTests</automated>
  </verify>
  <acceptance_criteria>
    - `apps/ios/package.json` contains `"jest"` AND `"jest-expo"` AND `"@testing-library/react-native"` AND `"@testing-library/jest-native"` AND `"babel-jest"` under devDependencies (`grep -c` each returns 1)
    - `apps/ios/package.json` scripts has `"test"` key (`grep -E '"test"\s*:' apps/ios/package.json` matches)
    - `apps/ios/jest.config.js` exists and contains `preset: 'jest-expo'`
    - `apps/ios/jest.config.js` contains `setupFiles: ['./jest-setup.ts']` (single setup file — pinned standard option, not setupFilesAfterEach)
    - `apps/ios/jest-setup.ts` exists and contains `import '@testing-library/jest-native/extend-expect';` (`grep -q "@testing-library/jest-native/extend-expect" apps/ios/jest-setup.ts`)
    - `apps/ios/jest.config.js` moduleNameMapper resolves `^@/(.*)$` to `<rootDir>/$1` (`grep -q "'@/(.*)'" apps/ios/jest.config.js` OR exact string `"^@/(.*)$"`)
    - `apps/ios/__mocks__/shared-defaults.ts` exports `SharedDefaults` with `get`, `set`, `remove`, `__clear` methods (4 `grep -c` each returns ≥1)
    - `apps/ios/__tests__/.gitkeep` exists
    - `docs/manual-uat-phase3.md` exists with at least 5 "## Scenario" headings (`grep -c "^## Scenario" docs/manual-uat-phase3.md` returns ≥5)
    - `docs/manual-uat-phase3.md` references each requirement ID (`grep -c "SAVE-01\|SAVE-02\|SAVE-03\|SAVE-04\|SAVE-05" docs/manual-uat-phase3.md` returns ≥5)
    - `pnpm --filter @moajoa/ios test --passWithNoTests` exits 0 (Jest config loads without error, even with 0 tests)
    - No `.js` extension in jest.config.js moduleNameMapper targets (`grep -n "src/index.ts" apps/ios/jest.config.js` matches — `.ts` not `.js`)
  </acceptance_criteria>
  <done>Test infrastructure runs, manual UAT doc complete with 5 scenarios + 2 negative cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration → production DB | DDL applied via `supabase db push` or PR-driven CI. Append-only constraint (CLAUDE.md §4.3) is the safety rail |
| packages/core → all consumers | Constants exported are weak typing of secrets-adjacent data (App Group ID). No actual secrets in source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | Migration 0005 (potential index loss) | mitigate | Partial index `where link_id is not null` preserves aggregation perf for link-keyed queries; tested in CI by `supabase db reset` cycle |
| T-03-01-02 | Information Disclosure | APP_GROUP_ID exported in packages/core (bundled into web + iOS) | accept | App Group ID is not a secret per Apple security model — it identifies a sandbox shared by apps signed by the same developer team. Public knowledge of the string grants no access. |
| T-03-01-03 | Repudiation | extraction_costs.link_id NULL rows lose link traceability | accept | Manual searches (resolve-place, Phase 3 Plan 03-03) intentionally have no link; aggregation by `provider='google_places'` filter still distinguishes them. |
</threat_model>

<verification>
- [ ] Migration 0005 is the ONLY new migration file added (no edits to 0001-0004)
- [ ] `supabase db reset` (if available locally) replays 0001 → 0005 without error
- [ ] `pnpm --filter @moajoa/core typecheck` clean
- [ ] `pnpm --filter @moajoa/ios test --passWithNoTests` clean
- [ ] No new `.js` extension imports introduced
</verification>

<success_criteria>
- Migration 0005 file present and idempotent
- Constants APP_GROUP_ID + SharedDefaultsKeys + extractChannelName exported from packages/core
- apps/ios jest config loads (even with 0 tests)
- docs/manual-uat-phase3.md has 5 scenarios + N1/N2 negatives, all citing requirement IDs
</success_criteria>

<output>
After completion, create `.planning/phases/03-ios-save-flow/03-01-SUMMARY.md` with:
- Migration 0005 SQL summary
- Exact constant exports added (line numbers in packages/core/src/constants.ts)
- jest.config.js contents
- Manual UAT doc index (scenario list)
- Anything that surprised you (e.g., jest-expo preset rejecting a config option)
</output>
