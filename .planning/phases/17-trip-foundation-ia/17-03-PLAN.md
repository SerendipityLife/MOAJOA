---
phase: 17-trip-foundation-ia
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/migrations/0016_trips_baseline.sql
  - packages/api/src/queries/trips.ts
  - packages/api/src/queries/boards.ts
  - packages/api/src/queries/index.ts
  - packages/api/src/queries/links.ts
  - packages/api/src/queries/places.ts
  - packages/api/src/queries/memberships.ts
  - packages/api/src/types/database.ts
  - supabase/functions/extract-youtube/index.ts
autonomous: false
requirements: [SETUP-02, NAV-04]
must_haves:
  truths:
    - "supabase db reset applies 0016 alone and reproduces the full trips-native schema"
    - "trips table has representative_id auto-set to the creator on insert"
    - "All cross-table RLS checks go through SECURITY DEFINER helpers (no direct EXISTS) — no 42P17 recursion"
    - "join_shared_trip, ensure_share_slug, public_trip_view, vote_counts_for_places, add_manual_place all survive the squash"
    - "places.confidence (0006), places.source_kind + places.inferred_city (0004) survive the squash so api typecheck + the map port stay green"
    - "profiles_first_board_trigger is NOT ported — onboarding (Plan 05) owns first-trip creation so the 0-trip branch stays reachable"
    - "packages/api exposes listMyTrips/createTrip/getPublicTripBySlug with trip vocab"
    - "packages/api/src/types/database.ts is regenerated and references trips (not boards)"
  artifacts:
    - path: "supabase/migrations/0016_trips_baseline.sql"
      provides: "single trips-native baseline (squash of 0001-0015)"
      contains: "create table trips"
    - path: "packages/api/src/queries/trips.ts"
      provides: "trip CRUD + public_trip_view RPC"
      contains: "export async function listMyTrips"
  key_links:
    - from: "trips RLS policies"
      to: "am_trip_owner / am_trip_member / can_*_trip helpers"
      via: "SECURITY DEFINER helper call (never direct EXISTS)"
      pattern: "am_trip_member"
    - from: "packages/api/src/queries/trips.ts"
      to: "public_trip_view RPC"
      via: "client.rpc"
      pattern: "rpc\\('public_trip_view'"
---

<objective>
Squash migrations 0001–0015 into a single trips-native baseline (`0016_trips_baseline.sql`, D-03 — append-only intentionally overridden this once), porting EVERY SECURITY DEFINER helper / trigger / RPC / view to trip vocabulary without resurrecting RLS recursion, then rename the `packages/api` query layer to trip vocab, regenerate the database types, and repoint the extract-youtube Edge Function's `board_id` references. Includes the [BLOCKING] schema push so verification reflects the live database, not config-derived types.

Purpose: `trips` is the source of truth for the canonical `trip_id` identifier (Architectural Responsibility Map). The squash is the highest-regression-risk work in the phase (Pitfall 3/4): a direct cross-table EXISTS resurrects the 42P17 recursion 0002 fixed, and a dropped function/column silently breaks sharing/public-view/voting/auto-added-by or the downstream api typecheck + map port.
Output: a single 0016 baseline that `supabase db reset` applies cleanly; `packages/api` with trip-vocab queries; regenerated `database.ts`; repointed EF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-trip-foundation-ia/17-CONTEXT.md
@.planning/phases/17-trip-foundation-ia/17-RESEARCH.md
@.planning/phases/17-trip-foundation-ia/17-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Squash inventory (17-PATTERNS.md §0016 + RESEARCH §RLS helper transfer lines 442-453).
     EVERY item below MUST appear in 0016, renamed board→trip where noted. -->
<!--
 set_updated_at                 (0001:25-33)   reuse as-is
 handle_new_auth_user           (0001:63-88)   reuse as-is (DEFINER trigger fn)
 ensure_share_slug ×2 triggers  (0001:117-141) board→trip table refs
 am_board_owner/am_board_member (0002:18-48)   → am_trip_owner/am_trip_member (DEFINER)
 can_read/edit/vote_board       (0001:263-324) → can_*_trip (DEFINER)
 links/places/votes default fns (0001:248,373,427) board_id→trip_id
 join_shared_board              (0009:40-69)   → join_shared_trip (DEFINER write, slug bearer-invite)
 accepted_member_count          (0009:75-88)   reuse, board_id→trip_id (anon grant)
 vote_counts_for_places         (0001:472-485) reuse (anon grant)
 public_board_view              (0013:17-84 LATEST body w/ google_place_id/address/summary_ko)
                                               → public_trip_view (DO NOT use 0001's older body — Pitfall 4)
 add_manual_place               (0001:558-608) board_id→trip_id
 PostGIS geog generated col + GIST index (0001:358,371) reproduce verbatim
 representative_id auto-set      NEW           model on links_default_added_by:
                                               coalesce(new.representative_id, auth.uid())
-->
<!-- ⛔ INTENTIONALLY RETIRED — DO NOT PORT (decision recorded in Task 1 step 1):
 profiles_create_first_board / profiles_first_board_trigger  (0006:101-119, AFTER INSERT ON
   profiles, SECURITY DEFINER auto-creating "내 첫 여행")  → DROP from 0016.
   Rationale: ONBOARD-01's auto-board behavior is deliberately retired. Per CONTEXT D-11
   (onboarding "일정 정해졌나요?" branch is exposed) + NAV-01 (0 trips → onboarding), the
   onboarding "정해짐" creation path (Plan 05) is now the SOLE owner of first-trip creation.
   Porting this trigger would give every new signup exactly 1 trip → decideEntryRoute([1 trip])
   returns {kind:'trip'} → the 0-trip onboarding branch (Plan 05) becomes PERMANENTLY
   UNREACHABLE and Plan 04 Task 4 UAT scenario 1 (fresh account → 0 trips → onboarding) fails.
 profiles backfill insert (0006:121-129)  → DROP too (squash wipes all data, D-03 — nothing
   to backfill; new accounts get 0 trips by design). -->
<!-- Deltas folded from later migrations (PATTERNS.md line 68):
 0003 owner default · 0007 board start/end_date · 0010 extraction_started_at
 0008 summaries (links.summary_ko, places.summary_ko) · 0011 member count incl owner
 0012 vote upsert + owner self-join guard · 0013 public view place detail (latest view body)
 0014 profiles gender/birthday · 0015 place signature_menu
 ── places columns folded from EARLY-numbered migrations (Pitfall 4 — these are NOT in 0001
    and the 0013 view body REFERENCES them; dropping any breaks api typecheck + the map port):
 0004 places.source_kind  text not null default 'ai' check (source_kind in ('ai','manual'))   [0004:17-19]
 0004 places.inferred_city text                       (EXTRACT-03 — used by public view/map)   [0004:21-22]
 0005 (fold whatever extraction-hardening deltas 0005 adds — read it in read_first)
 0006 places.confidence   numeric(3,2) check (confidence is null or (confidence >= 0 and confidence <= 1))   [0006:15-17]
      └ copy the EXACT type + CHECK constraint verbatim from 0006:15-17.
        consumed by packages/api places.ts:121, the boards/[id].tsx:237 map port (→ map.tsx),
        pin-sheet.tsx:57. The 0013 public_trip_view body SELECTS source_kind + confidence +
        summary_ko + google_place_id + address — every one must be backed by a folded column. -->

<!-- api boards.ts exports to rename (signatures identical, vocab only):
 listMyBoards→listMyTrips, listMyBoardsWithPreview→listMyTripsWithPreview, getBoard→getTrip,
 createBoard→createTrip, updateBoard→updateTrip, deleteBoard→deleteTrip,
 shareBoard→shareTrip, getPublicBoardBySlug→getPublicTripBySlug -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 0016 trips-native squash baseline (RLS/trigger/view/join port)</name>
  <files>supabase/migrations/0016_trips_baseline.sql</files>
  <read_first>
    - supabase/migrations/0001_init.sql (entire — tables, triggers, RPCs, can_* helpers, geog col/index, add_manual_place, public_board_view base)
    - supabase/migrations/0002_fix_rls_recursion.sql (entire — am_board_owner/am_board_member canonical DEFINER bodies + the policies that consume them)
    - supabase/migrations/0004_extraction_hardening.sql (entire — places.source_kind 17-19, places.inferred_city 21-22 — MUST fold; do NOT need extraction_costs table for the api typecheck but fold it for fidelity)
    - supabase/migrations/0006_trust_ui_onboarding.sql (entire — places.confidence 15-17 type+CHECK to fold; profiles_create_first_board 101-119 + backfill 121-129 to RETIRE, NOT port)
    - supabase/migrations/0007_board_dates.sql (start_date/end_date columns)
    - supabase/migrations/0008_extraction_summaries.sql (links.summary_ko, places.summary_ko)
    - supabase/migrations/0009_join_shared_board.sql (join_shared_board, accepted_member_count)
    - supabase/migrations/0011_member_count_includes_owner.sql
    - supabase/migrations/0012_vote_upsert_and_owner_join_guard.sql
    - supabase/migrations/0013_public_view_place_detail.sql (entire — LATEST public_board_view body; use THIS, not 0001's. Confirm its place SELECT list — source_kind/confidence/summary_ko/google_place_id/address — is fully backed by folded columns)
    - supabase/migrations/0014_profile_gender_birthday.sql
    - supabase/migrations/0015_place_signature_menu.sql
    - supabase/migrations/0003_boards_owner_default.sql, 0005, 0010 (deltas to fold)
    - 17-PATTERNS.md §"0016_trips_baseline.sql" + Shared Patterns §1 (exact DEFINER helper idiom)
    - 17-RESEARCH.md §"RLS 헬퍼 이전" (lines 442-453) + Pitfall 3/4
    - CLAUDE.md §4.3 (migration rules) + §4.4 (RLS deny-by-default, helper-only cross-table)
  </read_first>
  <action>
    Build ONE file `supabase/migrations/0016_trips_baseline.sql` that, applied alone against an empty DB, reproduces the entire current schema with `boards`→`trips` renamed and `representative_id` added. This is the deliberate one-time append-only override (D-03); after this, append-only resumes.

    Steps:
    1. **Inventory cross-check FIRST + record two explicit decisions.** Build the function/trigger/RPC inventory from the <interfaces> block and the read_first migrations. Every item in the PORT list MUST land in 0016. Pay special attention to assets/columns that live in LATER (and EARLY non-0001) migrations, not 0001 (Pitfall 4): start/end_date (0007), summary_ko columns (0008), the latest public view body (0013), member-count-includes-owner (0011), vote upsert + owner self-join guard (0012), profile gender/birthday (0014), place signature_menu (0015), **places.source_kind + places.inferred_city (0004)**, **places.confidence (0006, exact type + CHECK)**.
       - **DECISION A — RETIRE the auto-first-board trigger (BLOCKER 1 resolution).** Do NOT port `profiles_create_first_board` / `profiles_first_board_trigger` (0006:101-119) or its profiles backfill insert (0006:121-129). Add a SQL comment in 0016 at the place where you would otherwise install it — but **do NOT write the literal function name in that comment** (the Task 1 grep gate asserts the name appears 0 times in the file). Use wording that omits the identifier, e.g. `-- (ONBOARD-01) auto "내 첫 여행" signup trigger intentionally NOT ported: the onboarding "정해짐" path (Plan 05) owns first-trip creation so decideEntryRoute keeps a reachable 0-trip → onboarding branch (NAV-01, D-11).` Rationale: porting it makes every new signup have exactly 1 trip, which permanently hides the onboarding branch and fails Plan 04 Task 4 UAT scenario 1.
       - **DECISION B — FOLD the 0004/0006 places columns.** `places.source_kind` (0004:17-19, `text not null default 'ai' check (source_kind in ('ai','manual'))`), `places.inferred_city` (0004:21-22, `text`), and `places.confidence` (0006:15-17, `numeric(3,2) check (confidence is null or (confidence >= 0 and confidence <= 1))`) must appear on the `places` table definition. The 0013 `public_trip_view` body selects all three (+ summary_ko + google_place_id + address); packages/api places.ts:121 and the map port (boards/[id].tsx:237 → map.tsx, pin-sheet.tsx:57) consume `confidence`. Copy types + CHECK constraints verbatim.

    2. **Tables** — port `profiles` (with 0014 gender/birthday), then `trips` (renamed from boards) ADD `representative_id uuid references profiles(id) on delete set null` (NULLABLE-safe via trigger below), keep all existing columns incl. start_date/end_date (0007), share_slug (8-32 check), city_code (<=20). Then `memberships`, `links` (incl. 0008 summary_ko + 0010 extraction_started_at), `places` (incl. 0004 source_kind + 0004 inferred_city + 0006 confidence + 0008 summary_ko + 0015 signature_menu + the PostGIS `geog` generated column and GIST index verbatim), `votes`. Rename every `board_id` FK column → `trip_id` across memberships/links/places/votes. ADD the empty `booking_clicks` table (D-07, Open Q2): `id uuid pk default gen_random_uuid()`, `trip_id uuid not null references trips(id) on delete cascade`, `place_id uuid references places(id) on delete set null`, `user_id uuid not null references profiles(id) on delete cascade`, `provider text not null`, `created_at timestamptz not null default now()`. Model its FK+RLS shape on the `votes` table (0001:414-465). RLS deny-by-default (owner-only read for now; minting/INSERT path = Phase 20).

    3. **Triggers/functions** — port every item in the PORT inventory (and explicitly NOT the retired trigger from Decision A). Use the EXACT DEFINER idiom (PATTERNS Shared Patterns §1), e.g.:
       ```sql
       create or replace function am_trip_owner(p_trip_id uuid)
       returns boolean language sql stable security definer set search_path = public as $$
         select exists(select 1 from trips where id = p_trip_id and owner_id = auth.uid());
       $$;
       grant execute on function am_trip_owner(uuid) to authenticated;
       ```
       Apply the same to `am_trip_member`, `can_read_trip`, `can_edit_trip`, `can_vote_trip`. Port `join_shared_board`→`join_shared_trip` (keep hard-coded `'voter'` role + `user_id = auth.uid()` + `on conflict do nothing` + 0012 owner self-join guard), `accepted_member_count` (0011 includes-owner variant), `vote_counts_for_places`, `public_board_view`→`public_trip_view` (use the 0013 body verbatim, renamed — its place SELECT must reference the folded source_kind/confidence/summary_ko/google_place_id/address columns), `add_manual_place` (board_id→trip_id), `ensure_share_slug` (×2 triggers), `links/places/votes` default-fk triggers (board_id→trip_id), `set_updated_at`, `handle_new_auth_user`.

    4. **representative_id auto-set trigger** (D-10, Open Q3): model on `links_default_added_by`:
       ```sql
       create or replace function trips_default_representative()
       returns trigger language plpgsql as $$
       begin
         new.representative_id := coalesce(new.representative_id, auth.uid());
         return new;
       end; $$;
       create trigger trips_representative_default before insert on trips
         for each row execute function trips_default_representative();
       ```

    5. **Policies** — every cross-table policy calls a helper, NEVER a direct `exists(select ... from <other table>)` (Pitfall 3 / CLAUDE.md §4.4). e.g. `using ( visibility in ('shared','public') and am_trip_member(id) )`.

    6. **Anon grants** — `public_trip_view`, `accepted_member_count`, `vote_counts_for_places` keep `grant execute ... to authenticated, anon` (public SSR read).

    Do NOT yet delete 0001-0015 in this task (Task 2 handles archival decision after a successful local reset). Do NOT regenerate types yet (Task 3, after push).
  </action>
  <verify>
    <automated>grep -c 'profiles_create_first_board' supabase/migrations/0016_trips_baseline.sql | grep -q '^0$' && echo "OK: auto-first-board trigger NOT ported (onboarding owns first-trip creation)" || echo "FAIL: profiles_create_first_board must NOT appear in 0016 (BLOCKER 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "create table trips" supabase/migrations/0016_trips_baseline.sql` returns 1
    - `grep -c "representative_id" supabase/migrations/0016_trips_baseline.sql` >= 2 (column + trigger)
    - `grep -c "create table booking_clicks" supabase/migrations/0016_trips_baseline.sql` returns 1
    - `grep -c "public_trip_view" supabase/migrations/0016_trips_baseline.sql` >= 1 AND `grep -c "public_board_view" supabase/migrations/0016_trips_baseline.sql` returns 0
    - `grep -c "join_shared_trip" supabase/migrations/0016_trips_baseline.sql` >= 1
    - `grep -cE "am_trip_owner|am_trip_member|can_read_trip|can_edit_trip|can_vote_trip" supabase/migrations/0016_trips_baseline.sql` >= 5
    - `grep -c "security definer" supabase/migrations/0016_trips_baseline.sql` >= 8 AND `grep -c "set search_path = public" supabase/migrations/0016_trips_baseline.sql` >= 8
    - `grep -c "summary_ko" supabase/migrations/0016_trips_baseline.sql` >= 2 (0008 folded) AND `grep -c "signature_menu" supabase/migrations/0016_trips_baseline.sql` >= 1 (0015 folded)
    - `grep -c "confidence" supabase/migrations/0016_trips_baseline.sql` >= 1 (0006 folded — column + 0013 view ref) AND `grep -c "source_kind" supabase/migrations/0016_trips_baseline.sql` >= 1 (0004 folded) AND `grep -c "inferred_city" supabase/migrations/0016_trips_baseline.sql` >= 1 (0004 folded)
    - `grep -c "geography\|geog" supabase/migrations/0016_trips_baseline.sql` >= 1 (PostGIS col) AND `grep -ci "gist" supabase/migrations/0016_trips_baseline.sql` >= 1 (GIST index)
    - `grep -ci "board_id" supabase/migrations/0016_trips_baseline.sql` returns 0 (all FKs renamed to trip_id)
    - `grep -ci "profiles_create_first_board" supabase/migrations/0016_trips_baseline.sql` returns 0 (BLOCKER 1 — auto-first-board trigger intentionally retired)
  </acceptance_criteria>
  <done>0016 baseline contains the full trips-native schema with all helpers/triggers/RPCs/view ported, representative auto-trigger, empty booking_clicks, folded places columns (source_kind/inferred_city/confidence), zero board_id refs, zero direct cross-table EXISTS in policies, and the profiles auto-first-board trigger intentionally NOT ported (onboarding owns first-trip creation).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Apply squash — local reset, archive 0001-0015, regenerate types</name>
  <read_first>
    - 17-RESEARCH.md §"squash 마이그레이션 워크플로우" (lines 425-440) + §"Don't Hand-Roll" (db dump / migration repair / supabase:types)
    - 17-RESEARCH.md §Open Questions Q1 (archive vs delete 0001-0015) + Environment Availability (Docker/linked project)
    - root package.json (supabase:reset = `supabase db reset`, supabase:types = `supabase gen types typescript --local > packages/api/src/types/database.ts`)
    - CLAUDE.md §4.3 (after schema change: regenerate types)
  </read_first>
  <what-built>
    0016_trips_baseline.sql (Task 1) is the single trips-native squash. This step applies it locally, resolves the old migration files, and regenerates the typed database client — the [BLOCKING] schema push required by the phase gate (build/type checks pass without it, creating a false-positive verification state). The local `supabase db reset` is the REAL 42P17 recursion gate (a direct cross-table EXISTS in a policy errors here) and the REAL dropped-object gate (a missing folded column/function errors here — Pitfall 4).
  </what-built>
  <action>
    This task is `checkpoint:human-action` because a squash reset is destructive (drops local data, requires the linked remote reset confirmation) and Docker/Supabase CLI auth may need an interactive prompt. The executor automates everything it can via Bash and pauses only for the destructive remote confirmation.

    AUTOMATE (executor runs):
    1. **Decide old-file handling (Open Q1).** Recommended (D-03 spirit, cleanest history): move `0001_*`–`0015_*` into `supabase/migrations/_archive/` so `supabase migration list` sees only `0016` going forward. Keep them in git history. (If `supabase db reset` requires a contiguous history that breaks with archived files, fall back to leaving them and declaring 0016 the new baseline — document the choice in SUMMARY.)
    2. **Local reset to validate 0016 alone reproduces the schema:** `pnpm supabase:reset` (= `supabase db reset`). Must succeed with only 0016 (+ archived olds out of the active set). This is the authoritative 42P17 gate (a direct cross-table EXISTS in a policy raises `infinite recursion detected ... 42P17` here) and the authoritative dropped-object gate. If it fails on a missing object/column, return to Task 1 — an inventory item was dropped (Pitfall 4; common culprits: the folded 0004/0006 places columns, or a 0013-view column with no backing column).
    3. **Regenerate types:** `pnpm supabase:types`. This rewrites `packages/api/src/types/database.ts` from the local DB (now trips-native). ⚠️ Known hazard (STATE.md Phase 5 note): if the shell redirect runs while the local DB is down it can ZERO the file — verify the file is non-empty and contains `trips` after regen; if zeroed, `git checkout` and re-run after `supabase start`.

    PAUSE for human (remote reset — destructive, D-03 approved, external users = 0):
    4. Run the linked remote reset to the 0016 baseline. Exact `supabase migration repair` / push order depends on the linked project's `schema_migrations` state (RESEARCH A3 — confirm at this point). Provide the human the precise command sequence you intend to run and the confirmation it requires; the human authorizes the destructive remote reset. Windows teammate must also `supabase db reset` locally.
  </action>
  <how-to-verify>
    1. `supabase db reset` (local) completes with 0016 as the sole active baseline — no errors about missing functions/columns and no 42P17 recursion error.
    2. `packages/api/src/types/database.ts` is non-empty and `grep -c "trips" packages/api/src/types/database.ts` >= 1, `grep -ci "boards" packages/api/src/types/database.ts` returns 0 (table renamed).
    3. `grep -c "confidence" packages/api/src/types/database.ts` >= 1 AND `grep -c "source_kind" packages/api/src/types/database.ts` >= 1 (folded places columns survived the reset and are in the generated types).
    4. Remote linked DB reset confirmed by human; `supabase migration list` shows 0016 baseline reconciled.
  </how-to-verify>
  <acceptance_criteria>
    - `supabase db reset` exits 0 with only the 0016 baseline active, no 42P17 recursion (manual/CLI evidence in SUMMARY)
    - `packages/api/src/types/database.ts` regenerated: `grep -ci "boards" packages/api/src/types/database.ts` returns 0 AND file is non-empty (> 100 lines) AND contains `confidence` + `source_kind`
    - Human confirmed remote reset (or explicitly deferred remote with reason logged in SUMMARY)
  </acceptance_criteria>
  <resume-signal>Type "applied" once local reset + types regen succeeded and remote reset is authorized/deferred.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Rename packages/api query layer + repoint extract-youtube EF to trip vocab</name>
  <files>packages/api/src/queries/trips.ts, packages/api/src/queries/boards.ts, packages/api/src/queries/index.ts, packages/api/src/queries/links.ts, packages/api/src/queries/places.ts, packages/api/src/queries/memberships.ts, packages/api/src/types/database.ts, supabase/functions/extract-youtube/index.ts</files>
  <read_first>
    - packages/api/src/queries/boards.ts (entire — listMyBoards/listMyBoardsWithPreview/getBoard/createBoard/updateBoard/deleteBoard/shareBoard/getPublicBoardBySlug)
    - packages/api/src/queries/index.ts (barrel — line 1 `export * from './boards'`)
    - packages/api/src/queries/links.ts, places.ts (entire — board_id references + the places.ts:121 confidence read), memberships.ts (board_id references to rename)
    - supabase/functions/extract-youtube/index.ts (board_id / boards references)
    - 17-PATTERNS.md §"packages/api/src/queries/trips.ts" (query/RPC/share idioms with line anchors)
    - packages/api/src/types/database.ts (regenerated in Task 2 — trip types now available, incl. confidence/source_kind/inferred_city columns)
  </read_first>
  <action>
    Rename the query layer to trip vocab (signatures identical, RESEARCH: "시그니처 동일") and repoint the EF. This task runs AFTER Task 2 so the regenerated `database.ts` trip types exist (including the folded confidence/source_kind/inferred_city place columns — places.ts must still typecheck against them).

    1. **Create `packages/api/src/queries/trips.ts`** from `boards.ts`, renaming: `.from('boards')`→`.from('trips')`; `listMyBoards`→`listMyTrips`, `listMyBoardsWithPreview`→`listMyTripsWithPreview`, `getBoard`→`getTrip`, `createBoard`→`createTrip`, `updateBoard`→`updateTrip`, `deleteBoard`→`deleteTrip`, `shareBoard`→`shareTrip`, `getPublicBoardBySlug`→`getPublicTripBySlug`. Import `Trip`/`TripCreate` from `@moajoa/core` (Plan 01). The public-view call becomes `client.rpc('public_trip_view', { p_slug: slug })`. `shareTrip` relies on the `ensure_share_slug` trigger (preserved by squash). `createTrip` lets the `trips_default_representative` trigger set representative_id (no client field needed — SETUP-02).
    2. **Delete `packages/api/src/queries/boards.ts`** (clean break, D-13).
    3. **Update `packages/api/src/queries/index.ts`**: `export * from './boards'` → `export * from './trips'`.
    4. **Rename `board_id`→`trip_id`** in `links.ts`, `places.ts`, `memberships.ts` query payloads/filters (match the renamed columns from 0016). The `places.ts:121` `confidence` read is unchanged (the column was folded in 0016, Task 1 Decision B) — confirm it still resolves against the regenerated types.
    5. **Repoint `supabase/functions/extract-youtube/index.ts`**: every `board_id` → `trip_id`, every `.from('boards')` → `.from('trips')`. Surgical — only the table/column refs (CLAUDE.md §3.3). The Realtime broadcast channel `extract:{link_id}` is unchanged (link-scoped, not board-scoped).
    6. No `.js` extension on workspace imports (CLAUDE.md §4.5).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function listMyTrips" packages/api/src/queries/trips.ts` returns 1
    - `grep -c "export async function createTrip" packages/api/src/queries/trips.ts` returns 1
    - `grep -c "rpc('public_trip_view'" packages/api/src/queries/trips.ts` returns 1
    - `test ! -f packages/api/src/queries/boards.ts` (removed)
    - `grep -c "export \* from './trips'" packages/api/src/queries/index.ts` returns 1
    - `grep -rci "board_id" packages/api/src/queries/ supabase/functions/extract-youtube/index.ts` returns 0
    - `pnpm --filter @moajoa/api typecheck` exits 0 (places.ts confidence/source_kind reads resolve against folded columns)
  </acceptance_criteria>
  <done>packages/api exposes trip-vocab queries against the regenerated trip types; extract-youtube EF uses trip_id; typecheck clean (folded places columns resolve).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon (unauthenticated SSR) → public_trip_view | public share read; only the curated view is anon-grantable |
| authenticated user → trips/places/votes RLS | cross-table access mediated by DEFINER helpers |
| shared-link bearer → join_shared_trip | invite acceptance escalates membership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06 | Elevation of Privilege / DoS | trips/memberships RLS recursion (42P17) | mitigate | All cross-table policy checks call `am_trip_owner`/`am_trip_member`/`can_*_trip` SECURITY DEFINER helpers with `set search_path = public` — never a direct EXISTS against another table (CLAUDE.md §4.4, 0002 pattern). The Task 2 `supabase db reset` is the authoritative 42P17 gate (recursion errors at apply time). |
| T-17-07 | Information Disclosure | over-permissive read on trips/places/booking_clicks | mitigate | deny-by-default RLS on every new table. `booking_clicks` is owner-read only this phase (no INSERT path until Phase 20). Anon can reach only `public_trip_view`/`accepted_member_count`/`vote_counts_for_places` via explicit grants. |
| T-17-08 | Tampering / Spoofing | join_shared_trip membership escalation | mitigate | Ported as DEFINER with hard-coded `'voter'` role, `user_id = auth.uid()`, `on conflict do nothing`, and the 0012 owner self-join guard — bearer-invite model unchanged, no role injection. |
| T-17-09 | Information Disclosure | regenerated database.ts / public view leaks extra columns | mitigate | `public_trip_view` uses the 0013 curated column set (google_place_id/address/summary_ko/source_kind/confidence — all backed by folded columns) — the same fields already public; no new columns exposed by the rename. |
</threat_model>

<verification>
- `supabase db reset` succeeds with 0016 as the sole baseline, no 42P17 recursion (Task 2 evidence)
- `pnpm supabase:types` produces a trips-native `database.ts` (no `boards`; has `confidence`/`source_kind`)
- `pnpm --filter @moajoa/api typecheck` clean
- `grep -rci "board_id" supabase/migrations/0016_trips_baseline.sql packages/api/src/queries/ supabase/functions/extract-youtube/index.ts` returns 0
- `grep -ci "profiles_create_first_board" supabase/migrations/0016_trips_baseline.sql` returns 0 (auto-first-board retired)
- No direct cross-table EXISTS in any 0016 policy body (helpers only — 42P17 guard)
</verification>

<success_criteria>
- 0016 single baseline reproduces the full trips-native schema (D-03), all helpers/triggers/RPCs/view + folded places columns (source_kind/inferred_city/confidence) ported (Pitfall 4 — none dropped)
- profiles auto-first-board trigger intentionally retired so the 0-trip onboarding branch stays reachable (BLOCKER 1; onboarding Plan 05 owns first-trip creation per D-11/NAV-01)
- representative_id auto-set on insert (SETUP-02)
- RLS uses DEFINER helpers exclusively for cross-table checks (Pitfall 3 — no 42P17)
- packages/api trip-vocab queries + regenerated types + EF repointed (NAV-04 route hygiene at the data layer)
- empty booking_clicks table present for Phase 20 (D-07)
</success_criteria>

<output>
After completion, create `.planning/phases/17-trip-foundation-ia/17-03-SUMMARY.md`
</output>
</content>
</invoke>
