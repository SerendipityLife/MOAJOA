---
phase: 21-travel-ledger
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - workers/inbound-email/src/index.ts
  - workers/inbound-email/wrangler.toml
  - workers/inbound-email/package.json
  - supabase/functions/inbound-email/index.ts
  - supabase/functions/inbound-email/deno.json
  - supabase/functions/parse-email/index.ts
  - supabase/functions/parse-email/pipeline/mail.ts
  - supabase/functions/parse-email/pipeline/claude.ts
  - supabase/functions/parse-email/pipeline/fx.ts
  - supabase/functions/parse-email/pipeline/mail.test.ts
  - supabase/functions/parse-email/pipeline/claude.test.ts
  - supabase/functions/parse-email/pipeline/fx.test.ts
  - supabase/functions/parse-email/deno.json
  - supabase/config.toml
autonomous: false
requirements: [LEDGER-02, LEDGER-03, LEDGER-04, LEDGER-05]
user_setup:
  - service: cloudflare (사용자 계정)
    why: "Email Worker 배포 + Email Routing 활성화 = moajoa.app DNS를 Cloudflare 네임서버로 이전해야 가능. end-to-end 메일 UAT의 전제"
    env_vars:
      - name: INGEST_SECRET
        source: "Claude 생성 랜덤값 → wrangler secret put + supabase functions secrets set 양쪽 동일값"
      - name: (기존) ANTHROPIC_API_KEY
        source: "Supabase function secrets 기존 값 재사용"
      - name: EXPO_PUBLIC_FORWARDING_DOMAIN
        source: "예: ledger.moajoa.app 또는 moajoa.app (catch-all 방식 실측 후)"
must_haves:
  truths:
    - "inbound-email EF가 x-ingest-secret 미일치를 401로 거부하고, To 토큰 미매칭을 drop(202)하며, 매칭 시 ledger_entries status='pending'을 INSERT한다 (LEDGER-05)"
    - "parse-email EF가 atomic claim(pending→processing) 후 postal-mime 파싱 + claude 추출 + 환율 결정 + trip 매칭으로 status='ready'|'needs_review'|'failed'로 전이한다 (LEDGER-02/04)"
    - "환율은 메일 명시값 있으면 fx_source='email', 없으면 Frankfurter(결제일) fx_source='frankfurter', 실패시 'unavailable'로 5요소 원자 저장된다 (LEDGER-03)"
    - "CF Email Worker가 raw MIME + 봉투 메타를 x-ingest-secret 헤더로 inbound-email EF에 POST한다"
    - "config.toml에 inbound-email/parse-email verify_jwt=false가 설정된다"
  artifacts:
    - "workers/inbound-email/ (Worker + wrangler.toml)"
    - "supabase/functions/{inbound-email,parse-email}/"
    - "supabase/config.toml (수정)"
  key_links:
    - "Worker → inbound-email EF (x-ingest-secret 공유)"
    - "inbound-email → forwarding_addresses 조회(To 토큰) → ledger_entries INSERT → parse-email fire-and-forget"
    - "parse-email/pipeline/claude.ts → generate-plan/pipeline/claude.ts verbatim(프롬프트 교체) + LedgerParseOutput(21-02)"
    - "parse-email/pipeline/fx.ts → routes.ts null-on-failure idiom + Frankfurter"
---

<objective>
인바운드 메일 파이프라인 구축 — 얇은 CF Email Worker(raw→EF) + `inbound-email` EF(시크릿+To토큰 게이트+저장) + `parse-email` EF(postal-mime+claude+환율+trip매칭). extract-youtube 골격 + generate-plan claude.ts/routes.ts를 재활용하고 프롬프트/파서만 교체한다 (LEDGER-02/03/04/05 핵심).

Purpose: 이 파이프라인이 가계부의 자동 입력 경로 전부. 등록 주소(To 토큰)+SPF/DKIM(CF 수신 거부)만 처리(LEDGER-05), 앱 미경유 예약도 메일만 오면 포착(LEDGER-04), 환율 5요소 원자 저장(LEDGER-03). claude 재활용으로 신규 LLM 도입 0.
Output: Worker + 2 EF + pipeline 3파일 + deno 테스트 + config.toml. deno check/test green. end-to-end 메일은 human-action(DNS 이전 후).
</objective>

<execution_context>
GSD execute-plan 부재 — 수동. EF는 Deno(jsr/npm import). 서비스롤 EF만(§4.4). 실값 env 배선, 하드코딩 금지(§4.7). verify_jwt=false는 자체 시크릿으로 보완(Pitfall 7).
</execution_context>

<context>
@.planning/phases/21-travel-ledger/21-RESEARCH.md
@.planning/phases/21-travel-ledger/21-PATTERNS.md
@supabase/functions/extract-youtube/index.ts
@supabase/functions/generate-plan/index.ts
@supabase/functions/generate-plan/pipeline/claude.ts
@supabase/functions/generate-plan/pipeline/routes.ts
</context>

## Artifacts this plan produces
- `workers/inbound-email/` — 얇은 Worker(raw+봉투→EF POST) + wrangler.toml + package.json
- `supabase/functions/inbound-email/index.ts` — 시크릿 검증 + To 토큰 매칭 + 저장 + parse 트리거
- `supabase/functions/parse-email/` — index.ts(claim+오케스트레이션) + pipeline/{mail.ts, claude.ts, fx.ts} + 테스트 3
- `supabase/config.toml` — [functions.inbound-email]/[functions.parse-email] verify_jwt=false

<tasks>

<task type="auto">
  <name>Task 1: parse-email pipeline (mail/claude/fx) + deno 테스트 (RED→GREEN)</name>
  <files>supabase/functions/parse-email/pipeline/mail.ts, .../claude.ts, .../fx.ts, .../mail.test.ts, .../claude.test.ts, .../fx.test.ts, supabase/functions/parse-email/deno.json</files>
  <read_first>
    - supabase/functions/generate-plan/pipeline/claude.ts (호출 헬퍼 verbatim 대상: PLAN_MODEL·fetch·temperature 0·extractJsonBlock·Zod parse·usage ?? 0·validatePlanIds)
    - supabase/functions/generate-plan/pipeline/routes.ts (null-on-failure: try/catch→null, !ok→null, 옵셔널체이닝)
    - supabase/functions/generate-plan/pipeline/claude.test.ts + routes.test.ts (deno 테스트 규약)
    - supabase/functions/extract-youtube/deno.json (imports)
    - .planning/phases/21-travel-ledger/21-PATTERNS.md §parse-email
    - packages/core/src/schemas/ledger.ts (LedgerParseOutput — EF서 로컬 재선언할 정본, @moajoa/core는 Deno import 불가라 미러)
  </read_first>
  <action>
    deno.json = extract-youtube 복사 + `"postal-mime": "npm:postal-mime@2"` import 추가.

    **mail.ts:** `parseMime(raw: string): Promise<{subject, from, text, html, date}>` — `import PostalMime from 'postal-mime'` → `PostalMime.parse(raw)` → 필드 추출. text 우선, 없으면 html→간단 strip. forwarded 래핑은 그대로 claude에 넘김(정제 최소).

    **claude.ts:** generate-plan claude.ts **verbatim 미러** — `LEDGER_MODEL='claude-sonnet-4-6'`, fetch(anthropic messages, x-api-key, anthropic-version 2023-06-01, temperature 0, max_tokens 2048), extractJsonBlock, `stop_reason==='max_tokens'` throw, usage `?? 0`. **교체:**
    - SYSTEM_PROMPT = "너는 예약/카드 결제 알림 메일에서 결제 정보를 추출한다. 플랫폼(Klook/Agoda/항공사/카드사 등)·카드 끝 4자리·통화(ISO 3자)·금액·결제일·(메일에 있으면)원화 청구액·청구환율을 뽑는다. 사용자 여행 목록이 주어지면 이 결제가 어느 여행인지 확신 있을 때만 matched_trip_id에 그 id를, 애매하면 null. 값이 없으면 null. JSON만 출력."
    - `buildLedgerPrompt(mail, trips): string`(export, 테스트가 fragment 단언) — 메일 subject/text + 사용자 trip 목록(id/title/city/start~end) 주입.
    - `LedgerParseOutput` zod 로컬 재선언(21-02 정본 미러) + `parseLedgerOutput(text): LedgerParseOutput`.
    - `validateTripId(inputTripIds: string[], matched: string|null): string|null` — validatePlanIds idiom: matched가 입력 trip 교집합에 없으면 null(환각 방어, T-21-11).

    **fx.ts:** `resolveFx(currency, paidDate, mailKrw, mailRate): Promise<{fx_rate:number|null, fx_source:'email'|'frankfurter'|'unavailable', fx_as_of:string|null, amount_krw:number|null}>`:
    - mailKrw/mailRate 있으면 → fx_source='email', fx_rate=mailRate ?? (mailKrw/amountForeign 역산), amount_krw=mailKrw, fx_as_of=paidDate.
    - 없으면 `fetch('https://api.frankfurter.dev/v1/'+paidDate+'?base='+currency+'&symbols=KRW')` — routes.ts null-on-failure(try/catch→null, !ok→null, `data?.rates?.KRW` 옵셔널). 성공 → fx_source='frankfurter', fx_rate=rates.KRW, **fx_as_of=data.date(응답 date, 주말이면 직전영업일)**, amount_krw=round(amount*rate).
    - 실패 → fx_source='unavailable', fx_rate=null, amount_krw=null.
    - currency==='KRW'면 fetch 없이 fx_rate=1 처리.

    **테스트(deno):** mail.test.ts(postal-mime fixture 파싱 — 간단 MIME 문자열); claude.test.ts(buildLedgerPrompt fragment 단언 + parseLedgerOutput fence-strip + validateTripId 교집합/환각 null); fx.test.ts(mail값 우선→'email' / API 성공→'frankfurter' + fx_as_of=응답date / API 실패→'unavailable' / KRW→rate 1). Frankfurter fetch는 globalThis.fetch stub.
  </action>
  <verify>
    <automated>cd supabase/functions/parse-email && deno test --allow-net --allow-env pipeline/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "claude-sonnet-4-6" supabase/functions/parse-email/pipeline/claude.ts` >= 1
    - `grep -c "temperature: 0" supabase/functions/parse-email/pipeline/claude.ts` >= 1
    - `grep -c "frankfurter" supabase/functions/parse-email/pipeline/fx.ts` >= 1
    - `grep -c "data.date\|\.date" supabase/functions/parse-email/pipeline/fx.ts` >= 1 (fx_as_of=응답date)
    - `deno test` 전 케이스 green (mail/claude/fx)
    - fx.ts null-on-failure: API 실패시 unavailable 반환 단언 통과
  </acceptance_criteria>
  <done>pipeline 3파일이 generate-plan idiom 미러 + 프롬프트/파서 교체, deno 테스트 green</done>
</task>

<task type="auto">
  <name>Task 2: inbound-email EF (시크릿 + To 토큰 게이트 + 저장 + 트리거)</name>
  <files>supabase/functions/inbound-email/index.ts, supabase/functions/inbound-email/deno.json, supabase/config.toml</files>
  <read_first>
    - supabase/functions/extract-youtube/index.ts (Deno.serve + OPTIONS/POST + corsHeaders/jsonError/jsonOk + admin service-role)
    - .planning/phases/21-travel-ledger/21-PATTERNS.md §inbound-email
    - supabase/config.toml (functions 블록 위치)
  </read_first>
  <action>
    extract-youtube 골격 재활용. **차이:**
    - auth.getUser 대신 `if (req.headers.get('x-ingest-secret') !== Deno.env.get('INGEST_SECRET')) return jsonError(401,'unauthorized')` (INGEST_SECRET 미설정 시 500). verify_jwt=false.
    - Zod: `{ to: z.string(), from: z.string(), rawSize: z.number(), raw: z.string() }`.
    - To 토큰: local-part 추출(`to.split('@')[0]`; `+` 서브어드레싱이면 `+` 앞) → `admin.from('forwarding_addresses').select('user_id').eq('token', token).maybeSingle()`. **미매칭 → 202 jsonOk({status:'ignored'})** (drop, 존재여부 미노출 — 항상 202로 일관, T-21-12).
    - 매칭 → `admin.from('ledger_entries').insert({ owner_user_id: row.user_id, status:'pending', platform: <from 도메인 힌트 nullable>, raw_mime: raw, raw_expires_at: new Date(Date.now()+7*24*3600*1000).toISOString() }).select('id').single()`.
    - **parse-email fire-and-forget:** `fetch(Deno.env.get('PARSE_EMAIL_URL'), {method:'POST', headers:{'content-type':'application/json','x-ingest-secret':Deno.env.get('INGEST_SECRET')}, body: JSON.stringify({entry_id: entry.id})}).catch(()=>{})` **no await** (Pitfall 5). PARSE_EMAIL_URL 미설정 시 조용히 skip(로컬).
    - 200 jsonOk({status:'accepted', entry_id}).
    deno.json = extract-youtube 복사(postal-mime 불필요 — 파싱은 parse-email).
    **config.toml:** `[functions.inbound-email]\nverify_jwt = false` + `[functions.parse-email]\nverify_jwt = false` 추가(extract-youtube 블록 무수정).
  </action>
  <verify>
    <automated>cd supabase/functions/inbound-email && deno check index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "x-ingest-secret" supabase/functions/inbound-email/index.ts` >= 2 (검증 + 트리거 전달)
    - `grep -c "auth.getUser" supabase/functions/inbound-email/index.ts` == 0 (시크릿 게이트지 유저 게이트 아님)
    - `grep -c "forwarding_addresses" supabase/functions/inbound-email/index.ts` >= 1
    - `grep -cE "await fetch\(.*parse" supabase/functions/inbound-email/index.ts` == 0 (fire-and-forget — await 금지)
    - `grep -c "verify_jwt = false" supabase/config.toml` >= 2
    - `deno check index.ts` clean
  </acceptance_criteria>
  <done>inbound-email이 시크릿·To토큰 게이트 + 저장 + 논블로킹 트리거, config verify_jwt=false</done>
</task>

<task type="auto">
  <name>Task 3: parse-email EF index (claim + 오케스트레이션)</name>
  <files>supabase/functions/parse-email/index.ts</files>
  <read_first>
    - supabase/functions/generate-plan/index.ts (atomic claim + 멱등 + 서비스롤 + broadcast)
    - supabase/functions/extract-youtube/index.ts (claim UPDATE .or 조건 + cost 로깅)
    - Task 1 pipeline (mail/claude/fx 시그니처)
  </read_first>
  <action>
    - 시크릿 게이트(inbound과 동일 x-ingest-secret) + Zod `{entry_id: z.string().uuid()}`.
    - **atomic claim**: `admin.from('ledger_entries').update({status:'processing'}).eq('id',entry_id).in('status',['pending','failed']).select('*').single()` — 0행이면 409 already processing.
    - entry에서 raw_mime + owner_user_id 로드. `parseMime(raw_mime)`.
    - 사용자 trip 목록 로드: `admin.from('trips').select('id,title,city_code,start_date,end_date').or('owner_id.eq.'+owner_user_id+',...')` — owner + accepted 멤버 trip(can_read_trip service-role 동치; generate-plan can_edit_trip 동치 idiom 참고, 여기선 read 범위). 실무 단순화: owner 트립 우선(멤버 트립까지 매칭 대상 확장은 planner 재량).
    - `buildLedgerPrompt(mail, trips)` → `callClaudeLedger` → `parseLedgerOutput` → `validateTripId(trips.map(id), out.matched_trip_id)`.
    - `resolveFx(out.currency, out.paid_at, out.krw_amount, out.fx_rate)`.
    - status 결정: `out.confidence==='high' && out.amount_foreign!=null && out.currency!=null ? 'ready' : 'needs_review'`.
    - UPDATE ledger_entries: 5요소 + amount_krw + platform/merchant/card_last4/paid_at + trip_id(validated) + status + **raw_mime=null**(파싱 후 폐기, D-03).
    - extraction_costs 로깅(provider='anthropic', link_id=null — 기존 CHECK, 확장 불필요; frankfurter 무료 미로깅).
    - 에러 → status='failed'(extract-youtube catch idiom), 부분값 보존.
    - broadcast는 선택(파싱 짧음 — 생략 가능, 클라 AppState refetch로 충분). planner 판단.
  </action>
  <verify>
    <automated>cd supabase/functions/parse-email && deno check index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "in('status'\|\.in(\"status\"" supabase/functions/parse-email/index.ts` >= 1 (atomic claim)
    - `grep -c "needs_review" supabase/functions/parse-email/index.ts` >= 1 (confidence 분기)
    - `grep -c "raw_mime: null\|raw_mime:null" supabase/functions/parse-email/index.ts` >= 1 (파싱 후 폐기 D-03)
    - `grep -c "validateTripId" supabase/functions/parse-email/index.ts` >= 1 (환각 trip 방어)
    - `deno check index.ts` clean
  </acceptance_criteria>
  <done>parse-email이 claim→파싱→환율→매칭→상태전이, raw 폐기, cost 로깅</done>
</task>

<task type="auto">
  <name>Task 4: CF Email Worker (얇은 raw→EF)</name>
  <files>workers/inbound-email/src/index.ts, workers/inbound-email/wrangler.toml, workers/inbound-email/package.json</files>
  <read_first>
    - .planning/phases/21-travel-ledger/21-RESEARCH.md §"Pattern 1: 얇은 Worker → EF"
  </read_first>
  <action>
    `workers/inbound-email/` 신설(레포 내 코드 커밋, 시크릿은 CF):
    - `src/index.ts`: `export default { async email(message, env, ctx) {...} }` — rawSize 가드(>5MB → setReject), `const raw = await new Response(message.raw).text()`, `await fetch(env.INBOUND_EF_URL, {method:'POST', headers:{'content-type':'application/json','x-ingest-secret':env.INGEST_SECRET}, body: JSON.stringify({to:message.to, from:message.from, rawSize:message.rawSize, raw})})`. Worker엔 DB·claude 접근 0(INBOUND_EF_URL + INGEST_SECRET만).
    - `wrangler.toml`: name/main/compatibility_date. Email 트리거는 CF 대시보드 Email Routing 룰에서 지정(catch-all 또는 서브도메인 — Task 5 실측). vars/secret 주석.
    - `package.json`: 최소(wrangler devDep). postal-mime 미사용(EF서 파싱).
  </action>
  <verify>
    <automated>test -f workers/inbound-email/src/index.ts && test -f workers/inbound-email/wrangler.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "async email" workers/inbound-email/src/index.ts` >= 1
    - `grep -c "x-ingest-secret" workers/inbound-email/src/index.ts` >= 1
    - `grep -cE "ANTHROPIC|claude|supabase.*key|service_role" workers/inbound-email/src/index.ts` == 0 (얇은 Worker — 시크릿/DB 없음)
    - wrangler.toml 존재
  </acceptance_criteria>
  <done>얇은 Worker가 raw+봉투를 시크릿 헤더로 EF POST, DB/LLM 접근 0</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: [BLOCKING] CF 인프라 배포 + DNS 이전 + 시크릿</name>
  <files>(인프라 게이트 — 코드 변경 없음)</files>
  <read_first>
    - .planning/phases/21-travel-ledger/21-RESEARCH.md §Environment Availability + Pitfall 8
  </read_first>
  <what-built>Worker + 2 EF + config 완성. end-to-end 메일 수신은 CF 인프라가 있어야 검증 가능.</what-built>
  <action>사용자 수행(응답 대기):
    1. moajoa.app DNS를 Cloudflare 네임서버로 이전(기존 Vercel 웹 레코드 CF에 복제 → 무중단). 
    2. CF 대시보드 Email Routing 활성화 + catch-all(apex) 또는 서브도메인(ledger.moajoa.app) 룰 → Worker 지정. **catch-all은 zone(apex)만 지원 — 서브도메인은 Worker 처리**(RESEARCH A6) 실측 후 EXPO_PUBLIC_FORWARDING_DOMAIN 확정.
    3. INGEST_SECRET 랜덤 생성(Claude 제공) → `wrangler secret put INGEST_SECRET`(Worker) + `supabase secrets set INGEST_SECRET=...`(EF) 동일값. + INBOUND_EF_URL/PARSE_EMAIL_URL(배포 EF URL) Worker/EF env.
    4. EF 배포: `supabase functions deploy inbound-email parse-email` (verify_jwt=false는 config.toml 반영). `wrangler deploy`(Worker).
    5. 스모크: 본인 예약 메일 1건을 `{token}@{forwardingDomain}`로 전달 → ledger_entries 행 생성 + parse-email이 ready/needs_review로 전이 확인.
  </action>
  <verify>사용자로부터 "CF 배포·스모크 완료" 또는 "코드만 커밋(인프라 후속)" 수신</verify>
  <acceptance_criteria>파이프라인 배포 여부 확정 — end-to-end UAT는 phase verify로 이관 가능</acceptance_criteria>
  <done>CF 인프라 배포 또는 후속 이관 결정</done>
  <resume-signal>"CF 배포 완료" 또는 "코드만 커밋"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 외부 메일 발신자 → CF Email Routing | SPF/DKIM 실패 거부(CF 인프라) |
| CF Worker → inbound-email EF | x-ingest-secret 공유(verify_jwt=false 보완) |
| 메일 본문 → claude LLM | 프롬프트 인젝션 표면 |
| parse-email(service-role) → ledger_entries | 유일 INSERT/UPDATE 경로 |

## STRIDE Threat Register
| Threat | Category | Severity | Mitigation |
|--------|----------|----------|------------|
| T-21-11 | Tampering | high | 메일 프롬프트 인젝션으로 matched_trip_id 환각 → validateTripId 교집합만(validatePlanIds idiom) + Zod 강제 + temperature 0 |
| T-21-12 | Info Disclosure | medium | To 토큰 존재여부 probing → 매칭/미매칭 모두 202(타이밍 최소·존재 미노출) |
| T-21-13 | Spoofing | high | inbound EF 무단 호출(가짜 결제 주입) → x-ingest-secret 미일치 401 |
| T-21-14 | Spoofing | high | 위조 발신 메일 → CF SPF/DKIM 수신 거부(2025-07-03~) + To 토큰(추측불가) |
| T-21-15 | Info Disclosure | medium | 메일 원문 장기 저장 → parse 후 raw_mime=null + raw_expires_at TTL(D-03) |
| T-21-16 | DoS | low | 대용량 메일 → Worker rawSize>5MB setReject |
| T-21-17 | Info Disclosure | high | INGEST_SECRET/ANTHROPIC 유출 → Worker엔 INGEST만(DB/LLM 없음), EF secret은 Supabase, 커밋 금지 |
</threat_model>

<verification>
- deno check/test green (pipeline + 2 EF)
- Worker에 DB/LLM 시크릿 부재(grep)
- config.toml verify_jwt=false 2건
- end-to-end 메일 = Manual-Only(DNS 이전 후)
</verification>

<success_criteria>
- 등록 주소(To 토큰)+SPF/DKIM(CF) 통과 메일만 처리(LEDGER-05)
- 앱 미경유 예약도 메일만으로 ledger 행 생성(LEDGER-04)
- 환율 5요소 원자 저장 + 메일 우선/API fallback(LEDGER-03)
- claude 재활용으로 결제정보 추출(LEDGER-02, 신규 LLM 0)
</success_criteria>

<output>
Create `.planning/phases/21-travel-ledger/21-04-SUMMARY.md` when done
</output>
