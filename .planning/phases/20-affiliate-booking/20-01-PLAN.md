---
phase: 20-affiliate-booking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - packages/api/package.json
  - apps/web/package.json
  - apps/ios/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [GAP-19D]
user_setup: []
must_haves:
  truths:
    - "pnpm이 @supabase/supabase-js를 모노레포 전체에서 2.110.0으로 해석한다 (override 잔존 핀 없음)"
    - "기존 전체 테스트 베이스라인(core 77 / api 35 / web 65 / ios 87)이 업그레이드 후에도 green"
    - "apps/web은 @supabase/ssr ^0.12.0과 peer 정합 (빌드 peer warning 0)"
  artifacts:
    - "package.json (root) — pnpm.overrides의 supabase-js가 2.110.0"
    - "pnpm-lock.yaml — 2.110.0 해석 반영"
  key_links:
    - "root pnpm.overrides → 각 패키지 ^2.110.0 (override 미갱신 시 조용한 no-op — RESEARCH Pitfall 5)"
    - "@supabase/ssr 0.12.0 peer ^2.108.0 ↔ supabase-js 2.110.0 (Pitfall 6)"
---

<objective>
GAP-19D(presence 미동작) 해소를 위한 supabase-js 모노레포 업그레이드 2.45.4 → 2.110.0 + @supabase/ssr 0.5.1 → 0.12.0. CONTEXT folded todo가 명시한 대로 **예약 도메인 코드와 완전 분리된 독립 wave** — 이 플랜의 diff는 package.json 4개 + lockfile 뿐이어야 한다.

Purpose: Phase 19 presence("지금 N명 보는 중")가 realtime-js 2.10.2의 presence_state 프로토콜 비호환으로 죽어 있음 — 2.108.2+에서 fix 실측 완료(todo 문서). 앱 코드 무수정, 버전만 올린다.
Output: 버전 bump 4개 파일 + lockfile, 전체 테스트 베이스라인 green.
</objective>

<execution_context>
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/workflows/execute-plan.md
@$HOME/AppData/Roaming/mydoo/cc-config/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/supabase-js-upgrade-presence.md
@.planning/phases/20-affiliate-booking/20-RESEARCH.md
</context>

## Artifacts this phase produces (this plan's share)

- root `package.json` — `pnpm.overrides["@supabase/supabase-js"] = "2.110.0"` (기존 정확 핀 2.45.4 교체)
- `packages/api/package.json` — `"@supabase/supabase-js": "^2.110.0"`
- `apps/web/package.json` — `"@supabase/supabase-js": "^2.110.0"` + `"@supabase/ssr": "^0.12.0"`
- `apps/ios/package.json` — `"@supabase/supabase-js": "^2.110.0"`
- `pnpm-lock.yaml` 재생성

<!-- planner-discipline-allow: 2.45.4 -->

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 패키지 정당성 확인 (supply-chain 게이트, T-20-SC)</name>
  <files>(설치 전 게이트 — 파일 변경 없음)</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-RESEARCH.md §Package Legitimacy Audit ([SUS: too-new] 판정 근거)
  </read_first>
  <what-built>RESEARCH Package Legitimacy Audit이 @supabase/supabase-js 2.110.0(2일 전 publish)과 @supabase/ssr 0.12.0(3주 전 publish)을 [SUS: too-new]로 플래그. 둘 다 이미 repo에 구버전으로 설치된 Supabase 공식 1st-party(주간 20.6M/4.5M 다운로드)라 슬롭스쿼팅이 아닌 버전 신선도 신호이나, [SUS] 패키지 설치 전 인간 확인은 필수 게이트.</what-built>
  <action>사용자에게 아래 how-to-verify 절차를 제시하고 승인을 기다린다 — 승인 전 Task 2(설치)를 시작하지 않는다. 이 체크포인트는 auto_advance와 무관하게 자동 승인 불가 (blocking-human).</action>
  <how-to-verify>
    1. https://www.npmjs.com/package/@supabase/supabase-js 접속 — 버전 2.110.0이 존재하고 publisher가 supabase 공식 org인지 확인
    2. https://www.npmjs.com/package/@supabase/ssr 접속 — 버전 0.12.0 동일 확인
    3. 이상 징후(다른 스코프, postinstall 스크립트 경고, 비정상 다운로드 수) 없는지 확인
  </how-to-verify>
  <verify>사용자의 명시적 승인 메시지 수신</verify>
  <acceptance_criteria>사용자가 npmjs 공식 페이지 2건을 확인하고 "approved"를 입력함 — 그 전까지 pnpm install 미실행</acceptance_criteria>
  <done>정당성 승인 획득 (또는 이상 발견 시 업그레이드 중단·orchestrator 보고)</done>
  <resume-signal>"approved" 입력 (또는 이상 발견 시 내용 기술 — 발견 시 업그레이드 중단하고 orchestrator에 보고)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 버전 bump + install + 해석 검증</name>
  <files>package.json, packages/api/package.json, apps/web/package.json, apps/ios/package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (root — L32-37 pnpm.overrides 블록: supabase-js "2.45.4" 정확 핀)
    - packages/api/package.json (L21)
    - apps/web/package.json (L23-24 — ssr 0.5.1 + supabase-js)
    - apps/ios/package.json (L27)
    - .planning/todos/pending/supabase-js-upgrade-presence.md (검증 기준 원문)
  </read_first>
  <action>
    4개 파일에서 supabase 버전만 수정 — 다른 dependency·스크립트·설정 라인 무수정 (Karpathy 3 surgical):
    1. root `pnpm.overrides["@supabase/supabase-js"]`: "2.45.4" → "2.110.0" (RESEARCH Pitfall 5 — override 미갱신 시 각 패키지 bump가 조용한 no-op). react/react-dom override 무수정.
    2. packages/api: "^2.45.4" → "^2.110.0"
    3. apps/web: "@supabase/supabase-js" "^2.45.4" → "^2.110.0" AND "@supabase/ssr" "^0.5.1" → "^0.12.0" (peer 짝 — 0.12.0 peers ^2.108.0, Pitfall 6)
    4. apps/ios: "^2.45.4" → "^2.110.0"
    5. `pnpm install` 실행 (이 Windows 머신은 pnpm이 npm 경유 설치됨 — 메모리 dev-env-windows; 명령은 동일). lockfile 재생성.
    6. `pnpm why @supabase/supabase-js`로 전 워크스페이스 해석 버전이 2.110.0 단일인지 확인.
    주의: supabase/functions/* 의 deno.lock(jsr:@supabase/supabase-js@2 핀)은 별도 세계 — 절대 손대지 않음 (RESEARCH Pitfall 6 검증 완료). 마이그레이션/스키마 무변경 (todo 명시).
  </action>
  <verify>
    <automated>pnpm why @supabase/supabase-js | grep -c "2.110.0"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm why @supabase/supabase-js` 출력에 2.110.0만 나타나고 2.45.4는 0회 (`pnpm why @supabase/supabase-js | grep -c "2.45.4"` == 0)
    - `git diff --stat`이 정확히 package.json 4개 + pnpm-lock.yaml만 보임 (그 외 파일 0)
    - root package.json에서 `grep -c '"@supabase/supabase-js": "2.110.0"' package.json` == 1
    - apps/web/package.json에서 `grep -c '"@supabase/ssr": "\^0.12.0"' apps/web/package.json` == 1
    - supabase/functions 하위 diff 0 (`git status --porcelain supabase/functions` 빈 출력)
  </acceptance_criteria>
  <done>4개 manifest + lockfile만 변경, 2.110.0 단일 해석, Edge Functions 무접촉</done>
</task>

<task type="auto">
  <name>Task 3: 전체 회귀 게이트 (베이스라인 green)</name>
  <files>(코드 변경 없음 — 검증 전용; 회귀 발견 시에만 버전 보수 핀으로 재수정)</files>
  <read_first>
    - .planning/phases/20-affiliate-booking/20-VALIDATION.md (베이스라인 수치)
    - .planning/todos/pending/supabase-js-upgrade-presence.md (검증 기준: auth/타입드 쿼리 회귀 0)
  </read_first>
  <action>
    전체 테스트 + typecheck 게이트 실행 (이 플랜이 wave 1 SOLO인 이유 — 다른 플랜의 동시 변경 없이 깨끗한 베이스라인에서 회귀 판정):
    1. `pnpm --filter @moajoa/core test` (77 green 기대)
    2. `pnpm --filter @moajoa/api test` (35) + `pnpm --filter @moajoa/api typecheck`
    3. `pnpm --filter @moajoa/web test` (65) + web typecheck + `pnpm --filter @moajoa/web build` (SSR/@supabase/ssr 경로 실빌드 검증)
    4. `pnpm --filter @moajoa/ios test -- --watchman=false` (87) + ios typecheck
    회귀 발견 시 fallback (RESEARCH Alternatives): 2.109.0으로 보수 핀 (Node 20 마지막 지원, presence fix floor는 2.108.2) — override + 4개 manifest 동일 방식 재수정 후 게이트 재실행. 어느 버전이든 최종 상태에서 전 suite green이 이 태스크의 완료 조건.
    presence 2-브라우저 수렴 실측은 원격 realtime + 사람 눈 필요 → phase verify UAT 항목 (아래 human-check).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/core test && pnpm --filter @moajoa/api test && pnpm --filter @moajoa/web test && pnpm --filter @moajoa/ios test -- --watchman=false</automated>
    <human-check>phase UAT: /poll/[code] 2-브라우저 열기 → 양쪽 "지금 2명 보는 중" 수렴 (GAP-19D 재현 스크립트 패턴) + iOS sim subscribePollChannel 스모크 + 매직링크 로그인 회귀 0</human-check>
  </verify>
  <acceptance_criteria>
    - core/api/web/ios 4개 suite 전부 exit 0 (건수 베이스라인 77/35/65/87 이상)
    - `pnpm --filter @moajoa/web build` exit 0, peer dependency warning에 @supabase 항목 0
    - 4개 워크스페이스 typecheck exit 0
  </acceptance_criteria>
  <done>업그레이드된 supabase-js로 전 자동 베이스라인 green; presence 실측은 phase UAT로 이관</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | 외부 패키지 코드가 빌드·런타임에 유입 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-SC | Tampering | pnpm install (@supabase/supabase-js 2.110.0, @supabase/ssr 0.12.0) | high | mitigate | Task 1 blocking-human 정당성 체크포인트(npmjs 공식 org 확인) + Task 3 전체 회귀 게이트. 둘 다 [SUS: too-new] — 기설치 1st-party의 버전 신선도 신호 (RESEARCH audit) |
| T-20-SC2 | Elevation | postinstall 스크립트 | low | accept | root pnpm `neverBuiltDependencies` 정책 기존 유지 + supabase 패키지는 postinstall 없음 (audit 확인) |
</threat_model>

<verification>
- `git log --oneline -2`에 이 플랜 커밋이 예약 도메인 파일(packages/core/src/booking* 등)을 포함하지 않음 — 격리 확인
- 전 suite green + web build PASS
</verification>

<success_criteria>
- supabase-js 2.110.0 (또는 회귀 시 2.109.0 보수 핀) 단일 해석
- @supabase/ssr 0.12.0 peer 정합
- 자동 베이스라인 무회귀; presence UAT 항목이 phase verify에 등록됨
</success_criteria>

<output>
Create `.planning/phases/20-affiliate-booking/20-01-SUMMARY.md` when done
</output>
