---
phase: 27-hardening-wrapup
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - .planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md
autonomous: true
requirements: [SEC-01, NAME-01]

must_haves:
  truths:
    - "프로덕션 extract-youtube가 게이트 포함 버전으로 ACTIVE다 (version bump 확인)"
    - "main이 origin에 push되어 Vercel 프로덕션이 카피 스윕 포함 버전을 서빙한다"
    - "프로덕션에서 anon-key 원시 토큰 401 + 비멤버 익명 세션 403이 무비용으로 실증된다"
    - "27-HUMAN-UAT.md가 D-07의 4개 소스(SC-3 시나리오·Phase 25 잔여·Phase 28 라이브 2건·presence)와 SEC-01 실증 항목을 [Claude]/[human] 태깅과 함께 전부 포함한다"
  artifacts:
    - path: ".planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md"
      provides: "통합 UAT 체크리스트 (25-HUMAN-UAT 포맷 계승)"
      min_lines: 80
      contains: "status:"
  key_links:
    - from: "supabase functions list 출력"
      to: "extract-youtube 신버전"
      via: "배포 전후 version 비교 (Pitfall 1: 머지 ≠ 배포)"
      pattern: "extract-youtube.*ACTIVE"
    - from: "27-HUMAN-UAT.md"
      to: "25-HUMAN-UAT.md 잔여 + 28-VERIFICATION.md uat_pending + presence todo"
      via: "D-07 통합 — 항목 누락 0"
      pattern: "iPhone|일정 만들기|presence|403"
---

<objective>
Phase 27 변경분(게이트 + 카피 + revalidate fix)을 프로덕션에 배포하고, 통합 UAT 문서(27-HUMAN-UAT.md)를 작성하며, SEC-01 게이트를 프로덕션에서 무비용 실증한다.

Purpose: 머지 ≠ 배포 (Pitfall 1) — 배포 없이는 UAT가 구버전을 검증하게 됨. UAT 문서는 D-07 잠금대로 잔여 검증 전부를 한 문서에 합류.
Output: 배포 완료 (Vercel + extract-youtube EF version bump) + 27-HUMAN-UAT.md + 라이브 401/403 실증 기록.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-hardening-wrapup/27-CONTEXT.md
@.planning/phases/27-hardening-wrapup/27-RESEARCH.md
@.planning/phases/25-guest-unified-share/25-HUMAN-UAT.md
@.planning/phases/28-add-trip-redesign-ai/28-VERIFICATION.md
@.planning/todos/pending/supabase-js-upgrade-presence.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: main push + extract-youtube EF 배포 (--use-api) + version 확인</name>
  <files>(코드 변경 없음 — 배포 작업)</files>
  <read_first>
    - .planning/phases/27-hardening-wrapup/27-RESEARCH.md §Common Pitfalls 1·2 (배포 함정)
    - git log origin/main..main 출력 (push 대상 커밋 확인 — wave 1 커밋 5~6개 예상)
  </read_first>
  <action>
    순서 고정:

    1. **사전 확인:** `CI=true pnpm -r test` 풀 스위트 그린 (기준선: web 267 · core 192 · api 112 — Pitfall 4: bare test는 watch, CI=true 필수) + `cd supabase/functions/extract-youtube && deno task test` 31 그린 + `git status` clean. 신규 마이그레이션 0 확인: `git diff origin/main --stat -- supabase/migrations` → 0파일.
    2. **배포 전 버전 기록:** `supabase functions list` 출력에서 extract-youtube의 현재 VERSION 기록 (SUMMARY에 남김).
    3. **main push:** `git push origin main` — Vercel(moajoa-web.vercel.app) 자동 재배포 + Supabase GitHub 연동 (마이그레이션 없으므로 DB 무변경). push가 인증 실패하면 auth gate checkpoint 생성 (사전 계획 아님 — 동적).
    4. **EF 수동 배포:** `supabase functions deploy extract-youtube --use-api` — colima 환경에서 기본 deploy는 eszip ENOENT 실패 (Pitfall 2, STATE L395 실측). generate-plan v2 배포 선례(a7b3332)와 동일 절차.
    5. **배포 확인:** `supabase functions list` 재실행 → extract-youtube VERSION이 2단계에서 기록한 값보다 증가 + STATUS ACTIVE. Vercel 반영은 Task 3에서 라이브 페이지 카피("찜")로 겸사 확인 (A1 커버 — 배포 커밋 해시 직접 확인 수단 부재 시 관찰 가능한 변경으로 대체).

    커밋 없음 (배포만). 결과(전후 버전·push 커밋 해시)는 SUMMARY에 기록.
  </action>
  <verify>
    <automated>bash -c 'supabase functions list | grep extract-youtube | grep -q ACTIVE && [ -z "$(git log origin/main..main --oneline)" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `git log origin/main..main --oneline` → 0줄 (push 완료)
    - `supabase functions list`에 extract-youtube ACTIVE + VERSION이 배포 전 기록값보다 큼
    - 풀 스위트 `CI=true pnpm -r test` exit 0 (push 전 실행 로그 존재)
  </acceptance_criteria>
  <done>프로덕션 EF가 게이트 포함 신버전 ACTIVE이고 main이 origin과 동기화됐다.</done>
</task>

<task type="auto">
  <name>Task 2: 27-HUMAN-UAT.md 작성 (D-07 통합 — 4개 소스 + SEC-01 합류)</name>
  <files>.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/25-guest-unified-share/25-HUMAN-UAT.md (포맷 선례 — frontmatter·Tests·Summary·Gaps·round 기록 패턴 + Phase 25 잔여 항목 원문)
    - .planning/phases/28-add-trip-redesign-ai/28-VERIFICATION.md (frontmatter uat_pending 2건 원문)
    - .planning/todos/pending/supabase-js-upgrade-presence.md (presence 확인 배경 — GAP-19D·2.110.0 업그레이드)
    - .planning/ROADMAP.md Phase 27 섹션 L312-323 (SC-3 시나리오 원문)
  </read_first>
  <action>
    `.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md` 신규 작성 — 25-HUMAN-UAT.md 포맷 계승 (D-08 잠금). UAT 문서 구조·순서는 재량 항목 — 아래 구성으로 확정 (SC-3 판정 기준을 문서 상단에 명시).

    **Frontmatter:**
    ```markdown
    ---
    status: pending
    phase: 27-hardening-wrapup
    source: [27-01-PLAN.md, 27-02-PLAN.md, 25-HUMAN-UAT.md, 28-VERIFICATION.md, supabase-js-upgrade-presence.md]
    started: (작성 시각 ISO)
    updated: (작성 시각 ISO)
    ---
    ```

    **SC-3 판정 기준 (문서 상단):** 항목 2·3·4 전부 pass = SC-3 충족. 나머지는 D-07 합류분 (SC-3 판정과 독립 기록).

    **Tests — 각 항목에 `[Claude]`(브라우저 두 컨텍스트로 자동 실증 가능) / `[human]`(카카오 실로그인·iPhone 실기기·유료 API 트리거) 태그 + expected/result 필드 (D-08 하이브리드):**

    1. `[Claude]` SEC-01 라이브 게이트 — anon-key 원시 토큰 401 + 비멤버 익명 세션 403 (본 plan Task 3에서 실행·기록. expected: 401/403, 유료 API 발화 0)
    2. `[human]` 호스트 A 흐름 (SC-3 전반) — 카카오 로그인 → 온보딩(여행지+미정+함께) → 유튜브 링크 → #1..#N 핀 → 함께 정하기 '둘다' → 링크 복사. expected: 각 단계 완주 + 멤버 추출 정상(게이트 통과). ※ 이미 로그인된 사용자 브라우저 세션이 있으면 로그인 이후 단계는 [Claude] 실증 가능 — 27-04에서 판단
    3. `[Claude]` 게스트 B 흐름 (시크릿) — /t/[slug] 즉시 렌더(SSR "찜" 문구 확인 = Vercel 배포 겸사 검증) → 찜 탭 시 닉네임 게이트 → 날짜투표 + 장소추가(#N+1) → 채팅 "#3 어때?" 전송. expected: 전 단계 완주, 순번 연속
    4. `[Claude]` A 복귀 검증 — 실시간 반영(B의 장소·채팅이 A 화면에), 찜순 정렬, 순번 불변. expected: 새로고침 없이 수렴
    5. `[Claude]` presence 수렴 — 모아 채팅 "지금 N명 보는 중"이 두 컨텍스트 입장·퇴장에 실시간 수렴. expected: N 증감 정확. **pass 시 D-09: supabase-js-upgrade-presence todo 닫기 / fail 시 todo 유지 + 증상 갱신** (Pitfall 8: 채팅 presence 확인이 판정 기준 — /poll 별도 확인은 선택)
    6. `[Claude]` 크로스브라우저 실시간 (Phase 25 잔여) — 두 브라우저 컨텍스트 간 찜·장소 fan-out
    7. `[human]` iPhone 실기기 재확인 (Phase 25 잔여) — 공유시트 footer CTA · 달력 nav · 하트
    8. `[human]` Test 4 카카오 승격 (Phase 25 잔여) — 게스트 익명 세션 → linkIdentity 카카오 전환, 찜/장소 이력 유지
    9. `[human]` Phase 28 라이브 1 — 날짜 미정 + 기간 pill "2박3일" → '일정 만들기' → expected: Day 탭 3개 (day_count 기준, 1개 아님). ※ 유료 API 왕복 — 사용자 트리거
    10. `[human]` Phase 28 라이브 2 — 장소를 Day 3으로 수동 이동 → '일정 다시 만들기' → expected: 그 장소가 Day 3에 그대로 유지 (D-25 카피대로)
    11. `[Claude]` revalidate 확인 — shared 모아에서 추출 완료 후 /t/[slug] SSR 스냅샷 갱신 (27-01 Task 3 fix 검증. 추출 유료 발화가 필요하므로 항목 2 또는 9 진행 시 겸사 관찰 — 독립 트리거 금지)

    **Current Test:** "awaiting — 27-04에서 [Claude] 태그 항목 선소진 후 human-only 안내" 명시.
    **Summary:** total 11 / passed 0 / issues 0 / pending 11 / skipped 0 / blocked 0.
    **Gaps:** 골격만 (빈 상태).

    커밋: `docs(27-03): create integrated UAT checklist — SC-3 + Phase 25/28 residuals + presence (D-07)`
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md; test -f $f && grep -q "iPhone" $f && grep -q "2박3일" $f && grep -q "presence" $f && grep -q "403" $f && grep -q "카카오 승격" $f && grep -q "\[Claude\]" $f && grep -q "\[human\]" $f'</automated>
  </verify>
  <acceptance_criteria>
    - 파일 존재 + frontmatter에 `status: pending`, `phase: 27-hardening-wrapup`
    - 다음 문자열 각 ≥1건: `iPhone`(P25 잔여), `2박3일`·`다시 만들기`(P28 2건), `presence`(D-09), `403`(SEC-01), `카카오 승격`(P25 Test 4), `[Claude]`, `[human]`
    - Tests 항목 수 = 11 (`grep -c "^### " $f` = 11)
    - Summary 블록에 `total: 11`
  </acceptance_criteria>
  <done>D-07의 잔여 검증 전부가 한 문서에 [Claude]/[human] 분류로 합류됐고 SC-3 판정 기준이 명시됐다.</done>
</task>

<task type="auto">
  <name>Task 3: 프로덕션 게이트 무비용 실증 (401/403) + UAT 문서 기록</name>
  <files>.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md</files>
  <read_first>
    - apps/web/.env.local (NEXT_PUBLIC_SUPABASE_URL·NEXT_PUBLIC_SUPABASE_ANON_KEY — 프로덕션 원격 프로젝트 값. 값을 로그·문서에 절대 옮겨적지 말 것)
    - .planning/phases/25-guest-unified-share/25-HUMAN-UAT.md (기존 라이브 shared trip slug 단서)
    - supabase/tests/extract_gate_smoke.sh (로컬에서 검증된 단언 스타일 재사용)
  </read_first>
  <action>
    프로덕션에서 SEC-01을 무비용으로 실증한다 (Pitfall 3 트릭 — 어떤 케이스도 유료 파이프라인에 도달하지 않음):

    1. **환경:** `URL=$NEXT_PUBLIC_SUPABASE_URL`, `KEY=$NEXT_PUBLIC_SUPABASE_ANON_KEY` (apps/web/.env.local에서 셸 변수로만 로드 — 문서·커밋에 값 노출 금지).
    2. **(a) anon-key 원시 토큰 → 401 (무조건 실행 가능):**
       ```bash
       curl -s -o /dev/null -w '%{http_code}' -X POST "$URL/functions/v1/extract-youtube" \
         -H "Authorization: Bearer $KEY" -H "apikey: $KEY" -H "Content-Type: application/json" \
         -d '{"link_id":"00000000-0000-0000-0000-000000000000"}'   # expect 401 (getUser가 link 로드 전에 거부)
       ```
    3. **(b) 비멤버 익명 세션 → 403:** 프로덕션 익명 signup (`POST $URL/auth/v1/signup`, apikey 헤더, rate limit 30/hr 유의 — 세션 1회만 생성) → access_token 확보. 실존 link_id 확보: 25-HUMAN-UAT.md 또는 사용자 라이브 모아의 share slug로 `POST $URL/rest/v1/rpc/public_trip_view -d '{"p_slug":"<slug>"}'` (anon grant — 응답 links 배열에서 id 추출). 그 link_id로 extract-youtube 호출 → expect **403** (join_moa 미호출 비멤버). 링크가 ready가 아니어도 무방 — 게이트가 claim보다 앞이라 403이 먼저다.
    4. **slug를 못 찾는 경우 fallback:** (b)를 실행하지 않고 27-HUMAN-UAT.md 항목 1의 result를 `pending`으로, reason에 "라이브 shared slug 미확보 — 27-04 브라우저 세션에서 slug 확보 후 재실행"을 기록. (a)의 401은 무조건 기록.
    5. **기록:** 27-HUMAN-UAT.md 항목 1의 result를 pass(401+403) 또는 partial(401만)로 갱신 + Current Test 갱신. 실증에 사용한 것은 HTTP 코드만 — 토큰·키·응답 본문은 문서에 미기재.

    커밋: `docs(27-03): record live SEC-01 gate probe results (401/403, zero paid calls)`
  </action>
  <verify>
    <automated>bash -c 'grep -A3 "SEC-01 라이브 게이트" .planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md | grep -qE "result: (pass|partial)"'</automated>
  </verify>
  <acceptance_criteria>
    - 27-HUMAN-UAT.md 항목 1에 `result: pass`(401+403 실증) 또는 `result: partial`(401 실증 + 403 pending 사유 기록)
    - 실행 로그에 (a) 401 확인 존재
    - `grep -cE "eyJ|sb_|service_role" .planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md` = 0 (토큰·키 미기재)
    - 프로덕션 links 데이터 무오염: 403/401 응답만 — extraction_status 변경 요청 자체가 거부됨 (게이트가 claim 앞이므로 검증 행위가 상태를 바꾸지 않음)
  </acceptance_criteria>
  <done>프로덕션 게이트가 무비용으로 실증(최소 401, 가능 시 403까지)되고 결과가 UAT 문서에 기록됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 → 프로덕션 Supabase | 배포·프로브에 anon key만 사용 (service-role 미사용) |
| UAT 문서 → git 원격 | 문서에 비밀값이 실리면 push로 영구 노출 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-08 | I | 27-HUMAN-UAT.md — 프로브 중 토큰·키 문서 유입 | mitigate | Task 3: HTTP 코드만 기록, `grep -cE "eyJ|sb_|service_role"` = 0 acceptance로 강제 |
| T-27-09 | D | 프로덕션 익명 signup rate limit (30/hr) 소진 | mitigate | Task 3: 세션 1회 생성·재사용 (Pitfall 5) |
| T-27-10 | T | 프로브가 프로덕션 링크 상태를 오염 | mitigate | 게이트가 claim 앞 — 401/403 응답은 DB 무변이 (27-01 산출물이 자체 방어) |
| T-27-11 | E | 배포 누락으로 구버전(게이트 없음)이 라이브 유지 | mitigate | Task 1: version bump 확인 필수 (Pitfall 1) — 비멤버 403 미출력 시 배포부터 의심 |
</threat_model>

<verification>
- `supabase functions list` extract-youtube ACTIVE + version bump
- `git log origin/main..main` → 0줄
- 27-HUMAN-UAT.md 존재 + 11개 항목 + [Claude]/[human] 태깅 + SEC-01 result 기록
- 프로덕션 401 (필수) / 403 (slug 확보 시) 실증
</verification>

<success_criteria>
- SC-1의 라이브 절반 실증: 비멤버(익명 세션)의 프로덕션 추출 호출 거부 (403) — 멤버 통과 확인은 27-04 UAT 몫
- UAT 실행(27-04)이 이 문서 하나로 진행 가능 — 소스 4종 항목 누락 0
- 배포 상태가 코드와 정합 (Vercel + EF)
</success_criteria>

<output>
After completion, create `.planning/phases/27-hardening-wrapup/27-03-SUMMARY.md`
</output>
