---
phase: 08-extraction-depth
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - supabase/functions/extract-youtube/pipeline/claude.ts
  - supabase/functions/extract-youtube/pipeline/claude.test.ts
  - supabase/functions/extract-youtube/index.ts
autonomous: true
requirements: [EXTRACT-12, EXTRACT-13, EXTRACT-14]
must_haves:
  truths:
    - "The single Claude call's output schema accepts per-place summary_ko and a video-level video_summary_ko, both optional (omitted → undefined, never an error)"
    - "The prompt instructs Claude to write Korean commentary grounded only in transcript/description, empty when ungrounded, in Korean even for JP/EN videos"
    - "places upsert writes summary_ko: r.cand.summary_ko ?? null; links success update writes summary_ko: candidates.video_summary_ko ?? null"
    - "A missing/omitted summary degrades to NULL and NEVER introduces a new failure path (no new try/catch, extraction still succeeds)"
  artifacts:
    - path: "supabase/functions/extract-youtube/pipeline/claude.ts"
      provides: "Widened LLMOutput + PlaceCandidate Zod schema + extended prompt"
      contains: "summary_ko"
    - path: "supabase/functions/extract-youtube/pipeline/claude.test.ts"
      provides: "Deno tests: schema accepts/omits summary_ko + video_summary_ko"
      contains: "Deno.test"
    - path: "supabase/functions/extract-youtube/index.ts"
      provides: "Persistence wiring of both summary fields via ?? null"
      contains: "summary_ko"
  key_links:
    - from: "claude.ts LLMOutput.parse(parsed)"
      to: "index.ts rows[].summary_ko / links.update.summary_ko"
      via: "?? null bridge at the write site (no new failure branch)"
      pattern: "summary_ko: .*\\?\\? null"
---

<objective>
Widen the SINGLE existing Claude call so each extracted place carries a 1~2 sentence Korean
commentary (`places.summary_ko`) and the video carries a 2~3 sentence Korean TL;DR
(`links.summary_ko`). No new API call, no extra latency, model `claude-sonnet-4-6` unchanged —
only the output schema and prompt grow, and two existing write sites gain a `?? null` field.

Purpose: EXTRACT-12 (place commentary), EXTRACT-13 (video TL;DR), EXTRACT-14 (commentary failure
never fails extraction). The anti-hallucination guard is preserved: commentary only within
transcript/description grounding; ungrounded → short or empty (never invented). Existing
`confidence < 0.4` skip + mandatory `source_quote` rules stay verbatim.
Output: Extended claude.ts (schema + prompt) + new deno test + index.ts persistence wiring.
The summary is read off the ALREADY-PARSED candidates object — zero new try/catch, zero new
failure path (08-PATTERNS.md §Error-handling contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-extraction-depth/08-CONTEXT.md
@.planning/phases/08-extraction-depth/08-PATTERNS.md
@.planning/phases/08-extraction-depth/08-01-SUMMARY.md

<design_locks>
From 08-CONTEXT.md (LOCKED):
- SINGLE Claude call — widen OUTPUT SCHEMA only. No new call, no extra latency. Model unchanged.
- Commentary only within transcript/description grounding; no grounding → short or empty (never invent).
- Keep existing `confidence < 0.4` skip + `source_quote` mandatory.
- Korean output even if video is JP/EN.
- Commentary failure NEVER fails extraction → missing/parse-omit stores NULL.
- Multi-video place upsert overwrites summary is ACCEPTED (do NOT change conflict strategy / no new table).
</design_locks>

<optional_vs_nullable_idiom>
claude.ts LLM-side schemas use `.optional()` (→ undefined). The persistence bridge in index.ts is
`r.cand.field ?? null`. Copy `name_ko` (claude.ts:13) for the per-place field and add a sibling on
LLMOutput for the video field. Do NOT use `.nullable()` here.
</optional_vs_nullable_idiom>

<interfaces>
claude.ts PlaceCandidate (11-18) — current last field `inferred_city: z.string().max(100).optional()`.
claude.ts LLMOutput (20-23) — `{ reasoning?, places: PlaceCandidate[].max(30) }`.
index.ts places upsert rows (199-216) — last field `confidence: r.cand.confidence` (line 215),
  `source_quote: r.cand.source_quote ?? null` (line 212) is the `?? null` twin to copy.
index.ts links success update (228-239) — fields include title/thumbnail_url/author_name/external_id/
  extraction_confidence/extracted_at. `candidates` is the parsed LLMOutput in scope here.
deno test idiom (pipeline/youtube.test.ts): `import { assertEquals } from 'jsr:@std/assert'`,
  `Deno.test('name', () => {...})`. Task: `deno test --allow-net --allow-env --allow-read pipeline/*.test.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen claude.ts output schema + prompt; add deno test</name>
  <read_first>
    - supabase/functions/extract-youtube/pipeline/claude.ts (entire, 141 lines — PlaceCandidate:11-18, LLMOutput:20-23, SYSTEM_PROMPT:92, buildPrompt:94-129, output schema block:98-111, constraints:113-118)
    - supabase/functions/extract-youtube/pipeline/youtube.test.ts (deno test idiom)
    - 08-PATTERNS.md §1 (a)(b)(c)
  </read_first>
  <behavior>
    New file pipeline/claude.test.ts (deno, jsr:@std/assert). The schema under test is module-private
    (LLMOutput is not exported); test via the EXPORTED `extractCandidatesFromContext` is impractical
    (it fetches Anthropic). So EXPORT the schema for testing: add `export` to LLMOutput (and
    PlaceCandidate stays internal — export LLMOutput is enough since it nests PlaceCandidate).
    Tests (assertEquals on parsed result, all synchronous, no network):
    - "LLMOutput accepts place WITH summary_ko" → parse `{ places: [{ name_local:'스시O', source_quote:'q', summary_ko:'1~2문장' }] }` → result.places[0].summary_ko === '1~2문장'.
    - "LLMOutput accepts place WITHOUT summary_ko (optional)" → parse `{ places: [{ name_local:'스시O', source_quote:'q' }] }` → result.places[0].summary_ko === undefined (no throw).
    - "LLMOutput accepts video_summary_ko" → parse `{ places: [], video_summary_ko:'영상 TL;DR' }` → result.video_summary_ko === '영상 TL;DR'.
    - "LLMOutput accepts omitted video_summary_ko" → parse `{ places: [] }` → result.video_summary_ko === undefined.
    - "LLMOutput rejects non-string summary_ko" → expect parse throw on `{ places:[{ name_local:'x', source_quote:'q', summary_ko: 123 }] }` (assertThrows from jsr:@std/assert).
  </behavior>
  <action>
    1. PlaceCandidate (claude.ts:11-18): add after `name_ko` (line 13), copying that optional-string idiom:
       ```ts
       summary_ko: z.string().max(500).optional(),
       ```
    2. LLMOutput (20-23): add `export` keyword and a video-level field:
       ```ts
       export const LLMOutput = z.object({
         reasoning: z.string().optional(),
         places: z.array(PlaceCandidate).max(30),
         video_summary_ko: z.string().max(800).optional(),
       });
       ```
    3. buildPrompt output schema block (98-111): add `"summary_ko"` to the per-place object and add a
       top-level `"video_summary_ko"` key, e.g.:
       - inside the place object, after `"confidence"`: `"summary_ko": "<1~2문장 한국어 해설, 자막·설명 근거 범위 내에서만. 근거 없으면 빈 문자열로 생략>",`
       - as a sibling of `"places"`: `"video_summary_ko": "<영상 전체 2~3문장 한국어 TL;DR. 자막·설명 근거 범위 내에서만>"`
    4. Constraints block (113-118): add two lines mirroring the EXISTING source_quote/confidence rules:
       - `- summary_ko / video_summary_ko: 반드시 한국어로. 영상이 일본어/영어여도 한국어로 작성.`
       - `- 해설은 자막·설명에 실제 근거가 있을 때만 작성. 근거 없으면 비워라(지어내지 마라). source_quote 규칙과 동일한 grounding.`
    5. SYSTEM_PROMPT (line 92): may append ONE clause keeping the "never invent" spirit, e.g.
       ` You also write short Korean commentary grounded strictly in the provided transcript/description; if there is no grounding, you leave it empty rather than inventing.` Do NOT weaken the existing "never invent places" sentence.
    6. Parsing: DO NOT touch. `LLMOutput.parse(parsed)` (line 87), extractJsonBlock (131-139), try/catch
       (80-84) are unchanged — adding schema fields auto-covers them (08-PATTERNS.md §1(c)).
    7. Create pipeline/claude.test.ts with the behaviors above (`import { assertEquals, assertThrows } from 'jsr:@std/assert'; import { LLMOutput } from './claude.ts';`).
    Keep `max_tokens: 2048` (output bump is small — within budget < $0.005/video per CONTEXT).
  </action>
  <verify>
    <automated>cd supabase/functions/extract-youtube && deno test --allow-net --allow-env --allow-read pipeline/claude.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "summary_ko: z.string().max(500).optional()" supabase/functions/extract-youtube/pipeline/claude.ts` succeeds
    - `grep -q "video_summary_ko: z.string().max(800).optional()" supabase/functions/extract-youtube/pipeline/claude.ts` succeeds
    - `grep -q "export const LLMOutput" supabase/functions/extract-youtube/pipeline/claude.ts` succeeds
    - prompt grounding constraint present: `grep -q "근거 없으면 비워" supabase/functions/extract-youtube/pipeline/claude.ts` succeeds (or equivalent "지어내지 마라" line — grep one of them)
    - `grep -q "한국어" supabase/functions/extract-youtube/pipeline/claude.ts` succeeds (Korean-output instruction)
    - `grep -q "confidence < 0.4" supabase/functions/extract-youtube/pipeline/claude.ts` STILL succeeds (existing skip rule untouched)
    - `grep -q "Omitting source_quote will cause" supabase/functions/extract-youtube/pipeline/claude.ts` STILL succeeds (existing source_quote rule untouched)
    - `cd supabase/functions/extract-youtube && deno test --allow-net --allow-env --allow-read pipeline/claude.test.ts` reports 0 failures (5 new tests pass)
    - Existing `deno test ... pipeline/*.test.ts` (youtube.test.ts) still passes (0 regressions)
  </acceptance_criteria>
  <done>Output schema carries place summary_ko + video_summary_ko (both optional), prompt instructs grounded Korean commentary, existing confidence/source_quote rules verbatim, deno tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire persistence — index.ts places upsert + links update</name>
  <read_first>
    - supabase/functions/extract-youtube/index.ts:195-241 (places upsert 199-216, upsert call 218-220, links success update 228-239)
    - 08-PATTERNS.md §2 (a)(b) + §Error-handling contract
  </read_first>
  <action>
    1. Places upsert rows (index.ts:199-216): add ONE field to the mapped row object, copying the
       `?? null` idiom from `source_quote` (line 212). Place it after `confidence: r.cand.confidence`
       (line 215):
       ```ts
       confidence: r.cand.confidence,
       summary_ko: r.cand.summary_ko ?? null,
       ```
       Do NOT change the upsert call `onConflict: 'board_id,google_place_id', ignoreDuplicates: true`
       (line 220) — multi-video overwrite is the ACCEPTED known limitation (CONTEXT §Known Limitation).
    2. Links success update (228-239): add ONE field after `extracted_at: new Date().toISOString()`
       (line 237):
       ```ts
       extracted_at: new Date().toISOString(),
       summary_ko: candidates.video_summary_ko ?? null,
       ```
       Confirm `candidates` is in scope at this site (it is the parsed LLMOutput). If the local
       variable name differs, use whatever holds the parsed output that already feeds `validPlaces`.
    3. Do NOT add summary_ko to the no-places-found branch update (149-160) — that branch has no
       extracted commentary context and a video TL;DR without places is out of scope here; leave it
       to degrade to the column default NULL.
    4. Add NO new try/catch, NO new failure branch (08-PATTERNS.md §Error-handling contract).
       Missing summary → undefined → `?? null`. The outer catch (276-285) is unchanged.
  </action>
  <verify>
    <automated>cd supabase/functions/extract-youtube && deno check index.ts 2>&1 | grep -v "pre-existing" || true; grep -c "summary_ko" index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "summary_ko: r.cand.summary_ko ?? null" supabase/functions/extract-youtube/index.ts` succeeds
    - `grep -q "summary_ko: candidates.video_summary_ko ?? null" supabase/functions/extract-youtube/index.ts` succeeds (or the actual in-scope parsed-output var name — confirm via read_first)
    - `grep -q "onConflict: 'board_id,google_place_id', ignoreDuplicates: true" supabase/functions/extract-youtube/index.ts` STILL succeeds (conflict strategy unchanged)
    - `grep -c "summary_ko" supabase/functions/extract-youtube/index.ts` == 2 (only the two write sites — NOT the no-places branch)
    - No new `try`/`catch` added around the summary writes (diff review: the two added lines are field assignments only)
  </acceptance_criteria>
  <done>Both summary fields persist via `?? null` at their existing write sites; conflict strategy and error handling unchanged; deno check has no NEW errors (pre-existing 04-02 deferred deno errors excluded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anthropic LLM output → DB persistence | untrusted generated text crosses into places.summary_ko / links.summary_ko |
| video transcript/description → LLM prompt | untrusted user-supplied source already in pipeline (pre-existing boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / Injection | summary_ko persisted from LLM output | mitigate | LLMOutput.parse (zod) validates type + max length (500/800) before persist; stored as plain text in a text column (no SQL interpolation — supabase-js parameterizes). Rendered escaped on web (see 08-03 T-08-06). |
| T-08-05 | Denial of Service | output token growth | accept | max_tokens stays 2048; summary fields are short (500/800 char caps). Budget < $0.005/video preserved (CONTEXT). No new call. |
| T-08-EXTRACT-14 | Availability | commentary failure path | mitigate | No new try/catch; missing summary → `?? null`. Extraction success path is unchanged, so commentary generation can never fail the extraction (EXTRACT-14). Asserted by acceptance criteria (no new catch) + the optional-schema deno tests. |
</threat_model>

<verification>
- `cd supabase/functions/extract-youtube && deno test --allow-net --allow-env --allow-read pipeline/*.test.ts` — all pass (new claude.test.ts + existing youtube.test.ts).
- `deno check index.ts` introduces no NEW type errors (pre-existing 04-02 deferred errors noted in deferred-items.md are excluded).
- Existing anti-hallucination rules (`confidence < 0.4`, mandatory source_quote) present verbatim in claude.ts.
- Live extraction of a transcript-rich travel video confirming non-empty summary + no hallucination is DEFERRED to the autonomous:false gate 08-04 (needs Anthropic key + live invoke).
</verification>

<success_criteria>
- Single Claude call widened (schema + prompt) — no new call, model unchanged, budget preserved.
- Both summary fields persist via `?? null`; conflict strategy + error handling untouched.
- Commentary failure cannot fail extraction (EXTRACT-14) — verified by no-new-catch + optional-schema tests.
- Korean-output + grounding constraints present in prompt.
</success_criteria>

<output>
After completion, create `.planning/phases/08-extraction-depth/08-02-SUMMARY.md`
</output>
