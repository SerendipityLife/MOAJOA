---
phase: 25-guest-unified-share
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/poll/[code]/_components/poll-vote-island.tsx
  - apps/web/app/poll/[code]/_components/__tests__/poll-vote-island.test.tsx
  - apps/web/app/moa/[id]/_components/place-list.tsx
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/app/moa/[id]/_components/__tests__/place-list.test.tsx
autonomous: true
requirements: [SHARE-03]

must_haves:
  truths:
    - "poll-vote-island가 /t 임베드에서 익명 auth.uid를 device_token 자리에, 저장 닉네임을 nickname으로 받아 자체 게이트를 건너뛴다 (/poll 무회귀)"
    - "poll-vote-island 첫 투표 시 게스트 컨텍스트에서는 외부 게이트(onRequireMember)를 거쳐 익명 인증·join을 먼저 돌린다"
    - "장소 행의 삭제 어포던스가 게스트에게는 자기 장소(added_by=auth.uid)에만 렌더되고(UI 게이트), 실제 소프트삭제는 hide_place_as_member DEFINER RPC(0029/25-01)로 호스트(am_trip_owner)만 전체 hide하고 게스트-에디터 포함 그 외는 DB에서 own-only가 강제된다 (D-12 DB-airtight)"
  artifacts:
    - path: "apps/web/app/poll/[code]/_components/poll-vote-island.tsx"
      provides: "optional deviceToken/nickname/onRequireMember props (embed seam)"
      contains: "deviceToken"
    - path: "apps/web/app/moa/[id]/_components/place-list.tsx"
      provides: "own-only 삭제 게이트 (currentUserId/ownerId) + DB own-only RPC 연계 주석"
      contains: "currentUserId"
  key_links:
    - from: "poll-vote-island castDateVote 호출부(L225)"
      to: "deviceToken prop"
      via: "deviceToken ?? getDeviceToken()"
      pattern: "deviceToken \\?\\? getDeviceToken"
    - from: "place-list 삭제 버튼(L250-259)"
      to: "added_by === currentUserId"
      via: "own-only 조건부 렌더"
      pattern: "added_by === currentUserId"
    - from: "moa-island handleDelete → hidePlace"
      to: "hide_place_as_member(0029/25-01)"
      via: "hidePlace가 25-01에서 RPC 경유로 전환됨 (call-site 무변경)"
      pattern: "hidePlace"
---

<objective>
게스트가 재사용할 **기존 컴포넌트 2종을 파라미터화**해 guest-surface(Plan 03)가 탐색 없이 배선하게 만든다. (1) `poll-vote-island`에 optional `deviceToken`/`nickname`/`onRequireMember` prop seam 추가 — `/t` dates·both 임베드에서 익명 `auth.uid`·저장 닉네임 주입 및 첫 투표 전 외부 게이트 호출. prop 부재 시 기존 `getDeviceToken()`/내부 게이트 그대로라 `/poll` 레거시 **무회귀**(D-10). (2) `place-list` 삭제 어포던스를 own-only로 게이트(D-12, Pitfall 5) — 현재 무조건 렌더되는 삭제 버튼(L250-259)을 `added_by === currentUserId || currentUserId === ownerId`일 때만 렌더. 실제 소프트삭제 권한은 **25-01의 `hide_place_as_member` DEFINER RPC로 DB에서 own-only가 airtight하게 강제**된다 — 호스트(am_trip_owner)만 전체 hide, 게스트-에디터 포함 그 외는 자기 것만(`hidePlace`가 그 RPC 경유로 전환됨).

Purpose: guest-surface가 poll·MoaIsland를 재사용할 때 신원(auth.uid)·권한(own-only)이 자연 통합되도록 seam을 먼저 연다. 신규 realtime/투표/색 로직 작성 0.
Output: 파라미터화된 poll-vote-island + own-only 삭제 게이트된 place-list/moa-island + 회귀 테스트.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-guest-unified-share/25-RESEARCH.md
@.planning/phases/25-guest-unified-share/25-PATTERNS.md

<interfaces>
poll-vote-island (`apps/web/app/poll/[code]/_components/poll-vote-island.tsx`):
- Props L41-53: `{ code, tripId, mode, status, options, initialRangeTally?, initialGridTally?, initialNickname? }`.
- L87 `const [nickname, setNickname] = useState(initialNickname ?? '')`.
- L110-113 클라이언트 hydrate: `const stored = getStoredNickname()`.
- L147 presence key: `const deviceToken = getDeviceToken()`.
- L223-226 vote: `await castDateVote(client, { code, deviceToken: getDeviceToken(), nickname, ... })`.
- L322-346 내부 닉네임 게이트(`if (!nickname)` inline 카드 + confirmNickname).

place-list (`apps/web/app/moa/[id]/_components/place-list.tsx`):
- Props L27: `onDelete: (placeId: string) => void`. L175 `colorFor(p.added_by)`. L186 `{...}님이 담음`.
- L250-259: 삭제 버튼 — 현재 **무조건 렌더**(펼친 아코디언 내).

moa-island (`apps/web/app/moa/[id]/_components/moa-island.tsx`):
- L56-67 props(currentUserId·trip 보유). L109 `colorFor` = memberColor(uid, trip.owner_id, ...). L323 `<PlaceList onDelete={handleDelete} ... />`.
- L11 `import { hidePlace }`. L235-238 `handleDelete` → `await hidePlace(getSupabaseBrowser(), placeId)`. **hidePlace는 25-01에서 `hide_place_as_member` RPC 경유로 전환됨** — handleDelete call-site 무변경으로 DB own-only 자동 승계.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poll-vote-island 임베드 seam — deviceToken/nickname/onRequireMember optional props</name>
  <read_first>
    apps/web/app/poll/[code]/_components/poll-vote-island.tsx L41-53·L87·L110-147·L198-230·L322-346 전체 흐름 ·
    25-PATTERNS.md §poll-vote-island (Open Q3 파라미터화).
  </read_first>
  <files>apps/web/app/poll/[code]/_components/poll-vote-island.tsx, apps/web/app/poll/[code]/_components/__tests__/poll-vote-island.test.tsx</files>
  <behavior>
    - deviceToken prop 주입 시 presence key(L147)·castDateVote 인자(L225)가 `deviceToken ?? getDeviceToken()` 사용.
    - nickname prop 주입 시 `initialNickname` 경로처럼 내부 게이트 스킵(nickname 채워짐).
    - onRequireMember 주입 시: 첫 투표에서 nickname 미확정이면 `await onRequireMember()`로 {uid, nickname} 받아 진행(외부 BottomSheet 게이트). 부재 시 기존 내부 inline 게이트 동작.
    - 세 prop 전부 부재 = /poll 레거시 동작 완전 동일(getDeviceToken/getStoredNickname/inline 게이트).
  </behavior>
  <action>
Props 인터페이스(L41-53)에 optional 추가: `deviceToken?: string`, `nickname?: string`, `onRequireMember?: () => Promise<{ uid: string; nickname: string }>`.

- L87: `useState(nickname ?? initialNickname ?? '')` (prop 우선).
- L147·L225의 `getDeviceToken()`를 `deviceToken ?? getDeviceToken()`로 감싼다.
- L110-113 hydrate: nickname prop 있으면 stored 조회 스킵.
- 투표 핸들러(`onToggleVote`/L~213): nickname 미확정 AND `onRequireMember` 존재 시 진입부에서 `const m = await onRequireMember(); setNickname(m.nickname);` 후 그 deviceToken(=m.uid)로 진행. `onRequireMember` 부재 시 기존 `if (!nickname)` inline 게이트(L322) 유지.

**핵심 규율:** prop 전부 미전달이면 diff가 런타임 동작을 바꾸지 않음(fallback 체인). /poll 페이지(`poll/[code]/page.tsx`) 호출부는 무수정.

테스트(`__tests__/poll-vote-island.test.tsx` 신규 또는 기존 확장): (a) deviceToken+nickname prop 주입 시 castDateVote가 그 deviceToken로 호출·inline 게이트 미렌더, (b) prop 부재 시 getDeviceToken 경로(기존 회귀), (c) onRequireMember 주입 시 첫 투표가 콜백 1회 호출 후 투표 진행.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web exec vitest run app/poll/[code]/_components/__tests__/poll-vote-island.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "deviceToken ?? getDeviceToken" apps/web/app/poll/[code]/_components/poll-vote-island.tsx`
    - `grep -q "onRequireMember" apps/web/app/poll/[code]/_components/poll-vote-island.tsx`
    - `grep -Eq "deviceToken\\?:|nickname\\?:" apps/web/app/poll/[code]/_components/poll-vote-island.tsx`
    - `poll/[code]/page.tsx` diff 0: `git diff --stat apps/web/app/poll/[code]/page.tsx` 결과 없음
    - web typecheck 0: `pnpm --filter @moajoa/web exec tsc --noEmit`
  </acceptance_criteria>
  <done>3 optional prop seam·fallback 체인·/poll 무회귀 테스트 그린.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: place-list own-only 삭제 게이트 (D-12) + moa-island prop 배선</name>
  <read_first>
    apps/web/app/moa/[id]/_components/place-list.tsx L20-60(Props)·L245-260(삭제 버튼) ·
    apps/web/app/moa/[id]/_components/moa-island.tsx L11·L56-67·L109·L235-238·L315-325(PlaceList 렌더·handleDelete) ·
    25-RESEARCH.md Pitfall 5 · 25-01-PLAN.md (hide_place_as_member RPC) · 25-UI-SPEC.md C5.
  </read_first>
  <files>apps/web/app/moa/[id]/_components/place-list.tsx, apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/app/moa/[id]/_components/__tests__/place-list.test.tsx</files>
  <behavior>
    - place-list에 currentUserId·ownerId prop 주입 시: 삭제 버튼(L250-259)이 `p.added_by === currentUserId || currentUserId === ownerId`일 때만 렌더.
    - prop 부재(레거시 호출부): 기존처럼 무조건 렌더(backward-compat).
    - 게스트(비owner) 컨텍스트: 자기 장소만 삭제 버튼, 남의 장소는 삭제 어포던스 0.
    - 호스트(owner) 컨텍스트: owner 분기로 전 장소 삭제 유지(무회귀).
  </behavior>
  <action>
`place-list.tsx` Props에 optional `currentUserId?: string`·`ownerId?: string` 추가. 삭제 버튼 블록(L250-259)을 조건부 렌더: `{(currentUserId === undefined || p.added_by === currentUserId || currentUserId === ownerId) && ( ...삭제 버튼... )}`. (undefined = 레거시 무조건 렌더.)

`moa-island.tsx` L323 `<PlaceList ...>`에 `currentUserId={currentUserId}` `ownerId={trip.owner_id}` 추가. (host는 owner라 owner 분기로 전 장소 삭제 유지 — 무회귀.)

**Pitfall 5 DB 봉합(25-01 연계):** 이제 D-12는 UI 게이트 + DB 양쪽에서 강제된다. `moa-island.tsx` `handleDelete`(L235-238)가 호출하는 `hidePlace`는 **25-01에서 `hide_place_as_member` DEFINER RPC 경유로 전환**되어 DB에서 own-only(비에디터는 added_by=auth.uid만)가 airtight하게 강제됨 — Phase 27 이월 **아님**. handleDelete call-site는 무변경(hidePlace가 그대로 airtight RPC를 탐). place-list.tsx 삭제 블록 주석에 이 연계를 1줄 명시: `// D-12: own-only affordance (UI) — DB는 hide_place_as_member RPC(0029)가 강제`.

테스트(`place-list.test.ts` 확장): (a) currentUserId=게스트·ownerId≠게스트일 때 남의 장소 행에 삭제 버튼 미렌더·내 장소엔 렌더, (b) currentUserId=ownerId일 때 전 장소 삭제 버튼 렌더, (c) prop 부재 시 무조건 렌더(회귀).
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/web exec vitest run app/moa/[id]/_components/__tests__/place-list.test.tsx app/moa/[id]/_components/__tests__/moa-island.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "added_by === currentUserId" apps/web/app/moa/[id]/_components/place-list.tsx`
    - `grep -Eq "currentUserId\\?:|ownerId\\?:" apps/web/app/moa/[id]/_components/place-list.tsx`
    - `grep -q "ownerId={trip.owner_id}" apps/web/app/moa/[id]/_components/moa-island.tsx`
    - place-list.tsx 삭제 블록 주석에 DB own-only RPC 연계 언급: `grep -q "hide_place_as_member" apps/web/app/moa/[id]/_components/place-list.tsx`
    - moa-island 기존 테스트 무회귀
    - web typecheck 0: `pnpm --filter @moajoa/web exec tsc --noEmit`
  </acceptance_criteria>
  <done>own-only 삭제 게이트·host owner 분기 무회귀·DB own-only RPC 연계 문서화·테스트 그린.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 게스트 브라우저 → place-list 삭제 어포던스 | 익명 editor가 UI로 남의 장소 삭제 시도 |
| /t 임베드 → poll-vote-island | 익명 세션이 날짜투표 write |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Tampering | 게스트가 남의 장소 삭제(hidden_at) | mitigate (UI) + mitigate (DB, 0029 own-only RPC) | place-list own-only UI 게이트로 어포던스 차단 + `hide_place_as_member` DEFINER RPC(0029/25-01)가 소프트삭제 유일 경로로 호스트(am_trip_owner)만 전체 hide, 게스트-에디터 포함 그 외를 added_by=auth.uid로 제한 — API의 raw `places UPDATE`(can_edit_trip) 대체. D-12 DB-airtight (RESEARCH Pitfall 5 봉합, place-list 주석 명시) |
| T-25-06 | Tampering | poll 투표 device_token 위조 | mitigate | /t 임베드는 deviceToken=auth.uid 주입(서버 발급 신원). cast_date_vote_authed(Plan 01) 채택으로 서버파생 이중 차단 |
| T-25-07 | Regression | /poll 레거시 동작 변경 | accept/verify | prop 부재 fallback 체인 + page.tsx diff 0 게이트로 무회귀 실증 |
</threat_model>

<verification>
- `pnpm --filter @moajoa/web exec vitest run` 관련 파일 그린 · web typecheck 0
- `poll/[code]/page.tsx` diff 0 (/poll 무회귀) · moa-island 기존 테스트 무회귀
- `.js` 워크스페이스 import 0 · iOS 무접촉
</verification>

<success_criteria>
poll-vote-island가 optional seam으로 /t 임베드(deviceToken=auth.uid·외부 게이트)를 지원하되 /poll 레거시는 완전 무회귀하고, place-list 삭제 어포던스가 게스트에게 own-only로 렌더되며, 실제 소프트삭제 권한은 25-01의 hide_place_as_member RPC로 호스트 외(게스트-에디터 포함) own-only가 DB에서 강제된다(D-12 airtight).
</success_criteria>

<output>
완료 후 `.planning/phases/25-guest-unified-share/25-02-SUMMARY.md` 생성.
</output>
</content>
