---
phase: 18-auto-plan-ai
plan: 05
type: execute
wave: 3
depends_on: [18-01, 18-04]
files_modified:
  - apps/ios/lib/realtime.ts
  - apps/ios/__tests__/realtime.test.ts
  - apps/ios/app/trip/[id]/(tabs)/plan.tsx
  - apps/ios/__tests__/plan.test.tsx
  - apps/ios/components/plan/day-section.tsx
  - apps/ios/components/plan/plan-item-row.tsx
  - apps/ios/components/plan/unplaced-pool.tsx
  - apps/ios/components/plan/travel-mode-toggle.tsx
autonomous: false
requirements: [PLAN-01, PLAN-02, PLAN-03, PLAN-04, PLAN-05]
must_haves:
  truths:
    - "subscribePlanProgress(tripId, cb) subscribes to plan:{trip_id} and passes broadcast payloads through"
    - "plan.tsx: State A (no places) keeps the stub; State B (places, no plan) shows the 플랜 만들기 button; tap → State C skeleton+progress; done → State D draft render"
    - "Draft shows a 초안 chip, Day sections, ordered place rows, adjacent leg pills ({n}분 / 이동시간 —), a transit/walk/drive toggle (default 전철), and a 미배치 pool"
    - "Reorder within a day drags; placed↔pool uses explicit affordances (제거 → pool, 일정에 추가 → day); 필수 star marks an anchor; 플랜 다시 만들기 opens an overwrite confirm modal passing anchors"
    - "친구와 같이 정하기 toggle sets collaborative + reveals share; no in-plan vote UI"
  artifacts:
    - path: "apps/ios/lib/realtime.ts"
      provides: "subscribePlanProgress + PlanProgress (appended; subscribeExtractProgress untouched)"
      contains: "subscribePlanProgress"
    - path: "apps/ios/app/trip/[id]/(tabs)/plan.tsx"
      provides: "filled plan tab: button → skeleton → draft itinerary + editing"
      contains: "플랜 만들기"
    - path: "apps/ios/components/plan/plan-item-row.tsx"
      provides: "place-card row with drag handle + 필수 star + 제거 / 일정에 추가 affordance + leg pill"
  key_links:
    - from: "apps/ios/app/trip/[id]/(tabs)/plan.tsx"
      to: "generatePlan / getPlanByTrip / reorderPlanItem / setTravelMode / setCollaborative (@moajoa/api)"
      via: "import + call"
      pattern: "generatePlan|getPlanByTrip"
    - from: "apps/ios/app/trip/[id]/(tabs)/plan.tsx"
      to: "subscribePlanProgress (lib/realtime)"
      via: "useEffect subscribe + supabase.removeChannel cleanup"
      pattern: "subscribePlanProgress"
---

<objective>
Fill the `plan.tsx` stub with the full Phase 18 plan experience per UI-SPEC States A–F: a user-triggered "플랜 만들기" button (D-01 — never auto), a trip-scoped realtime skeleton+progress card during generation, a rendered draft itinerary (초안 chip, Day sections, ordered place rows with adjacent leg pills, a transit/walk/drive toggle defaulting to 전철, and a 미배치 pool), drag-reorder within a day, explicit placed↔pool moves (D-13), a 필수 anchor star, a "플랜 다시 만들기" overwrite confirm modal (D-11) that passes anchors (D-10), and a "친구와 같이 정하기" collaborative toggle (D-14 — flag + share only).

Purpose: PLAN-01..05 made visible. This is ~recombination of shipped iOS idioms (map.tsx screen, step-indicator.tsx progress, place-list.tsx cards, pin-sheet.tsx sheets) + one genuinely new surface: the drag-reorder UI. Per RESEARCH Open Q1, the drag library is decided by a SKETCH first — `react-native-reanimated-dnd@2.0.0` (Reanimated-4-native) OR hand-rolled gesture-handler+Reanimated 4; do NOT use `react-native-draggable-flatlist` (Pitfall 3 — breaks at runtime on this Reanimated 4 / New Arch project).

Output: `lib/realtime.ts` (appended) + `plan.tsx` (filled) + `components/plan/{day-section,plan-item-row,unplaced-pool,travel-mode-toggle}.tsx` + tests. `autonomous: false` — Task 3 ends in a device/sim UAT checkpoint (drag + realtime + Routes recompute are RN gestures / live calls that only a human can confirm).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-auto-plan-ai/18-CONTEXT.md
@.planning/phases/18-auto-plan-ai/18-RESEARCH.md
@.planning/phases/18-auto-plan-ai/18-PATTERNS.md
@.planning/phases/18-auto-plan-ai/18-UI-SPEC.md

<interfaces>
<!-- iOS analogs to mirror. Extracted from codebase. -->

From apps/ios/lib/realtime.ts (subscribeExtractProgress — add a sibling, do NOT edit it):
```typescript
import type { RealtimeChannel } from '@supabase/supabase-js';
export interface ExtractProgress { step: '...'; progress_pct?: number; ... }
export function subscribeExtractProgress(linkId, onProgress): RealtimeChannel {
  return supabase.channel(extractChannelName(linkId))
    .on('broadcast', { event: 'progress' }, (msg) => onProgress(msg.payload as ExtractProgress))
    .subscribe();
}
// Caller MUST supabase.removeChannel(ch) in useEffect cleanup (leak guard).
```

From @moajoa/core (Plan 01): planChannelName(tripId) → 'plan:{tripId}', PlanStep, PLAN_STEP_KO, TravelMode.
From @moajoa/api (Plan 04): getPlanByTrip, generatePlan, reorderPlanItem, setTravelMode, moveToPool, moveToDay, setAnchor, setCollaborative; listPlacesByTrip, getTrip (existing, used in map.tsx).

From apps/ios/app/trip/[id]/(tabs)/plan.tsx (CURRENT stub — keep verbatim as State A):
```tsx
// brand-50 circle + Ionicons 'sparkles' + text-xl/600 heading 아직 플랜이 없어요 + body + 링크를 공유해 시작하기 accent line
```

From apps/ios/app/trip/[id]/(tabs)/map.tsx (screen idiom): useLocalSearchParams<{ id }>, useState trip/places, load useCallback in Promise.all, useEffect(load), SafeAreaView, filled brand-500 button (bg-brand-500 rounded-lg/xl, disabled:opacity-50, white font-semibold label).

From apps/ios/components/boards/step-indicator.tsx: white rounded-3xl card, ActivityIndicator color="#2979FF", step rows brand/neutral/outline dots.
From apps/ios/components/boards/place-list.tsx L16-90: ROW_SHADOW const, white rounded-2xl mb-2.5 px-3 py-3 flex-row items-center card, vibe-tinted w-10 h-10 rounded-xl leading icon via vibeOf/VIBE_STYLE from @/lib/category, text-sm font-semibold title + text-xs text-neutral-500 subtitle, brand-50 stadium chip trailing.
From apps/ios/components/boards/pin-sheet.tsx L4-8: @gorhom/bottom-sheet; visible content MUST be in an inner <View className="..."> (NativeWind on BottomSheetView can silently no-op — Pitfall 3 idiom).

From apps/ios/__tests__/realtime.test.ts L53-70 (channel-name + payload pass-through assertion to mirror).
</interfaces>

<copywriting>
<!-- Verbatim from UI-SPEC Copywriting Contract — do NOT paraphrase. -->
- Primary CTA (places, no plan): 플랜 만들기
- Generating (disabled): 플랜을 짜고 있어요…
- Regenerate: 플랜 다시 만들기
- Draft chip: 초안
- Empty (no places) heading/body: 아직 플랜이 없어요 / 유튜브 링크를 추출하면 영상 속 장소로 플랜을 만들 수 있어요. + 링크를 공유해 시작하기
- Empty (places, no plan): 장소가 모였어요 / 모은 장소로 동선과 날짜별 일정을 짜드릴게요. 아래 버튼을 눌러보세요.
- 미배치 pool title / helper: 미배치 / 일정에 넣지 않은 장소예요. 추가하거나 그대로 둘 수 있어요. / (coordinateless) 위치 정보가 없어 자동 배치에서 빠졌어요.
- Leg present / null: {n}분 / 이동시간 —
- Travel-mode labels: 전철 / 도보 / 차  (default 전철)
- 필수: 필수 장소로 지정 / set → filled star + 필수 tag
- Error (gen failed): 플랜을 만들지 못했어요. 잠시 후 다시 시도해 주세요. + 다시 시도
- Error (no placeable): 자동 배치할 장소가 없어요. 위치가 있는 장소를 먼저 모아주세요.
- Regenerate confirm: title 플랜을 다시 만들까요? · body 초안을 다시 만들면 지금까지 편집한 내용이 사라져요. 필수로 표시한 장소는 그대로 반영돼요. · confirm 다시 만들기 (red) · cancel 취소
- Move-to-pool toast: 미배치로 옮겼어요.
- Collaborative: 친구와 같이 정하기
</copywriting>

<color>
<!-- UI-SPEC Color — brand.500 #2979FF rationed to the explicit list only. -->
brand.500 #2979FF reserved for: 플랜 만들기 button (filled), regenerate label+icon (ghost), active progress step, active travel-mode segment, set 필수 star, present leg pill (brand-50 chip + brand-600 text), collaborative active + 일정에 추가 glyph. Destructive #EF4444 for regenerate-confirm CTA + 제거 glyph. Everything else white/neutral. Category vibe tints on the leading icon are pre-existing, NOT counted against the 10% accent budget.
</color>
</context>

<tasks>

<task type="auto">
  <name>Task 1: realtime.ts subscribePlanProgress (+ test) and drag-library decision spike</name>
  <files>apps/ios/lib/realtime.ts, apps/ios/__tests__/realtime.test.ts</files>
  <read_first>
    - apps/ios/lib/realtime.ts (full — subscribeExtractProgress sibling to mirror)
    - apps/ios/__tests__/realtime.test.ts L53-70 (channel-name + payload assertion idiom)
    - packages/core/src/constants.ts (planChannelName, PlanStep — Plan 01)
    - .planning/phases/18-auto-plan-ai/18-RESEARCH.md Open Q1, Pitfall 3, Standard Stack (reanimated-dnd@2.0.0 peer match), Environment Availability (reanimated 4.3.1 / worklets 0.8.3 / gesture-handler 2.31.2 installed)
    - .planning/phases/18-auto-plan-ai/18-PATTERNS.md "apps/ios/lib/realtime.ts (MODIFY)" + "components/plan/*" drag note
  </read_first>
  <action>
    SURGICAL — append to realtime.ts; do NOT edit subscribeExtractProgress.

    1. In `apps/ios/lib/realtime.ts`, append:
    ```typescript
    import { planChannelName } from '@moajoa/core';   // add to existing core import
    export interface PlanProgress {
      step: 'loading' | 'clustering' | 'routing' | 'done' | 'error';
      progress_pct?: number;
      error?: string;
    }
    export function subscribePlanProgress(
      tripId: string,
      onProgress: (p: PlanProgress) => void,
    ): RealtimeChannel {
      return supabase
        .channel(planChannelName(tripId))
        .on('broadcast', { event: 'progress' }, (msg) => onProgress(msg.payload as PlanProgress))
        .subscribe();
    }
    ```
    Same cleanup contract (caller `supabase.removeChannel(ch)` in useEffect return).

    2. In `apps/ios/__tests__/realtime.test.ts`, mirror the existing channel-name + payload test for `subscribePlanProgress`: assert `supabase.channel` is called with `'plan:{trip_id}'`, the broadcast handler passes `msg.payload` to the callback, and the returned channel identity matches.

    3. **[DRAG-LIBRARY SPIKE — RESEARCH Open Q1 / A4]:** Before committing the UI plan, decide the drag approach by a quick sketch (CLAUDE.md GSD spike norm):
       - Attempt A: `pnpm --filter @moajoa/ios-app add react-native-reanimated-dnd` (peer reanimated ≥4.2.0 matches installed 4.3.1) and render a minimal Sortable in-day list in plan.tsx scratch.
       - Attempt B (fallback, zero new dep): hand-roll a long-press pan reorder on the installed `react-native-gesture-handler` + Reanimated 4.
       - DECIDE based on which gives a working in-day reorder in `pnpm sim` (Reanimated 4 needs a dev build, not Expo Go — this project uses pnpm sim/EAS, so test there).
       - **placed↔pool is an EXPLICIT affordance (제거 / 일정에 추가), NOT cross-zone drag** (D-13 / A4 — reanimated-dnd marks cross-list as roadmap). Record the chosen library + rationale in the SUMMARY. Do NOT adopt `react-native-draggable-flatlist` (Pitfall 3).
    Run `cd apps/ios && pnpm test --watchman=false realtime`.
  </action>
  <verify>
    <automated>cd apps/ios && pnpm test --watchman=false realtime 2>&1 | tail -20 && grep -c "subscribePlanProgress" apps/ios/lib/realtime.ts apps/ios/__tests__/realtime.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `apps/ios/lib/realtime.ts` exports `subscribePlanProgress` + `PlanProgress`; `subscribeExtractProgress` unchanged (git diff shows only appended lines).
    - realtime.test.ts has a passing case asserting the channel name is `plan:{trip_id}` and payload pass-through.
    - `cd apps/ios && pnpm test --watchman=false realtime` GREEN.
    - SUMMARY records the drag-library decision (reanimated-dnd OR hand-roll) with rationale; `react-native-draggable-flatlist` is NOT in package.json.
  </acceptance_criteria>
  <done>subscribePlanProgress + PlanProgress appended (subscribeExtractProgress untouched); realtime test GREEN asserting plan:{trip_id}; drag-library decision recorded; draggable-flatlist not added.</done>
</task>

<task type="auto">
  <name>Task 2: plan.tsx fill — States A–F (button → skeleton+progress → draft render) + RNTL test</name>
  <files>apps/ios/app/trip/[id]/(tabs)/plan.tsx, apps/ios/__tests__/plan.test.tsx</files>
  <read_first>
    - apps/ios/app/trip/[id]/(tabs)/plan.tsx (current stub = State A, keep)
    - apps/ios/app/trip/[id]/(tabs)/map.tsx (screen idiom: params, load Promise.all, useEffect, SafeAreaView, brand-500 button)
    - apps/ios/components/boards/step-indicator.tsx (progress card)
    - apps/ios/__tests__/step-indicator.test.tsx (RNTL render assertions)
    - .planning/phases/18-auto-plan-ai/18-UI-SPEC.md States A–F + Copywriting + Color (full)
  </read_first>
  <action>
    Fill `apps/ios/app/trip/[id]/(tabs)/plan.tsx`. Use `useLocalSearchParams<{ id }>`, state for `trip`/`places`/`plan` (with items), and a `load` useCallback (`Promise.all([getTrip, listPlacesByTrip, getPlanByTrip])`) in `useEffect`.

    State machine (UI-SPEC):
    - **State A** (`places.length === 0`): keep the current stub verbatim (아직 플랜이 없어요 + 링크를 공유해 시작하기; no button).
    - **State B** (places exist, no draft plan): heading `장소가 모였어요` + body, then a full-width filled brand-500 `플랜 만들기` button (rounded-lg, white text-sm/600, ≥44px). On tap → call `generatePlan({ trip_id: id, travel_mode, anchor_place_ids: [], removed_place_ids: [] })`; immediately go to State C.
    - **State C** (generating): disable the button (`플랜을 짜고 있어요…` + spinner — Pitfall 5 double-tap guard). Subscribe `subscribePlanProgress(id, cb)` in a useEffect; drive a step-indicator-style card (`플랜을 짜고 있어요`, ActivityIndicator #2979FF), mapping broadcast steps via `PLAN_STEP_KO` (장소 불러오기 → 동선 짜기 → 이동시간 계산 → 완료; pct 10/50/80/100). On `done` → refetch plan → State D. On `error` → State F. Clean up via `supabase.removeChannel`.
    - **State D** (draft rendered): a `초안` stadium chip near the title; a `travel-mode-toggle` (Task 3) at plan scope; ordered Day sections (Task 3 `day-section`) computed from trip start_date/end_date day count; leg pills between adjacent same-day rows; a 미배치 pool (Task 3 `unplaced-pool`) at the bottom separated by a 32px gap + neutral-100 hairline; a `플랜 다시 만들기` ghost (brand) action that opens the overwrite confirm.
    - **Regenerate confirm modal (D-11):** `Alert.alert` or a centered `bg-black/30` scrim + `bg-white rounded-t-3xl` sheet with the UI-SPEC confirm copy (title 플랜을 다시 만들까요? / body … / confirm 다시 만들기 red / cancel 취소). Confirm → `generatePlan({ ..., anchor_place_ids: <ids of is_anchor items> })` (D-10 — anchors passed) → State C.
    - **친구와 같이 정하기 toggle (D-14):** accessible from the header/overflow; on → `setCollaborative(plan.id, id)` → reveal share affordance (reuse existing share UI). No vote UI.
    - **State F** (error): error copy + `다시 시도` retry button (→ re-invoke generatePlan). The `no placeable` 400 from the EF → show `자동 배치할 장소가 없어요…` copy.

    Travel-mode toggle change (D-08) → `setTravelMode(plan.id, mode)` then re-fetch the plan (the EF recompute path is server-side; v1 may re-invoke generatePlan if a dedicated recompute endpoint isn't built — keep it simple: toggling persists the mode and triggers a regenerate-with-same-anchors, OR if a lighter recompute is in scope, recompute affected legs only per D-07. Document the chosen scope in SUMMARY).

    RNTL test `apps/ios/__tests__/plan.test.tsx` (mock @moajoa/api + lib/realtime): assert State A renders 아직 플랜이 없어요 when places empty; State B renders the 플랜 만들기 button when places exist + no plan; tapping the button calls `generatePlan`; a draft plan prop renders the 초안 chip + at least one Day header.
    Run `cd apps/ios && pnpm test --watchman=false plan` + `pnpm --filter @moajoa/ios-app typecheck` (or the repo's ios typecheck command).
  </action>
  <verify>
    <automated>cd apps/ios && pnpm test --watchman=false plan 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/ios && pnpm test --watchman=false plan` GREEN (State A empty, State B button + generatePlan call, State D 초안 chip + Day header).
    - iOS typecheck exits 0.
    - grep plan.tsx matches `플랜 만들기`, `플랜을 짜고 있어요…`, `초안`, `subscribePlanProgress`, `generatePlan`, `removeChannel`, `다시 만들기`, `친구와 같이 정하기`.
    - State A copy preserved verbatim from the stub; no auto-generate-on-mount (D-01 — button only; grep confirms generatePlan is NOT called inside the initial load useEffect).
  </acceptance_criteria>
  <done>plan.tsx renders States A–F (button-triggered, never auto); RNTL test GREEN; iOS typecheck clean; D-01 button-only confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: components/plan/* (day-section, plan-item-row, unplaced-pool, travel-mode-toggle) + device/sim UAT</name>
  <files>apps/ios/components/plan/day-section.tsx, apps/ios/components/plan/plan-item-row.tsx, apps/ios/components/plan/unplaced-pool.tsx, apps/ios/components/plan/travel-mode-toggle.tsx</files>
  <read_first>
    - apps/ios/components/boards/place-list.tsx (card + ROW_SHADOW + vibe icon + brand-50 chip)
    - apps/ios/components/boards/pin-sheet.tsx (sheet idiom for toggle if presented as a sheet)
    - .planning/phases/18-auto-plan-ai/18-UI-SPEC.md States D/E + Spacing + Typography + Color (full)
    - the drag-library decision recorded in Task 1 SUMMARY
  </read_first>
  <what-built>
    Built the 4 plan components and wired them into plan.tsx State D/E:
    - `plan-item-row.tsx`: place-card (white rounded-2xl, ROW_SHADOW, vibe-tinted w-10 h-10 leading icon, text-sm/600 name + text-xs neutral-500 category·address) + a `reorder-three` drag handle (neutral-400, ≥44px hit), a 필수 star (set = filled brand star + 필수 tag; unset = neutral-400 outline → `setAnchor`), a 제거 glyph (#EF4444 → `moveToPool` + 미배치로 옮겼어요 toast). Between adjacent rows: a centered brand-50 leg pill ({n}분 with the active-mode glyph, or neutral 이동시간 — when null).
    - `day-section.tsx`: `Day N` header (text-sm/600 neutral-900 + text-xs neutral-500 date sublabel) wrapping ordered plan-item-rows; in-day reorder via the chosen drag lib (Task 1); on drop → `reorderPlanItem` + recompute the ≤2 affected legs (D-07).
    - `unplaced-pool.tsx`: 미배치 title + helper copy; pool rows reuse the card with an `add-circle-outline` brand-500 `일정에 추가` affordance → `moveToDay` (append to last day end); coordinateless places show the 위치 정보가 없어… helper (D-09).
    - `travel-mode-toggle.tsx`: 3-segment 전철/도보/차 control, active = brand-500/brand-50; default 전철; change → `setTravelMode` + recompute (D-08).
  </what-built>
  <how-to-verify>
    1. `cd apps/ios && pnpm sim` (xcodebuild — expo run:ios is broken on Xcode 26, per CLAUDE.md §4.1). Sign in; open a trip that has multiple extracted, placeable places (a real Tokyo multi-link trip is ideal). Ensure the local/targeted Supabase has the 0017 migration applied (Plan 02) and the generate-plan EF deployed.
    2. **PLAN-01:** On the plan tab (State B), tap **플랜 만들기**. Confirm a skeleton + realtime progress card appears (장소 불러오기 → 동선 짜기 → 이동시간 계산), then a draft itinerary renders. Confirm a 초안 chip is visible.
    3. **PLAN-01 quality (manual/subjective):** same-neighborhood places fall on the same day; ~4–5 places/day; the 미배치 pool holds sensibly-left-out places + any coordinateless ones (위치 정보가 없어… helper).
    4. **PLAN-02:** drag a row within a day to reorder → order persists. Tap 제거 on a placed row → it moves to 미배치 (toast). Tap 일정에 추가 on a pool row → it appends to a day.
    5. **PLAN-03:** tap the 필수 star on a place → 필수 tag; tap 플랜 다시 만들기 → confirm modal (플랜을 다시 만들까요?) → 다시 만들기 → the regenerated plan keeps the 필수 place and reclusters around it.
    6. **PLAN-04:** confirm leg pills show {n}분 between adjacent items; toggle 전철 → 도보 → 차 and confirm the adjacent leg times recompute (or null legs render 이동시간 — gracefully if Routes is unenabled — see Plan 03 enablement note).
    7. **PLAN-05:** toggle 친구와 같이 정하기 → a share affordance appears (no in-plan voting UI in Phase 18).
  </how-to-verify>
  <action>
    Build the 4 components above (analogs: place-list.tsx, pin-sheet.tsx) and wire them into plan.tsx States D/E. Use the drag library chosen in Task 1. Honor UI-SPEC Spacing (px-6 screen padding, mb-2.5 card gap, 32px placed↔pool divider, ≥44px hit areas), Typography (text-sm/600 titles, text-xs/400 meta), and the Color budget (brand.500 only on the explicit list; #EF4444 destructive; vibe tints excepted). Run `cd apps/ios && pnpm test --watchman=false` (full ios suite) + iOS typecheck before the checkpoint. Then pause for the human UAT above.
  </action>
  <resume-signal>Type "approved" once States B→C→D→E verify on device/sim (PLAN-01..05), or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS client → @moajoa/api → Supabase / generate-plan EF | All calls carry the user's session; RLS + the EF auth gate (Plans 02/03) enforce access. The client holds no service-role/Anthropic/Routes secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-18 | DoS-by-cost | rapid double-tap of 플랜 만들기 | mitigate | Button disabled + spinner on tap (State C, Pitfall 5); EF idempotent overwrite (Plan 03) backs this up. |
| T-18-19 | Info disclosure | secrets in the client bundle | mitigate | iOS only calls @moajoa/api (anon-key session) + invokes the EF; no ANTHROPIC/Routes/service-role key on the client (CLAUDE.md §5). |
| T-18-20 | Tampering | client sets collaborative on a trip it can't edit | mitigate | setCollaborative goes through RLS-gated plans update + shareTrip owner RLS (Plans 02/04); client cannot bypass. |
</threat_model>

<verification>
- `cd apps/ios && pnpm test --watchman=false` GREEN (realtime + plan + full suite); iOS typecheck exit 0.
- No auto-generate on mount (D-01); drag-library decision recorded; draggable-flatlist absent.
- Device/sim UAT signs off PLAN-01..05 (or issues logged + fixed).
</verification>

<success_criteria>
- plan.tsx renders States A–F per UI-SPEC; button-triggered generation (never auto); realtime skeleton+progress; draft itinerary with 초안 chip, Day sections, adjacent leg pills, travel-mode toggle, 미배치 pool.
- Drag reorder + explicit placed↔pool moves + 필수 anchor + overwrite-confirm regenerate (anchors passed) + collaborative flag+share.
- All automated iOS tests green; human UAT approved.
</success_criteria>

<output>
After completion, create `.planning/phases/18-auto-plan-ai/18-05-SUMMARY.md`
</output>
