---
phase: 27-hardening-wrapup
plan: 04
type: execute
wave: 3
depends_on: ["27-03"]
files_modified:
  - .planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md
  - .planning/todos/pending/supabase-js-upgrade-presence.md
autonomous: false

requirements: [SEC-01, NAME-01]

must_haves:
  truths:
    - "27-HUMAN-UAT.md의 [Claude] 태그 항목이 브라우저 두 컨텍스트(일반+시크릿)로 실증되고 result가 기록된다"
    - "human-only 항목(카카오 실로그인·승격·iPhone 실기기·유료 API 2건)이 사용자에게 명확한 절차로 제시되고 결과가 기록된다"
    - "presence 판정에 따라 supabase-js-upgrade-presence todo가 닫히거나(pass) 증상이 갱신된다(fail) — D-09"
    - "SC-3 판정(항목 2·3·4)이 명시적으로 기록된다 — pass 또는 Gaps 엔트리"
  artifacts:
    - path: ".planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md"
      provides: "전 항목 result 기록 + Summary 집계 + round 엔트리"
      contains: "round"
  key_links:
    - from: "presence 항목 result"
      to: ".planning/todos/pending/supabase-js-upgrade-presence.md"
      via: "pass → .planning/todos/done/ 이동 / fail → 증상 갱신"
      pattern: "supabase-js-upgrade-presence"
    - from: "27-HUMAN-UAT.md Summary"
      to: "SC-3 판정"
      via: "항목 2·3·4 pass 집계"
      pattern: "total: 11"
---

<objective>
통합 UAT를 하이브리드로 실행한다 (D-08): Claude가 브라우저 두 컨텍스트(일반+시크릿)로 자동 실증 가능한 항목을 먼저 소진하고, human-only 항목은 checkpoint로 사용자에게 넘긴 뒤, 전체 결과를 27-HUMAN-UAT.md에 판정 기록한다. presence pass 시 folded todo를 닫는다 (D-09).

Purpose: Phase 27 SC-3 + Phase 25/28 잔여 검증의 최종 소진 — 이 plan이 통과하면 테스트 버전이 외부 노출 가능한 상태.
Output: 27-HUMAN-UAT.md 전 항목 result + Summary 집계 + presence todo 처리. blocker 발견 시 Gaps 엔트리 (gap closure 재계획 입력).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md
@.planning/phases/27-hardening-wrapup/27-CONTEXT.md
@.planning/todos/pending/supabase-js-upgrade-presence.md
@.planning/phases/25-guest-unified-share/25-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [Claude] 태그 항목 브라우저 실증 (두 컨텍스트) + round 기록</name>
  <files>.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md (실행 대상 체크리스트 — 27-03 산출물, 항목 1의 403 pending 여부 포함)
    - .planning/phases/25-guest-unified-share/25-HUMAN-UAT.md L89-93 (round 기록 패턴 — Gaps에 info 엔트리로 누적)
  </read_first>
  <action>
    브라우저 도구(claude-in-chrome — 일반 컨텍스트 + 시크릿/별도 프로필 컨텍스트)로 moajoa-web.vercel.app에서 [Claude] 태그 항목을 순서대로 실증한다:

    1. **사전:** 일반 컨텍스트에 사용자 로그인 세션이 있는지 확인. 있으면 항목 2(호스트 A 흐름)의 로그인 이후 단계도 이 태스크에서 실증하고, 없으면 항목 2 전체를 human으로 유지 (27-HUMAN-UAT.md 항목 2의 ※ 조건).
    2. **항목 3 (게스트 B, 시크릿):** 호스트 A의 공유 링크(/t/[slug]) 열기 → SSR 즉시 렌더 + "찜" 문구 확인 (= NAME-01 라이브 + Vercel 배포 겸사 검증) → 찜 탭 → 닉네임 게이트 → 닉네임 입력 → 날짜투표 참여 → 장소추가(#N+1 순번 확인) → 채팅 "#3 어때?" 전송. ※ 게스트 세션은 한 번 만들고 보존 (Pitfall 5: 익명 signup 30/hr — 시크릿 창 반복 재생성 금지)
    3. **항목 4 (A 복귀):** 일반 컨텍스트에서 B의 장소·채팅이 새로고침 없이 반영되는지, 찜순 정렬·순번 불변 확인.
    4. **항목 5 (presence):** 두 컨텍스트에서 모아 채팅 열고 "지금 N명 보는 중" 입장·퇴장 수렴 확인. **pass면 즉시 D-09 실행:** `mkdir -p .planning/todos/done && git mv .planning/todos/pending/supabase-js-upgrade-presence.md .planning/todos/done/` + 파일 끝에 닫기 근거 3줄 append (확인 일시, supabase-js 2.110.0에서 채팅 presence 수렴 실증, 원 증상 GAP-19D는 2.45.4 시절). **fail이면** todo를 pending에 유지하고 증상 섹션에 현 관찰(버전 2.110.0·증상 상세) append.
    5. **항목 6 (크로스브라우저 실시간):** 두 컨텍스트 간 찜·장소 fan-out.
    6. **항목 1 후속 (있으면):** 27-03에서 403이 pending이면, 브라우저에서 확보한 실 slug로 public_trip_view → link_id → 익명 세션 403 프로브 재실행 (curl, 무비용 — 기존 게스트 세션 토큰 재사용).
    7. **항목 11 (revalidate):** 이 태스크에서는 트리거하지 않음 — 유료 발화 필요. 항목 2/9 진행 시 겸사 관찰로 남김.
    8. **기록:** 각 항목 result(pass/issue) + 25-HUMAN-UAT round 패턴으로 Gaps에 info 엔트리 1개 추가 (`truth: "UAT round 1 — Claude 자동분 실증 (일시)"`, reason에 통과/잔여 요약). issue 발견 시 해당 항목에 reported/severity/notes 필드 기록. Summary 집계 갱신.

    브라우저 도구를 사용할 수 없는 환경이면: 실증을 시도하지 말고 [Claude] 항목들을 pending으로 유지 + Current Test에 사유 기록 후 Task 2 checkpoint에서 사용자에게 함께 안내 (허위 pass 기록 금지).

    커밋: `docs(27-04): UAT round 1 — Claude browser verification results`
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md; grep -q "round 1" $f && grep -qE "result: (pass|issue|pending)" $f'</automated>
  </verify>
  <acceptance_criteria>
    - 27-HUMAN-UAT.md에 round 1 엔트리 존재 (`grep -c "round 1"` ≥ 1)
    - [Claude] 항목 3·4·5·6 각각에 result 필드가 pending이 아닌 값(pass/issue)으로 기록 — 단, 브라우저 도구 부재 시 pending + Current Test에 사유
    - presence 판정 반영: pass면 `test -f .planning/todos/done/supabase-js-upgrade-presence.md` / fail이면 pending에 잔존 + 파일 mtime 갱신(증상 append)
    - Summary의 passed+issues+pending 합계 = 11 유지
  </acceptance_criteria>
  <done>Claude 실증 가능분이 소진되어 result가 기록됐고, presence 판정에 따라 todo가 처리됐다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: human-only 항목 검증 (카카오·iPhone·유료 API 2건)</name>
  <files>(변경 없음 — 사용자 검증 대기. 결과 반영은 Task 3)</files>
  <action>아래 what-built를 요약 제시하고 how-to-verify의 5+1개 절차를 안내한 뒤 실행을 멈추고 사용자 보고를 기다린다. 사용자를 대신해 카카오 로그인·iPhone 조작·유료 API 트리거를 시도하지 않는다.</action>
  <what-built>
    Phase 27 전체가 프로덕션에 배포된 상태: extract-youtube 멤버십 게이트(비멤버 403), "찜" 카피, revalidate shared fix. Claude가 [Claude] 태그 항목(게스트 흐름·실시간·presence·크로스브라우저)을 브라우저로 선실증하고 결과를 27-HUMAN-UAT.md에 기록했습니다.
  </what-built>
  <how-to-verify>
    27-HUMAN-UAT.md의 [human] 항목 5건 — 각 항목의 expected와 대조해 주세요:

    1. **호스트 A 흐름 (항목 2, Claude 미실증분):** moajoa-web.vercel.app 카카오 로그인 → 온보딩(여행지+미정+함께) → 유튜브 링크 추가 → 지도에 #1..#N 핀 확인 → 함께 정하기 '둘다' → 링크 복사. (Claude가 로그인 세션으로 일부 실증했다면 남은 단계만)
    2. **카카오 승격 (항목 8):** 시크릿 게스트로 참여 후 초대 카드의 "로그인하고 내 여행에 담기" → 카카오 로그인 → 기존 찜·추가 장소가 그대로 유지되는지
    3. **iPhone 실기기 (항목 7):** 공유시트 footer CTA · 달력 nav · 하트 탭 — 3종 동작
    4. **AI 일정 1 (항목 9, 유료 API 1회):** 날짜 미정 + 기간 "2박3일" 모아에서 '일정 만들기' → Day 탭이 **3개** (1개면 fail)
    5. **AI 일정 2 (항목 10, 유료 API 1회):** 장소 하나를 Day 3으로 수동 이동 → '일정 다시 만들기' → 그 장소가 Day 3에 유지
    6. (겸사) **revalidate (항목 11):** 4번 진행 중 링크 추출이 일어나면, 시크릿 창에서 /t/[slug] 새로고침 시 장소 스냅샷이 갱신되는지

    각 항목 결과를 pass / 문제 있으면 증상과 함께 알려주세요.
  </how-to-verify>
  <resume-signal>항목별 결과 보고 (예: "1 pass, 2 pass, 3은 달력 nav 안 됨, 4 pass, 5 pass") 또는 "전부 통과"</resume-signal>
  <verify>사용자 보고 수신 (resume-signal) — 자동 검증 없음, 후속 반영·정합 검증은 Task 3의 automated가 담당</verify>
  <done>[human] 항목 5건(+겸사 revalidate)에 대한 사용자 보고가 확보됐다.</done>
</task>

<task type="auto">
  <name>Task 3: UAT 결과 반영 + 최종 판정 기록</name>
  <files>.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md (Task 1·2 결과가 반영된 현재 상태)
    - .planning/phases/25-guest-unified-share/25-HUMAN-UAT.md L43-51 (Gaps 엔트리 필드 패턴 — truth/status/reason/severity/test/artifacts/missing)
  </read_first>
  <action>
    checkpoint에서 받은 사용자 보고를 27-HUMAN-UAT.md에 반영한다:

    1. [human] 항목 5건의 result 갱신 (pass/issue + 사용자 보고 원문을 reported 필드에).
    2. issue 항목은 Gaps에 25-HUMAN-UAT 필드 패턴(truth/status/reason/severity/test/artifacts/missing)으로 엔트리 추가 — severity: blocker/major/minor 판정 포함. blocker 존재 시 SUMMARY에 "gap closure 재계획 필요 (`/gsd-plan-phase 27 --gaps`)" 명시.
    3. Summary 최종 집계 (total 11 = passed + issues + pending + skipped) + frontmatter `status:` 갱신: 전 항목 pass면 `complete`, 일부 pending/issue면 `partial`. `updated:` 시각 갱신.
    4. SC-3 판정을 문서 상단 판정 기준 아래 명시: 항목 2·3·4 전부 pass → "SC-3: PASS", 아니면 "SC-3: FAIL — {사유}".
    5. presence 관련: Task 1에서 미처리(브라우저 부재 등)였고 checkpoint 경유로 확인됐다면 이 시점에 D-09 처리 (todo 이동 또는 증상 갱신).
    6. 25-HUMAN-UAT.md의 잔여 항목(Test 3 잔여·Test 4)이 이번 UAT로 소진됐으면 해당 문서에는 손대지 않고, 27-HUMAN-UAT.md Gaps에 info 엔트리로 "Phase 25 잔여 → 27 UAT로 소진(항목 7·8)" 상호참조만 남긴다 (타 phase 문서 무수정 — 수술적 원칙).

    커밋: `docs(27-04): finalize integrated UAT verdict — SC-3 + residuals (D-07~09)`
  </action>
  <verify>
    <automated>bash -c 'f=.planning/phases/27-hardening-wrapup/27-HUMAN-UAT.md; grep -qE "^status: (complete|partial)" $f && grep -q "SC-3:" $f && ! grep -q "result: *$" $f'</automated>
  </verify>
  <acceptance_criteria>
    - frontmatter `status:` = complete 또는 partial (pending 아님)
    - `grep -c "SC-3: \(PASS\|FAIL\)" $f` = 1 (명시적 판정)
    - 전 11개 항목에 result 값 존재 (빈 result 0)
    - Summary 집계 합 = 11
    - presence todo 최종 상태가 항목 5 result와 정합: pass ↔ `.planning/todos/done/`에 존재, fail/pending ↔ `.planning/todos/pending/`에 존재
    - blocker Gaps 존재 시 SUMMARY에 gap closure 안내 문구 포함
  </acceptance_criteria>
  <done>통합 UAT의 최종 판정이 기록되고, SC-3 pass 또는 gap closure 입력이 확정됐다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude 브라우저 → 프로덕션 | 실 사용자 세션·익명 세션으로 라이브 데이터 조작 (테스트 모아 한정) |
| UAT 문서 → git 원격 | 사용자 보고·스크린샷 경로에 개인정보 유입 가능 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-12 | I | 27-HUMAN-UAT.md — 실 계정 이메일·토큰·개인 닉네임 기재 | mitigate | Task 1·3: HTTP 코드·판정만 기록, 토큰/키/이메일 미기재 (27-03 T-27-08과 동일 grep 원칙) |
| T-27-13 | D | 익명 signup rate limit 소진 (시크릿 세션 반복 생성) | mitigate | Task 1: 게스트 세션 1회 생성·보존 (Pitfall 5) |
| T-27-14 | R | UAT 판정 조작 가능성 (실증 없이 pass 기록) | mitigate | Task 1: 브라우저 도구 부재 시 pending 유지 강제 (허위 pass 금지 명문화) + round 엔트리에 실증 방식 기록 |
| T-27-15 | D(비용) | 유료 API 항목의 불필요 반복 트리거 | mitigate | 항목 9·10·11은 human 트리거 1회 한정, revalidate는 겸사 관찰만 (독립 트리거 금지) |
</threat_model>

<verification>
- 27-HUMAN-UAT.md: status complete/partial + SC-3 명시 판정 + 11개 항목 result 전부 기록
- presence todo 최종 위치가 항목 5 판정과 정합 (D-09)
- blocker 존재 시 gap closure 경로 안내 — 부재 시 Phase 27 verify-work 진행 가능
</verification>

<success_criteria>
- SC-3: 2인극 UAT 판정 완료 (호스트 A / 게스트 B / A 복귀 — pass 또는 구체적 gap)
- D-07 합류분(Phase 25 잔여·Phase 28 라이브 2건·presence) 전부 소진 또는 사유 있는 pending
- D-09: presence todo 닫힘(pass) 또는 증상 갱신(fail)
</success_criteria>

<output>
After completion, create `.planning/phases/27-hardening-wrapup/27-04-SUMMARY.md`
</output>
