---
phase: 10-web-voting
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - packages/api/src/types/database.ts
autonomous: false
requirements: [COLLAB-01, COLLAB-02]
must_haves:
  truths:
    - "Migration 0009 is applied to the linked Supabase project (db push succeeds)."
    - "Generated types include join_shared_board + accepted_member_count so the memberships.ts casts are backed by real signatures."
    - "A real browser flow works end-to-end: open a shared /b/[slug] → magic-link login → join → ❤️ vote → 확정 appears."
    - "The visibility-broadening of /b/[slug] to 'shared' boards is confirmed intended by a human."
    - "CLAUDE.md web-role wording update is presented as a recommended diff and applied only after human confirmation."
  artifacts:
    - path: "packages/api/src/types/database.ts"
      provides: "regenerated types after 0009 (verified non-empty)"
      contains: "join_shared_board"
  key_links:
    - from: "supabase db push"
      to: "0009 RPCs live"
      via: "supabase CLI against linked project"
      pattern: "join_shared_board"
---

<objective>
Apply the Phase 10 backend, regenerate types, and verify the full collaboration flow in a real browser. This is the BLOCKING morning gate — it requires Docker/Supabase auth, a live browser session, and human governance decisions (visibility broadening sanity-check + CLAUDE.md doc edit), none of which are autonomously verifiable.

Purpose: 10-01 wrote the SQL and 10-02 wired the UI, both offline-verified. This plan flips the switch (db push), backs the type casts with real signatures (supabase:types), and confirms COLLAB-01/02 work against production data — plus the two human-gated governance items CONTEXT D/E reserved for the morning gate.

Output: applied migration, regenerated database.ts, a verified live flow, and (after human OK) the CLAUDE.md wording update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-web-voting/10-CONTEXT.md
@.planning/phases/10-web-voting/10-PATTERNS.md
@CLAUDE.md
@docs/SESSION-NOTES-2026-06-07.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Plan 10-01 created `supabase/migrations/0009_join_shared_board.sql` (join_shared_board + accepted_member_count + public_board_view re-issue). It is committed but NOT yet applied to the linked Supabase project. Type regen (`pnpm supabase:types`) needs Docker + supabase auth, which Claude cannot drive in this session.
  </what-built>
  <how-to-verify>
    1. Ensure Docker is running and the project is linked (`supabase status` / `supabase link` if needed).
    2. Apply: `supabase db push` — confirm 0009 applies cleanly (no error; 0001-0008 already applied, only 0009 new).
    3. Regenerate types: `pnpm supabase:types`. IMPORTANT (Phase 5 lesson, STATE.md): the shell-redirect destroys `packages/api/src/types/database.ts` if `gen` fails. If the file ends up empty, recover with `git checkout HEAD -- packages/api/src/types/database.ts` and retry only once Docker/auth is healthy.
    4. Verify regen succeeded: `grep -q join_shared_board packages/api/src/types/database.ts && grep -q accepted_member_count packages/api/src/types/database.ts` → both present.
    5. Confirm the app still typechecks against real signatures: `pnpm --filter @moajoa/api typecheck` and `pnpm --filter web exec tsc --noEmit` exit 0.
  </how-to-verify>
  <resume-signal>Type "applied" once db push + supabase:types succeeded and both grep checks pass, or paste any error.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm that broadening `/b/[slug]` (public_board_view) to resolve `visibility='shared'` boards — not just `'public'` — is intended.</decision>
  <context>
    Migration 0009 changed `public_board_view` WHERE from `visibility = 'public'` to `visibility in ('public','shared')`. This is required so a friend with a shared-board slug can reach the "참여해서 투표하기" entry point (CONTEXT B — the slug-as-bearer-invite model). The privacy implication: anyone holding a `shared` board's slug can now load its title + pins via the anon-readable view (previously `shared` boards `notFound()`ed on web). Slug entropy is ~60 bits (ensure_share_slug), so this is a bearer-invite, not public discovery. This is a deliberate governance decision flagged in 10-PATTERNS GOTCHA 2 + threat T-10-06.
  </context>
  <options>
    <option id="confirm">
      <name>Confirm — broadening is intended (bearer-invite model)</name>
      <pros>Shared-link voting flow is reachable on web (the core COLLAB-01 use case); matches SESSION-NOTES §3.</pros>
      <cons>Slug-holders see title/pins of shared boards (acceptable per bearer-invite model).</cons>
    </option>
    <option id="revert">
      <name>Revert — keep web public-only, move join to a different surface</name>
      <pros>No change to shared-board web exposure.</pros>
      <cons>Breaks the no-install web join entry point; requires re-planning the join surface. Would need a NEW migration (append-only — cannot edit 0009 once pushed).</cons>
    </option>
  </options>
  <resume-signal>Select: confirm or revert (if revert, a follow-up gap plan is needed).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full web voting surface: self-join via slug (COLLAB-01) + ❤️ voting with 확정 filter (COLLAB-02), hydrated as a client island on `/b/[slug]` (preserves the public SSR cache).
  </what-built>
  <how-to-verify>
    Use a SHARED board (visibility='shared' with a share_slug) that has at least 2-3 extracted pins. Open its `/b/[slug]` in a browser:
    1. Logged out: confirm "참여해서 투표하기" CTA shows and links to login; no ❤️ toggles visible.
    2. Log in via magic link (email → /auth/callback).
    3. Confirm "이 보드에 참여하기" CTA appears (logged-in non-member). Click it → confirm you become a member (no error toast) and the ❤️ toggles appear.
    4. Tap ❤️ on a pin → count increments; refresh → count persists (server truth). Tap again → retracts; count decrements.
    5. With the board having 2 members, get one pin to love/members ≥ 0.5 → confirm it shows the 확정 badge and the "확정만 보기" filter narrows the list to confirmed pins.
    6. Open a LEGACY board (0 members / 0 votes) → confirm the page renders without layout breakage and nothing is marked 확정.
  </how-to-verify>
  <resume-signal>Type "verified" if all 6 steps pass, or describe any failure (becomes a gap-closure plan).</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Apply the recommended CLAUDE.md web-role wording update?</decision>
  <context>
    SESSION-NOTES §3 pre-specified expanding the web role from "열람·공개" to "열람·공개 + 투표 참여" once voting ships. CLAUDE.md is a governance doc — CONTEXT E reserves the edit for human confirmation (no auto-edit). The hard rule (no web board-create / add-link UI) STAYS. Recommended diff for CLAUDE.md §1 and §4.1 (apply only if you select "apply"):

    - §1 본 레포 line / project framing: clarify `web/ = 열람·공개 + 투표 참여` (조회 + 공유 + 멤버 참여/투표), `ios/ = 저장·공유·캡처·편집` 전담.
    - §4.1 모노레포 구조 comment: `apps/web/   Next.js 15 (열람·공개 보드 + 투표 참여)` (was `(열람·공개 보드)`).
    - §5 절대 하지 말 것: KEEP the existing "Web에 새로운 보드 생성·링크 추가 UI 추가" prohibition verbatim — add a parenthetical clarifying that 투표/참여(join+vote) UI is explicitly allowed (SESSION-NOTES §3), 생성이 아니므로.

    Provide the exact diff in the plan log; do NOT auto-edit until confirmed.
  </context>
  <options>
    <option id="apply">
      <name>Apply the recommended CLAUDE.md diff now</name>
      <pros>Doc matches shipped reality; future sessions know web voting is in-scope.</pros>
      <cons>None significant — hard rule preserved.</cons>
    </option>
    <option id="defer">
      <name>Defer the doc edit</name>
      <pros>Keeps this gate shorter.</pros>
      <cons>CLAUDE.md temporarily stale on web role.</cons>
    </option>
  </options>
  <resume-signal>Select: apply (then edit CLAUDE.md §1/§4.1/§5 per the diff) or defer.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration apply → production data | 0009 RPCs become live; the visibility broadening takes effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Information Disclosure | shared boards become web-readable on apply | accept | Human-gated confirm step (checkpoint:decision above) before relying on it; bearer-invite model (T-10-06). Append-only — reversal needs a new migration. |
| T-10-15 | Tampering | empty database.ts from failed type regen | mitigate | Phase 5 lesson baked into the verify steps: grep-check non-empty + `git checkout HEAD -- ...` recovery path documented. |
</threat_model>

<verification>
- `supabase db push` applies 0009 with no error.
- `packages/api/src/types/database.ts` contains `join_shared_board` + `accepted_member_count` (grep) and is non-empty; @moajoa/api + web typecheck pass against real signatures.
- Live browser flow: all 6 human-verify steps pass.
- Visibility-broadening confirmed by human; CLAUDE.md edit applied or deferred per human choice.
</verification>

<success_criteria>
- Migration 0009 applied; types regenerated and verified non-empty with the two new RPC names.
- COLLAB-01 (join) + COLLAB-02 (vote + 확정) verified working end-to-end in a real browser on a shared board; legacy board renders intact.
- Visibility-broadening governance decision recorded; CLAUDE.md web-role wording handled per human choice (hard rule preserved either way).
</success_criteria>

<output>
After completion, create `.planning/phases/10-web-voting/10-03-SUMMARY.md`
</output>
