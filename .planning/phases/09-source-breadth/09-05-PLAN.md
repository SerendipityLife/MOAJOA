---
phase: 09-source-breadth
plan: 05
type: execute
wave: 4
depends_on: ["09-03", "09-04"]
files_modified:
  - apps/ios/app/boards/[id].tsx
  - apps/ios/lib/pending.ts
autonomous: false
requirements: [SRC-01, SRC-02]
must_haves:
  truths:
    - "iOS triggers extraction for blog and instagram links (not only youtube), verified on a real device"
    - "A real tistory/velog and a real naver blog URL extract places + summary_ko live (after 08-04 summary_ko migration is pushed)"
    - "A real instagram URL surfaces an explicit failure reason in the iOS failed-status UI (no silent stall)"
  artifacts:
    - path: "apps/ios/app/boards/[id].tsx"
      provides: "Broadened trigger gate youtube → auto-extractable kinds"
      contains: "triggerExtraction"
    - path: "apps/ios/lib/pending.ts"
      provides: "drain trigger broadened to blog/insta"
      contains: "triggerExtraction"
  key_links:
    - from: "apps/ios boards/[id].tsx + pending.ts"
      to: "triggerExtraction (extract-youtube)"
      via: "detectSourceKind-based trigger for blog/insta"
      pattern: "triggerExtraction"
---

<objective>
[MORNING GATE — autonomous:false] Broaden the iOS extraction trigger to blog/instagram, then
verify the whole Phase 9 pipeline LIVE against real URLs on a real device. iOS code is not
shipped from an unverified overnight run (CONTEXT decision E: iOS needs device verification),
and live blog/insta extraction needs real network + a hallucination spot-check. This plan also
depends on Phase 8's 08-04 migration push so `places.summary_ko` / `links.summary_ko` persist.

Purpose: close SRC-01/SRC-02 end-to-end (live), not just offline. The offline router + adapters
land autonomously in Plans 01-03; this gate is the human-verified live confirmation + iOS device leg.

Output: edited iOS trigger sites + a live spot-check record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-source-breadth/09-CONTEXT.md
@.planning/phases/09-source-breadth/09-RESEARCH.md
@apps/ios/app/boards/[id].tsx
@apps/ios/lib/pending.ts

<interfaces>
<!-- iOS trigger sites. detectSourceKind(url)==='youtube' is the current gate at both. -->
```typescript
// boards/[id].tsx ~L145:  if (detectSourceKind(link.url) === 'youtube') { setAnalyzing(link.id); triggerExtraction(...) }
// pending.ts ~L74:        if (detectSourceKind(item.url) === 'youtube') { triggerExtraction(supabase, link.id)... }
// detectSourceKind → 'youtube' | 'blog' | 'instagram' | null   (@moajoa/core)
```
Failed-status surfacing already exists (TRUST-03, Phase 5 05-03): failed links show a Korean
reason + retry. Instagram's explicit failure rides that existing path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden iOS trigger gates (blog/insta)</name>
  <read_first>
    - apps/ios/app/boards/[id].tsx (~L145 youtube gate + surrounding setAnalyzing/triggerExtraction/retry)
    - apps/ios/lib/pending.ts (~L74 drain gate)
    - 09-CONTEXT.md §"Implementation Decisions" E + §deferred (iOS = morning gate)
  </read_first>
  <files>apps/ios/app/boards/[id].tsx, apps/ios/lib/pending.ts</files>
  <action>
    Surgical edit at both trigger sites — change `detectSourceKind(...) === 'youtube'` to fire for
    any auto-extractable kind (`!== 'manual'` and `!= null`, i.e. youtube|blog|instagram). Keep the
    existing setAnalyzing spinner, fire-and-forget `.catch`, and TRUST-03 retry affordance unchanged —
    only the gate condition broadens. Do NOT add new UI. Instagram will resolve to failed status
    server-side; that flows through the existing failed-row retry/reason UI (no new handling needed).
    Match existing style; touch only the gate lines (Karpathy §3.3).
  </action>
  <acceptance_criteria>
    - `grep -q "!== 'manual'\|=== 'blog'\|=== 'instagram'" apps/ios/app/boards/[id].tsx` succeeds.
    - `grep -q "!== 'manual'\|=== 'blog'\|=== 'instagram'" apps/ios/lib/pending.ts` succeeds.
    - `cd apps/ios && pnpm exec tsc --noEmit` (or the repo's `pnpm --filter ios typecheck`) exits 0.
    - `cd apps/ios && pnpm test` (jest) exits 0 (no regression to existing iOS suites).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/wcb/Documents/MOAJOA/apps/ios && pnpm test</automated>
  </verify>
  <done>Both iOS trigger sites fire for blog/insta; spinner + retry unchanged; typecheck + jest green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 9 source-router is deployed (Plans 01-03), web dev-tool trigger broadened (Plan 04), and
    iOS trigger broadened (Task 1 above). This checkpoint verifies the full pipeline LIVE — something
    that cannot be confirmed offline (real network fetch + hallucination spot-check + real device).
    Prerequisite: Phase 8 08-04 migration (places.summary_ko / links.summary_ko) must be pushed first,
    else summaries won't persist.
  </what-built>
  <how-to-verify>
    PREREQUISITE — confirm 08-04 done:
    0. Confirm `supabase db push` (migration 0008 summary_ko) ran and `pnpm supabase:types` regenerated.
       If not, complete 08-04 first.

    DEPLOY:
    1. `supabase functions deploy extract-youtube` (bundles npm:@mozilla/readability + deno-dom WASM).
       Confirm deploy succeeds and bundle size is accepted (RESEARCH Runtime State Inventory).

    BLOG LIVE (SRC-01) — via web dev tool (NEXT_PUBLIC_ENABLE_DEV_TOOLS=1) or iOS device:
    2. Add a real STATIC blog URL (a tistory or velog travel post with named places). Expect:
       extraction_status → ready, ≥1 place with non-empty summary_ko, summaries grounded (no hallucinated places).
    3. Add a real NAVER blog URL (blog.naver.com travel post). Expect: PostView rewrite works,
       body extracted (se-main-container), places + summary_ko. IF Naver geo/header-blocks the Edge
       region (RESEARCH Open Question 1 / Assumption A1): record the failure; treat naver like instagram
       (graceful fail) until a proxy is added — do NOT block the phase on naver alone.

    INSTAGRAM LIVE (SRC-02):
    4. Add a real instagram post URL. Expect: extraction_status → failed with the explicit Korean
       reason ('instagram: 자동 추출 미지원...'), surfaced in the failed-status UI with retry (TRUST-03).
       Confirm it does NOT stall in 'processing' and never marks 'ready'.

    iOS DEVICE (SRC-01/02 trigger leg):
    5. On a real iPhone, paste a blog URL in board detail → spinner shows → places appear (or explicit
       failure for blocked hosts). Paste an instagram URL → explicit failure reason shows in the list.

    RECORD:
    6. Write results into `.planning/phases/09-source-breadth/09-live-spotcheck.md`:
       per-URL source_kind, final status, place count, a sample summary_ko, hallucination yes/no,
       and the naver verdict (works / graceful-fail-for-now).
  </how-to-verify>
  <resume-signal>Type "approved" once blog (≥1 static host) extracts places+summary live, instagram fails explicitly, and iOS device trigger works — or describe what failed (incl. naver verdict).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS client → Edge Function | Device fires `triggerExtraction`; URL user-supplied |
| Edge Function → external hosts (LIVE) | Real server-side fetch of real blog/insta URLs — SSRF + DoS surface exercised live |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | SSRF | live blog fetch | mitigate (verify live) | `assertFetchableUrl` (Plan 01) + router allowlist (Plan 03) already gate the fetch; this gate confirms no internal-host fetch occurs in practice. The live URLs are public blog/insta hosts only. |
| T-09-08 | Info Disclosure (hallucination at scale) | live LLM extraction | mitigate (spot-check) | Step 2/3 spot-check confirms summaries are grounded (no invented places) — the human-verify is the control. Records hallucination yes/no in 09-live-spotcheck.md. |
| T-09-09 | DoS (real slow hosts) | live fetch | accept | `AbortSignal.timeout(10_000)` (Plan 02) bounds hang; failures map to status='failed'. Confirmed not to leave links stuck in 'processing'. |
</threat_model>

<verification>
- iOS: typecheck + jest green (Task 1, offline-verifiable).
- LIVE (human gate): blog static host → ready + places + grounded summary_ko; naver verdict recorded; instagram → explicit failure; iOS device trigger works; results in 09-live-spotcheck.md.
- Depends on 08-04 summary_ko migration being live.
</verification>

<success_criteria>
- iOS triggers blog/insta extraction (device-verified).
- ≥1 static blog host extracts places + summary_ko live with no hallucination in spot-check.
- Instagram returns an explicit failure reason (no stall, never ready).
- Naver verdict recorded (works or graceful-fail-for-now — not a phase blocker).
</success_criteria>

<output>
After completion, create `.planning/phases/09-source-breadth/09-05-SUMMARY.md` and `.planning/phases/09-source-breadth/09-live-spotcheck.md`
</output>
