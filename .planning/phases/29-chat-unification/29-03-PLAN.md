---
phase: 29-chat-unification
plan: 03
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - apps/web/app/poll/[code]/_components/poll-vote-island.tsx
  - apps/web/app/poll/[code]/_components/poll-chat.tsx
  - apps/web/__tests__/poll-vote-island.test.tsx
  - packages/api/src/queries/date-polls.ts
  - packages/api/src/queries/date-polls.test.ts
autonomous: true
requirements: [CHAT-06, CHAT-07]

must_haves:
  truths:
    - "한마디 표면·라벨이 apps/web 소스에서 완전히 소멸한다 (HC-7 grep 앵커 3종 0건)"
    - "poll:{tripId} 채널의 투표 broadcast는 계속 동작한다 (iOS 호스트 수신 계약 — 채널 프로토콜 무변경)"
    - "stored nickname만 있고 세션 없는 재방문자가 authed 투표 RPC 401을 맞지 않는다 (Pitfall 1 봉합)"
    - "date_comments 테이블·0018 DB 함수는 무접촉 (D-02a — DROP 없음)"
  artifacts:
    - path: "apps/web/app/poll/[code]/_components/poll-vote-island.tsx"
      provides: "PollChat·presence·embedded 제거된 투표 전용 island"
      min_lines: 300
    - path: "packages/api/src/queries/date-polls.ts"
      provides: "postComment/deleteComment orphan 제거 후의 date-polls 래퍼"
  key_links:
    - from: "poll-vote-island.tsx"
      to: "poll:{tripId} broadcast"
      via: "channelRef.current.send (투표 fan-out 유지)"
      pattern: "channelRef"
    - from: "poll-vote-island stored-nickname hydrate"
      to: "onRequireMember 게이트"
      via: "skip 조건에 onRequireMember 포함"
      pattern: "onRequireMember\\) return"
---

<objective>
D-02: 한마디(poll-chat) 완전 은퇴 — `poll-chat.tsx` 삭제, `PollVoteIsland`에서 PollChat 마운트·presence 표시/track·`embedded` prop 제거, api `postComment`/`deleteComment` orphan 제거. 부수: Pitfall 1(stored-nickname 게이트 우회 → castDateVoteAuthed 401) 1줄 봉합.

Purpose: CHAT-06 — 중복 대화 표면 제거. presence 표면도 채팅 presence(`moa:{tripId}`)로 단일화(재량 1/A-4). 투표 UI·집계·`poll:{tripId}` 투표 broadcast는 무변경(iOS 동결 — 채널 프로토콜은 iOS 수신 계약).
Output: poll-chat.tsx 부재 + 정리된 poll-vote-island + date-polls orphan 0 + HC-7 grep 게이트 통과.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-chat-unification/29-CONTEXT.md (D-02/D-02a, 재량 3)
@.planning/phases/29-chat-unification/29-RESEARCH.md (Q5 제거 표 · 은퇴 지도 · Pitfall 1/9)
@.planning/phases/29-chat-unification/29-PATTERNS.md (§2 실측 라인 제거 지도 · Retirement Map)

<interfaces>
<!-- poll-vote-island.tsx 제거/유지 경계 (실측 라인) -->
<!-- 유지: channelRef.current = channel(:178) — 투표 broadcast .send()(:286-290)용
     유지: broadcast 'vote' 바인딩(:182-185) — iOS 호스트 plan 탭 수신 계약
     유지: castVote optimistic+rollback(:235-304) · inline 닉네임 게이트(:381-414) · GridCalendar -->
<!-- Pitfall 1 봉합 지점 (:137-141 현재 코드): -->
```typescript
useEffect(() => {
  if (nicknameProp || initialNickname) return;   // ← `|| onRequireMember` 추가
  const stored = getStoredNickname();
  if (stored) setNickname(stored);
}, [nicknameProp, initialNickname]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poll-vote-island 은퇴 수술 (PollChat·presence·embedded 제거 + Pitfall 1 봉합) + 테스트 재작성</name>
  <files>apps/web/app/poll/[code]/_components/poll-vote-island.tsx, apps/web/__tests__/poll-vote-island.test.tsx</files>
  <read_first>
    - apps/web/app/poll/[code]/_components/poll-vote-island.tsx 전문 (아래 제거 좌표의 현재 상태 확인 — PATTERNS §2 실측 라인은 HEAD 67ee119 기준)
    - apps/web/__tests__/poll-vote-island.test.tsx 전문 (PollChat/postComment/deleteComment mock :48-56 · embedded/한마디 케이스 :202-211 · 유지할 투표/게이트 케이스)
    - .planning/phases/29-chat-unification/29-PATTERNS.md §2 (제거 지도 표)
  </read_first>
  <behavior>
    - Test (신규 — Pitfall 1): `onRequireMember` 전달 + localStorage stored nickname 존재 상태에서 렌더 → 첫 투표 클릭 시 `onRequireMember`가 호출된다 (stored nickname이 게이트를 우회하지 않음)
    - Test (기존 유지): 투표 optimistic·롤백·inline 게이트(onRequireMember 부재 시) 케이스 전부 무수정 그린
    - Test (삭제): embedded/한마디 케이스(:202-211) + PollChat·postComment·deleteComment mock(:48-56)
  </behavior>
  <action>
**RED (commit `test(29-03): retire 한마디/embedded cases, add stored-nickname gate case`):** poll-vote-island.test.tsx —
- :48-56의 postComment/deleteComment/PollChat 관련 mock 제거
- :202-211 embedded/한마디 케이스 삭제
- Pitfall 1 케이스 추가: `getStoredNickname` mock이 값 반환 + `onRequireMember` prop 전달 → 투표 버튼 클릭 → `onRequireMember` 호출 단언 (현행 구현은 stored nickname hydrate로 게이트를 스킵해 실패 — RED 확인)
- 나머지 기존 케이스(투표·롤백·집계·게이트) 무수정

**GREEN (commit `feat(29-03): retire PollChat/presence/embedded from poll-vote-island (D-02)`):** poll-vote-island.tsx — PATTERNS §2 제거 지도 그대로 (실측 라인 기준, read_first에서 재확인):

| 제거 | 좌표 |
|------|------|
| `import { PollChat } from './poll-chat'` | :14 |
| `embedded?: boolean` prop 선언+JSDoc(주석에 "한마디" — 통째 삭제) | :68-73, destructure :108 |
| `viewers` state | :121 |
| `sharedChannel` state + JSDoc | :126-133 |
| presence config(`config: { presence: ... }`) + `setSharedChannel(channel)` | :174-177, :179 |
| presence sync 바인딩 | :186-188 |
| SUBSCRIBED 시 `channel.track(...)` | :189-193 |
| cleanup의 `setSharedChannel(null)` | :197 |
| nickname 재-track effect | :204-208 |
| closed 분기 `{!embedded && <PollChat …/>}` | :371-373 |
| presence 스트립 렌더 + 주석 | :422-429 |
| open 분기 `{!embedded && <PollChat …/>}` | :560-562 |

**유지 (무접촉 — 지우면 투표 fan-out·iOS 계약 파손):** `channelRef.current = channel`(:178) · broadcast `'vote'` 바인딩(:182-185) · `.subscribe()` 자체 · castVote 전체(:235-304) · inline 닉네임 게이트(:381-414) · GridCalendar.

**Pitfall 1 봉합 (1줄):** :137-141 stored-hydrate effect의 skip 조건에 `|| onRequireMember` 추가: `if (nicknameProp || initialNickname || onRequireMember) return;` — 이유: RPC 선택은 onRequireMember **존재**(:265), 게이트 호출은 nickname **부재**(:243)로 분기 — 어긋나면 세션 없는 재방문자가 authed RPC 401. deps 배열 무변경(onRequireMember는 렌더 간 안정 함수 prop — 기존 관례 유지).

제거 후 orphan import(예: track/presence 전용이던 것) 정리 — 단 이번 변경으로 orphan이 된 것만 (§3.3).
  </action>
  <verify>
    <automated>CI=true pnpm --filter web test -- __tests__/poll-vote-island.test.tsx && pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -n "PollChat\|embedded\|viewers\|sharedChannel\|\.track(" 'apps/web/app/poll/[code]/_components/poll-vote-island.tsx'` (은퇴 식별자 0)
    - `grep -n "channelRef" 'apps/web/app/poll/[code]/_components/poll-vote-island.tsx'` 존재 (투표 broadcast 유지) + `grep -n "'vote'"` 존재 (iOS 수신 이벤트 유지)
    - `grep -n "onRequireMember) return" 'apps/web/app/poll/[code]/_components/poll-vote-island.tsx'` 1건 (Pitfall 1 봉합)
    - `! grep -n "한마디" 'apps/web/app/poll/[code]/_components/poll-vote-island.tsx' apps/web/__tests__/poll-vote-island.test.tsx`
    - `CI=true pnpm --filter web test -- __tests__/poll-vote-island.test.tsx` exit 0 · `pnpm --filter web typecheck` exit 0
  </acceptance_criteria>
  <done>poll-vote-island이 투표 전용 island — 한마디·presence 표시 소멸, vote broadcast·집계·게이트 무회귀, stored-nickname 401 엣지 봉합</done>
</task>

<task type="auto">
  <name>Task 2: poll-chat.tsx 삭제 + api orphan 제거 + HC-7 grep 게이트 + 풀 스위트</name>
  <files>apps/web/app/poll/[code]/_components/poll-chat.tsx, packages/api/src/queries/date-polls.ts, packages/api/src/queries/date-polls.test.ts</files>
  <read_first>
    - packages/api/src/queries/date-polls.ts (postComment :94-107 · deleteComment :109-119, JSDoc 포함 — 제거 경계 확인 · castDateVote :16 / getPollTally :87 / confirmPollDate :121 등 유지 대상)
    - packages/api/src/queries/date-polls.test.ts (postComment/deleteComment describe :162-197 — 제거 대상, 나머지 describe 유지)
    - apps/web/app/poll/[code]/_components/poll-chat.tsx (삭제 전 최종 참조 확인 — import하는 곳이 Task 1 이후 0임을 grep으로 검증)
  </read_first>
  <action>
**commit `feat(29-03): delete poll-chat and orphaned comment queries (D-02)`:**

1. **삭제 전 참조 0 확인 (소비 형태만):** `grep -rn "from './poll-chat'\|<PollChat" apps/web --include="*.ts" --include="*.tsx"` → poll-chat.tsx 자신 외 0건 (Task 1이 유일 소비처를 제거했음) — 0이 아니면 STOP, 잔여 소비처부터 처리. **알려진 예외(무접촉):** `moa-chat.tsx:14`·`:34`의 설명 주석("inverting poll-chat's…", "poll-chat idiom")은 소비가 아님 — moa-chat.tsx는 HC-3/HC-6 diff-0 회귀 앵커이므로 주석을 수정하지 말 것.
2. `apps/web/app/poll/[code]/_components/poll-chat.tsx` **파일 삭제** (`git rm`).
3. `packages/api/src/queries/date-polls.ts`에서 `postComment`(:94-107)·`deleteComment`(:109-119) JSDoc 포함 블록 제거. **유지:** `castDateVote`·`getDeviceToken` 참조 경로 (RESEARCH A4 — /poll 래퍼가 authed 경로로 전환해도 D-02 범위 밖·surgical §3.3, 제거는 미요청)·`pollByCode`·`getPublicTripPoll`·`castDateVoteAuthed`·`getPollTally`·`confirmPollDate`·`createDatelessTrip`·`getPollByTrip` 전부 무접촉.
4. `packages/api/src/queries/date-polls.test.ts`에서 postComment/deleteComment describe(:162-197) 제거 — 나머지 케이스 무수정.
5. **date_comments 무접촉 확인 (D-02a):** 마이그레이션 신규 파일 0 · database.ts 무수정 (date_comments 타입은 typegen 산출물 — 테이블이 남으므로 자연 유지).
6. **HC-7 grep 게이트 + 풀 스위트** (verify 참조).
  </action>
  <verify>
    <automated>! test -f 'apps/web/app/poll/[code]/_components/poll-chat.tsx' && ! grep -rn "postComment\|deleteComment" packages/api/src apps/web --include="*.ts" --include="*.tsx" && ! grep -rn "한마디\|투표가 마감되어 메시지를 남길 수 없어요\|이 메시지를 삭제할까요?" apps/web --include="*.ts" --include="*.tsx" --exclude-dir=.next && CI=true pnpm -r test && pnpm -r typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `! test -f 'apps/web/app/poll/[code]/_components/poll-chat.tsx'`
    - `! grep -rn "postComment\|deleteComment" packages/api/src apps/web --include="*.ts" --include="*.tsx"` (orphan 0)
    - HC-7: `! grep -rn "한마디\|투표가 마감되어 메시지를 남길 수 없어요\|이 메시지를 삭제할까요?" apps/web --include="*.ts" --include="*.tsx" --exclude-dir=.next` (은퇴 문자열 3종 0건 — 29-02가 guest-surface 몫을 선소진했으므로 이 시점에 전부 0)
    - `git log --diff-filter=D --name-only -1` 또는 `git status`상 삭제 파일은 poll-chat.tsx 1개뿐
    - supabase/migrations 신규/변경 파일 0 (D-02a — DROP 없음)
    - `CI=true pnpm -r test` exit 0 (core·api·web·ios 전 스위트) · `pnpm -r typecheck` exit 0
  </acceptance_criteria>
  <done>한마디 표면·라벨·api 완전 소멸, date_comments는 미사용 방치 — 전 스위트 그린</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /poll 재방문 브라우저(localStorage nickname) → castDateVoteAuthed RPC | 세션 없는 상태에서 authed RPC로 진입하는 지점 (Pitfall 1) |
| 웹 poll:{tripId} 채널 ↔ iOS 호스트 수신 | broadcast 프로토콜 계약 경계 (iOS 동결) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11 | Spoofing | stored-nickname으로 신원 게이트 우회 | mitigate | hydrate skip 조건에 onRequireMember 추가(Task 1 봉합) — 우회해도 castDateVoteAuthed는 auth.uid 서버 파생이라 fail-closed 401(피해 0), 봉합은 UX 결함 제거 |
| T-29-12 | Denial of Service | 투표 broadcast 채널 파손 (과잉 제거) | mitigate | channelRef·'vote' 바인딩·subscribe 유지를 acceptance grep으로 고정 — iOS 채널 프로토콜 무변경 |
| T-29-13 | Information Disclosure | 잔존 date_comments 데이터 | accept | 원래 휘발성 broadcast-only 설계 — anon SELECT 경로 자체가 없음(0018). DROP은 append-only 규칙상 별건 후속 (D-02a) |
| T-29-14 | Repudiation | iOS 호스트 "보는 중" 카운트에서 웹 방문자 누락 | accept | 웹 poll presence track 중단의 알려진 비용 — 기능 파손 아닌 카운트 감소, iOS 동결이라 대안 없음 (RESEARCH A2) |
</threat_model>

<verification>
- `CI=true pnpm -r test && pnpm -r typecheck` exit 0
- HC-7 grep 3종 0건 (apps/web 소스, .next 제외)
- `git diff --stat main` 기준 apps/ios 0파일 · supabase/migrations 0파일(이 plan) · moa-chat/moa-island/moa-tab-bar 0파일(이 plan)
</verification>

<success_criteria>
- 한마디 표면·라벨·코드·api·테스트 완전 은퇴 (CHAT-06 — SC3)
- /poll 투표·집계·broadcast·iOS 수신 무회귀 + 기존 채팅 스위트 무회귀 (CHAT-07)
- date_comments 테이블·0018 DB 함수 무접촉 (D-02a)
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-unification/29-03-SUMMARY.md`
</output>
