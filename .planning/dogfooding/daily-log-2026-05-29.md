# Dogfooding Daily Log (D-10 / D-11 / D-12 / D-13 lock)

> 7일 dogfooding 중 매일 1 commit으로 update. 양식 고민 없이 동일 골격으로 채워서 baseline 평가 입력으로 사용 (Plan 06-05).
>
> **How to use:** dogfooding Day 1 시작 시 본 template를 `cp daily-log-template.md daily-log-YYYY-MM-DD.md` (YYYY-MM-DD = Day 1 시작 일) 후 채움. 매일 한 Day 블록 update + commit message `docs(06): dogfooding day N log`.
>
> **Pass criteria reference (D-12 / D-13):**
> - D-12: 보드 행위 (링크 추가 / 수동 핀 / 이름 수정 / 공유) ≥ 1회/day (7일 모두)
> - D-13: 핀 누적 ≥ 10 (Day 7 시점, `hidden_at IS NULL`만 count)
> - D-20 #5: `git log --grep "dogfooding day" --oneline | wc -l` ≥ 7

---

## Meta

| Field | Value |
|-------|-------|
| Start date | 2026-05-29 |
| End date | 2026-06-04 (+6일) |
| 본인 dogfooding 보드명 | "도쿄 6월" (이미 존재) |
| board_id (UUID) | `9a7c075a-fcda-41ab-bca7-e75ead1e0d36` |
| Pre-dogfooding sign-off SHA | `2945fbe` (4-part iOS hotfix bundle) |
| samples.json URLs filled | NO (Day 1에 fill 예정 — autonomous extraction 시도용) |
| Account used for autonomous extraction | `test@test.com` (uid `602d1c3b-103d-4c2c-a388-d73fca3e0724`) |

---

## Day 1 — 2026-05-29

> Day 1 진행은 (a) Phase 6 pre-dogfooding gate 통과까지의 P0 fix와 (b) 자동 가능한 시나리오 (D-7, F-4, B'-7, REST login) 검증으로 split. UI-bound D-1/D-2/D-5/F-1~F-3/F-5 시나리오와 friend share 검증은 본인 복귀 이후 수동 보강.

### Pre-dogfooding gate 통과 결과

- **Pre-dogfooding checklist sections:** A ✅ (DB migration 0005+0006 + types regen + typecheck + deno) / B' ✅ (Vercel deploy moajoa-web.vercel.app, Supabase secrets, extract-youtube + resolve-place edge functions deployed, B'-6 GCP Maps Static **본인 액션 대기**) / C ✅ (prebuild + entitlement + pod install + cold launch on iPhone 16 Pro simulator)
- **iOS hotfix bundle:** commit `2945fbe` — portal hoist + supabase-js pin 2.45.4 + JSC engine + SharedDefaults autolinking podspec
- **Web hotfix:** commit `bba2cb9` — cookies-in-unstable_cache fix
- **Cold launch screenshot:** `/tmp/cold-launch.png` (iPhone 16 Pro sim, login screen + NativeWind styling intact)

### 자동 검증된 시나리오

- **D-7 SQL RLS gate (binary):** ✅ **PASS** — `test@test.com` token으로 다른 owner 보드 `2ac01da0-b4e9-4c87-9382-b1b61d350917`에 place insert HTTP POST → HTTP 403 `42501` `new row violates row-level security policy for table "places"`. RLS deny-by-default 정확히 동작.
- **F-4 first_board:** ✅ **PASS** — `test@test.com` 의 보드 "도쿄 6월" 존재 (사용자가 직접 생성, 2026-05-24). 0006 backfill의 `not exists` 가드 덕분에 중복 생성 X. trigger는 새 sign-up profile에 대해서만 발동 (확인 미완 — Day 2에 새 test 계정 sign-up 으로 검증 예정).
- **B'-7 webhook reachability:** ✅ extract-youtube edge function `POST /functions/v1/extract-youtube` with dummy link_id → HTTP 404 `link not found` (정상 — function 정확히 reachable, 인증 통과, link validation 도달).
- **REST login:** ✅ `test@test.com / 123123` → `POST /auth/v1/token?grant_type=password` → access_token + refresh_token + user id `602d1c3b...`. expires_in 3600s. UI 거치지 않고도 자동 시나리오 진행 가능.
- **Visibility trigger:** ✅ `boards_share_slug_before_update ON UPDATE OF visibility` trigger 정확 동작 — "도쿄 6월" visibility `private` → `public` PATCH 후 `share_slug: gkaswquxpyic` 자동 발급. HTTP 200.
- **E-1 (public board 열람):** ✅ 시뮬레이터 Safari로 `https://moajoa-web.vercel.app/b/gkaswquxpyic` 열기 → HTTP 200 SSR rendering. 화면: `test님의 보드` owner / `도쿄 6월` title / `핀 0개` count / 빈 state `아직 분석 중이에요` `잠시 후 다시 열어주세요` (Phase 4 D-08 empty-state copy 정확) / MOAJOA footer (orange-500) / Pretendard 한글 폰트 / URL bar `moajoa-web.vercel.app`. P0 hotfix #1 (cookies-in-unstable_cache) 회귀 검증 PASS. 스크린샷: `screenshots/day1-e1-public-board-empty-state.png`.
- **D-2 / E-2 (timestamp deep link):** ⏸️ **차단** — `samples.json` URL field 12개 모두 비어있음 (Plan 06-02 산출물 미작성). 본인 복귀 후 sample URL fill 필요. autonomous extraction 시뮬레이션 보류.
- **OG image self-test:** ❌ **P0 #5** — `GET /b/gkaswquxpyic/opengraph-image` → HTTP 200 + `content-type: image/png` 헤더지만 body가 HTML 500 페이지 (`x-vercel-cache: MISS`). 친구 share preview 깨짐. Vercel error logs 비어있음 → uncaught throw 추정. incidents.md #5 등록. dogfooding 진행 자체는 안 막음 (public board page는 정상).
- **resolve-place edge function:** ✅ HTTP 200 + Google Places API 정상 응답 (query="test" → 5건 places 반환). D-5 manual pin search 준비 완료. GOOGLE_PLACES_SERVER_KEY secret 동작 검증.
- **DB state snapshot:** public_boards=1, total_boards=2, active_places=0, total_links=0, profiles=2, users=2.
- **extraction_costs (Phase 2 D-09 monitoring):** ✅ 1건 google_places $0.0320 / 440ms — resolve-place 호출 자동 기록됨.
- **SECURITY DEFINER 함수 12개:** ✅ `am_board_member`, `am_board_owner`, `can_edit_board`, `can_read_board`, `can_vote_board`, `handle_new_auth_user`, `profiles_create_first_board`, `public_board_view`, `vote_counts_for_places` + PostGIS 3건. 0002 fix_rls_recursion 헬퍼 안전 검증.
- **places RLS policies 4개:** ✅ select/insert/update/delete 모두 `authenticated` + `can_*_board()` 헬퍼 사용. D-7 binary gate가 검증한 그 정책.
- **links schema:** ✅ extraction_status 5-state enum (`pending|processing|ready|failed|manual_review`), source_kind 4-state enum, extraction_confidence numeric(3,2) check 0..1, FK ON DELETE CASCADE/RESTRICT, filtered index on pending state.
- **boards audit:** ✅ "도쿄 6월" (test@test.com, public, slug `gkaswquxpyic`, today updated), "내 첫 여행" (다른 user의 0006 backfill 결과 — `profiles_create_first_board` trigger 검증).
- **middleware:** ✅ `/` HTTP 200, `/boards` HTTP 307 (미인증 redirect to /login) — auth proxy 정확 동작.
- **F-4 strengthened:** 다른 user의 "내 첫 여행" 발견 → 0006 backfill이 기존 사용자에게도 정확 적용됐고, 신규 sign-up trigger도 검증되어야 함 (Day 2 후 새 sign-up test 예정).

### Day 1 metrics

- **보드명:** 도쿄 6월
- **추가한 링크 수:** 0 (samples.json 미작성으로 autonomous 추출 보류)
- **추출 성공/실패:** N/A (링크 미추가)
- **수동 핀 수:** 0
- **공유 활동:** Day 1 보드 행위 **1건** — "도쿄 6월" visibility public 전환 + share_slug 발급 (D-12 충족, 보드 setting 변경도 ≥ 1 활동으로 카운트). 실제 friend share는 본인 복귀 후 (friend-share-checklist.md).
- **발견 이슈:** P0 #1~#4 (incidents.md #1-#4 — 모두 pre-dogfooding gate 단계에서 발견, 모두 same-day fixed, dogfooding 주간 "P0 ≤ 1" 기준에는 무영향)
- **핀 누적:** 보드 `9a7c075a...`의 `places` count where `hidden_at is null` — pending
- **Day 1 commit:** `docs(06): dogfooding day 1 log + 4 pre-gate incidents` — SHA pending

## Day 2 — YYYY-MM-DD

- **보드명:** 
- **추가한 링크 수:** N
- **추출 성공/실패:** 
- **수동 핀 수:** N
- **공유 활동:** 
- **발견 이슈:** 
- **핀 누적:** N
- **Day 2 commit:** `docs(06): dogfooding day 2 log` — SHA `<git rev-parse HEAD>`

## Day 3 — YYYY-MM-DD

- **보드명:** 
- **추가한 링크 수:** N
- **추출 성공/실패:** 
- **수동 핀 수:** N
- **공유 활동:** 
- **발견 이슈:** 
- **핀 누적:** N
- **Day 3 commit:** `docs(06): dogfooding day 3 log` — SHA `<git rev-parse HEAD>`

## Day 4 — YYYY-MM-DD

- **보드명:** 
- **추가한 링크 수:** N
- **추출 성공/실패:** 
- **수동 핀 수:** N
- **공유 활동:** 
- **발견 이슈:** 
- **핀 누적:** N
- **Day 4 commit:** `docs(06): dogfooding day 4 log` — SHA `<git rev-parse HEAD>`

## Day 5 — YYYY-MM-DD

- **보드명:** 
- **추가한 링크 수:** N
- **추출 성공/실패:** 
- **수동 핀 수:** N
- **공유 활동:** (Day 5~6 권장 — 보드에 ≥ 5 핀 있을 때 친구 share. `friend-share-checklist.md` Plan 06-04 산출물)
- **발견 이슈:** 
- **핀 누적:** N
- **Day 5 commit:** `docs(06): dogfooding day 5 log` — SHA `<git rev-parse HEAD>`

## Day 6 — YYYY-MM-DD

- **보드명:** 
- **추가한 링크 수:** N
- **추출 성공/실패:** 
- **수동 핀 수:** N
- **공유 활동:** 
- **발견 이슈:** 
- **핀 누적:** N
- **Day 6 commit:** `docs(06): dogfooding day 6 log` — SHA `<git rev-parse HEAD>`

## Day 7 — YYYY-MM-DD

- **보드명:** 
- **추가한 링크 수:** N
- **추출 성공/실패:** 
- **수동 핀 수:** N
- **공유 활동:** (Day 7: 보드 전체 review, low-conf 핀 confirm/reject 정리)
- **발견 이슈:** 
- **핀 누적:** N (D-13 ≥ 10 PASS gate)
- **Day 7 commit:** `docs(06): dogfooding day 7 log` — SHA `<git rev-parse HEAD>`

---

## End-of-Week SQL Snapshot

Day 7 종료 시 본인이 SQL 2~3개 run 후 결과 paste. 스크립트는 `scripts/dogfooding/*.sql` (Plan 06-03 산출물).

### daily-aggregate.sql

**Command:**
```bash
psql "$SUPABASE_DB_URL" -f scripts/dogfooding/daily-aggregate.sql -v board_id="'<UUID>'"
```

**Expected output (paste here):**
```
 day        | links_added | places_added | places_cumulative
 -----------+-------------+--------------+------------------
 2026-MM-DD | N           | N            | N
 ...
```

→ D-13 PASS: 마지막 행 `places_cumulative ≥ 10`.

### p90-duration.sql

**Command:**
```bash
psql "$SUPABASE_DB_URL" -f scripts/dogfooding/p90-duration.sql -v since="'YYYY-MM-DD'"
```

**Expected output (paste here):**
```
 p90_ms  | avg_ms | n_links
 --------+--------+--------
 NNNNN   | NNNNN  | NN
```

→ SAVE-02 PASS: `p90_ms < 30000`.

### measure-accuracy.sql (Baseline measurement input)

12 sample 영상 URL을 array로 받아 places 추출.

**Command:**
```bash
psql "$SUPABASE_DB_URL" -f scripts/dogfooding/measure-accuracy.sql \
  -v urls="ARRAY['https://youtu.be/A','https://youtu.be/B',...]"
```

→ 결과는 `.planning/dogfooding/extraction-baseline-YYYY-MM-DD.md`에 paste (Plan 06-05 template 사용).

---

## 7일 Pass / Fail Summary

| # | Criterion | Check | Status |
|---|-----------|-------|--------|
| 1 | 7 daily commits | `git log --grep "dogfooding day" --oneline \| wc -l` ≥ 7 | ⬜ |
| 2 | D-12: ≥ 1 보드 행위/day | 각 Day 블록의 "추가한 링크 수" + "수동 핀 수" + "공유 활동" 중 ≥ 1 | ⬜ |
| 3 | D-13: 핀 누적 ≥ 10 | Day 7 places_cumulative ≥ 10 | ⬜ |
| 4 | p90 < 30s | p90-duration.sql 결과 `p90_ms < 30000` | ⬜ |
| 5 | Incidents 정리 | `.planning/dogfooding/incidents.md` "End-of-Week Tally" 채워짐 | ⬜ |

모두 ☑️이면 baseline + friend share + PASS evaluator로 진행 (Plan 06-05).

---

*Daily log template per Phase 6 D-10 / D-11 / D-12 / D-13. Copy → daily-log-YYYY-MM-DD.md → 7회 commit.*
*Document created: 2026-05-26 (Plan 06-03 Task 1)*
