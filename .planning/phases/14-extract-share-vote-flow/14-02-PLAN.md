---
phase: 14-extract-share-vote-flow
plan: 02
type: execute
wave: 2
depends_on: []
files_modified:
  - apps/ios/app/boards/[id].tsx
  - apps/ios/components/boards/place-list.tsx
autonomous: true
must_haves:
  truths:
    - "추출 완료 후 보드 화면에 장소가 리스트로 보인다 — 카드마다 이름·카테고리·출처 ▶칩(타임스탬프)."
    - "장소가 1개 이상이면 지도가 모든 핀을 담도록 fitToCoordinates로 자동 맞춤된다(보드 도시 기본값은 places 0개일 때만)."
    - "리스트는 flat이며 places 그대로 렌더 — 같은 place id는 이미 DB upsert로 dedup되어 한 줄이다."
    - "기존 링크/추출 진행(StepIndicator)·수동 핀(+핀)·마커 탭(PinBottomSheet) 동작은 회귀 없이 유지된다."
  artifacts:
    - path: "apps/ios/components/boards/place-list.tsx"
      provides: "flat 장소 카드 리스트 (이름/카테고리/출처 ▶칩) — props: places, links, onPressSource"
      exports: ["PlaceList"]
    - path: "apps/ios/app/boards/[id].tsx"
      provides: "MapView ref + fitToCoordinates(places) on load + 링크목록 자리를 PlaceList로 교체(링크/진행은 상단 축소)"
      contains: "fitToCoordinates"
---

<objective>
A안(리스트 중심) 화면 재구성. (1) `MapView`에 `ref` 추가, `places` 로드/변경 시 `mapRef.current?.fitToCoordinates(places.map(p => ({latitude,longitude})), { edgePadding, animated:true })` 호출 → `initialRegion` 1회성 도쿄 고정 버그 해소(places 0개면 기존 도시 기본 유지). (2) 현재 링크 FlatList 자리를 **flat 장소 리스트**(`PlaceList` 신규 컴포넌트)로 교체 — 각 카드: `name_local`, `category`, 출처 ▶칩(`▶ {mm:ss}` from `source_timestamp_sec`, place.link_id→link). 링크 목록/추출 진행은 상단에 축소 배치(추출 중 StepIndicator 유지). 출처 ▶칩 탭은 콜백만 노출(실제 플레이어는 14-03).

데이터(`places`, `links`)는 이미 로드됨([boards/[id].tsx:46-60](../../../apps/ios/app/boards/[id].tsx)). 신규 쿼리 없음.
</objective>

## Steps
1. `components/boards/place-list.tsx` 신규: `PlaceList({ places, links, onPressSource })`. linksById Map으로 place.link_id→link.title/url. 출처 ▶칩 라벨은 `source_timestamp_sec`로 `mm:ss`(없으면 "▶ 영상"). 마커 색/저신뢰 규칙은 보드 화면과 일관(`source_kind`/`confidence`).
2. `boards/[id].tsx`:
   - `const mapRef = useRef<MapView>(null)` + `<MapView ref={mapRef} ...>`.
   - `useEffect([places])`: places≥1이면 `mapRef.current?.fitToCoordinates(...)`; 0이면 skip(기본 region 유지).
   - `region` 계산은 places 0개 fallback 용도로 단순화(첫 마운트만). fit이 실질 카메라 담당.
   - 링크 FlatList → `PlaceList` 렌더로 교체. 링크/진행(StepIndicator) 블록은 ListHeader 상단에 축소 유지.
3. `onPressSource(place)` 콜백은 14-03에서 플레이어 시트로 연결(이 plan에선 stub/콘솔 또는 기존 PinBottomSheet open).

## Verify
- `grep -q 'fitToCoordinates' apps/ios/app/boards/[id].tsx`
- `grep -q 'export function PlaceList\|export const PlaceList' apps/ios/components/boards/place-list.tsx`
- `pnpm --filter @moajoa/ios typecheck` green
- (UAT, 14-04 빌드 후) 추출된 보드에서 장소 리스트 표시 + 지도가 핀에 맞춰짐(도쿄 고정 아님)
