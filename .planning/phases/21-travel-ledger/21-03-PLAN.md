---
phase: 21-travel-ledger
plan: 03
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - packages/api/src/queries/ledger.ts
  - packages/api/src/queries/forwarding.ts
  - packages/api/src/queries/ledger.test.ts
  - packages/api/src/queries/index.ts
autonomous: true
requirements: [LEDGER-01, LEDGER-05, LEDGER-06]
must_haves:
  truths:
    - "listLedger/listUnassignedLedger/listNeedsReview가 올바른 필터(.eq trip_id / .is trip_id null / .eq status)로 조회한다"
    - "assignTripToEntry/updateLedgerEntry/deleteLedgerEntry가 행 소유자 write RLS 위에서 동작한다 (파생/파싱 로직 api 금지)"
    - "getOrCreateForwardingAddress가 없으면 INSERT(user_id=auth.uid())로 발급하고 token을 반환한다"
    - "모든 쿼리가 client-first {error} throw 하우스 계약을 따른다"
  artifacts:
    - "packages/api/src/queries/ledger.ts"
    - "packages/api/src/queries/forwarding.ts"
    - "packages/api/src/queries/ledger.test.ts"
  key_links:
    - "ledger.ts 쿼리 → 0022 RLS(trip_id NULL 분기·owner write) 위에서 동작"
    - "LedgerEntry 타입 ← @moajoa/core(21-02)"
    - "getOrCreateForwardingAddress ← ensure_forwarding_token 트리거(0022)가 token 생성"
---

<objective>
`@moajoa/api`에 ledger·forwarding 타입드 쿼리 계층 신설 — iOS(21-05)가 호출. bookings.ts house 계약(client-first {error} throw, 순수 CRUD, 파생 로직 금지)을 미러해 가계부 조회/배정/수정/삭제 + 전달주소 발급 래퍼를 만든다 (LEDGER-01/05/06 api 측면).

Purpose: iOS가 raw supabase 호출 대신 타입드 래퍼로 소비. 파싱·환율·파생은 EF/core 소유 — api는 diff/CRUD만(관심사 분리, bookings.ts T-20-11 idiom).
Output: ledger.ts + forwarding.ts 쿼리 + TDD 테스트(RED 먼저), api typecheck 0.
</objective>

<execution_context>
GSD execute-plan 부재 — 수동. TDD RED→GREEN(bookings.test.ts 하네스). `.js` import 금지.
</execution_context>

<context>
@.planning/phases/21-travel-ledger/21-RESEARCH.md
@.planning/phases/21-travel-ledger/21-PATTERNS.md
@packages/api/src/queries/bookings.ts
@packages/api/src/queries/bookings.test.ts
</context>

## Artifacts this plan produces
- `packages/api/src/queries/ledger.ts` — listLedger/listUnassignedLedger/listNeedsReview/assignTripToEntry/updateLedgerEntry/deleteLedgerEntry
- `packages/api/src/queries/forwarding.ts` — getOrCreateForwardingAddress
- `packages/api/src/queries/ledger.test.ts` — makeChain/makeClient 하네스, 함수별 (테이블/eq/payload 단언 + {error} reject)
- barrel index.ts

<tasks>

<task type="auto">
  <name>Task 1: ledger.test.ts RED (계약 먼저)</name>
  <files>packages/api/src/queries/ledger.test.ts</files>
  <read_first>
    - packages/api/src/queries/bookings.test.ts (makeChain L30-40: select/eq/is/in/not/order/update/insert/delete 체이너블 + single/maybeSingle Promise + thenable; makeClient L42-48)
    - .planning/phases/21-travel-ledger/21-PATTERNS.md §ledger.ts/forwarding.ts
  </read_first>
  <action>
    bookings.test.ts makeChain/makeClient 하네스 복사. 함수별 케이스(구현 전이라 RED):
    - `listLedger(client, tripId)` → `.from('ledger_entries').select('*').eq('trip_id',tripId).order('paid_at',{ascending:false})` 단언 + {error} reject
    - `listUnassignedLedger(client)` → `.is('trip_id', null).order('created_at')` 단언 (미분류 인박스; `.is` 체이너블 커버)
    - `listNeedsReview(client, tripId)` → `.eq('status','needs_review')` 단언
    - `assignTripToEntry(client, entryId, tripId)` → `.update({trip_id: tripId}).eq('id',entryId).select('*').single()` payload 단언
    - `updateLedgerEntry(client, entryId, patch)` → `.update(patch).eq('id',entryId).select('*').single()` (needs_review 수정 — 금액/통화/결제일/status)
    - `deleteLedgerEntry(client, entryId)` → `.delete().eq('id',entryId)`
    - `getOrCreateForwardingAddress(client)` (forwarding) → 존재시 `.select('token').maybeSingle()` 반환 / 없으면 `.insert({user_id})` 후 `.select('token').single()`; auth.uid()는 supabase가 채움(insert payload에 user_id 명시 or RLS default — planner 판단, 테스트는 insert 호출·token 반환 단언)
    각 {error}→`.rejects.toBeTruthy()` 페어.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api test -- ledger</automated>
  </verify>
  <acceptance_criteria>
    - ledger.test.ts 존재, 함수별 테이블/필터 단언 포함
    - 구현 전이라 RED(import 실패 또는 미구현) — 커밋은 RED 상태로(TDD)
  </acceptance_criteria>
  <done>계약 테스트가 RED로 커밋됨</done>
</task>

<task type="auto">
  <name>Task 2: ledger.ts + forwarding.ts GREEN</name>
  <files>packages/api/src/queries/ledger.ts, packages/api/src/queries/forwarding.ts, packages/api/src/queries/index.ts</files>
  <read_first>
    - packages/api/src/queries/bookings.ts (house 계약: (client,...)=>Promise<T>, const {data,error}=...; if(error) throw error; 배럴)
    - packages/api/src/queries/ledger.test.ts (Task 1 — 단언 대상)
    - packages/core/src/schemas/ledger.ts (LedgerEntry 타입)
  </read_first>
  <action>
    bookings.ts idiom으로 구현:
    - **ledger.ts:** 6 함수 전부 `(client: MoajoaSupabaseClient, ...) => Promise<T>`, `const {data,error}=await client.from(...)...; if(error) throw error; return data`. listLedger/listUnassignedLedger/listNeedsReview → `LedgerEntry[]`. assign/update → `LedgerEntry`(single). delete → `void`. **파생·파싱 로직 없음**(주석: 환율·파싱은 EF/core 소유, api는 CRUD만 — bookings.ts T-20-11 미러).
    - **forwarding.ts:** `getOrCreateForwardingAddress(client): Promise<{token:string}>` — `.from('forwarding_addresses').select('token').maybeSingle()` → data 있으면 `{token: data.token}` 반환; 없으면 `.insert({}).select('token').single()`(RLS with check user_id=auth.uid() + 트리거 token 생성; user_id는 클라가 auth.uid()로 채우거나 insert에 명시 — 0022 정책과 정합하게). 
    - barrel: index.ts에 `export * from './ledger'` + `export * from './forwarding'`.
    Task 1 테스트가 GREEN 되도록.
  </action>
  <verify>
    <automated>pnpm --filter @moajoa/api test -- ledger</automated>
  </verify>
  <acceptance_criteria>
    - ledger.test.ts 전 케이스 GREEN
    - `grep -c "export \* from './ledger'" packages/api/src/queries/index.ts` == 1
    - `grep -c "export \* from './forwarding'" packages/api/src/queries/index.ts` == 1
    - `pnpm --filter @moajoa/api typecheck` exit 0
    - `grep -cE "amount_krw.*=|fx_rate.*\*|frankfurter" packages/api/src/queries/ledger.ts` == 0 (파생·파싱 로직 api 금지)
    - api 전체 suite 무회귀
  </acceptance_criteria>
  <done>ledger/forwarding 쿼리가 house 계약대로, 테스트 GREEN, typecheck 0</done>
</task>

</tasks>

<threat_model>
| Threat | Category | Mitigation |
|--------|----------|------------|
| T-21-09 | Info Disclosure | 쿼리가 RLS 우회 시도 → 클라 anon 키 + RLS만(redundant 클라 체크 0). 0022 정책이 게이트 |
| T-21-10 | (correctness) | api에 환율/파생 로직 유입 → grep 게이트로 금지(EF/core 소유) |
</threat_model>

<verification>
- 전 쿼리 client-first {error} throw, RLS-only(클라 권한 체크 0)
- 파생·파싱 로직 api 부재(grep 게이트)
</verification>

<success_criteria>
- iOS(21-05)가 @moajoa/api barrel로 ledger/forwarding 쿼리 import 가능
- 관심사 분리 유지(api=CRUD, EF=파싱/환율, core=파생)
</success_criteria>

<output>
Create `.planning/phases/21-travel-ledger/21-03-SUMMARY.md` when done
</output>
