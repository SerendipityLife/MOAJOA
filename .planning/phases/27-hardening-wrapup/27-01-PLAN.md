---
phase: 27-hardening-wrapup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/functions/extract-youtube/index.ts
  - supabase/tests/extract_gate_smoke.sh
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "비멤버(익명 세션 포함)가 extract-youtube를 호출하면 claim UPDATE·유료 파이프라인 이전에 403을 받는다"
    - "멤버(owner 또는 accepted owner/editor — join_moa 승격 게스트 포함)의 추출 호출은 기존과 동일하게 동작한다 (ready 링크는 409)"
    - "anon-key 원시 토큰 호출은 기존대로 401 (T-18-08 무회귀)"
    - "visibility='shared' 모아의 추출 완료 시에도 revalidate webhook이 발화한다"
  artifacts:
    - path: "supabase/tests/extract_gate_smoke.sh"
      provides: "게이트 3단 스모크 (401/403/409 — 유료 API 발화 0)"
      min_lines: 40
    - path: "supabase/functions/extract-youtube/index.ts"
      provides: "멤버십 게이트 (can_edit_trip 미러) + revalidate shared 조건"
      contains: "canEdit"
  key_links:
    - from: "supabase/functions/extract-youtube/index.ts 멤버십 게이트"
      to: "claim UPDATE (extraction_status→processing)"
      via: "게이트가 claim보다 앞 라인에 위치"
      pattern: "canEdit.*forbidden 블록이 STALE_PROCESSING_MS보다 앞"
    - from: "trips select"
      to: "links.trip_id"
      via: "eq('id', link.trip_id) — board_id 아님 (Pitfall 6)"
      pattern: "select\\('owner_id, city_code, share_slug, visibility'\\)"
    - from: "revalidate webhook 조건"
      to: "shareMoa visibility='shared'"
      via: "public || shared 분기"
      pattern: "visibility === 'shared'"
---

<objective>
SEC-01: extract-youtube Edge Function에 generate-plan의 검증된 멤버십 게이트(T-18-09)를 이식해 비멤버(익명 세션 포함)의 유료 추출 트리거를 403으로 차단한다. 게이트는 claim UPDATE(첫 DB 쓰기) 앞에 배치해 링크 상태 오염까지 차단한다. 같은 파일 재배포에 편승해 revalidate webhook의 visibility 갭('public'만 → 'shared' 포함)을 1줄 확장한다 (재량 결정: RESEARCH Open Q1 권고 채택 — fix 포함, 별도 커밋으로 diff 추적).

Purpose: 익명 세션도 auth.getUser를 통과하므로 현재 비멤버가 임의 link_id로 유료 LLM 왕복을 트리거 가능 — 외부 노출 전 비용 남용 차단 (D-01, D-02).
Output: 게이트 스모크 스크립트(RED→GREEN) + 게이트 적용된 index.ts + revalidate shared fix. 신규 마이그레이션 0 (D-01), 클라이언트 무변경 (D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-hardening-wrapup/27-CONTEXT.md
@.planning/phases/27-hardening-wrapup/27-RESEARCH.md
@.planning/phases/27-hardening-wrapup/27-PATTERNS.md
@supabase/functions/generate-plan/index.ts
@supabase/functions/extract-youtube/index.ts
@supabase/tests/web_share_smoke.sh

<interfaces>
<!-- 미러 원본 — generate-plan/index.ts L116-140 (T-18-09 게이트, verbatim 실측) -->
```typescript
// ---- Edit-rights check (T-18-09, Security V4) -------------------------------
// Must happen BEFORE any paid Claude/Routes call. Mirror the can_edit_trip
// helper (0016 L313-336) with a service-role query, since auth.uid() is null
// under the service role: owner OR an accepted owner/editor membership.
const { data: trip, error: tripErr } = await admin
  .from('trips')
  .select('id, owner_id, start_date, end_date, day_count')
  .eq('id', trip_id)
  .maybeSingle();
if (tripErr) return jsonError(500, tripErr.message);
if (!trip) return jsonError(404, 'trip not found');

let canEdit = trip.owner_id === callerId;
if (!canEdit) {
  const { data: membership } = await admin
    .from('memberships')
    .select('role, accepted_at')
    .eq('trip_id', trip_id)
    .eq('user_id', callerId)
    .maybeSingle();
  canEdit = !!membership &&
    membership.accepted_at !== null &&
    (membership.role === 'owner' || membership.role === 'editor');
}
if (!canEdit) return jsonError(403, 'forbidden');
```

<!-- extract-youtube 기존 구조 (실측 라인) -->
- L71-79: getUser 게이트 (T-18-08) — `caller.user` 확보 지점. `callerId` 상수는 아직 없음 (신설 필요)
- L104-107: KNOWN_SOURCES 체크 — 이 블록 **바로 뒤**가 게이트 삽입 지점
- L118~: claim UPDATE (`extraction_status: 'processing'`) — 게이트는 반드시 이보다 앞
- L143-149: 기존 trip fetch (`select('city_code, share_slug, visibility')`) — 게이트 위치로 이동 + `owner_id` 추가
- L425: revalidate 조건 `if (trip?.visibility === 'public' && trip.share_slug)` — Task 3 대상
- L538-543: `jsonError(status, message)` 헬퍼 — `{ error: message }` shape, 신규 shape 금지 (D-02)

<!-- links 테이블 (0016 L363-383) — 스모크 시드용 NOT NULL 컬럼 -->
links: id(uuid pk), trip_id(uuid NOT NULL), added_by(uuid NOT NULL → profiles), source_kind('youtube'|'blog'|'instagram'|'manual' NOT NULL), url(text NOT NULL), original_url(text NOT NULL), extraction_status(default 'pending')
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 게이트 스모크 스크립트 작성 (RED — 비멤버 403 단언 실패 확인)</name>
  <files>supabase/tests/extract_gate_smoke.sh</files>
  <read_first>
    - supabase/tests/web_share_smoke.sh (부트스트랩·익명 signup·psql 시드·HTTP code 단언 4종 빌딩 블록 — verbatim 계승)
    - supabase/functions/extract-youtube/index.ts (현재 게이트 부재 상태 — 401/409 경로 확인)
    - supabase/migrations/0016_trips_baseline.sql L363-383 (links NOT NULL 컬럼)
  </read_first>
  <behavior>
    - 단언 (1): anon-key 원시 토큰으로 POST /functions/v1/extract-youtube → HTTP 401 (T-18-08 기존 게이트 무회귀)
    - 단언 (2): 비멤버 익명 세션(signup만, join_moa 미호출)으로 ready 링크 호출 → HTTP 403 (신규 게이트 — RED 시점엔 409가 나와 FAIL이어야 정상)
    - 단언 (3): 멤버(owner 세션)로 같은 ready 링크 호출 → HTTP 409 "already" (게이트 통과 실증 — ready라 claim 실패, 유료 API 발화 0)
  </behavior>
  <action>
    `supabase/tests/extract_gate_smoke.sh` 신규 작성 (재량 결정: RESEARCH Open Q3 — web_share_smoke.sh append 대신 신규 소형 스크립트. 기존 파일이 이미 6케이스로 큼, 빌딩 블록은 verbatim 계승). 실행권한 `chmod +x`.

    구성 (web_share_smoke.sh 패턴 verbatim 계승):

    1. 헤더 주석: `# Phase 27 smoke — SEC-01 extract-youtube 멤버십 게이트 (401/403/409, 유료 API 발화 0)` + 전제 주석 2줄: `# 전제: 로컬 supabase 스택 실행 중(colima+docker) + 별도 터미널에서 supabase functions serve extract-youtube` + `# 주의: 익명 signup은 IP당 30/hr rate limit — 반복 실행 시 유의.` (Pitfall 5)
    2. 부트스트랩 (web_share_smoke.sh L6-9 verbatim):
       ```bash
       set -euo pipefail
       DB="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
       API="http://127.0.0.1:54321"
       ANON_KEY=$(supabase status -o env 2>/dev/null | grep ANON_KEY | cut -d= -f2 | tr -d '"')
       ```
    3. 호스트 signup + id 확보 (L28-33 패턴): EMAIL="gatehost@local.test", signup `|| true`, `HOST=$(psql "$DB" -tAc "select id from auth.users where email='$EMAIL' limit 1")`. 호스트 JWT는 signup이 기존 유저로 실패할 수 있으므로 password grant로 확보:
       ```bash
       HOST_JWT=$(curl -s -X POST "$API/auth/v1/token?grant_type=password" \
         -H "apikey: $ANON_KEY" -H "Content-Type: application/json" \
         -d "{\"email\":\"$EMAIL\",\"password\":\"password-gate-1\"}" \
         | python3 -c "import json,sys; print(json.load(sys.stdin)['access_token'])")
       ```
    4. trip + ready 링크 시드 (psql -qtAc, `-q` 필수 — INSERT RETURNING 커맨드 태그 혼입 방지):
       ```bash
       TRIP=$(psql "$DB" -qtAc "insert into trips (owner_id, title) values ('$HOST','gate-smoke-$(date +%s)') returning id")
       LINK=$(psql "$DB" -qtAc "insert into links (trip_id, added_by, source_kind, url, original_url, extraction_status) values ('$TRIP','$HOST','youtube','https://www.youtube.com/watch?v=gatesmoke','https://www.youtube.com/watch?v=gatesmoke','ready') returning id")
       ```
       source_kind='youtube' 필수 — 게이트가 KNOWN_SOURCES 체크 **뒤**라서 manual이면 400으로 먼저 걸림. extraction_status='ready' 필수 — 무비용 트릭(Pitfall 3)의 전제.
    5. 비멤버 익명 세션 (L12-16 패턴): `POST $API/auth/v1/signup` with `-d '{"data":{"name":"게이트게스트"}}'` → `GUEST_JWT` 추출 (python3 json 파싱). join_moa는 **호출하지 않음** (비멤버 유지가 핵심).
    6. 단언 3종 (curl `-s -o /dev/null -w '%{http_code}'` 스타일, web_share_smoke.sh L55-58 미러):
       ```bash
       C1=$(curl -s -o /dev/null -w '%{http_code}' -X POST "$API/functions/v1/extract-youtube" \
         -H "Authorization: Bearer $ANON_KEY" -H "Content-Type: application/json" \
         -d "{\"link_id\":\"$LINK\"}")
       [ "$C1" = "401" ] || { echo "FAIL: (1) anon-key raw → $C1 (want 401)"; exit 1; }
       C2=$(...동일, Bearer $GUEST_JWT...)
       [ "$C2" = "403" ] || { echo "FAIL: (2) non-member anon session → $C2 (want 403 — SEC-01 gate)"; exit 1; }
       C3=$(...동일, Bearer $HOST_JWT...)
       [ "$C3" = "409" ] || { echo "FAIL: (3) owner on ready link → $C3 (want 409 already)"; exit 1; }
       echo "PASS: extract gate smoke (401/403/409)"
       ```
    7. RED 실행: `supabase start` 상태 확인 후 별도 백그라운드로 `supabase functions serve extract-youtube` 기동 → 스크립트 실행 → **단언 (2)에서 FAIL exit 1 (C2=409) 확인이 RED 성공 조건** (게이트 부재 시 비멤버도 claim까지 도달해 409).

    커밋: `test(27-01): add failing extract gate smoke — non-member expects 403, currently 409`
  </action>
  <verify>
    <automated>bash supabase/tests/extract_gate_smoke.sh; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/tests/extract_gate_smoke.sh` 존재 + 실행권한 (`test -x`)
    - `grep -c "403" supabase/tests/extract_gate_smoke.sh` ≥ 1, `grep -c "409" ...` ≥ 1, `grep -c "401" ...` ≥ 1
    - `grep -n "join_moa" supabase/tests/extract_gate_smoke.sh` → 0건 (비멤버 세션 유지 — 멤버 케이스는 owner HOST_JWT로 커버)
    - `grep -n "extraction_status" supabase/tests/extract_gate_smoke.sh`에 `'ready'` 포함 (무비용 트릭)
    - RED 실행 로그: 단언 (1) 통과 후 단언 (2)에서 `FAIL: (2) non-member anon session → 409` exit 1
  </acceptance_criteria>
  <done>스모크 스크립트가 존재하고, 게이트 부재 상태에서 비멤버 403 단언이 409로 실패(RED)하며, 401 단언은 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: 멤버십 게이트 이식 (GREEN — generate-plan T-18-09 미러, claim 앞 배치)</name>
  <files>supabase/functions/extract-youtube/index.ts</files>
  <read_first>
    - supabase/functions/extract-youtube/index.ts (전문 — 특히 L71-79 getUser, L104-107 KNOWN_SOURCES, L118~ claim, L143-149 기존 trip fetch)
    - supabase/functions/generate-plan/index.ts L110-145 (미러 원본 — callerId 상수 L114 + 게이트 블록)
  </read_first>
  <action>
    D-01 구현. 세 부분 수정 (수술적 — 다른 라인 무접촉):

    **(1) callerId 상수 신설** — getUser 게이트 직후(L79 `return jsonError(401, 'unauthorized');` 닫힌 뒤): generate-plan L114 미러로 `const callerId = caller.user.id;` 추가. (배치는 body 파싱 앞이든 게이트 직전이든 실행자 재량 — generate-plan은 getUser 직후.)

    **(2) 멤버십 게이트 삽입** — KNOWN_SOURCES 체크 블록(L104-107) **바로 뒤**, claim 주석(`// ---- Claim the link (atomic)`) **앞**에 다음 블록 삽입. 동시에 기존 trip fetch(L143-149, `// Trip row feeds the LLM city hint...` 주석 포함 7줄)를 **삭제**하고 이 위치로 통합 (신규 쿼리는 memberships 1개뿐):

    ```typescript
    // ---- Membership gate (SEC-01, Security V4) ------------------------------
    // Must happen BEFORE the claim UPDATE (first DB write) and any paid call —
    // a non-member must not be able to flip extraction_status to 'processing'.
    // Mirror the can_edit_trip helper (0016 L313-336) with a service-role query,
    // since auth.uid() is null under the service role: owner OR an accepted
    // owner/editor membership. The trip row is also reused downstream for the
    // LLM city hint, the Places language choice, and the revalidate webhook.
    const { data: trip, error: tripErr } = await admin
      .from('trips')
      .select('owner_id, city_code, share_slug, visibility')
      .eq('id', link.trip_id)
      .maybeSingle();
    if (tripErr) return jsonError(500, tripErr.message);
    if (!trip) return jsonError(404, 'trip not found');

    let canEdit = trip.owner_id === callerId;
    if (!canEdit) {
      const { data: membership } = await admin
        .from('memberships')
        .select('role, accepted_at')
        .eq('trip_id', link.trip_id)
        .eq('user_id', callerId)
        .maybeSingle();
      canEdit = !!membership &&
        membership.accepted_at !== null &&
        (membership.role === 'owner' || membership.role === 'editor');
    }
    if (!canEdit) return jsonError(403, 'forbidden');
    ```

    핵심 어휘 치환 (Pitfall 6): `eq('id', link.trip_id)` — CONTEXT D-01 원문의 "board_id"는 구 표기, 실제 컬럼은 `links.trip_id`. select는 generate-plan의 `start_date, end_date, day_count` 대신 extract-youtube 기존 용도 컬럼 `city_code, share_slug, visibility` + **owner_id 추가**.

    **(3) 기존 trip fetch 자리(구 L143-149)** — 블록 삭제만. 후속 사용처 `trip?.city_code`(cityHint)·revalidate 블록의 `trip` 참조는 이동된 선언이 같은 스코프 상위에 있으므로 무변경 동작. `if (!trip) return 404` 강화로 "trip 없어도 진행" 시맨틱이 바뀌지만 links.trip_id FK cascade상 링크가 있으면 trip이 반드시 존재 — 실질 무영향 (RESEARCH §이식 스케치).

    금지: 신규 마이그레이션·RPC (D-01), jsonError 외 신규 에러 shape (D-02), `can_edit_trip` RPC 직접 호출 (service-role에서 auth.uid()=null → 항상 false, Anti-Pattern 2), resolve-place 등 다른 EF 접촉 (D-03 — resolve-place getUser 부재는 CONTEXT Deferred에 이미 flag됨, 변경 금지).

    GREEN 검증: 스모크 재실행 → 401/403/409 전부 통과 exit 0. EF pipeline 무회귀: `cd supabase/functions/extract-youtube && deno task test` → 31 green.

    커밋: `feat(27-01): extract-youtube membership gate — mirror generate-plan T-18-09 before claim (SEC-01)`
  </action>
  <verify>
    <automated>bash supabase/tests/extract_gate_smoke.sh && cd supabase/functions/extract-youtube && deno task test</automated>
  </verify>
  <acceptance_criteria>
    - 스모크 exit 0 + 출력 `PASS: extract gate smoke (401/403/409)`
    - `deno task test` → 31 pass (기준선, Phase 28 검증 시점)
    - `grep -n "const callerId = caller.user.id" supabase/functions/extract-youtube/index.ts` → 1건
    - `grep -n "link.trip_id" supabase/functions/extract-youtube/index.ts` → 게이트 trips select + memberships eq 포함 (board_id 문자열 0건: `grep -c "board_id" ...` = 0)
    - 게이트가 claim 앞: `grep -n "canEdit\|STALE_PROCESSING_MS" supabase/functions/extract-youtube/index.ts`에서 마지막 `canEdit` 라인 번호 < `STALE_PROCESSING_MS` 라인 번호
    - `grep -c "from('trips')" supabase/functions/extract-youtube/index.ts` = 1 (fetch 이동·통합 — 중복 fetch 0)
    - `git diff --stat supabase/migrations apps/ios` → 0파일 (append-only·iOS 동결 준수)
  </acceptance_criteria>
  <done>비멤버 익명 세션 403, 멤버 409, anon-key 401이 로컬 스모크로 전부 실증되고 EF 단위 테스트 31개가 그린이다. 클라이언트 코드 diff 0.</done>
</task>

<task type="auto">
  <name>Task 3: revalidate webhook visibility 조건 확장 ('shared' 포함 — 별도 커밋)</name>
  <files>supabase/functions/extract-youtube/index.ts</files>
  <read_first>
    - supabase/functions/extract-youtube/index.ts L418-440 (revalidate webhook 블록 — Task 2 이후 라인 이동 가능, `revalidate-webhook` grep으로 위치 확인)
    - packages/api/src/queries/trips.ts L212-227 (shareMoa가 visibility='shared'를 쓰는 근거 — 읽기만, 무변경)
  </read_first>
  <action>
    재량 결정 채택 (RESEARCH Open Q1 권고): v2.1 웹 공유 `shareMoa`는 `visibility='shared'`로 UPDATE하고 `/t/[slug]`는 'shared'도 렌더하는데(0029), revalidate webhook은 `'public'`일 때만 발화 → 공유된 모아의 추출 완료가 SSR 캐시(1h TTL)를 안 깨움. D-01과 같은 파일·같은 배포이므로 1줄 확장으로 해소. diff 추적을 위해 별도 커밋 (Task 2와 분리).

    변경 (현재 L425 — Task 2 이후 라인 시프트 주의, `trip?.visibility === 'public'` grep으로 위치 확인):
    ```typescript
    // 변경 전
    if (trip?.visibility === 'public' && trip.share_slug) {
    // 변경 후
    if ((trip?.visibility === 'public' || trip?.visibility === 'shared') && trip.share_slug) {
    ```
    바로 위 주석의 `only POST when visibility=public + slug present`도 `visibility=public/shared + slug present`로 갱신 (주석-코드 정합). 그 외 블록 무변경 (fire-and-forget·.catch 유지).

    참고: `['public','shared'].includes(...)` 형태는 TS narrowing을 깨서 `trip.share_slug` 접근이 타입 에러가 될 수 있음 — 명시적 `||` 비교 사용.

    커밋: `fix(27-01): fire revalidate webhook for shared trips too — shareMoa uses visibility='shared'`
  </action>
  <verify>
    <automated>grep -n "visibility === 'shared'" supabase/functions/extract-youtube/index.ts && cd supabase/functions/extract-youtube && deno task test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "visibility === 'shared'" supabase/functions/extract-youtube/index.ts` = 1 (revalidate 블록 내)
    - `grep -n "visibility=public/shared" supabase/functions/extract-youtube/index.ts` → 1건 (주석 갱신)
    - `deno task test` → 31 pass 유지
    - 이 커밋의 diff가 revalidate 블록 2줄(조건+주석)에 한정: `git show --stat HEAD` 1파일, `git show HEAD | grep -c "^[+-][^+-]"` ≤ 6
  </acceptance_criteria>
  <done>shared 모아의 추출 완료도 revalidate webhook을 발화하는 조건이 커밋되고 EF 테스트가 그린이다. (라이브 발화 확인은 27-04 UAT에서 SSR 갱신으로 겸사 검증.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 클라이언트 → extract-youtube EF | verify_jwt 통과한 모든 Bearer 토큰 (익명 세션 포함 — untrusted) |
| EF → DB (service-role) | RLS bypass — 게이트 통과 후에만 쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | E/D(비용) | extract-youtube 핸들러 — 익명 세션 유료 추출 남용 (SEC-01 갭) | mitigate | Task 2: can_edit_trip 미러 게이트를 첫 DB 쓰기(claim)·유료 호출 전에 배치 → 403 |
| T-27-02 | T | links.extraction_status — 비멤버가 남의 링크를 'processing'으로 오염 (10분 stale 창) | mitigate | Task 2: 게이트가 claim UPDATE 앞 (Anti-Pattern 1 회피) |
| T-27-03 | I | public_trip_view가 anon에 link id 노출 → link_id 열거 | mitigate | 게이트로 link_id 지식만으로는 무력화 — 멤버십 필요 (Task 2가 동시 해소) |
| T-27-04 | D(비용, $0.032/call) | resolve-place getUser 게이트 부재 (실측 확인 — 헤더 주석 L9-10 의도적 설계) | accept | D-03 잠금: 이번 phase 변경 금지. CONTEXT Deferred에 향후 하드닝 항목으로 flag 완료 |
| T-27-05 | S | revalidate webhook secret | accept(기존 통제) | timingSafeEqual 기존 유지 — Task 3은 발화 조건만 확장, secret 경로 무접촉 |
</threat_model>

<verification>
- `bash supabase/tests/extract_gate_smoke.sh` exit 0 (401/403/409 — 유료 API 발화 0)
- `cd supabase/functions/extract-youtube && deno task test` → 31 pass
- `git diff main --stat -- supabase/migrations apps/ios packages/` → 0파일 (마이그레이션 0·iOS 동결·클라이언트 무변경 D-02)
- 커밋 3개: test(RED) → feat(gate GREEN) → fix(revalidate)
</verification>

<success_criteria>
- 비멤버 익명 세션의 extract-youtube 호출이 claim 이전 403으로 거부된다 (로컬 실증)
- 멤버·anon-key 경로 무회귀 (409/401), EF pipeline 테스트 31 그린
- revalidate 조건이 'shared'를 포함한다
- 프로덕션 반영은 27-03(배포) 몫 — 이 plan에서는 배포하지 않음 (Pitfall 1: 머지 ≠ 배포 — wave 2에서 처리)
</success_criteria>

<output>
After completion, create `.planning/phases/27-hardening-wrapup/27-01-SUMMARY.md`
</output>
