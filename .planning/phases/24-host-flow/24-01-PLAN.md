---
phase: 24-host-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - apps/web/package.json
  - pnpm-lock.yaml
  - supabase/migrations/0026_realtime_publication.sql
  - supabase/tests/realtime_publication_smoke.sh
  - supabase/tests/realtime_events_smoke.mjs
  - packages/api/src/types/database.ts
autonomous: true
requirements: [MOA-03]
must_haves:
  truths:
    - "0026이 places·links를 supabase_realtime publication에 등록해, postgres_changes 구독이 이벤트를 실수신한다 (D-14의 무음 no-op 방지)"
    - "설치된 supabase-js가 선언 버전 2.110.x와 일치한다 (stale 2.45.4 해소)"
    - "원격 DB에 0024·0025·0026이 적용되어 Vercel Preview e2e가 가능하다"
  artifacts:
    - path: "supabase/migrations/0026_realtime_publication.sql"
      provides: "supabase_realtime publication에 places·links 추가"
      contains: "alter publication supabase_realtime add table"
    - path: "supabase/tests/realtime_publication_smoke.sh"
      provides: "publication 등록 + 이벤트 실수신 + RLS 필터 검증 하네스"
    - path: "supabase/tests/realtime_events_smoke.mjs"
      provides: "postgres_changes WALRUS 이벤트 수신 node 스모크"
  key_links:
    - from: "supabase/tests/realtime_publication_smoke.sh"
      to: "pg_publication_tables"
      via: "psql 카운트 단언"
      pattern: "pg_publication_tables"
    - from: "supabase/tests/realtime_events_smoke.mjs"
      to: "postgres_changes"
      via: "supabase-js channel().on('postgres_changes',...)"
      pattern: "postgres_changes"
---

<objective>
Phase 24 실행 환경을 잠근다: (1) stale node_modules 해소 + react-day-picker 설치, (2) 레포 최초 realtime publication 마이그레이션 0026, (3) [BLOCKING] 로컬 적용·타입 재생성·이벤트 실수신 스모크·원격 push.

Purpose: D-14(postgres_changes)는 publication 미등록 시 "구독 SUBSCRIBED인데 이벤트 0건"으로 조용히 죽는다(RESEARCH Pitfall 2). 원격은 0023까지만 적용 상태(23-07 실측)라 push 없이는 프로덕션에서 share_mode·seq_no가 전부 런타임 에러다. 이 plan이 통과해야 이후 모든 plan의 검증이 유효하다.
Output: 0026 마이그레이션 + 스모크 하네스 2종 + 로컬/원격 DB 정합 + supabase-js 2.110.x 실체화.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-host-flow/24-CONTEXT.md
@.planning/phases/24-host-flow/24-RESEARCH.md (Pattern 1, Pitfall 1·2·3)
@supabase/migrations/0025_web_share.sql (헤더·결정기록 주석 스타일 analog)
@supabase/tests/web_share_smoke.sh (bash 하네스 선례 — exit code 단언 스타일)

핵심 환경 사실 (재발견 금지):
- 실행은 메인 트리에서 순차 (worktree에서 pnpm 모노레포 깨짐 — 메모리 노트)
- colima Docker + supabase local stack 실행 중 (0025까지 적용 상태)
- `supabase db reset`은 KAKAO env 로딩 필요 — gitignored `supabase/.env.local`에 더미값 실존 (23-04 선례)
- `pnpm -r test`는 vitest watch로 hang — 절대 사용 금지
- 원격 프로젝트: `xfoauhsraguyrifingct` (linked), 0016~0023 정합·0024/0025 미적용 (23-07 실측)
- 로컬 anon/service_role 키·DB URL은 `supabase status` 출력에서 획득 (로컬 데모 키 — 시크릿 아님)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 환경 게이트 — pnpm install + react-day-picker 9.14.0 설치</name>
  <files>package.json, apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - pnpm-lock.yaml (첫 30줄 — supabase-js 선언 버전 확인용)
    - apps/web/package.json (dependencies 블록)
  </read_first>
  <action>
    1. 레포 루트에서 `pnpm install` 실행 — lockfile에 선언된 @supabase/supabase-js ^2.110.0을 실체화한다 (현재 node_modules는 2.45.4 stale — RESEARCH Pitfall 1, F-20-3에서 presence가 이걸로 조용히 죽은 전례).
    2. 설치본 검증: `node -e "console.log(require('@supabase/supabase-js/package.json').version)"` 출력이 2.110.x여야 한다.
    3. `pnpm --filter web add react-day-picker@9.14.0` — D-06 캘린더 range 픽커 (RESEARCH 잠금: 9.14.0 정확히, v10은 @daypicker/react로 개명된 신규 메이저라 회피).
    4. 무회귀 확인: `pnpm --filter @moajoa/core test:run && pnpm --filter @moajoa/api test:run && pnpm --filter web test:run` 전부 그린 (기존 core 169·api 81·web 65).
  </action>
  <verify>
    <automated>node -e "const v=require('@supabase/supabase-js/package.json').version; if(!v.startsWith('2.1')) process.exit(1); console.log(v)" && node -e "console.log(require('/Users/wcb/Documents/MOAJOA/apps/web/node_modules/react-day-picker/package.json').version)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('@supabase/supabase-js/package.json').version)"` 출력이 `2.110` prefix (2.45.4 아님)
    - `grep '"react-day-picker"' apps/web/package.json` → `9.14.0` 포함
    - 3패키지 test:run 전부 exit 0 (watch 모드 금지 — test:run만)
  </acceptance_criteria>
  <done>supabase-js 2.110.x 실체화 + react-day-picker 9.14.0 설치 + 기존 3패키지 스위트 무회귀 그린</done>
</task>

<task type="auto">
  <name>Task 2: 0026 마이그레이션 + realtime 스모크 하네스 작성</name>
  <files>supabase/migrations/0026_realtime_publication.sql, supabase/tests/realtime_publication_smoke.sh, supabase/tests/realtime_events_smoke.mjs</files>
  <read_first>
    - supabase/migrations/0025_web_share.sql (헤더 주석 스타일 — 목적·결정기록·append-only 명시 미러)
    - supabase/tests/web_share_smoke.sh (bash exit-code 단언 스타일 선례)
    - supabase/migrations/0016_trips_baseline.sql 중 places 테이블 정의 부분 (grep -n "create table places" 후 해당 블록 — 스모크 INSERT 필수 컬럼 확인)
  </read_first>
  <action>
    1. `supabase/migrations/0026_realtime_publication.sql` 신규 작성 (0016~0025 절대 무수정 — append-only):
    ```sql
    -- 0026_realtime_publication.sql — Phase 24 Host Flow (D-14 postgres_changes)
    --
    -- 결정 기록:
    --   D-14: 추출 완료 실시간 반영은 Supabase Realtime postgres_changes (presence 금지).
    --   postgres_changes는 supabase_realtime publication 멤버십이 전제 — 이 레포의 어떤
    --   마이그레이션도 테이블을 등록한 적 없음 (grep 0건). 등록 없이는 구독이
    --   SUBSCRIBED 상태로 이벤트 0건 무음 no-op.
    --   범위: places(INSERT 반영) + links(extraction_status UPDATE 반영)만.
    --   votes는 Phase 25 몫(0027) — D-14가 places/links만 잠갔다 (RESEARCH Open Q3).
    --
    -- Append-only: 0016..0025 are NEVER modified.

    alter publication supabase_realtime add table places;
    alter publication supabase_realtime add table links;
    ```
    2. `supabase/tests/realtime_events_smoke.mjs` 신규 작성 — postgres_changes 실수신 + WALRUS RLS 필터 검증 (RESEARCH A3 실증). 로직:
       - env `SUPABASE_URL`(기본 `http://127.0.0.1:54321`)·`SUPABASE_ANON_KEY`·`SUPABASE_SERVICE_ROLE_KEY` 사용 (`supabase status`에서 획득해 호출측 셸이 주입)
       - `createClient(url, anonKey)`로 owner 클라이언트 생성 → `auth.signUp({ email: \`rt-smoke-\${Date.now()}@test.local\`, password: 'smoke-pass-1234' })` (로컬은 confirm 불요)
       - owner로 `from('trips').insert({ title: 'rt smoke', city_code: 'tokyo' }).select('id').single()` → tripId
       - owner 클라이언트: `client.channel(\`moa:\${tripId}\`).on('postgres_changes', { event: 'INSERT', schema: 'public', table: 'places', filter: \`trip_id=eq.\${tripId}\` }, cb).subscribe()` — SUBSCRIBED 대기
       - 두 번째 `createClient(url, anonKey)`로 **비멤버** 클라이언트 생성(`auth.signInAnonymously()`) → 동일 filter로 places INSERT 구독 (RLS 배제 검증용)
       - service_role 클라이언트로 `from('places').insert({ trip_id: tripId, added_by: ownerUserId, name_local: 'rt smoke place', lat: 35.0, lng: 139.0, source_kind: 'manual' })` (seq_no는 0024 트리거 채번 — 절대 전송 금지)
       - 10초 타임아웃: owner가 이벤트 1건 수신하면 PASS 절반, 비멤버가 0건이면 PASS 나머지 절반 (비멤버 수신 시 exit 1 — WALRUS RLS 누출)
       - 종료 전 `removeChannel` 양쪽 + `process.exit(0|1)`
    3. `supabase/tests/realtime_publication_smoke.sh` 신규 작성 (실행권한 chmod +x, `bash -n` 클린):
       - `set -euo pipefail`
       - DB_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"
       - 단언 1: `psql "$DB_URL" -tAc "select count(*) from pg_publication_tables where pubname='supabase_realtime' and tablename in ('places','links')"` 결과가 정확히 `2` 아니면 exit 1
       - 단언 2: `supabase status -o env`에서 ANON_KEY·SERVICE_ROLE_KEY 추출해 `node supabase/tests/realtime_events_smoke.mjs` 실행, exit code 전파
       - 23-01 하네스 관례: 각 단계 echo 라벨 + 실패 시 비영(非零) exit
  </action>
  <verify>
    <automated>bash -n supabase/tests/realtime_publication_smoke.sh && node --check supabase/tests/realtime_events_smoke.mjs && grep -c "alter publication supabase_realtime add table" supabase/migrations/0026_realtime_publication.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "alter publication supabase_realtime add table" supabase/migrations/0026_realtime_publication.sql` == 2 (places 1줄 + links 1줄)
    - `grep "votes" supabase/migrations/0026_realtime_publication.sql | grep -v "^--"` == 0건 (votes는 SQL 문장에 없음 — 주석만 허용)
    - `git diff --stat supabase/migrations/0016* supabase/migrations/0024* supabase/migrations/0025*` == 변경 0 (append-only)
    - `bash -n supabase/tests/realtime_publication_smoke.sh` exit 0, `test -x supabase/tests/realtime_publication_smoke.sh` true
    - realtime_events_smoke.mjs에 `seq_no` 문자열이 INSERT payload에 없음 (`grep -n "seq_no" supabase/tests/realtime_events_smoke.mjs`는 주석 라인만 허용)
  </acceptance_criteria>
  <done>0026 마이그레이션(places+links만) + 스모크 스크립트 2종 작성·문법 클린. 실행은 Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] 스키마 적용 게이트 — 로컬 reset + 타입 재생성 + 스모크 실행 + 원격 push</name>
  <files>packages/api/src/types/database.ts</files>
  <read_first>
    - supabase/tests/realtime_publication_smoke.sh (Task 2 산출물 — 실행 전 최종 상태 확인)
    - .planning/phases/24-host-flow/24-RESEARCH.md의 Pitfall 3 (원격 0023 상태)
  </read_first>
  <action>
    **이 태스크는 [BLOCKING]이다 — 실패 시 이후 wave 진행 금지.**

    1. 로컬 적용: `supabase db reset` (0016~0026 전체 클린 적용 — 42P17 recursion 0건이어야 함. KAKAO env는 gitignored `supabase/.env.local` 더미값이 로딩됨, 23-04 선례. 스택이 이미 실행 중이므로 reset만으로 충분).
    2. 타입 재생성: `pnpm supabase:types` — 0026은 스키마 무변경이라 `packages/api/src/types/database.ts` diff 0 예상이지만 관례 준수 (CLAUDE.md §4.3).
    3. 로컬 스모크 실행: `bash supabase/tests/realtime_publication_smoke.sh` — exit 0 필수. 실패 시 publication 등록 또는 WALRUS RLS 문제 — 0026/스크립트 수정 후 재실행 (0016~0025는 무수정).
    4. 원격 push (Vercel Preview e2e 선행조건 — 23-07 잠금): 
       - `supabase migration list` 로 현재 상태 확인 (원격 0023까지 예상)
       - `supabase db push` 실행 — 0024·0025·0026 세 개가 적용 대상. 비대화형 환경에서 확인 프롬프트가 뜨면 `echo Y | supabase db push`로 재시도. `SUPABASE_ACCESS_TOKEN` 미설정·DB 비밀번호 프롬프트로 진행 불가하면 **여기서 멈추고 auth gate 체크포인트 생성** — 사용자에게 "터미널에서 `supabase db push` 직접 실행 또는 SUPABASE_ACCESS_TOKEN·SUPABASE_DB_PASSWORD 환경변수 제공"을 요청하고 완료 신호 후 5로 진행.
       - push 후 `supabase migration list`로 원격에 0024·0025·0026 정합 확인.
    5. 무회귀: `pnpm --filter @moajoa/core test:run && pnpm --filter @moajoa/api test:run` 그린 + `pnpm --filter @moajoa/api typecheck` exit 0.
  </action>
  <verify>
    <automated>bash supabase/tests/realtime_publication_smoke.sh && pnpm --filter @moajoa/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db reset` 출력에 `0026_realtime_publication` 적용 라인 존재, `42P17` 문자열 0건
    - `bash supabase/tests/realtime_publication_smoke.sh` exit 0 (publication 카운트 2 + owner 이벤트 수신 + 비멤버 0건)
    - `supabase migration list` 출력에서 0024·0025·0026이 Local·Remote 양쪽 정합 (또는 auth gate 체크포인트로 사용자 완료 확인)
    - core·api test:run 그린, api typecheck exit 0
  </acceptance_criteria>
  <done>로컬 DB 0026 적용 + postgres_changes 이벤트 실수신 실증(WALRUS RLS 필터 포함) + 원격 0024/0025/0026 push 완료. 이후 plan들의 realtime 코드가 무음 no-op 없이 동작하는 기반 확보.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB WAL → Realtime → 브라우저 | publication에 등록된 테이블의 변경이 구독자에게 흐름 — RLS(WALRUS)가 유일한 필터 |
| 로컬 CLI → 원격 프로덕션 DB | `supabase db push`가 prod 스키마를 변경 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Info Disclosure | 0026 publication (places·links WAL 노출) | mitigate | WALRUS가 구독자 JWT로 RLS 평가 — realtime_events_smoke.mjs가 비멤버 0건 수신을 exit code로 단언 (Task 2·3) |
| T-24-02 | Tampering | 원격 push (prod 스키마) | mitigate | append-only 준수 — 0016~0025 git diff 0 단언, 0026은 additive(publication만·데이터 무변경). push 후 migration list 정합 확인 |
| T-24-03 | Info Disclosure | service_role 키 (스모크 스크립트) | accept | 로컬 데모 키만 env로 주입(`supabase status` 출력) — 하드코딩 0, 원격 키 미사용, 스크립트는 127.0.0.1 기본값 |
</threat_model>

<verification>
- `supabase db reset` 클린 (42P17 = 0) + 0026 적용 라인
- `bash supabase/tests/realtime_publication_smoke.sh` exit 0
- `supabase migration list` 원격 0024·0025·0026 정합
- supabase-js 설치본 2.110.x + react-day-picker 9.14.0
- 3패키지 test:run 무회귀 그린
</verification>

<success_criteria>
- postgres_changes 구독이 로컬에서 이벤트를 실수신하고, 비멤버는 수신하지 못함이 exit code로 증명됨
- 원격 DB가 0026까지 정합 — Vercel Preview e2e 차단 요소 해소
- 이후 plan이 소비할 react-day-picker·supabase-js 2.110.x 준비 완료
</success_criteria>

<output>
After completion, create `.planning/phases/24-host-flow/24-01-SUMMARY.md`
</output>
