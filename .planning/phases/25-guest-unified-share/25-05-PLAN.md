---
phase: 25-guest-unified-share
plan: 05
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - supabase/tests/web_share_smoke.sh
  - supabase/tests/realtime_events_smoke.mjs
  - supabase/tests/place_seq_concurrency.sh
autonomous: true
requirements: [AUTH-08, SHARE-03, SHARE-04]

must_haves:
  truths:
    - "익명 세션(비 device_token)이 join_moa 후 places INSERT(editor)·votes·trip_messages를 RLS 통과로 write할 수 있다 (SHARE-03)"
    - "게스트 익명 세션의 places/votes INSERT가 moa 채널로 fan-out되고 비멤버 익명은 0건 수신한다 (SHARE-04)"
    - "게스트가 추가한 장소가 이어지는 순번(#N+1)을 받고 결번·중복이 없다 (SHARE-04, MOA-01)"
  artifacts:
    - path: "supabase/tests/web_share_smoke.sh"
      provides: "익명 멤버 RLS 프로브 (places/votes/trip_messages)"
      contains: "signInAnonymously"
    - path: "supabase/tests/realtime_events_smoke.mjs"
      provides: "게스트 익명 세션 realtime fan-out 케이스"
      contains: "anon"
  key_links:
    - from: "익명 세션 auth.uid"
      to: "join_moa → places INSERT RLS"
      via: "authenticated role 통과"
      pattern: "join_moa"
---

<objective>
게스트 참여의 **DB/realtime 레이어를 직접 실증**하는 bash/mjs 스모크 확장 — 웹 UI 없이 익명 세션(auth.uid, 비 device_token)이 SHARE-03/04를 만족함을 증명한다. 기존 하네스 3종에 익명-세션 케이스를 append(신규 파일 아님, 확장). (1) `web_share_smoke.sh` — 익명 signup→join_moa→places INSERT(editor)·votes·trip_messages RLS 통과 프로브(비멤버 익명은 0건). (2) `realtime_events_smoke.mjs` — 게스트 익명 세션 places INSERT가 moa 채널로 fan-out·비멤버 익명 0건. (3) `place_seq_concurrency.sh` — 게스트(익명) 추가 장소가 이어지는 #N+1 순번.

Purpose: 웹 컴포넌트 테스트(Plan 03)는 mock이라 실 RLS·realtime·순번을 검증 못 한다. 이 스모크가 SHARE-03/04의 DB 진실을 phase gate로 잠근다.
Output: 익명-세션 케이스가 추가된 스모크 3종(exit 0).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-guest-unified-share/25-RESEARCH.md
@.planning/phases/25-guest-unified-share/25-PATTERNS.md

<interfaces>
기존 스모크(확장 대상, 직접 read):
- `supabase/tests/web_share_smoke.sh` — 23-02/23-04 익명 signup·join_moa·trip_messages RLS 프로브 스타일(is_anonymous·role·both→editor/dates→voter 실증).
- `supabase/tests/realtime_events_smoke.mjs` — 24-01 owner setAuth→places INSERT postgres_changes 수신 ≥1 AND 비멤버 익명 0건(WALRUS RLS) exit code 패턴. 2회 insert 재시도 하드닝(reset 직후 WAL 콜드스타트).
- `supabase/tests/place_seq_concurrency.sh` — 23-04 MOA-01 동시 채번·hard-delete 무재사용·forge 차단 하네스.

RLS 검증표(25-RESEARCH.md §RLS 검증표) — 익명 멤버 통과 기대값:
- places INSERT(add_manual_place): editor 통과 · votes upsert: user_id=auth.uid+can_vote 통과 · trip_messages: can_read/can_vote 통과 · 실시간: 구독자 JWT RLS 재평가(비멤버 0건).
전제: Plan 01의 0029 로컬 적용 완료(supabase db reset). colima+docker 가동 필요(프로젝트 메모리).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: web_share_smoke.sh — 익명 세션 join 후 RLS 통과 프로브 (SHARE-03)</name>
  <read_first>supabase/tests/web_share_smoke.sh (전체 — 기존 익명 signup/join_moa 스텝) · 25-RESEARCH.md §RLS 검증표.</read_first>
  <files>supabase/tests/web_share_smoke.sh</files>
  <action>
기존 `web_share_smoke.sh`에 익명-세션 게스트 케이스 append(기존 스텝 무수정): (a) `signInAnonymously`로 익명 세션 발급(is_anonymous=true·role=authenticated 재확인), (b) join **전** places/votes/trip_messages direct-read가 RLS로 0건(Pitfall 1 실증), (c) `join_moa(slug)` 후 places INSERT(add_manual_place editor)·votes upsert·trip_messages INSERT가 통과, (d) 익명 세션이 서버파생 `cast_date_vote_authed`(Plan 01)로 날짜투표 저장·device_token=auth.uid 확인. 각 단언 실패 시 non-zero exit.
  </action>
  <verify>
    <automated>bash supabase/tests/web_share_smoke.sh; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "signInAnonymously\|is_anonymous" supabase/tests/web_share_smoke.sh`
    - `grep -q "join_moa" supabase/tests/web_share_smoke.sh`
    - `grep -q "cast_date_vote_authed" supabase/tests/web_share_smoke.sh`
    - `bash supabase/tests/web_share_smoke.sh` exit 0
  </acceptance_criteria>
  <done>익명 세션 join 전 0건·join 후 places/votes/trip_messages·cast_date_vote_authed 통과 실증·exit 0.</done>
</task>

<task type="auto">
  <name>Task 2: realtime_events_smoke.mjs — 게스트 익명 fan-out + place_seq #N+1 (SHARE-04)</name>
  <read_first>supabase/tests/realtime_events_smoke.mjs · supabase/tests/place_seq_concurrency.sh (전체).</read_first>
  <files>supabase/tests/realtime_events_smoke.mjs, supabase/tests/place_seq_concurrency.sh</files>
  <action>
`realtime_events_smoke.mjs`에 게스트 익명 케이스 append: 익명 세션 join_moa 후 places INSERT가 호스트 구독(moa 채널)으로 fan-out 수신 ≥1 AND **비멤버 익명 세션**은 동일 INSERT 0건 수신(WALRUS RLS 재평가). 24-01 콜드스타트 2회 재시도 하드닝 패턴 재사용.

`place_seq_concurrency.sh`에 게스트 케이스 append: 기존 N개 장소가 있는 trip에 익명(게스트) 세션이 add_manual_place → seq_no = #N+1(이어짐·중복/결번 0). 서버 채번(0024 트리거)이라 forge 불가 재확인.
  </action>
  <verify>
    <automated>node supabase/tests/realtime_events_smoke.mjs; echo "rt=$?"; bash supabase/tests/place_seq_concurrency.sh; echo "seq=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qi "anon\|익명\|guest" supabase/tests/realtime_events_smoke.mjs`
    - `grep -qi "anon\|익명\|guest" supabase/tests/place_seq_concurrency.sh`
    - `node supabase/tests/realtime_events_smoke.mjs` exit 0 (게스트 fan-out ≥1·비멤버 0건)
    - `bash supabase/tests/place_seq_concurrency.sh` exit 0 (#N+1 이어짐)
  </acceptance_criteria>
  <done>게스트 익명 INSERT fan-out·비멤버 0건·#N+1 순번 실증·exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| 익명 세션 → 멤버 테이블 write + realtime | join 후 게스트 기여가 호스트 구독으로 전파 |
| 비멤버 익명 세션 → realtime 구독 | 미참여자의 데이터 유출 시도 |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-15 | Information Disclosure | 비멤버 익명이 realtime로 타 trip 데이터 수신 | mitigate | realtime_events_smoke가 비멤버 익명 0건 수신을 exit code로 실증(WALRUS 구독자 RLS 재평가) |
| T-25-16 | Tampering | 게스트가 순번(seq_no) 위조/충돌 유발 | mitigate | place_seq_concurrency가 서버 채번 #N+1·forge 불가 실증(0024 advisory-lock 트리거) |
| T-25-17 | Elevation of Privilege | 비멤버 익명이 join 없이 write | mitigate | web_share_smoke가 join 전 0건·join 후만 통과를 실증(can_*_trip DEFINER 헬퍼) |
</threat_model>

<verification>
- `bash web_share_smoke.sh` / `node realtime_events_smoke.mjs` / `bash place_seq_concurrency.sh` 전부 exit 0
- 기존 스텝 무수정(append-only 확장) · colima+docker 가동 전제 · iOS 무접촉
</verification>

<success_criteria>
익명 세션이 join 후에만 places/votes/trip_messages를 RLS 통과로 write하고, 게스트 기여가 moa 채널로 fan-out되며 비멤버는 0건 수신하고, 게스트 추가 장소가 #N+1 순번을 받는다 — 전 스모크 exit 0.
</success_criteria>

<output>
완료 후 `.planning/phases/25-guest-unified-share/25-05-SUMMARY.md` 생성.
</output>
