---
phase: 15-llm-vibe-llm-vibe-vibe-core-resolver
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/core/src/category.ts
  - packages/core/src/category.test.ts
  - packages/core/src/index.ts
autonomous: true
requirements: [EXTRACT-12]
must_haves:
  truths:
    - "Calling placeVibe('food') returns 'food' (exact vibe key passthrough)"
    - "Calling placeVibe('ramen_restaurant') returns 'food' (Google primaryType substring)"
    - "Calling placeVibe('coffee_shop') returns 'cafe' (cafe is now its own bucket, not food)"
    - "Calling placeVibe('onsen') or placeVibe('lodging') returns 'other' (wellness/lodging collapse to other)"
    - "Calling placeVibe(null) returns 'other'"
    - "VIBE_META exposes a shared color hex for each of the 6 canonical vibes"
  artifacts:
    - path: "packages/core/src/category.ts"
      provides: "placeVibe resolver + Vibe type + VIBE_META (color hex per vibe)"
      exports: ["Vibe", "placeVibe", "VIBE_META"]
      contains: "export function placeVibe"
    - path: "packages/core/src/category.test.ts"
      provides: "Unit tests for both vibe-key and Google-type inputs"
      contains: "placeVibe"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/category.ts"
      via: "barrel re-export"
      pattern: "export .* category"
---

<objective>
Create the single canonical vibe resolver in `@moajoa/core` (D2). `placeVibe(category)` maps BOTH exact vibe keys AND Google Places primaryType substrings to one of 6 canonical vibes: food / cafe / nature / culture / shopping / other (D1). Also export the `Vibe` type and `VIBE_META` (shared color hex per vibe — color is a token, sharable; icons stay per-client).

Purpose: Eliminate the duplicated, vibe-key-blind mappers in iOS (`vibeOf`) and web (`categoryVisual`). This plan ships the contract that Plan 15-03 (client swap) and the Edge insert (15-02) normalize against.
Output: `packages/core/src/category.ts` + tests, re-exported from the core barrel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-CONTEXT.md

<interfaces>
<!-- Existing iOS mapper (apps/ios/lib/category.ts) — the resolver logic to consolidate.
     NOTE its taxonomy differs from the new canonical one: it has `wellness` (removed) and
     lacks `cafe` (new). The new core taxonomy is the 6 keys below. -->

New canonical taxonomy (D1): 'food' | 'cafe' | 'nature' | 'culture' | 'shopping' | 'other'
  - bar / izakaya / pub → food
  - cafe / coffee / bakery / dessert / tea → cafe   (NEW bucket, split out of food)
  - wellness / spa / gym / onsen / hot_spring / lodging / hotel → other   (collapsed)

Existing iOS substring rules to migrate (apps/ios/lib/category.ts), re-bucketed for the new taxonomy:
  food:    ['restaurant', 'bar', 'food', 'meal', 'ramen', 'izakaya', 'pub']
  cafe:    ['cafe', 'coffee', 'bakery', 'dessert', 'tea']
  nature:  ['park', 'natural', 'beach', 'mountain', 'zoo', 'aquarium', 'garden', 'forest', 'hiking']
  culture: ['tourist', 'museum', 'art', 'temple', 'shrine', 'church', 'landmark', 'historical', 'castle', 'tower', 'attraction']
  shopping:['store', 'shop', 'mall', 'market', 'boutique', 'department']
  (everything else, incl. spa/gym/onsen/hot_spring/lodging/hotel/wellness/health) → other

Existing color hexes (reuse where the vibe survives; pick new hex for cafe):
  food #FF8F00 · nature #43A047 · culture #5C6BC0 · shopping #8D6E63 · other #2979FF
  cafe — choose a coffee/amber hex distinct from food (e.g. #8D5A2B); document the choice.

Core barrel (packages/core/src/index.ts):
  export * from './schemas/index';
  export * from './types/index';
  export * from './constants';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing tests for placeVibe (RED)</name>
  <files>packages/core/src/category.test.ts</files>
  <behavior>
    Use the same test runner already used by packages/core (check packages/core/package.json scripts + any *.test.ts in the repo for the harness — likely jest or vitest; match the existing pattern, do not introduce a new runner). Assert:
    - placeVibe('food') === 'food', placeVibe('cafe') === 'cafe', etc. (all 6 exact keys pass through)
    - placeVibe('ramen_restaurant') === 'food'
    - placeVibe('coffee_shop') === 'cafe'  (cafe split — NOT food)
    - placeVibe('bakery') === 'cafe'
    - placeVibe('bar') === 'food'  (bar collapses to food per D1)
    - placeVibe('tourist_attraction') === 'culture'
    - placeVibe('shopping_mall') === 'shopping'
    - placeVibe('national_park') === 'nature'
    - placeVibe('onsen') === 'other', placeVibe('lodging') === 'other', placeVibe('spa') === 'other'
    - placeVibe(null) === 'other', placeVibe('') === 'other', placeVibe(undefined) === 'other'
    - placeVibe is case-insensitive: placeVibe('RAMEN_RESTAURANT') === 'food'
    - VIBE_META has an entry for all 6 vibes, each with a non-empty `color` hex string (/^#[0-9A-Fa-f]{6}$/)
  </behavior>
  <action>Create the test file importing { placeVibe, VIBE_META, type Vibe } from './category'. Write the assertions above. Run the suite — it MUST fail (module does not exist yet). This is the RED commit.</action>
  <verify>
    <automated>cd packages/core && (pnpm test 2>&1 || npx vitest run 2>&1 || npx jest 2>&1) | tail -20</automated>
  </verify>
  <done>Test file exists, references placeVibe/VIBE_META/Vibe, and the run fails because category.ts is absent. Commit: `test(15-01): add failing tests for placeVibe resolver`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement placeVibe + Vibe + VIBE_META, wire barrel (GREEN)</name>
  <files>packages/core/src/category.ts, packages/core/src/index.ts</files>
  <action>
    Create `packages/core/src/category.ts`:
    - `export type Vibe = 'food' | 'cafe' | 'nature' | 'culture' | 'shopping' | 'other';` (D1 — exactly these 6).
    - A `Set<Vibe>` of the 6 keys for the exact-key passthrough branch.
    - An ordered `Array<[Vibe, string[]]>` of substring rules using the re-bucketed needles in <interfaces> (cafe rule must precede food so 'coffee'/'bakery'/'cafe' don't get swallowed; nature/culture/shopping after). First hit wins.
    - `export function placeVibe(category: string | null | undefined): Vibe` — lowercase input; if it exactly equals one of the 6 keys return it; else substring-scan rules; else 'other'. Per D2 (recognizes both vibe keys AND Google primaryType substrings) and D3 (column stores primaryType ?? vibe, so both shapes arrive).
    - `export const VIBE_META: Record<Vibe, { color: string; labelKo: string }>` — color hex per <interfaces> (reuse existing hexes for surviving vibes; pick a documented cafe hex e.g. #8D5A2B). labelKo: food 맛집 / cafe 카페 / nature 자연 / culture 명소 / shopping 쇼핑 / other 기타 (D1 Korean labels). Add a `// why` comment: color is a shared token (sharable), icons stay per-client because Ionicons ≠ lucide.
    Then add `export * from './category';` to `packages/core/src/index.ts` (append after the existing three re-exports). Do NOT use a `.js` extension on the import (CLAUDE.md §4.5 / Turbopack).
    Run tests — MUST pass.
  </action>
  <verify>
    <automated>cd packages/core && (pnpm test 2>&1 || npx vitest run 2>&1 || npx jest 2>&1) | tail -15 && (pnpm typecheck 2>&1 || npx tsc --noEmit 2>&1) | tail -5</automated>
  </verify>
  <done>All Task-1 assertions pass; `@moajoa/core` re-exports placeVibe/VIBE_META/Vibe; typecheck clean. Commit: `feat(15-01): placeVibe resolver + Vibe + VIBE_META in core`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored `places.category` → placeVibe | Mixed Google-type / vibe-key strings from DB flow into a pure mapper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | placeVibe input | accept | Pure string→enum map with an 'other' default; no eval, no I/O, no injection surface. Worst case is a wrong color (low-stakes per D5). |
| T-15-02 | Information disclosure | VIBE_META | accept | Only static color hex + Korean labels; no secrets, no PII. |
</threat_model>

<verification>
- `placeVibe` returns one of the 6 canonical keys for every input class (vibe key, Google type, null/empty).
- `cafe` is a distinct bucket from `food`; `wellness`/`onsen`/`lodging` resolve to `other` (taxonomy migrated from old iOS 6-vibe set).
- Core barrel re-exports the three new symbols; `pnpm typecheck` clean in packages/core.
</verification>

<success_criteria>
- All unit tests pass (both vibe-key and Google-type input classes covered).
- `import { placeVibe, VIBE_META, type Vibe } from '@moajoa/core'` resolves.
- No `.js` extension on the new barrel import.
</success_criteria>

<output>
After completion, create `.planning/phases/15-llm-vibe-llm-vibe-vibe-core-resolver/15-01-SUMMARY.md`
</output>
