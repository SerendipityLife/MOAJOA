---
phase: 23-web-first-foundation
plan: 05
type: execute
wave: 3
depends_on: ["23-04"]
files_modified:
  - packages/core/src/constants.ts
  - packages/core/src/schemas/chat.ts
  - packages/core/src/schemas/chat.test.ts
  - packages/core/src/schemas/index.ts
  - packages/core/src/schemas/trip.ts
  - packages/core/src/schemas/trip.test.ts
  - packages/core/src/schemas/place.ts
  - packages/core/src/schemas/place.test.ts
autonomous: true
requirements: [MOA-01] # seq_no 계약 미러 + SHARE/CHAT 계약 seam 기반 (e2e는 Phase 24~26)
must_haves:
  truths:
    - "ShareMode 상수가 0025 CHECK와 문자 단위로 일치하고 테스트가 회귀를 가드한다"
    - "moaChannelName이 planChannelName/pollChannelName 미러이고 '화면당 단일 채널' 규약이 주석에 잠긴다"
    - "TripCreateDraftSchema가 온보딩 dates-optional 계약을 refine 2개로 강제한다"
    - "TripMessageSchema·PlaceSchema.seq_no가 0025/0024 컬럼과 1:1이다"
  artifacts:
    - path: packages/core/src/constants.ts
      provides: "ShareMode + ShareModeType + MOA_CHANNEL_PREFIX + moaChannelName"
      contains: "moaChannelName"
    - path: packages/core/src/schemas/chat.ts
      provides: "TripMessageSchema + TripMessageCreateSchema"
      contains: "TripMessageSchema"
    - path: packages/core/src/schemas/trip.ts
      provides: "TripCreateDraftSchema (+TripSchema share_mode/companion 미러)"
      contains: "TripCreateDraftSchema"
    - path: packages/core/src/schemas/place.ts
      provides: "PlaceSchema.seq_no (migration 0024 미러)"
      contains: "seq_no"
  key_links:
    - from: packages/core/src/constants.ts
      to: supabase/migrations/0025_web_share.sql
      via: "CHECK 값 문자 단위 잠금 + .toEqual 회귀 테스트"
      pattern: "'dates', 'places', 'both'"
    - from: packages/core/src/schemas/index.ts
      to: packages/core/src/schemas/chat.ts
      via: "barrel export"
      pattern: "export \\* from './chat'"
---

<objective>
웹·iOS·EF가 공유할 core 계약 seam을 잠근다: ShareMode enum(0025 CHECK 잠금) + moaChannelName(Phase 26 단일 채널) + TripCreateDraftSchema(온보딩 dates-optional) + TripSchema share_mode/companion 미러 + chat.ts 신규(TripMessageSchema) + PlaceSchema.seq_no. 전부 TDD (21-02 ledger 선례).

Purpose: Phase 24(온보딩)·25(join·찜)·26(채팅)이 import할 단일 출처. DB가 진실, core는 미러 (§4.2 짝지음 룰 — 0024/0025와 같은 phase).
Output: core 신규/수정 스키마 + 회귀 테스트, `pnpm --filter @moajoa/core test` 그린.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-web-first-foundation/23-RESEARCH.md
@.planning/phases/23-web-first-foundation/23-PATTERNS.md

<interfaces>
<!-- 0025 컬럼 (23-02 산출물) — chat.ts가 1:1 미러할 대상 -->
trip_messages: id uuid · trip_id uuid · user_id uuid · nickname text · body text (1~140 CHECK) · reply_to_place_id uuid nullable · created_at timestamptz
trips 추가 컬럼: share_mode text CHECK in ('dates','places','both') nullable · companion text (≤20) nullable
places 추가 컬럼(0024): seq_no int not null (트리거 채번, 클라이언트 forge 불가)

<!-- 기존 house style (PATTERNS 검증) -->
constants.ts L211~215: PLAN_CHANNEL_PREFIX = 'plan:' + planChannelName(tripId) — 미러 대상
constants.ts L55~56: TripVisibility = [...] as const + (typeof X)[number] — enum 쌍 idiom
schemas/ledger.ts: 신규 계약 파일 헤더("locked CHARACTER-FOR-CHARACTER to the 0022 CHECK") — 미러 대상
schemas/trip.ts L30~39: TripCreateSchema (dates 필수) — Draft는 별도 공존, 수정 금지 (iOS 동결)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ShareMode·moaChannelName 상수 + chat.ts 신규 스키마 (TDD)</name>
  <files>packages/core/src/constants.ts, packages/core/src/schemas/chat.ts, packages/core/src/schemas/chat.test.ts, packages/core/src/schemas/index.ts</files>
  <read_first>
    - packages/core/src/constants.ts (L55~56 enum 쌍 idiom, L211~238 채널 빌더 + pollChannelName 단일 채널 주석 — 미러 원본)
    - packages/core/src/schemas/ledger.ts (신규 계약 파일 헤더·Row 스키마 house style)
    - packages/core/src/schemas/ledger.test.ts (enum .toEqual 잠금 + UUID 픽스처 idiom)
    - packages/core/src/schemas/index.ts (barrel — 맨 끝 추가)
  </read_first>
  <behavior>
    - `expect(ShareMode).toEqual(['dates', 'places', 'both'])` — 0025 CHECK 문자 단위 회귀 가드 (순서 포함)
    - `moaChannelName('abc')` === `'moa:abc'`, `MOA_CHANNEL_PREFIX` === `'moa:'`
    - TripMessageSchema: 유효 픽스처(uuid v4 상수 재사용) parse 성공 / body 빈 문자열·141자 reject / user_id 비-uuid reject / reply_to_place_id null 허용
    - TripMessageCreateSchema: trip_id·nickname·body(+선택 reply_to_place_id)만 — id·user_id·created_at 없음 (서버/RLS 몫)
  </behavior>
  <action>
RED→GREEN 순서. 먼저 `packages/core/src/schemas/chat.test.ts`에 behavior의 케이스를 작성해 실패 확인 후 구현한다.

1. `constants.ts` — TripVisibility 쌍 idiom으로 추가 (0025 CHECK 문자 단위 일치 주석 필수, ledger.ts "locked CHARACTER-FOR-CHARACTER" 문구 미러):

```typescript
/** Phase 23 — trips.share_mode. Locked CHARACTER-FOR-CHARACTER to the 0025 CHECK. */
export const ShareMode = ['dates', 'places', 'both'] as const;
export type ShareModeType = (typeof ShareMode)[number];

/**
 * Phase 23 — Realtime channel for the unified moa share screen (Phase 26 consumer).
 * ONE channel per screen carries presence + message + vote + place_added —
 * never open two channels for one topic (Phase 19/20 lesson).
 */
export const MOA_CHANNEL_PREFIX = 'moa:';
export function moaChannelName(tripId: string): string {
  return `moa:${tripId}`;
}
```

2. `schemas/chat.ts` 신규 — ledger.ts 헤더 스타일(Phase 23 — 0025 trip_messages 계약, 소비처: Phase 26 채팅·@moajoa/api). D-A2(nickname 비정규화 = 발화 시점 스냅샷) 주석. **Phase 25 호출 계약 주석 필수** (RESEARCH Pitfall 4): "익명 세션 생성은 `signInAnonymously({ options: { data: { name: nickname } } })` — metadata 미주입 시 display_name='user'가 된다."

```typescript
export const TripMessageSchema = z.object({
  id: z.string().uuid(),
  trip_id: z.string().uuid(),
  user_id: z.string().uuid(),
  nickname: z.string().min(1),
  body: z.string().min(1).max(140), // 0025 CHECK: char_length between 1 and 140
  reply_to_place_id: z.string().uuid().nullable(), // 장소 hard delete 시 set null (칩만 소멸)
  created_at: z.string(),
});
export type TripMessage = z.infer<typeof TripMessageSchema>;

export const TripMessageCreateSchema = TripMessageSchema.pick({
  trip_id: true,
  nickname: true,
  body: true,
}).extend({ reply_to_place_id: z.string().uuid().nullable().optional() });
export type TripMessageCreate = z.infer<typeof TripMessageCreateSchema>;
```

3. `schemas/index.ts` 맨 끝에 `export * from './chat';` 한 줄 추가 (기존 나열 순서 무수정).
4. ShareMode `.toEqual` 가드와 moaChannelName 테스트도 chat.test.ts에 포함 (share/chat 도메인 응집 — 배치 결정 기록).
  </action>
  <acceptance_criteria>
    - `grep -q "export const ShareMode = \['dates', 'places', 'both'\] as const" packages/core/src/constants.ts` 매치 (0025 CHECK와 문자 단위 동일)
    - `grep -q "moa:\${tripId}" packages/core/src/constants.ts` 매치
    - `grep -q "export \* from './chat'" packages/core/src/schemas/index.ts` 매치 — `.js` 확장자 0 (§4.5)
    - chat.test.ts에 `toEqual(['dates', 'places', 'both'])` 존재
    - `pnpm --filter @moajoa/core test` exit 0 (신규 포함 전체 그린, 기존 143+ 무회귀)
    - chat.ts에 `signInAnonymously` 호출 계약 주석 존재 (`grep -q "signInAnonymously" packages/core/src/schemas/chat.ts`)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @moajoa/core test && grep -q "export \* from './chat'" packages/core/src/schemas/index.ts && grep -q "signInAnonymously" packages/core/src/schemas/chat.ts</automated>
  </verify>
  <done>ShareMode·moaChannelName·TripMessage 계약이 테스트 가드와 함께 잠김. Phase 25 익명 생성 호출 계약이 seam에 명문화됨.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TripCreateDraftSchema + TripSchema 미러 + PlaceSchema.seq_no (TDD)</name>
  <files>packages/core/src/schemas/trip.ts, packages/core/src/schemas/trip.test.ts, packages/core/src/schemas/place.ts, packages/core/src/schemas/place.test.ts</files>
  <read_first>
    - packages/core/src/schemas/trip.ts (L4~23 TripSchema 필드 순서·주석 스타일, L30~39 TripCreateSchema — 수정 금지 원본)
    - packages/core/src/schemas/trip.test.ts (기존 케이스 스타일 — Draft 케이스 append)
    - packages/core/src/schemas/place.ts (L59~67 confidence 주석 스타일 — migration 번호 명시 idiom, L15 board_id 레거시 드리프트 — **건드리지 않음**)
  </read_first>
  <behavior>
    - TripCreateDraftSchema: {title, city_code, start_date:null, end_date:null, companion:null} parse 성공 (미정 — ONBOARD-04)
    - {start_date:'2026-08-01', end_date:'2026-08-03'} 성공 / start만 null reject / end<start reject / companion 21자 reject
    - TripSchema: share_mode 'both'·null 허용, 'invalid' reject; companion null 허용
    - PlaceSchema: seq_no 양의 정수 성공 / 0·음수·소수 reject
  </behavior>
  <action>
RED→GREEN. 테스트 먼저.

1. `schemas/trip.ts` — 기존 TripCreateSchema **바로 아래**에 추가 (기존 스키마 무수정 — iOS 동결):

```typescript
// Phase 23 — 온보딩 4단계 draft (ONBOARD-03/04). dates optional: 미정 = 둘 다 null.
// TripCreateSchema(dates 필수, iOS)와 공존 — 웹 온보딩 전용 계약.
export const TripCreateDraftSchema = z
  .object({
    title: z.string().min(1).max(Limits.TripTitleMax),
    city_code: z.string().max(20),
    start_date: z.string().date().nullable(),
    end_date: z.string().date().nullable(),
    companion: z.string().max(20).nullable(), // 0025 trips.companion 미러 (자유 텍스트 — A3)
  })
  .refine((v) => (v.start_date === null) === (v.end_date === null), {
    message: 'dates must be both set or both null',
  })
  .refine((v) => v.start_date === null || v.end_date! >= v.start_date, {
    message: 'end_date must be >= start_date',
  });
export type TripCreateDraft = z.infer<typeof TripCreateDraftSchema>;
```

2. `TripSchema`에 0025 컬럼 미러 2필드 추가 (기존 필드 순서·주석 스타일 유지): `share_mode: z.enum(ShareMode).nullable()` (constants에서 import), `companion: z.string().max(20).nullable()`. 주석에 "migration 0025" 명시.
3. `schemas/place.ts` — confidence 주석 idiom 미러로 추가. board_id 드리프트는 **무수정** (Open Q4 — Surgical Changes):

```typescript
  /**
   * Trip-scoped permanent ordinal (#1, #2…) — MOA-01.
   * Source of truth: places.seq_no column (migration 0024) — advisory-lock
   * trigger assigns it; client-supplied values are ignored (forge-proof).
   * Never reassigned: survives soft-delete/restore and hard deletes.
   */
  seq_no: z.number().int().positive(),
```

4. `place.test.ts` 신규 — behavior의 seq_no 케이스 (유효 픽스처는 PlaceSchema 필수 필드 최소셋으로 구성). `trip.test.ts`에 Draft·share_mode 케이스 append.
  </action>
  <acceptance_criteria>
    - `grep -q "TripCreateDraftSchema" packages/core/src/schemas/trip.ts` 매치, 기존 `TripCreateSchema` 정의 diff 0 (`git diff` 확인 — refine 라인 포함 무수정)
    - `grep -q "seq_no: z.number().int().positive()" packages/core/src/schemas/place.ts` 매치, `board_id` 라인 diff 0
    - trip.test.ts에 `both set or both null` 관련 reject 케이스 존재, place.test.ts 존재
    - `pnpm --filter @moajoa/core test && pnpm --filter @moajoa/core typecheck` exit 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @moajoa/core test && pnpm --filter @moajoa/core typecheck && grep -q "TripCreateDraftSchema" packages/core/src/schemas/trip.ts && grep -q "seq_no" packages/core/src/schemas/place.ts</automated>
  </verify>
  <done>온보딩 draft·share_mode·seq_no 계약이 DB 미러 + 테스트 가드로 잠김. 기존 iOS 소비 스키마 무수정.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 외부 입력 → Zod 스키마 | 웹 온보딩 폼·채팅 입력이 이 계약을 통과해 DB로 감 (§4.5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-14 | Tampering | enum ↔ DB CHECK 드리프트 | mitigate | `.toEqual(['dates','places','both'])` 회귀 테스트 — 21-02 선례. 드리프트 = 테스트 red |
| T-23-15 | Tampering | body 길이·형식 우회 | mitigate | Zod min(1).max(140) = 0025 CHECK 이중화 (클라이언트 조기 거부 + DB 최종 방어) |
| T-23-01 | Tampering | seq_no 클라이언트 위조 | transfer | 계약 주석에 "client-supplied values are ignored" 명시 — 실방어는 0024 트리거 (23-01/23-04) |
</threat_model>

<verification>
- `pnpm --filter @moajoa/core test` 전체 그린 (기존 143+ 무회귀 + 신규)
- core typecheck exit 0, `.js` import 확장자 0
- 기존 TripCreateSchema·board_id 무수정 (git diff)
</verification>

<success_criteria>
- 성공 기준 1 후반부("재생성된 타입 위에서 core 테스트 그린")의 core 절반 완결
- Phase 24~26이 import할 계약 4종(ShareMode·moaChannelName·Draft·TripMessage) + seq_no 미러 잠김
</success_criteria>

<output>
완료 후 `.planning/phases/23-web-first-foundation/23-05-SUMMARY.md` 생성.
</output>
