---
phase: 29-chat-unification
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [CHAT-09]
files_modified:
  - apps/web/app/moa/[id]/_components/moa-island.tsx
  - apps/web/__tests__/moa-island.test.tsx
  - apps/web/app/t/[slug]/_components/guest-surface.tsx
  - apps/web/__tests__/guest-surface.test.tsx

must_haves:
  truths:
    - "게스트가 /t join 전 화면(places·dates·both 공통)에서 채팅 진입 어포던스(빈 상태 카피 + 입력창 + 보내기)를 본다"
    - "비회원은 메시지 목록을 못 읽고 empty-state('참여하면 지금까지의 대화를 볼 수 있어요')만 본다 (listTripMessages 미호출 — RLS SELECT 멤버 전용)"
    - "게스트가 채팅 입력창을 focus하거나 보내기를 누르면 투표/찜과 동일한 닉네임 게이트가 열린다"
    - "게이트 통과 후 게스트가 MoaIsland(hideHostControls)로 전환되며 [채팅] 탭에 착지한다"
    - "재방문 멤버·투표/찜 게이트 경로는 기존대로 [모으기] 탭에 착지한다 (무회귀)"
    - "호스트 /moa/[id]는 initialTab 미전달 = 기존 'moa' 기본 탭 (diff 0)"
  artifacts:
    - path: "apps/web/app/moa/[id]/_components/moa-island.tsx"
      provides: "additive optional initialTab prop → activeTab 초기값"
      contains: "initialTab"
    - path: "apps/web/app/t/[slug]/_components/guest-surface.tsx"
      provides: "비회원 채팅 teaser + openChatGate + pendingChatIntent → MoaIsland initialTab='chat'"
      contains: "참여하면 지금까지의 대화를 볼 수 있어요"
  key_links:
    - from: "guest-surface.tsx chatTeaser input/button"
      to: "openChatGate"
      via: "onFocus / onClick"
      pattern: "openChatGate"
    - from: "guest-surface.tsx pendingChatIntent"
      to: "MoaIsland initialTab"
      via: "initialTab={pendingChatIntent ? 'chat' : undefined}"
      pattern: "initialTab="
    - from: "moa-island.tsx initialTab prop"
      to: "activeTab useState"
      via: "useState<MoaTab>(initialTab ?? 'moa')"
      pattern: "useState<MoaTab>\\(initialTab"
---

<objective>
게스트 `/t/[slug]` join 전 화면에 **채팅 진입 어포던스**를 추가한다 (신규 사용자 요청, 2026-07-15, CHAT-09). 전 share_mode(places·dates·both) 공통. 비회원은 `trip_messages` RLS SELECT 불가(멤버 전용)라 메시지 목록을 못 읽고 empty-state + 입력창만 본다. 입력 시도(focus 또는 보내기) 시 투표/찜과 동일한 닉네임 게이트(`ensureGuestMember`)를 태우고, 통과 후 join → MoaIsland(hideHostControls) 전환 + **[채팅] 탭 착지**로 채팅하려던 맥락을 보존한다.

Purpose: UAT Gap `truth: "게스트 /t 뷰(join 전)에 채팅 진입 어포던스가 있고, 입력 시도 시 투표와 동일하게 닉네임 게이트→앱쉘 채팅탭으로 이동한다"` (status: new_request, planned_by: 29-06) 종결. 현재 /t join 전 뷰엔 상시 채팅 섹션이 없다 (상시 채팅은 /poll에만 — UI-SPEC A-2). 이 요청이 그 규칙을 /t로 확장한다 (A-2 대체).

Output:
- `MoaIsland`에 additive optional `initialTab` prop (미전달 = 기존 'moa' 기본, 무회귀)
- `guest-surface.tsx` 비회원 채팅 teaser + chat-intent 게이트 브리지
- 회귀 커버리지 (moa-island + guest-surface 테스트)

**신규 컴포넌트 0** — poll-guest-island의 empty-state 채팅 어포던스 패턴 + 기존 `ensureGuestMember` + `MoaIsland` + `NicknameGateSheet` 재사용.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/29-chat-unification/29-HUMAN-UAT.md
@.planning/phases/29-chat-unification/29-CONTEXT.md
@.planning/phases/29-chat-unification/29-UI-SPEC.md

<interfaces>
<!-- 실행자가 탐색 없이 쓸 계약. 코드베이스에서 추출됨. -->

MoaTab (apps/web/app/moa/[id]/_components/moa-tab-bar.tsx:22) — moa-island.tsx이 이미 import:
```typescript
export type MoaTab = 'moa' | 'chat';
```

MoaIsland activeTab 초기화 지점 (apps/web/app/moa/[id]/_components/moa-island.tsx:129, import는 :43):
```typescript
import { MoaTabBar, type MoaTab } from './moa-tab-bar';
// ...
const [activeTab, setActiveTab] = useState<MoaTab>('moa');   // ← initialTab ?? 'moa'로 교체
```

MoaIsland 채팅 뷰 wrapper (apps/web/app/moa/[id]/_components/moa-island.tsx:665-682) — activeTab==='chat'일 때 `fixed inset-0 ... ` (아니면 `hidden`). initialTab='chat'이면 첫 렌더에 이 wrapper가 visible.

guest-surface 게이트/브리지 (apps/web/app/t/[slug]/_components/guest-surface.tsx):
```typescript
const [gateOpen, setGateOpen] = useState(false);
const pendingAction = useRef<(() => void) | null>(null);
function openGate(action?: () => void) { pendingAction.current = action ?? null; setGateOpen(true); }        // :227 (찜)
function requireMember(): Promise<{ uid; nickname }> { /* :233 poll 투표 게이트 */ }
async function handleConfirmNickname(nick: string) { /* :243 ensureGuestMember→hydrate→setJoined(true) */ }
function handleCloseGate() { /* :267 게이트 취소 */ }
const gate = ( <NicknameGateSheet open={gateOpen} onConfirm={handleConfirmNickname} onClose={handleCloseGate} /> );  // :275
// 세 return 분기: dates :334 / both :362 / places :397
```

poll-guest-island 비회원 empty-state 어포던스 (apps/web/app/poll/[code]/_components/poll-guest-island.tsx:252-300) — ANALOG. 이식 대상 카피/시각:
- 섹션: `<section className="mt-8 border-t border-neutral-200 pt-6">` + `<h3 className="mb-3 text-sm font-semibold text-neutral-700">채팅</h3>`
- empty-state: `<p className="... py-10 text-center text-sm text-neutral-400">참여하면 지금까지의 대화를 볼 수 있어요</p>`
- compose row: `<input ... placeholder="메시지 남기기" ...>` + `<button ...>보내기</button>` (같은 토큰 클래스)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoaIsland additive optional initialTab prop</name>
  <files>apps/web/app/moa/[id]/_components/moa-island.tsx, apps/web/__tests__/moa-island.test.tsx</files>

  <read_first>
    - apps/web/app/moa/[id]/_components/moa-island.tsx:43 (MoaTab import — 이미 존재), :45-71 (MoaIslandProps), :85-101 (destructure), :129 (activeTab useState), :665-682 (chat 뷰 wrapper)
    - apps/web/__tests__/moa-island.test.tsx:222-226 (MoaTabBar mock — tab-모으기/tab-채팅 버튼), :516-524 (ANALOG: chatWrapper = moa-chat.parentElement.parentElement, `.className` not contain 'hidden')
  </read_first>

  <behavior>
    - Test A (초기 탭 착지): `render(<MoaIsland {...baseProps} initialTab="chat" />)` → 탭 클릭 없이 chatWrapper(`screen.getByTestId('moa-chat').parentElement!.parentElement!`)의 className에 'hidden' 미포함 (채팅 뷰가 첫 렌더에 visible).
    - Test B (무회귀 기본값): `render(<MoaIsland {...baseProps} />)` (initialTab 미전달) → chatWrapper className에 'hidden' 포함 (기존 'moa' 기본 탭 유지). Test 12(탭 전환) 등 기존 케이스 전부 그린 유지.
  </behavior>

  <action>
    1. `MoaIslandProps`(moa-island.tsx :45-71)에 additive optional prop 추가 — `moas?` 아래 위치:
       ```typescript
       /** 게스트가 채팅 진입 어포던스로 join한 경우 [채팅] 탭에 착지(CHAT-09). 미전달=기존 'moa' 기본(호스트 /moa 무회귀). */
       initialTab?: MoaTab;
       ```
       `MoaTab`은 이미 :43에서 import됨 — 신규 import 금지.
    2. destructure(:85-101 함수 시그니처)에 `initialTab` 추가.
    3. :129 `const [activeTab, setActiveTab] = useState<MoaTab>('moa');` → `useState<MoaTab>(initialTab ?? 'moa');`로 교체. **이 한 줄만 변경** — 나머지 activeTab 로직·탭 전환·채널·wrapper 무변경 (surgical §3.3).
    4. moa-island.test.tsx에 Test A/B 추가 (기존 describe 블록 말미). 기존 케이스 무수정.
  </action>

  <acceptance_criteria>
    - `grep -n "initialTab?: MoaTab" apps/web/app/moa/[id]/_components/moa-island.tsx` → 1건
    - `grep -n "useState<MoaTab>(initialTab" apps/web/app/moa/[id]/_components/moa-island.tsx` → 1건
    - `grep -c "import.*MoaTab" apps/web/app/moa/[id]/_components/moa-island.tsx` → 1 (신규 import 추가 안 됨)
    - `grep -rn "initialTab" apps/web/app/moa/[id]/page.tsx` → 0건 (호스트 caller 미전달 = diff 0)
    - `CI=true pnpm --filter web test -- moa-island` exit 0 (Test A/B 포함 전 케이스 그린)
  </acceptance_criteria>

  <verify>
    <automated>CI=true pnpm --filter web test -- moa-island</automated>
  </verify>

  <done>initialTab prop이 additive optional로 추가되고, initialTab="chat"이면 첫 렌더에 채팅 뷰 visible, 미전달이면 기존 'moa' 기본 유지. 호스트 page.tsx diff 0. moa-island 테스트 전 케이스 그린.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: guest-surface 비회원 채팅 teaser + chat-intent 게이트 → MoaIsland 채팅탭 착지</name>
  <files>apps/web/app/t/[slug]/_components/guest-surface.tsx, apps/web/__tests__/guest-surface.test.tsx</files>

  <read_first>
    - apps/web/app/t/[slug]/_components/guest-surface.tsx:83-95 (state·refs), :226-241 (openGate/requireMember), :243-273 (handleConfirmNickname/handleCloseGate), :275-277 (gate 공유 element), :304-331 (readOnlyPlaces), :334-360 (dates 분기), :362-395 (both 분기), :397-403 (places 분기)
    - apps/web/app/poll/[code]/_components/poll-guest-island.tsx:252-300 (ANALOG — 비회원 empty-state 채팅 섹션 카피·시각. /t는 join 후 MoaIsland가 실 compose를 소유하므로 여기 input은 **게이트 트리거**일 뿐 — draft state 불필요)
    - apps/web/__tests__/guest-surface.test.tsx:79-98 (MoaIsland mock — data-* 프롭 캡처), :224-250 (첫 찜→게이트→MoaIsland ANALOG), :361-383 (dates 첫 join ANALOG)
  </read_first>

  <behavior>
    - Test 1 (teaser 렌더 · places): places 비join → `screen.getByText('참여하면 지금까지의 대화를 볼 수 있어요')` + `screen.getByPlaceholderText('메시지 남기기')` 존재. `listTripMessages` 미호출(비회원 이력 미읽기).
    - Test 2 (teaser 렌더 · dates): dates 비join → 같은 teaser 카피·입력창 존재 (pollSection과 공존).
    - Test 3 (teaser 렌더 · both): both 비join → 같은 teaser 존재 (readOnlyPlaces와 공존).
    - Test 4 (보내기 → 게이트): places 비join에서 `보내기` 클릭 → `gate-sheet` testid 노출.
    - Test 5 (focus → 게이트): teaser 입력창 focus → `gate-sheet` 노출.
    - Test 6 (게이트 통과 → 채팅탭 착지): teaser 보내기→게이트→닉네임 '철수' 시작하기 → `signInAnonymously`+`joinMoa` 1회 → MoaIsland 마운트 + `data-initial-tab="chat"`.
    - Test 7 (무회귀 — 찜 게이트는 채팅탭 아님): places에서 `guest-vote-p1` 찜 클릭→게이트 통과 → MoaIsland `data-initial-tab`가 'chat' 아님(빈 문자열/undefined). 기존 찜·투표·재접속·both pollSlot 케이스 전부 그린 유지.
  </behavior>

  <action>
    1. **state 추가** (:83-89 근처): `const [pendingChatIntent, setPendingChatIntent] = useState(false);`
    2. **openChatGate 함수 추가** (openGate :227 근처):
       ```typescript
       /** 채팅 teaser 입력 시도(focus/보내기) — 게이트를 열고 join 후 MoaIsland를 채팅탭에 착지시킨다(CHAT-09). */
       function openChatGate() {
         setPendingChatIntent(true);
         setGateOpen(true);
       }
       ```
    3. **다른 게이트 진입점은 chat-intent를 끈다** (leak 방지): `openGate`(:227) 본문 시작과 `requireMember`(:233) 게이트 오픈 직전에 `setPendingChatIntent(false);` 추가. `handleCloseGate`(:267)에도 `setPendingChatIntent(false);` 추가. (handleConfirmNickname은 pendingChatIntent를 소비만 — 리셋 불필요, MoaIsland가 initialTab을 mount 시 1회 읽음.)
    4. **chatTeaser 공유 element 추가** (gate const :275 근처, gate처럼 분기 공통 재사용). poll-guest-island :252-296 카피·토큰 클래스 미러하되 **draft state 없이 게이트 트리거만**:
       ```tsx
       const chatTeaser = (
         <section className="mt-8 border-t border-neutral-200 pt-6">
           <h3 className="mb-3 text-sm font-semibold text-neutral-700">채팅</h3>
           <div className="flex flex-col">
             <p className="py-10 text-center text-sm text-neutral-400">
               참여하면 지금까지의 대화를 볼 수 있어요
             </p>
             <div className="mt-3 flex gap-2">
               <input
                 type="text"
                 readOnly
                 onFocus={openChatGate}
                 placeholder="메시지 남기기"
                 className="min-w-0 flex-1 rounded-lg border border-neutral-200 px-3 py-2.5 text-sm text-neutral-900 outline-none focus:border-brand-300"
               />
               <button
                 type="button"
                 onClick={openChatGate}
                 className="shrink-0 rounded-lg bg-brand-600 px-4 py-2.5 text-sm font-semibold text-white transition-colors hover:bg-brand-700"
               >
                 보내기
               </button>
             </div>
           </div>
         </section>
       );
       ```
       `readOnly` — 입력창은 실제 compose가 아니라 게이트 트리거다(join 후 MoaIsland가 실 compose 소유). readOnly로 모바일 키보드 대신 게이트 시트만 뜨게 한다. 신규 hex 0 (토큰 클래스만, HC-2).
    5. **teaser를 세 비join 분기에 렌더 + 세 joined MoaIsland에 initialTab 배선**:
       - **dates**(:338-359): joined 분기 `<MoaIsland ... />`에 `initialTab={pendingChatIntent ? 'chat' : undefined}` 추가. 비join 분기 `pollSection` → `<>{pollSection}{chatTeaser}</>`.
       - **both**(:366-393): joined MoaIsland에 `initialTab={pendingChatIntent ? 'chat' : undefined}` 추가. 비join 분기 fragment 말미(readOnlyPlaces 뒤)에 `{chatTeaser}` 추가.
       - **places**(:398-402): `{joined && moaSeed ? <MoaIsland {...moaSeed} hideHostControls initialTab={pendingChatIntent ? 'chat' : undefined} /> : <>{readOnlyPlaces}{chatTeaser}</>}`.
       기존 pollSlot·hideHostControls·hidePlaceAdd·게이트·hydrate·pollMeta fetch·pollSlot·비join 경로 무변경 (surgical §3.3 — 추가 라인만 사용자 요청에 직결).
    6. **guest-surface.test.tsx**:
       - MoaIsland mock(:79-98)에 `data-initial-tab={props.initialTab ?? ''}` 속성 추가 (`initialTab` prop 캡처).
       - Test 1~7 추가 (behavior 참조). 기존 케이스 무수정.
  </action>

  <acceptance_criteria>
    - `grep -n "참여하면 지금까지의 대화를 볼 수 있어요" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 1건
    - `grep -c "openChatGate" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 3건 이상 (정의 1 + onFocus 1 + onClick 1)
    - `grep -c "initialTab={pendingChatIntent" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 3건 (dates·both·places)
    - `grep -c "chatTeaser" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 4건 (정의 1 + 3분기 렌더)
    - `grep -n "readOnly" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 1건 (게이트 트리거 input)
    - 신규 hex 리터럴 0: `grep -nE "#[0-9a-fA-F]{6}" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 0건 (HC-2)
    - `.js` 워크스페이스 import 0: `grep -n "from '@moajoa/[a-z]*\.js'" apps/web/app/t/[slug]/_components/guest-surface.tsx` → 0건 (§4.5)
    - `CI=true pnpm --filter web test -- guest-surface` exit 0 (Test 1~7 + 기존 전 케이스 그린)
  </acceptance_criteria>

  <verify>
    <automated>CI=true pnpm --filter web test -- guest-surface</automated>
  </verify>

  <done>비회원 /t 화면(places·dates·both)에 채팅 teaser(empty-state 카피 + 입력창 + 보내기)가 렌더되고, focus 또는 보내기 시 닉네임 게이트가 열리며, 통과 후 MoaIsland가 `initialTab="chat"`으로 마운트되어 [채팅] 탭에 착지한다. 찜/투표 게이트·재접속은 기존대로 'moa' 탭 착지(무회귀). guest-surface 테스트 전 케이스 그린.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게스트 브라우저(익명/미인증) → Supabase RLS | 공유 링크 열람자는 신뢰 불가. `trip_messages` SELECT/INSERT는 멤버(`can_read_trip`/RLS)만 |
| 게스트 입력(닉네임) → `ensureGuestMember`(signInAnonymously + joinMoa) | 익명 세션 승격 경계 — 기존 재사용 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Information Disclosure | 비회원 채팅 teaser | mitigate | teaser는 empty-state만 렌더 — `listTripMessages`/hydrate를 join 전 **미호출**. 비회원은 메시지 목록·닉네임·PII 노출 0 (RLS SELECT member-only가 최종 방어, teaser는 그 앞에서 이력 요청 자체를 안 함). Task 2 Test 1이 미호출 검증 |
| T-29-06-02 | Spoofing / Elevation | 익명 join (openChatGate → ensureGuestMember → joinMoa) | accept | 신규 노출 표면 0 — 기존 `ensureGuestMember`(signInAnonymously+joinMoa, Phase 25 실증) 재사용. 게이트 트리거만 채팅 어포던스로 확장, 승격 메커니즘·RPC·RLS 무변경. 익명 세션도 real `auth.uid` 멤버 → trip_messages INSERT는 기존 voter/member RLS(0025)가 게이트 |
| T-29-06-03 | Tampering | MoaIsland initialTab prop | accept | 클라이언트 초기 탭 표시일 뿐 — 권한/데이터 접근에 무관. 조작해도 RLS가 실 send/read를 게이트. additive optional, 호스트 무회귀 |
</threat_model>

<verification>
- Task 1·2 자동 검증 (`CI=true pnpm --filter web test -- moa-island` / `-- guest-surface`) 전부 exit 0
- 전 web 스위트 무회귀: `CI=true pnpm --filter web test` exit 0
- 타입: `pnpm --filter web exec tsc --noEmit` exit 0
- 무회귀 앵커 (surgical §3.3):
  - `git diff --stat apps/web/app/moa/[id]/page.tsx` → 빈 diff (호스트 caller initialTab 미전달)
  - `git diff apps/ios packages/core packages/api supabase/migrations` → 빈 diff (iOS 동결 §5·마이그레이션 무변경·백엔드 로직 0 HC-4)
  - HC-7 은퇴 문자열 유지: `grep -rn "한마디" apps/web` → 0건 (회귀 없음 재확인)
- `.js` 워크스페이스 import 0 (§4.5)
</verification>

<success_criteria>
- 게스트가 /t(places·dates·both) join 전에 채팅 진입 어포던스(empty-state + 입력창)를 본다
- 비회원은 메시지 이력을 못 읽고 empty-state만 (listTripMessages 미호출)
- 입력창 focus 또는 보내기 → 닉네임 게이트(ensureGuestMember) → join → MoaIsland(hideHostControls) [채팅] 탭 착지
- 찜/투표 게이트·재접속 멤버는 기존대로 [모으기] 탭 착지 (무회귀)
- 호스트 /moa/[id]·iOS·마이그레이션·core/api diff 0
- 신규 컴포넌트 0 (poll-guest-island 패턴 + ensureGuestMember + MoaIsland + NicknameGateSheet 재사용)
</success_criteria>

<output>
After completion, create `.planning/phases/29-chat-unification/29-06-SUMMARY.md`
</output>
